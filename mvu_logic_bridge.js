
(() => {
  const __mvuBridgeRoot = typeof globalThis !== 'undefined' ? globalThis : window;
  const resolveSharedSkillMechanismRegistry = () => {
    if (__mvuBridgeRoot.__LWCS_SKILL_MECHANISM_REGISTRY__ && typeof __mvuBridgeRoot.__LWCS_SKILL_MECHANISM_REGISTRY__ === 'object') {
      return __mvuBridgeRoot.__LWCS_SKILL_MECHANISM_REGISTRY__;
    }
    try {
      if (window.parent && window.parent !== window && window.parent.__LWCS_SKILL_MECHANISM_REGISTRY__ && typeof window.parent.__LWCS_SKILL_MECHANISM_REGISTRY__ === 'object') {
        return window.parent.__LWCS_SKILL_MECHANISM_REGISTRY__;
      }
    } catch (error) {}
    try {
      if (window.top && window.top !== window && window.top.__LWCS_SKILL_MECHANISM_REGISTRY__ && typeof window.top.__LWCS_SKILL_MECHANISM_REGISTRY__ === 'object') {
        return window.top.__LWCS_SKILL_MECHANISM_REGISTRY__;
      }
    } catch (error) {}
    return null;
  };
  const __sharedSkillMechanismRegistry = resolveSharedSkillMechanismRegistry();
  if (!__mvuBridgeRoot.__LWCS_SKILL_MECHANISM_REGISTRY__ && __sharedSkillMechanismRegistry) {
    __mvuBridgeRoot.__LWCS_SKILL_MECHANISM_REGISTRY__ = __sharedSkillMechanismRegistry;
  }
  __mvuBridgeRoot.__LWCS_BRIDGE_REGISTRY_SOURCE__ = __sharedSkillMechanismRegistry ? 'shared' : 'bridge_fallback_pending';
  if (__mvuBridgeRoot.__MVU_LOGIC_BRIDGE_LOADED__ === true) {
    console.warn('[DragonUI] mvu_logic_bridge duplicate load skipped.');
    return;
  }
  __mvuBridgeRoot.__MVU_LOGIC_BRIDGE_LOADED__ = true;

    const detailModal = document.getElementById('detailModal');
    if (detailModal && detailModal.parentElement !== document.body) {
      document.body.appendChild(detailModal);
    }

    const modalPanel = detailModal ? (detailModal.querySelector('.mvu-modal-panel') || detailModal.querySelector('.modal-panel')) : null;
    const modalTitle = document.getElementById('modalTitle');
    const modalSubtitle = document.getElementById('modalSubtitle');
    const modalLevel = document.getElementById('modalLevel');
    const modalPath = document.getElementById('modalPath');
    const modalSummary = document.getElementById('modalSummary');
    const modalBody = document.getElementById('modalBody');
    const modalClose = document.getElementById('modalClose');

    let modalStack = [];
    function getModalRefs() {
      const currentDetailModal = document.getElementById('detailModal') || detailModal;
      const currentModalPanel = currentDetailModal
        ? (currentDetailModal.querySelector('.mvu-modal-panel') || currentDetailModal.querySelector('.modal-panel') || modalPanel)
        : modalPanel;
      return {
        detailModal: currentDetailModal,
        modalPanel: currentModalPanel,
        modalTitle: document.getElementById('modalTitle') || modalTitle,
        modalSubtitle: document.getElementById('modalSubtitle') || modalSubtitle,
        modalLevel: document.getElementById('modalLevel') || modalLevel,
        modalPath: document.getElementById('modalPath') || modalPath,
        modalSummary: document.getElementById('modalSummary') || modalSummary,
        modalBody: document.getElementById('modalBody') || modalBody,
        modalClose: document.getElementById('modalClose') || modalClose
      };
    }
    function getMobileShellBridge() {
      try {
        const bridge = window.__MVU_MOBILE_SHELL__;
        return bridge && typeof bridge.getModalHost === 'function' ? bridge : null;
      } catch (err) {
        return null;
      }
    }
    function resolveShellPreviewTitle(previewKey) {
      const key = String(previewKey || '').trim();
      if (key.startsWith('鎶€鑳借璁″彴锛?)) {
        try {
          const payload = JSON.parse(decodeURIComponent(key.slice('鎶€鑳借璁″彴锛?.length)));
          const scope = String(payload && payload.scope || '').trim();
          const label = String(payload && payload.label || '').trim();
          const scopeTitleMap = {
            姝﹂瓊铻嶅悎鎶€: '姝﹂瓊铻嶅悎鎶€璁捐',
            art: '鍔熸硶璁捐',
            鑷垱榄傛妧: '鑷垱榄傛妧璁捐',
            榄傛妧: '榄傛妧璁捐',
            soul_bone_skill: '榄傞鎶€鑳借璁?,
            blood_skill: '琛€鑴夋妧鑳借璁?,
            blood_passive: '琛€鑴夎鍔ㄨ璁?
          };
          return [scopeTitleMap[scope] || '鎶€鑳借璁?, label].filter(Boolean).join(' / ') || '鎶€鑳借璁?;
        } catch (err) {
          return '鎶€鑳借璁?;
        }
      }
      const previewTitleMap = {
        '\u89d2\u8272\u5207\u6362\u5668': '\u89d2\u8272',
        '\u751f\u547d\u56fe\u8c31\u8be6\u60c5\u9875': '\u8be6\u7ec6\u6863\u6848',
        '\u751f\u547d\u56fe\u8c31\u8be6\u7ec6\u9875': '\u8be6\u7ec6\u6863\u6848',
        '\u79c1\u5bc6\u6863\u6848\u8be6\u7ec6\u9875': '\u79c1\u5bc6\u6863\u6848',
        '\u6b66\u88c5\u5de5\u574a\u8be6\u60c5\u9875': '\u6b66\u88c5',
        '\u6b66\u88c5\u5de5\u574a\u8be6\u7ec6\u9875': '\u6b66\u88c5',
        '\u50a8\u7269\u4ed3\u5e93\u8be6\u60c5\u9875': '\u4ed3\u5e93',
        '\u50a8\u7269\u4ed3\u5e93\u8be6\u7ec6\u9875': '\u4ed3\u5e93',
        '\u793e\u4f1a\u6863\u6848\u8be6\u60c5\u9875': '\u793e\u4f1a',
        '\u793e\u4f1a\u6863\u6848\u8be6\u7ec6\u9875': '\u793e\u4f1a',
        '\u6240\u5c5e\u52bf\u529b\u8be6\u60c5\u9875': '\u52bf\u529b',
        '\u4eba\u7269\u5173\u7cfb\u8be6\u60c5\u9875': '\u5173\u7cfb',
        '\u60c5\u62a5\u5e93\u8be6\u60c5\u9875': '\u60c5\u62a5\u5e93',
        '\u7b2c\u4e00\u6b66\u9b42\u8be6\u60c5\u9875': '\u7b2c\u4e00\u6b66\u9b42',
        '\u7b2c\u4e00\u6b66\u9b42\u8be6\u7ec6\u9875': '\u7b2c\u4e00\u6b66\u9b42',
        '\u7b2c\u4e8c\u6b66\u9b42\u8be6\u60c5\u9875': '\u7b2c\u4e8c\u6b66\u9b42',
        '\u7b2c\u4e8c\u6b66\u9b42\u8be6\u7ec6\u9875': '\u7b2c\u4e8c\u6b66\u9b42',
        '\u8840\u8109\u5c01\u5370\u8be6\u60c5\u9875': '\u8840\u8109',
        '\u5168\u606f\u661f\u56fe\u4e3b\u753b\u5e03': '\u661f\u56fe',
        '\u5f53\u524d\u8282\u70b9\u8be6\u60c5': '\u5f53\u524d\u8282\u70b9',
        '\u56fe\u5c42\u63a7\u5236\u4e0e\u8dd1\u56fe': '\u8dd1\u56fe',
        '\u52a8\u6001\u5730\u70b9\u4e0e\u6269\u5c55\u8282\u70b9': '\u52a8\u6001\u5730\u70b9',
        '\u4e16\u754c\u72b6\u6001\u603b\u89c8': '\u4e16\u754c',
        '\u7f16\u5e74\u53f2\u6863\u6848': '\u7f16\u5e74',
        '\u62cd\u5356\u4e0e\u8b66\u62a5': '\u8b66\u62a5',
        '\u52bf\u529b\u77e9\u9635\u603b\u89c8': '\u52bf\u529b',
        '\u6211\u7684\u9635\u8425\u8be6\u60c5': '\u6211\u7684\u9635\u8425',
        '\u672c\u5730\u636e\u70b9\u8be6\u60c5': '\u672c\u5730\u636e\u70b9',
        '\u7cfb\u7edf\u64ad\u62a5\u4e0e\u65e5\u5fd7': '\u64ad\u62a5',
        '\u9b42\u73af\u751f\u6210\u786e\u8ba4': '\u9b42\u73af',
        '\u8bd5\u70bc\u4e0e\u60c5\u62a5': '\u60c5\u62a5',
        '\u8fd1\u671f\u89c1\u95fb': '\u89c1\u95fb',
        '\u602a\u7269\u56fe\u9274': '\u56fe\u9274',
        '\u4efb\u52a1\u754c\u9762': '\u4efb\u52a1',
        '\u6218\u6597\u7ec8\u7aef': '\u6218\u6597'
      };
      return previewTitleMap[key] || key || '\u8be6\u60c5';
    }
    function notifyShellPreviewChange(previewKey) {
      const bridge = getMobileShellBridge();
      if (!bridge || typeof bridge.onPreviewChange !== 'function') return;
      const key = String(previewKey || '').trim();
      if (!key) return;
      try {
        bridge.onPreviewChange({ previewKey: key, title: resolveShellPreviewTitle(key) });
      } catch (err) {}
    }
    function notifyShellPreviewClosed() {
      const bridge = getMobileShellBridge();
      if (!bridge || typeof bridge.onPreviewClosed !== 'function') return;
      try { bridge.onPreviewClosed(); } catch (err) {}
    }
    function resolveDetailModalDefaultParent() {
      return document.body || document.documentElement || null;
    }
    function isMobileShellModalActive(options = {}) {
      const bridge = getMobileShellBridge();
      if (!bridge || typeof bridge.isOpen !== 'function' || !bridge.isOpen()) return false;
      if (options && options.unifiedMode === false) return false;
      if (typeof bridge.isDetailActive === 'function' && !bridge.isDetailActive()) return false;
      const modalHost = bridge.getModalHost();
      return !!(modalHost && modalHost.isConnected);
    }
    function syncDetailModalHost(refs = getModalRefs(), options = {}) {
      const currentDetailModal = refs && refs.detailModal ? refs.detailModal : null;
      const currentModalPanel = refs && refs.modalPanel ? refs.modalPanel : null;
      if (!currentDetailModal) return false;
      const bridge = getMobileShellBridge();
      const useShellHost = isMobileShellModalActive(options);
      const shellHost = bridge && typeof bridge.getModalHost === 'function' ? bridge.getModalHost() : null;
      const targetParent = useShellHost && shellHost ? shellHost : resolveDetailModalDefaultParent();
      if (targetParent && currentDetailModal.parentElement !== targetParent) {
        targetParent.appendChild(currentDetailModal);
      }
      currentDetailModal.classList.toggle('mvu-modal-display-shell', !!useShellHost);
      if (currentModalPanel) currentModalPanel.classList.toggle('mvu-modal-display-shell', !!useShellHost);
      if (currentModalPanel) {
        if (useShellHost) {
          currentModalPanel.removeAttribute('role');
          currentModalPanel.removeAttribute('aria-modal');
          currentModalPanel.removeAttribute('aria-labelledby');
          currentModalPanel.removeAttribute('aria-describedby');
        } else {
          currentModalPanel.setAttribute('role', 'dialog');
          currentModalPanel.setAttribute('aria-modal', 'true');
          currentModalPanel.setAttribute('aria-labelledby', 'modalTitle');
        }
      }
      currentDetailModal.dataset.modalHost = useShellHost ? 'shell' : 'body';
      return !!useShellHost;
    }
    const BASE_CANVAS_WIDTH = 636;
    const CANVAS_WIDTH = 1060;

    const previewMap = {
      '瑙掕壊鍒囨崲鍣?: {
        title: '瑙掕壊娴忚 / 鍒囨崲寮圭獥',
        summary: '鏌ョ湅鍏ㄩ儴瑙掕壊鎽樿锛屽苟榛樿鑱氱劍褰撳墠鐜╁瑙掕壊銆?,
        fields: ['sd.char', 'char[*].is_player', 'char[*].灞炴€?绛夌骇', 'char[*].鐘舵€?浣嶇疆', 'char[*].绀句氦.涓昏韩浠?],
        duties: ['娴忚鎵€鏈夎鑹?, '鍒囨崲褰撳墠鏌ョ湅瑙掕壊', '楂樹寒鐜╁瑙掕壊涓庡叧閿?NPC'],
        actions: ['瑙掕壊鍗″垪琛?, '绛涢€夛細鐜╁ / 闃佃惀 / 瀛樻椿鐘舵€?, '鍒囨崲鍚庡埛鏂伴椤垫憳瑕?]
      },
      '閰嶇疆闈㈡澘': {
        title: '鐣岄潰閰嶇疆寮圭獥',
        summary: '绾?UI 灞傞厤缃紝涓嶇洿鎺ユ敼鍔?MVU 涓氬姟鏁版嵁銆?,
        fields: ['鏈湴鏄剧ず寮€鍏?, '鎽樿鏄鹃殣绛栫暐', '鍦板浘灞傜骇鏄剧ず鍋忓ソ'],
        duties: ['绠＄悊鍗＄墖鏄鹃殣', '鍒囨崲绱у噾/灞曞紑瑙嗗浘', '淇濈暀瑙嗚鍋忓ソ'],
        actions: ['鏄剧ず杩囨护', '瀛楀彿瀵嗗害', '涓婚鍒囨崲']
      },
      '鐢熷懡鍥捐氨璇︾粏椤?: {
        title: '璇︾粏妗ｆ',
        summary: '瑙掕壊鐘舵€併€佷慨涓恒€佸瑙備笌鎴愰暱璁板綍銆?,
        fields: ['activeChar.灞炴€?*', 'activeChar.鐘舵€?*', 'activeChar.灞炴€?鐘舵€佹晥鏋?, 'activeChar.榄傛牳.鏍稿績', 'activeChar.琛€鑴変箣鍔?鍩虹鎬?'],
        duties: ['灞曠ず瀹屾暣鐢熷懡浣撳緛', '闆嗕腑鏄剧ず鐘舵€?浼ゅ娍/棰嗗煙', '鏀剁撼鎴愰暱淇涓庨瓊鏍歌繘搴?],
        actions: ['鍒嗗尯鏄剧ず鍩虹灞炴€?, '寮傚父鐘舵€佸垪琛?, '鎭㈠涓庡垽瀹氳鏄?]
      },
      '绀句細妗ｆ璇︾粏椤?: {
        title: '绀句細妗ｆ寮圭獥',
        summary: '鐢扁€滃悕鏈涚瓑绾р€濊姱鐗囪繘鍏ワ紝鑱氱劍澹版湜銆佽韩浠戒笌绉板彿銆?,
        fields: ['activeChar.绀句氦.澹版湜', 'activeChar.绀句氦.鍚嶆湜绛夌骇', 'activeChar.绀句氦.涓昏韩浠?, 'activeChar.绀句氦.绉板彿'],
        duties: ['灞曠ず鍏紑韬唤', '鏄剧ず绉板彿鏉ユ簮', '鏁寸悊鍚嶆湜绛夌骇涓庡叕寮€鎯呮姤鐘舵€?],
        actions: ['鍒囨崲鍒板叧绯?闃佃惀/鎯呮姤瀛愰〉', '淇濈暀澹版湜鏉ユ簮璇存槑']
      },
      '鎵€灞炲娍鍔涜缁嗛〉': {
        title: '闃佃惀韬唤寮圭獥',
        summary: '鐢扁€滄墍灞炲娍鍔涒€濊姱鐗囪繘鍏ワ紝鐩存帴灞曠ず璇ヨ鑹插湪鍚勫娍鍔涗腑鐨勮韩浠姐€?,
        fields: ['activeChar.绀句氦.鍔垮姏'],
        duties: ['灞曠ず鎵€灞炲娍鍔涘垪琛?, '鏄剧ず韬唤涓庢潈闄愮骇', '鍦ㄥ娍鍔涚煩闃典腑楂樹寒褰撳墠褰掑睘'],
        actions: ['璺宠浆鍔垮姏鐭╅樀', '鏌ョ湅鏅嬪崌鍏ュ彛']
      },
      '浜虹墿鍏崇郴璇︾粏椤?: {
        title: '浜虹墿鍏崇郴寮圭獥',
        summary: '鐢扁€滃叧绯绘憳瑕佲€濊姱鐗囪繘鍏ワ紝鎵挎帴閲嶅瀷 绀句氦.鍏崇郴 缁撴瀯銆?,
        fields: ['activeChar.绀句氦.鍏崇郴'],
        duties: ['鏄剧ず濂芥劅搴︿笌闃舵', '鏄剧ず鍏崇郴璺嚎涓庢帹杩涙彁绀?, '灞曠ず瀵规柟韬唤銆佸ソ鎰熷姞鎴?涓庡垎鏋愮粨鏋?],
        actions: ['鏌ョ湅鍏崇郴璺嚎', '鏌ョ湅鍏崇郴鎺ㄨ繘閲嶇偣', '闂茶亰 / 閫佺ぜ / 璇锋暀 / 鍒囩 / 琛ㄧ櫧']
      },
      '鎯呮姤搴撹缁嗛〉': {
        title: '鎯呮姤搴撳脊绐?,
        summary: '鐢扁€滃凡瑙ｉ攣鎯呮姤鈥濊姱鐗囪繘鍏ワ紝缁熶竴灞曞紑宸茬煡淇℃伅銆佷换鍔¤褰曚笌鎴樻枟璁板綍銆?,
        fields: ['activeChar.宸叉帉鎻℃儏鎶?, 'activeChar.璁板綍', 'activeChar.鎴樻枟鍘嗗彶'],
        duties: ['灞曠ず宸茶В閿佹儏鎶?, '鑱氬悎浠诲姟璁板綍', '鑱氬悎杩戞湡璁板綍涓庢垬鏂楁憳瑕?],
        actions: ['鎸夋儏鎶ョ瓫閫?, '璺宠浆鍏宠仈鑺傜偣']
      },
      '姝﹁宸ュ潑璇︾粏椤?: {
        title: '姝﹁宸ュ潑寮圭獥',
        summary: '鏌ョ湅褰撳墠姝﹁銆佹枟閾犻儴浠朵笌鍓亴涓氬伐鍧娿€?,
        fields: ['activeChar.瑁呭.姝﹀櫒', 'activeChar.瑁呭.鏂楅摖', 'activeChar.瑁呭.鏈虹敳', 'activeChar.鑱屼笟'],
        duties: ['灞曠ず姝﹀櫒/鏂楅摖/鏈虹敳', '灞曠ず瑁呭妲戒綅鐘舵€?, '鏄剧ず鍓亴涓氱瓑绾т笌铻嶅悎淇℃伅'],
        actions: ['鎵撳紑鏂楅摖鎬昏', '鏌ョ湅妲戒綅瑕嗙洊', '娴忚瑁呭鎽樿']
      },
      '姝﹂瓊铻嶅悎鎶€璇︾粏椤?: {
        title: '姝﹂瓊铻嶅悎鎶€妗ｆ',
        summary: '闆嗕腑鏌ョ湅褰撳墠瑙掕壊宸插綍鍏ョ殑姝﹂瓊铻嶅悎鎶€銆佽嚜浣撹瀺鍚堜笌澶氫汉铻嶅悎淇℃伅銆?,
        fields: ['activeChar.姝﹂瓊铻嶅悎鎶€.*.铻嶅悎妯″紡', 'activeChar.姝﹂瓊铻嶅悎鎶€.*.铻嶅悎瀵硅薄', 'activeChar.姝﹂瓊铻嶅悎鎶€.*.鏉ユ簮姝﹂瓊', 'activeChar.姝﹂瓊铻嶅悎鎶€.*.铻嶅悎鍙備笌鑰?, 'activeChar.姝﹂瓊铻嶅悎鎶€.*.鎶€鑳芥暟鎹?],
        duties: ['灞曠ず铻嶅悎鎶€鎬昏', '鍖哄垎鑷綋铻嶅悎涓庡浜鸿瀺鍚?, '鐩存帴涓嬮捇鍒拌瀺鍚堟妧璁捐涓庢晥鏋滆鎯?],
        actions: ['鏌ョ湅铻嶅悎妯″紡', '鏌ョ湅鏉ユ簮姝﹂瓊', '鎵撳紑铻嶅悎鎶€璇︽儏']
      },
      '鍌ㄧ墿浠撳簱璇︾粏椤?: {
        title: '鍌ㄧ墿浠撳簱',
        summary: '鑱氬悎褰撳墠鑳屽寘涓庤揣甯佹暟鎹紝骞朵互浠撳簱寮忚鍥惧睍绀烘牳蹇冪墿璧勩€?,
        fields: ['activeChar.鑳屽寘', 'activeChar.璐㈠瘜'],
        duties: ['灞曠ず鐗╁搧鏁伴噺', '灞曠ず鍝佽川/鍝侀樁/鎻忚堪', '鏁村悎澶氱璐у竵涓庤祫浜ф憳瑕?],
        actions: ['鎸夊垎绫荤瓫閫?, '鏌ョ湅鐗╁搧璇︽儏', '鏁寸悊 / 浣跨敤閬撳叿']
      },
      '琛€鑴夊皝鍗拌缁嗛〉': {
        title: '琛€鑴夊皝鍗板脊绐?,
        summary: '鎵挎帴琛€鑴変綋绯荤殑鐘舵€佹ā鍧楋紝鍦?1 涓〉闈㈠唴姹囨€诲皝鍗板眰绾т笌褰撳墠鑳藉姏銆?,
        fields: ['activeChar.姝﹂瓊', 'activeChar.榄傞', 'activeChar.琛€鑴変箣鍔?鎶€鑳?, 'activeChar.琛€鑴変箣鍔?姘旇榄傜幆', 'activeChar.鍔熸硶 / 鑷垱榄傛妧'],
        duties: ['灞曠ず琛€鑴夋湰浣?, '灞曠ず灏佸嵃/榄傜幆/榄傛妧', '灞曠ず褰撳墠宸插浐鍖栬兘鍔?],
        actions: ['鎸夐〉绛炬煡鐪嬮瓊鐜?/ 鑳藉姏 / 榄傞 / 琛€鑴?, '鎮诞榄傜幆鏌ョ湅鎶€鑳借鏄?, '鏌ョ湅灏佸嵃灞傜骇']
      },
      '鍏ㄦ伅鏄熷浘涓荤敾甯?: {
        title: '鍏ㄦ伅鏄熷浘涓荤敾甯?,
        summary: '鏄熷浘椤垫湰韬氨鏄?1绾у湴鍥鹃〉锛涚偣鍑讳富鐢诲竷鎴栬妭鐐瑰悗锛岀洿鎺ヤ笅閽诲埌鑺傜偣璇︽儏寮圭獥銆?,
        fields: ['WORLD_MAP_TREE', 'sd.world.鍦扮偣', 'sd.world.鍔ㄦ€佸湴鐐?, 'activeChar.鐘舵€?浣嶇疆'],
        duties: ['鏄剧ず鍦板浘涓昏鍥?, '楂樹寒褰撳墠鑺傜偣', '椹卞姩鑺傜偣绾т笅閽?],
        actions: ['鐐瑰嚮鑺傜偣寮€璇︽儏', '鍒囨崲鍥惧眰鎺у埗', '鏌ョ湅璺戝浘涓庝氦鏄撳彲鐢ㄦ€?]
      },
      '褰撳墠鑺傜偣璇︽儏': {
        title: '褰撳墠鑺傜偣璇︽儏寮圭獥',
        summary: '鐢ㄤ簬鏌ョ湅褰撳墠浣嶇疆褰掍竴鍖栧悗鐨勫畬鏁磋妭鐐逛俊鎭€?,
        fields: ['activeChar.鐘舵€?浣嶇疆', 'sd.world.鍦扮偣[normalizedLoc]', 'WORLD_MAP_TREE 褰撳墠鑺傜偣', '鍟嗗簵'],
        duties: ['灞曠ず鎺屾帶鍔垮姏/浜哄彛/缁忔祹', '鏄剧ず鏈湴鍟嗗簵鎽樿', '鏄剧ず鐖惰妭鐐逛笌瀛愯妭鐐?],
        actions: ['璺宠浆鍟嗗簵寮圭獥', '璺宠浆鍦板浘灞傜骇', '鏌ョ湅鍔ㄦ€佸湴鐐?]
      },
      '鍥惧眰鎺у埗涓庤窇鍥?: {
        title: '鍥惧眰鎺у埗 / 璺戝浘寮圭獥',
        summary: '鍦板浘椤电洿鎺ユ壙鎷呭湴鐐瑰垏鎹笌绉诲姩瑙勫垝锛屼笉鍐嶆樉绀虹敓纭殑绯荤粺璇存槑銆?,
        highlights: ['鍒囨崲澶ч檰 / 鍩庡競 / 璁炬柦灞傜骇', '鏌ョ湅鎺ㄨ崘绉诲姩鏂瑰紡', '纭鐩爣涓庨璁¤€楁椂'],
        duties: ['鏌ョ湅褰撳墠鐒︾偣涓庡眰绾?, '鏌ョ湅鍙敤绉诲姩鏂瑰紡', '纭鍑哄彂鍓嶇殑璺嚎寤鸿'],
        actions: ['鍒囨崲鍦板浘灞傜骇', '閫夋嫨鐩爣鍦扮偣', '纭鏄惁鍚▼']
      },
      '鍔ㄦ€佸湴鐐逛笌鎵╁睍鑺傜偣': {
        title: '鍔ㄦ€佸湴鐐瑰脊绐?,
        summary: '鎵挎帴 world.鍔ㄦ€佸湴鐐癸紝閬垮厤鎶婂姩鎬佽妭鐐圭‖濉炶繘鍦板浘棣栭〉銆?,
        fields: ['sd.world.鍔ㄦ€佸湴鐐?],
        duties: ['鍒楀嚭鍓ф儏鏂板鍦扮偣', '鏄剧ず褰掑睘鐖惰妭鐐?, '鐢ㄤ簬琛ュ湴鍥炬爲鐨勫姩鎬佹墿灞?],
        actions: ['鎸夌埗鑺傜偣绛涢€?, '璺冲埌鍦板浘鑺傜偣璇︽儏']
      },
      '鎷嶅崠涓庤鎶?: {
        title: '鎷嶅崠琛?/ 涓栫晫璀︽姤寮圭獥',
        summary: '鎷嶅崠鐘舵€併€佹媿鍝佷笌褰撳墠涓栫晫璀︽姤銆?,
        fields: ['sd.world.鎷嶅崠', 'sd.world.绱鍑绘潃骞撮檺'],
        duties: ['鏄剧ず鎷嶅崠鐘舵€佷笌鎷嶅搧', '鏄剧ず鐢熸€侀闄╀笌鍏芥疆鐩稿叧闃堝€?, '鏄剧ず涓嬫鍒锋柊鏃堕棿'],
        actions: ['鏌ョ湅鎷嶅崠娓呭崟', '璇勪及褰撳墠涓栫晫椋庨櫓']
      },
      '鍔垮姏鐭╅樀鎬昏': {
        title: '鍔垮姏鐭╅樀寮圭獥',
        summary: '婊¤冻鈥滄煡鐪嬫墍鏈夊娍鍔涒€濈殑鏍稿績鍏ュ彛锛屼笉鍐嶅彧鏄剧ず褰撳墠瑙掕壊瑙嗚銆?,
        fields: ['sd.org', 'activeChar.绀句氦.鍔垮姏'],
        duties: ['娴忚鎵€鏈夊娍鍔?, '鏄剧ず褰卞搷鍔涗笌搴曡暣', '楂樹寒褰撳墠瑙掕壊鎵€灞炲娍鍔?],
        actions: ['鎸夐樀钀ョ瓫閫?, '鏌ョ湅鍔垮姏妗ｆ', '鏌ョ湅鏈湴鎹偣涓庝氦鏄撳叧鑱?]
      },
      '鎴戠殑闃佃惀璇︽儏': {
        title: '鎴戠殑闃佃惀寮圭獥',
        summary: '鑱氱劍褰撳墠鏌ョ湅瑙掕壊鍦ㄥ悇鍔垮姏涓殑韬唤涓庢潈闄愩€?,
        fields: ['activeChar.绀句氦.鍔垮姏', 'activeChar.绀句氦.涓昏韩浠?, 'activeChar.绀句氦.绉板彿'],
        duties: ['灞曠ず鎵€灞炲娍鍔?, '灞曠ず韬唤涓庢潈闄愮骇', '琛ュ厖涓昏韩浠戒笌澶磋'],
        actions: ['璺宠浆鍔垮姏鐭╅樀', '鏌ョ湅鏅嬪崌鍏ュ彛']
      },
      '鏈湴鎹偣璇︽儏': {
        title: '鏈湴鎹偣寮圭獥',
        summary: '鍥寸粫褰撳墠鑺傜偣灞曠ず鎹偣绾т俊鎭紝鏄槦鍥句笌鍔垮姏椤电殑浜ゅ弶鍏ュ彛銆?,
        fields: ['activeChar.鐘舵€?浣嶇疆', 'sd.world.鍦扮偣[normalizedLoc]'],
        duties: ['鏄剧ず鎺屾帶鍔垮姏/浜哄彛/瀹堟姢鍐涘洟', '鏄剧ず缁忔祹鐘跺喌', '鏁寸悊鏈湴璁炬柦涓庡晢搴?],
        actions: ['鏌ョ湅鏈湴鍟嗗簵淇℃伅', '璺冲洖鍦板浘鑺傜偣璇︽儏']
      },
      '绯荤粺鎾姤涓庢棩蹇?: {
        title: '绯荤粺鎾姤 / 鏃ュ織寮圭獥',
        summary: '缁堢椤靛乏渚т富妯″潡锛岄泦涓睍绀虹郴缁熷箍鎾笌鏈€杩戞棩蹇椼€?,
        fields: ['sd.sys.绯荤粺鎾姤', 'sd.sys.鏈€杩戞瀹?, 'sd.sys.鏈€缁堟垚鍔熺巼', 'sd.world.鏃堕棿绾?],
        duties: ['鏄剧ず鏈€鏂版挱鎶?, '鏄剧ず鏈€杩戝叏灞€浜嬩欢', '鏄剧ず妫€瀹氱粨鏋?],
        actions: ['鎸夋椂闂村€掑簭', '淇濈暀鏃ュ織绛涢€変笌澶嶅埗鑳藉姏']
      },
      '鎿嶄綔鎬荤嚎': {
        title: '杩戞湡瀹夋帓',
        summary: '鎶婅繎鏈熻鍋氱殑浜嬫寜琛岀▼涓庡緟鍔為泦涓敹绾筹紝璁╅椤电洿鎺ョ湅鎳傛帴涓嬫潵瑕佸仛浠€涔堛€?,
        highlights: ['浠婃棩琛岀▼瀹夋帓', '鐪间笅鏈€璇ュ鐞嗙殑浜?, '鍔ㄨ韩鍓嶈繕缂轰粈涔?],
        duties: ['鏌ョ湅褰撳墠琛岀▼椤哄簭', '鏌ョ湅浼樺厛寰呭姙', '鏌ョ湅鏄惁閫傚悎绔嬪埢鍑哄彂'],
        actions: ['璋冩暣浠婃棩璺嚎', '鎸変紭鍏堢骇澶勭悊寰呭姙']
      },
      '璇曠偧涓庢儏鎶?: {
        title: '鎯呮姤',
        summary: '鎶婅瘯鐐煎叆鍙ｃ€佽繎鏈熺嚎绱㈠拰涓嬩竴姝ヨ皟鏌ラ噸鐐规斁鍦ㄥ悓涓€椤甸噷鐩存帴鐪嬨€?,
        highlights: ['褰撳墠鍙幓鐨勮瘯鐐?, '宸茬粡鎺屾彙鐨勬儏鎶?, '涓嬩竴姝ユ渶鍊煎緱璺熻繘鐨勭嚎绱?],
        duties: ['鏌ョ湅璇曠偧寮€鏀炬儏鍐?, '鏌ョ湅杩戞湡閲嶇偣鎯呮姤', '鏌ョ湅涓嬩竴姝ヨ鍔ㄥ缓璁?],
        actions: ['閫夋嫨鍏堝仛璇曠偧杩樻槸鍏堟煡绾跨储', '鏍规嵁娑堟伅璋冩暣杩滆椤哄簭']
      },
      '杩戞湡瑙侀椈': {
        title: '瑙侀椈',
        summary: '灏嗚繎鏈熷箍鎾€佸湴鐐瑰姩鎬佷笌鍙窡杩涚嚎绱㈤泦涓敹绾充簬姝ゃ€?,
        highlights: ['鏈€杩戝箍鎾笌鍔ㄦ€?, '鍦扮偣涓庤绋嬬浉鍏虫秷鎭?, '鍊煎緱璺熻繘鐨勭嚎绱?],
        duties: ['鏌ョ湅褰撳墠鏈€閲嶈鐨勮繎鏈熸秷鎭?, '鏌ョ湅鍦扮偣涓庝汉鐗╃殑鏈€鏂板彉鍖?, '鍒ゆ柇鍝簺鍔ㄩ潤鍊煎緱浼樺厛璺熻繘'],
        actions: ['鍓嶅線鐩稿叧鍦扮偣缁х画纭', '鏍规嵁娑堟伅璋冩暣浠婃棩璺嚎']
      },
      '鎬墿鍥鹃壌': {
        title: '鎬墿鍥鹃壌',
        summary: '宸查伃閬囩殑娣辨笂鐢熺墿涓庨瓊鍏芥爣鍑嗘暟鎹褰曘€?,
        fields: ['sd.world.鍥鹃壌'],
        duties: ['鏌ョ湅宸茶褰曟€墿', '澶嶇敤鐗╃鍩虹鏁版嵁', '鍥為【閬亣瀵硅薄'],
        actions: ['鎸夊悕绉版煡闃?, '鏌ョ湅宸茶褰曟潯鐩?]
      },
      '妫灄浠囨仺鍊?: {
        title: '妫灄浠囨仺鍊?,
        summary: '鏄熸枟澶ф．鏋楃疮璁″嚮鏉€榄傚吔骞撮檺涓庡吔娼槇鍊肩洃鎺с€?,
        fields: ['sd.world.绱鍑绘潃骞撮檺', 'sd.world.鍏芥疆宸茶Е鍙?],
        duties: ['鏌ョ湅绱鍑绘潃骞撮檺', '璇勪及璺濈鍏芥疆闃堝€?, '瑙傚療鏄惁瑙﹀彂鍏芥疆'],
        actions: ['鏌ョ湅闃堝€艰繘搴?, '璇勪及妫灄椋庨櫓']
      }
    };

    function normalizeModalItems(items) {
      return (items || []).map(item => String(item || '')
        .replace(/activeChar\.[^,锛孿s]+/g, '褰撳墠瑙掕壊淇℃伅')
        .replace(/sd\.[^,锛孿s]+/g, '涓栫晫鐘舵€?)
        .replace(/WORLD_MAP_TREE/g, '鍦板浘鑺傜偣')
        .replace(/travel[\s_]?request/gi, '鍑鸿瀹夋帓')
        .replace(/player_action/gi, '鐜╁琛屽姩')
        .replace(/settle_result/gi, '缁撶畻缁撴灉')

        .replace(/鎹愮尞璇锋眰/gi, '鎹愯禒瀹夋帓')
        .replace(/request 鍙橀噺/gi, '褰撳墠瀹夋帓')
        .replace(/灞曠ず/g, '鏌ョ湅')
        .replace(/鏄剧ず/g, '鏌ョ湅')
        .replace(/鏁寸悊/g, '鏀剁撼')
        .replace(/缁熶竴绠＄悊/g, '闆嗕腑鏌ョ湅')
        .replace(/鍐欏叆/g, '鐢熸垚')
        .replace(/璺宠浆/g, '鍓嶅線')
        .replace(/鎵挎帴/g, '鏌ョ湅')
        .replace(/瀛楁/g, '淇℃伅')
        .replace(/鍙橀噺/g, '鍐呭')
        .replace(/鐢ㄤ簬鏄剧ず/g, '灞曠ず')
        .replace(/杩涘叆杩欓噷/g, '鏀剁撼浜庢')
        .replace(/鍙綔涓?g, '鍙浣?)
        .replace(/棰勭暀/g, '')
        .replace(/鍚庣画/g, '')
        .replace(/杩愯鏃?g, '')
        .replace(/\(\)/g, '')
        .replace(/\s{2,}/g, ' ')
        .trim())
        .filter(Boolean);
    }

    function renderGenericModalBody(config) {
      const focusItems = normalizeModalItems(config.highlights && config.highlights.length ? config.highlights : config.duties).slice(0, 6);
      const actionItems = normalizeModalItems(config.actions).slice(0, 6);
      const renderPills = (items, fallback, mode = '') => {
        const source = items.length ? items : [fallback];
        return `<div class="status-tag-rack">${source.map(item => `<span class="status-pill ${mode}">${item}</span>`).join('')}</div>`;
      };
      return `
        <div class="modal-block">
          <div class="modal-block-title">褰撳墠瑕佺偣</div>
          ${renderPills(focusItems, '褰撳墠鏆傛棤棰濆瑕佺偣銆?)}
        </div>
        <div class="modal-block">
          <div class="modal-block-title">鍙仛浜嬮」</div>
          ${renderPills(actionItems, '鏆傛椂娌℃湁棰濆鎿嶄綔銆?, 'live')}
        </div>
      `;
    }

    function makeRadarSvg(labels, values, realValues = null, accent = 'cyan') {
      const size = 220;
      const cx = 110;
      const cy = 110;
      const radius = 70;
      const levels = 4;
      const angleStep = (Math.PI * 2) / labels.length;

      const point = (index, ratio) => {
        const angle = -Math.PI / 2 + angleStep * index;
        return {
          x: cx + Math.cos(angle) * radius * ratio,
          y: cy + Math.sin(angle) * radius * ratio
        };
      };

      let grids = '';
      for (let level = 1; level <= levels; level++) {
        const ratio = level / levels;
        const pts = labels.map((_, idx) => {
          const p = point(idx, ratio);
          return `${p.x},${p.y}`;
        }).join(' ');
        grids += `<polygon class="radar-grid-line" points="${pts}"></polygon>`;
      }

      const axes = labels.map((_, idx) => {
        const p = point(idx, 1);
        return `<line class="radar-axis-line" x1="${cx}" y1="${cy}" x2="${p.x}" y2="${p.y}"></line>`;
      }).join('');

      const shapePoints = values.map((value, idx) => {
        const p = point(idx, Math.max(0.08, Math.min(1, value / 100)));
        return `${p.x},${p.y}`;
      }).join(' ');

      const labelTexts = labels.map((label, idx) => {
        const p = point(idx, 1.22);
        let anchor = 'middle';
        if (p.x < cx - 10) anchor = 'end';
        else if (p.x > cx + 10) anchor = 'start';
        return `<text x="${p.x}" y="${p.y}" text-anchor="${anchor}" dominant-baseline="middle" class="radar-label">${label}</text>`;
      }).join('');

      const legend = labels.map((label, idx) => `
        <div class="legend-chip">
          <b>${label}</b>
          <span>${realValues ? realValues[idx] : values[idx]}</span>
        </div>
      `).join('');

      return `
        <div class="radar-shell">
          <div class="radar-visual">
            <svg class="radar-svg" viewBox="0 0 ${size} ${size}">
              ${grids}
              ${axes}
              <polygon class="radar-data-shape ${accent === 'gold' ? 'gold' : ''}" points="${shapePoints}"></polygon>
              ${labelTexts}
            </svg>
          </div>
          <div class="radar-legend">${legend}</div>
        </div>
      `;
    }

    function makeTileGrid(items, className = '') {
      const tileItems = Array.isArray(items) ? items : [];
      const compactIntelGrid =
        tileItems.length === 2
        && tileItems.every(item => ['绾跨储', '褰卞搷'].includes(toText(item && item.label, '')));
      const gridStyle = compactIntelGrid ? ' style="grid-template-columns:minmax(0, 2.2fr) minmax(120px, 0.8fr); gap:12px;"' : '';
      const tileStyle = compactIntelGrid ? ' style="min-height:0; padding:12px 14px;"' : '';
      const valueStyle = compactIntelGrid ? ' style="line-height:1.7;"' : '';
      return `
        <div class="archive-tile-grid ${className}"${gridStyle}>
          ${tileItems.map(item => `
            <div class="archive-tile"${tileStyle}>
              <b>${item.label}</b>
              <span${valueStyle}>${item.value}</span>
            </div>
          `).join('')}
        </div>
      `;
    }

    function escapeHtmlAttr(value) {
      return String(value ?? '')
        .replace(/&/g, '&amp;')
        .replace(/</g, '&lt;')
        .replace(/>/g, '&gt;')
        .replace(/"/g, '&quot;');
    }

    function makeInteractiveTileGrid(items, className = '') {
      return `
        <div class="archive-tile-grid ${className}">
          ${(items || []).map(item => `
            <div class="archive-tile ${item.className || ''} ${item.preview ? 'clickable' : ''}"${item.preview ? ` data-preview="${escapeHtmlAttr(item.preview)}"` : ''}>
              <b>${item.label}</b>
              <span>${item.value}</span>
            </div>
          `).join('')}
        </div>
      `;
    }

    function makeTagCloud(items, className = '') {
      return `
        <div class="tag-cloud ${className}">
          ${(items || []).map(item => `<span class="tag-chip ${item.className || ''}">${item.text}</span>`).join('')}
        </div>
      `;
    }

    function makeWalletStrip(items) {
      return `
        <div class="wallet-strip">
          ${(items || []).map(item => `
            <div class="wallet-chip ${item.className || ''}">
              <b>${item.label}</b>
              <span>${item.value}</span>
            </div>
          `).join('')}
        </div>
      `;
    }

    function makeFigureBoard(coreLabel, slots) {
      return `
        <div class="figure-board">
          <div class="figure-core">${coreLabel}</div>
          ${(slots || []).map(slot => `
            <div class="figure-slot ${slot.className || ''}" style="left:${slot.x}%;top:${slot.y}%">${slot.label}</div>
          `).join('')}
        </div>
      `;
    }

    function makeInteractiveFigureBoard(coreLabel, slots, core = {}) {
      return `
        <div class="figure-board">
          <div class="figure-core ${core.preview ? 'clickable' : ''}"${core.preview ? ` data-preview="${escapeHtmlAttr(core.preview)}"` : ''}>${coreLabel}</div>
          ${(slots || []).map(slot => `
            <div class="figure-slot ${slot.className || ''} ${slot.preview ? 'clickable' : ''}"${slot.preview ? ` data-preview="${escapeHtmlAttr(slot.preview)}"` : ''} style="left:${slot.x}%;top:${slot.y}%">${slot.label}</div>
          `).join('')}
        </div>
      `;
    }

    function summarizeArmoryValue(value) {
      if (value === undefined || value === null || value === '') return '鏃?;
      if (Array.isArray(value)) return value.map(item => summarizeArmoryValue(item)).filter(Boolean).slice(0, 4).join(' / ') || '鏃?;
      if (typeof value === 'object') {
        const named = value.name || value['鍚嶇О'] || value.title || value['鏍囬'];
        if (named) return String(named);
        return Object.entries(value)
          .filter(([, v]) => v !== undefined && v !== null && v !== '' && typeof v !== 'object')
          .slice(0, 4)
          .map(([k, v]) => `${k}:${v}`)
          .join(' / ') || '鏃?;
      }
      return String(value);
    }


    function buildStatsBonusItems(statsBonus, options = {}) {
      const bonus = statsBonus && typeof statsBonus === 'object' ? statsBonus : {};
      const normalizeBonusValue = value => {
        const numeric = toNumber(value, 0);
        if (numeric > 0) return Math.floor(numeric);
        if (numeric < 0) return Math.ceil(numeric);
        return 0;
      };
      const items = [];
      if (options.includeLvEquiv && toNumber(bonus.绛夋晥绛夌骇, 0) > 0) items.push({ label: '绛夋晥绛夌骇', value: String(toNumber(bonus.绛夋晥绛夌骇, 0)) });
      items.push(
        { label: '浣撳姏鍔犳垚', value: formatNumber(normalizeBonusValue(bonus.浣撳姏涓婇檺)) },
        { label: '榄傚姏鍔犳垚', value: formatNumber(normalizeBonusValue(bonus.榄傚姏涓婇檺)) },
        { label: '绮剧鍔犳垚', value: formatNumber(normalizeBonusValue(bonus.绮剧鍔涗笂闄?) },
        { label: '鍔涢噺鍔犳垚', value: formatNumber(normalizeBonusValue(bonus.鍔涢噺)) },
        { label: '闃插尽鍔犳垚', value: formatNumber(normalizeBonusValue(bonus.闃插尽)) },
        { label: '鏁忔嵎鍔犳垚', value: formatNumber(normalizeBonusValue(bonus.鏁忔嵎)) }
      );
      return items;
    }

    function buildEditableStatBonusItems(basePath = [], statsBonus = {}, options = {}) {
      const path = Array.isArray(basePath) ? basePath : [];
      const bonus = statsBonus && typeof statsBonus === 'object' ? statsBonus : {};
      const includeSoulPower = options.includeSoulPower !== false;
      const makeBonusValue = field => {
        const numeric = toNumber(bonus[field], 0);
        const rawValue = numeric > 0 ? Math.floor(numeric) : numeric < 0 ? Math.ceil(numeric) : 0;
        return path.length
          ? makeInlineEditableValue(formatNumber(rawValue), {
              path: [...path, field],
              kind: 'number',
              rawValue,
              editorMeta: { min: 0, integer: true, hint: '鏈€灏?0 路 鏁存暟' }
            })
          : htmlEscape(formatNumber(rawValue));
      };
      return [
        { label: '浣撳姏鍔犳垚', value: makeBonusValue('浣撳姏涓婇檺') },
        ...(includeSoulPower ? [{ label: '榄傚姏鍔犳垚', value: makeBonusValue('榄傚姏涓婇檺') }] : []),
        { label: '绮剧鍔犳垚', value: makeBonusValue('绮剧鍔涗笂闄?) },
        { label: '鍔涢噺鍔犳垚', value: makeBonusValue('鍔涢噺') },
        { label: '闃插尽鍔犳垚', value: makeBonusValue('闃插尽') },
        { label: '鏁忔嵎鍔犳垚', value: makeBonusValue('鏁忔嵎') }
      ];
    }

    const SPIRIT_ATTRIBUTE_SYSTEM_OPTIONS = Object.freeze(['鏃?, '鍏冪礌', '浜旇']);
    const SPIRIT_ATTRIBUTE_TOKEN_OPTIONS = Object.freeze([
      '閲?, '鏈?, '姘?, '鐏?, '鍦?,
      '椋?, '闆?, '鍐?,
      '鍏?, '鏆?, '绮剧',
      '绌洪棿', '鏃堕棿',
      '鍒涢€?, '姣佺伃',
    ]);
    const WUXING_ATTRIBUTE_TOKEN_OPTIONS = Object.freeze(['閲?, '鏈?, '姘?, '鐏?, '鍦?]);
    const STATUS_ACTION_RUNTIME_OPTIONS = Object.freeze([
      '鏃ュ父',
      '鍐ユ兂',
      '鐫＄湢',
      '鎴樻枟',
      '鑲変綋璁粌',
      '绮剧璁粌',
    ]);
    const RING_COLOR_META = Object.freeze({
      white: Object.freeze({ label: '鐧?, glyph: '鐧?, aliases: ['鐧借壊'] }),
      yellow: Object.freeze({ label: '榛?, glyph: '榛?, aliases: ['榛勮壊'] }),
      purple: Object.freeze({ label: '绱?, glyph: '绱?, aliases: ['绱壊'] }),
      black: Object.freeze({ label: '榛?, glyph: '榛?, aliases: ['榛戣壊'] }),
      red: Object.freeze({ label: '绾?, glyph: '绾?, aliases: ['绾㈣壊'] }),
      gold: Object.freeze({ label: '閲?, glyph: '閲?, aliases: ['閲戣壊'] }),
      orangegold: Object.freeze({ label: '姗欓噾', glyph: '姗?, aliases: ['姗?, '姗欒壊', '姗欓噾鑹?] }),
      whitegold: Object.freeze({ label: '鐧介噾', glyph: '閾?, aliases: ['鐧介噾鑹?, '閾傞噾'] }),
      silver: Object.freeze({ label: '閾?, glyph: '閾?, aliases: ['閾惰壊', '鐧介摱'] }),
      frostsilver: Object.freeze({ label: '闇滈摱', glyph: '闇?, aliases: ['闇滈摱鑹?] }),
      moonwhite: Object.freeze({ label: '鏈堢櫧', glyph: '鏈?, aliases: ['鏈堢櫧鑹?] }),
      iceblue: Object.freeze({ label: '鍐拌摑', glyph: '鍐?, aliases: ['鍐拌摑鑹?, '鍐伴潚'] }),
      starblue: Object.freeze({ label: '鏄熻摑', glyph: '鏄?, aliases: ['鏄熻摑鑹?, '婀涜摑'] }),
      cyan: Object.freeze({ label: '闈?, glyph: '闈?, aliases: ['闈掕壊', '澶╅潚'] }),
      teal: Object.freeze({ label: '娴烽潚', glyph: '娴?, aliases: ['娴烽潚鑹?, '闈掔ⅶ'] }),
      jade: Object.freeze({ label: '纰х帀', glyph: '纰?, aliases: ['纰?, '鐜夎壊', '闈掔帀'] }),
      emerald: Object.freeze({ label: '缈＄繝', glyph: '缈?, aliases: ['缈＄繝鑹?, '缈犵豢', '缈犺壊'] }),
      'green-gold': Object.freeze({ label: '缁块噾', glyph: '缁?, aliases: ['缁块噾鑹?, '缈犻噾', '鑻嶇繝閲?] }),
      'blue-gold': Object.freeze({ label: '钃濋噾', glyph: '钃?, aliases: ['钃濋噾鑹?, '闈掗噾', '娴烽噾'] }),
      'purple-gold': Object.freeze({ label: '绱噾', glyph: '閹?, aliases: ['绱噾鑹?] }),
      'scarlet-gold': Object.freeze({ label: '璧ら噾', glyph: '璧?, aliases: ['璧ら噾鑹?, '缁噾'] }),
      'rose-gold': Object.freeze({ label: '鐜噾', glyph: '鐜?, aliases: ['鐜噾鑹?, '鐜懓閲?] }),
      'amber-gold': Object.freeze({ label: '鐞ョ弨閲?, glyph: '鐞?, aliases: ['鐞ョ弨閲戣壊', '铚滈噾'] }),
      'obsidian-gold': Object.freeze({ label: '鏇滈粦閲?, glyph: '鏇?, aliases: ['鏇滈粦閲戣壊', '榛戦噾', '鐜勯噾'] }),
      aurora: Object.freeze({ label: '鏋佸厜', glyph: '鏋?, aliases: ['鏋佸厜鑹?] }),
      rainbow: Object.freeze({ label: '铏瑰僵', glyph: '铏?, aliases: ['铏瑰僵鑹?, '涓冨僵', '骞诲僵'] }),
      crimson: Object.freeze({ label: '缁孩', glyph: '缁?, aliases: ['缁孩鑹?, '鐚╃孩', '琛€绾?] }),
    });
    const RING_COLOR_OPTIONS = Object.freeze(Object.values(RING_COLOR_META).map(meta => meta.label));
    const RING_COLOR_ALIAS_MAP = Object.freeze(
      Object.entries(RING_COLOR_META).reduce((bucket, [key, meta]) => {
        [meta.label, ...(Array.isArray(meta.aliases) ? meta.aliases : [])].forEach(alias => {
          const label = toText(alias, '').trim();
          if (label) bucket[label] = key;
        });
        return bucket;
      }, {})
    );

    function normalizeSpiritAttributeTokenList(value = []) {
      return normalizeEditorStringList(value).filter(token => SPIRIT_ATTRIBUTE_TOKEN_OPTIONS.includes(token));
    }

    function resolveSpiritAttributeUiState(spiritData = {}) {
      const rawSystem = normalizeSkillUiText(spiritData && spiritData['灞炴€т綋绯?], '鏃?);
      const attributeSystem = SPIRIT_ATTRIBUTE_SYSTEM_OPTIONS.includes(rawSystem) ? rawSystem : '鏃?;
      const unlockedAttrs = normalizeSpiritAttributeTokenList(spiritData && spiritData['宸茶В閿佸睘鎬?]);
      let capacityAttrs = normalizeSpiritAttributeTokenList(spiritData && spiritData['鍙绾冲睘鎬?]);
      if (!capacityAttrs.length) {
        if (attributeSystem === '浜旇') capacityAttrs = [...WUXING_ATTRIBUTE_TOKEN_OPTIONS];
        else if (attributeSystem === '鍏冪礌' && unlockedAttrs.length) capacityAttrs = [...unlockedAttrs];
      }
      return {
        attributeSystem,
        unlockedAttrs,
        capacityAttrs,
      };
    }

    function resolveArmorPartData(armor, slotLabel) {
      const source = armor && typeof armor === 'object' ? armor : {};
      const aliasMap = {
        '澶寸洈': ['澶寸洈', '澶撮儴', 'helmet'],
        '鑳搁摖': ['鑳搁摖', '鑳哥敳', 'chest'],
        '宸﹁偐': ['宸﹁偐', '鑲╃敳宸?, 'left_shoulder'],
        '鍙宠偐': ['鍙宠偐', '鑲╃敳鍙?, 'right_shoulder'],
        '宸﹁噦': ['宸﹁噦', '鑷傜敳宸?, 'left_arm'],
        '鍙宠噦': ['鍙宠噦', '鑷傜敳鍙?, 'right_arm'],
        '宸﹁吙': ['宸﹁吙', '鑵跨敳宸?, 'left_leg'],
        '鍙宠吙': ['鍙宠吙', '鑵跨敳鍙?, 'right_leg'],
        '鎴樿': ['鎴樿', '瑁欑敳', 'skirt'],
        '鎴橀澊': ['鎴橀澊', '闈村瓙', 'boots'],
        '鎴掓寚': ['鎴掓寚', 'ring']
      };
      const aliases = aliasMap[slotLabel] || [slotLabel];
      for (const key of aliases) {
        const found = deepGet(source, ['閮ㄤ欢', key], null)
          || deepGet(source, ['閮ㄤ欢', key], null)
          || deepGet(source, ['slots', key], null)
          || deepGet(source, ['components', key], null)
          || deepGet(source, key, null);
        if (found) return found;
      }
      return null;
    }

    function buildArmoryActionRequest(snapshot, actionType) {
      if (!snapshot || !snapshot.rootData) return null;
      if (!isSnapshotPlayerControlled(snapshot)) return null;
      const activeKey = resolveSnapshotCharKey(snapshot, toText(snapshot.activeName, ''));
      if (!activeKey) return null;
      const chars = deepGet(snapshot, 'rootData.char', {});
      const activeChar = chars && typeof chars === 'object' ? (chars[activeKey] || {}) : {};
      const activeName = toText(activeChar && (activeChar.name || deepGet(activeChar, 'base.name', '')), toText(snapshot.activeName, activeKey));
      const currentLoc = toText(deepGet(activeChar, '鐘舵€?浣嶇疆', snapshot.currentLoc || '褰撳墠浣嶇疆'), '褰撳墠浣嶇疆').replace(/^鏂楃綏澶ч檰-/, '').replace(/^鏂楃伒澶ч檰-/, '');
      const armor = deepGet(activeChar, '瑁呭.鏂楅摖', {});
      const mech = deepGet(activeChar, '瑁呭.鏈虹敳', {});
      const weapon = deepGet(activeChar, '瑁呭.姝﹀櫒', {});
      const armorName = toText(armor.鍚嶇О || armor['鍚嶇О'], '褰撳墠鏂楅摖');
      const mechName = toText(mech.鍨嬪彿 || mech['鍨嬪彿'], '褰撳墠鏈虹敳');
      const actionMap = {
        equip_mech: { title: '瑁呭鏈虹敳', playerInput: `鎴戣鍦ㄣ€?{currentLoc}銆戣澶囨満鐢层€?{mechName}銆戙€俙, note: `璇锋寜 MVU 瑁呭瑙勫垯灏?char.瑁呭.鏈虹敳.瑁呭鐘舵€?澶勭悊涓衡€滃凡瑁呭鈥濄€傝嫢褰撳墠鏂楅摖宸茶澶囦笖鏈虹敳涓嶆槸绾㈢骇锛岃鍚屾澶勭悊鏂楅摖鍗镐笅骞舵竻绌哄搴?_灞炴€у姞鎴愩€俙 },
        unequip_mech: { title: '鍗镐笅鏈虹敳', playerInput: `鎴戣鍦ㄣ€?{currentLoc}銆戣В闄ゆ満鐢层€?{mechName}銆戠殑瑁呰浇銆俙, note: `璇峰皢 char.瑁呭.鏈虹敳.瑁呭鐘舵€?澶勭悊涓衡€滄湭瑁呭鈥濓紝骞跺皢 鏈虹敳._灞炴€у姞鎴?娓呴浂銆俙 },
        equip_armor: { title: '绌挎埓鏂楅摖', playerInput: `鎴戣鍦ㄣ€?{currentLoc}銆戠┛鎴存枟閾犮€?{armorName}銆戙€俙, note: `璇锋寜 MVU 瑁呭瑙勫垯灏濊瘯灏?char.瑁呭.鏂楅摖.瑁呭鐘舵€?澶勭悊涓衡€滃凡瑁呭鈥濄€傞渶瑕佹牎楠屾枟閾犵瓑绾ч棬妲涳紙1瀛?0绾?2瀛?0绾?3瀛?0绾?4瀛?0绾э級锛涜嫢涓嶆弧瓒冲垯淇濇寔鏈澶囧苟鍐欏叆瑁呭鍙嶅櫖銆傝嫢褰撳墠宸茶澶囬潪绾㈢骇鏈虹敳锛屾枟閾犱笉鑳戒繚鎸佸凡瑁呭銆俙 },
        unequip_armor: { title: '鍗镐笅鏂楅摖', playerInput: `鎴戣鍦ㄣ€?{currentLoc}銆戣В闄ゆ枟閾犮€?{armorName}銆戠殑瑁呭鐘舵€併€俙, note: `璇峰皢 char.瑁呭.鏂楅摖.瑁呭鐘舵€?澶勭悊涓衡€滄湭瑁呭鈥濓紝骞跺皢 鏂楅摖._灞炴€у姞鎴?娓呴浂銆俙 }
      };
      const actionMeta = actionMap[actionType];
      if (!actionMeta) return null;
      const systemPrompt = `浠ヤ笅鍐呭灞炰簬鍓嶇鍙戣捣鐨勮澶囩鐞嗚姹傦紝涓嶈鍦ㄦ鏂囩洿鎺ュ杩扳€滅郴缁熸彁绀?/ 璇锋眰绫诲瀷 / JSONPatch鈥濈瓑鏈銆俓n\n[瑁呭绠＄悊]\n瑙掕壊锛?{activeName}\n鍦扮偣锛?{currentLoc}\n鍔ㄤ綔锛?{actionMeta.title}\n褰撳墠鏂楅摖锛?{summarizeArmoryValue(armor.鍚嶇О || armor['鍚嶇О'] || armor.瑁呭鐘舵€?|| '鏃?)}\n褰撳墠鏈虹敳锛?{summarizeArmoryValue(mech.鍨嬪彿 || mech['鍨嬪彿'] || mech.瑁呭鐘舵€?|| '鏃?)}\n褰撳墠涓绘鍣細${summarizeArmoryValue(weapon.鍚嶇О || weapon['鍚嶇О'] || '鏃?)}\n\n${actionMeta.note}\n\n璇峰皢杩欐鎿嶄綔鍐欐垚鑷劧鍓ф儏锛屽苟鍦ㄩ渶瑕佹椂鍚屾鏇存柊瑙掕壊鐨勮澶囩姸鎬併€佺浉鍏宠澶囧瓧娈典笌绯荤粺鎾姤銆俙;
      return {
        playerInput: actionMeta.playerInput,
        systemPrompt,
        requestKind: 'equip_manage'
      };
    }

    function buildInventoryUseRequest(snapshot, meta = {}) {
      if (!snapshot || !snapshot.rootData) return null;
      if (!isSnapshotPlayerControlled(snapshot)) return null;
      const charKey = toText(meta && meta.charKey, '').trim() || resolveSnapshotCharKey(snapshot, toText(snapshot.activeName, ''));
      if (!charKey) return null;
      const chars = deepGet(snapshot, 'rootData.char', {});
      const activeChar = chars && typeof chars === 'object' ? (chars[charKey] || {}) : {};
      const activeName = toText(activeChar && (activeChar.name || deepGet(activeChar, 'base.name', '')), toText(snapshot.activeName, charKey));
      const currentLoc = toText(deepGet(activeChar, '鐘舵€?浣嶇疆', snapshot.currentLoc || '褰撳墠浣嶇疆'), '褰撳墠浣嶇疆').replace(/^鏂楃綏澶ч檰-/, '').replace(/^鏂楃伒澶ч檰-/, '');
      const itemName = toText(meta && meta.itemName, '鏈煡鐗╁搧');
      const actionLabel = toText(meta && meta.actionLabel, '浣跨敤');
      const summaryText = toText(meta && meta.summaryText, '宸茶Е鍙戠墿鍝佹晥鏋?);
      const systemPrompt = `浠ヤ笅鍐呭灞炰簬鍓嶇宸茬粡瀹屾垚鐨勮儗鍖?{actionLabel}缁撶畻锛屼笉瑕佸湪姝ｆ枃鐩存帴澶嶈堪鈥滅郴缁熸彁绀?/ JSONPatch / 鍙橀噺鏇存柊 / 鍓ф儏鎺ㄨ繘璇锋眰鈥濈瓑鏈銆俓n\n[鑳屽寘${actionLabel}]\n瑙掕壊锛?{activeName}\n鍦扮偣锛?{currentLoc}\n鍔ㄤ綔锛?{actionLabel}\n鐗╁搧锛?{itemName}\n宸茬粨绠楁晥鏋滐細${summaryText}\n\n璇风洿鎺ユ壙鎺ヨ繖娆?{actionLabel}鍚庣殑鑷劧鍓ф儏鍙嶉锛屽啓鍑鸿鑹茬殑鍗虫椂浣撴劅銆佸懆鍥翠汉鐨勫彲瑙佸弽搴斻€佸眬閮ㄧ幆澧冨彉鍖栦笌鎺ヤ笅鏉ョ殑鍙帹杩涚┖闂淬€傞櫎闈炲墽鎯呬笂纭湁鏂板鍙樺寲锛屼笉瑕侀噸澶嶇粨绠楀悓涓€鎵瑰彉閲忋€俙;
      return {
        playerInput: `鎴?{actionLabel}浜嗐€?{itemName}銆戙€俙,
        systemPrompt,
        requestKind: 'inventory_use',
      };
    }

    function buildConstructSkillUseRequest(snapshot, meta = {}) {
      if (!snapshot || !snapshot.rootData) return null;
      if (!isSnapshotPlayerControlled(snapshot)) return null;
      const charKey = toText(meta && meta.charKey, '').trim() || resolveSnapshotCharKey(snapshot, toText(snapshot.activeName, ''));
      if (!charKey) return null;
      const chars = deepGet(snapshot, 'rootData.char', {});
      const activeChar = chars && typeof chars === 'object' ? (chars[charKey] || {}) : {};
      const activeName = toText(activeChar && (activeChar.name || deepGet(activeChar, 'base.name', '')), toText(snapshot.activeName, charKey));
      const currentLoc = toText(deepGet(activeChar, '鐘舵€?浣嶇疆', snapshot.currentLoc || '褰撳墠浣嶇疆'), '褰撳墠浣嶇疆').replace(/^鏂楃綏澶ч檰-/, '').replace(/^鏂楃伒澶ч檰-/, '');
      const skillName = toText(meta && meta.skillName, '閫犵墿鎶€鑳?);
      const producedItems = Array.isArray(meta && meta.producedItems) ? meta.producedItems : [];
      const costText = toText(meta && meta.costText, '鏃?);
      const itemSummary = producedItems.length ? producedItems.join('銆?) : '涓存椂閫犵墿';
      const systemPrompt = `浠ヤ笅鍐呭灞炰簬鍓嶇宸茬粡瀹屾垚鐨勬棩甯搁€犵墿鏂藉睍缁撶畻锛屼笉瑕佸湪姝ｆ枃鐩存帴澶嶈堪鈥滅郴缁熸彁绀?/ JSONPatch / 鍙橀噺鏇存柊 / 鍓ф儏鎺ㄨ繘璇锋眰鈥濈瓑鏈銆俓n\n[鏃ュ父閫犵墿鏂藉睍]\n瑙掕壊锛?{activeName}\n鍦扮偣锛?{currentLoc}\n鎶€鑳斤細${skillName}\n鐢熸垚缁撴灉锛?{itemSummary}\n璧勬簮娑堣€楋細${costText}\n\n璇风洿鎺ユ壙鎺ヨ繖娆℃棩甯告柦灞曞悗鐨勮嚜鐒跺墽鎯呭弽棣堬紝鍐欏嚭鏂芥硶琛ㄧ幇銆佷骇鐗╄惤鎴愮姸鎬併€佸懆鍥寸幆澧冩垨鏃佽鑰呭弽搴旓紝浠ュ強瀹冧负鍚庣画鍓ф儏甯︽潵鐨勫彲缁х画鎺ㄨ繘鐐广€傞櫎闈炲墽鎯呬笂纭湁鏂板鍙樺寲锛屼笉瑕侀噸澶嶇粨绠楀悓涓€鎵瑰彉閲忋€俙;
      return {
        playerInput: `鎴戝湪銆?{currentLoc}銆戞棩甯告柦灞曚簡銆?{skillName}銆戙€俙,
        systemPrompt,
        requestKind: 'daily_construct_cast',
      };
    }

    function makeInventoryGrid(items) {
      const attr = (value) => String(value ?? '')
        .replace(/&/g, '&amp;')
        .replace(/</g, '&lt;')
        .replace(/>/g, '&gt;')
        .replace(/"/g, '&quot;');

      return `
        <div class="inventory-grid">
          ${(items || []).map(item => `
            <div class="inventory-cell ${item.className || ''}"
              data-hover-title="${attr(item.name)}"
              data-hover-char="${attr(item.charKey || '')}"
              data-hover-type="${attr(item.type || item.meta || '--')}"
              data-hover-rarity="${attr(item.rarity || '--')}"
              data-hover-qty="${attr(`脳${item.qty}`)}"
              data-hover-count="${attr(item.qty)}"
              data-hover-source="${attr(item.source || '--')}"
              data-hover-trigger="${attr(item.trigger || '--')}"
              data-hover-expiry="${attr(item.expiry || '--')}"
              data-hover-usage="${attr(item.usage || item.meta || '--')}"
              data-hover-equip="${attr(item.canEquip ? 'true' : '')}"
              data-hover-usable="${attr(item.canUse ? 'true' : '')}"
              data-hover-action-label="${attr(item.actionLabel || '浣跨敤')}"
              data-hover-tags="${attr((item.tags || []).join('|'))}">
              <div class="cell-top">
                <b>${item.name}</b>
                <span class="qty">脳${item.qty}</span>
              </div>
              <div class="tier">${item.meta}</div>
            </div>
          `).join('')}
        </div>
      `;
    }

    function makeSealColumn(items) {
      return `
        <div class="seal-column">
          ${(items || []).map(item => `
            <div class="seal-node ${item.className || ''}">
              <span>${item.label}</span>
              <b>${item.state}</b>
            </div>
          `).join('')}
        </div>
      `;
    }

    function makeFactionLadder(items) {
      return `
        <div class="faction-ladder">
          ${(items || []).map(item => `
            <div class="faction-row ${item.className || ''}">
              <b>${item.name}</b>
              <span>${item.desc}</span>
            </div>
          `).join('')}
        </div>
      `;
    }

    const modalPaginationState = Object.create(null);
    const modalFocusState = Object.create(null);

    function makeTimelineStack(items) {
      return `
        <div class="timeline-stack">
          ${(items || []).map(item => `
            <div class="timeline-card${item && item.className ? ` ${item.className}` : ''}${item && item.preview ? ' clickable' : ''}"${item && item.preview ? ` data-preview="${escapeHtmlAttr(item.preview)}"` : ''}>
              <b>${item.title}</b>
              <span${item && item.descTitle ? ` title="${escapeHtmlAttr(toText(item.descTitle, ''))}"` : ''}>${item.desc}</span>
            </div>
          `).join('')}
        </div>
      `;
    }

    function makeDossierRows(items, className = '') {
      const rowItems = Array.isArray(items) ? items.filter(Boolean) : [];
      const columnCount = /\bdossier-row-grid--three\b/.test(className)
        ? 3
        : (/\bdossier-row-grid--two\b/.test(className) ? 2 : 1);
      const paddedItems = [];
      let usedSlots = 0;

      rowItems.forEach(item => {
        const itemClassName = item && item.className ? item.className : '';
        const isWide = columnCount > 1 && /\bdossier-row--wide\b/.test(itemClassName);

        if (isWide && usedSlots > 0) {
          while (usedSlots < columnCount) {
            paddedItems.push({ label: '\u00A0', value: '\u00A0', className: 'dossier-row--ghost' });
            usedSlots += 1;
          }
          usedSlots = 0;
        }

        paddedItems.push(item);

        if (columnCount === 1) return;
        if (isWide) {
          usedSlots = 0;
          return;
        }

        usedSlots += 1;
        if (usedSlots >= columnCount) usedSlots = 0;
      });

      if (columnCount > 1 && usedSlots > 0) {
        while (usedSlots < columnCount) {
          paddedItems.push({ label: '\u00A0', value: '\u00A0', className: 'dossier-row--ghost' });
          usedSlots += 1;
        }
      }

      return `
        <div class="dossier-row-grid ${className}">
          ${paddedItems.map(item => `
            <div class="dossier-row ${item && item.className ? item.className : ''}">
              <b>${htmlEscape(toText(item && item.label, ''))}</b>
              <span>${item && item.value !== undefined && item.value !== null ? item.value : ''}</span>
            </div>
          `).join('')}
        </div>
      `;
    }

    function makeDossierTags(items, className = '') {
      return `
        <div class="dossier-tag-row ${className}">
          ${(Array.isArray(items) ? items : []).map(item => `<span class="dossier-pill ${item && item.className ? item.className : ''}">${htmlEscape(toText(item && item.text, ''))}</span>`).join('')}
        </div>
      `;
    }

    function makeDossierList(items, className = '') {
      const listItems = Array.isArray(items) ? items : [];
      return `
        <div class="dossier-list ${className}">
          ${listItems.map(item => `
            <div class="dossier-list-row ${item && item.className ? item.className : ''} ${item && item.preview ? 'clickable' : ''}"${item && item.preview ? ` data-preview="${escapeHtmlAttr(item.preview)}"` : ''}>
              <b>${htmlEscape(toText(item && item.title, ''))}</b>
              <span>${item && item.desc !== undefined && item.desc !== null ? item.desc : ''}</span>
            </div>
          `).join('')}
        </div>
      `;
    }

    function makeDossierMeter(label, value, percent, className = '') {
      return `
        <div class="dossier-meter ${className}">
          <div class="dossier-meter-head">
            <b>${htmlEscape(toText(label, ''))}</b>
            <span>${value !== undefined && value !== null ? value : ''}</span>
          </div>
          <div class="dossier-meter-track"><div class="dossier-meter-fill" style="width:${Math.max(0, Math.min(100, toNumber(percent, 0)))}%;"></div></div>
        </div>
      `;
    }

    function paginateModalItems(items, previewKey, sectionKey, pageSize = 50) {
      const list = Array.isArray(items) ? items : [];
      const safePreviewKey = toText(previewKey, 'modal');
      const safeSectionKey = toText(sectionKey, 'section');
      const stateKey = `${safePreviewKey}::${safeSectionKey}`;
      const totalPages = Math.max(1, Math.ceil(list.length / Math.max(1, pageSize)));
      const currentPage = Math.max(1, Math.min(totalPages, toNumber(modalPaginationState[stateKey], 1) || 1));
      modalPaginationState[stateKey] = currentPage;
      const start = (currentPage - 1) * pageSize;
      return {
        stateKey,
        page: currentPage,
        totalPages,
        total: list.length,
        items: list.slice(start, start + pageSize)
      };
    }

    function makeModalPaginationControls(sectionKey, page, totalPages, total) {
      if (totalPages <= 1) return '';
      return `
        <div class="tag-cloud armory-quick-actions" style="margin-top:12px;justify-content:flex-end;">
          <button type="button" class="tag-chip clickable" data-page-nav="prev" data-page-section="${escapeHtmlAttr(sectionKey)}" ${page <= 1 ? 'disabled' : ''}>涓婁竴椤?/button>
          <span class="tag-chip">绗?${page} / ${totalPages} 椤?路 鍏?${total} 鏉?/span>
          <button type="button" class="tag-chip clickable" data-page-nav="next" data-page-section="${escapeHtmlAttr(sectionKey)}" ${page >= totalPages ? 'disabled' : ''}>涓嬩竴椤?/button>
        </div>
      `;
    }

    const archiveModalBuilders = Object.create(null);

    function buildEmptyRingLane(ringClass = 'ring-white', count = 10) {
      return Array.from({ length: count }).map(() => `<div class="ring ${ringClass} empty"></div>`).join('');
    }

    function buildArchiveSkeletonModal(previewKey) {
      const key = String(previewKey || '');
      const isMapNode = key.startsWith('鍦板浘鑺傜偣锛?);
      const nodeName = isMapNode ? key.replace('鍦板浘鑺傜偣锛?, '') : '';

      if (key === '鐢熷懡鍥捐氨璇︾粏椤?) {
        return {
          title: '璇︾粏妗ｆ',
          body: `
            <div class="archive-modal-grid life-graph-grid">
              <div class="archive-card life-growth-card">
                <div class="archive-card-head"><div class="archive-card-title">鎴愰暱淇℃伅</div></div>
                <div class="identity-growth-grid">
                  <div class="meta-item"><b>绛夌骇</b><span></span></div>
                  <div class="meta-item"><b>绮剧澧冪晫</b><span></span></div>
                  <div class="meta-item"><b>澶╄祴姊槦</b><span></span></div>
                  <div class="meta-item"><b>绯诲埆</b><span></span></div>
                </div>
              </div>
              <div class="archive-card life-profile-card">
                <div class="archive-card-head"><div class="archive-card-title">瑙掕壊鍚嶇墖</div></div>
                <div class="profile-snapshot life-profile-snapshot">
                  <div class="identity-card">
                    <h3>褰撳墠瑙掕壊</h3>
                    <div class="identity-panel">
                      <div class="identity-panel-title">鍩虹鎻忚堪</div>
                      <div class="identity-basic-grid">
                        <div class="meta-item"><b>骞撮緞 / 鎬у埆</b><span></span></div>
                        <div class="meta-item"><b>鎬ф牸</b><span></span></div>
                        <div class="meta-item meta-item-wide"><b>鍚嶆湜</b><span></span></div>
                      </div>
                    </div>
                    <div class="identity-panel identity-look-panel">
                      <div class="identity-panel-title">澶栬矊姒傝</div>
                      <div class="identity-look-grid">
                        <div class="meta-item"><b>鍙戣壊</b><span></span></div>
                        <div class="meta-item"><b>鍙戝瀷</b><span></span></div>
                        <div class="meta-item"><b>鐬宠壊</b><span></span></div>
                        <div class="meta-item"><b>韬珮</b><span></span></div>
                        <div class="meta-item"><b>浣撳瀷</b><span></span></div>
                        <div class="meta-item meta-item-wide"><b>闀跨浉鎻忚堪</b><span></span></div>
                        <div class="meta-item meta-item-wide"><b>鐗瑰緛</b><span></span></div>
                      </div>
                    </div>
                  </div>
                  <div class="status-card">
                    <h4 style="margin:0 0 6px;font-size:13px;font-family:var(--font-title);color:var(--white);">褰撳墠鐘舵€?/h4>
                    <div class="status-list">
                      <div class="status-row"><b>琛屽姩鐘舵€?/b><span></span></div>
                      <div class="status-row"><b>浼ゅ娍</b><span></span></div>
                      <div class="status-row"><b>鐏电墿鍚告敹</b><span></span></div>
                      <div class="status-row"><b>榄傛牳鐘舵€?/b><span></span></div>
                      <div class="status-row"><b>琛€鑴夌姸鎬?/b><span></span></div>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          `
        };
      }

      if (key === '绀句細妗ｆ璇︾粏椤?) {
        return {
          title: '绀句細妗ｆ寮圭獥',
          body: `
            <div class="archive-modal-grid">
              <div class="archive-card">
                <div class="archive-card-head"><div class="archive-card-title">韬唤鍚嶇墖</div></div>
                <div class="profile-snapshot"><div class="identity-card"><h3>褰撳墠瑙掕壊</h3>${makeTileGrid([
                  { label: '褰撳墠韬唤', value: '' },
                  { label: '鍚嶆湜灞傜骇', value: '' },
                  { label: '鍏紑鎯呮姤', value: '' },
                  { label: '涓昏鍦堝眰', value: '' }
                ], 'two')}</div></div>
              </div>
              <div class="archive-card">
                <div class="archive-card-head"><div class="archive-card-title">绀句細鏍囩</div></div>
                ${makeTagCloud([])}
              </div>
              <div class="archive-card full">
                <div class="archive-card-head"><div class="archive-card-title">褰撳墠绀句細浣嶇疆</div></div>
                ${makeTileGrid([
                  { label: '涓诲叕寮€韬唤', value: '' },
                  { label: '鍚嶆湜绛夌骇', value: '' },
                  { label: '涓昏鍦堝眰', value: '' },
                  { label: '鍏紑搴?, value: '' },
                  { label: '闃佃惀鍏宠仈', value: '' },
                  { label: '澶栭儴鍗拌薄', value: '' }
                ])}
              </div>
            </div>
          `
        };
      }

      if (key === '鎵€灞炲娍鍔涜缁嗛〉' || key === '鎴戠殑闃佃惀璇︽儏') {
        return {
          title: key === '鎴戠殑闃佃惀璇︽儏' ? '鎴戠殑闃佃惀寮圭獥' : '闃佃惀韬唤寮圭獥',
          body: `
            <div class="archive-modal-grid faction-matrix-grid">
              <div class="archive-card full">
                <div class="archive-card-head"><div class="archive-card-title">鎵€灞為樁姊?/div></div>
                ${makeFactionLadder([
                  { name: '瀛﹂櫌', desc: '', className: 'highlight' },
                  { name: '瀹楅棬', desc: '', className: '' },
                  { name: '瀹樻柟', desc: '', className: '' },
                  { name: '鐗规畩', desc: '', className: '' }
                ])}
              </div>
              <div class="archive-card">
                <div class="archive-card-head"><div class="archive-card-title">褰撳墠闃佃惀浣嶇疆</div></div>
                ${makeTileGrid([
                  { label: '褰撳墠鎵€灞?, value: '' },
                  { label: '韬唤', value: '' },
                  { label: '鏉冮檺绾?, value: '' },
                  { label: '涓昏缁戝畾', value: '' }
                ], 'two')}
              </div>
            </div>
          `
        };
      }

      if (key === '浜虹墿鍏崇郴璇︾粏椤?) {
        return {
          title: '浜虹墿鍏崇郴寮圭獥',
          body: `
            <div class="archive-modal-grid" style="grid-template-columns:1fr;">
              <div class="archive-card full">
                <div class="archive-card-head"><div class="archive-card-title">缇佺粖鏄熻建鍥?/div></div>
                <div class="topology-board" style="min-height:280px;">
                  <svg class="topology-svg" viewBox="0 0 100 100" preserveAspectRatio="none">
                    <line class="topology-link cyan" x1="50" y1="50" x2="20" y2="25"></line>
                    <line class="topology-link gold" x1="50" y1="50" x2="50" y2="18"></line>
                    <line class="topology-link cyan" x1="50" y1="50" x2="82" y2="25"></line>
                    <line class="topology-link alert" x1="50" y1="50" x2="25" y2="75"></line>
                    <line class="topology-link cyan" x1="50" y1="50" x2="75" y2="75"></line>
                  </svg>
                  <div class="topology-node center" style="left:50%;top:50%"><b>褰撳墠瑙掕壊</b><span>鑷垜</span></div>
                  <div class="topology-node interactive-ring" style="left:20%;top:25%"><b></b><span></span></div>
                  <div class="topology-node interactive-ring gold" style="left:50%;top:18%"><b></b><span></span></div>
                  <div class="topology-node interactive-ring" style="left:82%;top:25%"><b></b><span></span></div>
                  <div class="topology-node interactive-ring warn hover-up" style="left:25%;top:75%"><b></b><span></span></div>
                  <div class="topology-node interactive-ring hover-up" style="left:75%;top:75%"><b></b><span></span></div>
                </div>
              </div>
              <div class="archive-card full">
                <div class="archive-card-head"><div class="archive-card-title">鏍稿績鎺ㄨ繘绾跨储</div></div>
                <div class="intel-layout"><div class="intel-card"><b></b><span></span></div><div class="intel-card"><b></b><span></span></div></div>
              </div>
            </div>
          `
        };
      }

      if (key === '鎯呮姤搴撹缁嗛〉') {
        return {
          title: '鎯呮姤搴撳脊绐?,
          body: `
            <div class="archive-modal-grid" style="grid-template-columns:1fr;">
              <div class="archive-card full">
                <div class="archive-card-head"><div class="archive-card-title">绾跨储鎷撴墤缃戠粶</div></div>
                <div class="intel-network-board"></div>
              </div>
              <div class="archive-card full">
                <div class="archive-card-head"><div class="archive-card-title">鎯呮姤姒傝</div></div>
                <div class="intel-progress-slots" style="display:grid; grid-template-columns:repeat(3, minmax(0, 1fr)); gap:12px;">
                  <div class="intel-slot" style="min-height:0; padding:12px 14px;"><div class="intel-slot-name">宸茶В閿佹儏鎶?/div><div class="intel-slot-bar-wrap" style="height:6px; margin:8px 0 10px;"><div class="intel-slot-bar" style="width:0%;"></div></div><div class="intel-slot-value">0</div></div>
                  <div class="intel-slot" style="min-height:0; padding:12px 14px;"><div class="intel-slot-name">鏈€鏂版儏鎶?/div><div class="intel-slot-bar-wrap" style="height:6px; margin:8px 0 10px;"><div class="intel-slot-bar gold" style="width:0%;"></div></div><div class="intel-slot-value" style="color:var(--gold);">0</div></div>
                  <div class="intel-slot" style="min-height:0; padding:12px 14px;"><div class="intel-slot-name">浠诲姟璁板綍</div><div class="intel-slot-bar-wrap" style="height:6px; margin:8px 0 10px;"><div class="intel-slot-bar" style="width:0%; background:var(--red); box-shadow:0 0 8px var(--red);"></div></div><div class="intel-slot-value" style="color:var(--red);">0</div></div>
                </div>
              </div>
            </div>
          `
        };
      }

      if (key === '姝﹁宸ュ潑璇︾粏椤?) {
        return {
          title: '姝﹁宸ュ潑寮圭獥',
          body: `
            <div class="equipment-layout">
              <div class="archive-card full">
                <div class="archive-card-head"><div class="archive-card-title">褰撳墠瑁呭鎬昏</div></div>
                ${makeTileGrid([
                  { label: '鏂楅摖', value: '' },
                  { label: '鏈虹敳', value: '' },
                  { label: '涓绘鍣?, value: '' },
                  { label: '鍓亴涓?, value: '' },
                  { label: '鎴樻枟褰㈡€?, value: '' }
                ])}
              </div>
              <div class="archive-card">
                <div class="archive-card-head"><div class="archive-card-title">瑁呭妲戒綅鍒嗗竷</div></div>
                ${makeFigureBoard('褰撳墠瑁呰浇', [
                  { x: 50, y: 12, label: '澶寸洈', className: 'off' },
                  { x: 50, y: 34, label: '鑳搁摖', className: 'off' },
                  { x: 24, y: 34, label: '宸﹁偐', className: 'off' },
                  { x: 76, y: 34, label: '鍙宠偐', className: 'off' },
                  { x: 18, y: 52, label: '宸﹁噦', className: 'off' },
                  { x: 82, y: 52, label: '鍙宠噦', className: 'off' },
                  { x: 34, y: 82, label: '宸﹁吙', className: 'off' },
                  { x: 66, y: 82, label: '鍙宠吙', className: 'off' },
                  { x: 50, y: 68, label: '鎴樿', className: 'off' },
                  { x: 50, y: 94, label: '鎴橀澊', className: 'off' },
                  { x: 82, y: 10, label: '鎴掓寚', className: 'off' }
                ])}
              </div>
            </div>
          `
        };
      }

      if (key === '鍌ㄧ墿浠撳簱璇︾粏椤?) {
        return {
          title: '鍌ㄧ墿浠撳簱',
          body: `
            <div class="archive-modal-grid">
              <div class="archive-card full">
                <div class="archive-card-head"><div class="archive-card-title">閽卞寘鏉?/div></div>
                ${makeWalletStrip([
                  { label: '鑱旈偊甯?, value: '', className: 'gold' },
                  { label: '鏄熺綏甯?, value: '', className: 'cyan' },
                  { label: '鍞愰棬绉垎', value: '', className: 'cyan' },
                  { label: '瀛﹂櫌绉垎', value: '', className: 'cyan' },
                  { label: '鎴樺姛', value: '', className: 'red' }
                ])}
              </div>
              <div class="archive-card full vault-main-card">
                <div class="archive-card-head"><div class="archive-card-title">鑳屽寘鏍奸樀鍒?/div></div>
                <div class="inventory-scroll-shell">${makeInventoryGrid([])}</div>
              </div>
            </div>
          `
        };
      }

      if (key === '绗竴姝﹂瓊璇︾粏椤? || key === '绗簩姝﹂瓊璇︾粏椤?) {
        const badge = key === '绗竴姝﹂瓊璇︾粏椤? ? '绗竴姝﹂瓊' : '绗簩姝﹂瓊';
        const badgeClass = key === '绗竴姝﹂瓊璇︾粏椤? ? 'live' : 'warn';
        return {
          title: '姝﹂瓊',
          body: `
            <div class="archive-modal-grid">
              <div class="archive-card full">
                <div class="archive-card-head"><div class="archive-card-title">姝﹂瓊</div></div>
                <div class="spirit-main-card"><h4></h4><div class="spirit-head-tags"><span class="tag-chip ${key === '绗竴姝﹂瓊璇︾粏椤? ? 'live' : 'warn'}">${badge}</span></div></div>
              </div>
              <div class="archive-card full spirit-flow-card">
                <div class="archive-card-head"><div class="archive-card-title">榄傜伒</div></div>
                <div class="rings soul-ring-lane">${buildEmptyRingLane(key === '绗竴姝﹂瓊璇︾粏椤? ? 'ring-white' : 'ring-gold')}</div>
              </div>
            </div>
          `
        };
      }

      if (key === '姝﹂瓊铻嶅悎鎶€璇︾粏椤?) {
        return {
          title: '姝﹂瓊铻嶅悎鎶€妗ｆ',
          body: `
            <div class="archive-modal-grid dossier-shell">
              <div class="archive-card dossier-card">
                <div class="archive-card-head"><div class="archive-card-title">铻嶅悎鎶€姒傝</div></div>
                <section class="dossier-section">
                  <div class="dossier-section-title">鍩虹缁熻</div>
                  ${makeDossierRows([
                    { label: '鎬绘敹褰?, value: '' },
                    { label: '鍙屼汉铻嶅悎', value: '' },
                    { label: '鑷綋铻嶅悎', value: '' },
                    { label: '褰撳墠瑙掕壊', value: '' }
                  ], 'dossier-row-grid--two')}
                </section>
              </div>
              <div class="archive-card dossier-card">
                <div class="archive-card-head"><div class="archive-card-title">铻嶅悎鎶€娓呭崟</div></div>
                <section class="dossier-section">
                  <div class="dossier-section-title">鎶€鑳芥潯鐩?/div>
                  <div class="dossier-empty-note">铻嶅悎鎶€鍐呭姝ｅ湪鏁寸悊涓€?/div>
                </section>
              </div>
            </div>
          `
        };
      }

      if (key === '琛€鑴夊皝鍗拌缁嗛〉') {
        return {
          title: '琛€鑴夊皝鍗板脊绐?,
          body: `
            <div class="archive-modal-grid">
              <div class="archive-card full">
                <div class="archive-card-head"><div class="archive-card-title">琛€鑴夋湰浣?/div></div>
                <div class="spirit-main-card"><h4></h4><div class="spirit-head-tags"><span class="tag-chip warn">琛€鑴夊皝鍗?/span></div></div>
              </div>
              <div class="archive-card">${makeSealColumn([
                { label: '绗?灞傚皝鍗?, state: '', className: 'locked' },
                { label: '绗?灞傚皝鍗?, state: '', className: 'locked' },
                { label: '绗?灞傚皝鍗?, state: '', className: 'locked' },
                { label: '绗?灞傚皝鍗?, state: '', className: 'locked' }
              ])}</div>
              <div class="archive-card full spirit-flow-card"><div class="archive-card-head"><div class="archive-card-title">閲戣壊榄傜幆杞ㄩ亾</div></div><div class="orbit-track">${buildEmptyRingLane('ring-gold', 3)}</div></div>
            </div>
          `
        };
      }

      if (key === '缂栧勾鍙叉。妗?) {
        return {
          title: '缂栧勾鍙插脊绐?,
          body: `
            <div class="archive-modal-grid" style="grid-template-columns:1fr;">
              <div class="archive-card full">
                <div class="archive-card-head"><div class="archive-card-title">瀹忚鏃堕棿杞?/div></div>
                <div class="history-timeline-wrap"><div class="history-timeline-track"><div class="history-node major"><div class="history-node-date"></div><div class="history-node-dot"></div><div class="history-node-label"></div></div><div class="history-node"><div class="history-node-date"></div><div class="history-node-dot"></div><div class="history-node-label"></div></div><div class="history-node"><div class="history-node-date"></div><div class="history-node-dot"></div><div class="history-node-label"></div></div></div></div>
                <div class="history-floating-card"><div class="history-floating-title">褰撳墠閿氱偣</div><div class="history-floating-desc"></div></div>
              </div>
              <div class="archive-card full"><div class="archive-card-head"><div class="archive-card-title">杩戞湡璇︾粏鏃ュ織</div></div>${makeTimelineStack([])}</div>
            </div>
          `
        };
      }

      if (key === '鏈湴鎹偣璇︽儏' || key === '褰撳墠鑺傜偣璇︽儏' || isMapNode) {
        return {
          title: isMapNode ? `鏈湴鎹偣 / ${nodeName}` : (key === '褰撳墠鑺傜偣璇︽儏' ? '褰撳墠鑺傜偣璇︽儏' : '鏈湴鎹偣 / 褰撳墠鑺傜偣'),
          body: `
            <div class="intel-layout">
              <div class="archive-card"><div class="archive-card-head"><div class="archive-card-title">鎹偣姒傝</div></div>${makeTileGrid([
                { label: '鎵€鍦ㄥ湴鐐?, value: '' },
                { label: '鎺屾帶鍔垮姏', value: '' },
                { label: '甯镐綇浜哄彛', value: '' },
                { label: '缁忔祹鐘跺喌', value: '' },
                { label: '瀹堟姢鍐涘洟', value: '' },
                { label: '鍟嗗簵鏁伴噺', value: '' }
              ])}</div>
              <div class="archive-card"><div class="archive-card-head"><div class="archive-card-title">椹诲湴姘涘洿</div></div><div class="relation-side-list"><div class="relation-card"><b>琛楀尯绉╁簭</b><span></span></div><div class="relation-card"><b>琛ョ粰鎯呭喌</b><span></span></div><div class="relation-card"><b>浜ら€氱姸鎬?/b><span></span></div></div></div>
            </div>
          `
        };
      }

      if (key === '鎿嶄綔鎬荤嚎') {
        return {
          title: '杩戞湡瀹夋帓',
          body: `
            <div class="archive-modal-grid" style="grid-template-columns:1fr;">
              <div class="archive-card full"><div class="archive-card-head"><div class="archive-card-title">褰撳墠鍔ㄤ綔</div></div>${makeTileGrid([
                { label: '褰撳墠琛屽姩', value: '' },
                { label: '鎵€鍦ㄤ綅缃?, value: '' },
                { label: '浼ゅ娍', value: '' },
                { label: '寰呭鐞嗚姹?, value: '' }
              ], 'two')}</div>
              <div class="archive-card full"><div class="archive-card-head"><div class="archive-card-title">璇锋眰闃熷垪</div></div>${makeTimelineStack([])}</div>
            </div>
          `
        };
      }

      if (key === '璇曠偧涓庢儏鎶?) {
        return {
          title: '璇曠偧涓庢儏鎶?,
          body: `
            <div class="intel-layout">
              <div class="archive-card"><div class="archive-card-head"><div class="archive-card-title">鍙墠寰€璇曠偧</div></div>${makeTileGrid([
                { label: '鍗囩伒鍙伴棬绁?, value: '' },
                { label: '榄傜伒濉旀渶楂?, value: '' },
                { label: '鍥鹃壌鏉＄洰', value: '' },
                { label: '椋庨櫓璇勪及', value: '' }
              ])}</div>
              <div class="archive-card"><div class="archive-card-head"><div class="archive-card-title">宸叉帉鎻℃儏鎶?/div></div><div class="intel-cabinet"></div></div>
            </div>
          `
        };
      }

      if (key === '杩戞湡瑙侀椈') {
        return {
          title: '杩戞湡瑙侀椈 / 鍩庡唴绠€鎶?,
          body: `
            <div class="archive-modal-grid" style="grid-template-columns:1fr;">
              <div class="archive-card full"><div class="archive-card-head"><div class="archive-card-title">鍩庡唴绠€鎶?/div></div>${makeTimelineStack([
                { title: '瀛﹂櫌', desc: '' },
                { title: '鍦板尯', desc: '' },
                { title: '鍟嗚矾', desc: '' },
                { title: '浜ら€?, desc: '' }
              ])}</div>
            </div>
          `
        };
      }

      if (key === '涓栫晫鐘舵€佹€昏') {
        return {
          title: '涓栫晫鐘舵€佸脊绐?,
          body: `
            <div class="archive-modal-grid">
            <div class="archive-modal-grid" style="grid-template-columns:1fr;">
              <div class="archive-card full"><div class="archive-card-head"><div class="archive-card-title">鍔垮姏姊樀</div></div>${makeFactionLadder([
                { name: '瀛﹂櫌', desc: '', className: 'highlight' },
                { name: '瀹楅棬', desc: '', className: '' },
                { name: '瀹樻柟', desc: '', className: '' },
                { name: '鐗规畩', desc: '', className: '' }
              ])}</div>
            </div>
          `
        };
      }

      if (key === '绯荤粺鎾姤涓庢棩蹇?) {
        return {
          title: '绯荤粺鎾姤 / 鏃ュ織寮圭獥',
          body: `
            <div class="archive-modal-grid" style="grid-template-columns:1fr;">
              <div class="archive-card full"><div class="archive-card-head"><div class="archive-card-title">绯荤粺骞挎挱</div></div>${makeTimelineStack([
                { title: '鏈€杩戞挱鎶?, desc: '' },
                { title: '鏈€杩戜簨浠?, desc: '' },
                { title: '瀹夋帓鎽樿', desc: '' },
                { title: '鎯呮姤鎽樿', desc: '' }
              ])}</div>
            </div>
          `
        };
      }

      return null;
    }

    let activeBattleUI = null;
    const BATTLE_INLINE_PREVIEW_KEY = '鎴樻枟缁堢';
    const PENDING_SOUL_RING_PREVIEW_KEY = '榄傜幆鐢熸垚纭';
    let battleInlineDismissed = false;
    let battleInlineMountToken = 0;
    let battleReturnEntrySyncToken = 0;
    let pendingBattleInlineMount = null;
    let playerNameRepairInFlight = '';
    let liveSnapshot = null;
    let lastRenderableSnapshot = null;
    let preferredActiveCharacterName = '';
    let currentModalPreviewKey = '';
    let currentUnifiedPreviewKey = '';
    let lastRenderedUnifiedPreviewKey = '';
    let mapDispatchContext = null;
    let activeSubUI = null;
    let lastAutoTradeRequestSignature = '';
    let hasHydratedAutoTradeRequestSignature = false;
    let lastAutoOpenedPendingSoulRingSignature = '';
    let lastRenderedShellPreviewKey = '';
    let activeInlineEditState = null;
    let inlineEditSessionToken = 0;
    let inlineEditGuardUntil = 0;
    let pendingLiveRefresh = false;
    let skillDesignerDraftStateByPreviewKey = Object.create(null);
    let lastHeaderRenderSignature = '';
    let lastDashboardRenderSignature = '';
    let lastDashboardSectionRenderSignatures = null;
    let liveUiRefCache = new Map();
    let 鍙帺閫夐」鐭攣鎴鏃堕棿 = 0;
    let 鍙帺閫夐」鐭攣閿?= '';
    let 鎱㈠埛鏂伴鏋跺凡鍚敤 = false;
    const 鎱㈠埛鏂伴鏋堕瑙堥敭鍒楄〃 = Object.freeze(['绯荤粺鎾姤涓庢棩蹇?, '鎿嶄綔鎬荤嚎', '璇曠偧涓庢儏鎶?]);
    const 鎱㈠埛鏂伴鏋堕瑙堥敭闆嗗悎 = new Set(鎱㈠埛鏂伴鏋堕瑙堥敭鍒楄〃);

    function htmlEscape(value) {
      return String(value == null ? '' : value)
        .replace(/&/g, '&amp;')
        .replace(/</g, '&lt;')
        .replace(/>/g, '&gt;')
        .replace(/"/g, '&quot;')
        .replace(/'/g, '&#39;');
    }

    function hasUiPlaceholderToken(value) {
      const text = String(value === undefined || value === null ? '' : value).trim();
      return !!text && (/寰呰ˉ鍏?i.test(text));
    }

    function toText(value, fallback = '鏃?) {
      if (value === undefined || value === null || value === '') return fallback;
      const text = String(value);
      return hasUiPlaceholderToken(text) ? fallback : text;
    }

    function toNumber(value, fallback = 0) {
      const parsed = Number(value);
      return Number.isFinite(parsed) ? parsed : fallback;
    }

    const 妯″潡渚濊禆鏄犲皠 = Object.freeze({
      浜ゆ槗缃戠粶: ['浜ゆ槗妯″潡'],
      浜ゆ槗妯″潡寮圭獥: ['浜ゆ槗妯″潡'],
      褰撳墠鑺傜偣璇︽儏: ['浜ゆ槗妯″潡', '鍦板浘妯″潡'],
      鍥惧眰鎺у埗涓庤窇鍥? ['鍦板浘妯″潡'],
      鍏ㄦ伅鏄熷浘涓荤敾甯? ['鍦板浘妯″潡'],
      鍔ㄦ€佸湴鐐逛笌鎵╁睍鑺傜偣: ['鍦板浘妯″潡'],
      姝﹁宸ュ潑璇︾粏椤? ['鑱屼笟妯″潡'],
      鎴樻枟缁堢: ['鎴樻枟妯″潡']
    });
    const 棰勮渚濊禆浠诲姟琛?= new Map();

    function 鑾峰彇杞藉叆鍣ㄥ€欓€夌獥鍙?) {
      const 鍒楄〃 = [window];
      try {
        if (window.parent && window.parent !== window) 鍒楄〃.push(window.parent);
      } catch (閿欒) {}
      try {
        if (window.top && window.top !== window && !鍒楄〃.includes(window.top)) 鍒楄〃.push(window.top);
      } catch (閿欒) {}
      return 鍒楄〃;
    }

    function 璇诲彇绐楀彛鍑芥暟(閿悕) {
      for (const 鐩爣绐楀彛 of 鑾峰彇杞藉叆鍣ㄥ€欓€夌獥鍙?)) {
        const 鍑芥暟鍊?= 鐩爣绐楀彛 && typeof 鐩爣绐楀彛 === 'object' ? 鐩爣绐楀彛[閿悕] : null;
        if (typeof 鍑芥暟鍊?=== 'function') return 鍑芥暟鍊?bind(鐩爣绐楀彛);
      }
      return null;
    }

    function 璇诲彇妯″潡鐘舵€?妯″潡鍚? {
      const 鍚嶇О = String(妯″潡鍚?|| '').trim();
      if (!鍚嶇О) return null;
      for (const 鐩爣绐楀彛 of 鑾峰彇杞藉叆鍣ㄥ€欓€夌獥鍙?)) {
        const 鐘舵€佽〃 = 鐩爣绐楀彛 && typeof 鐩爣绐楀彛 === 'object' ? 鐩爣绐楀彛.__LWCS_妯″潡鐘舵€乢_ : null;
        if (鐘舵€佽〃 && typeof 鐘舵€佽〃 === 'object' && 鐘舵€佽〃[鍚嶇О]) {
          return 鐘舵€佽〃[鍚嶇О];
        }
      }
      return null;
    }

    function 妯″潡鏄惁宸插姞杞?妯″潡鍚? {
      const 鐘舵€?= 璇诲彇妯″潡鐘舵€?妯″潡鍚?;
      return !!(鐘舵€?&& 鐘舵€?鐘舵€?=== 'loaded');
    }

    function 鑾峰彇棰勮渚濊禆妯″潡鍒楄〃(棰勮閿? {
      const 閿?= String(棰勮閿?|| '').trim();
      if (!閿? return [];
      if (閿?startsWith('鍦板浘鑺傜偣锛?)) return ['鍦板浘妯″潡'];
      return Array.isArray(妯″潡渚濊禆鏄犲皠[閿甝) ? 妯″潡渚濊禆鏄犲皠[閿甝 : [];
    }

    async function 纭繚妯″潡渚濊禆宸插姞杞?妯″潡鍚? 鏉ユ簮 = 'bridge') {
      const 鍚嶇О = String(妯″潡鍚?|| '').trim();
      if (!鍚嶇О) return { ok: false, reason: 'empty_module_name' };
      if (妯″潡鏄惁宸插姞杞?鍚嶇О)) return { ok: true, 妯″潡鍚? 鍚嶇О, cached: true };
      const 鍔犺浇鍑芥暟 = 璇诲彇绐楀彛鍑芥暟('__LWCS_纭繚妯″潡宸插姞杞絖_');
      if (typeof 鍔犺浇鍑芥暟 !== 'function') {
        return { ok: false, 妯″潡鍚? 鍚嶇О, reason: 'loader_unavailable' };
      }
      try {
        const 缁撴灉 = await Promise.resolve(鍔犺浇鍑芥暟(鍚嶇О, { 鏉ユ簮, 鍏佽澶辫触闄嶇骇: true }));
        return 缁撴灉 && typeof 缁撴灉 === 'object'
          ? 缁撴灉
          : { ok: !!缁撴灉, 妯″潡鍚? 鍚嶇О, reason: 缁撴灉 ? '' : 'loader_returned_false' };
      } catch (閿欒) {
        return { ok: false, 妯″潡鍚? 鍚嶇О, reason: 閿欒 && 閿欒.message ? 閿欒.message : 'loader_exception', error: 閿欒 };
      }
    }

    function 璇锋眰棰勭儹棰勮渚濊禆(棰勮閿? 鏉ユ簮 = 'preview_open') {
      const 閿?= String(棰勮閿?|| '').trim();
      if (!閿? return Promise.resolve({ ok: true, skipped: true });
      const 浠诲姟閿?= 閿?
      if (棰勮渚濊禆浠诲姟琛?has(浠诲姟閿?) return 棰勮渚濊禆浠诲姟琛?get(浠诲姟閿?;

      const 棰勮鍔犺浇鍑芥暟 = 璇诲彇绐楀彛鍑芥暟('__LWCS_纭繚棰勮渚濊禆宸插姞杞絖_');
      const 缂哄け妯″潡鍒楄〃 = 鑾峰彇棰勮渚濊禆妯″潡鍒楄〃(閿?.filter(妯″潡鍚?=> !妯″潡鏄惁宸插姞杞?妯″潡鍚?);
      if (!棰勮鍔犺浇鍑芥暟 && !缂哄け妯″潡鍒楄〃.length) {
        return Promise.resolve({ ok: true, skipped: true });
      }

      const 浠诲姟 = (async () => {
        if (typeof 棰勮鍔犺浇鍑芥暟 === 'function') {
          const 缁撴灉 = await Promise.resolve(棰勮鍔犺浇鍑芥暟(閿? { 鏉ユ簮 }));
          return 缁撴灉 && typeof 缁撴灉 === 'object'
            ? 缁撴灉
            : { ok: !!缁撴灉, 棰勮閿? 閿? reason: 缁撴灉 ? '' : 'preview_loader_failed' };
        }
        const 缁撴灉鍒楄〃 = [];
        for (const 妯″潡鍚?of 缂哄け妯″潡鍒楄〃) {
          const 缁撴灉 = await 纭繚妯″潡渚濊禆宸插姞杞?妯″潡鍚? `${鏉ユ簮}:${閿畗`);
          缁撴灉鍒楄〃.push(缁撴灉);
        }
        return { ok: 缁撴灉鍒楄〃.every(item => item && item.ok), 棰勮閿? 閿? results: 缁撴灉鍒楄〃 };
      })()
        .catch(閿欒 => ({ ok: false, 棰勮閿? 閿? reason: 閿欒 && 閿欒.message ? 閿欒.message : 'preview_loader_exception', error: 閿欒 }))
        .finally(() => {
          棰勮渚濊禆浠诲姟琛?delete(浠诲姟閿?;
        });

      棰勮渚濊禆浠诲姟琛?set(浠诲姟閿? 浠诲姟);
      return 浠诲姟;
    }

    function formatCultivationLevelValue(value, fallback = '0') {
      const parsed = Number(value);
      if (Number.isFinite(parsed)) {
        if (Math.abs(parsed - 99.5) < 0.001) return '鍑嗙';
        return String(parsed);
      }
      const text = toText(value, fallback);
      return text || fallback;
    }

    function formatCultivationLevelBadge(value, fallback = '0') {
      return `Lv.${formatCultivationLevelValue(value, fallback)}`;
    }

    function isSkillDesignerPreviewKey(previewKey = '') {
      return String(previewKey || '').startsWith('鎶€鑳借璁″彴锛?);
    }

    function readCachedSkillDesignerDraft(previewKey = '') {
      const key = String(previewKey || '').trim();
      if (!key) return null;
      const draft = skillDesignerDraftStateByPreviewKey[key];
      return draft && typeof draft === 'object' ? cloneJsonValue(draft) : null;
    }

    function writeCachedSkillDesignerDraft(previewKey = '', draft = {}) {
      const key = String(previewKey || '').trim();
      if (!key) return;
      skillDesignerDraftStateByPreviewKey[key] = cloneJsonValue(draft && typeof draft === 'object' ? draft : {});
    }

    function clearCachedSkillDesignerDraft(previewKey = '') {
      const key = String(previewKey || '').trim();
      if (!key) return;
      delete skillDesignerDraftStateByPreviewKey[key];
    }

    function clearCachedSkillDesignerDrafts(previewKeys = []) {
      (Array.isArray(previewKeys) ? previewKeys : []).forEach(previewKey => {
        if (isSkillDesignerPreviewKey(previewKey)) clearCachedSkillDesignerDraft(previewKey);
      });
    }

    function formatTickToCalendarDateText(tickValue) {
      const safeTick = Math.max(0, toNumber(tickValue, 0));
      const totalMinutes = safeTick * 10;
      const days = Math.floor(totalMinutes / (24 * 60));
      const years = Math.floor(days / 360);
      const months = Math.floor((days % 360) / 30) + 1;
      const currentDay = (days % 30) + 1;
      const remainderMinutes = totalMinutes % (24 * 60);
      const hours = Math.floor(remainderMinutes / 60);
      const mins = remainderMinutes % 60;
      return `鏂楃綏鍘?{20000 + years}骞?{months}鏈?{currentDay}鏃?${hours.toString().padStart(2, '0')}:${mins.toString().padStart(2, '0')}`;
    }

    function getSnapshotWorldTimeText(snapshot) {
      const explicit = toText(
        deepGet(snapshot, 'rootData.world.鏃堕棿._calendar', deepGet(snapshot, 'rootData.world.鏃堕棿.calendar', '')),
        '',
      ).trim();
      if (explicit) return explicit;
      return formatTickToCalendarDateText(deepGet(snapshot, 'rootData.world.鏃堕棿.tick', 0));
    }

    function getLatestUnlockedIntelText(snapshot, limit = 12, fallback = '鏆傛棤') {
      const list = Array.isArray(snapshot && snapshot.unlockedKnowledges) ? snapshot.unlockedKnowledges : [];
      if (!list.length) return fallback;
      return shortenText(toText(list[list.length - 1], fallback), limit) || fallback;
    }

    function getStatusActionEditorOptions(currentValue = '') {
      const current = toText(currentValue, '').trim();
      if (current === '鍑濊仛榄傛牳') return [...STATUS_ACTION_RUNTIME_OPTIONS];
      if (current && !STATUS_ACTION_RUNTIME_OPTIONS.includes(current)) {
        return [current, ...STATUS_ACTION_RUNTIME_OPTIONS];
      }
      return [...STATUS_ACTION_RUNTIME_OPTIONS];
    }

    function formatNumber(value) {
      return new Intl.NumberFormat('zh-CN').format(toNumber(value, 0));
    }

    function getBaseSpMaxForLevel(lv) {
      const level = toNumber(lv, 0);
      let spBase = 100;
      if (level <= 10) spBase = 100 + (level - 1) * 73.66;
      else if (level <= 30) spBase = 763 + (level - 10) * 161.85;
      else if (level <= 50) spBase = 4000 + Math.pow(level - 30, 2) * 27.5;
      else if (level <= 70) spBase = 15000 + (level - 50) * 1000;
      else if (level <= 90) spBase = 35000 + (level - 70) * 1250;
      else if (level <= 95) spBase = 60000 + (level - 90) * 6818;
      else if (level < 98) spBase = 94090 + (level - 95) * 15303.3;
      else if (level === 98) spBase = 140000;
      else if (level === 99) spBase = 200000;
      else if (level === 99.5) spBase = 400000;
      else if (level >= 100) spBase = 700000;
      return Math.floor(spBase);
    }

    function getCharacterBaseSoulPowerTypeMultiplierForUi(charData = {}) {
      let maxTypeMult = 0;
      const typeMap = {
        寮烘敾绯? 1.0,
        闃插尽绯? 1.0,
        鏁忔敾绯? 1.0,
        鎺у埗绯? 1.0,
        杈呭姪绯? 1.2,
        椋熺墿绯? 1.2,
        娌荤枟绯? 1.2,
        绮剧绯? 1.0,
        鍏冪礌绯? 1.0,
      };
      Object.values(charData && charData.姝﹂瓊 && typeof charData.姝﹂瓊 === 'object' ? charData.姝﹂瓊 : {}).forEach(spiritData => {
        if (!spiritData || typeof spiritData !== 'object') return;
        const spiritType = toText(spiritData.type, '');
        maxTypeMult = Math.max(maxTypeMult, toNumber(typeMap[spiritType], 0));
      });
      if (maxTypeMult <= 0) {
        const selfType = toText(charData && deepGet(charData, '灞炴€?绯诲埆', ''), '');
        maxTypeMult = toNumber(typeMap[selfType], 1.0);
      }
      return Math.max(0.1, maxTypeMult || 1.0);
    }

    function getCharacterDualSpiritSoulPowerCoeffForUi(charData = {}) {
      const spiritEntries = Object.entries(charData && charData.姝﹂瓊 && typeof charData.姝﹂瓊 === 'object' ? charData.姝﹂瓊 : {})
        .filter(([, spiritData]) => spiritData && typeof spiritData === 'object');
      return spiritEntries.length >= 2 ? 1.2 : 1.0;
    }

    function getNextLevelSoulRequirement(stat, charData = null) {
      const currentLv = toNumber(stat && stat.绛夌骇, 0);
      if (currentLv >= 100) {
        return { needed: 0, nextLevel: currentLv, isMax: true };
      }
      const currentSpMax = toNumber(
        stat && (stat.绐佺牬榄傚姏涓婇檺 ?? stat.鍩虹榄傚姏涓婇檺 ?? stat.榄傚姏涓婇檺),
        0
      );
      const safeCharData = charData && typeof charData === 'object' ? charData : {};
      const nextLevel = currentLv + 1;
      const hiddenVar = Math.max(0.1, toNumber(deepGet(safeCharData, '灞炴€?搴曞瓙娉㈠姩', 1), 1));
      const nextSpMax = Math.floor(
        getBaseSpMaxForLevel(nextLevel) *
        getCharacterBaseSoulPowerTypeMultiplierForUi(safeCharData) *
        getCharacterDualSpiritSoulPowerCoeffForUi(safeCharData) *
        hiddenVar
      );
      return {
        needed: Math.max(0, nextSpMax - currentSpMax),
        nextLevel,
        isMax: false
      };
    }

    function getCharacterActualSoulRingCountForUi(charData = {}) {
      let total = 0;
      Object.values(charData && charData.姝﹂瓊 && typeof charData.姝﹂瓊 === 'object' ? charData.姝﹂瓊 : {}).forEach(spiritData => {
        if (!spiritData || typeof spiritData !== 'object') return;
        Object.values(spiritData.榄傜伒 && typeof spiritData.榄傜伒 === 'object' ? spiritData.榄傜伒 : {}).forEach(soulSpirit => {
          if (!soulSpirit || typeof soulSpirit !== 'object') return;
          Object.values(soulSpirit.榄傜幆 && typeof soulSpirit.榄傜幆 === 'object' ? soulSpirit.榄傜幆 : {}).forEach(ringData => {
            if (!ringData || typeof ringData !== 'object') return;
            const hasAge = toNumber(ringData.骞撮檺, 0) > 0;
            const hasSkill = Object.keys(ringData.榄傛妧 && typeof ringData.榄傛妧 === 'object' ? ringData.榄傛妧 : {}).length > 0;
            if (hasAge || hasSkill) total += 1;
          });
        });
        Object.values(spiritData.鐙珛榄傜幆 && typeof spiritData.鐙珛榄傜幆 === 'object' ? spiritData.鐙珛榄傜幆 : {}).forEach(ringData => {
          if (!ringData || typeof ringData !== 'object') return;
          const hasAge = toNumber(ringData.骞撮檺, 0) > 0;
          const hasSkill = Object.keys(ringData.榄傛妧 && typeof ringData.榄傛妧 === 'object' ? ringData.榄傛妧 : {}).length > 0;
          if (hasAge || hasSkill) total += 1;
        });
      });
      return Math.max(0, total);
    }

    function getCharacterSoulRingLevelCapForUi(charData = {}) {
      const ringCount = getCharacterActualSoulRingCountForUi(charData);
      return Math.min(100, Math.max(10, (ringCount + 1) * 10));
    }

    function getCharacterCoreLevelCapForUi(charData = {}) {
      const coreCount = toNumber(deepGet(charData, '榄傛牳.鏍稿績.鏁伴噺', 0), 0);
      if (coreCount >= 3) return 150;
      if (coreCount === 2) return 98;
      if (coreCount === 1) return 89;
      return 69;
    }

    function getNextLevelSoulRequirementWithCap(stat, charData = null) {
      const currentLv = toNumber(stat && stat.绛夌骇, 0);
      const safeCharData = charData && typeof charData === 'object' ? charData : {};
      const baseResult = getNextLevelSoulRequirement(stat, safeCharData);
      const ringCap = getCharacterSoulRingLevelCapForUi(safeCharData);
      const coreCap = getCharacterCoreLevelCapForUi(safeCharData);
      const levelCap = Math.min(ringCap, coreCap);
      if (currentLv >= levelCap) {
        const blockedByRing = ringCap <= coreCap;
        return {
          ...baseResult,
          levelCap,
          blocked: true,
          blockedBy: blockedByRing ? 'ring' : 'core',
        };
      }
      return {
        ...baseResult,
        levelCap,
        blocked: false,
        blockedBy: '',
      };
    }

    function ratioPercent(current, max) {
      const upper = Math.max(0, toNumber(max, 0));
      if (!upper) return 0;
      return Math.max(0, Math.min(100, Math.round((toNumber(current, 0) / upper) * 100)));
    }

    function getDisplayHpPair(stat = {}) {
      const hpMax = Math.max(1, toNumber(stat && (stat.HP涓婇檺 ?? stat.浣撳姏涓婇檺), 1));
      const hp = Math.max(0, Math.min(hpMax, toNumber(stat && (stat.HP ?? stat.浣撳姏), hpMax)));
      return { hp, hpMax };
    }

    function getDisplayWoundLabel(stat = {}) {
      const { hp, hpMax } = getDisplayHpPair(stat);
      const ratio = hp / Math.max(1, hpMax);
      if (ratio <= 0.05 && hp > 0) return '婵掓';
      if (ratio <= 0.2) return '閲嶄激';
      if (ratio <= 0.5) return '杞讳激';
      return '鏃?;
    }

    function normalizeInjurySeverity(value) {
      const text = toText(value, '杞?).trim();
      if (['杞?, '涓?, '閲?].includes(text)) return text;
      if (/閲?.test(text)) return '閲?;
      if (/涓?.test(text)) return '涓?;
      return '杞?;
    }

    function getDisplayInjuryEntries(status = {}) {
      const injuryMap =
        status && status.鍙椾激閮ㄤ綅 && typeof status.鍙椾激閮ㄤ綅 === 'object' && !Array.isArray(status.鍙椾激閮ㄤ綅)
          ? status.鍙椾激閮ㄤ綅
          : {};
      return Object.entries(injuryMap)
        .map(([part, injury]) => {
          const name = toText(part, '').trim();
          if (!name) return null;
          const info = injury && typeof injury === 'object' ? injury : {};
          return [
            name,
            {
              绋嬪害: normalizeInjurySeverity(info.绋嬪害),
              鎻忚堪: toText(info.鎻忚堪, '').trim(),
            },
          ];
        })
        .filter(Boolean);
    }

    function buildInjurySummaryText(status = {}, options = {}) {
      const entries = getDisplayInjuryEntries(status);
      const emptyText = toText(options.empty, '鏃?).trim() || '鏃?;
      if (!entries.length) return emptyText;
      const limit = Math.max(1, toNumber(options.limit, 2));
      const parts = entries
        .slice(0, limit)
        .map(([name, injury]) => `${shortenText(name, 6)}路${normalizeInjurySeverity(injury.绋嬪害)}`);
      if (entries.length > limit) parts.push(`绛?{entries.length}澶刞);
      return parts.join('銆?);
    }

    function buildInjuryCrudHtml(activeCharKey = '', status = {}) {
      const safeCharKey = toText(activeCharKey, '').trim();
      const entries = getDisplayInjuryEntries(status);
      const entryHtml = entries.length
        ? entries
          .map(([name, injury]) => {
            const basePath = ['char', safeCharKey, '鐘舵€?, '鍙椾激閮ㄤ綅', name];
            const renameMeta = { renameObjectKey: true, parentPath: ['char', safeCharKey, '鐘舵€?, '鍙椾激閮ㄤ綅'], oldKey: name };
            return `
              <div class="injury-entry">
                <div class="injury-entry-head">
                  <span class="injury-entry-name">${safeCharKey
                    ? makeInlineEditableValue(name, {
                      path: [...basePath],
                      kind: 'string',
                      rawValue: name,
                      editorMeta: renameMeta,
                    })
                    : htmlEscape(name)}</span>
                  <span class="injury-entry-degree">${safeCharKey
                    ? makeInlineEditableValue(normalizeInjurySeverity(injury.绋嬪害), {
                      path: [...basePath, '绋嬪害'],
                      kind: 'enum_select',
                      rawValue: normalizeInjurySeverity(injury.绋嬪害),
                      editorMeta: { options: ['杞?, '涓?, '閲?] },
                    })
                    : htmlEscape(normalizeInjurySeverity(injury.绋嬪害))}</span>
                  ${safeCharKey
                    ? `<button type="button" class="tag-chip injury-action-btn" data-injury-action="delete" data-injury-char="${escapeHtmlAttr(safeCharKey)}" data-injury-key="${escapeHtmlAttr(name)}">鍒犻櫎</button>`
                    : ''}
                </div>
                <div class="injury-entry-desc">${safeCharKey
                  ? makeInlineEditableValue(injury.鎻忚堪 || '鏈～鍐欐弿杩?, {
                    path: [...basePath, '鎻忚堪'],
                    kind: 'string',
                    rawValue: injury.鎻忚堪 || '',
                    multiline: true,
                  })
                  : htmlEscape(injury.鎻忚堪 || '鏈～鍐欐弿杩?)}</div>
              </div>`;
          })
          .join('')
        : `<div class="injury-empty">鏆傛棤鍙椾激閮ㄤ綅璁板綍</div>`;
      if (!safeCharKey) return `<div class="injury-crud-panel">${entryHtml}</div>`;
      return `
        <div class="injury-crud-panel">
          <div class="request-console-row injury-create-row" data-injury-panel="create">
            <input type="text" class="request-console-input injury-create-input" data-injury-input="name" placeholder="閮ㄤ綅鍚? />
            <select class="request-console-input injury-create-select" data-injury-input="degree">
              <option value="杞? selected>杞?/option>
              <option value="涓?>涓?/option>
              <option value="閲?>閲?/option>
            </select>
            <input type="text" class="request-console-input injury-create-input injury-create-desc" data-injury-input="desc" placeholder="鎻忚堪" />
            <button type="button" class="tag-chip live injury-action-btn" data-injury-action="add" data-injury-char="${escapeHtmlAttr(safeCharKey)}">鏂板</button>
          </div>
          ${entryHtml}
        </div>`;
    }

    function deepGet(source, path, fallback) {
      if (!source || !path) return fallback;
      const keys = Array.isArray(path) ? path : String(path).split('.');
      let current = source;
      for (const key of keys) {
        if (current == null || typeof current !== 'object' || !(key in current)) return fallback;
        current = current[key];
      }
      return current === undefined ? fallback : current;
    }

    function safeEntries(obj) {
      return obj && typeof obj === 'object' ? Object.entries(obj) : [];
    }

    function cloneJsonValue(value, fallback = {}) {
      try {
        return structuredClone(value);
      } catch (error) {}
      try {
        return JSON.parse(JSON.stringify(value));
      } catch (error) {}
      return fallback;
    }

    const TRAINED_BONUS_PREVIEW_FIELDS = Object.freeze(['鍔涢噺', '闃插尽', '鏁忔嵎', '浣撳姏涓婇檺', '绮剧鍔涗笂闄?]);
    const trainedBonusPreviewOverrides = new Map();

    function createTrackedStatPreviewMap(source = {}) {
      return {
        鍔涢噺: Math.floor(toNumber(source && source.鍔涢噺, 0)),
        闃插尽: Math.floor(toNumber(source && source.闃插尽, 0)),
        鏁忔嵎: Math.floor(toNumber(source && source.鏁忔嵎, 0)),
        浣撳姏涓婇檺: Math.floor(toNumber(source && source.浣撳姏涓婇檺, 0)),
        绮剧鍔涗笂闄? Math.floor(toNumber(source && source.绮剧鍔涗笂闄? 0)),
      };
    }

    function createTrackedStatDeltaMap(source = {}) {
      return {
        鍔涢噺: Math.floor(toNumber(source && source.鍔涢噺, 0)),
        闃插尽: Math.floor(toNumber(source && source.闃插尽, 0)),
        鏁忔嵎: Math.floor(toNumber(source && source.鏁忔嵎, 0)),
        浣撳姏涓婇檺: Math.floor(toNumber(source && source.浣撳姏涓婇檺, 0)),
        绮剧鍔涗笂闄? Math.floor(toNumber(source && source.绮剧鍔涗笂闄? 0)),
      };
    }

    function isTrainedBonusPreviewPath(path = []) {
      return Array.isArray(path)
        && path.length >= 5
        && path[0] === 'char'
        && path[2] === '灞炴€?
        && path[3] === '璁粌鍔犳垚'
        && TRAINED_BONUS_PREVIEW_FIELDS.includes(String(path[4] || '').trim());
    }

    function queueTrainedBonusPreviewOverride(path = [], previousValue = 0, nextValue = 0) {
      if (!isTrainedBonusPreviewPath(path)) return null;
      const charKey = String(path[1] || '').trim();
      const field = String(path[4] || '').trim();
      if (!charKey || !field) return null;

      const previousNumeric = Math.floor(toNumber(previousValue, 0));
      const nextNumeric = Math.floor(toNumber(nextValue, 0));
      const delta = nextNumeric - previousNumeric;
      if (!delta) return null;

      const hadPreviousEntry = trainedBonusPreviewOverrides.has(charKey);
      const previousEntry = hadPreviousEntry ? cloneJsonValue(trainedBonusPreviewOverrides.get(charKey), null) : null;
      const liveStat = liveSnapshot ? deepGet(liveSnapshot, ['rootData', 'char', charKey, '灞炴€?], {}) : {};
      const nextEntry = previousEntry && typeof previousEntry === 'object'
        ? previousEntry
        : { baseStats: createTrackedStatPreviewMap(liveStat), deltas: createTrackedStatDeltaMap() };

      if (!nextEntry.baseStats || typeof nextEntry.baseStats !== 'object') nextEntry.baseStats = createTrackedStatPreviewMap(liveStat);
      if (!nextEntry.deltas || typeof nextEntry.deltas !== 'object') nextEntry.deltas = createTrackedStatDeltaMap();
      nextEntry.deltas[field] = Math.floor(toNumber(nextEntry.deltas[field], 0) + delta);
      trainedBonusPreviewOverrides.set(charKey, nextEntry);

      return {
        charKey,
        existed: hadPreviousEntry,
        previousEntry,
      };
    }

    function restoreTrainedBonusPreviewOverride(rollbackToken) {
      if (!rollbackToken || !rollbackToken.charKey) return;
      if (rollbackToken.existed && rollbackToken.previousEntry && typeof rollbackToken.previousEntry === 'object') {
        trainedBonusPreviewOverrides.set(rollbackToken.charKey, rollbackToken.previousEntry);
        return;
      }
      trainedBonusPreviewOverrides.delete(rollbackToken.charKey);
    }

    function resolveTrackedStatPreview(charKey = '', stat = {}) {
      const safeCharKey = String(charKey || '').trim();
      const rawStats = createTrackedStatPreviewMap(stat);
      if (!safeCharKey || !trainedBonusPreviewOverrides.has(safeCharKey)) return rawStats;

      const entry = trainedBonusPreviewOverrides.get(safeCharKey) || {};
      const baseStats = createTrackedStatPreviewMap(entry.baseStats || rawStats);
      const deltas = createTrackedStatDeltaMap(entry.deltas || {});
      const previewStats = {
        鍔涢噺: baseStats.鍔涢噺 + deltas.鍔涢噺,
        闃插尽: baseStats.闃插尽 + deltas.闃插尽,
        鏁忔嵎: baseStats.鏁忔嵎 + deltas.鏁忔嵎,
        浣撳姏涓婇檺: baseStats.浣撳姏涓婇檺 + deltas.浣撳姏涓婇檺,
        绮剧鍔涗笂闄? baseStats.绮剧鍔涗笂闄?+ deltas.绮剧鍔涗笂闄?
      };

      const allSettled = TRAINED_BONUS_PREVIEW_FIELDS.every(field => rawStats[field] === previewStats[field]);
      const hasExternalDrift = TRAINED_BONUS_PREVIEW_FIELDS.some(field => rawStats[field] !== baseStats[field] && rawStats[field] !== previewStats[field]);
      if (allSettled || hasExternalDrift) {
        trainedBonusPreviewOverrides.delete(safeCharKey);
        return rawStats;
      }
      return previewStats;
    }

    function normalizeEditorPath(pathValue) {
      if (Array.isArray(pathValue)) {
        const normalized = pathValue
          .map(token => (typeof token === 'number' ? token : String(token ?? '').trim()))
          .filter(token => token !== '' && token !== null && token !== undefined);
        return stripWrapperPathPrefix(normalized);
      }
      const raw = String(pathValue ?? '')
        .trim()
        .replace(/^stat_data\./, '')
        .replace(/\[(\d+)\]/g, '.$1')
        .replace(/\["([^"]+)"\]/g, '.$1')
        .replace(/\['([^']+)'\]/g, '.$1');
      if (!raw) return [];
      const normalized = raw
        .split('.')
        .map(token => token.trim())
        .filter(Boolean)
        .map(token => (/^\d+$/.test(token) ? Number(token) : token))
        .filter(token => token !== '' && token !== null && token !== undefined);
      return stripWrapperPathPrefix(normalized);
    }

    const 鏈疆妯″潡缁撶畻璺緞鍏ㄥ眬閿?= '__LWCS_鏈疆妯″潡缁撶畻璺緞__';
    const 鏈疆妯″潡缁撶畻璺緞鏈夋晥姣 = 45000;

    function 鑾峰彇鏈疆妯″潡缁撶畻杩愯鏃舵牴鍒楄〃() {
      const 杩愯鏃舵牴鍒楄〃 = [];
      const 杩藉姞杩愯鏃舵牴 = 杩愯鏃舵牴 => {
        try {
          if (杩愯鏃舵牴 && typeof 杩愯鏃舵牴 === 'object' && !杩愯鏃舵牴鍒楄〃.includes(杩愯鏃舵牴)) 杩愯鏃舵牴鍒楄〃.push(杩愯鏃舵牴);
        } catch (error) {}
      };
      try { 杩藉姞杩愯鏃舵牴(window); } catch (error) {}
      try { 杩藉姞杩愯鏃舵牴(window.parent); } catch (error) {}
      try { 杩藉姞杩愯鏃舵牴(window.top); } catch (error) {}
      try { 杩藉姞杩愯鏃舵牴(globalThis); } catch (error) {}
      return 杩愯鏃舵牴鍒楄〃;
    }

    function 鏋勫缓妯″潡缁撶畻璺緞鍘婚噸閿?璺緞 = []) {
      return (Array.isArray(璺緞) ? 璺緞 : []).map(token => String(token)).join('\u0001');
    }

    function 鐧昏鏈疆妯″潡缁撶畻璺緞(璺緞鍒楄〃 = [], options = {}) {
      if (options && options.registerReadOnlyProjection === false) return;
      const 杩愯鏃舵牴鍒楄〃 = 鑾峰彇鏈疆妯″潡缁撶畻杩愯鏃舵牴鍒楄〃();
      if (!杩愯鏃舵牴鍒楄〃.length) return;
      const 褰撳墠鏃堕棿 = Date.now();
      let 璁板綍 = 杩愯鏃舵牴鍒楄〃
        .map(root => {
          try { return root[鏈疆妯″潡缁撶畻璺緞鍏ㄥ眬閿甝; } catch (error) { return null; }
        })
        .find(item => item && typeof item === 'object' && Number(item.杩囨湡鏃堕棿 || 0) > 褰撳墠鏃堕棿);
      if (!璁板綍 || typeof 璁板綍 !== 'object' || Number(璁板綍.杩囨湡鏃堕棿 || 0) <= 褰撳墠鏃堕棿) {
        璁板綍 = {
          璺緞鍒楄〃: [],
          鍘婚噸閿垪琛? [],
          鍒涘缓鏃堕棿: 褰撳墠鏃堕棿,
          鏇存柊鏃堕棿: 褰撳墠鏃堕棿,
          杩囨湡鏃堕棿: 褰撳墠鏃堕棿 + 鏈疆妯″潡缁撶畻璺緞鏈夋晥姣,
        };
      }
      const 宸叉湁閿?= new Set(Array.isArray(璁板綍.鍘婚噸閿垪琛? ? 璁板綍.鍘婚噸閿垪琛?: []);
      (Array.isArray(璺緞鍒楄〃) ? 璺緞鍒楄〃 : []).forEach(鍘熷璺緞 => {
        const 璺緞 = normalizeEditorPath(鍘熷璺緞);
        if (!璺緞.length) return;
        const 璺緞閿?= 鏋勫缓妯″潡缁撶畻璺緞鍘婚噸閿?璺緞);
        if (!璺緞閿?|| 宸叉湁閿?has(璺緞閿?) return;
        宸叉湁閿?add(璺緞閿?;
        璁板綍.璺緞鍒楄〃.push(璺緞);
      });
      璁板綍.鍘婚噸閿垪琛?= Array.from(宸叉湁閿?;
      璁板綍.鏇存柊鏃堕棿 = 褰撳墠鏃堕棿;
      璁板綍.杩囨湡鏃堕棿 = 褰撳墠鏃堕棿 + 鏈疆妯″潡缁撶畻璺緞鏈夋晥姣;
      杩愯鏃舵牴鍒楄〃.forEach(root => {
        try { root[鏈疆妯″潡缁撶畻璺緞鍏ㄥ眬閿甝 = 璁板綍; } catch (error) {}
      });
    }

    function deepSetMutable(target, pathValue, nextValue) {
      const path = normalizeEditorPath(pathValue);
      if (!target || typeof target !== 'object' || !path.length) {
        throw new Error('鍙橀噺璺緞涓嶈兘涓虹┖銆?);
      }
      let current = target;
      for (let index = 0; index < path.length; index += 1) {
        const token = path[index];
        const isLast = index === path.length - 1;
        if (Array.isArray(current) && token === '-') {
          if (isLast) {
            current.push(nextValue);
            return target;
          }
          const nextToken = path[index + 1];
          const created = typeof nextToken === 'number' || nextToken === '-' ? [] : {};
          current.push(created);
          current = created;
          continue;
        }
        if (isLast) {
          current[token] = nextValue;
          return target;
        }
        const nextToken = path[index + 1];
        if (!current[token] || typeof current[token] !== 'object') {
          current[token] = typeof nextToken === 'number' || nextToken === '-' ? [] : {};
        }
        current = current[token];
      }
      return target;
    }

    function deepDeleteMutable(target, pathValue) {
      const path = normalizeEditorPath(pathValue);
      if (!target || typeof target !== 'object' || !path.length) {
        throw new Error('鍙橀噺璺緞涓嶈兘涓虹┖銆?);
      }
      let current = target;
      for (let index = 0; index < path.length - 1; index += 1) {
        const token = path[index];
        if (!current || typeof current !== 'object' || !(token in current)) return target;
        current = current[token];
      }
      const lastToken = path[path.length - 1];
      if (Array.isArray(current) && typeof lastToken === 'number') {
        if (lastToken >= 0 && lastToken < current.length) current.splice(lastToken, 1);
        return target;
      }
      if (current && typeof current === 'object') {
        delete current[lastToken];
      }
      return target;
    }

    function decodeJsonPointerPath(pointer) {
      const raw = String(pointer ?? '').trim();
      if (!raw || raw === '/') return [];
      const normalized = raw
        .split('/')
        .slice(1)
        .map(token => token.replace(/~1/g, '/').replace(/~0/g, '~'))
        .map(token => (/^\d+$/.test(token) ? Number(token) : token));
      return stripWrapperPathPrefix(normalized);
    }

    async function applyJsonPatchOpsByEditor(patches, options = {}) {
      const safePatches = Array.isArray(patches) ? patches.filter(item => item && item.op && item.path) : [];
      if (!safePatches.length) throw new Error('娌℃湁鍙簲鐢ㄧ殑鍙橀噺鏀瑰姩銆?);
      鐧昏鏈疆妯″潡缁撶畻璺緞(safePatches.map(patch => decodeJsonPointerPath(patch.path)).filter(path => path.length), options);
      return mutateStatDataByEditor(statData => {
        const readMutableValue = pathValue => {
          const path = normalizeEditorPath(pathValue);
          if (!path.length) return statData;
          let current = statData;
          for (let index = 0; index < path.length; index += 1) {
            const token = path[index];
            if (current == null || typeof current !== 'object' || !(token in current)) return undefined;
            current = current[token];
          }
          return current;
        };
        const getDeviationDeltaMultiplier = () => {
          const raw = Number(readMutableValue(['world', '鍋忓樊鍊嶇巼']) ?? 1);
          return Number.isFinite(raw) && raw >= 0 ? raw : 1;
        };
        const isDeviationValuePath = path =>
          Array.isArray(path) &&
          path.length === 2 &&
          String(path[0] || '') === 'world' &&
          String(path[1] || '') === '鍋忓樊鍊?;
        safePatches.forEach(patch => {
          const path = decodeJsonPointerPath(patch.path);
          if (!path.length) return;
          if (patch.op === 'remove') {
            deepDeleteMutable(statData, path);
            return;
          }
          if (patch.op === 'replace' || patch.op === 'add' || patch.op === 'insert') {
            if (isDeviationValuePath(path)) {
              const 涓婁竴鍥炲悎鍊煎師濮?= Number(readMutableValue(path) ?? 0);
              const 杩欏洖鍚堝€煎師濮?= Number(patch.value ?? 涓婁竴鍥炲悎鍊煎師濮?;
              const 涓婁竴鍥炲悎鍊?= Number.isFinite(涓婁竴鍥炲悎鍊煎師濮? ? 涓婁竴鍥炲悎鍊煎師濮?: 0;
              const 杩欏洖鍚堝€?= Number.isFinite(杩欏洖鍚堝€煎師濮? ? 杩欏洖鍚堝€煎師濮?: 涓婁竴鍥炲悎鍊?
              const 鍘熷澧為噺 = 杩欏洖鍚堝€?- 涓婁竴鍥炲悎鍊?
              const 鍊嶇巼淇澧為噺 = Number((鍘熷澧為噺 * getDeviationDeltaMultiplier()).toFixed(4));
              const 淇鍚庡€?= Math.max(0, Number((涓婁竴鍥炲悎鍊?+ 鍊嶇巼淇澧為噺).toFixed(4)));
              deepSetMutable(statData, path, 淇鍚庡€?;
              return;
            }
            deepSetMutable(statData, path, cloneJsonValue(patch.value, patch.value));
            return;
          }
          if (patch.op === 'delta') {
            const previousValue = Number(readMutableValue(path) ?? 0);
            const rawDeltaValue = Number(patch.value ?? 0);
            const deltaValue = isDeviationValuePath(path)
              ? Number((rawDeltaValue * getDeviationDeltaMultiplier()).toFixed(4))
              : rawDeltaValue;
            const nextValue = (Number.isFinite(previousValue) ? previousValue : 0) + (Number.isFinite(deltaValue) ? deltaValue : 0);
            deepSetMutable(statData, path, nextValue);
          }
        });
      }, options);
    }

    function normalizeEditorStringList(rawValue) {
      if (Array.isArray(rawValue)) {
        return rawValue.map(item => toText(item, '').trim()).filter(Boolean);
      }
      return String(rawValue ?? '')
        .split(/[\n,锛屻€乚+/g)
        .map(item => item.trim())
        .filter(Boolean);
    }

    function parseEditorInputValue(rawValue, kind = 'string', meta = {}) {
      const safeKind = String(kind || 'string').trim().toLowerCase();
      const text = String(rawValue ?? '');
      if (safeKind === 'number') {
        const normalized = text.trim().replace(/,/g, '');
        if (!normalized) throw new Error('鏁板瓧瀛楁涓嶈兘涓虹┖銆?);
        const parsed = Number(normalized);
        if (!Number.isFinite(parsed)) throw new Error(`鏃犳硶璇嗗埆鏁板瓧锛?{text}`);
        const numericMeta = normalizeInlineNumericMeta(meta, parsed);
        if (numericMeta.integer && !Number.isInteger(parsed)) throw new Error('璇ュ瓧娈靛彧鎺ュ彈鏁存暟銆?);
        if (numericMeta.min !== null && parsed < numericMeta.min) throw new Error(`鏁板€间笉鑳藉皬浜?${formatNumber(numericMeta.min)}銆俙);
        if (numericMeta.max !== null && parsed > numericMeta.max) throw new Error(`鏁板€间笉鑳藉ぇ浜?${formatNumber(numericMeta.max)}銆俙);
        return parsed;
      }
      if (safeKind === 'boolean') {
        const normalized = text.trim().toLowerCase();
        if (['true', '1', 'yes', 'y', '鏄?, '寮€'].includes(normalized)) return true;
        if (['false', '0', 'no', 'n', '鍚?, '鍏?].includes(normalized)) return false;
        throw new Error(`鏃犳硶璇嗗埆甯冨皵鍊硷細${text}`);
      }
      if (safeKind === 'json') {
        const normalized = text.trim();
        if (!normalized) return null;
        return JSON.parse(normalized);
      }
      if (safeKind === 'string_list' || safeKind === 'token_multi') {
        const 鍒楄〃鍊?= normalizeEditorStringList(text);
        if (safeKind === 'token_multi') {
          const 鍊欓€?= normalizeInlineEditorOptions(meta);
          if (鍊欓€?length) {
            const 闈炴硶椤?= 鍒楄〃鍊?filter(item => !鍊欓€?includes(item));
            if (闈炴硶椤?length) throw new Error(`瀛樺湪闈炴硶閫夐」锛?{闈炴硶椤?join('銆?)}`);
          }
        }
        return 鍒楄〃鍊?
      }
      if (safeKind === 'enum_select') {
        const 鍗曞€?= text.trim();
        const 鍊欓€?= normalizeInlineEditorOptions(meta);
        if (鍊欓€?length && 鍗曞€?&& !鍊欓€?includes(鍗曞€?) {
          throw new Error(`鍙兘閫夋嫨鍊欓€夐」锛?{鍊欓€?join(' / ')}`);
        }
        return 鍗曞€?
      }
      if (safeKind === 'null') return null;
      return text;
    }

    function formatEditorValue(value, kind = 'string') {
      if (value === undefined || value === null) return '';
      if (['string_list', 'token_multi'].includes(String(kind || 'string').trim().toLowerCase())) {
        return normalizeEditorStringList(value).join('銆?);
      }
      if (kind === 'json') {
        try {
          return JSON.stringify(value, null, 2);
        } catch (error) {
          return String(value);
        }
      }
      if (typeof value === 'object') {
        try {
          return JSON.stringify(value, null, 2);
        } catch (error) {
          return String(value);
        }
      }
      return String(value);
    }

    function makeInlineEditableValue(displayText, options = {}) {
      const path = Array.isArray(options.path) ? options.path : normalizeEditorPath(options.path);
      if (!path.length) return htmlEscape(displayText);
      const kind = toText(options.kind, 'string');
      const rawValue = options.rawValue !== undefined ? options.rawValue : displayText;
      const className = toText(options.className, '').trim();
      const classAttr = className ? ` ${className}` : '';
      const multiline = options.multiline ? '1' : '0';
      const editorMeta =
        options.editorMeta && typeof options.editorMeta === 'object'
          ? ` data-inline-editor-meta="${escapeHtmlAttr(JSON.stringify(options.editorMeta))}"`
          : '';
      return `<span class="mvu-inline-editable${classAttr}" tabindex="0" data-inline-editable="1" data-inline-multiline="${multiline}" data-value-kind="${escapeHtmlAttr(kind)}" data-mvu-path="${escapeHtmlAttr(JSON.stringify(path))}" data-mvu-raw-value="${escapeHtmlAttr(formatEditorValue(rawValue, kind))}"${editorMeta}>${htmlEscape(displayText)}</span>`;
    }

    function readInlineEditorMeta(target) {
      if (!target || !(target instanceof HTMLElement)) return {};
      try {
        const raw = target.getAttribute('data-inline-editor-meta') || '{}';
        const parsed = JSON.parse(raw);
        return parsed && typeof parsed === 'object' ? parsed : {};
      } catch (error) {
        return {};
      }
    }

    function normalizeInlineEditorOptions(meta = {}) {
      return Array.from(
        new Set(
          (Array.isArray(meta && meta.options) ? meta.options : [])
            .map(item => toText(item, '').trim())
            .filter(Boolean),
        ),
      );
    }

    function normalizeInlineNumericMeta(meta = {}, rawValue = undefined) {
      const normalized = meta && typeof meta === 'object' ? meta : {};
      const min = Number(normalized.min);
      const max = Number(normalized.max);
      const step = Number(normalized.step);
      return {
        min: Number.isFinite(min) ? min : null,
        max: Number.isFinite(max) ? max : null,
        step: Number.isFinite(step) && step > 0 ? step : null,
        integer: normalized.integer === true || (Number.isFinite(step) && step === 1),
        hint: toText(normalized.hint, '').trim(),
      };
    }

    function formatInlineNumericHint(meta = {}, rawValue = undefined) {
      const normalized = normalizeInlineNumericMeta(meta, rawValue);
      if (normalized.hint) return normalized.hint;
      const parts = [];
      if (normalized.min !== null && normalized.max !== null) parts.push(`鑼冨洿 ${formatNumber(normalized.min)} - ${formatNumber(normalized.max)}`);
      else if (normalized.min !== null) parts.push(`鏈€灏?${formatNumber(normalized.min)}`);
      else if (normalized.max !== null) parts.push(`鏈€澶?${formatNumber(normalized.max)}`);
      else parts.push('鏈涓婁笅闄?);
      parts.push(normalized.integer ? '鏁存暟' : '鍙緭鍏ュ皬鏁?);
      if (normalized.step !== null) parts.push(`姝ラ暱 ${formatNumber(normalized.step)}`);
      return parts.join(' 路 ');
    }

    function buildInlineNumberEditor(target, rect, rawValue, meta = {}, state) {
      const wrapper = document.createElement('div');
      wrapper.className = 'mvu-inline-editor-shell';
      wrapper.style.minWidth = `${Math.max(132, Math.ceil(rect.width) + 40)}px`;
      wrapper.style.display = 'grid';
      wrapper.style.gap = '5px';

      const numericMeta = normalizeInlineNumericMeta(meta, rawValue);
      const input = document.createElement('input');
      input.className = 'mvu-inline-editor-input';
      input.type = 'text';
      input.inputMode = numericMeta.integer ? 'numeric' : 'decimal';
      input.value = formatEditorValue(rawValue, 'number');
      input.style.width = '100%';
      input.style.minHeight = `${Math.max(36, Math.ceil(rect.height) + 10)}px`;
      input.style.padding = '0 14px';
      input.style.borderRadius = '999px';
      input.style.border = '1px solid rgba(0, 229, 255, 0.28)';
      input.style.background = 'rgba(4, 14, 24, 0.96)';
      input.style.boxShadow = '0 10px 24px rgba(0, 0, 0, 0.25)';
      input.style.color = 'var(--white)';
      input.style.outline = 'none';
      input.style.font = 'inherit';
      input.style.boxSizing = 'border-box';

      const hint = document.createElement('div');
      hint.textContent = formatInlineNumericHint(meta, rawValue);
      hint.style.fontSize = '11px';
      hint.style.lineHeight = '1.35';
      hint.style.color = 'rgba(191, 233, 242, 0.82)';
      hint.style.padding = '0 4px';

      wrapper.appendChild(input);
      wrapper.appendChild(hint);
      wrapper.addEventListener('click', event => {
        event.stopPropagation();
      });
      input.addEventListener('keydown', event => {
        if (event.key === 'Enter') {
          event.preventDefault();
          event.stopPropagation();
          commitInlineEditState(state);
        } else if (event.key === 'Escape') {
          event.preventDefault();
          event.stopPropagation();
          restoreInlineEditState(state);
          flushPendingLiveRefresh();
        }
      });
      input.addEventListener('blur', () => {
        commitInlineEditState(state);
      });
      state.readValue = () => input.value;
      return { root: wrapper, inputEl: wrapper, focusEl: input };
    }

    function buildInlineEnumSelectEditor(target, rect, rawValue, meta = {}, state) {
      const select = document.createElement('select');
      select.className = 'mvu-inline-editor-input';
      select.style.width = `${Math.max(120, Math.ceil(rect.width) + 24)}px`;
      const options = normalizeInlineEditorOptions(meta);
      const normalizedValue = toText(rawValue, '').trim();
      select.innerHTML = options
        .map(option => `<option value="${escapeHtmlAttr(option)}"${option === normalizedValue ? ' selected' : ''}>${htmlEscape(option)}</option>`)
        .join('');
      select.addEventListener('change', () => {
        commitInlineEditState(state);
      });
      select.addEventListener('keydown', event => {
        if (event.key === 'Escape') {
          event.preventDefault();
          event.stopPropagation();
          restoreInlineEditState(state);
          flushPendingLiveRefresh();
        }
      });
      select.addEventListener('click', event => {
        event.stopPropagation();
      });
      select.addEventListener('blur', () => {
        commitInlineEditState(state);
      });
      return { root: select, inputEl: select };
    }

    function buildInlineTokenMultiEditor(target, rect, rawValue, meta = {}, state) {
      const wrapper = document.createElement('div');
      wrapper.className = 'mvu-inline-token-editor';
      wrapper.style.minWidth = `${Math.max(240, Math.ceil(rect.width) + 36)}px`;
      wrapper.style.padding = '10px 12px';
      wrapper.style.borderRadius = '12px';
      wrapper.style.border = '1px solid rgba(0, 229, 255, 0.28)';
      wrapper.style.background = 'rgba(4, 14, 24, 0.96)';
      wrapper.style.boxShadow = '0 14px 32px rgba(0, 0, 0, 0.35)';
      wrapper.style.display = 'grid';
      wrapper.style.gap = '10px';

      const optionGrid = document.createElement('div');
      optionGrid.style.display = 'flex';
      optionGrid.style.flexWrap = 'wrap';
      optionGrid.style.gap = '8px';

      const selected = new Set(normalizeEditorStringList(rawValue));
      normalizeInlineEditorOptions(meta).forEach(option => {
        const label = document.createElement('label');
        label.className = `skill-designer-check-chip${selected.has(option) ? ' active' : ''}`;
        label.style.cursor = 'pointer';
        const input = document.createElement('input');
        input.type = 'checkbox';
        input.value = option;
        input.checked = selected.has(option);
        input.style.display = 'none';
        input.addEventListener('change', () => {
          label.classList.toggle('active', input.checked);
        });
        label.appendChild(input);
        label.appendChild(document.createTextNode(option));
        optionGrid.appendChild(label);
      });

      const actions = document.createElement('div');
      actions.style.display = 'flex';
      actions.style.justifyContent = 'flex-end';
      actions.style.gap = '8px';

      const cancelBtn = document.createElement('button');
      cancelBtn.type = 'button';
      cancelBtn.className = 'tag-chip';
      cancelBtn.textContent = '鍙栨秷';
      cancelBtn.addEventListener('click', event => {
        event.preventDefault();
        event.stopPropagation();
        restoreInlineEditState(state);
        flushPendingLiveRefresh();
      });

      const saveBtn = document.createElement('button');
      saveBtn.type = 'button';
      saveBtn.className = 'tag-chip live';
      saveBtn.textContent = '纭畾';
      saveBtn.addEventListener('click', event => {
        event.preventDefault();
        event.stopPropagation();
        commitInlineEditState(state);
      });

      actions.appendChild(cancelBtn);
      actions.appendChild(saveBtn);
      wrapper.appendChild(optionGrid);
      wrapper.appendChild(actions);

      wrapper.addEventListener('click', event => {
        event.stopPropagation();
      });
      wrapper.addEventListener('keydown', event => {
        if (event.key === 'Escape') {
          event.preventDefault();
          event.stopPropagation();
          restoreInlineEditState(state);
          flushPendingLiveRefresh();
        }
        if ((event.ctrlKey || event.metaKey) && event.key === 'Enter') {
          event.preventDefault();
          event.stopPropagation();
          commitInlineEditState(state);
        }
      });

      state.readValue = () =>
        Array.from(wrapper.querySelectorAll('input[type="checkbox"]:checked'))
          .map(input => toText(input.value, '').trim())
          .filter(Boolean);
      state.manualCommit = true;

      return { root: wrapper, inputEl: wrapper, focusEl: saveBtn };
    }

    function showUiToast(message, type = 'info', duration = 3200) {
      try {
        if (window.MVU_Toast && typeof window.MVU_Toast.show === 'function') {
          window.MVU_Toast.show(message, type, duration);
          return;
        }
      } catch (error) {}
      if (type === 'error') {
        console.warn('[DragonUI]', message);
        if (typeof window.alert === 'function') window.alert(message);
      } else {
        console.log('[DragonUI]', message);
      }
    }

    function buildUiRequestInjectionId(requestKind = 'ui_request') {
      const normalizedKind = toText(requestKind, 'ui_request')
        .toLowerCase()
        .replace(/[^a-z0-9_-]+/g, '-')
        .replace(/^-+|-+$/g, '') || 'ui_request';
      return `dragon-ui-${normalizedKind}-${Date.now()}-${Math.random().toString(36).slice(2, 8)}`;
    }

    const pendingUiSystemInjections = new Map();
    const 寮傛鍔ㄤ綔閿佽〃 = new Map();
    const 寮傛鍔ㄤ綔閿佽秴鏃舵绉?= 120000;

    function 娓呯悊杩囨湡寮傛鍔ㄤ綔閿?) {
      const 褰撳墠鏃堕棿 = Date.now();
      for (const [閿侀敭, 閿佹椂闂碷 of Array.from(寮傛鍔ㄤ綔閿佽〃.entries())) {
        if (褰撳墠鏃堕棿 - Number(閿佹椂闂?|| 0) > 寮傛鍔ㄤ綔閿佽秴鏃舵绉? {
          寮傛鍔ㄤ綔閿佽〃.delete(閿侀敭);
        }
      }
    }

    function 鏋勫缓寮傛鍔ㄤ綔閿侀敭(璇锋眰绫诲瀷 = 'ui_request', 鐢ㄦ埛鏂囨湰 = '', 闄勫姞淇℃伅 = {}) {
      const 鏄惧紡閿侀敭 = toText(闄勫姞淇℃伅 && (闄勫姞淇℃伅.lockKey || 闄勫姞淇℃伅.actionLockKey), '').trim();
      if (鏄惧紡閿侀敭) return `explicit:${鏄惧紡閿侀敭}`;
      const 绫诲瀷 = toText(璇锋眰绫诲瀷, 'ui_request').trim();
      const 鏂囨湰 = toText(鐢ㄦ埛鏂囨湰, '').trim();
      if (!鏂囨湰) return '';
      return `${绫诲瀷}::${鏂囨湰}`;
    }

    function 灏濊瘯鍗犵敤寮傛鍔ㄤ綔閿?閿侀敭) {
      if (!閿侀敭) return true;
      娓呯悊杩囨湡寮傛鍔ㄤ綔閿?);
      if (寮傛鍔ㄤ綔閿佽〃.has(閿侀敭)) return false;
      寮傛鍔ㄤ綔閿佽〃.set(閿侀敭, Date.now());
      return true;
    }

    function 閲婃斁寮傛鍔ㄤ綔閿?閿侀敭) {
      if (!閿侀敭) return;
      寮傛鍔ㄤ綔閿佽〃.delete(閿侀敭);
    }

    function getCurrentUiRequestChatId() {
      try {
        const ctx = window.SillyTavern && typeof window.SillyTavern.getContext === 'function'
          ? window.SillyTavern.getContext()
          : null;
        return toText(ctx && ctx.chatId, '').trim();
      } catch (error) {
        return '';
      }
    }

    function clearPendingUiSystemInjection(injectionId = '') {
      const safeInjectionId = toText(injectionId, '').trim();
      if (!safeInjectionId) return;
      const record = pendingUiSystemInjections.get(safeInjectionId);
      if (!record) return;
      pendingUiSystemInjections.delete(safeInjectionId);
      if (record.timer) {
        try {
          window.clearInterval(record.timer);
        } catch (error) {}
      }
      try {
        if (record.uninject && typeof record.uninject === 'function') {
          record.uninject();
        } else if (window.TavernHelper && typeof window.TavernHelper.uninjectPrompts === 'function') {
          window.TavernHelper.uninjectPrompts([safeInjectionId]);
        }
      } catch (error) {
        console.warn('[DragonUI] Failed to clear pending UI system injection', error);
      }
    }

    function registerPersistentUiSystemInjection(helper, requestKind, hiddenPrompt) {
      const content = toText(hiddenPrompt, '').trim();
      if (!helper || typeof helper.injectPrompts !== 'function' || !content) return null;
      const injectionId = buildUiRequestInjectionId(requestKind);
      const injected = helper.injectPrompts([{
        id: injectionId,
        position: 'in_chat',
        depth: 0,
        role: 'system',
        content,
        should_scan: true
      }]);
      const record = {
        id: injectionId,
        chatId: getCurrentUiRequestChatId(),
        createdAt: Date.now(),
        userIndex: -1,
        timer: 0,
        uninject: injected && typeof injected.uninject === 'function' ? injected.uninject : null
      };
      record.timer = window.setInterval(() => {
        const ageMs = Date.now() - record.createdAt;
        if (ageMs >= 180000) {
          clearPendingUiSystemInjection(record.id);
          return;
        }
        try {
          const ctx = window.SillyTavern && typeof window.SillyTavern.getContext === 'function'
            ? window.SillyTavern.getContext()
            : null;
          const currentChatId = toText(ctx && ctx.chatId, '').trim();
          if (record.chatId && currentChatId && record.chatId !== currentChatId) {
            clearPendingUiSystemInjection(record.id);
            return;
          }
          const chat = Array.isArray(ctx && ctx.chat) ? ctx.chat : [];
          if (record.userIndex >= 0 && chat.length > record.userIndex + 1) {
            const assistantMessage = chat.slice(record.userIndex + 1).find(message => message && !message.is_user);
            if (assistantMessage && !record.processing && record.helper) {
              record.processing = true;
              postprocessUiContinuationResult(record.helper, record.userText, record.requestKind, record)
                .catch(error => {
                  console.warn('[DragonUI] Failed to postprocess UI continuation result', error);
                })
                .finally(() => {
                  record.processing = false;
                });
            }
          }
        } catch (error) {}
      }, 800);
      pendingUiSystemInjections.set(injectionId, record);
      return record;
    }

    function sanitizeUiContinuationVisibleText(text = '') {
      let cleaned = String(text || '');
      const contentMatch = cleaned.match(/<content>\s*([\s\S]*?)\s*<\/content>/i);
      if (contentMatch) cleaned = String(contentMatch[1] || '');
      cleaned = cleaned
        .replace(/<moduleSettlement>[\s\S]*?<\/moduleSettlement>/gi, '')
        .replace(/<moduleRoute>[\s\S]*?<\/moduleRoute>/gi, '')
        .replace(/<妯″潡璺敱>[\s\S]*?<\/妯″潡璺敱>/gi, '')
        .replace(/<thinking>[\s\S]*?<\/thinking>/gi, '')
        .replace(/<thought>[\s\S]*?<\/thought>/gi, '')
        .replace(/<tucao>[\s\S]*?<\/tucao>/gi, '')
        .replace(/<disclaimer>[\s\S]*?<\/disclaimer>/gi, '')
        .replace(/<UpdateVariable>[\s\S]*?<\/UpdateVariable>/gi, '')
        .replace(/<Analysis>[\s\S]*?<\/Analysis>/gi, '')
        .replace(/<JSONPatch>[\s\S]*?<\/JSONPatch>/gi, '')
        .replace(/<StatusPlaceHolderImpl\s*\/>/gi, '');
      cleaned = cleaned
        .split(/\r?\n/)
        .filter(line => !/^\s*\/(?:char|world|org|sys)\//.test(String(line || '').trim()))
        .join('\n')
        .replace(/^\s*Final:\s*/i, '')
        .replace(/\n{3,}/g, '\n\n')
        .trim();
      return cleaned;
    }

    async function waitForUiContinuationAssistantMessage(userIndex = -1, attempts = 25) {
      for (let attempt = 0; attempt < attempts; attempt += 1) {
        try {
          const ctx = window.SillyTavern && typeof window.SillyTavern.getContext === 'function'
            ? window.SillyTavern.getContext()
            : null;
          const chat = Array.isArray(ctx && ctx.chat) ? ctx.chat : [];
          if (userIndex >= 0) {
            const found = chat.slice(userIndex + 1).find(message => message && !message.is_user);
            if (found) return found;
          } else {
            const found = [...chat].reverse().find(message => message && !message.is_user);
            if (found) return found;
          }
        } catch (error) {}
        await new Promise(resolve => window.setTimeout(resolve, 200));
      }
      return null;
    }

    async function postprocessUiContinuationResult(helper, userText, requestKind, persistentInjection) {
      const userIndex = Number.isInteger(persistentInjection?.userIndex) ? persistentInjection.userIndex : -1;
      try {
        const ctx = window.SillyTavern && typeof window.SillyTavern.getContext === 'function'
          ? window.SillyTavern.getContext()
          : null;
        const chat = Array.isArray(ctx && ctx.chat) ? ctx.chat : [];
        const userMessage = userIndex >= 0 ? chat[userIndex] : null;
        if (userMessage && userMessage.is_user && String(userMessage.mes || '') !== userText && typeof helper?.setChatMessages === 'function') {
          await helper.setChatMessages([{ message_id: userMessage.message_id, mes: userText }], { refresh: 'affected' });
          userMessage.mes = userText;
        }
      } catch (error) {
        console.warn('[DragonUI] Failed to restore UI continuation user message', error);
      }
      const assistantMessage = await waitForUiContinuationAssistantMessage(userIndex, 2);
      if (!assistantMessage) return;
      const rawText = String(assistantMessage.mes || '');
      const visibleText = sanitizeUiContinuationVisibleText(rawText);
      if (visibleText && visibleText !== rawText && typeof helper?.setChatMessages === 'function') {
        await helper.setChatMessages([{ message_id: assistantMessage.message_id, mes: visibleText }], { refresh: 'affected' });
        assistantMessage.mes = visibleText;
      }
      if (persistentInjection?.id) {
        clearPendingUiSystemInjection(persistentInjection.id);
      }
    }

    async function dispatchUiAiRequest(playerInput, systemPrompt, meta = {}) {
      const helper = window.TavernHelper && typeof window.TavernHelper === 'object' ? window.TavernHelper : null;
      const requestKind = toText(meta && meta.requestKind, 'ui_request') || 'ui_request';
      const userText = toText(playerInput, '').trim();
      const hiddenPrompt = toText(systemPrompt, '').trim();
      const patchOps = Array.isArray(meta && meta.patchOps) ? meta.patchOps : [];
      const 璺宠繃鍔ㄤ綔閿?= meta && (meta.skipActionLock === true || meta.noActionLock === true);
      const 鍔ㄤ綔閿侀敭 = 璺宠繃鍔ㄤ綔閿?? '' : 鏋勫缓寮傛鍔ㄤ綔閿侀敭(requestKind, userText, meta);
      let persistentInjection = null;

      if (!userText) {
        showUiToast('璇锋眰鍐呭涓虹┖锛屾棤娉曟彁浜ゃ€?, 'error', 4200);
        return { ok: false, requestKind, reason: 'empty_input' };
      }
      if (!helper
        || typeof helper.injectPrompts !== 'function'
        || typeof helper.createChatMessages !== 'function'
        || typeof helper.triggerSlash !== 'function') {
        showUiToast('閰掗鍔╂墜鍙戦€佹帴鍙ｆ湭灏辩华锛屽綋鍓嶆棤娉曟彁浜よ姹傘€?, 'error', 4200);
        return { ok: false, requestKind, reason: 'helper_unavailable' };
      }
      if (鍔ㄤ綔閿侀敭 && !灏濊瘯鍗犵敤寮傛鍔ㄤ綔閿?鍔ㄤ綔閿侀敭)) {
        showUiToast('鍚屼竴鎿嶄綔浠嶅湪鎵ц锛岃绋嶅€欏啀璇曘€?, 'info', 2400);
        return { ok: false, requestKind, reason: 'request_locked' };
      }

      try {
        // Always persist MVU patches first, so generation reads newest state.
        if (patchOps.length) {
          await applyJsonPatchOpsByEditor(patchOps, { force: true });
          await refreshLiveSnapshot({ force: true });
        }
        Array.from(pendingUiSystemInjections.keys()).forEach(injectionId => {
          clearPendingUiSystemInjection(injectionId);
        });
        persistentInjection = hiddenPrompt
          ? registerPersistentUiSystemInjection(helper, requestKind, hiddenPrompt)
          : null;
        await helper.createChatMessages([{ role: 'user', message: userText }], { refresh: 'affected' });
        try {
          const ctx = window.SillyTavern && typeof window.SillyTavern.getContext === 'function'
            ? window.SillyTavern.getContext()
            : null;
          const chat = Array.isArray(ctx && ctx.chat) ? ctx.chat : [];
          const lastMessage = chat.length ? chat[chat.length - 1] : null;
          if (lastMessage && lastMessage.is_user && String(lastMessage.mes || '') === userText) {
            lastMessage._mvu_hidden_system_prompt = hiddenPrompt;
            lastMessage._mvu_request_kind = requestKind;
            if (persistentInjection) {
              lastMessage._mvu_hidden_system_injection_id = persistentInjection.id;
              persistentInjection.userIndex = chat.length - 1;
              persistentInjection.helper = helper;
              persistentInjection.userText = userText;
              persistentInjection.requestKind = requestKind;
            }
          }
        } catch (error) {}
        await helper.triggerSlash('/trigger await=true');
        return { ok: true, requestKind };
      } catch (error) {
        if (persistentInjection && persistentInjection.id) {
          clearPendingUiSystemInjection(persistentInjection.id);
        }
        console.error('[DragonUI] UI request dispatch failed', error);
        showUiToast(error && error.message ? error.message : '璇锋眰鎻愪氦澶辫触銆?, 'error', 4200);
        return { ok: false, requestKind, reason: 'dispatch_failed', error };
      } finally {
        if (鍔ㄤ綔閿侀敭) 閲婃斁寮傛鍔ㄤ綔閿?鍔ㄤ綔閿侀敭);
      }
    }

    function installBattleUiHostSendBridge() {
      const sendFromBattleUi = async (detail = {}) => {
        const requestKind = toText(
          detail.requestKind || detail.channels?.requestKind || detail.options?.requestKind,
          'battle_arbitration'
        );
        const patchOps = Array.isArray(detail.mvuUpdate?.patchOps) ? detail.mvuUpdate.patchOps : [];
        return dispatchUiAiRequest(detail.playerInput, detail.systemPrompt, { requestKind, patchOps });
      };
      window.BattleUIBridge = Object.assign(window.BattleUIBridge || {}, {
        hostSend: sendFromBattleUi
      });
      window.__battleUIHostAdapter = Object.assign(window.__battleUIHostAdapter || {}, {
        sendUIRequest: sendFromBattleUi,
        sendBattleRequest: sendFromBattleUi
      });
    }
    installBattleUiHostSendBridge();

    const MVU_EDITOR_STORE_COMMIT_DELAY = 140;
    const MVU_EDITOR_ROOT_OBJECT_KEYS = ['sys', 'world', 'org', 'char'];
    const MVU_WRAPPER_ROOT_KEYS = ['display_data', 'delta_data', 'initialized_lorebooks', 'schema', 'stat_data'];
    const MVU_WRAPPER_PREFIX_KEYS = new Set(['stat_data', 'display_data', 'data', 'variables', 'payload', 'root', 'mvu', 'state']);
    const MVU_RECENT_MESSAGE_SCAN_DEPTH = 24;
    const mvuEditorStore = {
      statData: null,
      messageId: 'latest',
      signature: '',
      dirty: false,
      version: 0,
      flushing: false,
      flushPromise: null,
      pendingTimer: 0,
      pendingWaiters: [],
    };

    function clearMvuEditorStorePendingTimer() {
      if (!mvuEditorStore.pendingTimer) return;
      try {
        window.clearTimeout(mvuEditorStore.pendingTimer);
      } catch (error) {}
      mvuEditorStore.pendingTimer = 0;
    }

    function settleMvuEditorStoreWaiters(error, value) {
      const waiters = Array.isArray(mvuEditorStore.pendingWaiters) ? mvuEditorStore.pendingWaiters.splice(0) : [];
      waiters.forEach(waiter => {
        if (!waiter) return;
        if (error) {
          try { waiter.reject(error); } catch (_) {}
          return;
        }
        try { waiter.resolve(value); } catch (_) {}
      });
    }

    function serializeMvuEditorStoreStatData(statData) {
      try {
        return JSON.stringify(statData == null ? null : statData);
      } catch (error) {
        return `__mvu_editor_store__${Date.now()}_${Math.random().toString(36).slice(2)}`;
      }
    }

    function isPlainObjectValue(value) {
      return !!value && typeof value === 'object' && !Array.isArray(value);
    }

    function countRootObjectFields(value) {
      if (!isPlainObjectValue(value)) return 0;
      return MVU_EDITOR_ROOT_OBJECT_KEYS.filter(key => isPlainObjectValue(value[key])).length;
    }

    function buildCanonicalSpiritSkillSlotKey(ringIndex = 1) {
      const safeIndex = Math.max(1, Math.floor(Number(ringIndex) || 0));
      return `绗?{safeIndex}榄傛妧`;
    }

    function normalizeSingleSpiritSkillSlotMapForEditor(skillMap = {}, ringIndex = 1) {
      if (!isPlainObjectValue(skillMap)) return skillMap;
      const entries = Object.entries(skillMap).filter(([, skill]) => isPlainObjectValue(skill));
      if (!entries.length) return skillMap;
      const canonicalKey = buildCanonicalSpiritSkillSlotKey(ringIndex);
      const hasMultipleEntries = entries.length > 1;
      const hasOrdinalKey = entries.some(([rawKey]) => /^绗??:\d+|[涓€浜屼笁鍥涗簲鍏竷鍏節鍗佺櫨]+)榄傛妧/u.test(String(rawKey || '').trim()));
      if (hasMultipleEntries || (hasOrdinalKey && skillMap[canonicalKey])) return skillMap;
      const [rawKey, rawSkill] = entries[0];
      const normalizedSkill = isPlainObjectValue(rawSkill) ? rawSkill : {};
      if (!normalizedSkill.榄傛妧鍚?|| !String(normalizedSkill.榄傛妧鍚?.trim()) {
        normalizedSkill.榄傛妧鍚?= String(rawKey || canonicalKey);
      }
      if (rawKey !== canonicalKey) delete skillMap[rawKey];
      skillMap[canonicalKey] = normalizedSkill;
      return skillMap;
    }

    function normalizeSpiritSkillSlotsInStatDataForEditor(statData = {}) {
      const chars = isPlainObjectValue(statData && statData.char) ? statData.char : {};
      Object.values(chars).forEach(charData => {
        const spirits = isPlainObjectValue(charData && charData.姝﹂瓊) ? charData.姝﹂瓊 : {};
        Object.values(spirits).forEach(spiritData => {
          const soulSpirits = isPlainObjectValue(spiritData && spiritData.榄傜伒) ? spiritData.榄傜伒 : {};
          Object.values(soulSpirits).forEach(soulSpirit => {
            const rings = isPlainObjectValue(soulSpirit && soulSpirit.榄傜幆) ? soulSpirit.榄傜幆 : {};
            Object.entries(rings).forEach(([ringIndex, ringData]) => {
              normalizeSingleSpiritSkillSlotMapForEditor(ringData && ringData['榄傛妧'], ringIndex);
            });
          });
        });
      });
      return statData;
    }

    function stripWrapperPathPrefix(pathTokens) {
      const normalized = Array.isArray(pathTokens) ? pathTokens.slice() : [];
      while (normalized.length > 0) {
        const head = normalized[0];
        if (typeof head === 'number') break;
        const headText = String(head ?? '').trim();
        if (!headText || !MVU_WRAPPER_PREFIX_KEYS.has(headText)) break;
        normalized.shift();
      }
      return normalized;
    }

    function pickBestRootCandidate(candidates = []) {
      let bestCandidate = null;
      let bestScore = -1;
      let bestSize = -1;
      candidates.forEach(candidate => {
        if (!isPlainObjectValue(candidate)) return;
        const score = countRootObjectFields(candidate);
        if (score <= 0) return;
        const size = Object.keys(candidate).length;
        if (score > bestScore || (score === bestScore && size > bestSize)) {
          bestCandidate = candidate;
          bestScore = score;
          bestSize = size;
        }
      });
      return bestCandidate;
    }

    function normalizeMvuDataEnvelopeForEditor(rawData) {
      const safeData = isPlainObjectValue(rawData) ? cloneJsonValue(rawData, {}) : {};
      const hasWrapperFields = MVU_WRAPPER_ROOT_KEYS.some(key => Object.prototype.hasOwnProperty.call(safeData, key));
      const envelope = hasWrapperFields ? cloneJsonValue(safeData, {}) : {};

      const bestRoot = pickBestRootCandidate([
        safeData.stat_data,
        safeData.display_data,
        safeData?.data?.stat_data,
        safeData?.variables?.stat_data,
        safeData?.payload?.stat_data,
        safeData?.root?.stat_data,
        safeData,
      ]);

      if (bestRoot) {
        envelope.stat_data = cloneJsonValue(bestRoot, {});
      } else if (isPlainObjectValue(envelope.stat_data)) {
        envelope.stat_data = cloneJsonValue(envelope.stat_data, {});
      } else {
        envelope.stat_data = {};
      }
      if (!isPlainObjectValue(envelope.initialized_lorebooks)) envelope.initialized_lorebooks = {};
      return envelope;
    }

    function buildSafeStatDataForEditorWrite(nextStatData, baseStatData) {
      const draft = isPlainObjectValue(nextStatData) ? cloneJsonValue(nextStatData, {}) : {};
      const base = isPlainObjectValue(baseStatData) ? baseStatData : {};

      MVU_EDITOR_ROOT_OBJECT_KEYS.forEach(key => {
        if (isPlainObjectValue(draft[key])) return;
        if (isPlainObjectValue(base[key])) {
          draft[key] = cloneJsonValue(base[key], {});
          return;
        }
        draft[key] = {};
      });

      normalizeSpiritSkillSlotsInStatDataForEditor(draft);
      return draft;
    }

    function writeMvuEditorStoreSnapshot(statData, options = {}) {
      const safeStatData = statData && typeof statData === 'object' ? cloneJsonValue(statData, {}) : {};
      mvuEditorStore.statData = safeStatData;
      if (options.messageId !== undefined) mvuEditorStore.messageId = options.messageId;
      mvuEditorStore.signature = serializeMvuEditorStoreStatData(safeStatData);
      if (!options.keepDirty) mvuEditorStore.dirty = false;
      return mvuEditorStore.statData;
    }

    function syncMvuEditorStoreFromRoot(statData, options = {}) {
      const safeStatData = statData && typeof statData === 'object' ? statData : {};
      const nextSignature = serializeMvuEditorStoreStatData(safeStatData);
      if (!options.force && mvuEditorStore.dirty) return false;
      if (!options.force && nextSignature === mvuEditorStore.signature) return false;
      writeMvuEditorStoreSnapshot(safeStatData);
      return true;
    }

    async function readLatestMvuDataByEditor() {
      await waitForMvuReady();
      const host = getMvuHost();
      if (!host || typeof host.getMvuData !== 'function' || typeof host.replaceMvuData !== 'function') {
        throw new Error('鏈壘鍒板彲鐢ㄧ殑 MVU 鍐欏洖鎺ュ彛銆?);
      }
      const scanned = await scanRecentMessageVariablesWithReaders([
        messageId => Promise.resolve(host.getMvuData({ type: 'message', message_id: messageId })),
      ]);
      const currentMvuData = scanned && scanned.vars ? scanned.vars : null;
      if (!currentMvuData || typeof currentMvuData !== 'object') {
        throw new Error('璇诲彇褰撳墠 MVU 鏁版嵁澶辫触銆?);
      }
      const safeMvuData = normalizeMvuDataEnvelopeForEditor(currentMvuData);
      return { host, mvuData: safeMvuData, messageId: scanned && scanned.messageId !== undefined ? scanned.messageId : 'latest' };
    }

    async function ensureMvuEditorStoreReady(options = {}) {
      if (!options.force && mvuEditorStore.dirty && mvuEditorStore.statData && typeof mvuEditorStore.statData === 'object') {
        return mvuEditorStore.statData;
      }
      const { mvuData, messageId } = await readLatestMvuDataByEditor();
      return writeMvuEditorStoreSnapshot(mvuData.stat_data, { messageId });
    }

    async function flushMvuEditorStore(options = {}) {
      clearMvuEditorStorePendingTimer();
      if (!mvuEditorStore.dirty) {
        settleMvuEditorStoreWaiters(null, mvuEditorStore.statData);
        return mvuEditorStore.statData;
      }
      if (mvuEditorStore.flushing && mvuEditorStore.flushPromise) {
        return mvuEditorStore.flushPromise.then(result => {
          if (mvuEditorStore.dirty) return flushMvuEditorStore(options);
          return result;
        });
      }
      mvuEditorStore.flushing = true;
      mvuEditorStore.flushPromise = (async () => {
        const flushVersion = mvuEditorStore.version;
        const flushStatData = cloneJsonValue(mvuEditorStore.statData, {});
        const { host, mvuData, messageId } = await readLatestMvuDataByEditor();
        const nextMvuData = cloneJsonValue(mvuData, {});
        const safeFlushStatData = buildSafeStatDataForEditorWrite(flushStatData, mvuData.stat_data);
        nextMvuData.stat_data = safeFlushStatData;
        await Promise.resolve(host.replaceMvuData(nextMvuData, { type: 'message', message_id: messageId }));
        if (mvuEditorStore.version === flushVersion) {
          mvuEditorStore.dirty = false;
          mvuEditorStore.messageId = messageId;
          mvuEditorStore.signature = serializeMvuEditorStoreStatData(safeFlushStatData);
        }
        if (options.refresh !== false) {
          await refreshLiveSnapshot();
        }
        settleMvuEditorStoreWaiters(null, nextMvuData);
        return nextMvuData;
      })().catch(error => {
        settleMvuEditorStoreWaiters(error);
        throw error;
      }).finally(() => {
        mvuEditorStore.flushing = false;
        mvuEditorStore.flushPromise = null;
        if (mvuEditorStore.dirty && !mvuEditorStore.pendingTimer) {
          scheduleMvuEditorStoreFlush(0).catch(() => {});
        }
      });
      return mvuEditorStore.flushPromise;
    }

    function scheduleMvuEditorStoreFlush(delay = MVU_EDITOR_STORE_COMMIT_DELAY) {
      clearMvuEditorStorePendingTimer();
      return new Promise((resolve, reject) => {
        mvuEditorStore.pendingWaiters.push({ resolve, reject });
        mvuEditorStore.pendingTimer = window.setTimeout(() => {
          mvuEditorStore.pendingTimer = 0;
          flushMvuEditorStore().catch(() => {});
        }, Math.max(0, toNumber(delay, MVU_EDITOR_STORE_COMMIT_DELAY)));
      });
    }

    async function mutateStatDataByEditor(mutator, options = {}) {
      if (typeof mutator !== 'function') throw new Error('鍙橀噺鏇存柊鍣ㄥ繀椤绘槸鍑芥暟銆?);
      await ensureMvuEditorStoreReady({ force: !!options.force });
      const nextStatData = cloneJsonValue(mvuEditorStore.statData, {});
      await Promise.resolve(mutator(nextStatData));
      mvuEditorStore.statData = nextStatData;
      mvuEditorStore.signature = serializeMvuEditorStoreStatData(nextStatData);
      mvuEditorStore.dirty = true;
      mvuEditorStore.version += 1;
      if (options.immediate === false) {
        return scheduleMvuEditorStoreFlush(options.delay);
      }
      return flushMvuEditorStore(options);
    }

    async function replaceStatDataByEditor(updates, options = {}) {
      const safeUpdates = Array.isArray(updates) ? updates.filter(item => item && item.path) : [];
      if (!safeUpdates.length) throw new Error('娌℃湁鍙啓鍥炵殑鍙橀噺鏇存柊銆?);
      return mutateStatDataByEditor(statData => {
        safeUpdates.forEach(({ path, value }) => {
          deepSetMutable(statData, path, value);
        });
      }, options);
    }

    async function deleteStatDataPathByEditor(pathValue, options = {}) {
      const path = normalizeEditorPath(pathValue);
      if (!path.length) throw new Error('鍙橀噺璺緞涓嶈兘涓虹┖銆?);
      return mutateStatDataByEditor(statData => {
        deepDeleteMutable(statData, path);
      }, options);
    }

    async function renameStatObjectKeyByEditor(parentPathValue, previousKey, nextKey, options = {}) {
      const parentPath = normalizeEditorPath(parentPathValue);
      const oldKey = toText(previousKey, '').trim();
      const newKey = toText(nextKey, '').trim();
      if (!parentPath.length) throw new Error('缂哄皯瀵硅薄璺緞锛屾棤娉曢噸鍛藉悕銆?);
      if (!oldKey) throw new Error('缂哄皯鍘熷瀛楁鍚嶃€?);
      if (!newKey) throw new Error('瀛楁鍚嶄笉鑳戒负绌恒€?);
      if (newKey === oldKey) return;
      return mutateStatDataByEditor(statData => {
        const target = deepGet(statData, parentPath, null);
        if (!target || typeof target !== 'object' || Array.isArray(target)) {
          throw new Error('鐩爣瀵硅薄涓嶅瓨鍦紝鏃犳硶閲嶅懡鍚嶃€?);
        }
        if (!Object.prototype.hasOwnProperty.call(target, oldKey)) {
          throw new Error('鍘熷瓧娈典笉瀛樺湪锛屾棤娉曢噸鍛藉悕銆?);
        }
        if (Object.prototype.hasOwnProperty.call(target, newKey)) {
          throw new Error('鍚屽悕瀛楁宸插瓨鍦紝璇锋崲涓€涓悕绉般€?);
        }
        const payload = cloneJsonValue(target[oldKey], {});
        delete target[oldKey];
        target[newKey] = payload;
      }, options);
    }

    function normalizeInlineComparableValue(value, kind = 'string') {
      const safeKind = String(kind || 'string').trim().toLowerCase();
      if (safeKind === 'number') {
        const parsed = Number(value);
        return Number.isFinite(parsed) ? String(parsed) : '';
      }
      if (safeKind === 'string_list' || safeKind === 'token_multi') {
        return normalizeEditorStringList(value).join('銆?);
      }
      if (safeKind === 'enum_select') return String(value ?? '').trim();
      if (safeKind === 'boolean') {
        return String(['true', '1', 'yes', 'y', '鏄?, '寮€'].includes(String(value ?? '').trim().toLowerCase()));
      }
      return String(value ?? '');
    }

    function 鏍￠獙鍐呰仈闄愰€夊啓鍥炲€?value, kind = 'string', meta = {}) {
      const safeKind = String(kind || 'string').trim().toLowerCase();
      const 鍊欓€?= normalizeInlineEditorOptions(meta);
      if (!鍊欓€?length) return value;
      if (safeKind === 'enum_select') {
        const 鍗曞€?= toText(value, '').trim();
        if (!鍗曞€?|| 鍊欓€?includes(鍗曞€?) return 鍗曞€?
        throw new Error(`鍙兘閫夋嫨鍊欓€夐」锛?{鍊欓€?join(' / ')}`);
      }
      if (safeKind === 'token_multi') {
        const 鍒楄〃鍊?= normalizeEditorStringList(value);
        const 闈炴硶椤?= 鍒楄〃鍊?filter(item => !鍊欓€?includes(item));
        if (闈炴硶椤?length) throw new Error(`瀛樺湪闈炴硶閫夐」锛?{闈炴硶椤?join('銆?)}`);
        return 鍒楄〃鍊?
      }
      return value;
    }

    function bumpInlineEditSession(duration = 320) {
      inlineEditSessionToken += 1;
      const holdMs = Math.max(0, toNumber(duration, 320));
      if (holdMs > 0) inlineEditGuardUntil = Math.max(inlineEditGuardUntil, Date.now() + holdMs);
      return inlineEditSessionToken;
    }

    function isInlineEditGuardActive() {
      return Date.now() < inlineEditGuardUntil;
    }

    function shouldBlockInlineEditRerender(options = {}) {
      if (options && (options.force || options.allowWhileInlineEdit)) return false;
      return hasActiveInlineEdit() || isInlineEditGuardActive();
    }

    function isInlineEditInteractionTarget(target) {
      if (!target || !(target instanceof Element)) return false;
      return !!target.closest('.mvu-inline-editor-input, .mvu-inline-token-editor, .mvu-inline-editor-shell');
    }

    function restoreInlineEditState(state = activeInlineEditState) {
      if (!state) return;
      if (activeInlineEditState === state) activeInlineEditState = null;
      if (state.inputEl && state.inputEl.isConnected) {
        state.inputEl.replaceWith(state.displayEl);
      }
    }

    function hasActiveInlineEdit() {
      return !!(activeInlineEditState && activeInlineEditState.inputEl && activeInlineEditState.inputEl.isConnected);
    }

    function hasFocusedEditorControl() {
      const activeEl = document.activeElement instanceof HTMLElement ? document.activeElement : null;
      if (!activeEl) return false;
      if (activeEl.classList.contains('mvu-inline-editor-input')) return true;
      if (activeEl.closest('.mvu-inline-token-editor')) return true;
      if (!activeEl.closest('.mvu-editor-form, .skill-designer-form')) return false;
      return activeEl.matches('input, textarea, select');
    }

    function hasOpenSkillDesignerModal() {
      return !!(
        (detailModal && detailModal.classList.contains('show') && isSkillDesignerPreviewKey(currentModalPreviewKey))
        || (isUnifiedInlinePreviewActive() && isSkillDesignerPreviewKey(currentUnifiedPreviewKey))
      );
    }

    function shouldPauseLiveRefresh(options = {}) {
      if (options && options.force) return false;
      return shouldBlockInlineEditRerender(options) || hasFocusedEditorControl() || hasOpenSkillDesignerModal();
    }

    function flushPendingLiveRefresh(options = {}) {
      const force = !!(options && options.force);
      if (!pendingLiveRefresh && !force) return;
      pendingLiveRefresh = false;
      window.setTimeout(() => {
        refreshLiveSnapshot({ force: true });
      }, 0);
    }

    function cancelActiveInlineEdit() {
      restoreInlineEditState(activeInlineEditState);
    }

    async function commitInlineEditState(state) {
      if (!state || state.committing) return;
      state.committing = true;
      bumpInlineEditSession(1200);
      let nextValue;
      try {
        nextValue = typeof state.readValue === 'function'
          ? state.readValue(state)
          : parseEditorInputValue(state.inputEl ? state.inputEl.value : '', state.kind, state.editorMeta || {});
        nextValue = 鏍￠獙鍐呰仈闄愰€夊啓鍥炲€?nextValue, state.kind, state.editorMeta || {});
      } catch (error) {
        restoreInlineEditState(state);
        showUiToast(error && error.message ? error.message : '鍙橀噺鏍煎紡涓嶆纭€?, 'error', 4200);
        flushPendingLiveRefresh();
        return;
      }
      const previousComparable = normalizeInlineComparableValue(state.rawValue, state.kind);
      const nextComparable = normalizeInlineComparableValue(nextValue, state.kind);
      restoreInlineEditState(state);
      if (previousComparable === nextComparable) {
        flushPendingLiveRefresh();
        return;
      }
      let previewRollback = null;
      try {
        if (state.editorMeta && state.editorMeta.renameObjectKey === true) {
          await renameStatObjectKeyByEditor(
            state.editorMeta.parentPath || state.path.slice(0, -1),
            state.editorMeta.oldKey || state.path[state.path.length - 1],
            nextValue,
          );
        } else {
          previewRollback = queueTrainedBonusPreviewOverride(state.path, state.rawValue, nextValue);
          await replaceStatDataByEditor([{ path: state.path, value: nextValue }]);
        }
      } catch (error) {
        restoreTrainedBonusPreviewOverride(previewRollback);
        showUiToast(error && error.message ? error.message : '鍙橀噺鍐欏洖澶辫触銆?, 'error', 4200);
      } finally {
        flushPendingLiveRefresh({ force: true });
      }
    }

    function beginInlineEdit(target) {
      if (!target || !(target instanceof HTMLElement)) return;
      if (activeInlineEditState && activeInlineEditState.displayEl === target) return;
      cancelActiveInlineEdit();
      bumpInlineEditSession(800);

      let path = [];
      try {
        path = JSON.parse(target.getAttribute('data-mvu-path') || '[]');
      } catch (error) {}
      if (!Array.isArray(path) || !path.length) return;

      const kind = target.getAttribute('data-value-kind') || 'string';
      const rawValue = target.getAttribute('data-mvu-raw-value') ?? target.textContent ?? '';
      const editorMeta = readInlineEditorMeta(target);
      const rect = target.getBoundingClientRect();
      const isMultiline = target.getAttribute('data-inline-multiline') === '1';
      const state = {
        displayEl: target,
        inputEl: null,
        path,
        kind,
        rawValue,
        editorMeta,
        multiline: isMultiline,
        committing: false,
      };
      activeInlineEditState = state;
      let editorRoot = null;
      let focusTarget = null;

      if (String(kind).trim().toLowerCase() === 'enum_select') {
        const editor = buildInlineEnumSelectEditor(target, rect, rawValue, editorMeta, state);
        editorRoot = editor.root;
        state.inputEl = editor.inputEl;
        focusTarget = editor.focusEl || editor.inputEl;
      } else if (String(kind).trim().toLowerCase() === 'token_multi') {
        const editor = buildInlineTokenMultiEditor(target, rect, rawValue, editorMeta, state);
        editorRoot = editor.root;
        state.inputEl = editor.inputEl;
        focusTarget = editor.focusEl || editor.inputEl;
      } else if (String(kind).trim().toLowerCase() === 'number') {
        const editor = buildInlineNumberEditor(target, rect, rawValue, editorMeta, state);
        editorRoot = editor.root;
        state.inputEl = editor.inputEl;
        focusTarget = editor.focusEl || editor.inputEl;
      } else {
        const input = document.createElement(isMultiline ? 'textarea' : 'input');
        input.className = 'mvu-inline-editor-input';
        if (!isMultiline) {
          input.type = 'text';
        } else {
          input.rows = Math.max(3, Math.min(8, String(formatEditorValue(rawValue, kind)).split(/\r?\n/).length + 1));
          input.style.minHeight = `${Math.max(72, Math.ceil(rect.height) + 28)}px`;
          input.style.resize = 'vertical';
          input.style.whiteSpace = 'pre-wrap';
        }
        input.value = formatEditorValue(rawValue, kind);
        input.style.width = `${Math.max(isMultiline ? 240 : 84, Math.ceil(rect.width) + 28)}px`;
        input.style.minHeight = isMultiline ? input.style.minHeight : `${Math.max(36, Math.ceil(rect.height) + 10)}px`;
        input.style.padding = isMultiline ? '10px 12px' : '0 14px';
        input.style.borderRadius = isMultiline ? '14px' : '999px';
        input.style.border = '1px solid rgba(0, 229, 255, 0.28)';
        input.style.background = 'rgba(4, 14, 24, 0.96)';
        input.style.boxShadow = '0 10px 24px rgba(0, 0, 0, 0.25)';
        input.style.color = 'var(--white)';
        input.style.outline = 'none';
        input.style.font = 'inherit';
        input.style.boxSizing = 'border-box';
        input.addEventListener('keydown', event => {
          if (event.key === 'Enter' && (!state.multiline || event.ctrlKey || event.metaKey)) {
            event.preventDefault();
            event.stopPropagation();
            commitInlineEditState(state);
          } else if (event.key === 'Escape') {
            event.preventDefault();
            event.stopPropagation();
            restoreInlineEditState(state);
            flushPendingLiveRefresh();
          }
        });
        input.addEventListener('click', event => {
          event.stopPropagation();
        });
        input.addEventListener('blur', () => {
          commitInlineEditState(state);
        });
        editorRoot = input;
        state.inputEl = input;
        focusTarget = input;
      }

      target.replaceWith(editorRoot);
      if (focusTarget && typeof focusTarget.focus === 'function') focusTarget.focus();
      if (focusTarget && typeof focusTarget.select === 'function') focusTarget.select();
    }

    function bindInlineEditing() {
      if (window.__mvuInlineEditingBound) return;
      window.__mvuInlineEditingBound = true;

      document.addEventListener('click', event => {
        const eventTarget = event.target instanceof Element ? event.target : (event.target && event.target.parentElement ? event.target.parentElement : null);
        if (!eventTarget) return;
        if (isInlineEditInteractionTarget(eventTarget)) {
          event.stopPropagation();
          return;
        }
        if (activeInlineEditState && activeInlineEditState.manualCommit) {
          restoreInlineEditState(activeInlineEditState);
          flushPendingLiveRefresh();
        }
        if (typeof 鏄惁澶勪簬AI缁存姢閫夋嫨浜や簰 === 'function' && 鏄惁澶勪簬AI缁存姢閫夋嫨浜や簰(eventTarget)) {
          return;
        }
        const inlineTarget = eventTarget.closest('[data-inline-editable="1"]');
        if (!inlineTarget) return;
        event.preventDefault();
        event.stopPropagation();
        beginInlineEdit(inlineTarget);
      }, true);

      document.addEventListener('keydown', event => {
        const eventTarget = event.target instanceof Element ? event.target : (event.target && event.target.parentElement ? event.target.parentElement : null);
        if (!eventTarget) return;
        if (eventTarget.classList.contains('mvu-inline-editor-input')) return;
        if (typeof 鏄惁澶勪簬AI缁存姢閫夋嫨浜や簰 === 'function' && 鏄惁澶勪簬AI缁存姢閫夋嫨浜や簰(eventTarget)) return;
        const inlineTarget = eventTarget.closest('[data-inline-editable="1"]');
        if (!inlineTarget) return;
        if (event.key === 'Enter' || event.key === ' ') {
          event.preventDefault();
          event.stopPropagation();
          beginInlineEdit(inlineTarget);
        }
      }, true);
    }

    const AI缁存姢_API瀛樺偍閿?= 'LWCS_AI缁存姢_API棰勮';
    const AI缁存姢_鏂瑰紡鍒楄〃 = Object.freeze(['琛ュ叏绌哄€?, '鏍规嵁鍓ф儏淇', '鏁寸悊鏍煎紡', '鐢熸垚缂哄け鏉＄洰', '妫€鏌ュ苟淇']);
    const AI缁存姢_鐘舵€?= {
      active: false,
      root: null,
      previewKey: '',
      surfaceOptions: {},
      selected: new Map(),
      busy: false,
    };

    function 鏋勫缓AI缁存姢璺緞閿?pathValue = []) {
      return normalizeEditorPath(pathValue).map(token => String(token)).join('\u0001');
    }

    function 鏍煎紡鍖朅I缁存姢璺緞(pathValue = []) {
      const path = normalizeEditorPath(pathValue);
      if (!path.length) return '';
      return `/${path.map(token => escapeJsonPointerValue(token)).join('/')}`;
    }

    function 闄愰暱AI缁存姢鏂囨湰(value, maxLength = 2600) {
      const raw = typeof value === 'string'
        ? value
        : (() => {
            try { return JSON.stringify(value, null, 2); } catch (error) { return String(value ?? ''); }
          })();
      const text = toText(raw, '');
      if (text.length <= maxLength) return text;
      return `${text.slice(0, Math.max(1, maxLength - 24))}\n...宸叉埅鏂?..`;
    }

    function 鑾峰彇AI缁存姢鏍瑰垪琛?) {
      const roots = [];
      const pushRoot = root => {
        try {
          if (root && typeof root === 'object' && !roots.includes(root)) roots.push(root);
        } catch (error) {}
      };
      try { pushRoot(window); } catch (error) {}
      try { pushRoot(window.parent); } catch (error) {}
      try { pushRoot(window.top); } catch (error) {}
      try { pushRoot(globalThis); } catch (error) {}
      return roots;
    }

    function 鑾峰彇AI缁存姢鏁版嵁搴揂PI() {
      return 鑾峰彇AI缁存姢鏍瑰垪琛?)
        .map(root => {
          try { return root.AutoCardUpdaterAPI; } catch (error) { return null; }
        })
        .find(api => api && typeof api.getApiPresets === 'function' && typeof api.callAI === 'function') || null;
    }

    function 璇诲彇AI缁存姢API棰勮鍒楄〃() {
      const api = 鑾峰彇AI缁存姢鏁版嵁搴揂PI();
      if (!api) return [];
      try {
        const presets = api.getApiPresets();
        return (Array.isArray(presets) ? presets : [])
          .map(item => toText(item && item.name, '').trim())
          .filter(Boolean);
      } catch (error) {
        return [];
      }
    }

    function 璇诲彇AI缁存姢宸查€堿PI棰勮() {
      try {
        return toText(window.localStorage && window.localStorage.getItem(AI缁存姢_API瀛樺偍閿?, '').trim();
      } catch (error) {
        return '';
      }
    }

    function 淇濆瓨AI缁存姢宸查€堿PI棰勮(presetName = '') {
      const safePresetName = toText(presetName, '').trim();
      try {
        if (window.localStorage) window.localStorage.setItem(AI缁存姢_API瀛樺偍閿? safePresetName);
      } catch (error) {}
      return safePresetName;
    }

    function 璇诲彇AI缁存姢鏈夋晥API棰勮() {
      const selected = 璇诲彇AI缁存姢宸查€堿PI棰勮();
      if (!selected) return '';
      return 璇诲彇AI缁存姢API棰勮鍒楄〃().includes(selected) ? selected : '';
    }

    function 鏋勫缓AI缁存姢API閫夋嫨鍣?) {
      const presets = 璇诲彇AI缁存姢API棰勮鍒楄〃();
      const selected = 璇诲彇AI缁存姢鏈夋晥API棰勮();
      const options = [
        `<option value=""${selected ? '' : ' selected'}>榛樿API</option>`,
        ...presets.map(name => `<option value="${escapeHtmlAttr(name)}"${name === selected ? ' selected' : ''}>${htmlEscape(name)}</option>`),
      ].join('');
      return `
        <label class="mvu-ai-maintenance-api" title="閫夋嫨AI缁存姢璋冪敤鐨凙PI">
          <b>AI API</b>
          <select data-ai-maintenance-api-select aria-label="AI缁存姢API" title="閫夋嫨AI缁存姢璋冪敤鐨凙PI">${options}</select>
        </label>
      `;
    }

    function 鏋勫缓AI缁存姢鏍囬鎸夐挳(previewKey = '') {
      const key = toText(previewKey, '').trim();
      if (!key || key === BATTLE_INLINE_PREVIEW_KEY) return '';
      return `<button type="button" class="tag-chip mvu-ai-maintenance-toggle mvu-ai-maintenance-title-button" data-ai-maintenance-toggle data-ai-maintenance-preview="${escapeHtmlAttr(key)}">AI缁存姢</button>`;
    }

    function 鍚屾AI缁存姢鏍囬鎸夐挳鐘舵€?) {
      const 鏄惁婵€娲?= !!(AI缁存姢_鐘舵€?active && AI缁存姢_鐘舵€?root && AI缁存姢_鐘舵€?root.isConnected);
      document.querySelectorAll('[data-ai-maintenance-toggle]').forEach(button => {
        button.classList.toggle('is-ai-active', 鏄惁婵€娲?;
        button.setAttribute('aria-pressed', 鏄惁婵€娲?? 'true' : 'false');
        button.textContent = 鏄惁婵€娲?? '閫夋嫨涓? : 'AI缁存姢';
      });
    }

    function 鏌ユ壘AI缁存姢鏍?previewKey = '') {
      const key = toText(previewKey, '').trim();
      const candidates = Array.from(document.querySelectorAll('[data-ai-maintenance-root]'));
      if (!key) return candidates.find(node => node instanceof Element && node.isConnected) || null;
      return candidates.find(node => node instanceof Element && node.isConnected && toText(node.getAttribute('data-unified-preview'), '').trim() === key) || null;
    }

    function 鍚屾AI缁存姢棣栭〉閰嶇疆鍏ュ彛() {
      const unifiedMount = document.getElementById('mvu-unified-mount');
      if (!unifiedMount) return;
      const homeMarkup = 鏋勫缓AI缁存姢API閫夋嫨鍣?);
      const 宸叉湁鍏ュ彛 = Array.from(unifiedMount.querySelectorAll('[data-ai-maintenance-home-api]'))
        .filter(node => node instanceof Element);
      if (宸叉湁鍏ュ彛.length) {
        宸叉湁鍏ュ彛.forEach(node => { node.innerHTML = homeMarkup; });
        return;
      }
      unifiedMount.querySelectorAll('.mvu-unified-toolbar').forEach(toolbar => {
        if (!(toolbar instanceof Element)) return;
        const 鐩爣瀹瑰櫒 = toolbar.querySelector('.mvu-unified-toolbar-side')
          || (toolbar.classList.contains('is-detail') ? toolbar.querySelector('.mvu-unified-toolbar-main') : null);
        if (!(鐩爣瀹瑰櫒 instanceof Element)) return;
        const 鎻掑叆浣嶇疆 = 鐩爣瀹瑰櫒.classList.contains('mvu-unified-toolbar-main') ? 'beforeend' : 'afterbegin';
        鐩爣瀹瑰櫒.insertAdjacentHTML(鎻掑叆浣嶇疆, `<div class="mvu-ai-maintenance-home-api" data-ai-maintenance-home-api>${homeMarkup}</div>`);
      });
      unifiedMount
        .querySelectorAll('.mvu-mobile-shell-frame[data-screen="home"] .mvu-mobile-shell-head-actions, .mvu-mobile-shell-frame[data-screen="detail"] .mvu-mobile-shell-head-actions')
        .forEach(shellActions => {
          if (!(shellActions instanceof Element)) return;
          shellActions.insertAdjacentHTML('afterbegin', `<div class="mvu-ai-maintenance-home-api mvu-ai-maintenance-home-api--shell" data-ai-maintenance-home-api>${homeMarkup}</div>`);
        });
    }

    function 鍚屾AI缁存姢鏍囬鍏ュ彛(previewKey = '') {
      const key = toText(previewKey, '').trim();
      document.querySelectorAll('[data-ai-maintenance-title-slot]').forEach(node => {
        if (node && node.parentNode) node.parentNode.removeChild(node);
      });
      if (!key || key === BATTLE_INLINE_PREVIEW_KEY) {
        鍚屾AI缁存姢鏍囬鎸夐挳鐘舵€?);
        return;
      }
      const buttonHtml = 鏋勫缓AI缁存姢鏍囬鎸夐挳(key);
      const unifiedMain = document.querySelector('#mvu-unified-mount .mvu-unified-toolbar.is-detail .mvu-unified-toolbar-main');
      if (unifiedMain) {
        unifiedMain.insertAdjacentHTML('beforeend', `<div class="mvu-ai-maintenance-title-slot" data-ai-maintenance-title-slot>${buttonHtml}</div>`);
      }
      const shellActions = document.querySelector('#mvu-unified-mount .mvu-mobile-shell-frame[data-screen="detail"] .mvu-mobile-shell-head-actions');
      if (shellActions) {
        shellActions.insertAdjacentHTML('afterbegin', `<div class="mvu-ai-maintenance-title-slot" data-ai-maintenance-title-slot>${buttonHtml}</div>`);
      }
      const modalTitleWrap = document.querySelector('#detailModal.show .modal-title-wrap');
      if (modalTitleWrap) {
        modalTitleWrap.insertAdjacentHTML('beforeend', `<div class="mvu-ai-maintenance-title-slot mvu-ai-maintenance-title-slot--modal" data-ai-maintenance-title-slot>${buttonHtml}</div>`);
      }
      鍚屾AI缁存姢棣栭〉閰嶇疆鍏ュ彛();
      鍚屾AI缁存姢鏍囬鎸夐挳鐘舵€?);
    }

    function 鏄惁澶勪簬AI缁存姢閫夋嫨浜や簰(target) {
      if (!AI缁存姢_鐘舵€?active || !target || !(target instanceof Element)) return false;
      const root = AI缁存姢_鐘舵€?root;
      return !!(root && root.isConnected && root.contains(target));
    }

    function 瑙ｆ瀽AI缁存姢璺緞灞炴€?element) {
      if (!element || !(element instanceof Element)) return [];
      try {
        const parsed = JSON.parse(element.getAttribute('data-mvu-path') || '[]');
        return normalizeEditorPath(parsed);
      } catch (error) {
        return [];
      }
    }

    function 鏋勫缓AI缁存姢椤甸潰鍙橀噺缁?previewKey = '', snapshot = liveSnapshot || lastRenderableSnapshot || {}) {
      const activeName = toText(snapshot && snapshot.activeName, '');
      const activeCharKey = resolveSnapshotCharKey(snapshot, activeName) || activeName;
      const groups = [];
      const add = (label, path, keywords = []) => {
        const safePath = normalizeEditorPath(path);
        if (!label || !safePath.length) return;
        groups.push({
          label,
          path: safePath,
          keywords: (Array.isArray(keywords) ? keywords : []).map(item => toText(item, '').trim()).filter(Boolean),
        });
      };

      const key = toText(previewKey, '').trim();
      if (activeCharKey) {
        if (key === '浠诲姟鐣岄潰') {
          add('鎴戠殑浠诲姟', ['char', activeCharKey, '璁板綍'], ['鎴戠殑浠诲姟', '浠诲姟璇︽儏', '浠诲姟']);
          add('浠诲姟璇锋眰', ['char', activeCharKey, '浠诲姟璇锋眰'], ['浠诲姟璇锋眰', '褰撳墠璺嚎']);
        }
        if (key === '浜虹墿鍏崇郴璇︾粏椤?) add('浜虹墿鍏崇郴', ['char', activeCharKey, '绀句氦', '鍏崇郴'], ['鍏崇郴', '浜虹墿鍏崇郴', '浜掑姩']);
        if (key === '鎯呮姤搴撹缁嗛〉' || key === '璇曠偧涓庢儏鎶?) {
          add('宸叉帉鎻℃儏鎶?, ['char', activeCharKey, '宸叉帉鎻℃儏鎶?], ['杩戞湡鎯呮姤', '宸叉帉鎻℃儏鎶?, '鎯呮姤']);
          add('瑙掕壊璁板綍', ['char', activeCharKey, '璁板綍'], ['浠诲姟璁板綍', '璁板綍']);
        }
        if (key === '鍌ㄧ墿浠撳簱璇︾粏椤?) {
          add('鑳屽寘', ['char', activeCharKey, '鑳屽寘'], ['鑳屽寘', '鐗╁搧', '浠撳簱']);
          add('璐㈠瘜', ['char', activeCharKey, '璐㈠瘜'], ['璐㈠瘜', '璐у竵']);
        }
        if (key === '姝﹁宸ュ潑璇︾粏椤? || key.startsWith('姝﹁璇︽儏')) {
          add('瑁呭', ['char', activeCharKey, '瑁呭'], ['瑁呭', '姝﹁', '鏂楅摖', '鏈虹敳']);
          add('鑱屼笟', ['char', activeCharKey, '鑱屼笟'], ['鑱屼笟', '宸ュ潑']);
        }
        if (key === '鐢熷懡鍥捐氨璇︾粏椤? || key === '鐢熷懡鍥捐氨璇︽儏椤?) {
          add('鐘舵€?, ['char', activeCharKey, '鐘舵€?], ['鐘舵€?, '褰撳墠鐘舵€?]);
          add('灞炴€?, ['char', activeCharKey, '灞炴€?], ['灞炴€?, '鐢熷懡浣撳緛']);
          add('澶栬矊', ['char', activeCharKey, '澶栬矊'], ['澶栬矊', '澶栬']);
        }
        if (key === '绀句細妗ｆ璇︾粏椤?) add('绀句細妗ｆ', ['char', activeCharKey, '绀句氦'], ['绀句細', '鍚嶆湜', '绉板彿']);
        if (key === '绗竴姝﹂瓊璇︾粏椤? || key === '绗簩姝﹂瓊璇︾粏椤? || key === '姝﹂瓊铻嶅悎鎶€璇︾粏椤?) {
          add('姝﹂瓊', ['char', activeCharKey, '姝﹂瓊'], ['姝﹂瓊', '榄傜伒', '榄傜幆']);
          add('铻嶅悎鎶€', ['char', activeCharKey, '姝﹂瓊铻嶅悎鎶€'], ['铻嶅悎']);
        }
        if (key === '琛€鑴夊皝鍗拌缁嗛〉') add('琛€鑴?, ['char', activeCharKey, '琛€鑴変箣鍔?], ['琛€鑴?, '灏佸嵃']);
        if (key === '褰撳墠鑺傜偣璇︽儏') add('浣嶇疆鐘舵€?, ['char', activeCharKey, '鐘舵€?, '浣嶇疆'], ['褰撳墠鑺傜偣', '鎵€鍦ㄤ綅缃?]);
        if (key === '璇曠偧涓庢儏鎶?) add('榄傜伒濉旇褰?, ['char', activeCharKey, '榄傜伒濉旇褰?], ['榄傜伒濉?, '璇曠偧鎬昏']);
      }

      if (key === '浠诲姟鐣岄潰') add('濮旀墭鏉?, ['world', '濮旀墭鏉?], ['濮旀墭鏉?, '濮旀墭璇︽儏', '鍙戝竷濮旀墭']);
      if (key === '绯荤粺鎾姤涓庢棩蹇?) {
        add('绯荤粺鎾姤', ['sys', '绯荤粺鎾姤'], ['绯荤粺骞挎挱', '鏈€杩戞挱鎶?]);
        add('鏃堕棿绾?, ['world', '鏃堕棿绾?], ['鏈€杩戜簨浠?, '缂栧勾', '鏃堕棿绾?]);
      }
      if (key === '鎿嶄綔鎬荤嚎') {
        add('鏃堕棿绾?, ['world', '鏃堕棿绾?], ['涓栫晫鏃ョ▼', '瀹夋帓']);
        if (activeCharKey) add('涓汉寰呭姙', ['char', activeCharKey, '璁板綍'], ['涓汉寰呭姙', '璇锋眰鎽樿']);
      }
      if (key === '璇曠偧涓庢儏鎶?) {
        add('鏈哄瘑鎯呮姤', ['world', '鏈哄瘑鎯呮姤'], ['鎯呮姤', '寰呮牳瀹?]);
        add('鍥鹃壌', ['world', '鍥鹃壌'], ['鍥鹃壌', '鎬墿']);
      }
      if (key === '杩戞湡瑙侀椈' || key === '缂栧勾鍙叉。妗? || key === '涓栫晫鐘舵€佹€昏') add('鏃堕棿绾?, ['world', '鏃堕棿绾?], ['瑙侀椈', '缂栧勾', '鏃堕棿绾?, '浜嬩欢']);
      if (key === '涓栫晫鐘舵€佹€昏' || key === '褰撳墠鑺傜偣璇︽儏' || key.startsWith('鍦板浘鑺傜偣锛?)) {
        add('鍦扮偣', ['world', '鍦扮偣'], ['鍦扮偣', '褰撳墠鑺傜偣', '鎹偣']);
        add('鍔ㄦ€佸湴鐐?, ['world', '鍔ㄦ€佸湴鐐?], ['鍔ㄦ€佸湴鐐?, '鎵╁睍鑺傜偣']);
      }
      if (key === '鍔垮姏鐭╅樀鎬昏' || key === '鎴戠殑闃佃惀璇︽儏' || key === '鎵€灞炲娍鍔涜缁嗛〉') add('鍔垮姏', ['org'], ['鍔垮姏', '闃佃惀']);
      if (key === '鎬墿鍥鹃壌') add('鍥鹃壌', ['world', '鍥鹃壌'], ['鍥鹃壌', '鎬墿']);
      if (key === '鎷嶅崠涓庤鎶?) add('鎷嶅崠', ['world', '鎷嶅崠'], ['鎷嶅崠', '璀︽姤']);
      return groups;
    }

    function 鑾峰彇AI缁存姢缁勫畾涔?pathKey = '') {
      const groups = 鏋勫缓AI缁存姢椤甸潰鍙橀噺缁?AI缁存姢_鐘舵€?previewKey, liveSnapshot || lastRenderableSnapshot || {});
      return groups.find(group => 鏋勫缓AI缁存姢璺緞閿?group.path) === pathKey) || null;
    }

    function 鑾峰彇AI缁存姢鑺傜偣鏍囩(node, path = []) {
      const safePath = normalizeEditorPath(path);
      const leaf = toText(safePath[safePath.length - 1], '').trim();
      const card = node && typeof node.closest === 'function'
        ? node.closest('.archive-card, .timeline-card, .intel-card, .relation-card, .archive-tile, .role-switch-tile, .dossier-row, .simple-row')
        : null;
      const titleNode = card
        ? card.querySelector('.archive-card-title, .simple-title, .role-switch-head b, b')
        : null;
      const title = toText(titleNode ? titleNode.textContent : '', '').replace(/\s+/g, ' ').trim();
      const inlineText = toText(node && node.textContent, '').replace(/\s+/g, ' ').trim();
      const valueText = inlineText && inlineText.length <= 18 ? inlineText : '';
      return [title, leaf, valueText].filter(Boolean).join(' / ') || 鏍煎紡鍖朅I缁存姢璺緞(safePath);
    }

    function 鏍囪AI缁存姢鍙€夌洰鏍?root, previewKey = '') {
      if (!root || !(root instanceof Element)) return 0;
      let count = 0;
      root.querySelectorAll('[data-mvu-path]').forEach(node => {
        const path = 瑙ｆ瀽AI缁存姢璺緞灞炴€?node);
        if (!path.length) return;
        const label = 鑾峰彇AI缁存姢鑺傜偣鏍囩(node, path);
        node.classList.add('mvu-ai-maintenance-selectable');
        node.setAttribute('data-ai-maintenance-field', 鏋勫缓AI缁存姢璺緞閿?path));
        node.setAttribute('data-ai-maintenance-label', label);
        node.setAttribute('title', node.getAttribute('title') || 鏍煎紡鍖朅I缁存姢璺緞(path));
        count += 1;
      });

      const groups = 鏋勫缓AI缁存姢椤甸潰鍙橀噺缁?previewKey, liveSnapshot || lastRenderableSnapshot || {});
      const cards = Array.from(root.querySelectorAll('.archive-card, .timeline-card, .intel-card, .relation-card, .archive-tile, .role-switch-tile'));
      groups.forEach(group => {
        const pathKey = 鏋勫缓AI缁存姢璺緞閿?group.path);
        const keywords = group.keywords && group.keywords.length ? group.keywords : [group.label];
        const matched = cards.filter(card => {
          const titleNode = card.querySelector('.archive-card-title, b, .simple-title, .role-switch-head');
          const titleText = toText(titleNode ? titleNode.textContent : card.textContent, '').trim();
          return keywords.some(keyword => keyword && titleText.includes(keyword));
        });
        matched.slice(0, 6).forEach(card => {
          if (card.hasAttribute('data-ai-maintenance-group')) return;
          card.classList.add('mvu-ai-maintenance-group-target');
          card.setAttribute('data-ai-maintenance-group', pathKey);
          card.setAttribute('data-ai-maintenance-label', group.label);
          card.setAttribute('title', card.getAttribute('title') || `${group.label} ${鏍煎紡鍖朅I缁存姢璺緞(group.path)}`);
          count += 1;
        });
      });
      return count;
    }

    function 娓呯悊AI缁存姢鐩爣(root) {
      if (!root || !(root instanceof Element)) return;
      root.querySelectorAll('.mvu-ai-maintenance-selectable').forEach(node => {
        node.classList.remove('mvu-ai-maintenance-selectable', 'is-ai-selected', 'is-ai-click-feedback');
        node.removeAttribute('data-ai-maintenance-field');
        node.removeAttribute('data-ai-maintenance-label');
        node.removeAttribute('data-ai-maintenance-selected-order');
      });
      root.querySelectorAll('.mvu-ai-maintenance-group-target').forEach(node => {
        node.classList.remove('mvu-ai-maintenance-group-target', 'is-ai-selected', 'is-ai-click-feedback');
        node.removeAttribute('data-ai-maintenance-group');
        node.removeAttribute('data-ai-maintenance-label');
        node.removeAttribute('data-ai-maintenance-selected-order');
      });
      const bar = root.querySelector('[data-ai-maintenance-bar]');
      if (bar) bar.remove();
    }

    function 鏋勫缓AI缁存姢娴潯() {
      const modeOptions = AI缁存姢_鏂瑰紡鍒楄〃
        .map((mode, index) => `<option value="${escapeHtmlAttr(mode)}"${index === 0 ? ' selected' : ''}>${htmlEscape(mode)}</option>`)
        .join('');
      return `
        <div class="mvu-ai-maintenance-bar" data-ai-maintenance-bar>
          <div class="mvu-ai-maintenance-selection-panel">
            <div class="mvu-ai-maintenance-count" data-ai-maintenance-count>鏈€夋嫨</div>
            <div class="mvu-ai-maintenance-selected-list" data-ai-maintenance-selected-list><span class="mvu-ai-maintenance-empty">鐐瑰嚮楂樹寒瀛楁鎴栧崱鐗?/span></div>
          </div>
          <select class="mvu-ai-maintenance-mode-select" data-ai-maintenance-mode aria-label="缁存姢鏂瑰紡">${modeOptions}</select>
          <input class="mvu-ai-maintenance-extra" data-ai-maintenance-extra placeholder="棰濆瑕佹眰" />
          <button type="button" class="tag-chip live" data-ai-maintenance-action="submit">鎻愪氦</button>
          <button type="button" class="tag-chip" data-ai-maintenance-action="clear">娓呯┖</button>
          <button type="button" class="tag-chip" data-ai-maintenance-action="cancel">鍙栨秷</button>
        </div>
      `;
    }

    function 鏇存柊AI缁存姢娴潯(root = AI缁存姢_鐘舵€?root) {
      if (!root || !(root instanceof Element)) return;
      const selectedEntries = Array.from(AI缁存姢_鐘舵€?selected.entries());
      const selectedOrderMap = new Map(selectedEntries.map(([key], index) => [key, String(index + 1)]));
      const countEl = root.querySelector('[data-ai-maintenance-count]');
      if (countEl) {
        const labelText = selectedEntries
          .slice(0, 3)
          .map(([, item], index) => `#${index + 1} ${shortenText(item.label || 鏍煎紡鍖朅I缁存姢璺緞(item.path), 14)}`)
          .join(' / ');
        countEl.textContent = selectedEntries.length
          ? `宸查€?${selectedEntries.length} 椤?路 ${labelText}${selectedEntries.length > 3 ? ` / +${selectedEntries.length - 3}` : ''}`
          : '鏈€夋嫨';
      }
      const listEl = root.querySelector('[data-ai-maintenance-selected-list]');
      if (listEl) {
        listEl.innerHTML = selectedEntries.length
          ? selectedEntries.slice(0, 10).map(([, item], index) => {
              const label = item.label || 鏍煎紡鍖朅I缁存姢璺緞(item.path);
              const pathText = 鏍煎紡鍖朅I缁存姢璺緞(item.path);
              return `
              <span class="mvu-ai-maintenance-selected-chip ${item.kind === '鍙橀噺缁? ? 'is-group' : 'is-field'}" title="${escapeHtmlAttr(`${item.kind} ${item.label || 鏍煎紡鍖朅I缁存姢璺緞(item.path)} ${鏍煎紡鍖朅I缁存姢璺緞(item.path)}`)}">
                <b>#${index + 1} ${htmlEscape(item.kind)}</b>
                <span>${htmlEscape(shortenText(label, 30))}</span>
                <small>${htmlEscape(shortenText(pathText, 56))}</small>
              </span>
            `;
            }).join('') + (selectedEntries.length > 10 ? `<span class="mvu-ai-maintenance-selected-chip is-more">+${selectedEntries.length - 10}</span>` : '')
          : '<span class="mvu-ai-maintenance-empty">鐐瑰嚮楂樹寒瀛楁鎴栧崱鐗?/span>';
      }
      root.querySelectorAll('[data-ai-maintenance-field], [data-ai-maintenance-group]').forEach(node => {
        const fieldKey = node.getAttribute('data-ai-maintenance-field');
        const groupKey = node.getAttribute('data-ai-maintenance-group');
        const selectedKey = fieldKey ? `瀛楁:${fieldKey}` : (groupKey ? `鍙橀噺缁?${groupKey}` : '');
        const selected = selectedKey && AI缁存姢_鐘舵€?selected.has(selectedKey);
        node.classList.toggle('is-ai-selected', !!selected);
        if (selected) node.setAttribute('data-ai-maintenance-selected-order', selectedOrderMap.get(selectedKey) || '');
        else node.removeAttribute('data-ai-maintenance-selected-order');
      });
      鍚屾AI缁存姢鏍囬鎸夐挳鐘舵€?);
    }

    function 鎾斁AI缁存姢閫夋嫨鍙嶉(node) {
      if (!node || !(node instanceof Element)) return;
      node.classList.remove('is-ai-click-feedback');
      void node.offsetWidth;
      node.classList.add('is-ai-click-feedback');
      window.setTimeout(() => {
        if (node && node.classList) node.classList.remove('is-ai-click-feedback');
      }, 420);
    }

    function 鍚姩AI缁存姢妯″紡(root, previewKey = '', options = {}) {
      if (!root || !(root instanceof Element)) return false;
      if (AI缁存姢_鐘舵€?active && AI缁存姢_鐘舵€?root === root) {
        缁撴潫AI缁存姢妯″紡();
        return true;
      }
      缁撴潫AI缁存姢妯″紡();
      AI缁存姢_鐘舵€?active = true;
      AI缁存姢_鐘舵€?root = root;
      AI缁存姢_鐘舵€?previewKey = toText(previewKey, '').trim();
      AI缁存姢_鐘舵€?surfaceOptions = options || {};
      AI缁存姢_鐘舵€?selected = new Map();
      AI缁存姢_鐘舵€?busy = false;
      root.classList.add('mvu-ai-maintenance-mode');
      const targetCount = 鏍囪AI缁存姢鍙€夌洰鏍?root, AI缁存姢_鐘舵€?previewKey);
      root.insertAdjacentHTML('beforeend', 鏋勫缓AI缁存姢娴潯());
      鏇存柊AI缁存姢娴潯(root);
      if (!targetCount) {
        showUiToast('褰撳墠椤垫病鏈夊彲缁存姢鍙橀噺銆?, 'info', 2400);
      } else {
        showUiToast('閫夋嫨瑕佷氦缁?AI 缁存姢鐨勫彉閲忋€?, 'info', 1800);
      }
      return true;
    }

    function 缁撴潫AI缁存姢妯″紡() {
      const root = AI缁存姢_鐘舵€?root;
      if (root && root instanceof Element) {
        root.classList.remove('mvu-ai-maintenance-mode');
        娓呯悊AI缁存姢鐩爣(root);
      }
      AI缁存姢_鐘舵€?active = false;
      AI缁存姢_鐘舵€?root = null;
      AI缁存姢_鐘舵€?previewKey = '';
      AI缁存姢_鐘舵€?surfaceOptions = {};
      AI缁存姢_鐘舵€?selected = new Map();
      AI缁存姢_鐘舵€?busy = false;
      鍚屾AI缁存姢鏍囬鎸夐挳鐘舵€?);
    }

    function 鍒囨崲AI缁存姢瀛楁閫夋嫨(node) {
      const path = 瑙ｆ瀽AI缁存姢璺緞灞炴€?node);
      if (!path.length) return;
      const pathKey = 鏋勫缓AI缁存姢璺緞閿?path);
      const key = `瀛楁:${pathKey}`;
      if (AI缁存姢_鐘舵€?selected.has(key)) AI缁存姢_鐘舵€?selected.delete(key);
      else AI缁存姢_鐘舵€?selected.set(key, { kind: '瀛楁', label: toText(node && node.getAttribute('data-ai-maintenance-label'), '').trim() || 鏍煎紡鍖朅I缁存姢璺緞(path), path });
      鎾斁AI缁存姢閫夋嫨鍙嶉(node);
      鏇存柊AI缁存姢娴潯();
    }

    function 鍒囨崲AI缁存姢鍙橀噺缁勯€夋嫨(node) {
      const pathKey = toText(node && node.getAttribute('data-ai-maintenance-group'), '').trim();
      if (!pathKey) return;
      const group = 鑾峰彇AI缁存姢缁勫畾涔?pathKey);
      if (!group) return;
      const key = `鍙橀噺缁?${pathKey}`;
      if (AI缁存姢_鐘舵€?selected.has(key)) AI缁存姢_鐘舵€?selected.delete(key);
      else AI缁存姢_鐘舵€?selected.set(key, { kind: '鍙橀噺缁?, label: group.label, path: group.path });
      鎾斁AI缁存姢閫夋嫨鍙嶉(node);
      鏇存柊AI缁存姢娴潯();
    }

    function 娓呯┖AI缁存姢閫夋嫨() {
      AI缁存姢_鐘舵€?selected.clear();
      鏇存柊AI缁存姢娴潯();
    }

    function 璺緞鏄惁鍦ˋI缁存姢杈圭晫鍐?changedPath = [], allowedPath = []) {
      const changed = normalizeEditorPath(changedPath);
      const allowed = normalizeEditorPath(allowedPath);
      if (!changed.length || !allowed.length) return false;
      if (changed.length < allowed.length) return false;
      return allowed.every((token, index) => String(changed[index]) === String(token));
    }

    function 鏀堕泦AI缁存姢宸紓璺緞(previousValue, nextValue, basePath = [], result = []) {
      if (JSON.stringify(previousValue) === JSON.stringify(nextValue)) return result;
      const prevIsObject = previousValue && typeof previousValue === 'object';
      const nextIsObject = nextValue && typeof nextValue === 'object';
      if (!prevIsObject || !nextIsObject || Array.isArray(previousValue) || Array.isArray(nextValue)) {
        result.push(basePath.slice());
        return result;
      }
      const keys = new Set([...Object.keys(previousValue), ...Object.keys(nextValue)]);
      keys.forEach(key => {
        鏀堕泦AI缁存姢宸紓璺緞(previousValue[key], nextValue[key], [...basePath, key], result);
      });
      return result;
    }

    function 鏋勫缓AI缁存姢涓婁笅鏂囨憳瑕?snapshot = liveSnapshot || lastRenderableSnapshot || {}) {
      const sys = deepGet(snapshot, 'rootData.sys', {});
      const latestTimeline = snapshot && snapshot.latestTimeline ? snapshot.latestTimeline[0] : '';
      return [
        `褰撳墠瑙掕壊锛?{toText(snapshot && snapshot.activeName, '鏈煡')}`,
        `褰撳墠浣嶇疆锛?{toText(snapshot && snapshot.currentLoc, '鏈煡')}`,
        `绯荤粺鎾姤锛?{shortenText(toText(sys && sys.绯荤粺鎾姤, '鏆傛棤'), 120)}`,
        `鏈€杩戜簨浠讹細${shortenText(toText(latestTimeline, '鏆傛棤'), 120)}`,
      ].join('\n');
    }

    function 鏋勫缓AI缁存姢璋冪敤娑堟伅(selectionItems = [], oldMvuData = {}, mode = '琛ュ叏绌哄€?, extra = '') {
      const statData = oldMvuData && oldMvuData.stat_data ? oldMvuData.stat_data : {};
      const selectionText = selectionItems.map((item, index) => {
        const value = deepGet(statData, item.path, undefined);
        const valueLimit = item.kind === '鍙橀噺缁? ? 5200 : 1600;
        return [
          `${index + 1}. ${item.kind} ${item.label || 鏍煎紡鍖朅I缁存姢璺緞(item.path)}`,
          `璺緞锛?{鏍煎紡鍖朅I缁存姢璺緞(item.path)}`,
          `褰撳墠鍊硷細${闄愰暱AI缁存姢鏂囨湰(value, valueLimit)}`,
        ].join('\n');
      }).join('\n\n');
      const boundaryText = selectionItems
        .map(item => `- ${item.kind} ${item.label || 鏍煎紡鍖朅I缁存姢璺緞(item.path)}锛?{鏍煎紡鍖朅I缁存姢璺緞(item.path)}`)
        .join('\n');
      const userText = [
        '[缁存姢鏂瑰紡]',
        mode,
        '',
        '[棰濆瑕佹眰]',
        toText(extra, '').trim() || '鏃?,
        '',
        '[灏戦噺涓婁笅鏂嘳',
        鏋勫缓AI缁存姢涓婁笅鏂囨憳瑕?liveSnapshot || lastRenderableSnapshot || {}),
        '',
        '[鍏佽缁存姢杈圭晫]',
        boundaryText,
        '',
        '[褰撳墠鍙橀噺鍊糫',
        selectionText || '鏃?,
      ].join('\n');
      return [
        {
          role: 'system',
          content: [
            '浣犳槸 LWCS 鐨勯潤榛?MVU 鍙橀噺缁存姢鍣ㄣ€?,
            '鍙緭鍑轰竴涓?<UpdateVariable> 鍙橀噺鏇存柊鍧楋紝涓嶈緭鍑哄墽鎯呮鏂囥€佽В閲娿€佸瘨鏆勬垨 Markdown銆?,
            '鏇存柊鍧楀唴浣跨敤 <JSONPatch> 杈撳嚭 JSON Patch 鏁扮粍锛岃矾寰勫繀椤讳娇鐢?/char/...銆?world/...銆?sys/...銆?org/... 褰㈠紡銆?,
            '鍙兘淇敼鐢ㄦ埛閫変腑鐨勮矾寰勬垨鍙橀噺缁勫唴閮ㄨ矾寰勩€傜姝㈡柊澧?MVU 缁撴瀯棰勭暀瀛楁锛岀姝慨鏀规湭閫変腑鐨勫彉閲忋€?,
            '鑻ユ棤闇€淇敼锛岃緭鍑虹┖鏁扮粍 []銆?
          ].join('\n')
        },
        {
          role: 'user',
          content: userText
        }
      ];
    }

    function 鑾峰彇AI缁存姢Mvu瑙ｆ瀽瀹夸富() {
      return 鑾峰彇AI缁存姢鏍瑰垪琛?)
        .map(root => {
          try { return root.Mvu; } catch (error) { return null; }
        })
        .find(host => host && typeof host.parseMessage === 'function') || null;
    }

    function 鍑嗗AI缁存姢鍐欏洖鏁版嵁(oldMvuData, parsedMvuData) {
      const parsedEnvelope = normalizeMvuDataEnvelopeForEditor(parsedMvuData);
      const nextMvuData = cloneJsonValue(oldMvuData, {});
      const nextStatData = buildSafeStatDataForEditorWrite(parsedEnvelope.stat_data, oldMvuData.stat_data);
      if (oldMvuData && oldMvuData.stat_data && Object.prototype.hasOwnProperty.call(oldMvuData.stat_data, 'display_all')) {
        nextStatData.display_all = cloneJsonValue(oldMvuData.stat_data.display_all, {});
      } else {
        delete nextStatData.display_all;
      }
      nextMvuData.stat_data = nextStatData;
      return nextMvuData;
    }

    function 鏍￠獙AI缁存姢鍐欏洖杈圭晫(oldMvuData, nextMvuData, selectionItems = []) {
      const allowedPaths = selectionItems.map(item => normalizeEditorPath(item.path)).filter(path => path.length);
      const oldStat = cloneJsonValue(oldMvuData && oldMvuData.stat_data, {});
      const nextStat = cloneJsonValue(nextMvuData && nextMvuData.stat_data, {});
      delete oldStat.display_all;
      delete nextStat.display_all;
      const changedPaths = 鏀堕泦AI缁存姢宸紓璺緞(oldStat, nextStat, []);
      if (!changedPaths.length) return { ok: false, reason: 'AI 鏈敓鎴愭湁鏁堝彉閲忔敼鍔ㄣ€?, changedPaths };
      const outOfRange = changedPaths.filter(path => !allowedPaths.some(allowed => 璺緞鏄惁鍦ˋI缁存姢杈圭晫鍐?path, allowed)));
      if (outOfRange.length) {
        return {
          ok: false,
          reason: `AI 璇曞浘淇敼鏈€変腑鐨勫彉閲忥細${outOfRange.slice(0, 4).map(鏍煎紡鍖朅I缁存姢璺緞).join('銆?)}`,
          changedPaths,
          outOfRange,
        };
      }
      return { ok: true, changedPaths };
    }

    async function 鎻愪氦AI缁存姢璇锋眰(root = AI缁存姢_鐘舵€?root) {
      if (!AI缁存姢_鐘舵€?active || AI缁存姢_鐘舵€?busy) return;
      const selectionItems = Array.from(AI缁存姢_鐘舵€?selected.values());
      if (!selectionItems.length) {
        showUiToast('璇峰厛閫夋嫨鍙橀噺銆?, 'info', 1800);
        return;
      }
      const api = 鑾峰彇AI缁存姢鏁版嵁搴揂PI();
      if (!api) {
        showUiToast('鏁版嵁搴?AI API 涓嶅彲鐢ㄣ€?, 'error', 3600);
        return;
      }
      const parseHost = 鑾峰彇AI缁存姢Mvu瑙ｆ瀽瀹夸富();
      if (!parseHost) {
        showUiToast('MVU 瑙ｆ瀽鎺ュ彛涓嶅彲鐢ㄣ€?, 'error', 3600);
        return;
      }
      const bar = root && root.querySelector ? root.querySelector('[data-ai-maintenance-bar]') : null;
      const modeEl = bar ? bar.querySelector('[data-ai-maintenance-mode]') : null;
      const extraEl = bar ? bar.querySelector('[data-ai-maintenance-extra]') : null;
      const mode = toText(modeEl && modeEl.value, '琛ュ叏绌哄€?);
      const extra = toText(extraEl && extraEl.value, '').trim();
      AI缁存姢_鐘舵€?busy = true;
      if (bar) bar.classList.add('is-busy');
      try {
        const { host, mvuData, messageId } = await readLatestMvuDataByEditor();
        const messages = 鏋勫缓AI缁存姢璋冪敤娑堟伅(selectionItems, mvuData, mode, extra);
        const presetName = 璇诲彇AI缁存姢鏈夋晥API棰勮();
        const aiText = await api.callAI(messages, { presetName, max_tokens: 2200 });
        if (!aiText || typeof aiText !== 'string') throw new Error('AI 娌℃湁杩斿洖鍙В鏋愬唴瀹广€?);
        const parsed = await Promise.resolve(parseHost.parseMessage(aiText, mvuData));
        const nextMvuData = 鍑嗗AI缁存姢鍐欏洖鏁版嵁(mvuData, parsed);
        const check = 鏍￠獙AI缁存姢鍐欏洖杈圭晫(mvuData, nextMvuData, selectionItems);
        if (!check.ok) throw new Error(check.reason || '鍙橀噺鏀瑰姩鏈€氳繃杈圭晫妫€鏌ャ€?);
        await Promise.resolve(host.replaceMvuData(nextMvuData, { type: 'message', message_id: messageId }));
        writeMvuEditorStoreSnapshot(nextMvuData.stat_data, { messageId });
        await refreshLiveSnapshot({ force: true });
        showUiToast(`AI缁存姢宸插啓鍥?${check.changedPaths.length} 椤广€俙, 'info', 2600);
        const previewKey = AI缁存姢_鐘舵€?previewKey;
        const surfaceOptions = AI缁存姢_鐘舵€?surfaceOptions || {};
        缁撴潫AI缁存姢妯″紡();
        if (previewKey) rerenderDetailSurface(previewKey, { ...surfaceOptions, force: true });
      } catch (error) {
        showUiToast(error && error.message ? error.message : 'AI缁存姢澶辫触銆?, 'error', 5200);
      } finally {
        AI缁存姢_鐘舵€?busy = false;
        if (bar) bar.classList.remove('is-busy');
      }
    }

    function formatAge(age) {
      const value = toNumber(age, 0);
      if (value >= 100000) return `${(value / 10000).toFixed(value % 10000 === 0 ? 0 : 1)}涓囧勾`;
      if (value >= 10000) return `${(value / 10000).toFixed(value % 10000 === 0 ? 0 : 1)}涓囧勾`;
      if (value > 0) return `${formatNumber(value)}骞碻;
      return '鏈畾';
    }

    function shortenText(value, max = 18) {
      const text = toText(value, '');
      if (!text) return '';
      return text.length > max ? `${text.slice(0, Math.max(1, max - 1))}鈥 : text;
    }

    function getRingColorKey(color = '') {
      return RING_COLOR_ALIAS_MAP[toText(color, '').trim()] || '';
    }

    function resolveRingTier(color, age, options = {}) {
      const key = getRingColorKey(color);
      const years = toNumber(age, NaN);
      const forceGold = !!(options && options.forceGold);

      if (forceGold) return 'gold';
      if (key) return key;

      if (Number.isFinite(years) && years > 0) {
        if (years >= 200000) return 'orangegold';
        if (years >= 100000) return 'red';
        if (years >= 10000) return 'black';
        if (years >= 1000) return 'purple';
        if (years >= 100) return 'yellow';
        return 'white';
      }
      return 'white';
    }

    function mapRingClass(color, age, options = {}) {
      return `ring-${resolveRingTier(color, age, options)}`;
    }

    function getRingDisplayColorLabel(color, age, options = {}) {
      const directKey = getRingColorKey(color);
      if (directKey && RING_COLOR_META[directKey]) return RING_COLOR_META[directKey].label;
      const tier = resolveRingTier(color, age, options);
      return RING_COLOR_META[tier] ? RING_COLOR_META[tier].label : normalizeSkillUiText(color, '鏈畾');
    }

    function ringGlyph(color, index, age, options = {}) {
      const tier = resolveRingTier(color, age, options);
      const directKey = getRingColorKey(color);
      const resolvedKey = directKey || tier;
      return (RING_COLOR_META[resolvedKey] && RING_COLOR_META[resolvedKey].glyph) || String(index || '鐜?);
    }

    function getMvuHost() {
      const candidates = [];
      const bridgedHost = ensureBridgedMvuHost();
      if (bridgedHost) candidates.push(bridgedHost);
      try {
        if (window.Mvu) candidates.push(window.Mvu);
      } catch (err) {}
      try {
        if (window.top && window.top !== window && window.top.Mvu) candidates.push(window.top.Mvu);
      } catch (err) {}
      try {
        if (typeof Mvu !== 'undefined' && Mvu) candidates.push(Mvu);
      } catch (err) {}
      return candidates.find(item => item) || null;
    }

    function collectMvuProviderCandidates() {
      const providers = [];
      const seen = new Set();
      const pushProvider = (label, candidate) => {
        if (!candidate || (typeof candidate !== 'object' && typeof candidate !== 'function')) return;
        if (seen.has(candidate)) return;
        const hasRead =
          typeof candidate.getAllVariables === 'function' ||
          typeof candidate.getVariables === 'function' ||
          typeof candidate.getMvuData === 'function';
        const hasWrite =
          typeof candidate.replaceVariables === 'function' ||
          typeof candidate.replaceMvuData === 'function';
        if (!hasRead || !hasWrite) return;
        seen.add(candidate);
        providers.push({ label, candidate });
      };

      try { pushProvider('褰撳墠绐楀彛', window); } catch (err) {}
      try {
        if (window.parent && window.parent !== window) pushProvider('鐖剁獥鍙?, window.parent);
      } catch (err) {}
      try {
        if (window.top && window.top !== window) pushProvider('椤跺眰绐楀彛', window.top);
      } catch (err) {}
      try {
        Array.from(window.frames || []).forEach((frame, index) => {
          const frameName = String(frame?.name || `frame_${index}`).trim() || `frame_${index}`;
          pushProvider(`瀛愭鏋?${frameName}`, frame);
        });
      } catch (err) {}

      return providers;
    }

    function buildBridgedMvuHost(providerInfo) {
      const provider = providerInfo && providerInfo.candidate;
      if (!provider) return null;
      return {
        __bridgeSource: String(providerInfo.label || '').trim() || '鏈煡鏉ユ簮',
        getAllVariables(...args) {
          if (typeof provider.getAllVariables === 'function') return provider.getAllVariables(...args);
          if (typeof provider.getVariables === 'function') return provider.getVariables(...args);
          if (typeof provider.getMvuData === 'function') return provider.getMvuData(...args);
          return null;
        },
        getMvuData(options = {}) {
          if (typeof provider.getMvuData === 'function') return provider.getMvuData(options);
          if (typeof provider.getVariables === 'function') return provider.getVariables(options);
          if (typeof provider.getAllVariables === 'function') return provider.getAllVariables(options);
          return null;
        },
        getVariables(options = {}) {
          if (typeof provider.getVariables === 'function') return provider.getVariables(options);
          if (typeof provider.getMvuData === 'function') return provider.getMvuData(options);
          if (typeof provider.getAllVariables === 'function') return provider.getAllVariables(options);
          return null;
        },
        replaceMvuData(nextValue, options = {}) {
          if (typeof provider.replaceMvuData === 'function') return provider.replaceMvuData(nextValue, options);
          if (typeof provider.replaceVariables === 'function') return provider.replaceVariables(nextValue, options);
          throw new Error('mvu_replace_unavailable');
        },
        replaceVariables(nextValue, options = {}) {
          if (typeof provider.replaceVariables === 'function') return provider.replaceVariables(nextValue, options);
          if (typeof provider.replaceMvuData === 'function') return provider.replaceMvuData(nextValue, options);
          throw new Error('mvu_replace_unavailable');
        },
      };
    }

    function ensureBridgedMvuHost() {
      const existingWindowHost = (() => {
        try {
          return window.Mvu && typeof window.Mvu === 'object' ? window.Mvu : null;
        } catch (err) {
          return null;
        }
      })();
      const existingHasRead =
        existingWindowHost &&
        (typeof existingWindowHost.getAllVariables === 'function' ||
          typeof existingWindowHost.getVariables === 'function' ||
          typeof existingWindowHost.getMvuData === 'function');
      const existingHasWrite =
        existingWindowHost &&
        (typeof existingWindowHost.replaceMvuData === 'function' ||
          typeof existingWindowHost.replaceVariables === 'function');
      if (existingHasRead && existingHasWrite) {
        __mvuBridgeRoot.__LWCS_BRIDGED_MVU_HOST__ = existingWindowHost;
        __mvuBridgeRoot.__LWCS_BRIDGED_MVU_HOST_SOURCE__ = String(existingWindowHost.__bridgeSource || '鍘熺敓Mvu');
        return existingWindowHost;
      }

      const cachedHost =
        __mvuBridgeRoot.__LWCS_BRIDGED_MVU_HOST__ && typeof __mvuBridgeRoot.__LWCS_BRIDGED_MVU_HOST__ === 'object'
          ? __mvuBridgeRoot.__LWCS_BRIDGED_MVU_HOST__
          : null;
      if (cachedHost) return cachedHost;

      const providerInfo = collectMvuProviderCandidates()[0] || null;
      if (!providerInfo) return null;
      const bridgedHost = buildBridgedMvuHost(providerInfo);
      if (!bridgedHost) return null;
      __mvuBridgeRoot.__LWCS_BRIDGED_MVU_HOST__ = bridgedHost;
      __mvuBridgeRoot.__LWCS_BRIDGED_MVU_HOST_SOURCE__ = bridgedHost.__bridgeSource || providerInfo.label || '鏈煡鏉ユ簮';
      try {
        if (!window.Mvu || typeof window.Mvu !== 'object') window.Mvu = bridgedHost;
      } catch (err) {}
      try {
        if (typeof window.getAllVariables !== 'function') {
          window.getAllVariables = (...args) => bridgedHost.getAllVariables(...args);
        }
      } catch (err) {}
      return bridgedHost;
    }

    async function waitForMvuReady() {
      if (ensureBridgedMvuHost()) return;
      try {
        if (typeof waitGlobalInitialized === 'function') {
          await waitGlobalInitialized('Mvu');
          return;
        }
      } catch (err) {}
      try {
        if (window.top && window.top !== window && typeof window.top.waitGlobalInitialized === 'function') {
          await window.top.waitGlobalInitialized('Mvu');
          return;
        }
      } catch (err) {}
    }

    function buildRecentMessageIdCandidates(depth = MVU_RECENT_MESSAGE_SCAN_DEPTH) {
      const ids = ['latest'];
      for (let index = 1; index <= Math.max(1, depth); index += 1) {
        ids.push(-index);
      }
      return ids;
    }

    async function scanRecentMessageVariablesWithReaders(readers = [], options = {}) {
      const depth = toNumber(options && options.depth, MVU_RECENT_MESSAGE_SCAN_DEPTH);
      const messageIds = buildRecentMessageIdCandidates(depth);
      let fallbackVars = null;
      let fallbackMessageId = 'latest';
      let bestVars = null;
      let bestMessageId = 'latest';
      let bestScore = -Infinity;

      for (const messageId of messageIds) {
        for (const reader of readers) {
          if (typeof reader !== 'function') continue;
          try {
            const vars = await reader(messageId);
            if (!vars || typeof vars !== 'object') continue;
            if (!fallbackVars) {
              fallbackVars = vars;
              fallbackMessageId = messageId;
            }
            const rootData = resolveRootData(vars);
            const nextScore = scoreRootDataCandidate(rootData);
            if (nextScore > bestScore) {
              bestScore = nextScore;
              bestVars = vars;
              bestMessageId = messageId;
            }
          } catch (err) {}
        }
      }

      return {
        vars: bestVars || fallbackVars,
        messageId: bestVars ? bestMessageId : fallbackMessageId,
      };
    }

    async function getLatestMessageVariablesFallback() {
      try {
        if (window.TavernHelper && typeof window.TavernHelper.getVariables === 'function') {
          const scanned = await scanRecentMessageVariablesWithReaders([
            messageId => Promise.resolve(window.TavernHelper.getVariables({ type: 'message', message_id: messageId })),
          ]);
          return scanned && scanned.vars ? scanned.vars : null;
        }
      } catch (err) {}
      return null;
    }

    async function getAllVariablesSafe() {
      const host = getMvuHost();
      const scanned = await scanRecentMessageVariablesWithReaders([
        host && typeof host.getMvuData === 'function'
          ? messageId => Promise.resolve(host.getMvuData({ type: 'message', message_id: messageId }))
          : null,
        window.TavernHelper && typeof window.TavernHelper.getVariables === 'function'
          ? messageId => Promise.resolve(window.TavernHelper.getVariables({ type: 'message', message_id: messageId }))
          : null,
      ]);
      if (scanned && scanned.vars) {
        return scanned.vars;
      }
      if (host && typeof host.getAllVariables === 'function') {
        try {
          const vars = await Promise.resolve(host.getAllVariables());
          if (vars && typeof vars === 'object') return vars;
        } catch (err) {}
      }
      if (window.getAllVariables && typeof window.getAllVariables === 'function') {
        try {
          const vars = await Promise.resolve(window.getAllVariables());
          if (vars && typeof vars === 'object') return vars;
        } catch (err) {}
      }
      return await getLatestMessageVariablesFallback();
    }

    function getSharedMvuRefreshHub() {
      const hubKey = '__dragonUiSharedMvuRefreshHub';
      const existingHub = window[hubKey];
      if (existingHub && typeof existingHub.subscribe === 'function' && typeof existingHub.trigger === 'function') {
        return existingHub;
      }

      const subscribers = new Map();
      const POLL_KEY = '__dragonUiSharedRefreshPollTimer';
      const CHAT_CONTEXT_POLL_KEY = '__dragonUiSharedChatContextPollTimer';
      const POLL_VIS_KEY = '__dragonUiSharedRefreshPollVisibilityBound';
      const POLL_FOCUS_KEY = '__dragonUiSharedRefreshPollFocusBound';
      let bindingsReady = false;
      let running = false;
      let pending = false;
      let lastTriggerArgs = [];
      let lastChatContextSignature = '';

      const getAllVariablesDirect = async () => {
        return await getAllVariablesSafe();
      };

      const schedulePendingDispatch = () => {
        const runner = () => hub.dispatch(...lastTriggerArgs);
        if (typeof queueMicrotask === 'function') {
          queueMicrotask(runner);
          return;
        }
        Promise.resolve().then(runner);
      };

      const hub = {
        async getAllVariables() {
          return await getAllVariablesDirect();
        },

        async dispatch(...args) {
          lastTriggerArgs = args;
          if (running) {
            pending = true;
            return;
          }
          running = true;
          try {
            const vars = await getAllVariablesDirect();
            const entries = Array.from(subscribers.entries());
            for (const [subscriberId, subscriber] of entries) {
              try {
                await Promise.resolve(subscriber.handler(vars, ...args));
              } catch (error) {
                console.warn(`[DragonUI] MVU shared refresh subscriber failed: ${subscriberId}`, error);
              }
            }
          } finally {
            running = false;
            if (pending) {
              pending = false;
              schedulePendingDispatch();
            }
          }
        },

        trigger(...args) {
          return hub.dispatch(...args);
        },

        ensureBindings() {
          if (bindingsReady) return;
          bindingsReady = true;

          const host = getMvuHost();
          const eventName = host && host.events ? host.events.VARIABLE_UPDATE_ENDED : '';
          const getChatContextSignature = () => {
            const meta = getCurrentChatContextMeta();
            return [
              toText(meta.characterId, '').trim(),
              toText(meta.groupId, '').trim(),
              toText(meta.chatId, '').trim(),
              toText(meta.name1, '').trim(),
              toText(meta.name2, '').trim(),
            ].join('||');
          };
          let bound = false;
          const triggerFromEvent = (...args) => hub.trigger({ source: 'event', eventName }, ...args);

          if (host && eventName && typeof host.on === 'function') {
            try {
              host.on(eventName, triggerFromEvent);
              bound = true;
            } catch (err) {}
          }

          if (host && eventName && typeof host.addEventListener === 'function') {
            try {
              host.addEventListener(eventName, triggerFromEvent);
              bound = true;
            } catch (err) {}
          }

          if (eventName) {
            try {
              window.addEventListener(eventName, triggerFromEvent);
              bound = true;
            } catch (err) {}
            try {
              if (window.top && window.top !== window && typeof window.top.addEventListener === 'function') {
                window.top.addEventListener(eventName, triggerFromEvent);
                bound = true;
              }
            } catch (err) {}
          }

          const shouldEnableFallbackPoll = !bound;
          if (shouldEnableFallbackPoll && !window[POLL_KEY]) {
            window[POLL_KEY] = window.setInterval(() => {
              if (document.visibilityState === 'hidden') return;
              hub.trigger({ source: 'poll' });
            }, 4000);
          }

          lastChatContextSignature = getChatContextSignature();
          if (!window[CHAT_CONTEXT_POLL_KEY]) {
            window[CHAT_CONTEXT_POLL_KEY] = window.setInterval(() => {
              if (document.visibilityState === 'hidden') return;
              const nextSignature = getChatContextSignature();
              if (nextSignature === lastChatContextSignature) return;
              lastChatContextSignature = nextSignature;
              try {
                if (!window.__MVU_MANUAL_CHAR_SET) {
                  setPreferredActiveCharacterName('');
                }
              } catch (err) {}
              hub.trigger({ source: 'chat_context', signature: nextSignature });
            }, 1000);
          }

          if (!window[POLL_VIS_KEY]) {
            document.addEventListener('visibilitychange', () => {
              if (document.visibilityState === 'visible') hub.trigger({ source: 'visibility' });
            });
            window[POLL_VIS_KEY] = true;
          }

          if (!window[POLL_FOCUS_KEY]) {
            window.addEventListener('focus', () => {
              hub.trigger({ source: 'focus' });
            });
            window[POLL_FOCUS_KEY] = true;
          }

          if (!bound && window.__MVU_DEBUG__) {
            console.info('[DragonUI] MVU update event not bound, shared polling fallback enabled.');
          }
        },

        subscribe(subscriberId, handler, options = {}) {
          if (!subscriberId || typeof handler !== 'function') return () => {};
          subscribers.set(subscriberId, { handler });
          hub.ensureBindings();
          if (options.immediate !== false) {
            hub.trigger({ source: 'subscribe', subscriberId });
          }
          return () => {
            subscribers.delete(subscriberId);
          };
        }
      };

      window[hubKey] = hub;
      return hub;
    }

    function bindMvuUpdates(handler) {
      const hub = getSharedMvuRefreshHub();
      return hub.subscribe('dragon-ui-main', async (vars, ...args) => {
        try {
          await Promise.resolve(handler(vars, ...args));
        } catch (error) {
          console.warn('[DragonUI] MVU 瀹炴椂鍒锋柊鎵ц澶辫触', error);
        }
      });
    }

    function resolveRootData(vars) {
      if (!vars || typeof vars !== 'object') return null;
      const candidates = [
        vars,
        vars.data,
        vars.variables,
        vars.payload,
        vars.state,
        vars.mvu,
        vars.root
      ];
      for (const item of candidates) {
        if (!item || typeof item !== 'object') continue;
        if (item.stat_data && typeof item.stat_data === 'object' && (item.stat_data.char || item.stat_data.world || item.stat_data.sys)) {
          return item.stat_data;
        }
        if (item.char || item.world || item.sys || item.org) {
          return item;
        }
      }
      return null;
    }

    function buildEffectiveSd(rawSd) {
      if (!rawSd || typeof rawSd !== 'object') return { rootData: null, rawData: null };

      const rootData = {
        sys: rawSd.sys || {},
        world: rawSd.world || {},
        org: rawSd.org || {},
        char: rawSd.char || {}
      };
      const currentPlayerName = toText(deepGet(rootData, 'sys.鐜╁鍚?, ''), '').trim();
      if (isInvalidRuntimePlayerName(currentPlayerName)) {
        const contextMeta = getCurrentChatContextMeta();
        const preferredName = getPreferredActiveCharacterName();
        const battlePlayerName = getBattlePlayerNameFromRootData(rootData);
        const candidates = [
          battlePlayerName,
          preferredName,
          toText(contextMeta.name1, '').trim(),
          toText(contextMeta.name2, '').trim(),
        ].filter(Boolean);
        const repairedPlayerName = candidates.find(name => rootData.char && typeof rootData.char === 'object' && rootData.char[name]);
        if (repairedPlayerName) {
          if (!rootData.sys || typeof rootData.sys !== 'object') rootData.sys = {};
          rootData.sys.鐜╁鍚?= repairedPlayerName;
        }
      }
      return {
        rootData,
        rawData: rawSd
      };
    }

    function getCurrentChatContextMeta() {
      try {
        const ctx = window.SillyTavern && typeof window.SillyTavern.getContext === 'function'
          ? window.SillyTavern.getContext()
          : null;
        if (!ctx || typeof ctx !== 'object') {
          return { name1: '', name2: '', chatId: '', characterId: '', groupId: '' };
        }
        return {
          name1: toText(ctx.name1, '').trim(),
          name2: toText(ctx.name2, '').trim(),
          chatId: toText(ctx.chatId, '').trim(),
          characterId: toText(ctx.characterId, '').trim(),
          groupId: toText(ctx.groupId, '').trim(),
        };
      } catch (err) {
        return { name1: '', name2: '', chatId: '', characterId: '', groupId: '' };
      }
    }

    function hasRootRuntimeSignals(rootData) {
      if (!rootData || typeof rootData !== 'object') return false;
      if (toText(deepGet(rootData, 'sys.鐜╁鍚?, ''), '').trim()) return true;
      if (safeEntries(deepGet(rootData, 'world.鏃堕棿绾?, {})).length) return true;
      if (deepGet(rootData, 'world.鍋忓樊鍊?, undefined) !== undefined) return true;
      if (toText(deepGet(rootData, 'world.鏃堕棿._calendar', deepGet(rootData, 'world.鏃堕棿.calendar', '')), '').trim()) return true;
      return false;
    }

    function isRootDataRelevantToCurrentChat(rootData) {
      if (!rootData || typeof rootData !== 'object') return false;
      const chars = rootData.char && typeof rootData.char === 'object' ? rootData.char : {};
      const preferredName = getPreferredActiveCharacterName();
      const sysPlayerName = toText(deepGet(rootData, 'sys.鐜╁鍚?, ''), '').trim();
      const contextMeta = getCurrentChatContextMeta();
      if (preferredName && chars[preferredName]) return true;
      if (sysPlayerName && chars[sysPlayerName]) return true;
      if (contextMeta.name1 && chars[contextMeta.name1]) return true;
      if (contextMeta.name2 && chars[contextMeta.name2]) return true;
      if (contextMeta.name1 || contextMeta.name2) return false;
      return hasRootRuntimeSignals(rootData);
    }

    function scoreRootDataCandidate(rootData) {
      if (!rootData || typeof rootData !== 'object') return -1;
      const chars = rootData.char && typeof rootData.char === 'object' ? rootData.char : {};
      const contextMeta = getCurrentChatContextMeta();
      let score = 0;
      if (rootData.sys && typeof rootData.sys === 'object') score += 6;
      if (toText(deepGet(rootData, 'sys.鐜╁鍚?, ''), '').trim()) score += 12;
      if (safeEntries(deepGet(rootData, 'world.鏃堕棿绾?, {})).length) score += 5;
      if (deepGet(rootData, 'world.鍋忓樊鍊?, undefined) !== undefined) score += 3;
      if (safeEntries(chars).length) score += 1;
      if (contextMeta.name1 && chars[contextMeta.name1]) score += 10;
      if (contextMeta.name2 && chars[contextMeta.name2]) score += 4;
      if (isRootDataRelevantToCurrentChat(rootData)) score += 8;
      return score;
    }

    function getPreferredActiveCharacterName() {
      return toText(preferredActiveCharacterName, '').trim();
    }

    function setPreferredActiveCharacterName(name) {
      preferredActiveCharacterName = toText(name, '').trim();
      try { window.__MVU_ACTIVE_CHARACTER__ = preferredActiveCharacterName; } catch (_) {}
      return preferredActiveCharacterName;
    }

    function isPlayerCharacterEntry(name, char, playerName = '') {
      const safeName = toText(name, '').trim();
      const safePlayerName = toText(playerName, '').trim();
      return !!safePlayerName && safeName === safePlayerName;
    }

    function isInvalidRuntimePlayerName(name = '') {
      const safeName = toText(name, '').trim();
      return !safeName || ['鏃犲悕姘?, '鐜╁', '宸辨柟', 'Unknown', '鏈煡'].includes(safeName);
    }

    function getBattlePlayerNameFromRootData(rootData = {}) {
      return toText(
        deepGet(rootData, 'world.鎴樻枟.鍙傛垬鑰?player.name', ''),
        ''
      ).trim();
    }

    function resolveEffectivePlayerName(rootData = {}, fallbackActiveName = '') {
      const sysPlayerName = toText(deepGet(rootData, 'sys.鐜╁鍚?, ''), '').trim();
      if (!isInvalidRuntimePlayerName(sysPlayerName)) return sysPlayerName;
      const battlePlayerName = getBattlePlayerNameFromRootData(rootData);
      if (battlePlayerName) return battlePlayerName;
      return toText(fallbackActiveName, '').trim();
    }

    function isTangWulinCharacter(name = '') {
      return toText(name, '').trim() === '鍞愯垶楹?;
    }

    function getPinnedCharacterOrder(name = '') {
      const safeName = toText(name, '').trim();
      const pinnedNames = [
        ['鍞愯垶楹?],
        ['鍙ゆ湀濞?, '鍙ゆ湀'],
        ['璋㈤倐'],
        ['璁稿皬瑷€'],
        ['涔愭瀹?],
        ['鍘熸仼澶滆緣'],
        ['鍙舵槦婢?],
        ['寰愮瑺鏅?],
        ['鑸為暱绌?]
      ];
      for (let index = 0; index < pinnedNames.length; index += 1) {
        if (pinnedNames[index].includes(safeName)) return index + 1;
      }
      return -1;
    }

    function getCharacterDisplayPriority(name, char, options = {}) {
      const { playerName = '', currentName = '' } = options || {};
      const safeName = toText(name, '').trim();
      if (isPlayerCharacterEntry(safeName, char, playerName)) return 0;
      const pinnedOrder = getPinnedCharacterOrder(safeName);
      if (pinnedOrder >= 0) return pinnedOrder;
      if (safeName && safeName === toText(currentName, '').trim()) return 20;
      return 100;
    }

    function sortCharacterEntries(entries, options = {}) {
      return [...(Array.isArray(entries) ? entries : [])].sort((a, b) => {
        const rankDiff = getCharacterDisplayPriority(a[0], a[1], options) - getCharacterDisplayPriority(b[0], b[1], options);
        if (rankDiff !== 0) return rankDiff;
        const lvDiff = toNumber(deepGet(b[1], '灞炴€?绛夌骇', 0), 0) - toNumber(deepGet(a[1], '灞炴€?绛夌骇', 0), 0);
        if (lvDiff !== 0) return lvDiff;
        return toText(a[0], '').localeCompare(toText(b[0], ''), 'zh-Hans-CN', { numeric: true, sensitivity: 'base' });
      });
    }

    function findRealPlayerEntry(charEntries, playerName = '') {
      return sortCharacterEntries(charEntries, { playerName }).find(([name, char]) => isPlayerCharacterEntry(name, char, playerName)) || null;
    }

    function resolveActiveCharacter(sd) {
      const chars = sd && sd.char ? sd.char : {};
      const charEntries = safeEntries(chars);
      const preferredName = getPreferredActiveCharacterName();
      const sysPlayerName = resolveEffectivePlayerName(sd || {}, preferredName);
      const contextMeta = getCurrentChatContextMeta();
      const sortedEntries = sortCharacterEntries(charEntries, { playerName: sysPlayerName, currentName: preferredName });
      const realPlayerEntry = findRealPlayerEntry(charEntries, sysPlayerName);
      
      // 濡傛灉鏈夊亸濂藉悕瀛楀苟涓斿瓨鍦?      if (preferredName && chars[preferredName]) {
        // 濡傛灉褰撳墠鍋忓ソ鐨勪笉鏄帺瀹讹紝浣嗗畤瀹欓噷宸茬粡鏈変簡鐪熸鐨勭帺瀹讹紝涓斿苟闈炵敤鎴锋樉寮忓浐瀹氱殑锛屽氨鑷姩閫€浣嶈璐?        const isPreferredPlayer = isPlayerCharacterEntry(preferredName, chars[preferredName], sysPlayerName);
        if (!isPreferredPlayer && realPlayerEntry && !window.__MVU_MANUAL_CHAR_SET) {
          return realPlayerEntry;
        }
        return [preferredName, chars[preferredName]];
      }

      const namedCandidates = [
        deepGet(sd, 'sys.鐜╁鍚?, ''),
        contextMeta.name1,
        contextMeta.name2
      ].filter(Boolean);

      for (const name of namedCandidates) {
        if (chars[name]) return [name, chars[name]];
      }

      if (realPlayerEntry) {
        return realPlayerEntry;
      }

      if (!sortedEntries.length) {
        return ['Unknown', {}];
      }

      const pinnedCandidate = sortedEntries.find(([name]) => getPinnedCharacterOrder(name) >= 0);
      if (pinnedCandidate) {
        return pinnedCandidate;
      }

      return sortedEntries[0];
    }

    function isSnapshotPlayerControlled(snapshot) {
      const playerName = resolveEffectivePlayerName(deepGet(snapshot, 'rootData', {}), toText(snapshot && snapshot.activeName, ''));
      const activeName = toText(snapshot && snapshot.activeName, '').trim();
      return isPlayerCharacterEntry(activeName, deepGet(snapshot, 'activeChar', {}), playerName);
    }

    function formatAppearanceMeta(澶栬矊鏁版嵁) {
      const data = 澶栬矊鏁版嵁 && typeof 澶栬矊鏁版嵁 === 'object' ? 澶栬矊鏁版嵁 : {};
      const features = Array.isArray(data['鐗规畩鐗瑰緛'])
        ? data['鐗规畩鐗瑰緛']
          .map(item => toText(item, '').trim())
          .filter(Boolean)
          .filter(item => !/^寰呰ˉ鍏╘(/.test(item))
        : [];
      return {
        hairColor: toText(data['鍙戣壊'], '鏈瀹?) || '鏈瀹?,
        hairStyle: toText(data['鍙戝瀷'], '鏈瀹?) || '鏈瀹?,
        eyes: toText(data['鐬宠壊'], '鏈瀹?) || '鏈瀹?,
        height: toText(data['韬珮'], '鏈瀹?) || '鏈瀹?,
        build: toText(data['浣撳瀷'], '鏈瀹?) || '鏈瀹?,
        looks: toText(data['闀跨浉鎻忚堪'], '鏈瀹?) || '鏈瀹?,
        features: features.length ? features.join('銆?) : '鏈瀹?
      };
    }

    function formatAppearanceText(澶栬矊鏁版嵁) {
      const meta = formatAppearanceMeta(澶栬矊鏁版嵁);
      const parts = [
        meta.hairColor,
        meta.hairStyle,
        meta.eyes,
        meta.height,
        meta.build
      ].filter(Boolean).filter(item => item !== '鏈瀹?);
      const looksText = meta.looks && meta.looks !== '鏈瀹? ? `锛涢暱鐩革細${meta.looks}` : '';
      const featureText = meta.features && meta.features !== '鏈瀹? ? `锛涚壒寰侊細${meta.features}` : '';
      if (parts.length) return `${parts.join(' / ')}${looksText}${featureText}`;
      if (looksText || featureText) return `${looksText}${featureText}`.replace(/^锛?, '');
      return '鏈瀹?;
    }

    function normalizeLocationName(sd, rawLoc) {
      const loc = toText(rawLoc, '鏈煡');
      const worldLocations = deepGet(sd, 'world.鍦扮偣', {});
      if (worldLocations[loc]) return loc;

      const separators = ['路', '锝?, '/', '鈫?];
      for (const separator of separators) {
        if (loc.includes(separator)) {
          const token = loc.split(separator)[0].trim();
          if (worldLocations[token]) return token;
        }
      }

      const fuzzy = safeEntries(worldLocations).find(([name]) => loc.includes(name) || name.includes(loc));
      return fuzzy ? fuzzy[0] : loc;
    }

    function resolveLocationData(sd, rawLoc) {
      const worldLocations = deepGet(sd, 'world.鍦扮偣', {});
      const exact = toText(rawLoc, '鏈煡');
      if (worldLocations[exact]) return { name: exact, data: worldLocations[exact] };
      const normalized = normalizeLocationName(sd, exact);
      if (worldLocations[normalized]) return { name: normalized, data: worldLocations[normalized] };
      return { name: normalized, data: null };
    }

    function isTodoPlaceholderText(value) {
      return hasUiPlaceholderToken(value);
    }

    function normalizeSkillUiText(value, fallback = '鏈煡') {
      const text = toText(value, '').replace(/\[鍚庡彴鎺ㄦ紨\]\s*/g, '').trim();
      if (!text || text === '鏃? || text === '鏈敓鎴? || isTodoPlaceholderText(text)) return fallback;
      return text;
    }

    function summarizeUsageLikeArray(effectArray) {
      const entries = Array.isArray(effectArray) ? effectArray : [];
      const packedNames = entries
        .map(effect => toText(effect && effect['鏈哄埗'], '').trim())
        .filter(Boolean)
        .filter(name => name !== '绯荤粺鍩虹');
      if (packedNames.length) return packedNames.join(' / ');
      const descNames = entries
        .map(effect => toText(effect && effect['description'], '').trim())
        .filter(Boolean);
      return descNames.length ? descNames.slice(0, 3).join(' / ') : '鏈煡';
    }

    function summarizeConstructEffectUi(effect) {
      const itemName = normalizeSkillUiText(effect && effect['浜х墿鍚嶇О'], '涓存椂閫犵墿');
      const itemType = normalizeSkillUiText(effect && effect['浜х墿绫诲瀷'], '閫犵墿');
      const triggerMode = normalizeSkillUiText(effect && effect['瑙﹀彂鏂瑰紡'], itemType === '椋熺墿' ? '椋熺敤' : '浣跨敤');
      const expiry = resolveExpiryUiText(effect, '');
      const usageSummary = summarizeUsageLikeArray(effect && effect['浣跨敤鏁堟灉']);
      const header = itemType === '椋熺墿' ? `鐢熸垚椋熺墿銆?{itemName}銆慲 : `鐢熸垚閫犵墿銆?{itemName}銆慲;
      return [
        header,
        triggerMode ? `瑙﹀彂:${triggerMode}` : '',
        expiry ? `鏈夋晥鏈?${expiry}` : '',
        usageSummary !== '鏈煡' ? `${triggerMode === '椋熺敤' ? '椋熺敤鍚? : '浣跨敤鍚?}:${usageSummary}` : ''
      ].filter(Boolean).join(' / ');
    }

    function summarizeCloneEffectUi(effect) {
      const cloneType = normalizeSkillUiText(effect && effect['鍒嗚韩绫诲瀷'], '鍒嗚韩');
      const cloneName = normalizeSkillUiText(effect && effect['鍒嗚韩鍚嶇О'], '');
      const cloneCount = Math.max(1, toNumber(effect && effect['鍒嗚韩鏁伴噺'], 1));
      const duration = Math.max(0, toNumber(effect && effect['鎸佺画鍥炲悎'], effect && effect['鎸佺画']));
      const stealth = Number(effect && effect['闅愯斀搴?]);
      const inheritRatio = Number(effect && effect['瀹炲姏缁ф壙姣斾緥']);
      return [
        `${cloneName ? `鍒嗗嚭銆?{cloneName}銆慲 : '鍒嗗嚭'}${cloneCount}浣?{cloneType}`,
        Number.isFinite(stealth) ? `闅愯斀:${Math.round(stealth * 100)}%` : '',
        Number.isFinite(inheritRatio) ? `缁ф壙:${Math.round(inheritRatio * 100)}%` : '',
        duration > 0 ? `鎸佺画:${duration}鍥炲悎` : '',
      ].filter(Boolean).join(' / ');
    }

    function extractConstructEffectMeta(effectArray) {
      const createEffect = (Array.isArray(effectArray) ? effectArray : []).find(effect => {
        const mech = toText(effect && effect['鏈哄埗'], '').trim();
        return mech === '鐢熸垚閫犵墿' || mech === '閫犵墿鐢熸垚';
      });
      if (!createEffect) return null;
      return { summary: summarizeConstructEffectUi(createEffect) };
    }

    const SKILL_DESIGNER_PREVIEW_PREFIX = '鎶€鑳借璁″彴锛?;
    const SKILL_SUMMARY_EFFECT_MECHANISM_SET = new Set();
    const SKILL_DESIGNER_SELF_FUSION_PARTNER = '鑷韩鍙屾榄?;
    const SHARED_SKILL_MECHANISM_REGISTRY =
      __mvuBridgeRoot.__LWCS_SKILL_MECHANISM_REGISTRY__ && typeof __mvuBridgeRoot.__LWCS_SKILL_MECHANISM_REGISTRY__ === 'object'
        ? __mvuBridgeRoot.__LWCS_SKILL_MECHANISM_REGISTRY__
        : null;
    const SKILL_DESIGNER_MECHANISM_META = Object.freeze(SHARED_SKILL_MECHANISM_REGISTRY?.鏈哄埗瀹氫箟 || {});
    const SKILL_DESIGNER_SKILL_TYPES = Object.freeze(['寮烘敾绯?, '鎺у埗绯?, '椋熺墿绯?, '绮剧绯?, '闃插尽绯?, '鏁忔敾绯?, '鍏冪礌绯?, '杈呭姪绯?, '娌荤枟绯?, '琚姩', '姝﹂瓊铻嶅悎鎶€', '鍔熸硶', '鑷垱榄傛妧']);
    const SKILL_DESIGNER_TARGET_OPTIONS = Object.freeze(['鑷韩', '鍙嬫柟鍗曚綋', '鍙嬫柟缇や綋', '鏁屾柟鍗曚綋', '鏁屾柟缇や綋', '鍏ㄥ満', '椋熺敤鑰?, '浣跨敤鑰?, '鍙敜鐗?, '閫犵墿']);
    const SKILL_DESIGNER_MAIN_MECHANIC_POOL = Object.freeze(SHARED_SKILL_MECHANISM_REGISTRY?.mainArchetypes || {
      '浼ゅ绫?: Object.freeze(['鍗曚綋浼ゅ', '缇や綋浼ゅ', '澶氭浼ゅ', '寤惰繜鐖嗗彂', '鎸佺画浼ゅ']),
      '鎺у埗绫?: Object.freeze(['纭帶', '杞帶', '浣嶇Щ闄愬埗', '鑺傚鎵撴柇', '灏佹妧']),
      '鍓婂急绫?: Object.freeze(['鍗曞睘鎬у墛寮?, '澶氬睘鎬у墛寮?, '绂佺枟', '娑堣€楁彁楂?, '鍓嶆憞鎷夐暱', '鎺屾帶鍘嬪埗', '閫熷害鍘嬪埗', '娌荤枟鍙嶈浆']),
      '澧炵泭绫?: Object.freeze(['鍗曞睘鎬у鐩?, '澶氬睘鎬у鐩?, '鍏ㄥ睘鎬у鐩?, '娑堣€楅檷浣?, '鍓嶆憞缂╃煭', '鎺屾帶鎻愬崌', '閫熷害鎻愬崌', '淇偧澧炵泭']),
      '闃插尽绫?: Object.freeze(['鎶ょ浘', '鍑忎激', '鏍兼尅/鎶垫秷', '闇镐綋', '鍏嶆/閿佽', '鏃犳晫閲戣韩', '浼ゅ鍙嶅皠', '浼ゅ鍒嗘憡', '鏇胯韩鎶垫秷', '澶嶈嫃']),
      '鍥炲绫?: Object.freeze(['浣撳姏鎭㈠', '榄傚姏鎭㈠', '绮剧鎭㈠', '鎸佺画鎭㈠', '鍑€鍖?瑙ｆ帶']),
      '鎰熺煡/璁ょ煡绫?: Object.freeze(['鎰熺煡骞叉壈', '鏍囪閿佸畾', '鍏变韩瑙嗛噹', '骞诲', '鍌湢', '璁ょ煡鎵洸']),
      '浣嶇Щ绫?: Object.freeze(['鑷韩浣嶇Щ', '寮哄埗浣嶇Щ', '浣嶇Щ浜ゆ崲', '杩藉嚮浣嶇Щ', '鑴辩浣嶇Щ']),
      '鐗规畩瑙勫垯绫?: Object.freeze(['鍒嗚韩', '澶嶅埗', '鍙嶅埗', '杞寲', '鐘舵€佷氦鎹?, '鐘舵€佽浆绉?, '寮哄埗缁戝畾/閿佸畾', '鏉′欢瑙﹀彂', '瑙勫垯鏀瑰啓', '寮曠垎鎸佺画浼ゅ', '鏂╃浘', '绐冨彇鎶ょ浘', '鍚炲櫖', '鑳藉姏鍏变韩', '鏈哄埗鎶规秷']),
    });
    const SKILL_DESIGNER_DELIVERY_FORM_POOL = Object.freeze(['鐩存帴鐢熸晥', '鑷韩闄勪綋', '杩滅▼鍛戒腑', '鑼冨洿灞曞紑', '鍙敜鎵胯浇', '閫犵墿鎵胯浇', '鏍囪瑙﹀彂', '寤惰繜瑙﹀彂']);
    const SKILL_DESIGNER_DELIVERY_FORM_BY_TYPE = Object.freeze({
      '寮烘敾绯?: Object.freeze(['鐩存帴鐢熸晥', '鑷韩闄勪綋', '杩滅▼鍛戒腑']),
      '鎺у埗绯?: Object.freeze(['鐩存帴鐢熸晥', '鑼冨洿灞曞紑', '寤惰繜瑙﹀彂', '鏍囪瑙﹀彂']),
      '椋熺墿绯?: Object.freeze(['閫犵墿鎵胯浇', '寤惰繜瑙﹀彂', '杩滅▼鍛戒腑']),
      '绮剧绯?: Object.freeze(['鐩存帴鐢熸晥', '鏍囪瑙﹀彂', '寤惰繜瑙﹀彂', '鑼冨洿灞曞紑']),
      '闃插尽绯?: Object.freeze(['鑷韩闄勪綋', '鐩存帴鐢熸晥', '鑼冨洿灞曞紑']),
      '鏁忔敾绯?: Object.freeze(['鐩存帴鐢熸晥', '杩滅▼鍛戒腑', '鑷韩闄勪綋']),
      '鍏冪礌绯?: Object.freeze(['杩滅▼鍛戒腑', '鑼冨洿灞曞紑', '寤惰繜瑙﹀彂', '鐩存帴鐢熸晥']),
      '杈呭姪绯?: Object.freeze(['鐩存帴鐢熸晥', '鑼冨洿灞曞紑', '鏍囪瑙﹀彂']),
      '娌荤枟绯?: Object.freeze(['鐩存帴鐢熸晥', '鑼冨洿灞曞紑', '鏍囪瑙﹀彂']),
      '琚姩': Object.freeze(['鑷韩闄勪綋', '鐩存帴鐢熸晥']),
      '鍔熸硶': Object.freeze(['鑷韩闄勪綋', '鐩存帴鐢熸晥', '鑼冨洿灞曞紑', '寤惰繜瑙﹀彂']),
      '鑷垱榄傛妧': Object.freeze(['鐩存帴鐢熸晥', '鑷韩闄勪綋', '鑼冨洿灞曞紑', '寤惰繜瑙﹀彂', '鏍囪瑙﹀彂']),
    });
    const SKILL_DESIGNER_ATTRIBUTE_HINTS_BY_TYPE = Object.freeze({
      '寮烘敾绯?: Object.freeze(['鍔涢噺', '榄傚姏', '闃插尽']),
      '鎺у埗绯?: Object.freeze(['榄傚姏', '绮剧鍔?, '鏁忔嵎']),
      '椋熺墿绯?: Object.freeze(['榄傚姏', '绮剧鍔?]),
      '绮剧绯?: Object.freeze(['绮剧鍔?, '榄傚姏']),
      '闃插尽绯?: Object.freeze(['闃插尽', '榄傚姏']),
      '鏁忔敾绯?: Object.freeze(['鏁忔嵎', '鍔涢噺', '榄傚姏']),
      '鍏冪礌绯?: Object.freeze(['绮剧鍔?, '榄傚姏', '鏁忔嵎']),
      '杈呭姪绯?: Object.freeze(['榄傚姏', '绮剧鍔?, '闃插尽']),
      '娌荤枟绯?: Object.freeze(['榄傚姏', '绮剧鍔?]),
    });
    const SKILL_DESIGNER_SECONDARY_BY_MAIN = Object.freeze(SHARED_SKILL_MECHANISM_REGISTRY?.secondaryByMain || {
      '浼ゅ绫?: Object.freeze(['绌块€?, '鍚歌', '鏂╂潃琛ヤ激', '娴佽DOT', '鎵撴柇', '鍙嶅嚮', '杩藉嚮', '寮曠垎鎸佺画浼ゅ', '鏂╃浘', '鍚炲櫖']),
      '鎺у埗绫?: Object.freeze(['鎵撴柇', '娌夐粯', '鍑忛€?, '鑷寸洸', '杩熺紦', '绂佺枟', '缂存', '鍢茶', '鐮撮殣', '灏佹妧']),
      '鍓婂急绫?: Object.freeze(['绂佺枟', '鍑忛€?, '杩熺紦', '鏍囪寮辩偣', '缂存', '椹辨暎澧炵泭', '鐮撮殣', '娌荤枟鍙嶈浆', '灏佹妧', '鏂╃浘', '鍚炲櫖', '鏈哄埗鎶规秷']),
      '澧炵泭绫?: Object.freeze(['灏忔姢鐩?, '鍑€鍖?, '瑙ｆ帶', '鍏变韩瑙嗛噹', '闅愯韩', '鎶ゅ崼', '鏃犳晫閲戣韩', '澶嶈嫃', '鑳藉姏鍏变韩', '淇偧澧炵泭']),
      '闃插尽绫?: Object.freeze(['灏忔姢鐩?, '鍙嶅嚮', '鍑€鍖?, '瑙ｆ帶', '鎶ゅ崼', '鍢茶', '鏃犳晫閲戣韩', '浼ゅ鍙嶅皠', '浼ゅ鍒嗘憡', '鏇胯韩鎶垫秷', '澶嶈嫃']),
      '鍥炲绫?: Object.freeze(['鍑€鍖?, '瑙ｆ帶', '灏忔姢鐩?, '榄傚姏鎭㈠', '绮剧鎭㈠', '椹辨暎澧炵泭', '鎶ゅ崼', '澶嶈嫃', '鑳藉姏鍏变韩', '淇偧澧炵泭']),
      '鎰熺煡/璁ょ煡绫?: Object.freeze(['鏍囪寮辩偣', '鍏变韩瑙嗛噹', '鐩爣閿佸畾', '鎵撴柇', '娌夐粯', '缂存', '椹辨暎澧炵泭', '绐冨彇澧炵泭', '鐮撮殣']),
      '浣嶇Щ绫?: Object.freeze(['鎵撴柇', '鍙嶅嚮', '鏍囪寮辩偣', '缂存', '闅愯韩', '鐮撮殣']),
      '鐗规畩瑙勫垯绫?: Object.freeze(['鍏变韩瑙嗛噹', '鏍囪寮辩偣', '鍑€鍖?, '椹辨暎澧炵泭', '绐冨彇澧炵泭', '闅愯韩', '鎶ゅ崼', '鐘舵€佽浆绉?, '寮曠垎鎸佺画浼ゅ', '鏂╃浘', '绐冨彇鎶ょ浘', '鍚炲櫖', '鑳藉姏鍏变韩', '鏈哄埗鎶规秷', '娌荤枟鍙嶈浆', '灏佹妧', '鏃犳晫閲戣韩', '浼ゅ鍙嶅皠', '浼ゅ鍒嗘憡', '鏇胯韩鎶垫秷', '澶嶈嫃']),
    });
    const SKILL_DESIGNER_SECONDARY_TYPE_BIAS = Object.freeze(SHARED_SKILL_MECHANISM_REGISTRY?.secondaryTypeBias || {
      '寮烘敾绯?: Object.freeze(['鏂╃浘', '寮曠垎鎸佺画浼ゅ', '鏃犳晫閲戣韩', '浼ゅ鍙嶅皠', '鍙嶅嚮', '杩藉嚮']),
      '闃插尽绯?: Object.freeze(['浼ゅ鍙嶅皠', '澶嶈嫃', '鎶ゅ崼']),
      '鏁忔敾绯?: Object.freeze(['闅愯韩', '杩藉嚮', '鐮撮殣', '鏇胯韩鎶垫秷', '鏃犳晫閲戣韩', '鏂╃浘']),
      '鎺у埗绯?: Object.freeze(['灏佹妧', '娌荤枟鍙嶈浆', '鐩爣閿佸畾', '缂存', '椹辨暎澧炵泭', '鍢茶', '浼ゅ鍒嗘憡', '鐘舵€佽浆绉?, '鍚炲櫖', '鏈哄埗鎶规秷']),
      '绮剧绯?: Object.freeze(['鐘舵€佽浆绉?, '灏佹妧', '娌荤枟鍙嶈浆', '绐冨彇澧炵泭', '鐩爣閿佸畾', '闅愯韩', '鍚炲櫖', '鑳藉姏鍏变韩', '鏈哄埗鎶规秷']),
      '鍏冪礌绯?: Object.freeze(['寮曠垎鎸佺画浼ゅ', '鏂╃浘', '灏佹妧', '娌荤枟鍙嶈浆', '椹辨暎澧炵泭', '鐮撮殣', '鍚炲櫖', '鏈哄埗鎶规秷']),
      '杈呭姪绯?: Object.freeze(['鎶ゅ崼', '澶嶈嫃', '鍏变韩瑙嗛噹', '椹辨暎澧炵泭', '绐冨彇鎶ょ浘', '鏃犳晫閲戣韩', '浼ゅ鍒嗘憡', '鑳藉姏鍏变韩']),
      '娌荤枟绯?: Object.freeze(['澶嶈嫃', '鎶ゅ崼', '椹辨暎澧炵泭', '鍏变韩瑙嗛噹', '鏃犳晫閲戣韩', '绐冨彇鎶ょ浘', '浼ゅ鍒嗘憡', '鑳藉姏鍏变韩']),
      '椋熺墿绯?: Object.freeze(['淇偧澧炵泭', '澶嶈嫃', '椹辨暎澧炵泭', '鍏变韩瑙嗛噹', '绐冨彇鎶ょ浘', '娌荤枟鍙嶈浆', '鎶ゅ崼', '鑳藉姏鍏变韩']),
    });
    const SKILL_DESIGNER_TARGET_SEMANTICS = Object.freeze(SHARED_SKILL_MECHANISM_REGISTRY?.鐩爣璇箟琛?|| {});
    const BRIDGE_FALLBACK_SKILL_MECHANISM_REGISTRY = Object.freeze({
      mainArchetypes: SKILL_DESIGNER_MAIN_MECHANIC_POOL,
      secondaryByMain: SKILL_DESIGNER_SECONDARY_BY_MAIN,
      secondaryTypeBias: SKILL_DESIGNER_SECONDARY_TYPE_BIAS,
      鏈哄埗瀹氫箟: SKILL_DESIGNER_MECHANISM_META,
      鐩爣璇箟琛? SKILL_DESIGNER_TARGET_SEMANTICS,
    });
    if (!__mvuBridgeRoot.__LWCS_SKILL_MECHANISM_REGISTRY__) {
      __mvuBridgeRoot.__LWCS_SKILL_MECHANISM_REGISTRY__ = BRIDGE_FALLBACK_SKILL_MECHANISM_REGISTRY;
    }
    __mvuBridgeRoot.__LWCS_BRIDGE_REGISTRY_SOURCE__ =
      SHARED_SKILL_MECHANISM_REGISTRY && SHARED_SKILL_MECHANISM_REGISTRY === __mvuBridgeRoot.__LWCS_SKILL_MECHANISM_REGISTRY__
        ? 'shared'
        : 'bridge_fallback';
    try {
      if (window.parent && window.parent !== window && !window.parent.__LWCS_SKILL_MECHANISM_REGISTRY__) {
        window.parent.__LWCS_SKILL_MECHANISM_REGISTRY__ = __mvuBridgeRoot.__LWCS_SKILL_MECHANISM_REGISTRY__;
      }
    } catch (error) {}
    try {
      if (window.top && window.top !== window && !window.top.__LWCS_SKILL_MECHANISM_REGISTRY__) {
        window.top.__LWCS_SKILL_MECHANISM_REGISTRY__ = __mvuBridgeRoot.__LWCS_SKILL_MECHANISM_REGISTRY__;
      }
    } catch (error) {}
    const SKILL_DESIGNER_ALL_SECONDARY_OPTIONS = Object.freeze(
      Array.from(new Set(Object.values(SKILL_DESIGNER_SECONDARY_BY_MAIN).flat())).sort(),
    );
    const SKILL_DESIGNER_ATTRIBUTE_OPTIONS = Object.freeze([
      '閲?, '鏈?, '姘?, '鐏?, '鍦?, '椋?, '闆?, '鍐?,
      '鍏?, '鏆?, '绮剧',
      '绌洪棿', '鏃堕棿',
      '鍒涢€?, '姣佺伃'
    ]);
    const SKILL_DESIGNER_RESOURCE_TYPE_OPTIONS = Object.freeze(['鏃?, '榄傚姏', '绮剧鍔?, '浣撳姏', '娣峰悎']);
    const SKILL_DESIGNER_PRIMARY_MECHANISM_HINTS = Object.freeze({
      '鐩存帴浼ゅ': Object.freeze({ main: '浼ゅ绫?, sub: '鍗曚綋浼ゅ' }),
      '澶氭浼ゅ': Object.freeze({ main: '浼ゅ绫?, sub: '澶氭浼ゅ' }),
      '鎸佺画浼ゅ': Object.freeze({ main: '浼ゅ绫?, sub: '鎸佺画浼ゅ' }),
      '寤惰繜鐖嗗彂': Object.freeze({ main: '浼ゅ绫?, sub: '寤惰繜鐖嗗彂' }),
      '纭帶': Object.freeze({ main: '鎺у埗绫?, sub: '纭帶' }),
      '杞帶': Object.freeze({ main: '鎺у埗绫?, sub: '杞帶' }),
      '浣嶇Щ闄愬埗': Object.freeze({ main: '鎺у埗绫?, sub: '浣嶇Щ闄愬埗' }),
      '鎵撴柇': Object.freeze({ main: '鎺у埗绫?, sub: '鑺傚鎵撴柇', secondary: '鎵撴柇' }),
      '鍗曞睘鎬у墛寮?: Object.freeze({ main: '鍓婂急绫?, sub: '鍗曞睘鎬у墛寮? }),
      '澶氬睘鎬у墛寮?: Object.freeze({ main: '鍓婂急绫?, sub: '澶氬睘鎬у墛寮? }),
      '绂佺枟': Object.freeze({ main: '鍓婂急绫?, sub: '绂佺枟', secondary: '绂佺枟' }),
      '娑堣€楁彁楂?: Object.freeze({ main: '鍓婂急绫?, sub: '娑堣€楁彁楂? }),
      '鍓嶆憞鎷夐暱': Object.freeze({ main: '鍓婂急绫?, sub: '鍓嶆憞鎷夐暱' }),
      '鎺屾帶鍘嬪埗': Object.freeze({ main: '鍓婂急绫?, sub: '鎺屾帶鍘嬪埗' }),
      '鍗曞睘鎬у鐩?: Object.freeze({ main: '澧炵泭绫?, sub: '鍗曞睘鎬у鐩? }),
      '澶氬睘鎬у鐩?: Object.freeze({ main: '澧炵泭绫?, sub: '澶氬睘鎬у鐩? }),
      '鍏ㄥ睘鎬у鐩?: Object.freeze({ main: '澧炵泭绫?, sub: '鍏ㄥ睘鎬у鐩? }),
      '娑堣€楅檷浣?: Object.freeze({ main: '澧炵泭绫?, sub: '娑堣€楅檷浣? }),
      '鍓嶆憞缂╃煭': Object.freeze({ main: '澧炵泭绫?, sub: '鍓嶆憞缂╃煭' }),
      '鎺屾帶鎻愬崌': Object.freeze({ main: '澧炵泭绫?, sub: '鎺屾帶鎻愬崌' }),
      '閫熷害鎻愬崌': Object.freeze({ main: '澧炵泭绫?, sub: '閫熷害鎻愬崌' }),
      '鏍囪寮辩偣': Object.freeze({ main: '鍓婂急绫?, sub: '鍗曞睘鎬у墛寮?, secondary: '鏍囪寮辩偣' }),
      '鎶ょ浘': Object.freeze({ main: '闃插尽绫?, sub: '鎶ょ浘' }),
      '鍑忎激': Object.freeze({ main: '闃插尽绫?, sub: '鍑忎激' }),
      '鏍兼尅': Object.freeze({ main: '闃插尽绫?, sub: '鏍兼尅/鎶垫秷' }),
      '闇镐綋': Object.freeze({ main: '闃插尽绫?, sub: '闇镐綋' }),
      '鍏嶆': Object.freeze({ main: '闃插尽绫?, sub: '鍏嶆/閿佽' }),
      '鏃犳晫閲戣韩': Object.freeze({ main: '闃插尽绫?, sub: '鏃犳晫閲戣韩', secondary: '鏃犳晫閲戣韩' }),
      '浼ゅ鍙嶅皠': Object.freeze({ main: '闃插尽绫?, sub: '浼ゅ鍙嶅皠', secondary: '浼ゅ鍙嶅皠' }),
      '浼ゅ鍒嗘憡': Object.freeze({ main: '闃插尽绫?, sub: '浼ゅ鍒嗘憡', secondary: '浼ゅ鍒嗘憡' }),
      '鏇胯韩鎶垫秷': Object.freeze({ main: '闃插尽绫?, sub: '鏇胯韩鎶垫秷', secondary: '鏇胯韩鎶垫秷' }),
      '澶嶈嫃': Object.freeze({ main: '闃插尽绫?, sub: '澶嶈嫃', secondary: '澶嶈嫃' }),
      '浣撳姏鎭㈠': Object.freeze({ main: '鍥炲绫?, sub: '浣撳姏鎭㈠' }),
      '榄傚姏鎭㈠': Object.freeze({ main: '鍥炲绫?, sub: '榄傚姏鎭㈠' }),
      '绮剧鎭㈠': Object.freeze({ main: '鍥炲绫?, sub: '绮剧鎭㈠' }),
      '鎸佺画鎭㈠': Object.freeze({ main: '鍥炲绫?, sub: '鎸佺画鎭㈠' }),
      '瑙ｆ帶': Object.freeze({ main: '鍥炲绫?, sub: '鍑€鍖?瑙ｆ帶', secondary: '瑙ｆ帶' }),
      '鍑€鍖?: Object.freeze({ main: '鍥炲绫?, sub: '鍑€鍖?瑙ｆ帶', secondary: '鍑€鍖? }),
      '鍏变韩瑙嗛噹': Object.freeze({ main: '鎰熺煡/璁ょ煡绫?, sub: '鍏变韩瑙嗛噹', secondary: '鍏变韩瑙嗛噹' }),
      '鏍囪閿佸畾': Object.freeze({ main: '鎰熺煡/璁ょ煡绫?, sub: '鏍囪閿佸畾' }),
      '骞诲': Object.freeze({ main: '鎰熺煡/璁ょ煡绫?, sub: '骞诲' }),
      '鍌湢': Object.freeze({ main: '鎰熺煡/璁ょ煡绫?, sub: '鍌湢' }),
      '鎰熺煡骞叉壈': Object.freeze({ main: '鎰熺煡/璁ょ煡绫?, sub: '鎰熺煡骞叉壈' }),
      '璁ょ煡鎵洸': Object.freeze({ main: '鎰熺煡/璁ょ煡绫?, sub: '璁ょ煡鎵洸' }),
      '鐩爣閿佸畾': Object.freeze({ main: '鎰熺煡/璁ょ煡绫?, sub: '鏍囪閿佸畾', secondary: '鐩爣閿佸畾' }),
      '鑷韩浣嶇Щ': Object.freeze({ main: '浣嶇Щ绫?, sub: '鑷韩浣嶇Щ' }),
      '寮哄埗浣嶇Щ': Object.freeze({ main: '浣嶇Щ绫?, sub: '寮哄埗浣嶇Щ' }),
      '浣嶇Щ浜ゆ崲': Object.freeze({ main: '浣嶇Щ绫?, sub: '浣嶇Щ浜ゆ崲' }),
      '杩藉嚮浣嶇Щ': Object.freeze({ main: '浣嶇Щ绫?, sub: '杩藉嚮浣嶇Щ' }),
      '鑴辩浣嶇Щ': Object.freeze({ main: '浣嶇Щ绫?, sub: '鑴辩浣嶇Щ' }),
      '杩藉嚮': Object.freeze({ main: '浣嶇Щ绫?, sub: '杩藉嚮浣嶇Щ', secondary: '杩藉嚮' }),
      '鍒嗚韩': Object.freeze({ main: '鐗规畩瑙勫垯绫?, sub: '鍒嗚韩' }),
      '澶嶅埗': Object.freeze({ main: '鐗规畩瑙勫垯绫?, sub: '澶嶅埗' }),
      '鍙嶅埗': Object.freeze({ main: '鐗规畩瑙勫垯绫?, sub: '鍙嶅埗' }),
      '鐘舵€佷氦鎹?: Object.freeze({ main: '鐗规畩瑙勫垯绫?, sub: '鐘舵€佷氦鎹? }),
      '鐘舵€佽浆绉?: Object.freeze({ main: '鐗规畩瑙勫垯绫?, sub: '鐘舵€佽浆绉?, secondary: '鐘舵€佽浆绉? }),
      '寮哄埗缁戝畾/閿佸畾': Object.freeze({ main: '鐗规畩瑙勫垯绫?, sub: '寮哄埗缁戝畾/閿佸畾' }),
      '鏉′欢瑙﹀彂': Object.freeze({ main: '鐗规畩瑙勫垯绫?, sub: '鏉′欢瑙﹀彂' }),
      '寮曠垎鎸佺画浼ゅ': Object.freeze({ main: '鐗规畩瑙勫垯绫?, sub: '寮曠垎鎸佺画浼ゅ', secondary: '寮曠垎鎸佺画浼ゅ' }),
      '鏂╃浘': Object.freeze({ main: '鐗规畩瑙勫垯绫?, sub: '鏂╃浘', secondary: '鏂╃浘' }),
      '绐冨彇鎶ょ浘': Object.freeze({ main: '鐗规畩瑙勫垯绫?, sub: '绐冨彇鎶ょ浘', secondary: '绐冨彇鎶ょ浘' }),
      '鍚炲櫖': Object.freeze({ main: '鐗规畩瑙勫垯绫?, sub: '鍚炲櫖', secondary: '鍚炲櫖' }),
      '鑳藉姏鍏变韩': Object.freeze({ main: '鐗规畩瑙勫垯绫?, sub: '鑳藉姏鍏变韩', secondary: '鑳藉姏鍏变韩' }),
      '淇偧澧炵泭': Object.freeze({ main: '澧炵泭绫?, sub: '淇偧澧炵泭', secondary: '淇偧澧炵泭' }),
      '鏈哄埗鎶规秷': Object.freeze({ main: '鐗规畩瑙勫垯绫?, sub: '鏈哄埗鎶规秷', secondary: '鏈哄埗鎶规秷' }),
      '鏁堟灉鍙嶈浆': Object.freeze({ main: '鐗规畩瑙勫垯绫?, sub: '瑙勫垯鏀瑰啓' }),
      '浼ゅ杞洖澶?: Object.freeze({ main: '鐗规畩瑙勫垯绫?, sub: '杞寲' }),
      '鍥炲杞激瀹?: Object.freeze({ main: '鐗规畩瑙勫垯绫?, sub: '杞寲' }),
      '娌荤枟鍙嶈浆': Object.freeze({ main: '鍓婂急绫?, sub: '娌荤枟鍙嶈浆', secondary: '娌荤枟鍙嶈浆' }),
      '灏佹妧': Object.freeze({ main: '鎺у埗绫?, sub: '灏佹妧', secondary: '灏佹妧' }),
      '椹辨暎澧炵泭': Object.freeze({ main: '鍓婂急绫?, sub: '鍗曞睘鎬у墛寮?, secondary: '椹辨暎澧炵泭' }),
      '绐冨彇澧炵泭': Object.freeze({ main: '鐗规畩瑙勫垯绫?, sub: '澶嶅埗', secondary: '绐冨彇澧炵泭' }),
      '闅愯韩': Object.freeze({ main: '澧炵泭绫?, sub: '鍗曞睘鎬у鐩?, secondary: '闅愯韩' }),
      '鎶ゅ崼': Object.freeze({ main: '闃插尽绫?, sub: '浼ゅ鍒嗘憡', secondary: '鎶ゅ崼' }),
      '鍢茶': Object.freeze({ main: '鎺у埗绫?, sub: '杞帶', secondary: '鍢茶' }),
      '鐮撮殣': Object.freeze({ main: '鎺у埗绫?, sub: '鑺傚鎵撴柇', secondary: '鐮撮殣' }),
      '缂存': Object.freeze({ main: '鎺у埗绫?, sub: '杞帶', secondary: '缂存' }),
    });
    const SKILL_DESIGNER_SECONDARY_MECHANISM_HINTS = Object.freeze({
      '鎵撴柇': Object.freeze(['鎵撴柇']),
      '娌夐粯': Object.freeze(['娌夐粯']),
      '鍑忛€?: Object.freeze(['鍑忛€?]),
      '鑷寸洸': Object.freeze(['鑷寸洸']),
      '杩熺紦': Object.freeze(['杩熺紦']),
      '绂佺枟': Object.freeze(['绂佺枟']),
      '鏍囪寮辩偣': Object.freeze(['鏍囪寮辩偣']),
      '鍏变韩瑙嗛噹': Object.freeze(['鍏变韩瑙嗛噹']),
      '鎶ょ浘': Object.freeze(['灏忔姢鐩?]),
      '瑙ｆ帶': Object.freeze(['瑙ｆ帶']),
      '鍑€鍖?: Object.freeze(['鍑€鍖?]),
      '榄傚姏鎭㈠': Object.freeze(['榄傚姏鎭㈠']),
      '绮剧鎭㈠': Object.freeze(['绮剧鎭㈠']),
      '鍙楀嚮鍙嶅嚮': Object.freeze(['鍙嶅嚮']),
      '鍙嶅嚮': Object.freeze(['鍙嶅嚮']),
      '椹辨暎澧炵泭': Object.freeze(['椹辨暎澧炵泭']),
      '绐冨彇澧炵泭': Object.freeze(['绐冨彇澧炵泭']),
      '闅愯韩': Object.freeze(['闅愯韩']),
      '鎶ゅ崼': Object.freeze(['鎶ゅ崼']),
      '鍢茶': Object.freeze(['鍢茶']),
      '鐮撮殣': Object.freeze(['鐮撮殣']),
      '鐩爣閿佸畾': Object.freeze(['鐩爣閿佸畾']),
      '杩藉嚮': Object.freeze(['杩藉嚮']),
      '缂存': Object.freeze(['缂存']),
      '灏佹妧': Object.freeze(['灏佹妧']),
      '鏃犳晫閲戣韩': Object.freeze(['鏃犳晫閲戣韩']),
      '浼ゅ鍙嶅皠': Object.freeze(['浼ゅ鍙嶅皠']),
      '浼ゅ鍒嗘憡': Object.freeze(['浼ゅ鍒嗘憡']),
      '鏇胯韩鎶垫秷': Object.freeze(['鏇胯韩鎶垫秷']),
      '澶嶈嫃': Object.freeze(['澶嶈嫃']),
      '娌荤枟鍙嶈浆': Object.freeze(['娌荤枟鍙嶈浆']),
      '鐘舵€佽浆绉?: Object.freeze(['鐘舵€佽浆绉?]),
      '寮曠垎鎸佺画浼ゅ': Object.freeze(['寮曠垎鎸佺画浼ゅ']),
      '鏂╃浘': Object.freeze(['鏂╃浘']),
      '绐冨彇鎶ょ浘': Object.freeze(['绐冨彇鎶ょ浘']),
      '鍚炲櫖': Object.freeze(['鍚炲櫖']),
      '鑳藉姏鍏变韩': Object.freeze(['鑳藉姏鍏变韩']),
      '淇偧澧炵泭': Object.freeze(['淇偧澧炵泭']),
      '鏈哄埗鎶规秷': Object.freeze(['鏈哄埗鎶规秷']),
    });
    const SKILL_DESIGNER_PACKED_PROPERTY_LABELS = Object.freeze({
      str: '鍔涢噺',
      def: '闃插尽',
      agi: '鏁忔嵎',
      vit_max: '浣撳姏涓婇檺',
      sp_max: '榄傚姏涓婇檺',
      men_max: '绮剧鍔涗笂闄?,
      vit: '浣撳姏',
      sp: '榄傚姏',
      men: '绮剧鍔?,
      鎺屾帶: '鎺屾帶',
      濞佸姏: '濞佸姏',
      娑堣€? '娑堣€?,
      鍓嶆憞: '鍓嶆憞',
      鎺у埗: '鎺у埗',
      閫熷害: '閫熷害',
    });
    const SKILL_DESIGNER_PARAM_ATTRIBUTE_OPTIONS = Object.freeze([
      '鍔涢噺', '闃插尽', '鏁忔嵎',
      '浣撳姏涓婇檺', '榄傚姏涓婇檺', '绮剧鍔涗笂闄?,
      '浣撳姏', '榄傚姏', '绮剧鍔?,
      '鎺屾帶', '濞佸姏', '娑堣€?, '鍓嶆憞', '鎺у埗', '閫熷害',
    ]);
    const SKILL_DESIGNER_PARAM_ATTRIBUTE_GROUP_OPTIONS = Object.freeze([
      '鍔涢噺/闃插尽',
      '鍔涢噺/鏁忔嵎',
      '闃插尽/鏁忔嵎',
      '鍔涢噺/闃插尽/鏁忔嵎',
      '浣撳姏涓婇檺/榄傚姏涓婇檺',
      '浣撳姏涓婇檺/绮剧鍔涗笂闄?,
      '榄傚姏涓婇檺/绮剧鍔涗笂闄?,
      '浣撳姏涓婇檺/榄傚姏涓婇檺/绮剧鍔涗笂闄?,
      '榄傚姏/绮剧鍔?,
      '鎺屾帶/濞佸姏',
      '鎺屾帶/鎺у埗',
      '娑堣€?鍓嶆憞',
      '閫熷害/鏁忔嵎',
    ]);
    const SKILL_DESIGNER_PARAM_PANEL_OPTIONS = Object.freeze(['鏀诲嚮', '闃插尽', '鍥炲', '閫熷害', '鎺у埗']);
    const SKILL_DESIGNER_PARAM_SUPPORT_TARGET_OPTIONS = Object.freeze(['鑷韩', '鍙嬫柟鍗曚綋', '鍙嬫柟缇や綋']);
    const SKILL_DESIGNER_PARAM_SHIELD_CAP_OPTIONS = Object.freeze(['鍩轰簬浣撳姏', '鍩轰簬榄傚姏', '鍩轰簬绮剧鍔?]);
    const SKILL_DESIGNER_PARAM_IMMUNE_LEVEL_OPTIONS = Object.freeze(['纭帶', '杞帶', '鍏ㄩ儴鎺у埗']);
    const SKILL_DESIGNER_PARAM_CLEANSE_PRIORITY_OPTIONS = Object.freeze([
      '鎺у埗 > 鍓婂急 > 寮傚父',
      '鍓婂急 > 鎺у埗 > 寮傚父',
      '寮傚父 > 鎺у埗 > 鍓婂急',
    ]);
    const SKILL_DESIGNER_PARAM_CLEANSE_GAIN_OPTIONS = Object.freeze(['鍥炲', '鎶ょ浘', '鍏嶇柅']);
    const SKILL_DESIGNER_PARAM_TRACKING_RULE_OPTIONS = Object.freeze(['涓嶅彲鑴遍攣', '鍏变韩鍧愭爣', '鍛戒腑鏄惧舰']);
    const SKILL_DESIGNER_PARAM_INFO_DEPTH_OPTIONS = Object.freeze(['浣嶇疆', '瑙嗛噹', '鐘舵€?, '浣嶇疆+瑙嗛噹', '瀹屾暣鍏变韩']);
    const SKILL_DESIGNER_PARAM_WAKE_RULE_OPTIONS = Object.freeze(['鍙椾激', '鍑€鍖?, '鏃堕棿缁撴潫']);
    const SKILL_DESIGNER_PARAM_ESCAPE_GAIN_OPTIONS = Object.freeze(['闅愬尶', '鎶ょ浘', '鍔犻€?]);
    const SKILL_DESIGNER_PARAM_COPY_TARGET_OPTIONS = Object.freeze(['鎷涘紡', '鐘舵€?, '灞炴€?]);
    const SKILL_DESIGNER_PARAM_COUNTER_TARGET_OPTIONS = Object.freeze(['杩滅▼', '鎺у埗', '鍙敜', '杩戞垬']);
    const SKILL_DESIGNER_PARAM_EXCHANGE_TARGET_OPTIONS = Object.freeze(['澧炵泭', '鍑忕泭', '鏍囪']);
    const SKILL_DESIGNER_PARAM_TRIGGER_RESULT_OPTIONS = Object.freeze(['鐖嗗彂', '鍒锋柊', '鍙敜']);
    const SKILL_DESIGNER_PARAM_REWRITE_DEPTH_OPTIONS = Object.freeze(['閮ㄥ垎鏀瑰啓', '瀹屾暣瑕嗙洊']);
    const SKILL_DESIGNER_PARAM_CLONE_TYPE_OPTIONS = Object.freeze(['鐗╃悊鍒嗚韩', '绮剧鍔涘垎韬?]);
    const SKILL_DESIGNER_PARAM_PENETRATION_TARGET_OPTIONS = Object.freeze(['闃插尽', '鎶ょ浘', '鎶楁€?]);
    const SKILL_DESIGNER_PARAM_LIFESTEAL_RESOURCE_OPTIONS = Object.freeze(['鐢熷懡', '榄傚姏', '绮剧鍔?]);
    const SKILL_DESIGNER_PARAM_RESOURCE_TRANSFER_TYPE_OPTIONS = Object.freeze(['榄傚姏', '绮剧鍔?, '鍙岃祫婧?]);
    const SKILL_DESIGNER_PARAM_MECHANISM_SUPPRESS_TARGET_OPTIONS = Object.freeze(['澶嶈嫃', '鎶ょ浘', '闅愯韩', '澧炵泭', '闃插尽鏈哄埗', '鍥炲鏈哄埗', '鎺у埗鏈哄埗', '鐗规畩瑙勫垯']);
    const SKILL_DESIGNER_PARAM_MECHANISM_SUPPRESS_MODE_OPTIONS = Object.freeze(['绉婚櫎骞跺皝閿?, '浠呭皝閿佸悗缁?]);
    const SKILL_DESIGNER_PARAM_INTERRUPT_WINDOW_OPTIONS = Object.freeze(['鍓嶆憞', '鍚熷敱涓?, '寮曞涓?]);
    const SKILL_DESIGNER_PARAM_COUNTER_RULE_OPTIONS = Object.freeze(['鍙楀嚮鍚?, '鏍兼尅鍚?, '鍙楁帶鍚?]);
    const SKILL_DESIGNER_PARAM_MUTE_SCOPE_OPTIONS = Object.freeze(['涓诲姩鎶€', '鍜忓敱鎶€', '閫犵墿鎶€', '鍏ㄩ儴鎶€鑳?]);
    const SKILL_DESIGNER_PARAM_BLIND_EFFECT_OPTIONS = Object.freeze(['鍛戒腑', '瑙嗛噹', '閿佸畾']);
    const SKILL_DESIGNER_PARAM_WEAK_POINT_TYPE_OPTIONS = Object.freeze(['鐮寸敳', '鏆村嚮', '灞炴€у厠鍒?]);
    const SKILL_DESIGNER_PARAM_OPTION_REGISTRY = Object.freeze({
      ATTRIBUTE: SKILL_DESIGNER_PARAM_ATTRIBUTE_OPTIONS,
      ATTRIBUTE_GROUP: SKILL_DESIGNER_PARAM_ATTRIBUTE_GROUP_OPTIONS,
      SHIELD_CAP: SKILL_DESIGNER_PARAM_SHIELD_CAP_OPTIONS,
      IMMUNE_LEVEL: SKILL_DESIGNER_PARAM_IMMUNE_LEVEL_OPTIONS,
      CLEANSE_PRIORITY: SKILL_DESIGNER_PARAM_CLEANSE_PRIORITY_OPTIONS,
      CLEANSE_GAIN: SKILL_DESIGNER_PARAM_CLEANSE_GAIN_OPTIONS,
      TRACKING_RULE: SKILL_DESIGNER_PARAM_TRACKING_RULE_OPTIONS,
      INFO_DEPTH: SKILL_DESIGNER_PARAM_INFO_DEPTH_OPTIONS,
      WAKE_RULE: SKILL_DESIGNER_PARAM_WAKE_RULE_OPTIONS,
      ESCAPE_GAIN: SKILL_DESIGNER_PARAM_ESCAPE_GAIN_OPTIONS,
      COPY_TARGET: SKILL_DESIGNER_PARAM_COPY_TARGET_OPTIONS,
      COUNTER_TARGET: SKILL_DESIGNER_PARAM_COUNTER_TARGET_OPTIONS,
      EXCHANGE_TARGET: SKILL_DESIGNER_PARAM_EXCHANGE_TARGET_OPTIONS,
      TRIGGER_RESULT: SKILL_DESIGNER_PARAM_TRIGGER_RESULT_OPTIONS,
      REWRITE_DEPTH: SKILL_DESIGNER_PARAM_REWRITE_DEPTH_OPTIONS,
      CLONE_TYPE: SKILL_DESIGNER_PARAM_CLONE_TYPE_OPTIONS,
      PENETRATION_TARGET: SKILL_DESIGNER_PARAM_PENETRATION_TARGET_OPTIONS,
      LIFESTEAL_RESOURCE: SKILL_DESIGNER_PARAM_LIFESTEAL_RESOURCE_OPTIONS,
      RESOURCE_TRANSFER_TYPE: SKILL_DESIGNER_PARAM_RESOURCE_TRANSFER_TYPE_OPTIONS,
      MECHANISM_SUPPRESS_TARGET: SKILL_DESIGNER_PARAM_MECHANISM_SUPPRESS_TARGET_OPTIONS,
      MECHANISM_SUPPRESS_MODE: SKILL_DESIGNER_PARAM_MECHANISM_SUPPRESS_MODE_OPTIONS,
      INTERRUPT_WINDOW: SKILL_DESIGNER_PARAM_INTERRUPT_WINDOW_OPTIONS,
      COUNTER_RULE: SKILL_DESIGNER_PARAM_COUNTER_RULE_OPTIONS,
      MUTE_SCOPE: SKILL_DESIGNER_PARAM_MUTE_SCOPE_OPTIONS,
      BLIND_EFFECT: SKILL_DESIGNER_PARAM_BLIND_EFFECT_OPTIONS,
      WEAK_POINT_TYPE: SKILL_DESIGNER_PARAM_WEAK_POINT_TYPE_OPTIONS,
    });
    const SKILL_DESIGNER_FULL_ATTRIBUTE_KEYS = Object.freeze(['str', 'def', 'agi', 'sp_max', 'men_max']);
    const SKILL_DESIGNER_SIDE_EFFECT_TRIGGER_OPTIONS = Object.freeze(['鏂芥斁鍚?, '鍛戒腑鍚?, '鍥炲悎缁撴潫鏃?, '鐘舵€佺粨鏉熷悗']);
    const SKILL_DESIGNER_SIDE_EFFECT_TARGET_OPTIONS = Object.freeze(['鏂芥湳鑰?, '鐘舵€佹寔鏈夎€?, '鍙楁湳鐩爣', '鍙屾柟']);
    const 鎶€鑳芥墽琛岄粦鍚嶅崟閿〃_V1 = Object.freeze([
      '鏂囨湰',
      '鎻忚堪',
      '鏁堟灉鎻忚堪',
      '鍓綔鐢ㄨ鏄?,
      '瀵硅薄宸紓鏉′欢鏂囨',
    ]);
    const 鎶€鑳芥墽琛岄粦鍚嶅崟閿泦鍚坃V1 = new Set(鎶€鑳芥墽琛岄粦鍚嶅崟閿〃_V1);

    function isSkillSummaryEffect(effect) {
      void effect;
      return false;
    }

    function normalizeSkillDesignerArray(value) {
      if (Array.isArray(value)) {
        return Array.from(new Set(value.map(item => toText(item, '').trim()).filter(Boolean)));
      }
      if (typeof value === 'string') {
        return Array.from(new Set(
          value
            .split(/[銆?锛?锝渱]+/)
            .map(item => toText(item, '').trim())
            .filter(Boolean)
        ));
      }
      return [];
    }

    function normalizeSkillDesignerSideEffectStatMap(value = {}) {
      const source = value && typeof value === 'object' && !Array.isArray(value) ? value : {};
      const normalized = {};
      ['str', 'def', 'agi', 'vit_max', 'sp_max', 'men_max'].forEach(key => {
        const parsed = Number(source[key]);
        if (Number.isFinite(parsed) && parsed > 0) normalized[key] = Number(parsed.toFixed(4));
      });
      return normalized;
    }

    function normalizeSkillDesignerSideEffectCombatMap(value = {}) {
      const source = value && typeof value === 'object' && !Array.isArray(value) ? value : {};
      const normalized = {};
      ['skip_turn'].forEach(key => {
        if (source[key] === true) normalized[key] = true;
      });
      ['random_target_rate', 'hit_penalty', 'dodge_penalty', 'cast_speed_penalty', 'control_success_penalty'].forEach(key => {
        const parsed = Number(source[key]);
        if (Number.isFinite(parsed) && parsed > 0) normalized[key] = Number(parsed.toFixed(4));
      });
      return normalized;
    }

    function normalizeSkillDesignerSideEffectEntry(value = {}) {
      if (!value || typeof value !== 'object' || Array.isArray(value)) return null;
      const 鍓綔鐢ㄧ被鍨?= normalizeSkillUiText(value['鍓綔鐢ㄧ被鍨?], '');
      if (!鍓綔鐢ㄧ被鍨? return null;
      const rawTrigger = normalizeSkillUiText(value['瑙﹀彂鏃舵満'], '鏂芥斁鍚?);
      const rawTarget = normalizeSkillUiText(value['鐢熸晥瀵硅薄'], '鏂芥湳鑰?);
      const 瑙﹀彂鏃舵満 = SKILL_DESIGNER_SIDE_EFFECT_TRIGGER_OPTIONS.includes(rawTrigger) ? rawTrigger : '鏂芥斁鍚?;
      const 鐢熸晥瀵硅薄 = SKILL_DESIGNER_SIDE_EFFECT_TARGET_OPTIONS.includes(rawTarget) ? rawTarget : '鏂芥湳鑰?;
      const 鎸佺画鍥炲悎 = Math.max(0, parseSkillDesignerIntegerInputValue(value['鎸佺画鍥炲悎'], 0, 0));
      const rawChance = parseSkillDesignerNumericInputValue(value['瑙﹀彂姒傜巼'], 1, 4);
      const 瑙﹀彂姒傜巼 = Number(Math.max(0, Math.min(1, Number.isFinite(rawChance) ? rawChance : 1)).toFixed(4));
      const 闈㈡澘淇敼姣斾緥 = normalizeSkillDesignerSideEffectStatMap(value['闈㈡澘淇敼姣斾緥']);
      const 鎴樻枟鏁堟灉 = normalizeSkillDesignerSideEffectCombatMap(value['鎴樻枟鏁堟灉']);
      const 鍙傛暟 = value['鍙傛暟'] && typeof value['鍙傛暟'] === 'object' && !Array.isArray(value['鍙傛暟'])
        ? cloneJsonValue(value['鍙傛暟'])
        : {};
      const 鍏宠仈鐘舵€?= normalizeSkillUiText(value['鍏宠仈鐘舵€?], '');
      if (safeEntries(闈㈡澘淇敼姣斾緥).length) 鍙傛暟['闈㈡澘淇敼姣斾緥'] = 闈㈡澘淇敼姣斾緥;
      if (safeEntries(鎴樻枟鏁堟灉).length) 鍙傛暟['鎴樻枟鏁堟灉'] = 鎴樻枟鏁堟灉;
      if (鍏宠仈鐘舵€? 鍙傛暟['鍏宠仈鐘舵€?] = 鍏宠仈鐘舵€?
      safeEntries(value).forEach(([key, raw]) => {
        if (
          ['鍓綔鐢ㄧ被鍨?, '瑙﹀彂鏃舵満', '鐢熸晥瀵硅薄', '鎸佺画鍥炲悎', '瑙﹀彂姒傜巼', '鍙傛暟', '闈㈡澘淇敼姣斾緥', '鎴樻枟鏁堟灉', '鍏宠仈鐘舵€?].includes(key)
        )
          return;
        if (鎶€鑳芥墽琛岄粦鍚嶅崟閿泦鍚坃V1.has(key)) return;
        if (raw === undefined || raw === null) return;
        if (typeof raw === 'string' && !raw.trim()) return;
        鍙傛暟[key] = cloneJsonValue(raw);
      });
      const normalized = { 鍓綔鐢ㄧ被鍨? 瑙﹀彂鏃舵満, 鐢熸晥瀵硅薄, 鎸佺画鍥炲悎, 瑙﹀彂姒傜巼 };
      if (safeEntries(鍙傛暟).length) normalized['鍙傛暟'] = 鍙傛暟;
      return normalized;
    }

    function normalizeSkillDesignerSideEffectList(value = []) {
      const source = Array.isArray(value) ? value : [];
      return source
        .map(item => normalizeSkillDesignerSideEffectEntry(item))
        .filter(Boolean);
    }

    function parseSkillDesignerSideEffectJson(value = '') {
      const text = normalizeSkillUiText(value, '').trim();
      if (!text) return [];
      try {
        const parsed = JSON.parse(text);
        return normalizeSkillDesignerSideEffectList(Array.isArray(parsed) ? parsed : []);
      } catch (error) {
        return [];
      }
    }

    function formatSkillDesignerSideEffectJson(value = []) {
      const normalized = normalizeSkillDesignerSideEffectList(value);
      if (!normalized.length) return '';
      try {
        return JSON.stringify(normalized, null, 2);
      } catch (error) {
        return '';
      }
    }

    function buildSkillDesignerSideEffectSummary(draft = {}) {
      const list = normalizeSkillDesignerSideEffectList(draft && draft['鍓綔鐢ㄥ垪琛?]);
      if (!list.length) return '';
      return list
        .map(item => {
          const trigger = normalizeSkillUiText(item['瑙﹀彂鏃舵満'], '鏂芥斁鍚?);
          const target = normalizeSkillUiText(item['鐢熸晥瀵硅薄'], '鏂芥湳鑰?);
          const duration = Math.max(0, Number(item['鎸佺画鍥炲悎'] || 0));
          return `${normalizeSkillUiText(item['鍓綔鐢ㄧ被鍨?], '鍓綔鐢?)}@${trigger}/${target}${duration > 0 ? `/鎸佺画${duration}鍥炲悎` : ''}`;
        })
        .join('锛?);
    }

    function normalizeSkillDesignerExecutionTargetModel(value = '', fallback = '鏁屾柟鍗曚綋') {
      const text = normalizeSkillUiText(value, '');
      if (!text) return normalizeSkillDesignerTargetForForm(fallback, '鏁屾柟鍗曚綋');
      const aliasMap = {
        '宸辨柟/鍗曚綋': '鍙嬫柟鍗曚綋',
        '宸辨柟/缇や綋': '鍙嬫柟缇や綋',
        '鏁屾柟/鍗曚綋': '鏁屾柟鍗曚綋',
        '鏁屾柟/缇や綋': '鏁屾柟缇や綋',
        椋熺敤鑰? '鍙嬫柟鍗曚綋',
        浣跨敤鑰? '鑷韩',
        闅忔満鐩爣: normalizeSkillDesignerTargetForForm(fallback, '鏁屾柟鍗曚綋'),
      };
      return normalizeSkillDesignerTargetForForm(aliasMap[text] || text, normalizeSkillDesignerTargetForForm(fallback, '鏁屾柟鍗曚綋'));
    }

    const SKILL_DESIGNER_OBJECT_DIFF_CONDITION_OPTIONS = Object.freeze([
      '鑷韩',
      '鍙嬫柟',
      '鏁屾柟',
      '閭瓊甯?,
      '娣辨笂鐢熺墿',
      '榄傚吔',
      '鏂芥湳鑰呬綆琛€閲?,
      '鏂芥湳鑰呴珮琛€閲?,
      '鐩爣浣庤閲?,
      '鐩爣楂樿閲?,
      '鐩爣鏈夌姸鎬?,
      '鐩爣鏃犵姸鎬?,
      '鐩爣鏈夋姢鐩?,
      '鐩爣鏃犳姢鐩?,
    ]);
    const SKILL_DESIGNER_OBJECT_DIFF_ACTION_OPTIONS = Object.freeze([
      '瑕嗙洊',
      '绂佺敤',
      '杞负浼ゅ',
      '鏇挎崲鏈哄埗',
      '鍊嶇巼鎻愬崌',
      '鍊嶇巼鍘嬪埗',
      '鎸佺画寤堕暱',
      '鎸佺画缂╃煭',
      '鏀逛负鑷韩',
      '鏀逛负鍙嬫柟',
      '鏀逛负鏁屾柟',
      '闄勫姞鐘舵€?,
    ]);

    function normalizeSkillDesignerExecutionObjectDiffRuleList(value = [], recordViolation = () => {}) {
      return (Array.isArray(value) ? value : [])
        .map(rule => {
          if (!rule || typeof rule !== 'object' || Array.isArray(rule)) return null;
          safeEntries(rule).forEach(([key]) => {
            if (鎶€鑳芥墽琛岄粦鍚嶅崟閿泦鍚坃V1.has(key)) recordViolation(`瀵硅薄宸紓瑙勫垯.${key}`);
          });
          const 鏉′欢鍘熷 = Array.isArray(rule['鏉′欢'])
            ? rule['鏉′欢']
            : normalizeSkillUiText(rule['鏉′欢'] || rule['鍖归厤'] || rule['瀵硅薄'] || rule.target, '')
              .split(/[\/|,锛屻€乗s]+/)
              .filter(Boolean);
          const 鏉′欢 = Array.from(new Set(鏉′欢鍘熷.map(item => normalizeSkillUiText(item, '')).filter(item =>
            SKILL_DESIGNER_OBJECT_DIFF_CONDITION_OPTIONS.includes(item),
          )));
          const 澶勭悊鍘熷 = normalizeSkillUiText(rule['澶勭悊'] || rule['鍔ㄤ綔'] || rule.action, '瑕嗙洊') || '瑕嗙洊';
          const 澶勭悊 = SKILL_DESIGNER_OBJECT_DIFF_ACTION_OPTIONS.includes(澶勭悊鍘熷) ? 澶勭悊鍘熷 : '瑕嗙洊';
          if (!鏉′欢.length) return null;
          const 鍙傛暟 = rule['鍙傛暟'] && typeof rule['鍙傛暟'] === 'object' && !Array.isArray(rule['鍙傛暟'])
            ? cloneJsonValue(rule['鍙傛暟'])
            : {};
          safeEntries(rule).forEach(([key, raw]) => {
            if (['鏉′欢', '鍖归厤', '瀵硅薄', 'target', '澶勭悊', '鍔ㄤ綔', 'action', '鍙傛暟'].includes(key)) return;
            if (鎶€鑳芥墽琛岄粦鍚嶅崟閿泦鍚坃V1.has(key)) return;
            if (raw === undefined || raw === null) return;
            if (typeof raw === 'string' && !raw.trim()) return;
            鍙傛暟[key] = cloneJsonValue(raw);
          });
          const normalized = { 鏉′欢, 澶勭悊 };
          if (safeEntries(鍙傛暟).length) normalized['鍙傛暟'] = 鍙傛暟;
          return normalized;
        })
        .filter(Boolean);
    }

    function normalizeSkillDesignerExecutionEffectEntry(value = {}, fallbackTargetModel = '鏁屾柟鍗曚綋', recordViolation = () => {}) {
      if (!value || typeof value !== 'object' || Array.isArray(value)) return null;
      const mechanism = normalizeSkillUiText(value['鏈哄埗'], '');
      if (!mechanism || mechanism === '绯荤粺鍩虹' || isSkillSummaryEffect(value)) return null;
      safeEntries(value).forEach(([key]) => {
        if (鎶€鑳芥墽琛岄粦鍚嶅崟閿泦鍚坃V1.has(key)) recordViolation(`${mechanism}.${key}`);
      });
      const 鐩爣妯″瀷 = normalizeSkillDesignerExecutionTargetModel(value['鐩爣妯″瀷'] || value['鐩爣'] || value['瀵硅薄'], fallbackTargetModel);
      const 鎸佺画鍥炲悎 = Math.max(0, parseSkillDesignerIntegerInputValue(value['鎸佺画鍥炲悎'] ?? value['鎸佺画'], 0, 0));
      const 瑙﹀彂鏃舵満 = normalizeSkillUiText(value['瑙﹀彂鏃舵満'] || value['瑙﹀彂'], '');
      const 鍙傛暟 = value['鍙傛暟'] && typeof value['鍙傛暟'] === 'object' && !Array.isArray(value['鍙傛暟'])
        ? cloneJsonValue(value['鍙傛暟'])
        : {};
      safeEntries(value).forEach(([key, raw]) => {
        if (['鏈哄埗', '鐩爣妯″瀷', '鐩爣', '瀵硅薄', '鎸佺画鍥炲悎', '鎸佺画', '瑙﹀彂鏃舵満', '瑙﹀彂', '鍙傛暟', '瀵硅薄宸紓瑙勫垯'].includes(key))
          return;
        if (鎶€鑳芥墽琛岄粦鍚嶅崟閿泦鍚坃V1.has(key)) return;
        if (raw === undefined || raw === null) return;
        if (typeof raw === 'string' && !raw.trim()) return;
        鍙傛暟[key] = cloneJsonValue(raw);
      });
      const 瀵硅薄宸紓瑙勫垯 = normalizeSkillDesignerExecutionObjectDiffRuleList(value['瀵硅薄宸紓瑙勫垯'] || [], recordViolation);
      const 鍒嗘敮鏍囪 = normalizeSkillUiText(value['鍒嗘敮鏍囪'], '');
      const normalized = { '鏈哄埗': mechanism, '鐩爣妯″瀷': 鐩爣妯″瀷 };
      if (鎸佺画鍥炲悎 > 0) normalized['鎸佺画鍥炲悎'] = 鎸佺画鍥炲悎;
      if (瑙﹀彂鏃舵満) normalized['瑙﹀彂鏃舵満'] = 瑙﹀彂鏃舵満;
      if (safeEntries(鍙傛暟).length) normalized['鍙傛暟'] = 鍙傛暟;
      if (瀵硅薄宸紓瑙勫垯.length) normalized['瀵硅薄宸紓瑙勫垯'] = 瀵硅薄宸紓瑙勫垯;
      if (鍒嗘敮鏍囪) normalized['鍒嗘敮鏍囪'] = 鍒嗘敮鏍囪;
      return normalized;
    }

    function normalizeSkillDesignerExecutionSystemBase(systemBase = {}, fallbackTargetModel = '鏁屾柟鍗曚綋', recordViolation = () => {}) {
      const source = systemBase && typeof systemBase === 'object' && !Array.isArray(systemBase) ? systemBase : {};
      safeEntries(source).forEach(([key]) => {
        if (鎶€鑳芥墽琛岄粦鍚嶅崟閿泦鍚坃V1.has(key)) recordViolation(`绯荤粺鍩虹.${key}`);
      });
      const 鐩爣妯″瀷 = normalizeSkillDesignerExecutionTargetModel(source['鐩爣妯″瀷'] || source['瀵硅薄'], fallbackTargetModel);
      const 鍓綔鐢ㄥ垪琛?= normalizeSkillDesignerSideEffectList(source['鍓綔鐢ㄥ垪琛?] || [])
        .map(item => {
          if (!item || typeof item !== 'object') return null;
          const normalized = {
            '鍓綔鐢ㄧ被鍨?: normalizeSkillUiText(item['鍓綔鐢ㄧ被鍨?], ''),
            '瑙﹀彂鏃舵満': normalizeSkillUiText(item['瑙﹀彂鏃舵満'], '鏂芥斁鍚?) || '鏂芥斁鍚?,
            '鐢熸晥瀵硅薄': normalizeSkillUiText(item['鐢熸晥瀵硅薄'], '鏂芥湳鑰?) || '鏂芥湳鑰?,
            '鎸佺画鍥炲悎': Math.max(0, parseSkillDesignerIntegerInputValue(item['鎸佺画鍥炲悎'], 0, 0)),
            '瑙﹀彂姒傜巼': Number(Math.max(0, Math.min(1, parseSkillDesignerNumericInputValue(item['瑙﹀彂姒傜巼'], 1, 4))).toFixed(4)),
          };
          const 鍙傛暟 = item['鍙傛暟'] && typeof item['鍙傛暟'] === 'object' && !Array.isArray(item['鍙傛暟'])
            ? cloneJsonValue(item['鍙傛暟'])
            : {};
          if (safeEntries(鍙傛暟).length) normalized['鍙傛暟'] = 鍙傛暟;
          if (!normalized['鍓綔鐢ㄧ被鍨?]) return null;
          return normalized;
        })
        .filter(Boolean);
      const normalized = {
        '鏈哄埗': '绯荤粺鍩虹',
        '鎶€鑳芥潵婧?: normalizeSkillUiText(source['鎶€鑳芥潵婧?], '榄傛妧') || '榄傛妧',
        '鎶€鑳界被鍨?: normalizeSkillUiText(source['鎶€鑳界被鍨?], '鏃?) || '鏃?,
        '鐩爣妯″瀷': 鐩爣妯″瀷,
        '缁撶畻绛栫暐': normalizeSkillUiText(
          source['缁撶畻绛栫暐'],
          ['鏁屾柟缇や綋', '鍙嬫柟缇や綋', '鍏ㄥ満'].includes(鐩爣妯″瀷) ? '鍏ㄧ洰鏍囩嫭绔? : '鍗曠洰鏍囩嫭绔?,
        ) || '鍗曠洰鏍囩嫭绔?,
        '娑堣€?: source['娑堣€?] === undefined ? '鏃? : cloneJsonValue(source['娑堣€?]),
        'cast_time': Math.max(0, toNumber(source['cast_time'], 0)),
      };
      if (鍓綔鐢ㄥ垪琛?length) normalized['鍓綔鐢ㄥ垪琛?] = 鍓綔鐢ㄥ垪琛?
      return normalized;
    }

    function normalizeSkillDesignerExecutionEffectArray(effectArray = [], fallbackTargetModel = '鏁屾柟鍗曚綋') {
      const source = Array.isArray(effectArray) ? effectArray : [];
      const violationList = [];
      const recordViolation = path => {
        if (!path) return;
        violationList.push(path);
      };
      const rawSystemBase = source.find(effect => normalizeSkillUiText(effect && effect['鏈哄埗'], '') === '绯荤粺鍩虹') || {};
      const systemBase = normalizeSkillDesignerExecutionSystemBase(rawSystemBase, fallbackTargetModel, recordViolation);
      const effectList = source
        .filter(effect => normalizeSkillUiText(effect && effect['鏈哄埗'], '') !== '绯荤粺鍩虹')
        .map(effect => normalizeSkillDesignerExecutionEffectEntry(effect, systemBase['鐩爣妯″瀷'], recordViolation))
        .filter(Boolean);
      if (violationList.length) {
        const uniqueList = Array.from(new Set(violationList));
        throw new Error(`[鎶€鑳芥墽琛岀粨鏋勬牎楠屽け璐 妫€娴嬪埌榛戝悕鍗曞瓧娈碉細${uniqueList.join('銆?)}`);
      }
      return [systemBase, ...effectList];
    }

    function formatSkillDesignerNumericInput(value, digits = 4) {
      const parsed = Number(value);
      if (!Number.isFinite(parsed)) return '';
      return String(Number(parsed.toFixed(digits)));
    }

    function getSkillDesignerEffectDuration(effect = {}) {
      const candidates = [effect && effect['鎸佺画'], effect && effect['鎸佺画鍥炲悎']];
      for (const item of candidates) {
        const parsed = Number(item);
        if (Number.isFinite(parsed) && parsed > 0) return parsed;
      }
      return 0;
    }

    function getSkillDesignerPackedPropertyLabel(property = '') {
      const key = normalizeSkillUiText(property, '');
      return SKILL_DESIGNER_PACKED_PROPERTY_LABELS[key] || key;
    }

    function mergeSkillDesignerMechanicParamSources(...sources) {
      const merged = {};
      sources.forEach(source => {
        safeEntries(source && typeof source === 'object' ? source : {}).forEach(([label, entry]) => {
          if (!label || !entry || typeof entry !== 'object') return;
          const nextEntry = {};
          safeEntries(entry).forEach(([key, value]) => {
            const text = normalizeSkillUiText(value, '');
            if (text) nextEntry[key] = text;
          });
          if (!safeEntries(nextEntry).length) return;
          merged[label] = { ...(merged[label] || {}), ...nextEntry };
        });
      });
      return merged;
    }

    function resolveSkillDesignerNestedEffectMechanism(entry = {}) {
      const explicitMechanism = normalizeSkillUiText(entry && entry['鏈哄埗'], '');
      if (explicitMechanism) return explicitMechanism;
      const description = '';
      if (!description) return '';
      if (/鍒嗚韩/.test(description)) return '鍒嗚韩';
      if (/闇镐綋/.test(description)) return '闇镐綋';
      if (/鎶ょ浘/.test(description)) return '鎶ょ浘';
      if (/鍑忎激/.test(description)) return '鍑忎激';
      if (/鍏嶆|閿佽/.test(description)) return '鍏嶆';
      if (/瑙ｆ帶/.test(description)) return '瑙ｆ帶';
      if (/鍑€鍖?.test(description)) return '鍑€鍖?;
      if (/(鍥炲|鎭㈠)/.test(description) && /榄傚姏/.test(description)) return '榄傚姏鎭㈠';
      if (/(鍥炲|鎭㈠)/.test(description) && /绮剧/.test(description)) return '绮剧鎭㈠';
      if (/(鍥炲|鎭㈠)/.test(description) && /(浣撳姏|鐢熷懡)/.test(description)) return '浣撳姏鎭㈠';
      if (/鍏变韩瑙嗛噹/.test(description)) return '鍏变韩瑙嗛噹';
      if (/鏍囪/.test(description) && /閿佸畾/.test(description)) return '鏍囪閿佸畾';
      return '';
    }

    function buildSkillDesignerNestedEffectEntry(entry = {}, fallback = {}) {
      const entryValue = entry && typeof entry === 'object' ? entry : {};
      const valueConfig = entryValue && entryValue.value && typeof entryValue.value === 'object' ? entryValue.value : {};
      const hasStructuredPayload =
        Object.prototype.hasOwnProperty.call(entryValue, 'type')
        || Object.prototype.hasOwnProperty.call(entryValue, 'target')
        || Object.prototype.hasOwnProperty.call(entryValue, '鐩爣')
        || Object.prototype.hasOwnProperty.call(entryValue, '鎸佺画鍥炲悎')
        || Object.prototype.hasOwnProperty.call(entryValue, '鎸佺画')
        || Object.prototype.hasOwnProperty.call(valueConfig, 'durationRounds')
        || Object.prototype.hasOwnProperty.call(valueConfig, 'duration');
      if (!hasStructuredPayload) return null;

      const mechanism = resolveSkillDesignerNestedEffectMechanism(entryValue);
      if (!mechanism) return null;
      const target = normalizeSkillUiText(
        entryValue['鐩爣'] || entryValue.target || entryValue['瀵硅薄'] || (fallback && fallback.target),
        '',
      );
      const duration = Math.max(
        0,
        toNumber(
          entryValue['鎸佺画鍥炲悎']
          ?? entryValue['鎸佺画']
          ?? valueConfig.durationRounds
          ?? valueConfig.duration
          ?? valueConfig.rounds,
          0,
        ),
      );
      const normalizedEffect = { '鏈哄埗': mechanism };
      if (target) normalizedEffect['鐩爣'] = target;
      if (duration > 0) normalizedEffect['鎸佺画鍥炲悎'] = duration;

      const immuneLevel = normalizeSkillUiText(entryValue['鍏嶆帶绾у埆'] || valueConfig.immuneLevel, '');
      const reduceRatio = formatSkillDesignerNumericInput(entryValue['鍑忎激姣斾緥'] ?? valueConfig.reduceRatio);
      const shieldSource = normalizeSkillUiText(entryValue['鎶ょ浘鏉ユ簮'] || valueConfig.shieldSource, '');
      if (immuneLevel) normalizedEffect['鍏嶆帶绾у埆'] = immuneLevel;
      if (reduceRatio) normalizedEffect['鍑忎激姣斾緥'] = reduceRatio;
      if (shieldSource) normalizedEffect['鎶ょ浘鏉ユ簮'] = shieldSource;
      return normalizedEffect;
    }

    function getSkillDesignerEffectSignature(effect = {}) {
      const mechanism = normalizeSkillUiText(effect && effect['鏈哄埗'], '');
      const target = normalizeSkillUiText(effect && (effect['鐩爣'] || effect['瀵硅薄']), '');
      const property = normalizeSkillUiText(effect && effect['灞炴€?], '');
      const action = normalizeSkillUiText(effect && effect['鍔ㄤ綔'], '');
      const duration = getSkillDesignerEffectDuration(effect);
      return [mechanism, target, property, action, String(duration)].join('|');
    }

    function appendSkillDesignerEffectEntry(bucket = [], seen = new Set(), effect = null) {
      if (!effect || typeof effect !== 'object') return;
      const mechanism = normalizeSkillUiText(effect['鏈哄埗'], '');
      if (!mechanism || mechanism === '绯荤粺鍩虹' || isSkillSummaryEffect(effect)) return;
      const signature = getSkillDesignerEffectSignature(effect);
      if (!signature || seen.has(signature)) return;
      seen.add(signature);
      bucket.push(effect);
    }

    function appendSkillDesignerEffectEntries(source, bucket = [], seen = new Set(), context = {}) {
      if (Array.isArray(source)) {
        source.forEach(entry => appendSkillDesignerEffectEntries(entry, bucket, seen, context));
        return bucket;
      }
      if (!source || typeof source !== 'object') return bucket;

      const fallbackTarget = normalizeSkillUiText(context && context.target, '');
      const resolvedTarget = normalizeSkillUiText(
        source['鐩爣'] || source['瀵硅薄'] || source.target || fallbackTarget,
        fallbackTarget,
      );
      const explicitMechanism = normalizeSkillUiText(source['鏈哄埗'], '');
      if (explicitMechanism) {
        const clonedEffect = cloneJsonValue(source);
        if (!normalizeSkillUiText(clonedEffect['鐩爣'] || clonedEffect['瀵硅薄'], '') && resolvedTarget) {
          clonedEffect['鐩爣'] = resolvedTarget;
        }
        appendSkillDesignerEffectEntry(bucket, seen, clonedEffect);
      } else {
        appendSkillDesignerEffectEntry(
          bucket,
          seen,
          buildSkillDesignerNestedEffectEntry(source, { target: resolvedTarget }),
        );
      }

      if (Array.isArray(source['浣跨敤鏁堟灉'])) {
        appendSkillDesignerEffectEntries(source['浣跨敤鏁堟灉'], bucket, seen, { target: resolvedTarget || fallbackTarget });
      }
      if (Array.isArray(source.effects)) {
        appendSkillDesignerEffectEntries(source.effects, bucket, seen, { target: resolvedTarget || fallbackTarget });
      }
      return bucket;
    }

    function getSkillDesignerSystemBaseEffect(effectArray = []) {
      return (Array.isArray(effectArray) ? effectArray : []).find(effect => {
        return normalizeSkillUiText(effect && effect['鏈哄埗'], '') === '绯荤粺鍩虹';
      }) || {};
    }

    function getSkillDesignerSystemBaseTags(effectArray = []) {
      return normalizeSkillDesignerArray(getSkillDesignerSystemBaseEffect(effectArray)['鏍囩']);
    }

    function analyzeSkillDesignerPackedEffects(effectArray = []) {
      const effectEntries = getSkillDesignerEffectEntries(effectArray);
      const analysis = {
        effectEntries,
        primaryHints: [],
        secondaryHints: [],
        attributeBuffs: [],
        attributeDebuffs: [],
        immediateRecovers: [],
        overtimeRecovers: [],
        paramHints: [],
      };

      const pushParamHint = (label, params) => {
        if (!label || !params || typeof params !== 'object') return;
        analysis.paramHints.push({ label, params });
      };

      effectEntries.forEach(effect => {
        const mechanism = normalizeSkillUiText(effect && effect['鏈哄埗'], '');
        const property = normalizeSkillUiText(effect && effect['灞炴€?], '');
        const action = normalizeSkillUiText(effect && effect['鍔ㄤ綔'], '');
        const valueText = formatSkillDesignerNumericInput(effect && effect['鏁板€?]);
        const duration = getSkillDesignerEffectDuration(effect);
        const durationText = duration > 0 ? formatSkillDesignerNumericInput(duration, 0) : '';
        const baseHint = buildSkillDesignerPrimaryHintFromRegistry(mechanism) || SKILL_DESIGNER_PRIMARY_MECHANISM_HINTS[mechanism];
        if (baseHint) {
          analysis.primaryHints.push(baseHint);
          if (baseHint.secondary) analysis.secondaryHints.push(baseHint.secondary);
        }
        normalizeSkillDesignerArray([
          ...buildSkillDesignerSecondaryHintsFromRegistry(mechanism),
          ...(SKILL_DESIGNER_SECONDARY_MECHANISM_HINTS[mechanism] || []),
        ]).forEach(label => {
          analysis.secondaryHints.push(label);
        });

        if (mechanism === '鎶ょ浘') {
          pushParamHint('鎶ょ浘', {
            duration: durationText,
            shieldCap: normalizeSkillUiText(effect && effect['鎶ょ浘鏉ユ簮'], ''),
          });
          return;
        }

        if (mechanism === '鍑忎激') {
          pushParamHint('鍑忎激', {
            duration: durationText,
            reduceRatio: formatSkillDesignerNumericInput(effect && effect['鍑忎激姣斾緥']),
            damageType: normalizeSkillUiText(effect && effect['瑕嗙洊绫诲瀷'], ''),
          });
          return;
        }

        if (mechanism === '闇镐綋') {
          pushParamHint('闇镐綋', {
            duration: durationText,
            immuneLevel: normalizeSkillUiText(effect && effect['鍏嶆帶绾у埆'], ''),
            reduceRatio: formatSkillDesignerNumericInput(effect && effect['鍑忎激姣斾緥']),
          });
          return;
        }

        if (mechanism === '鍒嗚韩') {
          pushParamHint('鍒嗚韩', {
            duration: durationText,
            cloneType: normalizeSkillUiText(effect && effect['鍒嗚韩绫诲瀷'], ''),
            cloneCount: formatSkillDesignerNumericInput(effect && effect['鍒嗚韩鏁伴噺'], 0),
            stealthRatio: formatSkillDesignerNumericInput(effect && effect['闅愯斀搴?]),
            inheritRatio: formatSkillDesignerNumericInput(effect && effect['瀹炲姏缁ф壙姣斾緥']),
            cloneName: normalizeSkillUiText(effect && effect['鍒嗚韩鍚嶇О'], ''),
          });
          return;
        }

        if (mechanism === '鏃犳晫閲戣韩') {
          pushParamHint('鏃犳晫閲戣韩', {
            duration: durationText,
            dailyLimit: formatSkillDesignerNumericInput(effect && effect['姣忔棩瑙﹀彂涓婇檺'], 3),
            tierThreshold: formatSkillDesignerNumericInput(effect && effect['鍏嶇柅浣嶉樁闃堝€?], 100),
            reduceRatio: formatSkillDesignerNumericInput(effect && effect['damage_reduction']),
          });
          return;
        }

        if (mechanism === '浼ゅ鍙嶅皠') {
          pushParamHint('浼ゅ鍙嶅皠', {
            duration: durationText,
            reflectRatio: formatSkillDesignerNumericInput(effect && (effect['鍙嶅皠姣斾緥'] ?? effect['damage_reflect_ratio'])),
          });
          return;
        }

        if (mechanism === '浼ゅ鍒嗘憡') {
          pushParamHint('浼ゅ鍒嗘憡', {
            duration: durationText,
            shareRatio: formatSkillDesignerNumericInput(effect && (effect['鍒嗘憡姣斾緥'] ?? effect['damage_share_ratio'])),
            shareCount: formatSkillDesignerNumericInput(effect && (effect['鍒嗘憡浜烘暟'] ?? effect['damage_share_count']), 1),
          });
          return;
        }

        if (mechanism === '鏇胯韩鎶垫秷') {
          pushParamHint('鏇胯韩鎶垫秷', {
            duration: durationText,
            substituteCount: formatSkillDesignerNumericInput(effect && (effect['鎶垫秷娆℃暟'] ?? effect['substitute_count']), 1),
          });
          return;
        }

        if (mechanism === '澶嶈嫃') {
          pushParamHint('澶嶈嫃', {
            duration: durationText,
            reviveCount: formatSkillDesignerNumericInput(effect && (effect['澶嶈嫃娆℃暟'] ?? effect['revive_count']), 1),
            reviveHealRatio: formatSkillDesignerNumericInput(effect && (effect['澶嶈嫃鍥炶姣斾緥'] ?? effect['revive_heal_ratio']), 0.25),
          });
          return;
        }

        if (mechanism === '娌荤枟鍙嶈浆') {
          pushParamHint('娌荤枟鍙嶈浆', {
            duration: durationText,
            invertRatio: formatSkillDesignerNumericInput(effect && (effect['鍙嶈浆姣斾緥'] ?? effect['heal_inversion_ratio']), 1),
          });
          return;
        }

        if (mechanism === '灏佹妧') {
          pushParamHint('灏佹妧', {
            duration: durationText,
            muteScope: normalizeSkillUiText(effect && effect['闄愬埗鑼冨洿'], ''),
          });
          return;
        }

        if (mechanism === '鐘舵€佽浆绉?) {
          pushParamHint('鐘舵€佽浆绉?, {
            transferMode: normalizeSkillUiText(effect && effect['杞Щ绫诲瀷'], ''),
          });
          return;
        }

        if (mechanism === '寮曠垎鎸佺画浼ゅ') {
          pushParamHint('寮曠垎鎸佺画浼ゅ', {
            detonateRatio: formatSkillDesignerNumericInput(effect && (effect['寮曠垎鍊嶇巼'] ?? effect['detonate_ratio']), 1.2),
            consumeMode: normalizeSkillUiText(effect && effect['娑堣€楁寔缁激瀹?], ''),
          });
          return;
        }

        if (mechanism === '鏂╃浘') {
          pushParamHint('鏂╃浘', {
            shieldBreakRatio: formatSkillDesignerNumericInput(effect && (effect['鏂╃浘鍊嶇巼'] ?? effect['shield_break_ratio']), 0.6),
          });
          return;
        }

        if (mechanism === '绐冨彇鎶ょ浘') {
          pushParamHint('绐冨彇鎶ょ浘', {
            shieldStealRatio: formatSkillDesignerNumericInput(effect && (effect['绐冪浘姣斾緥'] ?? effect['shield_steal_ratio']), 0.45),
            duration: durationText,
          });
          return;
        }

        if (mechanism === '鍚炲櫖') {
          pushParamHint('鍚炲櫖', {
            resourceType: normalizeSkillUiText(effect && effect['璧勬簮绫诲瀷'], '榄傚姏'),
            drainRatio: formatSkillDesignerNumericInput(effect && (effect['澶哄彇姣斾緥'] ?? effect['drain_ratio']), 0.18),
            convertRatio: formatSkillDesignerNumericInput(effect && (effect['杞寲姣斾緥'] ?? effect['convert_ratio']), 1),
          });
          return;
        }

        if (mechanism === '鑳藉姏鍏变韩') {
          pushParamHint('鑳藉姏鍏变韩', {
            resourceType: normalizeSkillUiText(effect && effect['璧勬簮绫诲瀷'], '榄傚姏'),
            refeedRatio: formatSkillDesignerNumericInput(effect && (effect['鍙嶇亴姣斾緥'] ?? effect['refeed_ratio']), 0.2),
          });
          return;
        }

        if (mechanism === '鏈哄埗鎶规秷') {
          pushParamHint('鏈哄埗鎶规秷', {
            suppressTarget: normalizeSkillUiText(effect && (effect['鎶规秷鐩爣'] ?? effect['鏈哄埗鎶规秷鐩爣']), '澶嶈嫃'),
            suppressMode: normalizeSkillUiText(effect && (effect['鎶规秷鏂瑰紡'] ?? effect['鏈哄埗鎶规秷鏂瑰紡']), '绉婚櫎骞跺皝閿?),
            duration: durationText,
          });
          return;
        }

        if (mechanism === '椹辨暎澧炵泭') {
          pushParamHint('椹辨暎澧炵泭', {
            dispelCount: formatSkillDesignerNumericInput(effect && effect['椹辨暎鏁伴噺'], 1),
          });
          return;
        }

        if (mechanism === '绐冨彇澧炵泭') {
          pushParamHint('绐冨彇澧炵泭', {
            stealCount: formatSkillDesignerNumericInput(effect && effect['绐冨彇鏁伴噺'], 1),
          });
          return;
        }

        if (mechanism === '闅愯韩') {
          pushParamHint('闅愯韩', {
            duration: durationText,
            stealthRatio: formatSkillDesignerNumericInput(effect && (effect['闅愯斀搴?] ?? effect['stealth_level']), 0.3),
            dodgeBonus: formatSkillDesignerNumericInput(effect && effect['dodge_bonus'], 0.1),
            reactionBonus: formatSkillDesignerNumericInput(effect && effect['reaction_bonus'], 0.08),
          });
          return;
        }

        if (mechanism === '鎶ゅ崼') {
          pushParamHint('鎶ゅ崼', {
            duration: durationText,
            reduceRatio: formatSkillDesignerNumericInput(effect && (effect['damage_reduction'] ?? effect['鍑忎激姣斾緥']), 0.1),
          });
          return;
        }

        if (mechanism === '鍢茶') {
          pushParamHint('鍢茶', {
            duration: durationText,
            focusRule: normalizeSkillUiText(effect && effect['鑱氱伀瑙勫垯'], ''),
          });
          return;
        }

        if (mechanism === '鐮撮殣') {
          pushParamHint('鐮撮殣', {
            hitBonus: formatSkillDesignerNumericInput(effect && effect['hit_bonus'], 0.1),
            lockLevel: formatSkillDesignerNumericInput(effect && effect['lock_level'], 1),
          });
          return;
        }

        if (mechanism === '鐩爣閿佸畾') {
          pushParamHint('鐩爣閿佸畾', {
            duration: durationText,
            hitBonus: formatSkillDesignerNumericInput(effect && effect['hit_bonus'], 0.1),
            lockLevel: formatSkillDesignerNumericInput(effect && effect['lock_level'], 1),
          });
          return;
        }

        if (mechanism === '杩藉嚮') {
          pushParamHint('杩藉嚮', {
            duration: durationText,
            bonusRatio: formatSkillDesignerNumericInput(effect && effect['final_damage_mult'], 1.2),
            followWindow: normalizeSkillUiText(effect && effect['杩藉嚮绐楀彛'], ''),
          });
          return;
        }

        if (mechanism === '缂存') {
          pushParamHint('缂存', {
            duration: durationText,
            disarmScope: normalizeSkillUiText(effect && effect['闄愬埗鑼冨洿'], ''),
          });
          return;
        }

        if (mechanism === '灞炴€у彉鍖?) {
          if (action === '鍔犲€? && ['vit', 'sp', 'men'].includes(property)) {
            const sub = property === 'sp' ? '榄傚姏鎭㈠' : property === 'men' ? '绮剧鎭㈠' : '浣撳姏鎭㈠';
            analysis.primaryHints.push({ main: '鍥炲绫?, sub });
            analysis.immediateRecovers.push({ property, valueText, durationText });
            pushParamHint(sub, { recoverRatio: valueText, repeatCount: '1' });
            return;
          }
          if (['鍔犲€?, '鍊嶇巼鎻愬崌'].includes(action)) {
            analysis.attributeBuffs.push({ property, action, valueText, durationText });
            return;
          }
          if (['鍑忓€?, '鍊嶇巼鍘嬪埗'].includes(action)) {
            analysis.attributeDebuffs.push({ property, action, valueText, durationText });
            return;
          }
          return;
        }

        if (mechanism === '鎸佺画鎭㈠') {
          analysis.primaryHints.push({ main: '鍥炲绫?, sub: '鎸佺画鎭㈠' });
          analysis.overtimeRecovers.push({ property, valueText, durationText });
          pushParamHint('鎸佺画鎭㈠', { recoverRatio: valueText, duration: durationText });
          return;
        }

        if (mechanism === '瑙ｆ帶' || mechanism === '鍑€鍖?) {
          pushParamHint('鍑€鍖?瑙ｆ帶', {
            cleanseCount: formatSkillDesignerNumericInput(effect && effect['娓呴櫎灞傜骇'], 1),
            cleansePriority: normalizeSkillUiText(effect && effect['娓呴櫎浼樺厛绾?], ''),
            extraGain: normalizeSkillUiText(effect && effect['闄勫甫鏀剁泭'], ''),
          });
          return;
        }

        if (mechanism === '娑堣€椾慨姝?) {
          const sub = action === '娑堣€楁彁楂? ? '娑堣€楁彁楂? : '娑堣€楅檷浣?;
          analysis.primaryHints.push({ main: action === '娑堣€楁彁楂? ? '鍓婂急绫? : '澧炵泭绫?, sub });
          pushParamHint(sub, { resourceType: '娣峰悎', gainRatio: valueText, duration: durationText });
          return;
        }

        if (mechanism === '鍓嶆憞淇') {
          const sub = action === '鍓嶆憞鎷夐暱' ? '鍓嶆憞鎷夐暱' : '鍓嶆憞缂╃煭';
          analysis.primaryHints.push({ main: action === '鍓嶆憞鎷夐暱' ? '鍓婂急绫? : '澧炵泭绫?, sub });
          pushParamHint(sub, { gainRatio: valueText, duration: durationText });
          return;
        }

        if (mechanism === '鎺屾帶淇') {
          const sub = action === '鍊嶇巼鍘嬪埗' ? '鎺屾帶鍘嬪埗' : '鎺屾帶鎻愬崌';
          analysis.primaryHints.push({ main: action === '鍊嶇巼鍘嬪埗' ? '鍓婂急绫? : '澧炵泭绫?, sub });
          pushParamHint(sub, { gainRatio: valueText, duration: durationText });
          return;
        }

        if (mechanism === '閫熷害淇') {
          const sub = action === '鍊嶇巼鍘嬪埗' ? '閫熷害鍘嬪埗' : '閫熷害鎻愬崌';
          analysis.primaryHints.push({ main: action === '鍊嶇巼鍘嬪埗' ? '鍓婂急绫? : '澧炵泭绫?, sub });
          pushParamHint(sub, { gainRatio: valueText, duration: durationText });
        }
      });

      return analysis;
    }

    function buildSkillDesignerEffectBasedPrimaryMechanic(effectAnalysis = null) {
      const analysis = effectAnalysis && typeof effectAnalysis === 'object' ? effectAnalysis : null;
      if (!analysis) return { main: '', sub: '' };
      if (analysis.primaryHints.length > 0) {
        const first = analysis.primaryHints.find(hint => hint && hint.main && hint.sub) || analysis.primaryHints[0] || {};
        return {
          main: normalizeSkillUiText(first.main, ''),
          sub: normalizeSkillUiText(first.sub, ''),
        };
      }
      if (analysis.attributeBuffs.length > 0) {
        const attrSet = new Set(analysis.attributeBuffs.map(item => normalizeSkillUiText(item.property, '')).filter(Boolean));
        const sub = SKILL_DESIGNER_FULL_ATTRIBUTE_KEYS.every(key => attrSet.has(key))
          ? '鍏ㄥ睘鎬у鐩?
          : attrSet.size > 1
            ? '澶氬睘鎬у鐩?
            : '鍗曞睘鎬у鐩?;
        return { main: '澧炵泭绫?, sub };
      }
      if (analysis.attributeDebuffs.length > 0) {
        const attrSet = new Set(analysis.attributeDebuffs.map(item => normalizeSkillUiText(item.property, '')).filter(Boolean));
        return { main: '鍓婂急绫?, sub: attrSet.size > 1 ? '澶氬睘鎬у墛寮? : '鍗曞睘鎬у墛寮? };
      }
      return { main: '', sub: '' };
    }

    function buildSkillDesignerInferredMechanicParams(effectArray = [], draft = {}) {
      const analysis = analyzeSkillDesignerPackedEffects(effectArray);
      const inferred = {};
      const pushParams = (label, params) => {
        if (!label || !params || typeof params !== 'object') return;
        inferred[label] = { ...(inferred[label] || {}), ...params };
      };

      analysis.paramHints.forEach(entry => pushParams(entry.label, entry.params));

      const primarySub = normalizeSkillUiText(draft.primarySub, '');
      if (['鍗曞睘鎬у鐩?, '澶氬睘鎬у鐩?, '鍏ㄥ睘鎬у鐩?].includes(primarySub) && analysis.attributeBuffs.length > 0) {
        const attrLabels = Array.from(
          new Set(analysis.attributeBuffs.map(item => getSkillDesignerPackedPropertyLabel(item.property)).filter(Boolean)),
        );
        const first = analysis.attributeBuffs[0];
        if (primarySub === '鍗曞睘鎬у鐩?) {
          pushParams('鍗曞睘鎬у鐩?, {
            buffAttr: attrLabels[0] || '',
            gainRatio: first.valueText || '',
            duration: first.durationText || '',
          });
        } else if (primarySub === '澶氬睘鎬у鐩?) {
          pushParams('澶氬睘鎬у鐩?, {
            buffAttrGroup: attrLabels.join('/'),
            gainRatio: first.valueText || '',
            duration: first.durationText || '',
          });
        } else {
          pushParams('鍏ㄥ睘鎬у鐩?, {
            allGainRatio: first.valueText || '',
            duration: first.durationText || '',
          });
        }
      }

      if (['鍗曞睘鎬у墛寮?, '澶氬睘鎬у墛寮?].includes(primarySub) && analysis.attributeDebuffs.length > 0) {
        const attrLabels = Array.from(
          new Set(analysis.attributeDebuffs.map(item => getSkillDesignerPackedPropertyLabel(item.property)).filter(Boolean)),
        );
        const first = analysis.attributeDebuffs[0];
        if (primarySub === '鍗曞睘鎬у墛寮?) {
          pushParams('鍗曞睘鎬у墛寮?, {
            debuffAttr: attrLabels[0] || '',
            reduceRatio: first.valueText || '',
            duration: first.durationText || '',
          });
        } else {
          pushParams('澶氬睘鎬у墛寮?, {
            debuffAttrGroup: attrLabels.join('/'),
            reduceRatio: first.valueText || '',
            duration: first.durationText || '',
          });
        }
      }

      return normalizeSkillDesignerMechanicParamMap(inferred, draft);
    }

    function getSkillDesignerEffectEntries(effectArray = []) {
      return appendSkillDesignerEffectEntries(Array.isArray(effectArray) ? effectArray : [], [], new Set());
    }

    function getSkillDesignerDefaultPrimarySub(primaryMain = '', target = '', type = '') {
      const normalizedMain = normalizeSkillUiText(primaryMain, '');
      const normalizedTarget = normalizeSkillUiText(target, '');
      const normalizedType = normalizeSkillUiText(type, '');
      if (normalizedMain === '浼ゅ绫?) return /缇や綋|鍏ㄥ満/.test(normalizedTarget) ? '缇や綋浼ゅ' : '鍗曚綋浼ゅ';
      if (normalizedMain === '鎺у埗绫?) return '纭帶';
      if (normalizedMain === '鍓婂急绫?) return /绂佺枟/.test(normalizedType) ? '绂佺枟' : '鍗曞睘鎬у墛寮?;
      if (normalizedMain === '澧炵泭绫?) return /缇や綋|鍏ㄥ満/.test(normalizedTarget) ? '澶氬睘鎬у鐩? : '鍗曞睘鎬у鐩?;
      if (normalizedMain === '闃插尽绫?) return '鎶ょ浘';
      if (normalizedMain === '鍥炲绫?) return '浣撳姏鎭㈠';
      if (normalizedMain === '鎰熺煡/璁ょ煡绫?) return '鏍囪閿佸畾';
      if (normalizedMain === '浣嶇Щ绫?) return '鑷韩浣嶇Щ';
      if (normalizedMain === '鐗规畩瑙勫垯绫?) return /鍙嶅埗/.test(normalizedType) ? '鍙嶅埗' : '鏉′欢瑙﹀彂';
      return '';
    }

    function inferSkillDesignerMainMechanicFromType(type = '', target = '', effectEntries = []) {
      const normalizedType = normalizeSkillUiText(type, '');
      if (!normalizedType || normalizedType === '鏈煡') return { main: '', sub: '' };
      const pickSub = main => {
        const match = effectEntries.find(effect => {
          const hint = SKILL_DESIGNER_PRIMARY_MECHANISM_HINTS[normalizeSkillUiText(effect && effect['鏈哄埗'], '')];
          return hint && hint.main === main && hint.sub;
        });
        return match
          ? SKILL_DESIGNER_PRIMARY_MECHANISM_HINTS[normalizeSkillUiText(match && match['鏈哄埗'], '')].sub
          : getSkillDesignerDefaultPrimarySub(main, target, normalizedType);
      };

      if (/浣嶇Щ/.test(normalizedType)) return { main: '浣嶇Щ绫?, sub: pickSub('浣嶇Щ绫?) };
      if (/鎺у埗/.test(normalizedType)) return { main: '鎺у埗绫?, sub: pickSub('鎺у埗绫?) };
      if (/鍓婂急|绂佺枟/.test(normalizedType)) return { main: '鍓婂急绫?, sub: pickSub('鍓婂急绫?) };
      if (/闃插尽|鎶ょ浘/.test(normalizedType)) return { main: '闃插尽绫?, sub: pickSub('闃插尽绫?) };
      if (/鍥炲|娌荤枟/.test(normalizedType)) return { main: '鍥炲绫?, sub: pickSub('鍥炲绫?) };
      if (/澧炵泭|杈呭姪|棰嗗煙/.test(normalizedType)) return { main: '澧炵泭绫?, sub: pickSub('澧炵泭绫?) };
      if (/鎰熺煡|璁ょ煡|骞诲|鍌湢|鏍囪/.test(normalizedType)) return { main: '鎰熺煡/璁ょ煡绫?, sub: pickSub('鎰熺煡/璁ょ煡绫?) };
      if (/杈撳嚭|鐪熶激|鐮寸敳|鐖嗗彂/.test(normalizedType)) return { main: '浼ゅ绫?, sub: pickSub('浼ゅ绫?) };
      if (/琚姩|鍙嶅埗|瑙勫垯|鐪熻韩|缁濇妧/.test(normalizedType)) return { main: '鐗规畩瑙勫垯绫?, sub: pickSub('鐗规畩瑙勫垯绫?) };
      return { main: '', sub: '' };
    }

    function inferSkillDesignerMechanicsFromEffectArray(effectArray = [], type = '', target = '') {
      const effectEntries = getSkillDesignerEffectEntries(effectArray);
      const effectAnalysis = analyzeSkillDesignerPackedEffects(effectArray);
      const effectHint = buildSkillDesignerEffectBasedPrimaryMechanic(effectAnalysis);
      const typeHint = inferSkillDesignerMainMechanicFromType(type, target, effectEntries);
      const systemTagHints = getSkillDesignerSystemBaseTags(effectArray)
        .map(tag => SKILL_DESIGNER_PRIMARY_MECHANISM_HINTS[normalizeSkillUiText(tag, '')])
        .filter(Boolean);
      let primaryMain = normalizeSkillUiText(effectHint.main, '');
      let primarySub = normalizeSkillUiText(effectHint.sub, '');
      const secondaryCandidates = [...normalizeSkillDesignerArray(effectAnalysis.secondaryHints)];

      effectAnalysis.primaryHints.forEach(primaryHint => {
        if (!primaryHint || typeof primaryHint !== 'object') return;
        if (!primaryMain && primaryHint.main) primaryMain = normalizeSkillUiText(primaryHint.main, '');
        if (!primarySub && primaryHint.sub && (!primaryMain || normalizeSkillUiText(primaryHint.main, '') === primaryMain)) {
          primarySub = normalizeSkillUiText(primaryHint.sub, '');
        }
      });

      systemTagHints.forEach(primaryHint => {
        if (!primaryHint || typeof primaryHint !== 'object') return;
        if (!primaryMain && primaryHint.main) primaryMain = normalizeSkillUiText(primaryHint.main, '');
        if (!primarySub && primaryHint.sub && (!primaryMain || normalizeSkillUiText(primaryHint.main, '') === primaryMain)) {
          primarySub = normalizeSkillUiText(primaryHint.sub, '');
        }
        if (primaryHint.secondary) secondaryCandidates.push(primaryHint.secondary);
      });

      if (!primaryMain && typeHint.main) primaryMain = normalizeSkillUiText(typeHint.main, '');
      if (!primarySub && typeHint.sub && (!primaryMain || normalizeSkillUiText(typeHint.main, '') === primaryMain)) {
        primarySub = normalizeSkillUiText(typeHint.sub, '');
      }

      if (!primaryMain && effectEntries.some(effect => normalizeSkillUiText(effect && effect['鏈哄埗'], '') === '鐘舵€佹寕杞?)) {
        primaryMain = /鑷韩|椋熺敤鑰厊浣跨敤鑰?.test(normalizeSkillUiText(target, '')) ? '澧炵泭绫? : '鍓婂急绫?;
      }
      if (!primarySub) primarySub = getSkillDesignerDefaultPrimarySub(primaryMain, target, type);

      effectAnalysis.primaryHints.forEach(primaryHint => {
        const secondarySub = normalizeSkillUiText(primaryHint && primaryHint.sub, '');
        if (secondarySub && secondarySub !== primarySub) secondaryCandidates.push(secondarySub);
      });
      const secondaryMechanics = normalizeSkillDesignerArray(
        secondaryCandidates.filter(label => {
          const normalizedLabel = normalizeSkillUiText(label, '');
          return !!normalizedLabel && normalizedLabel !== primarySub;
        }),
      );

      return {
        primaryMain: normalizeSkillUiText(primaryMain, ''),
        primarySub: normalizeSkillUiText(primarySub, ''),
        secondaryMechanics,
      };
    }

    function inferSkillDesignerDeliveryForm(effectArray = [], type = '', target = '') {
      const effectEntries = getSkillDesignerEffectEntries(effectArray);
      const mechanisms = effectEntries.map(effect => normalizeSkillUiText(effect && effect['鏈哄埗'], ''));
      const normalizedType = normalizeSkillUiText(type, '');
      const normalizedTarget = normalizeSkillUiText(target, '');
      if (mechanisms.includes('閫犵墿鐢熸垚') || /椋熺墿绯?.test(normalizedType)) return '閫犵墿鎵胯浇';
      if (mechanisms.includes('鍒嗚韩')) return '鍙敜鎵胯浇';
      if (mechanisms.includes('鍙敜涓庡満鍦?)) return '鍙敜鎵胯浇';
      if (mechanisms.includes('鏉′欢瑙﹀彂') || mechanisms.includes('寤惰繜鐖嗗彂')) return '寤惰繜瑙﹀彂';
      if (mechanisms.includes('鏍囪閿佸畾') || mechanisms.includes('鏍囪寮辩偣')) return '鏍囪瑙﹀彂';
      if (/缇や綋|鍏ㄥ満/.test(normalizedTarget)) return '鑼冨洿灞曞紑';

      const explicitTargets = effectEntries
        .map(effect => normalizeSkillUiText(effect && (effect['鐩爣'] || effect['瀵硅薄']), ''))
        .filter(Boolean);
      const 浠呰嚜韬?=
        explicitTargets.length > 0
        && explicitTargets.every(value => /鑷韩|浣跨敤鑰厊椋熺敤鑰?.test(value));
      if (浠呰嚜韬? return '鑷韩闄勪綋';

      const hasRangedDamage = effectEntries.some(effect => /杩滅▼/.test(normalizeSkillUiText(effect && effect['浼ゅ绫诲瀷'], '')));
      if (hasRangedDamage) return '杩滅▼鍛戒腑';
      return '鐩存帴鐢熸晥';
    }

    function inferSkillDesignerMainRole(type = '', target = '', primaryMain = '', primarySub = '') {
      const normalizedType = normalizeSkillUiText(type, '');
      const normalizedTarget = normalizeSkillUiText(target, '');
      const normalizedMain = normalizeSkillUiText(primaryMain, '');
      const normalizedSub = normalizeSkillUiText(primarySub, '');
      if (/琚姩/.test(normalizedType)) return '琚姩鍥哄寲';
      if (normalizedMain === '浼ゅ绫?) return /鎸佺画/.test(normalizedSub) ? '鎸佺画鍘嬪埗' : '鐖嗗彂杈撳嚭';
      if (normalizedMain === '鎺у埗绫? || normalizedMain === '鍓婂急绫?) return '鎺у埗璧锋墜';
      if (normalizedMain === '闃插尽绫?) return '淇濇姢鎵夸激';
      if (normalizedMain === '鍥炲绫?) return '鍥炲缁埅';
      if (normalizedMain === '澧炵泭绫?) return '澧炵泭杈呭姪';
      if (normalizedMain === '鎰熺煡/璁ょ煡绫? || normalizedMain === '浣嶇Щ绫? || normalizedMain === '鐗规畩瑙勫垯绫?)
        return '鐗规畩瑙勫垯';
      if (/鑷韩|浣跨敤鑰厊椋熺敤鑰?.test(normalizedTarget)) return '澧炵泭杈呭姪';
      return '鏈煡';
    }

    function inferSkillDesignerTags(effectArray = [], skill = {}, draft = {}) {
      const explicitTags = normalizeSkillDesignerArray((skill && skill['鏍囩']) || (draft && draft['鏍囩']) || []);
      if (explicitTags.length) return explicitTags;
      const systemTags = getSkillDesignerSystemBaseTags(effectArray);
      if (systemTags.length) return systemTags.slice(0, 8);
      return normalizeSkillDesignerArray(
        getSkillDesignerEffectEntries(effectArray)
          .map(effect => normalizeSkillUiText(effect && effect['鏈哄埗'], ''))
          .filter(Boolean),
      ).slice(0, 8);
    }

    function compactSkillDesignerStoredTags(tags = []) {
      const genericTags = new Set([
        ...Object.keys(SKILL_DESIGNER_MAIN_MECHANIC_POOL || {}),
        ...(Array.isArray(SKILL_DESIGNER_DELIVERY_FORM_POOL) ? SKILL_DESIGNER_DELIVERY_FORM_POOL : []),
        ...(Array.isArray(SKILL_DESIGNER_SKILL_TYPES) ? SKILL_DESIGNER_SKILL_TYPES : []),
      ].map(label => normalizeSkillUiText(label, '')).filter(Boolean));
      return normalizeSkillDesignerArray(tags).filter(label => !genericTags.has(normalizeSkillUiText(label, '')));
    }

    function getSkillSummaryEffectByMechanism(effectArray, mechanism = '') {
      void effectArray;
      void mechanism;
      return null;
    }

    function cleanSkillDisplayName(skill, rawName = '') {
      return normalizeSkillUiText(
        (skill && skill['榄傛妧鍚?]) || (skill && skill['name']) || rawName.replace(/\[鍚庡彴鎺ㄦ紨\]\s*/g, ''),
        '鏈懡鍚嶉瓊鎶€'
      );
    }

    function buildSkillDesignerPreviewKey(meta = {}) {
      const path = Array.isArray(meta.path) ? meta.path : [];
      if (!path.length) return '';
      const payload = {
        path,
        label: toText(meta.label, '鎶€鑳?),
        category: toText(meta.category, '鎶€鑳?),
        scope: toText(meta.scope, '榄傛妧'),
      };
      return `${SKILL_DESIGNER_PREVIEW_PREFIX}${encodeURIComponent(JSON.stringify(payload))}`;
    }

    function parseSkillDesignerPreviewKey(previewKey = '') {
      const raw = toText(previewKey, '');
      if (!raw.startsWith(SKILL_DESIGNER_PREVIEW_PREFIX)) return null;
      try {
        const payload = JSON.parse(decodeURIComponent(raw.slice(SKILL_DESIGNER_PREVIEW_PREFIX.length)));
        return {
          path: Array.isArray(payload && payload.path) ? payload.path : [],
          label: toText(payload && payload.label, '鎶€鑳?),
          category: toText(payload && payload.category, '鎶€鑳?),
          scope: toText(payload && payload.scope, '榄傛妧'),
        };
      } catch (error) {
        return null;
      }
    }

    function formatSkillDesignerChineseOrdinal(value, suffix = '') {
      const safeValue = Math.max(0, Math.floor(Number(value) || 0));
      if (!safeValue) return '';
      const digits = ['闆?, '涓€', '浜?, '涓?, '鍥?, '浜?, '鍏?, '涓?, '鍏?, '涔?];
      let numeral = '';
      if (safeValue < 10) {
        numeral = digits[safeValue];
      } else if (safeValue < 20) {
        numeral = safeValue === 10 ? '鍗? : `鍗?{digits[safeValue - 10]}`;
      } else if (safeValue < 100) {
        const tens = Math.floor(safeValue / 10);
        const ones = safeValue % 10;
        numeral = `${digits[tens]}鍗?{ones ? digits[ones] : ''}`;
      } else {
        numeral = String(safeValue);
      }
      return `绗?{numeral}${suffix}`;
    }

    function formatSkillDesignerSpiritSkillLabel(skillName = '', ringIndex = '') {
      const cleanName = toText(skillName, '').trim();
      const baseLabel = formatSkillDesignerChineseOrdinal(ringIndex, '榄傛妧');
      if (!cleanName) return baseLabel || '榄傛妧';
      if (/^绗??:\d+|[涓€浜屼笁鍥涗簲鍏竷鍏節鍗佺櫨]+)榄傛妧$/u.test(cleanName)) return baseLabel || cleanName;
      const suffixMatch = cleanName.match(/^绗??:\d+|[涓€浜屼笁鍥涗簲鍏竷鍏節鍗佺櫨]+)榄傛妧(.*)$/u);
      if (suffixMatch && suffixMatch[1]) return `${baseLabel || '榄傛妧'}${suffixMatch[1]}`;
      return cleanName;
    }

    function formatSkillDesignerWritebackLabel(previewMeta = {}) {
      const path = Array.isArray(previewMeta && previewMeta.path) ? previewMeta.path : [];
      const scope = toText(previewMeta && previewMeta.scope, '榄傛妧');
      const label = toText(previewMeta && previewMeta.label, '').trim();
      if (scope === '榄傛妧') {
        const ringMarkerIndex = path.findIndex(part => toText(part, '') === '榄傜幆');
        const ringIndex = ringMarkerIndex >= 0 ? path[ringMarkerIndex + 1] : '';
        const ringLabel = formatSkillDesignerChineseOrdinal(ringIndex, '榄傜幆');
        const skillLabel = formatSkillDesignerSpiritSkillLabel(label || path[path.length - 1], ringIndex);
        return [ringLabel, skillLabel].filter(Boolean).join(' / ') || '榄傜幆榄傛妧';
      }
      if (scope === '姝﹂瓊铻嶅悎鎶€') return `姝﹂瓊铻嶅悎鎶€ / ${label || '鏈懡鍚嶆妧鑳?}`;
      if (scope === 'art') return `鍔熸硶 / ${label || '鏈懡鍚嶆妧鑳?}`;
      if (scope === '鑷垱榄傛妧') return `鑷垱榄傛妧 / ${label || '鏈懡鍚嶆妧鑳?}`;
      if (scope === 'blood_passive') return `琛€鑴夎兘鍔?/ ${label || '鏈懡鍚嶆妧鑳?}`;
      return path.length ? path.slice(-4).join(' / ') : (label || '鏈瘑鍒矾寰?);
    }

    function resolveSkillDesignerTypeMeta(previewMeta = {}, fallbackType = '') {
      const scope = toText(previewMeta && previewMeta.scope, '榄傛妧');
      const category = toText(previewMeta && previewMeta.category, '鎶€鑳?);
      const normalizedFallback = normalizeSkillUiText(fallbackType, '');
      const path = Array.isArray(previewMeta && previewMeta.path) ? previewMeta.path : [];
      const rootData = (liveSnapshot && liveSnapshot.rootData) || readLatestMvuDataByEditor() || {};

      if (scope === '榄傛妧') {
        const spiritBasePath = path.length >= 4 ? path.slice(0, 4) : [];
        const spiritSlot = normalizeSkillUiText(spiritBasePath[3], '');
        const spiritType = normalizeSkillUiText(
          spiritBasePath.length ? deepGet(rootData, [...spiritBasePath, 'type'], '') : '',
          normalizedFallback || '鏈缃?
        );
        return {
          value: spiritType || '鏈缃?,
          display: spiritSlot && spiritType ? `${spiritSlot} / ${spiritType}` : (spiritType || spiritSlot || '鏈缃?)
        };
      }
      if (scope === '姝﹂瓊铻嶅悎鎶€') return { value: '姝﹂瓊铻嶅悎鎶€', display: '姝﹂瓊铻嶅悎鎶€' };
      if (scope === 'art') return { value: '鍔熸硶', display: '鍔熸硶' };
      if (scope === '鑷垱榄傛妧') return { value: '鑷垱榄傛妧', display: '鑷垱榄傛妧' };
      if (scope === 'blood_passive') return { value: '琚姩', display: '琚姩' };
      return {
        value: normalizedFallback || '鏈缃?,
        display: normalizedFallback || category || '鏈缃?
      };
    }

    function resolveSkillDesignerSourceCategory(previewMeta = {}) {
      const scope = toText(previewMeta && previewMeta.scope, '榄傛妧');
      if (scope === '姝﹂瓊铻嶅悎鎶€') return '姝﹂瓊铻嶅悎鎶€';
      if (scope === '鑷垱榄傛妧') return '鑷垱榄傛妧';
      if (scope === 'art') return '鍔熸硶';
      if (scope === 'blood_passive') return '琛€鑴夋妧鑳?;
      return '榄傛妧';
    }

    function getSkillDesignerScopeLabels(previewMeta = {}) {
      const scope = toText(previewMeta && previewMeta.scope, '榄傛妧');
      if (scope === 'art') {
        return {
          studioTitle: '鍔熸硶璁捐鍙?,
          anchorTitle: '鍔熸硶閿氱偣',
          parameterTitle: '鍔熸硶鍙傛暟',
          summaryTitle: '鍔熸硶閫熻',
          nameCardLabel: '鍔熸硶鍚嶇О',
          typeCardLabel: '鍔熸硶绫诲埆',
          nameFieldLabel: '鍔熸硶鍚?,
          targetLabel: '鐩爣妯″瀷',
          deliveryLabel: '杩愯浆鏂瑰紡',
          roleLabel: '鍔熸硶瀹氫綅',
          visualLabel: '杩愯浆琛ㄧ幇',
          effectLabel: '鍔熸硶鎻忚堪',
        };
      }
      if (scope === '鑷垱榄傛妧') {
        return {
          studioTitle: '鑷垱榄傛妧璁捐鍙?,
          anchorTitle: '鑷垱榄傛妧閿氱偣',
          parameterTitle: '鑷垱榄傛妧鍙傛暟',
          summaryTitle: '鑷垱榄傛妧閫熻',
          nameCardLabel: '鑷垱榄傛妧鍚嶇О',
          typeCardLabel: '鑷垱榄傛妧绫诲埆',
          nameFieldLabel: '鑷垱榄傛妧鍚?,
          targetLabel: '鐩爣妯″瀷',
          deliveryLabel: '鏂藉睍鏂瑰紡',
          roleLabel: '榄傛妧瀹氫綅',
          visualLabel: '琛ㄧ幇鎻忚堪',
          effectLabel: '榄傛妧鎻忚堪',
        };
      }
      if (scope === '姝﹂瓊铻嶅悎鎶€') {
        return {
          studioTitle: '姝﹂瓊铻嶅悎鎶€璁捐鍙?,
          anchorTitle: '姝﹂瓊铻嶅悎閿氱偣',
          parameterTitle: '姝﹂瓊铻嶅悎缁撴瀯',
          summaryTitle: '姝﹂瓊铻嶅悎閫熻',
          nameCardLabel: '姝﹂瓊铻嶅悎鎶€鍚嶇О',
          typeCardLabel: '鎶€鑳藉綊灞?,
          nameFieldLabel: '姝﹂瓊铻嶅悎鎶€鍚?,
          targetLabel: '鐩爣妯″瀷',
          deliveryLabel: '鏂芥斁褰㈠紡',
          roleLabel: '鎴樻枟瀹氫綅',
          visualLabel: '铻嶅悎琛ㄧ幇',
          effectLabel: '铻嶅悎鏁堟灉',
        };
      }
      return {
        studioTitle: '鎶€鑳借璁″彴',
        anchorTitle: '鎶€鑳介敋鐐?,
        parameterTitle: '璁捐鍙傛暟',
        summaryTitle: '璁捐閫熻',
        nameCardLabel: '鎶€鑳藉悕绉?,
        typeCardLabel: '鎶€鑳藉綊灞?,
        nameFieldLabel: '鎶€鑳藉悕',
        targetLabel: '鐩爣妯″瀷',
        deliveryLabel: '閲婃斁褰㈠紡',
        roleLabel: '鎴樻枟瀹氫綅',
        visualLabel: '鐢婚潰鎻忚堪',
        effectLabel: '鏁堟灉鎻忚堪',
      };
    }

    function getSkillDesignerDefaultTarget(previewMeta = {}, type = '') {
      const scope = toText(previewMeta && previewMeta.scope, 'skill');
      const normalizedType = normalizeSkillUiText(type, '');
      if (scope === 'art' || scope === 'blood_passive' || /琚姩/.test(normalizedType)) return '鑷韩';
      return '鏁屾柟鍗曚綋';
    }

    function normalizeSkillDesignerTargetForForm(target = '', fallback = '') {
      const text = normalizeSkillUiText(target, fallback);
      const aliasMap = {
        '宸辨柟/鍗曚綋': '鍙嬫柟鍗曚綋',
        '宸辨柟/缇や綋': '鍙嬫柟缇や綋',
        '鏁屾柟/鍗曚綋': '鏁屾柟鍗曚綋',
        '鏁屾柟/缇や綋': '鏁屾柟缇や綋',
      };
      return aliasMap[text] || text;
    }

    function deriveSkillDesignerTargetScaleFromTarget(target = '') {
      const normalizedTarget = normalizeSkillDesignerTargetForForm(target, '鏁屾柟鍗曚綋');
      if (normalizedTarget === '鑷韩') return '鑷韩';
      if (normalizedTarget === '鍏ㄥ満') return '鍏ㄥ満';
      if (/缇や綋/.test(normalizedTarget)) return '缇や綋';
      return '鍗曚綋';
    }

    function readSkillDesignerDraft(skill = {}, rawName = '') {
      const safeSkill = skill && typeof skill === 'object' ? skill : {};
      const effectArray = Array.isArray(safeSkill['_鏁堟灉鏁扮粍']) ? safeSkill['_鏁堟灉鏁扮粍'] : [];
      const designDraft = safeSkill['璁捐绋?] && typeof safeSkill['璁捐绋?] === 'object' ? safeSkill['璁捐绋?] : {};
      const systemBase = effectArray.find(effect => toText(effect && effect['鏈哄埗'], '').trim() === '绯荤粺鍩虹') || {};
      const resolvedType = normalizeSkillUiText(
        designDraft['鎶€鑳界被鍨?] || systemBase['鎶€鑳界被鍨?],
        '鏈煡',
      );
      const resolvedTarget = normalizeSkillDesignerTargetForForm(
        designDraft['鐩爣妯″瀷'] || designDraft['瀵硅薄'] || systemBase['鐩爣妯″瀷'] || systemBase['瀵硅薄'],
        '鏁屾柟鍗曚綋',
      );
      const explicitSecondaryMechanics = normalizeSkillDesignerArray(designDraft['闄勫姞鏈哄埗']);
      const inferredMechanics = inferSkillDesignerMechanicsFromEffectArray(effectArray, resolvedType, resolvedTarget);
      const resolvedPrimaryMain = normalizeSkillUiText(designDraft['涓绘満鍒?] || inferredMechanics.primaryMain, '');
      const resolvedPrimarySub = normalizeSkillUiText(designDraft['缁嗗垎鏈哄埗'] || inferredMechanics.primarySub, '');
      const resolvedSecondaryMechanics = explicitSecondaryMechanics.length
        ? explicitSecondaryMechanics
        : inferredMechanics.secondaryMechanics;
      const inferredDeliveryForm = inferSkillDesignerDeliveryForm(effectArray, resolvedType, resolvedTarget);
      const inferredMainRole = inferSkillDesignerMainRole(
        resolvedType,
        resolvedTarget,
        resolvedPrimaryMain,
        resolvedPrimarySub,
      );
      const inferredMechanicParams = buildSkillDesignerInferredMechanicParams(effectArray, {
        primaryMain: resolvedPrimaryMain,
        primarySub: resolvedPrimarySub,
        secondaryMechanics: resolvedSecondaryMechanics,
      });
      const mergedMechanicParams = mergeSkillDesignerMechanicParamSources(
        inferredMechanicParams,
        undefined,
        designDraft['鏈哄埗鍙傛暟'],
      );
      const resolvedSideEffects = normalizeSkillDesignerSideEffectList(
        designDraft['鍓綔鐢ㄥ垪琛?] || systemBase['鍓綔鐢ㄥ垪琛?] || [],
      );
      const costConfig = parseSkillDesignerCostConfig(
        designDraft['娑堣€?] || systemBase['娑堣€?],
        designDraft['娑堣€楄祫婧?],
        designDraft['娑堣€楁暟鍊?],
      );
      return {
        name: cleanSkillDisplayName(safeSkill, rawName),
        type: resolvedType,
        target: resolvedTarget,
        cost: formatSkillDesignerCostText(costConfig.resourceType, costConfig.resourceValue),
        resourceType: costConfig.resourceType,
        resourceValue: costConfig.resourceValue,
        mainRole: normalizeSkillUiText(inferredMainRole, '鏈煡'),
        primaryMain: resolvedPrimaryMain,
        primarySub: resolvedPrimarySub,
        deliveryForm: normalizeSkillUiText(designDraft['閲婃斁褰㈠紡'] || inferredDeliveryForm, ''),
        secondaryMechanics: resolvedSecondaryMechanics,
        attachedAttributes: normalizeSkillDesignerArray(
          designDraft['闄勫甫灞炴€?]
          || safeSkill['闄勫甫灞炴€?]
        ),
        artStage: normalizeSkillUiText(safeSkill['澧冪晫'] || designDraft['澧冪晫'], '鏈叆闂?),
        artLevel: Math.max(0, toNumber(safeSkill['lv'] ?? designDraft['lv'], 0)),
        artExp: Math.max(0, toNumber(safeSkill['exp'] ?? designDraft['exp'], 0)),
        fusionMode: (designDraft['铻嶅悎妯″紡'] || safeSkill['铻嶅悎妯″紡']) ? normalizeSkillDesignerFusionMode(designDraft['铻嶅悎妯″紡'] || safeSkill['铻嶅悎妯″紡']) : '',
        fusionPartner: normalizeSkillUiText(designDraft['铻嶅悎瀵硅薄'] || safeSkill['铻嶅悎瀵硅薄'], ''),
        fusionSourceSpirits: normalizeSkillDesignerArray(designDraft['鏉ユ簮姝﹂瓊'] || safeSkill['鏉ユ簮姝﹂瓊']),
        fusionParticipants: getSkillDesignerRawFusionParticipants(designDraft, safeSkill),
        mechanicParams: normalizeSkillDesignerMechanicParamMap(
          mergedMechanicParams,
          {
            primaryMain: resolvedPrimaryMain,
            primarySub: resolvedPrimarySub,
            secondaryMechanics: resolvedSecondaryMechanics,
          },
        ),
        tags: inferSkillDesignerTags(effectArray, safeSkill, designDraft),
        visualDesc: normalizeSkillUiText(safeSkill['鐢婚潰鎻忚堪'], '鏈煡'),
        effectDesc: normalizeSkillUiText(safeSkill['鏁堟灉鎻忚堪'] || safeSkill['鎻忚堪'], '鏈煡'),
        summaryText: normalizeSkillUiText(safeSkill['鐗规晥閲忓寲鍙傛暟'] || designDraft['璁捐鎽樿'], ''),
        鍓綔鐢ㄥ垪琛? resolvedSideEffects,
      };
    }

    function buildSkillDesignerMechanicSummary(draft = {}) {
      const parts = [];
      const primaryMain = normalizeSkillUiText(draft.primaryMain, '');
      const primarySub = getSkillDesignerEffectivePrimaryLabel(draft);
      const deliveryForm = normalizeSkillUiText(draft.deliveryForm, '');
      const secondaryMechanics = normalizeSkillDesignerSecondarySelection(primaryMain, draft.secondaryMechanics, draft.type);
      if (primaryMain) parts.push(primaryMain);
      if (primarySub && primarySub !== primaryMain) parts.push(primarySub);
      if (deliveryForm) parts.push(deliveryForm);
      let summary = parts.join(' / ');
      if (secondaryMechanics.length) {
        summary += `${summary ? ' 锝?' : ''}闄勫姞锛?{secondaryMechanics.join('銆?)}`;
      }
      return summary;
    }

    function buildSkillDesignerConstructSummary(draft = {}) {
      const deliveryForm = normalizeSkillUiText(draft && draft.deliveryForm, '');
      if (deliveryForm !== '閫犵墿鎵胯浇') return '';
      const type = normalizeSkillUiText(draft && draft.type, '');
      const isFood = type === '椋熺墿绯?;
      const itemType = isFood ? '椋熺墿' : '榄傛妧閫犵墿';
      const triggerMode = isFood ? '椋熺敤' : '浣跨敤';
      const effectTarget = isFood
        ? '椋熺敤鑰?
        : normalizeSkillUiText(draft && draft.target, '') || '浣跨敤鑰?;
      const itemName = normalizeSkillUiText(draft && draft.name, '鏈懡鍚嶆妧鑳?);
      return `閫犵墿锛?{itemType} / ${itemName}锛涜Е鍙戯細${triggerMode}锛涚敓鏁堝璞★細${effectTarget}`;
    }

    function summarizeSkillDesignerMechanismGrantContext(draft = {}) {
      const labels = collectSkillDesignerMechanicLabels(draft);
      if (!labels.length) return { grantTarget: '', triggerOwner: '' };
      const grantableLabels = labels.filter(label => isSkillDesignerGrantableMechanism(label));
      const selfOnlyLabels = labels.filter(label => isSkillDesignerSelfOnlyMechanism(label));
      if (grantableLabels.length > 0) {
        const grantTarget = resolveSkillDesignerMechanismTargetForRuntime(draft && draft.target, grantableLabels[0]);
        return {
          grantTarget,
          triggerOwner: /鍙嬫柟|椋熺敤鑰厊浣跨敤鑰?.test(grantTarget) ? '琚祴浜堝璞? : grantTarget === '鑷韩' ? '鏂芥湳鑰呰嚜韬? : '琚祴浜堝璞?,
        };
      }
      if (selfOnlyLabels.length > 0) {
        return {
          grantTarget: '鑷韩',
          triggerOwner: '鏂芥湳鑰呰嚜韬?,
        };
      }
      const hostileLabels = labels.filter(label => {
        const meta = getSkillDesignerMechanismRegistryMeta(label);
        return meta && normalizeSkillUiText(meta.鐩爣璇箟, '') === '鏁屽';
      });
      if (hostileLabels.length > 0) {
        const hostileTarget = resolveSkillDesignerMechanismTargetForRuntime(draft && draft.target, hostileLabels[0]);
        return {
          grantTarget: hostileTarget,
          triggerOwner: /鏁屾柟/.test(hostileTarget) ? '鍙楁湳鐩爣' : '鐩爣瀵硅薄',
        };
      }
      return { grantTarget: '', triggerOwner: '涓婁笅鏂囧喅瀹? };
    }

    function buildSkillDesignerExecutionSummary(draft = {}) {
      const parts = [];
      const target = normalizeSkillUiText(draft.target, '');
      const cost = formatSkillDesignerCostText(draft.costType, draft.costValue);
      const mechanismContext = summarizeSkillDesignerMechanismGrantContext(draft);
      const constructSummary = buildSkillDesignerConstructSummary(draft);
      if (target) parts.push(`瀵硅薄锛?{target}`);
      if (cost && cost !== '鏃?) parts.push(`娑堣€楋細${cost}`);
      if (constructSummary) parts.push(constructSummary);
      if (mechanismContext.grantTarget) parts.push(`璧嬩簣瀵硅薄锛?{mechanismContext.grantTarget}`);
      if (mechanismContext.triggerOwner) parts.push(`瑙﹀彂褰掑睘锛?{mechanismContext.triggerOwner}`);
      const sideEffectSummary = buildSkillDesignerSideEffectSummary(draft);
      if (sideEffectSummary) parts.push(`鍓綔鐢細${sideEffectSummary}`);
      return parts.join('锛?);
    }

    function buildSkillDesignerArtProgressSummary(draft = {}) {
      const type = normalizeSkillUiText(draft.type, '');
      if (type !== '鍔熸硶') return '';
      const stage = normalizeSkillUiText(draft.artStage, '鏈叆闂?);
      const level = Math.max(0, toNumber(draft.artLevel, 0));
      const exp = Math.max(0, toNumber(draft.artExp, 0));
      return `澧冪晫锛?{stage}锛涚瓑绾э細${level}锛涚啛缁冨害锛?{exp}`;
    }

    function buildSkillDesignerAttributeSummary(draft = {}) {
      const attachedAttributes = normalizeSkillDesignerArray(draft.attachedAttributes);
      return attachedAttributes.length ? `闄勫甫灞炴€э細${attachedAttributes.join('/')}` : '';
    }

    function buildSkillDesignerCompactSummary(draft = {}) {
      return [
        buildSkillDesignerFusionSummary(draft),
        buildSkillDesignerMechanicSummary(draft),
        buildSkillDesignerMechanicParamSummary(draft),
        buildSkillDesignerExecutionSummary(draft),
        buildSkillDesignerArtProgressSummary(draft),
        buildSkillDesignerAttributeSummary(draft),
      ]
        .filter(Boolean)
        .join(' 锝?');
    }

    function getSkillDesignerChildMechanicOptions(primaryMain = '') {
      return SKILL_DESIGNER_MAIN_MECHANIC_POOL[normalizeSkillUiText(primaryMain, '')] || [];
    }

    function getSkillDesignerDeliveryOptions(type = '') {
      return SKILL_DESIGNER_DELIVERY_FORM_BY_TYPE[normalizeSkillUiText(type, '')] || SKILL_DESIGNER_DELIVERY_FORM_POOL;
    }

    function getSkillDesignerSecondaryOptions(primaryMain = '') {
      return SKILL_DESIGNER_SECONDARY_BY_MAIN[normalizeSkillUiText(primaryMain, '')] || [];
    }

    function getSkillDesignerRecommendedSecondaryOptions(primaryMain = '', type = '') {
      return normalizeSkillDesignerArray([
        ...getSkillDesignerSecondaryOptions(primaryMain),
        ...(SKILL_DESIGNER_SECONDARY_TYPE_BIAS[normalizeSkillUiText(type, '')] || []),
      ]);
    }

    function isSkillDesignerRecommendedSecondaryOption(label = '', primaryMain = '', type = '') {
      const normalizedLabel = normalizeSkillUiText(label, '');
      if (!normalizedLabel) return false;
      return getSkillDesignerRecommendedSecondaryOptions(primaryMain, type).includes(normalizedLabel);
    }

    function normalizeSkillDesignerSecondarySelection(primaryMain = '', values = [], type = '') {
      const knownSet = new Set([
        ...SKILL_DESIGNER_ALL_SECONDARY_OPTIONS,
        ...getSkillDesignerRecommendedSecondaryOptions(primaryMain, type),
      ]);
      return normalizeSkillDesignerArray(values).filter(label => {
        if (!knownSet.has(label)) return false;
        const meta = getSkillDesignerMechanismRegistryMeta(label);
        return !meta || meta.鍙壇鏈哄埗 === true || normalizeSkillUiText(meta.designerSecondaryHint, '') === label;
      });
    }

    function getSkillDesignerSecondaryOptionList(primaryMain = '', extraValues = [], type = '') {
      const selected = normalizeSkillDesignerSecondarySelection(primaryMain, extraValues, type);
      const recommendedSet = new Set(getSkillDesignerRecommendedSecondaryOptions(primaryMain, type));
      const selectedSet = new Set(selected);
      return [...SKILL_DESIGNER_ALL_SECONDARY_OPTIONS]
        .sort((left, right) => {
          const leftSelected = selectedSet.has(left) ? 1 : 0;
          const rightSelected = selectedSet.has(right) ? 1 : 0;
          if (leftSelected !== rightSelected) return rightSelected - leftSelected;
          const leftRecommended = recommendedSet.has(left) ? 1 : 0;
          const rightRecommended = recommendedSet.has(right) ? 1 : 0;
          if (leftRecommended !== rightRecommended) return rightRecommended - leftRecommended;
          return left.localeCompare(right, 'zh-Hans-CN');
        });
    }

    function buildSkillDesignerSelectOptions(options = [], selected = '', blankLabel = '鏈缃?) {
      const rawOptions = Array.isArray(options) ? options : [];
      const normalizedOptions = rawOptions.map(option => {
        if (option && typeof option === 'object') {
          const value = toText(option.value ?? option.label, '').trim();
          const label = toText(option.label ?? option.value, value).trim();
          return value ? { value, label: label || value } : null;
        }
        const value = toText(option, '').trim();
        return value ? { value, label: value } : null;
      }).filter(Boolean);
      const optionList = [];
      const seen = new Set();
      if (blankLabel) optionList.push({ value: '', label: blankLabel });
      normalizedOptions.forEach(option => {
        if (seen.has(option.value)) return;
        seen.add(option.value);
        optionList.push(option);
      });
      const selectedText = toText(selected, '');
      return optionList.map(option => {
        const safeValue = toText(option.value, '');
        const label = toText(option.label, safeValue);
        return `<option value=\"${escapeHtmlAttr(safeValue)}\"${safeValue === selectedText ? ' selected' : ''}>${htmlEscape(label)}</option>`;
      }).join('');
    }

    function buildSkillDesignerCheckChipList(options = [], selectedValues = [], inputName = '', extraClassResolver = null) {
      const selectedSet = new Set(normalizeSkillDesignerArray(selectedValues));
      return (Array.isArray(options) ? options : []).map(option => {
        const label = toText(option, '').trim();
        if (!label) return '';
        const extraClass = typeof extraClassResolver === 'function' ? toText(extraClassResolver(label), '') : '';
        return `
          <label class=\"skill-designer-check-chip ${selectedSet.has(label) ? 'active' : ''} ${extraClass}\">
            <input type=\"checkbox\" name=\"${escapeHtmlAttr(inputName)}\" value=\"${escapeHtmlAttr(label)}\"${selectedSet.has(label) ? ' checked' : ''} />
            <span>${htmlEscape(label)}</span>
          </label>
        `;
      }).join('');
    }

    function normalizeSkillDesignerFusionMode(value = '') {
      const raw = normalizeSkillUiText(value, '').trim();
      if (!raw) return 'partner';
      if (raw === 'self' || /鑷綋|鑷韩|鑷繁|鍙岀敓|鍙屾榄?.test(raw)) return 'self';
      return 'partner';
    }

    function formatSkillDesignerFusionModeLabel(mode = '', participants = []) {
      if (normalizeSkillDesignerFusionMode(mode) === 'self') return '鑷綋铻嶅悎';
      return normalizeSkillDesignerFusionParticipantList(participants).length > 2 ? '澶氫汉铻嶅悎' : '鍙屼汉铻嶅悎';
    }

    function resolveSkillDesignerSpiritDisplayName(slotName = '', spiritData = {}) {
      const safeSpirit = spiritData && typeof spiritData === 'object' ? spiritData : {};
      return normalizeSkillUiText(
        safeSpirit['琛ㄨ薄鍚嶇О'] || safeSpirit.name || safeSpirit.type || slotName,
        slotName || '姝﹂瓊',
      );
    }

    function getSkillDesignerSpiritNameOptions(charData = {}) {
      const names = safeEntries(deepGet(charData, '姝﹂瓊', {}))
        .map(([slotName, spiritData]) => resolveSkillDesignerSpiritDisplayName(slotName, spiritData))
        .map(name => normalizeSkillUiText(name, '').trim())
        .filter(name => name && !/^(鏈缃畖鏈煡|鏃?$/.test(name));
      return Array.from(new Set(names));
    }

    function getSkillDesignerCharDisplayName(charKey = '', charData = {}) {
      return normalizeSkillUiText(
        charData && (charData.name || deepGet(charData, 'base.name', '')),
        charKey || '鏈懡鍚嶈鑹?,
      );
    }

    function getSkillDesignerCharByPreviewPath(rootData = {}, previewMeta = {}) {
      const path = Array.isArray(previewMeta && previewMeta.path) ? previewMeta.path : [];
      const charKey = path[0] === 'char' ? toText(path[1], '').trim() : '';
      return {
        charKey,
        charData: charKey ? deepGet(rootData, ['char', charKey], {}) : {},
      };
    }

    function getSkillDesignerFusionRecord(rootData = {}, previewMeta = {}) {
      const path = Array.isArray(previewMeta && previewMeta.path) ? previewMeta.path : [];
      if (toText(previewMeta && previewMeta.scope, '') !== '姝﹂瓊铻嶅悎鎶€') return {};
      if (toText(path[path.length - 1], '') !== '鎶€鑳芥暟鎹?) return {};
      const record = deepGet(rootData, path.slice(0, -1), {});
      return record && typeof record === 'object' ? record : {};
    }

    function findSkillDesignerCharKeyByName(rootData = {}, rawName = '') {
      const target = normalizeSkillUiText(rawName, '').trim();
      if (!target) return '';
      const charRoot = deepGet(rootData, 'char', {});
      if (charRoot && Object.prototype.hasOwnProperty.call(charRoot, target)) return target;
      const match = safeEntries(charRoot).find(([key, charData]) =>
        getSkillDesignerCharDisplayName(key, charData) === target
      );
      return match ? match[0] : '';
    }

    function getSkillDesignerFusionSelfEntry(rootData = {}, previewMeta = {}, snapshot = {}) {
      const { charKey, charData } = getSkillDesignerCharByPreviewPath(rootData, previewMeta);
      const fallbackName = normalizeSkillUiText(snapshot && snapshot.activeName, charKey || '褰撳墠瑙掕壊');
      return {
        charKey,
        charName: getSkillDesignerCharDisplayName(charKey, charData) || fallbackName,
        charData,
        spirits: getSkillDesignerSpiritNameOptions(charData),
      };
    }

    function getSkillDesignerFusionCharOptions(rootData = {}, previewMeta = {}, extraParticipants = []) {
      const selfEntry = getSkillDesignerFusionSelfEntry(rootData, previewMeta);
      const charOptions = safeEntries(deepGet(rootData, 'char', {}))
        .map(([key, charData]) => ({
          value: key,
          label: getSkillDesignerCharDisplayName(key, charData),
        }))
        .filter(option => option.value && option.label && option.value !== selfEntry.charKey);
      normalizeSkillDesignerFusionParticipantList(extraParticipants).forEach(participant => {
        if (
          participant.charKey
          && participant.charName
          && participant.charKey !== selfEntry.charKey
          && !charOptions.some(option => option.value === participant.charKey)
        ) {
          charOptions.unshift({ value: participant.charKey, label: participant.charName });
        }
      });
      return charOptions;
    }

    function getSkillDesignerFusionCharData(rootData = {}, charKey = '') {
      return charKey ? deepGet(rootData, ['char', charKey], {}) : {};
    }

    function getSkillDesignerFusionSpiritOptions(rootData = {}, charKey = '', fallbackSpirit = '') {
      const options = getSkillDesignerSpiritNameOptions(getSkillDesignerFusionCharData(rootData, charKey));
      const fallback = normalizeSkillUiText(fallbackSpirit, '');
      if (fallback && !options.includes(fallback)) options.unshift(fallback);
      return options;
    }

    function resolveSkillDesignerFusionSpirit(rootData = {}, charKey = '', currentSpirit = '') {
      const spiritOptions = getSkillDesignerSpiritNameOptions(getSkillDesignerFusionCharData(rootData, charKey));
      const current = normalizeSkillUiText(currentSpirit, '');
      if (current && spiritOptions.includes(current)) return current;
      return spiritOptions[0] || current;
    }

    function normalizeSkillDesignerFusionParticipant(participant = {}, rootData = {}) {
      const raw = participant && typeof participant === 'object' ? participant : {};
      const roleText = normalizeSkillUiText(raw.role || raw['绫诲瀷'] || raw['韬唤'], '');
      const role = roleText === 'self' || /鑷韩|鑷綋|鑷繁|鏈綋/.test(roleText) ? 'self' : 'partner';
      const rawCharName = normalizeSkillUiText(raw.charName || raw.char_name || raw.name || raw['瑙掕壊'] || raw['瑙掕壊鍚?], '');
      let charKey = normalizeSkillUiText(raw.charKey || raw.char_key || raw.key || raw['瑙掕壊閿?] || raw['瑙掕壊key'], '');
      if (!charKey && rawCharName) charKey = findSkillDesignerCharKeyByName(rootData, rawCharName);
      const charData = getSkillDesignerFusionCharData(rootData, charKey);
      const charName = getSkillDesignerCharDisplayName(charKey, charData) || rawCharName || charKey;
      const spirit = normalizeSkillUiText(raw.spirit || raw.spiritName || raw.spirit_name || raw.sourceSpirit || raw.source_spirit || raw['姝﹂瓊'] || raw['鏉ユ簮姝﹂瓊'], '');
      return {
        role,
        charKey,
        charName,
        spirit,
      };
    }

    function normalizeSkillDesignerFusionParticipantList(participants = [], rootData = {}) {
      if (!Array.isArray(participants)) return [];
      return participants
        .map(participant => normalizeSkillDesignerFusionParticipant(participant, rootData))
        .filter(participant => participant.charKey || participant.charName || participant.spirit);
    }

    function getSkillDesignerRawFusionParticipants(draft = {}, fusionRecord = {}) {
      const safeDraft = draft && typeof draft === 'object' ? draft : {};
      const safeRecord = fusionRecord && typeof fusionRecord === 'object' ? fusionRecord : {};
      const raw = [
        safeDraft.fusionParticipants,
        safeDraft['铻嶅悎鍙備笌鑰?],
        safeRecord['铻嶅悎鍙備笌鑰?],
      ].find(candidate => Array.isArray(candidate) && candidate.length);
      return Array.isArray(raw) ? raw : [];
    }

    function parseSkillDesignerFusionPartnerNames(value = '') {
      const raw = normalizeSkillUiText(value, '');
      if (!raw || raw === SKILL_DESIGNER_SELF_FUSION_PARTNER) return [];
      return raw
        .split(/[銆?锛?\/|锝滐紱;]/)
        .map(item => normalizeSkillUiText(item, '').trim())
        .filter(Boolean);
    }

    function createSkillDesignerFusionParticipant(role = 'partner', charKey = '', charName = '', spirit = '') {
      return {
        role: role === 'self' ? 'self' : 'partner',
        charKey: normalizeSkillUiText(charKey, ''),
        charName: normalizeSkillUiText(charName, ''),
        spirit: normalizeSkillUiText(spirit, ''),
      };
    }

    function createSkillDesignerDefaultPartnerParticipant(rootData = {}, previewMeta = {}, usedCharKeys = []) {
      const used = new Set(normalizeSkillDesignerArray(usedCharKeys));
      const options = getSkillDesignerFusionCharOptions(rootData, previewMeta);
      const option = options.find(item => !used.has(item.value)) || options[0] || { value: '', label: '' };
      const spiritOptions = getSkillDesignerFusionSpiritOptions(rootData, option.value);
      return createSkillDesignerFusionParticipant('partner', option.value, option.label, spiritOptions[0] || '');
    }

    function buildSkillDesignerFusionParticipants(snapshot = {}, previewMeta = {}, baseDraft = {}, fusionRecord = {}) {
      if (toText(previewMeta && previewMeta.scope, '') !== '姝﹂瓊铻嶅悎鎶€') return [];
      const rootData = snapshot && snapshot.rootData ? snapshot.rootData : {};
      const selfEntry = getSkillDesignerFusionSelfEntry(rootData, previewMeta, snapshot);
      const selfSpiritOptions = selfEntry.spirits;
      let fusionMode = normalizeSkillDesignerFusionMode(
        baseDraft.fusionMode
        || baseDraft['铻嶅悎妯″紡']
        || fusionRecord.铻嶅悎妯″紡
        || '',
      );
      if (fusionMode === 'self' && selfSpiritOptions.length < 2) fusionMode = 'partner';
      const rawParticipants = getSkillDesignerRawFusionParticipants(baseDraft, fusionRecord);
      let participants = normalizeSkillDesignerFusionParticipantList(rawParticipants, rootData);
      if (!participants.length) {
        const sourceSpirits = normalizeSkillDesignerArray(
          baseDraft.fusionSourceSpirits
          || baseDraft['鏉ユ簮姝﹂瓊']
          || fusionRecord.鏉ユ簮姝﹂瓊
        );
        if (fusionMode === 'self') {
          participants = [
            createSkillDesignerFusionParticipant('self', selfEntry.charKey, selfEntry.charName, sourceSpirits[0] || selfSpiritOptions[0] || ''),
            createSkillDesignerFusionParticipant('self', selfEntry.charKey, selfEntry.charName, sourceSpirits[1] || selfSpiritOptions[1] || selfSpiritOptions[0] || ''),
          ];
        } else {
          const partnerNames = parseSkillDesignerFusionPartnerNames(
            baseDraft.fusionPartner
            || baseDraft['铻嶅悎瀵硅薄']
            || fusionRecord['铻嶅悎瀵硅薄']
            || '',
          );
          participants = [
            createSkillDesignerFusionParticipant('self', selfEntry.charKey, selfEntry.charName, sourceSpirits[0] || selfSpiritOptions[0] || ''),
          ];
          partnerNames.forEach((name, index) => {
            const charKey = findSkillDesignerCharKeyByName(rootData, name);
            const charName = charKey ? getSkillDesignerCharDisplayName(charKey, getSkillDesignerFusionCharData(rootData, charKey)) : name;
            const spiritOptions = getSkillDesignerFusionSpiritOptions(rootData, charKey);
            participants.push(createSkillDesignerFusionParticipant('partner', charKey, charName, sourceSpirits[index + 1] || spiritOptions[0] || ''));
          });
          if (participants.length < 2) {
            participants.push(createSkillDesignerDefaultPartnerParticipant(rootData, previewMeta, participants.map(item => item.charKey)));
          }
        }
      }
      if (fusionMode === 'self') {
        const selfRows = participants.filter(participant => participant.role === 'self');
        const firstSpirit = resolveSkillDesignerFusionSpirit(rootData, selfEntry.charKey, selfRows[0] && selfRows[0].spirit ? selfRows[0].spirit : (selfSpiritOptions[0] || ''));
        let secondSpirit = resolveSkillDesignerFusionSpirit(rootData, selfEntry.charKey, selfRows[1] && selfRows[1].spirit ? selfRows[1].spirit : (selfSpiritOptions.find(name => name !== firstSpirit) || selfSpiritOptions[1] || firstSpirit));
        if (selfSpiritOptions.length > 1 && secondSpirit === firstSpirit) {
          secondSpirit = selfSpiritOptions.find(name => name !== firstSpirit) || secondSpirit;
        }
        return [
          createSkillDesignerFusionParticipant('self', selfEntry.charKey, selfEntry.charName, firstSpirit),
          createSkillDesignerFusionParticipant('self', selfEntry.charKey, selfEntry.charName, secondSpirit),
        ];
      }
      const normalized = participants.map((participant, index) => {
        if (index === 0 || participant.role === 'self') {
          const spirit = resolveSkillDesignerFusionSpirit(rootData, selfEntry.charKey, participant.spirit);
          return createSkillDesignerFusionParticipant('self', selfEntry.charKey, selfEntry.charName, spirit);
        }
        const charKey = participant.charKey || findSkillDesignerCharKeyByName(rootData, participant.charName);
        const charName = getSkillDesignerCharDisplayName(charKey, getSkillDesignerFusionCharData(rootData, charKey)) || participant.charName;
        const spirit = resolveSkillDesignerFusionSpirit(rootData, charKey, participant.spirit);
        return createSkillDesignerFusionParticipant('partner', charKey, charName, spirit);
      });
      if (!normalized.some(participant => participant.role === 'partner')) {
        normalized.push(createSkillDesignerDefaultPartnerParticipant(rootData, previewMeta, normalized.map(item => item.charKey)));
      }
      return normalized;
    }

    function deriveSkillDesignerFusionFields(mode = '', participants = [], fallbackPartner = '') {
      const participantList = normalizeSkillDesignerFusionParticipantList(participants);
      const fusionMode = normalizeSkillDesignerFusionMode(mode);
      const partnerNames = participantList
        .filter(participant => participant.role !== 'self')
        .map(participant => participant.charName || participant.charKey)
        .filter(Boolean);
      return {
        fusionMode,
        fusionPartner: fusionMode === 'self'
          ? SKILL_DESIGNER_SELF_FUSION_PARTNER
          : (partnerNames.length ? Array.from(new Set(partnerNames)).join('銆?) : normalizeSkillUiText(fallbackPartner, '')),
        fusionSourceSpirits: participantList.map(participant => participant.spirit).filter(Boolean),
        fusionParticipants: participantList,
      };
    }

    function isSkillDesignerFusionBlankText(value = '') {
      const text = normalizeSkillUiText(value, '').trim();
      return !text || /^(鏈缃畖鏈煡|鏃爘鏈帴鍏ユ榄?$/.test(text);
    }

    function assertSkillDesignerFusionState(draft = {}) {
      const fields = deriveSkillDesignerFusionFields(draft.fusionMode, draft.fusionParticipants, draft.fusionPartner);
      const participants = fields.fusionParticipants;
      if (participants.length < 2) throw new Error('铻嶅悎鎶€鑷冲皯闇€瑕佷袱涓弬涓庢榄傘€?);
      const incomplete = participants.some(participant =>
        (isSkillDesignerFusionBlankText(participant.charKey) && isSkillDesignerFusionBlankText(participant.charName))
        || isSkillDesignerFusionBlankText(participant.spirit)
      );
      if (incomplete) throw new Error('铻嶅悎瀵硅薄闇€瑕佹寚瀹氳鑹蹭笌姝﹂瓊銆?);
      if (fields.fusionMode === 'self') {
        const spirits = participants.map(participant => participant.spirit).filter(spirit => !isSkillDesignerFusionBlankText(spirit));
        if (new Set(spirits).size < 2) throw new Error('鑷綋铻嶅悎闇€瑕侀€夋嫨涓や釜涓嶅悓姝﹂瓊銆?);
      }
      return fields;
    }

    function buildSkillDesignerFusionParticipantRows(participants = [], rootData = {}, previewMeta = {}) {
      const participantList = normalizeSkillDesignerFusionParticipantList(participants, rootData);
      const mode = participantList.filter(participant => participant.role === 'partner').length ? 'partner' : 'self';
      const partnerCount = participantList.filter(participant => participant.role === 'partner').length;
      return participantList.map((participant, index) => {
        const isSelf = participant.role === 'self';
        const charOptions = isSelf
          ? [{ value: participant.charKey, label: participant.charName || participant.charKey || '褰撳墠瑙掕壊' }]
          : getSkillDesignerFusionCharOptions(rootData, previewMeta, participantList);
        const spiritOptions = getSkillDesignerFusionSpiritOptions(rootData, participant.charKey, participant.spirit);
        const charLabel = participant.charName || participant.charKey || '鏈缃?;
        return `
          <div class=\"skill-designer-fusion-participant-row\" data-fusion-participant-row>
            <input type=\"hidden\" value=\"${escapeHtmlAttr(participant.role)}\" data-skill-designer-fusion-role />
            <label class=\"mvu-editor-field skill-designer-fusion-char-field\">
              <span class=\"mvu-editor-label\">瑙掕壊</span>
              ${isSelf
                ? `
                  <div class=\"mvu-editor-static skill-designer-fusion-self-char\">${htmlEscape(charLabel)}</div>
                  <input type=\"hidden\" value=\"${escapeHtmlAttr(participant.charKey)}\" data-skill-designer-fusion-char data-skill-designer-fusion-char-name=\"${escapeHtmlAttr(charLabel)}\" />
                `
                : `
                  <select class=\"mvu-editor-select\" data-skill-designer-fusion-char data-skill-designer-disableable>
                    ${buildSkillDesignerSelectOptions(charOptions, participant.charKey, '鏈缃?)}
                  </select>
                `}
            </label>
            <label class=\"mvu-editor-field skill-designer-fusion-spirit-field\">
              <span class=\"mvu-editor-label\">姝﹂瓊</span>
              <select class=\"mvu-editor-select\" data-skill-designer-fusion-spirit data-skill-designer-disableable>
                ${buildSkillDesignerSelectOptions(spiritOptions, participant.spirit, spiritOptions.length ? '鏈缃? : '鏈帴鍏ユ榄?)}
              </select>
            </label>
            <div class=\"skill-designer-fusion-row-actions\">
              ${!isSelf && mode === 'partner' && partnerCount > 1
                ? `<button type=\"button\" class=\"tag-chip\" data-skill-designer-remove-fusion-participant data-skill-designer-disableable>绉婚櫎</button>`
                : ''}
            </div>
          </div>
        `;
      }).join('');
    }

    function getSkillDesignerFusionDraftSeed(snapshot = {}, previewMeta = {}, baseDraft = {}) {
      if (toText(previewMeta && previewMeta.scope, '') !== '姝﹂瓊铻嶅悎鎶€') return {};
      const rootData = snapshot && snapshot.rootData ? snapshot.rootData : {};
      const fusionRecord = getSkillDesignerFusionRecord(rootData, previewMeta);
      const selfEntry = getSkillDesignerFusionSelfEntry(rootData, previewMeta, snapshot);
      const spiritNames = selfEntry.spirits;
      const rawMode = baseDraft.fusionMode
        || baseDraft['铻嶅悎妯″紡']
        || fusionRecord.铻嶅悎妯″紡
        || '';
      let fusionMode = normalizeSkillDesignerFusionMode(rawMode);
      if (fusionMode === 'self' && spiritNames.length < 2) fusionMode = 'partner';
      const rawPartner = baseDraft.fusionPartner
        || baseDraft['铻嶅悎瀵硅薄']
        || fusionRecord['铻嶅悎瀵硅薄']
        || '';
      const fusionPartner = fusionMode === 'self'
        ? SKILL_DESIGNER_SELF_FUSION_PARTNER
        : normalizeSkillUiText(rawPartner, '');
      let fusionSourceSpirits = normalizeSkillDesignerArray(
        baseDraft.fusionSourceSpirits
        || baseDraft['鏉ユ簮姝﹂瓊']
        || fusionRecord.鏉ユ簮姝﹂瓊
      );
      if (fusionMode === 'self' && !fusionSourceSpirits.length) fusionSourceSpirits = spiritNames.slice(0, 2);
      const fusionParticipants = buildSkillDesignerFusionParticipants(snapshot, previewMeta, baseDraft, fusionRecord);
      const derivedFields = deriveSkillDesignerFusionFields(fusionMode, fusionParticipants, fusionPartner);
      return {
        fusionMode: derivedFields.fusionMode,
        fusionPartner: derivedFields.fusionPartner,
        fusionSourceSpirits: derivedFields.fusionSourceSpirits.length ? derivedFields.fusionSourceSpirits : fusionSourceSpirits,
        fusionParticipants: derivedFields.fusionParticipants,
      };
    }

    function buildSkillDesignerFusionSummary(draft = {}) {
      if (!draft || !draft.fusionMode) return '';
      const derivedFields = deriveSkillDesignerFusionFields(draft.fusionMode, draft.fusionParticipants, draft.fusionPartner);
      const participantText = derivedFields.fusionParticipants
        .map(participant => {
          const charName = participant.charName || participant.charKey || '鏈缃?;
          const spirit = participant.spirit || '鏈缃?;
          return `${charName}:${spirit}`;
        })
        .join(' + ');
      const fallbackSourceText = normalizeSkillDesignerArray(draft.fusionSourceSpirits).join('/');
      return [formatSkillDesignerFusionModeLabel(derivedFields.fusionMode, derivedFields.fusionParticipants), participantText || derivedFields.fusionPartner || fallbackSourceText].filter(Boolean).join(' / ');
    }

    function createSkillDesignerTextParam(key, label, placeholder = '') {
      return Object.freeze({ key, label, type: 'text', placeholder });
    }

    function createSkillDesignerNumberParam(key, label, placeholder = '', step = '0.1') {
      return Object.freeze({ key, label, type: 'number', placeholder, step });
    }

    function createSkillDesignerSelectParam(key, label, options = [], placeholder = '鏈缃?) {
      return Object.freeze({ key, label, type: 'select', options, placeholder });
    }

    function parseSkillDesignerCostConfig(rawCost = '', explicitType = '', explicitValue = '') {
      const explicitResourceType = normalizeSkillUiText(explicitType, '');
      const explicitResourceValue = normalizeSkillUiText(explicitValue, '');
      if (explicitResourceType) {
        return {
          resourceType: SKILL_DESIGNER_RESOURCE_TYPE_OPTIONS.includes(explicitResourceType) ? explicitResourceType : '娣峰悎',
          resourceValue: explicitResourceValue,
        };
      }
      const costText = normalizeSkillUiText(rawCost, '');
      if (!costText || ['鏃?, '鏈煡'].includes(costText)) {
        return { resourceType: '鏃?, resourceValue: '' };
      }
      const directType = SKILL_DESIGNER_RESOURCE_TYPE_OPTIONS.find(option => option !== '鏃? && option !== '娣峰悎' && costText.startsWith(option));
      if (directType) {
        return {
          resourceType: directType,
          resourceValue: costText.slice(directType.length).replace(/^[锛?\s]+/, '').trim(),
        };
      }
      if (costText.startsWith('娣峰悎')) {
        return {
          resourceType: '娣峰悎',
          resourceValue: costText.slice(2).replace(/^[锛?\s]+/, '').trim(),
        };
      }
      return { resourceType: '娣峰悎', resourceValue: costText };
    }

    function formatSkillDesignerCostText(resourceType = '', resourceValue = '') {
      const safeType = normalizeSkillUiText(resourceType, '鏃?) || '鏃?;
      const safeValue = normalizeSkillUiText(resourceValue, '');
      if (safeType === '鏃?) return safeValue || '鏃?;
      if (!safeValue) return safeType;
      return `${safeType} ${safeValue}`;
    }

    function getSkillDesignerMechanicParamDefs(mechanicLabel = '') {
      const label = normalizeSkillUiText(mechanicLabel, '');
      const registryDefs = resolveSkillDesignerMechanismParamDefsFromRegistry(label);
      if (registryDefs.length) return registryDefs;
      switch (label) {
        case '浼ゅ绫?:
          return [
            createSkillDesignerNumberParam('powerRatio', '寮哄害鍊嶇巼', '1.25'),
            createSkillDesignerNumberParam('hitCount', '鍛戒腑娆℃暟', '1', '1'),
            createSkillDesignerTextParam('range', '浣滅敤鑼冨洿', '鍗曚綋 / 鍗婂緞3绫?),
          ];
        case '鎺у埗绫?:
          return [
            createSkillDesignerNumberParam('duration', '鎸佺画鍥炲悎', '2', '1'),
            createSkillDesignerNumberParam('controlPower', '鎺у埗寮哄害', '1.0'),
            createSkillDesignerTextParam('hitRule', '鍛戒腑鏉′欢', '鍛戒腑鍚?/ 鐮撮槻鍚?),
          ];
        case '鍓婂急绫?:
          return [
            createSkillDesignerNumberParam('duration', '鎸佺画鍥炲悎', '2', '1'),
            createSkillDesignerNumberParam('weakenRatio', '鍓婂急骞呭害', '0.2'),
            createSkillDesignerSelectParam('affectedPanel', '褰卞搷瀵硅薄', SKILL_DESIGNER_PARAM_PANEL_OPTIONS),
          ];
        case '澧炵泭绫?:
          return [
            createSkillDesignerNumberParam('duration', '鎸佺画鍥炲悎', '3', '1'),
            createSkillDesignerNumberParam('gainRatio', '澧炵泭骞呭害', '0.25'),
            createSkillDesignerSelectParam('gainTarget', '瑕嗙洊瀵硅薄', SKILL_DESIGNER_PARAM_SUPPORT_TARGET_OPTIONS),
          ];
        case '闃插尽绫?:
          return [
            createSkillDesignerNumberParam('duration', '鎸佺画鍥炲悎', '2', '1'),
            createSkillDesignerNumberParam('guardValue', '闃叉姢寮哄害', '1.0'),
            createSkillDesignerTextParam('triggerRule', '瑙﹀彂鏉′欢', '鍙楀嚮鏃?/ 甯搁┗'),
          ];
        case '鍥炲绫?:
          return [
            createSkillDesignerNumberParam('recoverRatio', '鍥炲鍊嶇巼', '0.3'),
            createSkillDesignerNumberParam('duration', '鎸佺画鍥炲悎', '2', '1'),
            createSkillDesignerNumberParam('repeatCount', '鐢熸晥娆℃暟', '1', '1'),
          ];
        case '鎰熺煡/璁ょ煡绫?:
          return [
            createSkillDesignerNumberParam('duration', '鎸佺画鍥炲悎', '2', '1'),
            createSkillDesignerTextParam('senseRange', '鎰熺煡鑼冨洿', '鍗婂緞20绫?),
            createSkillDesignerNumberParam('disturbPower', '骞叉壈寮哄害', '1.0'),
          ];
        case '浣嶇Щ绫?:
          return [
            createSkillDesignerTextParam('moveDistance', '浣嶇Щ璺濈', '5绫?),
            createSkillDesignerNumberParam('repeatCount', '瑙﹀彂娆℃暟', '1', '1'),
            createSkillDesignerTextParam('followWindow', '杩藉嚮绐楀彛', '1鍥炲悎鍐?),
          ];
        case '鐗规畩瑙勫垯绫?:
          return [
            createSkillDesignerTextParam('triggerRule', '瑙﹀彂鏉′欢', '琚嚮涓悗 / 缁撶畻鍓?),
            createSkillDesignerTextParam('ruleTarget', '鏀瑰啓瀵硅薄', '鍗曟鎶€鑳?/ 鐘舵€?),
            createSkillDesignerTextParam('maintainCost', '缁存寔浠ｄ环', '姣忓洖鍚堥澶栨秷鑰?),
          ];
        case '鍗曚綋浼ゅ':
          return [
            createSkillDesignerNumberParam('powerRatio', '濞佸姏鍊嶇巼', '1.4'),
            createSkillDesignerNumberParam('hitCount', '鍛戒腑娆℃暟', '1', '1'),
            createSkillDesignerTextParam('range', '浣滅敤鑼冨洿', '鍗曚綋 / 鐩寸嚎 / 鐐规潃'),
          ];
        case '缇や綋浼ゅ':
          return [
            createSkillDesignerNumberParam('powerRatio', '濞佸姏鍊嶇巼', '1.1'),
            createSkillDesignerTextParam('range', '浣滅敤鍗婂緞', '鍗婂緞5绫?),
            createSkillDesignerNumberParam('hitCount', '鍛戒腑娆℃暟', '1', '1'),
          ];
        case '澶氭浼ゅ':
          return [
            createSkillDesignerNumberParam('segmentCount', '娈垫暟', '3', '1'),
            createSkillDesignerNumberParam('segmentRatio', '鍗曟鍊嶇巼', '0.45'),
            createSkillDesignerTextParam('segmentInterval', '娈甸棿闂撮殧', '0.2绉?),
          ];
        case '寤惰繜鐖嗗彂':
          return [
            createSkillDesignerTextParam('delayWindow', '寤惰繜鏃堕暱', '1鍥炲悎 / 3绉?),
            createSkillDesignerNumberParam('burstRatio', '鐖嗗彂鍊嶇巼', '1.8'),
            createSkillDesignerTextParam('triggerRule', '瑙﹀彂鏉′欢', '璁℃椂缁撴潫 / 鍐嶆寮曠垎'),
          ];
        case '鎸佺画浼ゅ':
          return [
            createSkillDesignerNumberParam('dotRatio', '姣忚烦鍊嶇巼', '0.35'),
            createSkillDesignerNumberParam('duration', '鎸佺画鍥炲悎', '3', '1'),
            createSkillDesignerNumberParam('stackLimit', '鍙犲眰涓婇檺', '3', '1'),
          ];
        case '纭帶':
          return [
            createSkillDesignerNumberParam('duration', '鎺у埗鍥炲悎', '2', '1'),
            createSkillDesignerNumberParam('controlPower', '鎺у埗寮哄害', '1.2'),
            createSkillDesignerTextParam('breakRule', '瑙ｉ櫎鏉′欢', '鍙椾激 / 鍑€鍖?),
          ];
        case '杞帶':
          return [
            createSkillDesignerNumberParam('duration', '鎺у埗鍥炲悎', '2', '1'),
            createSkillDesignerNumberParam('slowRatio', '鎺у埗骞呭害', '0.3'),
            createSkillDesignerTextParam('hitRule', '鍛戒腑鏉′欢', '鎺ヨЕ鍚?/ 瑙嗙嚎閿佸畾'),
          ];
        case '浣嶇Щ闄愬埗':
          return [
            createSkillDesignerTextParam('lockRange', '灏侀攣鑼冨洿', '鍗婂緞4绫?),
            createSkillDesignerNumberParam('duration', '鎸佺画鍥炲悎', '2', '1'),
            createSkillDesignerNumberParam('limitPower', '闄愬埗寮哄害', '1.0'),
          ];
        case '鑺傚鎵撴柇':
          return [
            createSkillDesignerNumberParam('interruptCount', '鍙墦鏂鏁?, '1', '1'),
            createSkillDesignerTextParam('interruptWindow', '鎵撴柇鏃舵満', '鍓嶆憞 / 鍚熷敱涓?),
            createSkillDesignerNumberParam('extraDelay', '杩藉姞鍍电洿', '0.5'),
          ];
        case '鍗曞睘鎬у墛寮?:
          return [
            createSkillDesignerSelectParam('debuffAttr', '鍘嬪埗瀵硅薄', SKILL_DESIGNER_PARAM_ATTRIBUTE_OPTIONS),
            createSkillDesignerNumberParam('reduceRatio', '鍘嬪埗鍊嶇巼', '0.8'),
            createSkillDesignerNumberParam('duration', '鎸佺画鍥炲悎', '2', '1'),
          ];
        case '澶氬睘鎬у墛寮?:
          return [
            createSkillDesignerSelectParam('debuffAttrGroup', '灞炴€х粍', SKILL_DESIGNER_PARAM_ATTRIBUTE_GROUP_OPTIONS),
            createSkillDesignerNumberParam('reduceRatio', '缁熶竴鍊嶇巼', '0.85'),
            createSkillDesignerNumberParam('duration', '鎸佺画鍥炲悎', '2', '1'),
          ];
        case '娑堣€楁彁楂?:
          return [
            createSkillDesignerNumberParam('gainRatio', '鎻愰珮鍊嶇巼', '1.2'),
            createSkillDesignerNumberParam('duration', '鎸佺画鍥炲悎', '2', '1'),
          ];
        case '鍓嶆憞鎷夐暱':
          return [
            createSkillDesignerNumberParam('gainRatio', '鎷夐暱鍊嶇巼', '1.2'),
            createSkillDesignerNumberParam('duration', '鎸佺画鍥炲悎', '2', '1'),
          ];
        case '鎺屾帶鍘嬪埗':
          return [
            createSkillDesignerNumberParam('gainRatio', '鍘嬪埗鍊嶇巼', '0.8'),
            createSkillDesignerNumberParam('duration', '鎸佺画鍥炲悎', '2', '1'),
          ];
        case '閫熷害鍘嬪埗':
          return [
            createSkillDesignerNumberParam('gainRatio', '鍘嬪埗鍊嶇巼', '0.85'),
            createSkillDesignerNumberParam('duration', '鎸佺画鍥炲悎', '2', '1'),
          ];
        case '鍗曞睘鎬у鐩?:
          return [
            createSkillDesignerSelectParam('buffAttr', '澧炲箙瀵硅薄', SKILL_DESIGNER_PARAM_ATTRIBUTE_OPTIONS),
            createSkillDesignerNumberParam('gainRatio', '澧炲箙鍊嶇巼', '0.3'),
            createSkillDesignerNumberParam('duration', '鎸佺画鍥炲悎', '3', '1'),
          ];
        case '澶氬睘鎬у鐩?:
          return [
            createSkillDesignerSelectParam('buffAttrGroup', '灞炴€х粍', SKILL_DESIGNER_PARAM_ATTRIBUTE_GROUP_OPTIONS),
            createSkillDesignerNumberParam('gainRatio', '澧炲箙鍊嶇巼', '0.2'),
            createSkillDesignerNumberParam('duration', '鎸佺画鍥炲悎', '3', '1'),
          ];
        case '鍏ㄥ睘鎬у鐩?:
          return [
            createSkillDesignerNumberParam('allGainRatio', '鍏ㄥ睘鎬у€嶇巼', '0.15'),
            createSkillDesignerNumberParam('duration', '鎸佺画鍥炲悎', '2', '1'),
          ];
        case '娑堣€楅檷浣?:
          return [
            createSkillDesignerNumberParam('gainRatio', '闄嶄綆鍊嶇巼', '0.85'),
            createSkillDesignerNumberParam('duration', '鎸佺画鍥炲悎', '3', '1'),
          ];
        case '鍓嶆憞缂╃煭':
          return [
            createSkillDesignerNumberParam('gainRatio', '缂╃煭鍊嶇巼', '0.85'),
            createSkillDesignerNumberParam('duration', '鎸佺画鍥炲悎', '2', '1'),
          ];
        case '鎺屾帶鎻愬崌':
          return [
            createSkillDesignerNumberParam('gainRatio', '鎻愬崌鍊嶇巼', '1.2'),
            createSkillDesignerNumberParam('duration', '鎸佺画鍥炲悎', '3', '1'),
          ];
        case '閫熷害鎻愬崌':
          return [
            createSkillDesignerNumberParam('gainRatio', '鎻愬崌鍊嶇巼', '1.15'),
            createSkillDesignerNumberParam('duration', '鎸佺画鍥炲悎', '2', '1'),
          ];
        case '鎶ょ浘':
        case '灏忔姢鐩?:
          return [
            createSkillDesignerNumberParam('shieldRatio', '鎶ょ浘鍊嶇巼', '0.8'),
            createSkillDesignerNumberParam('duration', '鎸佺画鍥炲悎', '2', '1'),
            createSkillDesignerSelectParam('shieldCap', '鎶ょ浘涓婇檺', SKILL_DESIGNER_PARAM_SHIELD_CAP_OPTIONS),
          ];
        case '鍑忎激':
          return [
            createSkillDesignerNumberParam('reduceRatio', '鍑忎激姣斾緥', '0.35'),
            createSkillDesignerNumberParam('duration', '鎸佺画鍥炲悎', '2', '1'),
            createSkillDesignerTextParam('damageType', '瑕嗙洊绫诲瀷', '鐗╃悊 / 鍏冪礌 / 鍏ㄤ激'),
          ];
        case '鏍兼尅/鎶垫秷':
          return [
            createSkillDesignerNumberParam('blockCount', '鏍兼尅娆℃暟', '1', '1'),
            createSkillDesignerTextParam('blockCap', '鍗曟涓婇檺', '鏈€澶氭姷娑堜竴娆″ぇ鎷?),
            createSkillDesignerTextParam('triggerRule', '瑙﹀彂鏉′欢', '鍙楀嚮鐬棿'),
          ];
        case '闇镐綋':
          return [
            createSkillDesignerNumberParam('duration', '鎸佺画鍥炲悎', '2', '1'),
            createSkillDesignerSelectParam('immuneLevel', '鍏嶆帶绾у埆', SKILL_DESIGNER_PARAM_IMMUNE_LEVEL_OPTIONS),
            createSkillDesignerNumberParam('reduceRatio', '棰濆鍑忎激', '0.2'),
          ];
        case '鍏嶆/閿佽':
          return [
            createSkillDesignerTextParam('triggerThreshold', '瑙﹀彂闃堝€?, '浣庝簬20%鐢熷懡'),
            createSkillDesignerTextParam('lockBloodFloor', '閿佽涓嬮檺', '淇濈暀1鐐?/ 10%'),
            createSkillDesignerTextParam('cooldown', '鍐峰嵈/娆℃暟', '姣忔垬1娆?),
          ];
        case '鏃犳晫閲戣韩':
          return [
            createSkillDesignerNumberParam('duration', '鎸佺画鍥炲悎', '2', '1'),
            createSkillDesignerNumberParam('dailyLimit', '姣忔棩瑙﹀彂', '3', '1'),
            createSkillDesignerNumberParam('tierThreshold', '鍏嶇柅浣嶉樁', '100', '0.5'),
            createSkillDesignerNumberParam('reduceRatio', '闄勫甫鍑忎激', '0.18'),
          ];
        case '浼ゅ鍙嶅皠':
          return [
            createSkillDesignerNumberParam('reflectRatio', '鍙嶅皠姣斾緥', '0.25'),
            createSkillDesignerNumberParam('duration', '鎸佺画鍥炲悎', '2', '1'),
            createSkillDesignerTextParam('reflectRule', '瑙﹀彂鏉′欢', '鍙楀嚮鍚?),
          ];
        case '浼ゅ鍒嗘憡':
          return [
            createSkillDesignerNumberParam('shareRatio', '鍒嗘憡姣斾緥', '0.35'),
            createSkillDesignerNumberParam('shareCount', '鍒嗘憡浜烘暟', '1', '1'),
            createSkillDesignerNumberParam('duration', '鎸佺画鍥炲悎', '2', '1'),
          ];
        case '鏇胯韩鎶垫秷':
          return [
            createSkillDesignerNumberParam('substituteCount', '鎶垫秷娆℃暟', '1', '1'),
            createSkillDesignerNumberParam('duration', '鎸佺画鍥炲悎', '2', '1'),
            createSkillDesignerTextParam('substituteRule', '瑙﹀彂鏉′欢', '鍙楀嚮鏃?),
          ];
        case '澶嶈嫃':
          return [
            createSkillDesignerNumberParam('reviveCount', '澶嶈嫃娆℃暟', '1', '1'),
            createSkillDesignerNumberParam('reviveHealRatio', '澶嶈嫃鍥炶', '0.25'),
            createSkillDesignerNumberParam('duration', '鎸佺画鍥炲悎', '3', '1'),
          ];
        case '浣撳姏鎭㈠':
        case '榄傚姏鎭㈠':
        case '绮剧鎭㈠':
          return [
            createSkillDesignerNumberParam('recoverRatio', '鍥炲鍊嶇巼', '0.35'),
            createSkillDesignerNumberParam('repeatCount', '鐢熸晥娆℃暟', '1', '1'),
          ];
        case '鎸佺画鎭㈠':
          return [
            createSkillDesignerNumberParam('recoverRatio', '姣忓洖鍚堝€嶇巼', '0.2'),
            createSkillDesignerNumberParam('duration', '鎸佺画鍥炲悎', '3', '1'),
            createSkillDesignerNumberParam('stackLimit', '鍙犲眰涓婇檺', '2', '1'),
          ];
        case '鍑€鍖?瑙ｆ帶':
        case '鍑€鍖?:
        case '瑙ｆ帶':
          return [
            createSkillDesignerNumberParam('cleanseCount', '娓呴櫎鏉＄洰鏁?, '2', '1'),
            createSkillDesignerSelectParam('cleansePriority', '鍑€鍖栦紭鍏堢骇', SKILL_DESIGNER_PARAM_CLEANSE_PRIORITY_OPTIONS),
            createSkillDesignerSelectParam('extraGain', '闄勫甫鏀剁泭', SKILL_DESIGNER_PARAM_CLEANSE_GAIN_OPTIONS),
          ];
        case '鎰熺煡骞叉壈':
          return [
            createSkillDesignerNumberParam('disturbPower', '骞叉壈寮哄害', '1.1'),
            createSkillDesignerNumberParam('duration', '鎸佺画鍥炲悎', '2', '1'),
          ];
        case '鏍囪閿佸畾':
          return [
            createSkillDesignerNumberParam('markDuration', '鏍囪鏃堕暱', '2', '1'),
            createSkillDesignerNumberParam('targetCap', '閿佸畾鐩爣鏁?, '1', '1'),
            createSkillDesignerSelectParam('trackingRule', '杩借釜瑙勫垯', SKILL_DESIGNER_PARAM_TRACKING_RULE_OPTIONS),
          ];
        case '鍏变韩瑙嗛噹':
          return [
            createSkillDesignerTextParam('shareRange', '鍏变韩鑼冨洿', '闃熶紞 / 鍗婂緞30绫?),
            createSkillDesignerNumberParam('duration', '鎸佺画鍥炲悎', '3', '1'),
            createSkillDesignerSelectParam('infoDepth', '鍏变韩娣卞害', SKILL_DESIGNER_PARAM_INFO_DEPTH_OPTIONS),
          ];
        case '骞诲':
          return [
            createSkillDesignerTextParam('illusionRange', '骞诲鑼冨洿', '鍗婂緞8绫?),
            createSkillDesignerNumberParam('duration', '鎸佺画鍥炲悎', '2', '1'),
            createSkillDesignerNumberParam('illusionPower', '骞绘湳寮哄害', '1.1'),
          ];
        case '鍌湢':
          return [
            createSkillDesignerNumberParam('duration', '鐫＄湢鍥炲悎', '2', '1'),
            createSkillDesignerSelectParam('wakeRule', '鍞ら啋鏉′欢', SKILL_DESIGNER_PARAM_WAKE_RULE_OPTIONS),
            createSkillDesignerTextParam('hitRule', '鍛戒腑鏉′欢', '瑙嗙嚎閿佸畾 / 澹版尝鎺ヨЕ'),
          ];
        case '璁ょ煡鎵洸':
          return [
            createSkillDesignerNumberParam('twistPower', '鎵洸寮哄害', '0.18'),
            createSkillDesignerNumberParam('duration', '鎸佺画鍥炲悎', '2', '1'),
          ];
        case '鐩爣閿佸畾':
          return [
            createSkillDesignerNumberParam('duration', '鎸佺画鍥炲悎', '2', '1'),
            createSkillDesignerNumberParam('hitBonus', '鍛戒腑澧炵泭', '0.1'),
            createSkillDesignerNumberParam('lockLevel', '閿佸畾灞傜骇', '1', '1'),
          ];
        case '鑷韩浣嶇Щ':
          return [
            createSkillDesignerTextParam('moveDistance', '浣嶇Щ璺濈', '5绫?),
          ];
        case '寮哄埗浣嶇Щ':
          return [
            createSkillDesignerTextParam('moveDistance', '浣嶇Щ璺濈', '4绫?),
            createSkillDesignerNumberParam('repeatCount', '瑙﹀彂娆℃暟', '1', '1'),
          ];
        case '浣嶇Щ浜ゆ崲':
          return [
            createSkillDesignerTextParam('exchangeRange', '浜ゆ崲鑼冨洿', '8绫?),
            createSkillDesignerNumberParam('duration', '鎸佺画鍥炲悎', '2', '1'),
            createSkillDesignerTextParam('triggerRule', '浜ゆ崲鏉′欢', '鍛戒腑鏍囪鐩爣'),
          ];
        case '杩藉嚮浣嶇Щ':
          return [
            createSkillDesignerTextParam('moveDistance', '杩藉嚮璺濈', '6绫?),
            createSkillDesignerTextParam('followWindow', '杩藉嚮绐楀彛', '鍛戒腑鍚?绉?),
            createSkillDesignerNumberParam('extraRatio', '杩藉姞鍊嶇巼', '0.3'),
          ];
        case '鑴辩浣嶇Щ':
          return [
            createSkillDesignerTextParam('moveDistance', '鑴辩璺濈', '7绫?),
            createSkillDesignerTextParam('escapeRule', '鑴辩鏉′欢', '鐢熷懡浣庝簬50%'),
            createSkillDesignerSelectParam('extraGain', '鑴辩鏀剁泭', SKILL_DESIGNER_PARAM_ESCAPE_GAIN_OPTIONS),
          ];
        case '鍒嗚韩':
          return [
            createSkillDesignerSelectParam('cloneType', '鍒嗚韩绫诲瀷', SKILL_DESIGNER_PARAM_CLONE_TYPE_OPTIONS),
            createSkillDesignerNumberParam('cloneCount', '鍒嗚韩鏁伴噺', '2', '1'),
            createSkillDesignerNumberParam('stealthRatio', '闅愯斀搴?, '0.45'),
            createSkillDesignerNumberParam('inheritRatio', '瀹炲姏缁ф壙', '0.55'),
            createSkillDesignerNumberParam('duration', '鎸佺画鍥炲悎', '2', '1'),
            createSkillDesignerTextParam('cloneName', '鍒嗚韩绉拌皳', '褰卞垎韬?/ 蹇冨儚'),
          ];
        case '澶嶅埗':
          return [
            createSkillDesignerSelectParam('copyTarget', '澶嶅埗瀵硅薄', SKILL_DESIGNER_PARAM_COPY_TARGET_OPTIONS),
            createSkillDesignerNumberParam('fidelity', '淇濈湡搴?, '0.8'),
            createSkillDesignerTextParam('duration', '缁存寔鏃堕暱', '2鍥炲悎'),
          ];
        case '鍙嶅埗':
          return [
            createSkillDesignerSelectParam('counterTarget', '鍙嶅埗瀵硅薄', SKILL_DESIGNER_PARAM_COUNTER_TARGET_OPTIONS),
            createSkillDesignerTextParam('triggerRule', '瑙﹀彂鏉′欢', '琚攣瀹氭椂 / 鍛戒腑鍓?),
            createSkillDesignerNumberParam('duration', '鎸佺画鍥炲悎', '2', '1'),
            createSkillDesignerNumberParam('counterRatio', '鍙嶅埗鍊嶇巼', '1.0'),
          ];
        case '杞寲':
          return [
            createSkillDesignerTextParam('convertPath', '杞寲鏂瑰悜', '浼ゅ鈫掓姢鐩?/ 鎺у埗鈫掑鐩?),
            createSkillDesignerNumberParam('convertRatio', '杞寲姣旂巼', '0.6'),
          ];
        case '鐘舵€佷氦鎹?:
          return [
            createSkillDesignerSelectParam('exchangeTarget', '浜ゆ崲瀵硅薄', SKILL_DESIGNER_PARAM_EXCHANGE_TARGET_OPTIONS),
            createSkillDesignerNumberParam('exchangeCount', '浜ゆ崲灞傛暟', '1', '1'),
            createSkillDesignerTextParam('triggerRule', '浜ゆ崲鏉′欢', '鍙屾柟鍚屾椂鍛戒腑'),
          ];
        case '鐘舵€佽浆绉?:
          return [
            createSkillDesignerTextParam('transferMode', '杞Щ妯″紡', '鑷韩璐熼潰->鐩爣'),
            createSkillDesignerTextParam('triggerRule', '杞Щ鏉′欢', '鍛戒腑鍚?),
          ];
        case '寮哄埗缁戝畾/閿佸畾':
          return [
            createSkillDesignerNumberParam('bindDuration', '缁戝畾鍥炲悎', '2', '1'),
            createSkillDesignerNumberParam('targetCap', '缁戝畾鐩爣鏁?, '1', '1'),
            createSkillDesignerTextParam('releaseRule', '瑙ｉ櫎鏉′欢', '瓒呰窛绂?/ 鍑€鍖?),
          ];
        case '鏉′欢瑙﹀彂':
          return [
            createSkillDesignerTextParam('triggerRule', '瑙﹀彂鏉′欢', '鍙楀嚮 / 浣庤 / 璁℃椂缁撴潫'),
            createSkillDesignerNumberParam('triggerCount', '瑙﹀彂娆℃暟', '1', '1'),
            createSkillDesignerSelectParam('triggerResult', '瑙﹀彂缁撴灉', SKILL_DESIGNER_PARAM_TRIGGER_RESULT_OPTIONS),
          ];
        case '瑙勫垯鏀瑰啓':
          return [
            createSkillDesignerSelectParam('rewriteDepth', '鏀瑰啓骞呭害', SKILL_DESIGNER_PARAM_REWRITE_DEPTH_OPTIONS),
          ];
        case '寮曠垎鎸佺画浼ゅ':
          return [
            createSkillDesignerNumberParam('detonateRatio', '寮曠垎鍊嶇巼', '1.2'),
            createSkillDesignerTextParam('consumeMode', '娑堣€楄鍒?, '娑堣€楀叏閮ㄦ寔缁激瀹?),
          ];
        case '鏂╃浘':
          return [
            createSkillDesignerNumberParam('shieldBreakRatio', '鏂╃浘鍊嶇巼', '0.6'),
          ];
        case '绐冨彇鎶ょ浘':
          return [
            createSkillDesignerNumberParam('shieldStealRatio', '绐冪浘姣斾緥', '0.45'),
            createSkillDesignerNumberParam('duration', '鎸佺画鍥炲悎', '2', '1'),
          ];
        case '鍚炲櫖':
          return [
            createSkillDesignerSelectParam('resourceType', '鍚炲櫖璧勬簮', SKILL_DESIGNER_PARAM_RESOURCE_TRANSFER_TYPE_OPTIONS),
            createSkillDesignerNumberParam('drainRatio', '鍚炲櫖姣斾緥', '0.18'),
            createSkillDesignerNumberParam('convertRatio', '杞寲姣斾緥', '1.0'),
          ];
        case '鑳藉姏鍏变韩':
          return [
            createSkillDesignerSelectParam('resourceType', '鍏变韩璧勬簮', SKILL_DESIGNER_PARAM_RESOURCE_TRANSFER_TYPE_OPTIONS),
            createSkillDesignerNumberParam('refeedRatio', '鍏变韩姣斾緥', '0.2'),
          ];
        case '鏈哄埗鎶规秷':
          return [
            createSkillDesignerSelectParam('suppressTarget', '鎶规秷鐩爣', SKILL_DESIGNER_PARAM_MECHANISM_SUPPRESS_TARGET_OPTIONS),
            createSkillDesignerSelectParam('suppressMode', '鎶规秷鏂瑰紡', SKILL_DESIGNER_PARAM_MECHANISM_SUPPRESS_MODE_OPTIONS),
            createSkillDesignerNumberParam('duration', '鎸佺画鍥炲悎', '2', '1'),
          ];
        case '绌块€?:
          return [
            createSkillDesignerNumberParam('penetrationRatio', '绌块€忔瘮渚?, '0.25'),
            createSkillDesignerSelectParam('penetrationTarget', '绌块€忓璞?, SKILL_DESIGNER_PARAM_PENETRATION_TARGET_OPTIONS),
          ];
        case '鍚歌':
          return [
            createSkillDesignerNumberParam('lifestealRatio', '鍚稿彇姣斾緥', '0.2'),
            createSkillDesignerSelectParam('resourceType', '鍚稿彇璧勬簮', SKILL_DESIGNER_PARAM_LIFESTEAL_RESOURCE_OPTIONS),
          ];
        case '鏂╂潃琛ヤ激':
          return [
            createSkillDesignerTextParam('executeLine', '瑙﹀彂琛€绾?, '浣庝簬25%'),
            createSkillDesignerNumberParam('bonusRatio', '琛ヤ激鍊嶇巼', '0.5'),
          ];
        case '娴佽DOT':
          return [
            createSkillDesignerNumberParam('dotRatio', '姣忚烦鍊嶇巼', '0.2'),
            createSkillDesignerNumberParam('duration', '鎸佺画鍥炲悎', '3', '1'),
          ];
        case '鎵撴柇':
          return [
            createSkillDesignerSelectParam('interruptWindow', '鎵撴柇鏃舵満', SKILL_DESIGNER_PARAM_INTERRUPT_WINDOW_OPTIONS),
            createSkillDesignerNumberParam('extraDelay', '杩藉姞鍍电洿', '0.5'),
          ];
        case '鍙嶅嚮':
          return [
            createSkillDesignerSelectParam('counterRule', '鍙嶅嚮鏉′欢', SKILL_DESIGNER_PARAM_COUNTER_RULE_OPTIONS),
            createSkillDesignerNumberParam('counterRatio', '鍙嶅嚮鍊嶇巼', '0.8'),
          ];
        case '杩藉嚮':
          return [
            createSkillDesignerTextParam('followWindow', '杩藉嚮绐楀彛', '鍛戒腑鍚?绉?),
            createSkillDesignerNumberParam('bonusRatio', '杩藉嚮鍊嶇巼', '1.2'),
            createSkillDesignerNumberParam('duration', '鎸佺画鍥炲悎', '2', '1'),
          ];
        case '娌夐粯':
          return [
            createSkillDesignerNumberParam('duration', '娌夐粯鍥炲悎', '2', '1'),
            createSkillDesignerSelectParam('muteScope', '闄愬埗鑼冨洿', SKILL_DESIGNER_PARAM_MUTE_SCOPE_OPTIONS),
          ];
        case '缂存':
          return [
            createSkillDesignerNumberParam('duration', '缂存鍥炲悎', '2', '1'),
            createSkillDesignerTextParam('disarmScope', '闄愬埗鑼冨洿', '杩戞垬 / 姝﹀櫒鎶€'),
          ];
        case '鍑忛€?:
        case '杩熺紦':
          return [
            createSkillDesignerNumberParam('slowRatio', '鍑忛€熷箙搴?, '0.3'),
            createSkillDesignerNumberParam('duration', '鎸佺画鍥炲悎', '2', '1'),
          ];
        case '鑷寸洸':
          return [
            createSkillDesignerNumberParam('duration', '鑷寸洸鍥炲悎', '2', '1'),
            createSkillDesignerSelectParam('blindEffect', '褰卞搷鍐呭', SKILL_DESIGNER_PARAM_BLIND_EFFECT_OPTIONS),
          ];
        case '绂佺枟':
          return [
            createSkillDesignerNumberParam('banHealRatio', '绂佺枟骞呭害', '1.0'),
            createSkillDesignerNumberParam('duration', '鎸佺画鍥炲悎', '2', '1'),
          ];
        case '娌荤枟鍙嶈浆':
          return [
            createSkillDesignerNumberParam('invertRatio', '鍙嶈浆鍊嶇巼', '1.0'),
            createSkillDesignerNumberParam('duration', '鎸佺画鍥炲悎', '2', '1'),
          ];
        case '鏍囪寮辩偣':
          return [
            createSkillDesignerSelectParam('weakPointType', '寮辩偣绫诲瀷', SKILL_DESIGNER_PARAM_WEAK_POINT_TYPE_OPTIONS),
            createSkillDesignerNumberParam('duration', '鎸佺画鍥炲悎', '2', '1'),
          ];
        case '椹辨暎澧炵泭':
          return [
            createSkillDesignerNumberParam('dispelCount', '椹辨暎鏁伴噺', '1', '1'),
          ];
        case '绐冨彇澧炵泭':
          return [
            createSkillDesignerNumberParam('stealCount', '绐冨彇鏁伴噺', '1', '1'),
          ];
        case '闅愯韩':
          return [
            createSkillDesignerNumberParam('duration', '鎸佺画鍥炲悎', '2', '1'),
            createSkillDesignerNumberParam('stealthRatio', '闅愯斀搴?, '0.3'),
            createSkillDesignerNumberParam('dodgeBonus', '闂伩澧炵泭', '0.1'),
            createSkillDesignerNumberParam('reactionBonus', '鍙嶅簲澧炵泭', '0.08'),
          ];
        case '鎶ゅ崼':
          return [
            createSkillDesignerNumberParam('duration', '鎸佺画鍥炲悎', '2', '1'),
            createSkillDesignerNumberParam('reduceRatio', '鎶ゅ崼鍑忎激', '0.1'),
          ];
        case '鍢茶':
          return [
            createSkillDesignerNumberParam('duration', '鎸佺画鍥炲悎', '2', '1'),
            createSkillDesignerTextParam('focusRule', '鑱氱伀瑙勫垯', '寮哄埗浼樺厛鑷韩'),
          ];
        case '鐮撮殣':
          return [
            createSkillDesignerNumberParam('hitBonus', '鍛戒腑澧炵泭', '0.1'),
            createSkillDesignerNumberParam('lockLevel', '閿佸畾灞傜骇', '1', '1'),
          ];
        case '灏佹妧':
          return [
            createSkillDesignerNumberParam('duration', '鎸佺画鍥炲悎', '2', '1'),
            createSkillDesignerSelectParam('muteScope', '闄愬埗鑼冨洿', SKILL_DESIGNER_PARAM_MUTE_SCOPE_OPTIONS),
          ];
        default:
          return [];
      }
    }

    function getSkillDesignerMechanicParamSections(draft = {}) {
      const sections = [];
      const primaryLabel = getSkillDesignerEffectivePrimaryLabel(draft);
      const primaryMain = normalizeSkillUiText(draft.primaryMain, '');
      if (primaryLabel) {
        sections.push({ kind: 'primary', label: primaryLabel });
      } else if (primaryMain && getSkillDesignerMechanicParamDefs(primaryMain).length) {
        sections.push({ kind: 'primary', label: primaryMain });
      }
      normalizeSkillDesignerSecondarySelection(primaryMain, draft.secondaryMechanics, draft.type).forEach(label => {
        sections.push({ kind: 'secondary', label });
      });
      return sections;
    }

    function collectSkillDesignerMechanicLabels(draft = {}) {
      return Array.from(new Set(
        getSkillDesignerMechanicParamSections(draft)
          .map(section => normalizeSkillUiText(section && section.label, ''))
          .filter(Boolean)
      ));
    }

    function normalizeSkillDesignerMechanicParamMap(value = {}, draft = {}) {
      const safeMap = value && typeof value === 'object' ? value : {};
      const normalized = {};
      collectSkillDesignerMechanicLabels(draft).forEach(label => {
        const defs = getSkillDesignerMechanicParamDefs(label);
        const source = safeMap[label] && typeof safeMap[label] === 'object' ? safeMap[label] : {};
        const nextEntry = {};
        defs.forEach(def => {
          const currentValue = normalizeSkillUiText(source[def.key], '');
          if (currentValue) nextEntry[def.key] = currentValue;
        });
        if (safeEntries(nextEntry).length) normalized[label] = nextEntry;
      });
      return normalized;
    }

    function buildSkillDesignerMechanicParamSummary(draft = {}) {
      const mechanicParams = normalizeSkillDesignerMechanicParamMap(draft.mechanicParams, draft);
      return collectSkillDesignerMechanicLabels(draft).map(label => {
        const defs = getSkillDesignerMechanicParamDefs(label);
        const paramState = mechanicParams[label] && typeof mechanicParams[label] === 'object' ? mechanicParams[label] : {};
        const segments = defs
          .map(def => {
            const value = normalizeSkillUiText(paramState[def.key], '');
            return value ? `${def.label}:${value}` : '';
          })
          .filter(Boolean);
        return segments.length ? `${label}(${segments.join(' / ')})` : '';
      }).filter(Boolean).join('锛?);
    }

    function buildSkillDesignerMechanicParamEditor(draft = {}) {
      const mechanicParams = normalizeSkillDesignerMechanicParamMap(draft.mechanicParams, draft);
      let secondaryIndex = 0;
      const sections = getSkillDesignerMechanicParamSections(draft).map(section => {
        const label = normalizeSkillUiText(section && section.label, '');
        const defs = getSkillDesignerMechanicParamDefs(label);
        if (!defs.length) return '';
        const paramState = mechanicParams[label] && typeof mechanicParams[label] === 'object' ? mechanicParams[label] : {};
        const title = section && section.kind === 'primary'
          ? `涓绘満鍒?/ ${label}`
          : `鍓満鍒?${++secondaryIndex} / ${label}`;
        return `
          <div class=\"skill-designer-subsection\">
            <div class=\"mvu-editor-label\">${htmlEscape(title)}</div>
            <div class=\"mvu-editor-field-grid\">
              ${defs.map(def => {
                const currentValue = normalizeSkillUiText(paramState[def.key], '');
                if (def.type === 'select') {
                  return `
                    <label class=\"mvu-editor-field\">
                      <span class=\"mvu-editor-label\">${htmlEscape(def.label)}</span>
                      <select class=\"mvu-editor-select\" data-skill-designer-mechanic=\"${escapeHtmlAttr(label)}\" data-skill-designer-param-key=\"${escapeHtmlAttr(def.key)}\" data-skill-designer-disableable>
                        ${buildSkillDesignerSelectOptions(def.options || [], currentValue, def.placeholder || '鏈缃?)}
                      </select>
                    </label>
                  `;
                }
                return `
                  <label class=\"mvu-editor-field\">
                    <span class=\"mvu-editor-label\">${htmlEscape(def.label)}</span>
                    <input class=\"mvu-editor-input\" type=\"${def.type === 'number' ? 'number' : 'text'}\"${def.type === 'number' ? ` step=\"${escapeHtmlAttr(def.step || '0.1')}\"` : ''} value=\"${escapeHtmlAttr(currentValue)}\"${def.placeholder ? ` placeholder=\"${escapeHtmlAttr(def.placeholder)}\"` : ''} data-skill-designer-mechanic=\"${escapeHtmlAttr(label)}\" data-skill-designer-param-key=\"${escapeHtmlAttr(def.key)}\" data-skill-designer-disableable />
                  </label>
                `;
              }).join('')}
            </div>
          </div>
        `;
      }).filter(Boolean);
      return sections.join('') || '<span class=\"tag-chip\">褰撳墠鏈哄埗鏆傛棤棰濆鍙傛暟</span>';
    }

    function readSkillDesignerMechanicParamState(mountEl, draft = {}) {
      const nextMap = {};
      collectSkillDesignerMechanicLabels(draft).forEach(label => {
        const defs = getSkillDesignerMechanicParamDefs(label);
        const entry = {};
        defs.forEach(def => {
          const input = mountEl
            ? Array.from(mountEl.querySelectorAll('[data-skill-designer-param-key]')).find(node =>
              node.getAttribute('data-skill-designer-mechanic') === label
              && node.getAttribute('data-skill-designer-param-key') === def.key,
            )
            : null;
          const value = input ? normalizeSkillUiText(input.value, '') : '';
          if (value) entry[def.key] = value;
        });
        if (safeEntries(entry).length) nextMap[label] = entry;
      });
      return nextMap;
    }

    function buildSkillDesignerFormStateFromDraft(draft = {}, previewMeta = {}) {
      const baseDraft = draft && typeof draft === 'object' ? draft : {};
      const typeMeta = resolveSkillDesignerTypeMeta(previewMeta, baseDraft.type);
      const costConfig = parseSkillDesignerCostConfig(baseDraft.cost, baseDraft.resourceType, baseDraft.resourceValue);
      const resolvedPrimaryMain = normalizeSkillUiText(baseDraft.primaryMain, '');
      let resolvedTarget = normalizeSkillDesignerTargetForForm(baseDraft.target, getSkillDesignerDefaultTarget(previewMeta, typeMeta.value));
      if (/^(鏈煡|鏈缃畖鏃?$/.test(resolvedTarget)) resolvedTarget = getSkillDesignerDefaultTarget(previewMeta, typeMeta.value);
      const isFusionScope = toText(previewMeta && previewMeta.scope, '') === '姝﹂瓊铻嶅悎鎶€';
      const rawFusionMode = isFusionScope ? normalizeSkillDesignerFusionMode(baseDraft.fusionMode || baseDraft['铻嶅悎妯″紡']) : '';
      const rawFusionPartner = isFusionScope ? normalizeSkillUiText(baseDraft.fusionPartner || baseDraft['铻嶅悎瀵硅薄'], '') : '';
      const rawFusionParticipants = isFusionScope ? normalizeSkillDesignerFusionParticipantList(getSkillDesignerRawFusionParticipants(baseDraft)) : [];
      const derivedFusionFields = isFusionScope
        ? deriveSkillDesignerFusionFields(rawFusionMode, rawFusionParticipants, rawFusionPartner)
        : { fusionMode: '', fusionPartner: '', fusionSourceSpirits: [], fusionParticipants: [] };
      const resolvedPrimarySub = normalizeSkillUiText(
        baseDraft.primarySub,
        getSkillDesignerDefaultPrimarySub(resolvedPrimaryMain, resolvedTarget, typeMeta.value),
      );
      const derivedMainRole = inferSkillDesignerMainRole(
        typeMeta.value,
        resolvedTarget,
        resolvedPrimaryMain,
        resolvedPrimarySub,
      );
      const coreState = {
        name: normalizeSkillUiText(baseDraft.name, toText(previewMeta && previewMeta.label, '鏈懡鍚嶆妧鑳?)),
        type: typeMeta.value,
        typeDisplay: typeMeta.display,
        target: resolvedTarget,
        cost: formatSkillDesignerCostText(costConfig.resourceType, costConfig.resourceValue),
        costType: costConfig.resourceType,
        costValue: costConfig.resourceValue,
        mainRole: normalizeSkillUiText(derivedMainRole, '鏈煡'),
        primaryMain: resolvedPrimaryMain,
        primarySub: resolvedPrimarySub,
        deliveryForm: normalizeSkillUiText(baseDraft.deliveryForm, ''),
        secondaryMechanics: normalizeSkillDesignerSecondarySelection(resolvedPrimaryMain, baseDraft.secondaryMechanics, typeMeta.value),
        attachedAttributes: normalizeSkillDesignerArray(baseDraft.attachedAttributes),
        tags: normalizeSkillDesignerArray(baseDraft.tags),
        visualDesc: normalizeSkillUiText(baseDraft.visualDesc, ''),
        effectDesc: normalizeSkillUiText(baseDraft.effectDesc, ''),
        artStage: normalizeSkillUiText(baseDraft.artStage, '鏈叆闂?),
        artLevel: Math.max(0, toNumber(baseDraft.artLevel, 0)),
        artExp: Math.max(0, toNumber(baseDraft.artExp, 0)),
        fusionMode: derivedFusionFields.fusionMode,
        fusionPartner: derivedFusionFields.fusionPartner,
        fusionSourceSpirits: derivedFusionFields.fusionSourceSpirits.length
          ? derivedFusionFields.fusionSourceSpirits
          : (isFusionScope ? normalizeSkillDesignerArray(baseDraft.fusionSourceSpirits || baseDraft['鏉ユ簮姝﹂瓊']) : []),
        fusionParticipants: derivedFusionFields.fusionParticipants,
        鍓綔鐢ㄥ垪琛? normalizeSkillDesignerSideEffectList(baseDraft['鍓綔鐢ㄥ垪琛?] || []),
      };
      return {
        ...coreState,
        mechanicParams: normalizeSkillDesignerMechanicParamMap(baseDraft.mechanicParams, coreState),
      };
    }

    function readSkillDesignerFormState(mountEl, previewMeta = {}) {
      const readField = key => {
        const input = mountEl ? mountEl.querySelector(`[data-skill-designer-field=\"${key}\"]`) : null;
        return input ? toText(input.value, '').trim() : '';
      };
      const readCheckedValues = name => Array.from(mountEl ? mountEl.querySelectorAll(`input[name=\"${name}\"]:checked`) : []).map(node => toText(node.value, '').trim()).filter(Boolean);
      const readFusionParticipants = () => Array.from(mountEl ? mountEl.querySelectorAll('[data-fusion-participant-row]') : [])
        .map(row => {
          const roleInput = row.querySelector('[data-skill-designer-fusion-role]');
          const charInput = row.querySelector('[data-skill-designer-fusion-char]');
          const spiritInput = row.querySelector('[data-skill-designer-fusion-spirit]');
          const role = normalizeSkillUiText(roleInput && roleInput.value, '') === 'self' ? 'self' : 'partner';
          const charKey = normalizeSkillUiText(charInput && charInput.value, '');
          const selectedCharOption = charInput && charInput.tagName === 'SELECT'
            ? charInput.options[charInput.selectedIndex]
            : null;
          const charName = normalizeSkillUiText(
            charInput && charInput.getAttribute('data-skill-designer-fusion-char-name'),
            normalizeSkillUiText(selectedCharOption && selectedCharOption.textContent, charKey),
          );
          const spirit = normalizeSkillUiText(spiritInput && spiritInput.value, '');
          return createSkillDesignerFusionParticipant(role, charKey, charName, spirit);
        })
        .filter(participant => participant.charKey || participant.charName || participant.spirit);
      const typeMeta = resolveSkillDesignerTypeMeta(previewMeta, readField('type'));
      const resolvedPrimaryMain = readField('primaryMain');
      const resolvedPrimarySub = readField('primarySub');
      const resolvedTarget = normalizeSkillDesignerTargetForForm(readField('target'), getSkillDesignerDefaultTarget(previewMeta, typeMeta.value));
      const isFusionScope = toText(previewMeta && previewMeta.scope, '') === '姝﹂瓊铻嶅悎鎶€';
      const fusionMode = isFusionScope ? normalizeSkillDesignerFusionMode(readField('fusionMode')) : '';
      const fusionParticipants = isFusionScope ? readFusionParticipants() : [];
      const derivedFusionFields = isFusionScope
        ? deriveSkillDesignerFusionFields(fusionMode, fusionParticipants, '')
        : { fusionPartner: '', fusionSourceSpirits: [], fusionParticipants: [] };
      const derivedMainRole = inferSkillDesignerMainRole(
        typeMeta.value,
        resolvedTarget,
        resolvedPrimaryMain,
        resolvedPrimarySub,
      );
      const baseState = {
        name: readField('name') || toText(previewMeta && previewMeta.label, '鏈懡鍚嶆妧鑳?),
        type: typeMeta.value,
        typeDisplay: typeMeta.display,
        target: resolvedTarget,
        costType: readField('costType') || '鏃?,
        costValue: readField('costValue'),
        mainRole: normalizeSkillUiText(derivedMainRole, '鏈煡'),
        primaryMain: resolvedPrimaryMain,
        primarySub: resolvedPrimarySub,
        deliveryForm: readField('deliveryForm'),
        secondaryMechanics: normalizeSkillDesignerSecondarySelection(readField('primaryMain'), readCheckedValues('skill-secondary'), readField('type')),
        attachedAttributes: readCheckedValues('skill-attribute'),
        tags: normalizeSkillDesignerArray(readField('tags')),
        visualDesc: readField('visualDesc'),
        effectDesc: readField('effectDesc'),
        artStage: readField('artStage') || '鏈叆闂?,
        artLevel: Math.max(0, toNumber(readField('artLevel'), 0)),
        artExp: Math.max(0, toNumber(readField('artExp'), 0)),
        fusionMode,
        fusionPartner: derivedFusionFields.fusionPartner,
        fusionSourceSpirits: derivedFusionFields.fusionSourceSpirits,
        fusionParticipants: derivedFusionFields.fusionParticipants,
        鍓綔鐢ㄥ垪琛? parseSkillDesignerSideEffectJson(readField('sideEffectJson')),
      };
      return {
        ...baseState,
        cost: formatSkillDesignerCostText(baseState.costType, baseState.costValue),
        mechanicParams: readSkillDesignerMechanicParamState(mountEl, baseState),
      };
    }

    function ensureSkillDesignerEffectArray(skill = {}) {
      const effectArray = cloneJsonValue(Array.isArray(skill && skill['_鏁堟灉鏁扮粍']) ? skill['_鏁堟灉鏁扮粍'] : []);
      let systemBaseIndex = effectArray.findIndex(effect => toText(effect && effect['鏈哄埗'], '').trim() === '绯荤粺鍩虹');
      if (systemBaseIndex < 0) {
        effectArray.unshift({ '鏈哄埗': '绯荤粺鍩虹' });
        systemBaseIndex = 0;
      }
      if (!effectArray[systemBaseIndex] || typeof effectArray[systemBaseIndex] !== 'object') effectArray[systemBaseIndex] = { '鏈哄埗': '绯荤粺鍩虹' };
      return {
        effectArray,
        systemBase: effectArray[systemBaseIndex]
      };
    }

    const SKILL_DESIGNER_RUNTIME_PROPERTY_KEY_BY_LABEL = Object.freeze({
      '鍔涢噺': 'str',
      '闃插尽': 'def',
      '鏁忔嵎': 'agi',
      '浣撳姏涓婇檺': 'vit_max',
      '榄傚姏涓婇檺': 'sp_max',
      '绮剧鍔涗笂闄?: 'men_max',
      '浣撳姏': 'vit',
      '榄傚姏': 'sp',
      '绮剧鍔?: 'men',
      '鎺屾帶': '鎺屾帶',
      '濞佸姏': '濞佸姏',
      '娑堣€?: '娑堣€?,
      '鍓嶆憞': '鍓嶆憞',
      '鎺у埗': '鎺у埗',
      '閫熷害': '閫熷害',
    });
    const SKILL_DESIGNER_RUNTIME_CAST_TIME_BY_DELIVERY = Object.freeze({
      '鐩存帴鐢熸晥': 10,
      '鑷韩闄勪綋': 6,
      '杩滅▼鍛戒腑': 12,
      '鑼冨洿灞曞紑': 18,
      '鍙敜鎵胯浇': 20,
      '閫犵墿鎵胯浇': 20,
      '鏍囪瑙﹀彂': 10,
      '寤惰繜瑙﹀彂': 16,
    });

    function buildSkillDesignerRuntimeObject(source = {}) {
      const result = {};
      safeEntries(source && typeof source === 'object' ? source : {}).forEach(([key, value]) => {
        if (value === undefined || value === null) return;
        if (typeof value === 'string' && !value.trim()) return;
        if (Array.isArray(value) && !value.length) return;
        if (value && typeof value === 'object' && !Array.isArray(value) && !safeEntries(value).length) return;
        result[key] = value;
      });
      return result;
    }

    function parseSkillDesignerNumericInputValue(value, fallback = 0, digits = 4) {
      const rawText = toText(value, '').trim();
      if (!rawText) return Number(Number(fallback || 0).toFixed(digits));
      const match = rawText.match(/-?\d+(?:\.\d+)?/);
      const parsed = Number(match ? match[0] : rawText);
      if (!Number.isFinite(parsed)) return Number(Number(fallback || 0).toFixed(digits));
      return Number(parsed.toFixed(digits));
    }

    function parseSkillDesignerIntegerInputValue(value, fallback = 0, minimum = 0) {
      const parsed = Math.round(parseSkillDesignerNumericInputValue(value, fallback, 0));
      if (!Number.isFinite(parsed)) return Math.max(minimum, Math.round(Number(fallback || 0)));
      return Math.max(minimum, parsed);
    }

    function parseSkillDesignerPercentRatio(value, fallback = 0) {
      const rawText = toText(value, '').trim();
      if (!rawText) return Number(Number(fallback || 0).toFixed(4));
      const parsed = parseSkillDesignerNumericInputValue(rawText, fallback, 4);
      if (!Number.isFinite(parsed)) return Number(Number(fallback || 0).toFixed(4));
      if (/%/.test(rawText) || parsed > 1) return Number((parsed / 100).toFixed(4));
      return Number(parsed.toFixed(4));
    }

    function parseSkillDesignerFactorInputValue(value, fallback = 1, options = {}) {
      const rawText = toText(value, '').trim();
      const plusBase = !!(options && options.plusBase);
      if (!rawText) return Number(Number(fallback || 0).toFixed(4));
      const parsed = parseSkillDesignerNumericInputValue(rawText, fallback, 4);
      if (!Number.isFinite(parsed)) return Number(Number(fallback || 0).toFixed(4));
      if (/%/.test(rawText)) {
        const ratio = parsed / 100;
        return Number((plusBase ? 1 + ratio : ratio).toFixed(4));
      }
      if (plusBase && parsed >= 0 && parsed <= 1) return Number((1 + parsed).toFixed(4));
      return Number(parsed.toFixed(4));
    }

    function parseSkillDesignerReductionFactor(value, fallback = 0.8) {
      const rawText = toText(value, '').trim();
      if (!rawText) return Number(Number(fallback || 0).toFixed(4));
      if (/%/.test(rawText)) {
        const ratio = parseSkillDesignerPercentRatio(rawText, Math.max(0, 1 - Number(fallback || 0)));
        return Number((1 - ratio).toFixed(4));
      }
      const parsed = parseSkillDesignerNumericInputValue(rawText, fallback, 4);
      if (!Number.isFinite(parsed)) return Number(Number(fallback || 0).toFixed(4));
      if (parsed >= 0 && parsed <= 1) return Number((1 - parsed).toFixed(4));
      if (parsed > 1 && parsed <= 100) return Number((1 - parsed / 100).toFixed(4));
      return Number(parsed.toFixed(4));
    }

    function parseSkillDesignerPercentPoints(value, fallback = 0) {
      const rawText = toText(value, '').trim();
      if (!rawText) return Number(Number(fallback || 0).toFixed(2));
      const parsed = parseSkillDesignerNumericInputValue(rawText, fallback, 2);
      if (!Number.isFinite(parsed)) return Number(Number(fallback || 0).toFixed(2));
      if (parsed >= 0 && parsed <= 1) return Number((parsed * 100).toFixed(2));
      return Number(parsed.toFixed(2));
    }

    function normalizeSkillDesignerTargetForRuntime(target = '', mode = 'effect') {
      const text = normalizeSkillUiText(target, mode === 'system' ? '鏁屾柟/鍗曚綋' : '鏁屾柟鍗曚綋');
      const effectMap = {
        '鑷韩': '鑷韩',
        '鍙嬫柟鍗曚綋': '鍙嬫柟鍗曚綋',
        '鍙嬫柟缇や綋': '鍙嬫柟缇や綋',
        '鏁屾柟鍗曚綋': '鏁屾柟鍗曚綋',
        '鏁屾柟缇や綋': '鏁屾柟缇や綋',
        '鍏ㄥ満': '鍏ㄥ満',
        '椋熺敤鑰?: '椋熺敤鑰?,
        '浣跨敤鑰?: '浣跨敤鑰?,
        '鍙敜鐗?: '鍙敜鐗?,
        '閫犵墿': '閫犵墿',
        '宸辨柟/鍗曚綋': '鍙嬫柟鍗曚綋',
        '宸辨柟/缇や綋': '鍙嬫柟缇や綋',
        '鏁屾柟/鍗曚綋': '鏁屾柟鍗曚綋',
        '鏁屾柟/缇や綋': '鏁屾柟缇や綋',
      };
      const systemMap = {
        '鑷韩': '鑷韩',
        '鍙嬫柟鍗曚綋': '宸辨柟/鍗曚綋',
        '鍙嬫柟缇や綋': '宸辨柟/缇や綋',
        '鏁屾柟鍗曚綋': '鏁屾柟/鍗曚綋',
        '鏁屾柟缇や綋': '鏁屾柟/缇や綋',
        '鍏ㄥ満': '鍏ㄥ満',
        '椋熺敤鑰?: '椋熺敤鑰?,
        '浣跨敤鑰?: '浣跨敤鑰?,
        '鍙敜鐗?: '鍙敜鐗?,
        '閫犵墿': '閫犵墿',
        '宸辨柟/鍗曚綋': '宸辨柟/鍗曚綋',
        '宸辨柟/缇や綋': '宸辨柟/缇や綋',
        '鏁屾柟/鍗曚綋': '鏁屾柟/鍗曚綋',
        '鏁屾柟/缇や綋': '鏁屾柟/缇や綋',
      };
      const mapping = mode === 'system' ? systemMap : effectMap;
      return mapping[text] || (mode === 'system' ? '鏁屾柟/鍗曚綋' : '鏁屾柟鍗曚綋');
    }

    function resolveSkillDesignerSupportTargetForRuntime(target = '') {
      const normalizedTarget = normalizeSkillDesignerTargetForRuntime(target, 'effect');
      if (/鏁屾柟/.test(normalizedTarget)) return '鑷韩';
      if (normalizedTarget === '鍏ㄥ満') return '鍙嬫柟缇や綋';
      return normalizedTarget || '鑷韩';
    }

    function isSkillDesignerGrantableMechanism(label = '') {
      const normalizedLabel = normalizeSkillUiText(label, '');
      return Array.isArray(SKILL_DESIGNER_TARGET_SEMANTICS.鍙祴浜?
        ? SKILL_DESIGNER_TARGET_SEMANTICS.鍙祴浜?includes(normalizedLabel)
        : false;
    }

    function isSkillDesignerSelfOnlyMechanism(label = '') {
      const normalizedLabel = normalizeSkillUiText(label, '');
      return Array.isArray(SKILL_DESIGNER_TARGET_SEMANTICS.浠呰嚜韬?
        ? SKILL_DESIGNER_TARGET_SEMANTICS.浠呰嚜韬?includes(normalizedLabel)
        : false;
    }

    function getSkillDesignerMechanismRegistryMeta(label = '') {
      const normalizedLabel = normalizeSkillUiText(label, '');
      if (!normalizedLabel) return null;
      const meta = SKILL_DESIGNER_MECHANISM_META[normalizedLabel];
      return meta && typeof meta === 'object' ? meta : null;
    }

    function buildSkillDesignerPrimaryHintFromRegistry(label = '') {
      const meta = getSkillDesignerMechanismRegistryMeta(label);
      if (!meta) return null;
      const main = normalizeSkillUiText(meta.designerMainHint, '');
      const sub = normalizeSkillUiText(meta.designerSubHint, '');
      const secondary = normalizeSkillUiText(meta.designerSecondaryHint, '');
      if (!main && !sub) return null;
      return {
        main: main || sub,
        sub: sub || main,
        secondary: secondary || undefined,
      };
    }

    function buildSkillDesignerSecondaryHintsFromRegistry(label = '') {
      const meta = getSkillDesignerMechanismRegistryMeta(label);
      if (!meta) return [];
      const hint = normalizeSkillUiText(meta.designerSecondaryHint, '');
      return hint ? [hint] : [];
    }

    function resolveSkillDesignerMechanismParamDefsFromRegistry(label = '') {
      const meta = getSkillDesignerMechanismRegistryMeta(label);
      const schema = Array.isArray(meta && meta.璁捐鍙板弬鏁板畾涔? ? meta.璁捐鍙板弬鏁板畾涔?: [];
      if (!schema.length) return [];
      return schema
        .map(def => {
          if (!def || typeof def !== 'object') return null;
          const type = normalizeSkillUiText(def.type, 'text');
          const key = normalizeSkillUiText(def.key, '');
          const title = normalizeSkillUiText(def.label, '');
          if (!key || !title) return null;
          const placeholder = normalizeSkillUiText(def.placeholder, '');
          if (type === 'select') {
            const optionKey = normalizeSkillUiText(def.optionsKey, '');
            const options = optionKey ? SKILL_DESIGNER_PARAM_OPTION_REGISTRY[optionKey] || [] : (Array.isArray(def.options) ? def.options : []);
            return createSkillDesignerSelectParam(key, title, options, placeholder || '鏈缃?);
          }
          if (type === 'number') {
            return createSkillDesignerNumberParam(key, title, placeholder, normalizeSkillUiText(def.step, '0.1'));
          }
          return createSkillDesignerTextParam(key, title, placeholder);
        })
        .filter(Boolean);
    }

    function resolveSkillDesignerMechanismTargetForRuntime(target = '', mechanismLabel = '') {
      const normalizedLabel = normalizeSkillUiText(mechanismLabel, '');
      const normalizedTarget = normalizeSkillDesignerTargetForRuntime(target, 'effect');
      if (isSkillDesignerSelfOnlyMechanism(normalizedLabel)) return '鑷韩';
      const meta = getSkillDesignerMechanismRegistryMeta(normalizedLabel);
      if (meta && meta.缇や綋璧嬩簣 === true) {
        if (/鏁屾柟/.test(normalizedTarget)) return '鑷韩';
        if (normalizedTarget === '鍏ㄥ満') return '鍙嬫柟缇や綋';
        if (normalizedTarget === '鍙嬫柟缇や綋' || normalizedTarget === '鍙嬫柟鍗曚綋') return normalizedTarget;
        return '鑷韩';
      }
      if (normalizedLabel === '鍏变韩瑙嗛噹') return resolveSkillDesignerSharedVisionTargetForRuntime(target);
      if (normalizedLabel && isSkillDesignerGrantableMechanism(normalizedLabel))
        return resolveSkillDesignerGrantableMechanismTargetForRuntime(target, normalizedLabel);
      return normalizedTarget || '鑷韩';
    }

    function resolveSkillDesignerGrantableMechanismTargetForRuntime(target = '', mechanismLabel = '') {
      const normalizedTarget = normalizeSkillDesignerTargetForRuntime(target, 'effect');
      if (mechanismLabel && !isSkillDesignerGrantableMechanism(mechanismLabel)) return normalizedTarget || '鑷韩';
      if (/鏁屾柟/.test(normalizedTarget)) return '鑷韩';
      if (normalizedTarget === '鍏ㄥ満') return '鍙嬫柟缇や綋';
      return normalizedTarget || '鑷韩';
    }

    function resolveSkillDesignerSharedVisionTargetForRuntime(target = '') {
      const normalizedTarget = normalizeSkillDesignerTargetForRuntime(target, 'effect');
      if (/鏁屾柟/.test(normalizedTarget) || normalizedTarget === '鍏ㄥ満') return '鍙嬫柟缇や綋';
      return normalizedTarget || '鍙嬫柟缇や綋';
    }

    function resolveSkillDesignerPropertyKeyByLabel(label = '') {
      const normalized = normalizeSkillUiText(label, '');
      return SKILL_DESIGNER_RUNTIME_PROPERTY_KEY_BY_LABEL[normalized] || normalized;
    }

    function resolveSkillDesignerPropertyKeysFromText(text = '', options = {}) {
      const includeFullSet = !!(options && options.includeFullSet);
      if (includeFullSet) return [...SKILL_DESIGNER_FULL_ATTRIBUTE_KEYS];
      return Array.from(new Set(
        normalizeSkillDesignerArray(text)
          .map(resolveSkillDesignerPropertyKeyByLabel)
          .filter(Boolean)
      ));
    }

    function getSkillDesignerMechanicParamsForRuntime(draft = {}, label = '') {
      const map = normalizeSkillDesignerMechanicParamMap(draft && draft.mechanicParams, draft || {});
      return map[label] && typeof map[label] === 'object' ? map[label] : {};
    }

    function getSkillDesignerEffectivePrimaryLabel(draft = {}) {
      const primarySub = normalizeSkillUiText(draft && draft.primarySub, '');
      if (primarySub) return primarySub;
      const primaryMain = normalizeSkillUiText(draft && draft.primaryMain, '');
      return getSkillDesignerDefaultPrimarySub(primaryMain, draft && draft.target, draft && draft.type) || primaryMain;
    }

    function estimateSkillDesignerShieldValue(shieldHint, shieldCapText = '') {
      const numericHint = parseSkillDesignerNumericInputValue(shieldHint, 0.8, 4);
      if (numericHint > 5) return Number(numericHint.toFixed(0));
      const activeChar = liveSnapshot && liveSnapshot.activeChar && typeof liveSnapshot.activeChar === 'object' ? liveSnapshot.activeChar : {};
      const vitMax = Number(
        deepGet(activeChar, 'battle.vit_max', 0)
        || deepGet(activeChar, 'battle.vit', 0)
        || deepGet(activeChar, 'vit_max', 0)
        || deepGet(activeChar, 'vit', 0)
        || 0
      );
      const spMax = Number(
        deepGet(activeChar, 'battle.sp_max', 0)
        || deepGet(activeChar, 'battle.sp', 0)
        || deepGet(activeChar, 'sp_max', 0)
        || deepGet(activeChar, 'sp', 0)
        || 0
      );
      const menMax = Number(
        deepGet(activeChar, 'battle.men_max', 0)
        || deepGet(activeChar, 'battle.men', 0)
        || deepGet(activeChar, 'men_max', 0)
        || deepGet(activeChar, 'men', 0)
        || 0
      );
      const capText = normalizeSkillUiText(shieldCapText, '');
      let baseValue = Math.max(vitMax, spMax, menMax, 100);
      if (/榄傚姏/.test(capText)) baseValue = Math.max(spMax, 100);
      else if (/绮剧/.test(capText)) baseValue = Math.max(menMax, 100);
      else if (/鐢熷懡|姘旇|浣撳姏/.test(capText)) baseValue = Math.max(vitMax, 100);
      return Math.max(1, Math.round(baseValue * Math.max(0.05, numericHint)));
    }

    function inferSkillDesignerRecoverProperty(draft = {}, fallback = 'vit') {
      const primaryLabel = getSkillDesignerEffectivePrimaryLabel(draft);
      if (primaryLabel === '绮剧鎭㈠') return 'men';
      if (primaryLabel === '榄傚姏鎭㈠') return 'sp';
      if (primaryLabel === '浣撳姏鎭㈠') return 'vit';
      const attached = normalizeSkillDesignerArray(draft && draft.attachedAttributes);
      if (attached.includes('绮剧')) return 'men';
      if (attached.includes('鐢熷懡')) return 'vit';
      if (attached.includes('鍏?)) return 'vit';
      if (attached.includes('姘?)) return 'vit';
      if (attached.includes('鏈?)) return 'vit';
      return fallback;
    }

    function inferSkillDesignerDamageType(draft = {}) {
      const type = normalizeSkillUiText(draft && draft.type, '');
      const deliveryForm = normalizeSkillUiText(draft && draft.deliveryForm, '');
      if (/绮剧/.test(type)) return '绮剧';
      if (/鍏冪礌/.test(type)) return /杩滅▼|鑼冨洿|寤惰繜/.test(deliveryForm) ? '鍏冪礌杩滅▼' : '鍏冪礌杩戞垬';
      if (/鎺у埗/.test(type)) return '榄傚姏鎺у埗';
      if (/娌荤枟|杈呭姪/.test(type)) return '榄傚姏';
      if (/杩滅▼|鑼冨洿|鏍囪|寤惰繜/.test(deliveryForm)) return '鐗╃悊杩滅▼';
      return '鐗╃悊杩戞垬';
    }

    function estimateSkillDesignerDotDamage(baseEffect = null, ratioHint = 0.2) {
      const ratio = parseSkillDesignerPercentRatio(ratioHint, 0.2);
      const basePower = Number(baseEffect && baseEffect['濞佸姏鍊嶇巼'] || 0);
      if (basePower > 0) return Number((basePower * ratio).toFixed(2));
      return Number(Math.max(1, ratio * 100).toFixed(2));
    }

    function buildSkillDesignerPackedAttributeEffect(mechanism, target, property, action, value, duration = 0, trigger = '绔嬪嵆鐢熸晥') {
      const numericValue = Number(value);
      if (!Number.isFinite(numericValue)) return null;
      return buildSkillDesignerRuntimeObject({
        '鏈哄埗': mechanism,
        '瀵硅薄': target || '鑷韩',
        '灞炴€?: property,
        '鍔ㄤ綔': action,
        '鏁板€?: Number(numericValue.toFixed(4)),
        '鎸佺画': Math.max(0, Number(duration || 0)),
        '瑙﹀彂': trigger || '绔嬪嵆鐢熸晥',
      });
    }

    function buildSkillDesignerPackedRecoverAttributeEffect(target, property, ratio, duration = 0, overtime = false) {
      const numericValue = Number(ratio);
      if (!Number.isFinite(numericValue) || numericValue <= 0) return null;
      return buildSkillDesignerPackedAttributeEffect(
        overtime ? '鎸佺画鎭㈠' : '灞炴€у彉鍖?,
        target,
        property,
        overtime ? '鎸佺画鎭㈠' : '鍔犲€?,
        numericValue,
        overtime ? duration : 0,
        overtime ? '姣忓洖鍚? : '绔嬪嵆鐢熸晥',
      );
    }

    function buildSkillDesignerAttributeEffectsForProperties(target, properties = [], action = '鍊嶇巼鎻愬崌', value = 1, duration = 0) {
      return (Array.isArray(properties) ? properties : [])
        .map(property => buildSkillDesignerPackedAttributeEffect(
          '灞炴€у彉鍖?,
          target,
          property,
          action,
          value,
          duration,
          duration > 0 ? '鐘舵€佹寔缁? : '绔嬪嵆鐢熸晥',
        ))
        .filter(Boolean);
    }

    function buildSkillDesignerStateCarrierEffect(stateName, target, duration = 0, options = {}) {
      return buildSkillDesignerRuntimeObject({
        '鏈哄埗': '鐘舵€佹寕杞?,
        '鐩爣': target || '鑷韩',
        '鐘舵€佸悕绉?: stateName || '鐘舵€佹寕杞?,
        '鎸佺画鍥炲悎': Math.max(0, Number(duration || 0)),
        '闈㈡澘淇敼姣斾緥': options && options.statMods && typeof options.statMods === 'object' ? options.statMods : undefined,
        '璁＄畻灞傛晥鏋?: options && options.calc && typeof options.calc === 'object' ? options.calc : undefined,
        '鐗规畩鏈哄埗鏍囪瘑': options && options.marker ? options.marker : stateName,
      });
    }

    function buildSkillDesignerSystemBaseEffect(skillSource = {}, draft = {}, previewMeta = {}) {
      const effectInfo = ensureSkillDesignerEffectArray(skillSource);
      const existing = effectInfo.systemBase && typeof effectInfo.systemBase === 'object' ? effectInfo.systemBase : {};
      const isPassive = /琚姩/.test(normalizeSkillUiText(draft && draft.type, '')) || toText(previewMeta && previewMeta.scope, '') === 'blood_passive';
      const fallbackCastTime = SKILL_DESIGNER_RUNTIME_CAST_TIME_BY_DELIVERY[normalizeSkillUiText(draft && draft.deliveryForm, '鐩存帴鐢熸晥')] || 10;
      const existingCastTime = Number(existing['cast_time']);
      const skillTypeText = (() => {
        const baseType = normalizeSkillUiText(draft && draft.type, '鎶€鑳?);
        if (isPassive && !/琚姩/.test(baseType)) return `琚姩/${baseType}`;
        return baseType || '鎶€鑳?;
      })();
      const sideEffects = normalizeSkillDesignerSideEffectList(draft && draft['鍓綔鐢ㄥ垪琛?]);
      return buildSkillDesignerRuntimeObject({
        '鏈哄埗': '绯荤粺鍩虹',
        '鎶€鑳芥潵婧?: resolveSkillDesignerSourceCategory(previewMeta),
        '鎶€鑳界被鍨?: skillTypeText,
        '鐩爣妯″瀷': normalizeSkillDesignerTargetForForm(draft && draft.target, '鏁屾柟鍗曚綋'),
        '缁撶畻绛栫暐': ['鏁屾柟缇や綋', '鍙嬫柟缇や綋', '鍏ㄥ満'].includes(normalizeSkillDesignerTargetForForm(draft && draft.target, '鏁屾柟鍗曚綋'))
          ? '鍏ㄧ洰鏍囩嫭绔?
          : '鍗曠洰鏍囩嫭绔?,
        '娑堣€?: isPassive ? '鏃? : (draft && draft.cost) || '鏃?,
        'cast_time': isPassive ? 0 : (Number.isFinite(existingCastTime) ? existingCastTime : fallbackCastTime),
        '鍓綔鐢ㄥ垪琛?: sideEffects,
      });
    }

    function buildSkillDesignerPrimaryEffects(draft = {}, runtimeState = {}) {
      const primaryLabel = getSkillDesignerEffectivePrimaryLabel(draft);
      const target = runtimeState && runtimeState.target ? runtimeState.target : normalizeSkillDesignerTargetForRuntime(draft && draft.target, 'effect');
      const supportTarget = runtimeState && runtimeState.supportTarget ? runtimeState.supportTarget : resolveSkillDesignerSupportTargetForRuntime(draft && draft.target);
      const sharedVisionTarget =
        runtimeState && runtimeState.sharedVisionTarget
          ? runtimeState.sharedVisionTarget
          : resolveSkillDesignerSharedVisionTargetForRuntime(draft && draft.target);
      const grantableTarget = runtimeState && runtimeState.grantableTarget ? runtimeState.grantableTarget : resolveSkillDesignerGrantableMechanismTargetForRuntime(draft && draft.target, primaryLabel);
      const params = getSkillDesignerMechanicParamsForRuntime(draft, primaryLabel);
      const duration = parseSkillDesignerIntegerInputValue(
        params['duration']
        || params['markDuration']
        || params['bindDuration'],
        2,
        0
      );
      switch (primaryLabel) {
        case '鍗曚綋浼ゅ':
        case '缇や綋浼ゅ':
        case '澶氭浼ゅ':
        case '寤惰繜鐖嗗彂':
        case '鎸佺画浼ゅ': {
          const mechanism = primaryLabel === '澶氭浼ゅ'
            ? '澶氭浼ゅ'
            : primaryLabel === '寤惰繜鐖嗗彂'
              ? '寤惰繜鐖嗗彂'
              : primaryLabel === '鎸佺画浼ゅ'
                ? '鎸佺画浼ゅ'
                : '鐩存帴浼ゅ';
          const powerKey = primaryLabel === '澶氭浼ゅ'
            ? 'segmentRatio'
            : primaryLabel === '寤惰繜鐖嗗彂'
              ? 'burstRatio'
              : primaryLabel === '鎸佺画浼ゅ'
                ? 'dotRatio'
                : 'powerRatio';
          const damageEffect = buildSkillDesignerRuntimeObject({
            '鏈哄埗': mechanism,
            '鐩爣': target,
            '濞佸姏鍊嶇巼': parseSkillDesignerFactorInputValue(params[powerKey], primaryLabel === '寤惰繜鐖嗗彂' ? 1.8 : (primaryLabel === '鎸佺画浼ゅ' ? 0.35 : 1.25)),
            '浼ゅ绫诲瀷': inferSkillDesignerDamageType(draft),
            '绌块€忎慨楗?: 0,
            '鍚歌姣斾緥': 0,
            '鍛戒腑娆℃暟': parseSkillDesignerIntegerInputValue(params['hitCount'], 1, 1),
            '娈垫暟': primaryLabel === '澶氭浼ゅ' ? parseSkillDesignerIntegerInputValue(params['segmentCount'], 3, 1) : undefined,
            '姣忔鍊嶇巼': primaryLabel === '澶氭浼ゅ' ? parseSkillDesignerFactorInputValue(params['segmentRatio'], 0.45) : undefined,
            '娈甸棿闂撮殧': primaryLabel === '澶氭浼ゅ' ? normalizeSkillUiText(params['segmentInterval'], '') : undefined,
            '寤惰繜鏃堕暱': primaryLabel === '寤惰繜鐖嗗彂' ? normalizeSkillUiText(params['delayWindow'], '') : undefined,
            '瑙﹀彂鏉′欢': primaryLabel === '寤惰繜鐖嗗彂' ? normalizeSkillUiText(params['triggerRule'], '') : undefined,
            '鎸佺画鍥炲悎': primaryLabel === '鎸佺画浼ゅ' ? parseSkillDesignerIntegerInputValue(params['duration'], 3, 1) : undefined,
            '鍙犲眰涓婇檺': primaryLabel === '鎸佺画浼ゅ' ? parseSkillDesignerIntegerInputValue(params['stackLimit'], 1, 1) : undefined,
            '浣滅敤鑼冨洿': normalizeSkillUiText(params['range'], ''),
          });
          return damageEffect ? [damageEffect] : [];
        }
        case '纭帶':
          return [
            buildSkillDesignerRuntimeObject({
              '鏈哄埗': '纭帶',
              '鐩爣': target,
              '鎸佺画鍥炲悎': parseSkillDesignerIntegerInputValue(params['duration'], 2, 1),
              '鎺у埗寮哄害': parseSkillDesignerFactorInputValue(params['controlPower'], 1.0),
              '瑙ｉ櫎鏉′欢': normalizeSkillUiText(params['breakRule'] || params['hitRule'], ''),
            }),
          ].filter(effect => safeEntries(effect).length);
        case '杞帶': {
          const controlPenalty = parseSkillDesignerPercentRatio(params['slowRatio'], 0.3);
          return [
            buildSkillDesignerRuntimeObject({
              '鏈哄埗': '杞帶',
              '鐩爣': target,
              '鎸佺画鍥炲悎': parseSkillDesignerIntegerInputValue(params['duration'], 2, 1),
              'reaction_penalty': Number(controlPenalty.toFixed(4)),
              'cast_speed_penalty': Number(Math.max(0, controlPenalty * 0.8).toFixed(4)),
              'dodge_penalty': Number(Math.max(0, controlPenalty * 0.8).toFixed(4)),
              '鍛戒腑鏉′欢': normalizeSkillUiText(params['hitRule'], ''),
            }),
          ].filter(effect => safeEntries(effect).length);
        }
        case '浣嶇Щ闄愬埗': {
          const limitPower = parseSkillDesignerPercentRatio(params['limitPower'], 0.2);
          return [
            buildSkillDesignerRuntimeObject({
              '鏈哄埗': '浣嶇Щ闄愬埗',
              '鐩爣': target,
              '鎸佺画鍥炲悎': parseSkillDesignerIntegerInputValue(params['duration'], 2, 1),
              'reaction_penalty': Number(limitPower.toFixed(4)),
              'dodge_penalty': Number(limitPower.toFixed(4)),
              'lock_level': parseSkillDesignerIntegerInputValue(params['limitPower'], 1, 1),
              '灏侀攣鑼冨洿': normalizeSkillUiText(params['lockRange'], ''),
            }),
          ].filter(effect => safeEntries(effect).length);
        }
        case '鑺傚鎵撴柇':
          return [
            buildSkillDesignerRuntimeObject({
              '鏈哄埗': '鎵撴柇',
              '鐩爣': target,
              '涓柇姒傜巼': Math.min(1, parseSkillDesignerNumericInputValue(params['interruptCount'], 1, 4)),
              '鎵撴柇鏃舵満': normalizeSkillUiText(params['interruptWindow'], ''),
              '杩藉姞鍍电洿': parseSkillDesignerNumericInputValue(params['extraDelay'], 0.5, 4),
            }),
          ].filter(effect => safeEntries(effect).length);
        case '灏佹妧':
          return [
            buildSkillDesignerRuntimeObject({
              '鏈哄埗': '灏佹妧',
              '鐩爣': target,
              '鎸佺画鍥炲悎': parseSkillDesignerIntegerInputValue(params['duration'], 2, 1),
              '闄愬埗鑼冨洿': normalizeSkillUiText(params['muteScope'], ''),
            }),
          ].filter(effect => safeEntries(effect).length);
        case '鍗曞睘鎬у墛寮?:
        case '澶氬睘鎬у墛寮?: {
          const properties = resolveSkillDesignerPropertyKeysFromText(
            primaryLabel === '鍗曞睘鎬у墛寮? ? params['debuffAttr'] : params['debuffAttrGroup'],
            { includeFullSet: false }
          );
          return buildSkillDesignerAttributeEffectsForProperties(
            target,
            primaryLabel === '鍗曞睘鎬у墛寮? ? properties.slice(0, 1) : properties,
            '鍊嶇巼鍘嬪埗',
            parseSkillDesignerFactorInputValue(params['reduceRatio'], 0.8),
            parseSkillDesignerIntegerInputValue(params['duration'], 2, 1),
          );
        }
        case '绂佺枟':
          return [
            buildSkillDesignerRuntimeObject({
              '鏈哄埗': '绂佺枟',
              '鐩爣': target,
              '鎸佺画鍥炲悎': parseSkillDesignerIntegerInputValue(params['duration'], 2, 1),
              'heal_block_ratio': parseSkillDesignerPercentRatio(params['banHealRatio'], 1.0),
            }),
          ].filter(effect => safeEntries(effect).length);
        case '娌荤枟鍙嶈浆':
          return [
            buildSkillDesignerRuntimeObject({
              '鏈哄埗': '娌荤枟鍙嶈浆',
              '鐩爣': target,
              '鎸佺画鍥炲悎': parseSkillDesignerIntegerInputValue(params['duration'], 2, 1),
              '鍙嶈浆姣斾緥': parseSkillDesignerFactorInputValue(params['invertRatio'], 1.0),
            }),
          ].filter(effect => safeEntries(effect).length);
        case '娑堣€楁彁楂?:
          return [
            buildSkillDesignerPackedAttributeEffect(
              '娑堣€椾慨姝?,
              target,
              '娑堣€?,
              '娑堣€楁彁楂?,
              parseSkillDesignerFactorInputValue(params['gainRatio'], 1.2),
              parseSkillDesignerIntegerInputValue(params['duration'], 2, 1),
              '鐘舵€佹寔缁?,
            ),
          ].filter(Boolean);
        case '鍓嶆憞鎷夐暱':
          return [
            buildSkillDesignerPackedAttributeEffect(
              '鍓嶆憞淇',
              target,
              '鍓嶆憞',
              '鍓嶆憞鎷夐暱',
              parseSkillDesignerFactorInputValue(params['gainRatio'], 1.2),
              parseSkillDesignerIntegerInputValue(params['duration'], 2, 1),
              '鐘舵€佹寔缁?,
            ),
          ].filter(Boolean);
        case '鎺屾帶鍘嬪埗':
          return [
            buildSkillDesignerPackedAttributeEffect(
              '鎺屾帶淇',
              target,
              '鎺屾帶',
              '鍊嶇巼鍘嬪埗',
              parseSkillDesignerFactorInputValue(params['gainRatio'], 0.8),
              parseSkillDesignerIntegerInputValue(params['duration'], 2, 1),
              '鐘舵€佹寔缁?,
            ),
          ].filter(Boolean);
        case '閫熷害鍘嬪埗':
          return [
            buildSkillDesignerPackedAttributeEffect(
              '閫熷害淇',
              target,
              '閫熷害',
              '鍊嶇巼鍘嬪埗',
              parseSkillDesignerFactorInputValue(params['gainRatio'], 0.85),
              parseSkillDesignerIntegerInputValue(params['duration'], 2, 1),
              '鐘舵€佹寔缁?,
            ),
          ].filter(Boolean);
        case '鍗曞睘鎬у鐩?:
        case '澶氬睘鎬у鐩?:
        case '鍏ㄥ睘鎬у鐩?: {
          const properties = primaryLabel === '鍏ㄥ睘鎬у鐩?
            ? resolveSkillDesignerPropertyKeysFromText('', { includeFullSet: true })
            : resolveSkillDesignerPropertyKeysFromText(
              primaryLabel === '鍗曞睘鎬у鐩? ? params['buffAttr'] : params['buffAttrGroup'],
              { includeFullSet: false }
            );
          const gainKey = primaryLabel === '鍏ㄥ睘鎬у鐩? ? 'allGainRatio' : 'gainRatio';
          const gainValue = parseSkillDesignerFactorInputValue(
            params[gainKey],
            primaryLabel === '鍏ㄥ睘鎬у鐩? ? 1.15 : 1.3,
            { plusBase: true },
          );
          return buildSkillDesignerAttributeEffectsForProperties(
            target,
            primaryLabel === '鍗曞睘鎬у鐩? ? properties.slice(0, 1) : properties,
            '鍊嶇巼鎻愬崌',
            gainValue,
            parseSkillDesignerIntegerInputValue(params['duration'], primaryLabel === '鍏ㄥ睘鎬у鐩? ? 2 : 3, 1),
          );
        }
        case '娑堣€楅檷浣?:
          return [
            buildSkillDesignerPackedAttributeEffect(
              '娑堣€椾慨姝?,
              target,
              '娑堣€?,
              '娑堣€楅檷浣?,
              parseSkillDesignerFactorInputValue(params['gainRatio'], 0.85),
              parseSkillDesignerIntegerInputValue(params['duration'], 3, 1),
              '鐘舵€佹寔缁?,
            ),
          ].filter(Boolean);
        case '鍓嶆憞缂╃煭':
          return [
            buildSkillDesignerPackedAttributeEffect(
              '鍓嶆憞淇',
              target,
              '鍓嶆憞',
              '鍓嶆憞缂╃煭',
              parseSkillDesignerFactorInputValue(params['gainRatio'], 0.85),
              parseSkillDesignerIntegerInputValue(params['duration'], 2, 1),
              '鐘舵€佹寔缁?,
            ),
          ].filter(Boolean);
        case '鎺屾帶鎻愬崌':
          return [
            buildSkillDesignerPackedAttributeEffect(
              '鎺屾帶淇',
              target,
              '鎺屾帶',
              '鍊嶇巼鎻愬崌',
              parseSkillDesignerFactorInputValue(params['gainRatio'], 1.2),
              parseSkillDesignerIntegerInputValue(params['duration'], 3, 1),
              '鐘舵€佹寔缁?,
            ),
          ].filter(Boolean);
        case '閫熷害鎻愬崌':
          return [
            buildSkillDesignerPackedAttributeEffect(
              '閫熷害淇',
              target,
              '閫熷害',
              '鍊嶇巼鎻愬崌',
              parseSkillDesignerFactorInputValue(params['gainRatio'], 1.15),
              parseSkillDesignerIntegerInputValue(params['duration'], 2, 1),
              '鐘舵€佹寔缁?,
            ),
          ].filter(Boolean);
        case '鎶ょ浘':
          return [
            buildSkillDesignerRuntimeObject({
              '鏈哄埗': '鎶ょ浘',
              '鐩爣': target,
              '鎶ょ浘鍊?: estimateSkillDesignerShieldValue(params['shieldRatio'], params['shieldCap']),
              '鎸佺画鍥炲悎': parseSkillDesignerIntegerInputValue(params['duration'], 2, 1),
              '鎶ょ浘鏉ユ簮': normalizeSkillUiText(params['shieldCap'], ''),
            }),
          ].filter(effect => safeEntries(effect).length);
        case '鍑忎激':
          return [
            buildSkillDesignerRuntimeObject({
              '鏈哄埗': '鍑忎激',
              '鐩爣': target,
              '鎸佺画鍥炲悎': parseSkillDesignerIntegerInputValue(params['duration'], 2, 1),
              '鍑忎激姣斾緥': parseSkillDesignerPercentRatio(params['reduceRatio'], 0.35),
              '瑕嗙洊绫诲瀷': normalizeSkillUiText(params['damageType'], ''),
            }),
          ].filter(effect => safeEntries(effect).length);
        case '鏍兼尅/鎶垫秷':
          return [
            buildSkillDesignerRuntimeObject({
              '鏈哄埗': '鏍兼尅',
              '鐩爣': target,
              '鎸佺画鍥炲悎': 2,
              '鎶垫秷娆℃暟': parseSkillDesignerIntegerInputValue(params['blockCount'], 1, 1),
              '鍗曟涓婇檺': normalizeSkillUiText(params['blockCap'], ''),
              '瑙﹀彂鏉′欢': normalizeSkillUiText(params['triggerRule'], ''),
            }),
          ].filter(effect => safeEntries(effect).length);
        case '闇镐綋':
          return [
            buildSkillDesignerRuntimeObject({
              '鏈哄埗': '闇镐綋',
              '鐩爣': target,
              '鎸佺画鍥炲悎': parseSkillDesignerIntegerInputValue(params['duration'], 2, 1),
              '鍏嶆帶绾у埆': normalizeSkillUiText(params['immuneLevel'], ''),
              '鍑忎激姣斾緥': parseSkillDesignerPercentRatio(params['reduceRatio'], 0.2),
            }),
          ].filter(effect => safeEntries(effect).length);
        case '鍏嶆/閿佽':
          return [
            buildSkillDesignerRuntimeObject({
              '鏈哄埗': '鍏嶆',
              '鐩爣': target,
              '鎸佺画鍥炲悎': 3,
              '瑙﹀彂娆℃暟': parseSkillDesignerIntegerInputValue(params['cooldown'], 1, 1),
              '瑙﹀彂闃堝€?: normalizeSkillUiText(params['triggerThreshold'], ''),
              '閿佽涓嬮檺': normalizeSkillUiText(params['lockBloodFloor'], ''),
            }),
          ].filter(effect => safeEntries(effect).length);
        case '鏃犳晫閲戣韩':
          return [
            buildSkillDesignerRuntimeObject({
              '鏈哄埗': '鏃犳晫閲戣韩',
              '鐩爣': grantableTarget,
              '鎸佺画鍥炲悎': parseSkillDesignerIntegerInputValue(params['duration'], 2, 1),
              '姣忔棩瑙﹀彂涓婇檺': parseSkillDesignerIntegerInputValue(params['dailyLimit'], 3, 1),
              '鍏嶇柅浣嶉樁闃堝€?: parseSkillDesignerNumericInputValue(params['tierThreshold'], 100, 1),
              'damage_reduction': parseSkillDesignerPercentRatio(params['reduceRatio'], 0.18),
            }),
          ].filter(effect => safeEntries(effect).length);
        case '浼ゅ鍙嶅皠':
          return [
            buildSkillDesignerRuntimeObject({
              '鏈哄埗': '浼ゅ鍙嶅皠',
              '鐩爣': grantableTarget,
              '鎸佺画鍥炲悎': parseSkillDesignerIntegerInputValue(params['duration'], 2, 1),
              '鍙嶅皠姣斾緥': parseSkillDesignerPercentRatio(params['reflectRatio'], 0.25),
              '瑙﹀彂鏉′欢': normalizeSkillUiText(params['reflectRule'], ''),
            }),
          ].filter(effect => safeEntries(effect).length);
        case '浼ゅ鍒嗘憡':
          return [
            buildSkillDesignerRuntimeObject({
              '鏈哄埗': '浼ゅ鍒嗘憡',
              '鐩爣': resolveSkillDesignerMechanismTargetForRuntime(draft && draft.target, '浼ゅ鍒嗘憡'),
              '鎸佺画鍥炲悎': parseSkillDesignerIntegerInputValue(params['duration'], 2, 1),
              '鍒嗘憡姣斾緥': parseSkillDesignerPercentRatio(params['shareRatio'], 0.35),
              '鍒嗘憡浜烘暟': parseSkillDesignerIntegerInputValue(params['shareCount'], 1, 1),
            }),
          ].filter(effect => safeEntries(effect).length);
        case '鏇胯韩鎶垫秷':
          return [
            buildSkillDesignerRuntimeObject({
              '鏈哄埗': '鏇胯韩鎶垫秷',
              '鐩爣': grantableTarget,
              '鎸佺画鍥炲悎': parseSkillDesignerIntegerInputValue(params['duration'], 2, 1),
              '鎶垫秷娆℃暟': parseSkillDesignerIntegerInputValue(params['substituteCount'], 1, 1),
              '瑙﹀彂鏉′欢': normalizeSkillUiText(params['substituteRule'], ''),
            }),
          ].filter(effect => safeEntries(effect).length);
        case '澶嶈嫃':
          return [
            buildSkillDesignerRuntimeObject({
              '鏈哄埗': '澶嶈嫃',
              '鐩爣': resolveSkillDesignerMechanismTargetForRuntime(draft && draft.target, '澶嶈嫃'),
              '鎸佺画鍥炲悎': parseSkillDesignerIntegerInputValue(params['duration'], 3, 1),
              '澶嶈嫃娆℃暟': parseSkillDesignerIntegerInputValue(params['reviveCount'], 1, 1),
              '澶嶈嫃鍥炶姣斾緥': parseSkillDesignerPercentRatio(params['reviveHealRatio'], 0.25),
            }),
          ].filter(effect => safeEntries(effect).length);
        case '浣撳姏鎭㈠':
          return [
            buildSkillDesignerPackedRecoverAttributeEffect(
              target,
              'vit',
              parseSkillDesignerPercentRatio(params['recoverRatio'], 0.35) * parseSkillDesignerIntegerInputValue(params['repeatCount'], 1, 1),
              0,
              false,
            ),
          ].filter(Boolean);
        case '榄傚姏鎭㈠':
          return [
            buildSkillDesignerPackedRecoverAttributeEffect(
              target,
              'sp',
              parseSkillDesignerPercentRatio(params['recoverRatio'], 0.35) * parseSkillDesignerIntegerInputValue(params['repeatCount'], 1, 1),
              0,
              false,
            ),
          ].filter(Boolean);
        case '绮剧鎭㈠':
          return [
            buildSkillDesignerPackedRecoverAttributeEffect(
              target,
              'men',
              parseSkillDesignerPercentRatio(params['recoverRatio'], 0.35) * parseSkillDesignerIntegerInputValue(params['repeatCount'], 1, 1),
              0,
              false,
            ),
          ].filter(Boolean);
        case '鎸佺画鎭㈠':
          return [
            buildSkillDesignerPackedRecoverAttributeEffect(
              target,
              inferSkillDesignerRecoverProperty(draft, 'vit'),
              parseSkillDesignerPercentRatio(params['recoverRatio'], 0.2),
              parseSkillDesignerIntegerInputValue(params['duration'], 3, 1),
              true,
            ),
          ].filter(Boolean);
        case '鍑€鍖?瑙ｆ帶':
          return [
            buildSkillDesignerRuntimeObject({
              '鏈哄埗': '瑙ｆ帶',
              '鐩爣': grantableTarget,
              '娓呴櫎灞傜骇': parseSkillDesignerIntegerInputValue(params['cleanseCount'], 2, 1),
              '娓呴櫎浼樺厛绾?: normalizeSkillUiText(params['cleansePriority'], ''),
              '闄勫甫鏀剁泭': normalizeSkillUiText(params['extraGain'], ''),
            }),
          ].filter(effect => safeEntries(effect).length);
        case '鎰熺煡骞叉壈': {
          const disturbRatio = parseSkillDesignerPercentRatio(params['disturbPower'], 0.12);
          return [
            buildSkillDesignerStateCarrierEffect(
              '鎰熺煡骞叉壈',
              target,
              parseSkillDesignerIntegerInputValue(params['duration'], 2, 1),
              {
                calc: {
                  'hit_penalty': Number(disturbRatio.toFixed(4)),
                  'reaction_penalty': Number(Math.max(0, disturbRatio * 0.7).toFixed(4)),
                },
                marker: '鎰熺煡骞叉壈',
              },
            ),
          ].filter(effect => safeEntries(effect).length);
        }
        case '鏍囪閿佸畾':
          return [
            buildSkillDesignerRuntimeObject({
              '鏈哄埗': '鏍囪閿佸畾',
              '鐩爣': target,
              '鎸佺画鍥炲悎': parseSkillDesignerIntegerInputValue(params['markDuration'], 2, 1),
              '鍒ゅ畾灞炴€?: 'men_max',
              '鍒ゅ畾闃堝€?: 1.0,
              '鎴愬姛鍙傛暟': {
                'hit_bonus': 0.1,
                'dodge_penalty': 0.1,
                'lock_level': parseSkillDesignerIntegerInputValue(params['targetCap'], 1, 1),
              },
              '澶辫触鍙傛暟': { 'hit_bonus': 0.03, 'lock_level': 0 },
              '杩借釜瑙勫垯': normalizeSkillUiText(params['trackingRule'], ''),
            }),
          ].filter(effect => safeEntries(effect).length);
        case '鍏变韩瑙嗛噹':
          return [
            buildSkillDesignerRuntimeObject({
              '鏈哄埗': '鍏变韩瑙嗛噹',
              '鐩爣': sharedVisionTarget,
              '鎸佺画鍥炲悎': parseSkillDesignerIntegerInputValue(params['duration'], 3, 1),
              'reaction_bonus': 0.1,
              'hit_bonus': 0.1,
              'lock_level': 1,
              '鍏变韩娣卞害': normalizeSkillUiText(params['infoDepth'], ''),
              '鍏变韩鑼冨洿': normalizeSkillUiText(params['shareRange'], ''),
            }),
          ].filter(effect => safeEntries(effect).length);
        case '骞诲': {
          const illusionPower = parseSkillDesignerFactorInputValue(params['illusionPower'], 1.1);
          const penalty = Number(Math.max(0.05, Math.min(0.45, illusionPower - 1)).toFixed(4));
          const agiRatio = Number(Math.max(0.55, 1 - penalty).toFixed(4));
          return [
            buildSkillDesignerRuntimeObject({
              '鏈哄埗': '骞诲',
              '鐩爣': target,
              '鎸佺画鍥炲悎': parseSkillDesignerIntegerInputValue(params['duration'], 2, 1),
              '鍒ゅ畾灞炴€?: 'men_max',
              '鍒ゅ畾闃堝€?: 1.05,
              '鎴愬姛鍙傛暟': {
                '闈㈡澘淇敼姣斾緥': { 'agi': agiRatio },
                'reaction_penalty': penalty,
                'skip_turn': illusionPower >= 1.35,
              },
              '澶辫触鍙傛暟': {
                'reaction_penalty': Number((penalty * 0.35).toFixed(4)),
              },
              '骞诲鑼冨洿': normalizeSkillUiText(params['illusionRange'], ''),
            }),
          ].filter(effect => safeEntries(effect).length);
        }
        case '鍌湢':
          return [
            buildSkillDesignerRuntimeObject({
              '鏈哄埗': '鍌湢',
              '鐩爣': target,
              '鎸佺画鍥炲悎': parseSkillDesignerIntegerInputValue(params['duration'], 2, 1),
              '鍒ゅ畾灞炴€?: 'men_max',
              '鍒ゅ畾闃堝€?: 1.1,
              '鎴愬姛鍙傛暟': { 'skip_turn': true, 'cannot_react': true },
              '澶辫触鍙傛暟': {},
              '鍞ら啋鏉′欢': normalizeSkillUiText(params['wakeRule'], ''),
              '鍛戒腑鏉′欢': normalizeSkillUiText(params['hitRule'], ''),
            }),
          ].filter(effect => safeEntries(effect).length);
        case '璁ょ煡鎵洸': {
          const twistPower = parseSkillDesignerPercentRatio(params['twistPower'], 0.18);
          return [
            buildSkillDesignerStateCarrierEffect(
              '璁ょ煡鎵洸',
              target,
              parseSkillDesignerIntegerInputValue(params['duration'], 2, 1),
              {
                calc: {
                  'hit_penalty': Number(twistPower.toFixed(4)),
                  'reaction_penalty': Number(Math.max(0, twistPower * 0.8).toFixed(4)),
                  'dodge_penalty': Number(Math.max(0, twistPower * 0.5).toFixed(4)),
                },
                marker: '璁ょ煡鎵洸',
              },
            ),
          ].filter(effect => safeEntries(effect).length);
        }
        case '鑷韩浣嶇Щ':
          return [
            buildSkillDesignerRuntimeObject({
              '鏈哄埗': '鑷韩浣嶇Щ',
              '鐩爣': grantableTarget,
              '鎸佺画鍥炲悎': 1,
              'dodge_bonus': 0.12,
              'attacker_speed_bonus': 0.08,
              'reaction_bonus': 0.08,
              '浣嶇Щ璺濈': normalizeSkillUiText(params['moveDistance'], ''),
            }),
          ].filter(effect => safeEntries(effect).length);
        case '寮哄埗浣嶇Щ':
          return [
            buildSkillDesignerRuntimeObject({
              '鏈哄埗': '寮哄埗浣嶇Щ',
              '鐩爣': target,
              '鎸佺画鍥炲悎': 1,
              'dodge_penalty': 0.12,
              'reaction_penalty': 0.08,
              'lock_level': 1,
              '浣嶇Щ璺濈': normalizeSkillUiText(params['moveDistance'], ''),
              '瑙﹀彂娆℃暟': parseSkillDesignerIntegerInputValue(params['repeatCount'], 1, 1),
            }),
          ].filter(effect => safeEntries(effect).length);
        case '浣嶇Щ浜ゆ崲':
          return [
            buildSkillDesignerRuntimeObject({
              '鏈哄埗': '浣嶇Щ浜ゆ崲',
              '鐩爣': target,
              '鎸佺画鍥炲悎': parseSkillDesignerIntegerInputValue(params['duration'], 2, 1),
              'dodge_penalty': 0.15,
              'reaction_penalty': 0.12,
              'lock_level': 1,
              '浜ゆ崲璺濈': normalizeSkillUiText(params['exchangeRange'], ''),
              '浜ゆ崲鏉′欢': normalizeSkillUiText(params['triggerRule'], ''),
            }),
          ].filter(effect => safeEntries(effect).length);
        case '杩藉嚮浣嶇Щ':
          return [
            buildSkillDesignerRuntimeObject({
              '鏈哄埗': '杩藉嚮浣嶇Щ',
              '鐩爣': grantableTarget,
              '鎸佺画鍥炲悎': 1,
              'attacker_speed_bonus': 0.12,
              'hit_bonus': 0.1,
              'final_damage_mult': parseSkillDesignerFactorInputValue(params['extraRatio'], 1.3, { plusBase: true }),
              '杩藉嚮璺濈': normalizeSkillUiText(params['moveDistance'], ''),
              '杩藉嚮绐楀彛': normalizeSkillUiText(params['followWindow'], ''),
            }),
          ].filter(effect => safeEntries(effect).length);
        case '鑴辩浣嶇Щ':
          return [
            buildSkillDesignerRuntimeObject({
              '鏈哄埗': '鑴辩浣嶇Щ',
              '鐩爣': grantableTarget,
              '鎸佺画鍥炲悎': 1,
              'dodge_bonus': 0.15,
              'cast_speed_bonus': 0.12,
              'reaction_bonus': 0.12,
              '鑴辩璺濈': normalizeSkillUiText(params['moveDistance'], ''),
              '鑴辩鏉′欢': normalizeSkillUiText(params['escapeRule'], ''),
              '鑴辩鏀剁泭': normalizeSkillUiText(params['extraGain'], ''),
            }),
          ].filter(effect => safeEntries(effect).length);
        case '鍒嗚韩': {
          const cloneType = normalizeSkillUiText(params['cloneType'], '鐗╃悊鍒嗚韩') || '鐗╃悊鍒嗚韩';
          const cloneCount = parseSkillDesignerIntegerInputValue(params['cloneCount'], 2, 1);
          const stealthRatio = parseSkillDesignerPercentRatio(params['stealthRatio'], 0.45);
          const inheritRatio = parseSkillDesignerPercentRatio(params['inheritRatio'], 0.55);
          const duration = parseSkillDesignerIntegerInputValue(params['duration'], 2, 1);
          const cloneName = normalizeSkillUiText(params['cloneName'], '');
          const safeTarget = resolveSkillDesignerMechanismTargetForRuntime(draft && draft.target, '鍒嗚韩');
          const statusName = cloneName ? `${cloneType}路${cloneName}` : cloneType;
          if (cloneType === '绮剧鍔涘垎韬?) {
            return [
              buildSkillDesignerRuntimeObject({
                '鏈哄埗': '鍒嗚韩',
                '鐩爣': safeTarget,
                '鐘舵€佸悕绉?: statusName,
                '鎸佺画鍥炲悎': duration,
                '鍒嗚韩绫诲瀷': cloneType,
                '鍒嗚韩鏁伴噺': cloneCount,
                '闅愯斀搴?: stealthRatio,
                '瀹炲姏缁ф壙姣斾緥': inheritRatio,
                '鍒嗚韩鍚嶇О': cloneName,
                'reaction_bonus': Number(Math.min(0.28, 0.04 + stealthRatio * 0.16 + inheritRatio * 0.08).toFixed(2)),
                'hit_bonus': Number(Math.min(0.3, 0.04 + inheritRatio * 0.15 + cloneCount * 0.03).toFixed(2)),
                'lock_level': Math.min(3, Math.max(1, Math.round(1 + inheritRatio * 1.2 + stealthRatio * 0.8))),
                'damage_reduction': Number(Math.min(0.18, 0.02 + stealthRatio * 0.05 + cloneCount * 0.01).toFixed(2)),
              }),
            ].filter(effect => safeEntries(effect).length);
          }
          return [
            buildSkillDesignerRuntimeObject({
              '鏈哄埗': '鍒嗚韩',
              '鐩爣': safeTarget,
              '鐘舵€佸悕绉?: statusName,
              '鎸佺画鍥炲悎': duration,
              '鍒嗚韩绫诲瀷': cloneType,
              '鍒嗚韩鏁伴噺': cloneCount,
              '闅愯斀搴?: stealthRatio,
              '瀹炲姏缁ф壙姣斾緥': inheritRatio,
              '鍒嗚韩鍚嶇О': cloneName,
              'dodge_bonus': Number(Math.min(0.35, 0.05 + stealthRatio * 0.18 + inheritRatio * 0.08 + cloneCount * 0.03).toFixed(2)),
              'attacker_speed_bonus': Number(Math.min(0.24, 0.03 + inheritRatio * 0.12 + cloneCount * 0.02).toFixed(2)),
              'damage_reduction': Number(Math.min(0.22, 0.02 + stealthRatio * 0.08 + cloneCount * 0.015).toFixed(2)),
              'final_damage_mult': Number(Math.min(1.28, 1 + inheritRatio * 0.12 + Math.max(0, cloneCount - 1) * 0.04).toFixed(2)),
            }),
          ].filter(effect => safeEntries(effect).length);
        }
        case '澶嶅埗':
          return [
            buildSkillDesignerRuntimeObject({
              '鏈哄埗': '澶嶅埗',
              '鐩爣': target,
              '鎸佺画鍥炲悎': parseSkillDesignerIntegerInputValue(params['duration'], 2, 1),
              '澶嶅埗绫诲瀷': /鍑忕泭|debuff/i.test(toText(params['copyTarget'], '')) ? 'debuff' : 'buff',
              '澶嶅埗鏁伴噺': 1,
              '淇濈湡搴?: parseSkillDesignerFactorInputValue(params['fidelity'], 0.8),
            }),
          ].filter(effect => safeEntries(effect).length);
        case '鍙嶅埗':
          return [
            buildSkillDesignerRuntimeObject({
              '鏈哄埗': '鍙嶅埗',
              '鐩爣': grantableTarget,
              '鎸佺画鍥炲悎': parseSkillDesignerIntegerInputValue(params['duration'], 2, 1),
              '鍙嶅嚮鍊嶇巼': parseSkillDesignerFactorInputValue(params['counterRatio'], 1.0),
              'damage_reduction': 0.15,
              '鍙嶅埗瀵硅薄': normalizeSkillUiText(params['counterTarget'], ''),
              '瑙﹀彂鏉′欢': normalizeSkillUiText(params['triggerRule'], ''),
            }),
          ].filter(effect => safeEntries(effect).length);
        case '杞寲': {
          const convertPath = normalizeSkillUiText(params['convertPath'], '');
          const mechanism = /鍥炲.*浼ゅ|healing?.*damage/i.test(convertPath) ? '鍥炲杞激瀹? : '浼ゅ杞洖澶?;
          return [
            buildSkillDesignerRuntimeObject({
              '鏈哄埗': mechanism,
              '鐩爣': resolveSkillDesignerMechanismTargetForRuntime(draft && draft.target, mechanism),
              '杞崲姣斾緥': parseSkillDesignerPercentRatio(params['convertRatio'], 0.6),
              '杞寲鏂瑰悜': convertPath,
            }),
          ].filter(effect => safeEntries(effect).length);
        }
        case '鐘舵€佷氦鎹?:
          return [
            buildSkillDesignerRuntimeObject({
              '鏈哄埗': '鐘舵€佷氦鎹?,
              '鐩爣': target,
              '浜ゆ崲鏁伴噺': parseSkillDesignerIntegerInputValue(params['exchangeCount'], 1, 1),
              '浼樺厛绛栫暐': normalizeSkillUiText(params['exchangeTarget'], '鑷韩璐熼潰鎹㈢洰鏍囨闈?),
              '浜ゆ崲鏉′欢': normalizeSkillUiText(params['triggerRule'], ''),
            }),
          ].filter(effect => safeEntries(effect).length);
        case '鐘舵€佽浆绉?:
          return [
            buildSkillDesignerRuntimeObject({
              '鏈哄埗': '鐘舵€佽浆绉?,
              '鐩爣': target,
              '杞Щ绫诲瀷': normalizeSkillUiText(params['transferMode'], '鑷韩璐熼潰->鐩爣'),
              '瑙﹀彂鏉′欢': normalizeSkillUiText(params['triggerRule'], ''),
            }),
          ].filter(effect => safeEntries(effect).length);
        case '寮哄埗缁戝畾/閿佸畾':
          return [
            buildSkillDesignerRuntimeObject({
              '鏈哄埗': '寮哄埗缁戝畾/閿佸畾',
              '鐩爣': target,
              '鎸佺画鍥炲悎': parseSkillDesignerIntegerInputValue(params['bindDuration'], 2, 1),
              'dodge_penalty': 0.1,
              'reaction_penalty': 0.1,
              'lock_level': parseSkillDesignerIntegerInputValue(params['targetCap'], 1, 1),
              '瑙ｉ櫎鏉′欢': normalizeSkillUiText(params['releaseRule'], ''),
            }),
          ].filter(effect => safeEntries(effect).length);
        case '鏉′欢瑙﹀彂': {
          const triggerRule = normalizeSkillUiText(params['triggerRule'], '');
          const judgeKey = /浣庤|鐢熷懡|琛€閲?.test(triggerRule) ? 'hp_ratio' : 'men_max';
          const judgeThreshold = judgeKey === 'hp_ratio' ? 0.5 : 1.0;
          return [
            buildSkillDesignerRuntimeObject({
              '鏈哄埗': '鏉′欢瑙﹀彂',
              '鐩爣': target,
              '鍒ゅ畾灞炴€?: judgeKey,
              '鍒ゅ畾闃堝€?: judgeThreshold,
              '鎴愬姛鍙傛暟': {
                'final_damage_mult': 1.2,
                'hit_bonus': 0.1,
              },
              '澶辫触鍙傛暟': {},
              '瑙﹀彂鏉′欢': triggerRule,
              '瑙﹀彂娆℃暟': parseSkillDesignerIntegerInputValue(params['triggerCount'], 1, 1),
              '瑙﹀彂缁撴灉': normalizeSkillUiText(params['triggerResult'], ''),
            }),
          ].filter(effect => safeEntries(effect).length);
        }
        case '瑙勫垯鏀瑰啓': {
          const rewriteDepth = normalizeSkillUiText(params['rewriteDepth'], '');
          const effects = [
            buildSkillDesignerRuntimeObject({
              '鏈哄埗': '楂樻尝鍔ㄩ殢鏈哄€?,
              '鐩爣': '鑷韩',
              '娉㈠姩涓嬮檺': 0.6,
              '娉㈠姩涓婇檺': rewriteDepth.includes('瀹屾暣') ? 1.8 : 1.5,
            }),
          ];
          if (/瀹屾暣|瑕嗙洊/.test(rewriteDepth)) {
            effects.push(buildSkillDesignerRuntimeObject({ '鏈哄埗': '闅忔満鐩爣', '鐩爣': '闅忔満鐩爣', '鍋忕Щ姒傜巼': 0.35 }));
          }
          if (/瀹屾暣/.test(rewriteDepth)) {
            effects.push(buildSkillDesignerRuntimeObject({ '鏈哄埗': '鑷韩涔熷彈褰卞搷', '鐩爣': '鑷韩', '鍙屽悜鐢熸晥': true }));
          }
          return effects.filter(effect => safeEntries(effect).length);
        }
        case '寮曠垎鎸佺画浼ゅ':
          return [
            buildSkillDesignerRuntimeObject({
              '鏈哄埗': '寮曠垎鎸佺画浼ゅ',
              '鐩爣': target,
              '寮曠垎鍊嶇巼': parseSkillDesignerFactorInputValue(params['detonateRatio'], 1.2),
              '娑堣€楁寔缁激瀹?: !/涓嶆秷鑰梶淇濈暀/.test(normalizeSkillUiText(params['consumeMode'], '')),
            }),
          ].filter(effect => safeEntries(effect).length);
        case '鏂╃浘':
          return [
            buildSkillDesignerRuntimeObject({
              '鏈哄埗': '鏂╃浘',
              '鐩爣': target,
              '鏂╃浘鍊嶇巼': parseSkillDesignerFactorInputValue(params['shieldBreakRatio'], 0.6),
            }),
          ].filter(effect => safeEntries(effect).length);
        case '绐冨彇鎶ょ浘':
          return [
            buildSkillDesignerRuntimeObject({
              '鏈哄埗': '绐冨彇鎶ょ浘',
              '鐩爣': target,
              '鎸佺画鍥炲悎': parseSkillDesignerIntegerInputValue(params['duration'], 2, 1),
              '绐冪浘姣斾緥': parseSkillDesignerPercentRatio(params['shieldStealRatio'], 0.45),
            }),
          ].filter(effect => safeEntries(effect).length);
        case '鍚炲櫖':
          return [
            buildSkillDesignerRuntimeObject({
              '鏈哄埗': '鍚炲櫖',
              '鐩爣': target,
              '璧勬簮绫诲瀷': normalizeSkillUiText(params['resourceType'], '榄傚姏'),
              '澶哄彇姣斾緥': parseSkillDesignerPercentRatio(params['drainRatio'], 0.18),
              '杞寲姣斾緥': parseSkillDesignerFactorInputValue(params['convertRatio'], 1),
            }),
          ].filter(effect => safeEntries(effect).length);
        case '鑳藉姏鍏变韩':
          return [
            buildSkillDesignerRuntimeObject({
              '鏈哄埗': '鑳藉姏鍏变韩',
              '鐩爣': target,
              '璧勬簮绫诲瀷': normalizeSkillUiText(params['resourceType'], '榄傚姏'),
              '鍙嶇亴姣斾緥': parseSkillDesignerPercentRatio(params['refeedRatio'], 0.2),
            }),
          ].filter(effect => safeEntries(effect).length);
        case '鏈哄埗鎶规秷':
          return [
            buildSkillDesignerRuntimeObject({
              '鏈哄埗': '鏈哄埗鎶规秷',
              '鐩爣': target,
              '鎶规秷鐩爣': normalizeSkillUiText(params['suppressTarget'], '澶嶈嫃'),
              '鎶规秷鏂瑰紡': normalizeSkillUiText(params['suppressMode'], '绉婚櫎骞跺皝閿?),
              '鎸佺画鍥炲悎': parseSkillDesignerIntegerInputValue(params['duration'], 2, 1),
            }),
          ].filter(effect => safeEntries(effect).length);
        default:
          return [];
      }
    }

    function buildSkillDesignerSecondaryEffects(draft = {}, runtimeState = {}, label = '') {
      const effects = [];
      const params = getSkillDesignerMechanicParamsForRuntime(draft, label);
      const offensiveTarget = runtimeState && runtimeState.target ? runtimeState.target : normalizeSkillDesignerTargetForRuntime(draft && draft.target, 'effect');
      const supportTarget = runtimeState && runtimeState.supportTarget ? runtimeState.supportTarget : resolveSkillDesignerSupportTargetForRuntime(draft && draft.target);
      const sharedVisionTarget = runtimeState && runtimeState.sharedVisionTarget ? runtimeState.sharedVisionTarget : resolveSkillDesignerSharedVisionTargetForRuntime(draft && draft.target);
      const grantableTarget = runtimeState && runtimeState.grantableTarget ? runtimeState.grantableTarget : resolveSkillDesignerGrantableMechanismTargetForRuntime(draft && draft.target, label);
      const primaryDamageEffect =
        runtimeState && runtimeState.primaryDamageEffect && typeof runtimeState.primaryDamageEffect === 'object'
          ? runtimeState.primaryDamageEffect
          : null;
      switch (label) {
        case '绌块€?:
          if (primaryDamageEffect) {
            primaryDamageEffect['绌块€忎慨楗?] = parseSkillDesignerPercentPoints(params['penetrationRatio'], primaryDamageEffect['绌块€忎慨楗?] || 25);
            primaryDamageEffect['绌块€忓璞?] = normalizeSkillUiText(params['penetrationTarget'], '');
          }
          break;
        case '鍚歌':
          if (primaryDamageEffect) {
            primaryDamageEffect['鍚歌姣斾緥'] = parseSkillDesignerPercentRatio(params['lifestealRatio'], 0.2);
            primaryDamageEffect['鍚稿彇璧勬簮'] = normalizeSkillUiText(params['resourceType'], '');
          }
          break;
        case '鏂╂潃琛ヤ激':
          effects.push(buildSkillDesignerRuntimeObject({
            '鏈哄埗': '鏂╂潃琛ヤ激',
            '鐩爣': offensiveTarget,
            '鍒ゅ畾灞炴€?: 'hp_ratio',
            '鍒ゅ畾闃堝€?: parseSkillDesignerPercentRatio(params['executeLine'], 0.25),
            '鎴愬姛鍙傛暟': { 'final_damage_mult': parseSkillDesignerFactorInputValue(params['bonusRatio'], 1.5, { plusBase: true }) },
            '澶辫触鍙傛暟': {},
          }));
          break;
        case '娴佽DOT':
          effects.push(buildSkillDesignerRuntimeObject({
            '鏈哄埗': '娴佽DOT',
            '鐩爣': offensiveTarget,
            '鐘舵€佸悕绉?: '娴佽',
            '鎸佺画鍥炲悎': parseSkillDesignerIntegerInputValue(params['duration'], 3, 1),
            'dot_damage': estimateSkillDesignerDotDamage(primaryDamageEffect, params['dotRatio']),
          }));
          break;
        case '鎵撴柇':
          effects.push(buildSkillDesignerRuntimeObject({
            '鏈哄埗': '鎵撴柇',
            '鐩爣': offensiveTarget,
            '涓柇姒傜巼': 1.0,
            '鎵撴柇鏃舵満': normalizeSkillUiText(params['interruptWindow'], ''),
            '杩藉姞鍍电洿': parseSkillDesignerNumericInputValue(params['extraDelay'], 0.5, 4),
          }));
          break;
        case '鍙嶅嚮':
          effects.push(buildSkillDesignerRuntimeObject({
            '鏈哄埗': '鍙楀嚮鍙嶅嚮',
            '鐩爣': grantableTarget,
            '鎸佺画鍥炲悎': 2,
            '鍙嶅嚮鍊嶇巼': parseSkillDesignerFactorInputValue(params['counterRatio'], 0.8),
            '鍙嶅嚮鏉′欢': normalizeSkillUiText(params['counterRule'], ''),
          }));
          break;
        case '娌夐粯':
          effects.push(buildSkillDesignerRuntimeObject({
            '鏈哄埗': '娌夐粯',
            '鐩爣': offensiveTarget,
            '鎸佺画鍥炲悎': parseSkillDesignerIntegerInputValue(params['duration'], 2, 1),
            '闄愬埗鑼冨洿': normalizeSkillUiText(params['muteScope'], ''),
          }));
          break;
        case '鍑忛€?:
        case '杩熺紦':
          effects.push(buildSkillDesignerRuntimeObject({
            '鏈哄埗': '鍑忛€?,
            '鐩爣': offensiveTarget,
            '鎸佺画鍥炲悎': parseSkillDesignerIntegerInputValue(params['duration'], 2, 1),
            'agi_ratio': parseSkillDesignerReductionFactor(params['slowRatio'], 0.8),
          }));
          break;
        case '鑷寸洸':
          effects.push(buildSkillDesignerRuntimeObject({
            '鏈哄埗': '鑷寸洸',
            '鐩爣': offensiveTarget,
            '鎸佺画鍥炲悎': parseSkillDesignerIntegerInputValue(params['duration'], 2, 1),
            '褰卞搷鍐呭': normalizeSkillUiText(params['blindEffect'], ''),
          }));
          break;
        case '绂佺枟':
          if (!runtimeState || !runtimeState.effects.some(effect => toText(effect && effect['鏈哄埗'], '') === '绂佺枟')) {
            effects.push(buildSkillDesignerRuntimeObject({
              '鏈哄埗': '绂佺枟',
              '鐩爣': offensiveTarget,
              '鎸佺画鍥炲悎': parseSkillDesignerIntegerInputValue(params['duration'], 2, 1),
              'heal_block_ratio': parseSkillDesignerPercentRatio(params['banHealRatio'], 1.0),
            }));
          }
          break;
        case '椹辨暎澧炵泭':
          effects.push(buildSkillDesignerRuntimeObject({
            '鏈哄埗': '椹辨暎澧炵泭',
            '鐩爣': offensiveTarget,
            '椹辨暎鏁伴噺': parseSkillDesignerIntegerInputValue(params['dispelCount'], 1, 1),
          }));
          break;
        case '绐冨彇澧炵泭':
          effects.push(buildSkillDesignerRuntimeObject({
            '鏈哄埗': '绐冨彇澧炵泭',
            '鐩爣': offensiveTarget,
            '绐冨彇鏁伴噺': parseSkillDesignerIntegerInputValue(params['stealCount'], 1, 1),
          }));
          break;
        case '灏忔姢鐩?:
          effects.push(buildSkillDesignerRuntimeObject({
            '鏈哄埗': '鎶ょ浘',
            '鐩爣': supportTarget,
            '鎶ょ浘鍊?: estimateSkillDesignerShieldValue(params['shieldRatio'], params['shieldCap']),
            '鎸佺画鍥炲悎': parseSkillDesignerIntegerInputValue(params['duration'], 2, 1),
            '鎶ょ浘鏉ユ簮': normalizeSkillUiText(params['shieldCap'], ''),
          }));
          break;
        case '鍑€鍖?:
        case '瑙ｆ帶':
          if (!runtimeState || !runtimeState.effects.some(effect => toText(effect && effect['鏈哄埗'], '') === '瑙ｆ帶')) {
            effects.push(buildSkillDesignerRuntimeObject({
              '鏈哄埗': '瑙ｆ帶',
              '鐩爣': supportTarget,
              '娓呴櫎灞傜骇': parseSkillDesignerIntegerInputValue(params['cleanseCount'], 2, 1),
              '娓呴櫎浼樺厛绾?: normalizeSkillUiText(params['cleansePriority'], ''),
              '闄勫甫鏀剁泭': normalizeSkillUiText(params['extraGain'], ''),
            }));
          }
          break;
        case '鍏变韩瑙嗛噹':
          if (!runtimeState || !runtimeState.effects.some(effect => toText(effect && effect['鏈哄埗'], '') === '鍏变韩瑙嗛噹')) {
            effects.push(buildSkillDesignerRuntimeObject({
              '鏈哄埗': '鍏变韩瑙嗛噹',
              '鐩爣': sharedVisionTarget,
              '鎸佺画鍥炲悎': parseSkillDesignerIntegerInputValue(params['duration'], 3, 1),
              'reaction_bonus': 0.1,
              'hit_bonus': 0.1,
              'lock_level': 1,
              '鍏变韩娣卞害': normalizeSkillUiText(params['infoDepth'], ''),
              '鍏变韩鑼冨洿': normalizeSkillUiText(params['shareRange'], ''),
            }));
          }
          break;
        case '闅愯韩':
          effects.push(buildSkillDesignerRuntimeObject({
            '鏈哄埗': '闅愯韩',
            '鐩爣': grantableTarget,
            '鎸佺画鍥炲悎': parseSkillDesignerIntegerInputValue(params['duration'], 2, 1),
            '闅愯斀搴?: parseSkillDesignerPercentRatio(params['stealthRatio'], 0.3),
            'dodge_bonus': parseSkillDesignerPercentRatio(params['dodgeBonus'], 0.1),
            'reaction_bonus': parseSkillDesignerPercentRatio(params['reactionBonus'], 0.08),
          }));
          break;
        case '鎶ゅ崼':
          effects.push(buildSkillDesignerRuntimeObject({
            '鏈哄埗': '鎶ゅ崼',
            '鐩爣': resolveSkillDesignerMechanismTargetForRuntime(draft && draft.target, '鎶ゅ崼'),
            '鎸佺画鍥炲悎': parseSkillDesignerIntegerInputValue(params['duration'], 2, 1),
            'damage_reduction': parseSkillDesignerPercentRatio(params['reduceRatio'], 0.1),
          }));
          break;
        case '鍢茶':
          effects.push(buildSkillDesignerRuntimeObject({
            '鏈哄埗': '鍢茶',
            '鐩爣': offensiveTarget,
            '鎸佺画鍥炲悎': parseSkillDesignerIntegerInputValue(params['duration'], 2, 1),
            '鑱氱伀瑙勫垯': normalizeSkillUiText(params['focusRule'], ''),
          }));
          break;
        case '榄傚姏鎭㈠':
          effects.push(buildSkillDesignerPackedRecoverAttributeEffect(
            supportTarget,
            'sp',
            parseSkillDesignerPercentRatio(params['recoverRatio'], 0.18) * parseSkillDesignerIntegerInputValue(params['repeatCount'], 1, 1),
            0,
            false,
          ));
          break;
        case '绮剧鎭㈠':
          effects.push(buildSkillDesignerPackedRecoverAttributeEffect(
            supportTarget,
            'men',
            parseSkillDesignerPercentRatio(params['recoverRatio'], 0.18) * parseSkillDesignerIntegerInputValue(params['repeatCount'], 1, 1),
            0,
            false,
          ));
          break;
        case '鏍囪寮辩偣':
          effects.push(buildSkillDesignerRuntimeObject({
            '鏈哄埗': '鏍囪寮辩偣',
            '鐩爣': offensiveTarget,
            '鎸佺画鍥炲悎': parseSkillDesignerIntegerInputValue(params['duration'], 2, 1),
            'final_damage_mult': 1.15,
            'dodge_penalty': 0.08,
            'lock_level': 1,
            '寮辩偣绫诲瀷': normalizeSkillUiText(params['weakPointType'], ''),
          }));
          break;
        case '鐮撮殣':
          effects.push(buildSkillDesignerRuntimeObject({
            '鏈哄埗': '鐮撮殣',
            '鐩爣': offensiveTarget,
            '鐮撮殣灞傜骇': 99,
            'hit_bonus': parseSkillDesignerPercentRatio(params['hitBonus'], 0.1),
            'lock_level': parseSkillDesignerIntegerInputValue(params['lockLevel'], 1, 1),
          }));
          break;
        case '鐩爣閿佸畾':
          effects.push(buildSkillDesignerRuntimeObject({
            '鏈哄埗': '鐩爣閿佸畾',
            '鐩爣': offensiveTarget,
            '鎸佺画鍥炲悎': parseSkillDesignerIntegerInputValue(params['duration'], 2, 1),
            'hit_bonus': parseSkillDesignerPercentRatio(params['hitBonus'], 0.1),
            'lock_level': parseSkillDesignerIntegerInputValue(params['lockLevel'], 1, 1),
          }));
          break;
        case '杩藉嚮':
          effects.push(buildSkillDesignerRuntimeObject({
            '鏈哄埗': '杩藉嚮',
            '鐩爣': offensiveTarget,
            '鎸佺画鍥炲悎': parseSkillDesignerIntegerInputValue(params['duration'], 2, 1),
            'attacker_speed_bonus': 0.08,
            'hit_bonus': 0.08,
            'final_damage_mult': parseSkillDesignerFactorInputValue(params['bonusRatio'], 1.2, { plusBase: true }),
            '杩藉嚮绐楀彛': normalizeSkillUiText(params['followWindow'], ''),
          }));
          break;
        case '缂存':
          effects.push(buildSkillDesignerRuntimeObject({
            '鏈哄埗': '缂存',
            '鐩爣': offensiveTarget,
            '鎸佺画鍥炲悎': parseSkillDesignerIntegerInputValue(params['duration'], 2, 1),
            '闄愬埗鑼冨洿': normalizeSkillUiText(params['disarmScope'], ''),
          }));
          break;
        case '鏃犳晫閲戣韩':
          effects.push(buildSkillDesignerRuntimeObject({
            '鏈哄埗': '鏃犳晫閲戣韩',
            '鐩爣': grantableTarget,
            '鎸佺画鍥炲悎': parseSkillDesignerIntegerInputValue(params['duration'], 2, 1),
            '姣忔棩瑙﹀彂涓婇檺': parseSkillDesignerIntegerInputValue(params['dailyLimit'], 3, 1),
            '鍏嶇柅浣嶉樁闃堝€?: parseSkillDesignerNumericInputValue(params['tierThreshold'], 100, 1),
            'damage_reduction': parseSkillDesignerPercentRatio(params['reduceRatio'], 0.18),
          }));
          break;
        case '浼ゅ鍙嶅皠':
          effects.push(buildSkillDesignerRuntimeObject({
            '鏈哄埗': '浼ゅ鍙嶅皠',
            '鐩爣': grantableTarget,
            '鎸佺画鍥炲悎': parseSkillDesignerIntegerInputValue(params['duration'], 2, 1),
            '鍙嶅皠姣斾緥': parseSkillDesignerPercentRatio(params['reflectRatio'], 0.25),
            '瑙﹀彂鏉′欢': normalizeSkillUiText(params['reflectRule'], ''),
          }));
          break;
        case '浼ゅ鍒嗘憡':
          effects.push(buildSkillDesignerRuntimeObject({
            '鏈哄埗': '浼ゅ鍒嗘憡',
            '鐩爣': resolveSkillDesignerMechanismTargetForRuntime(draft && draft.target, '浼ゅ鍒嗘憡'),
            '鎸佺画鍥炲悎': parseSkillDesignerIntegerInputValue(params['duration'], 2, 1),
            '鍒嗘憡姣斾緥': parseSkillDesignerPercentRatio(params['shareRatio'], 0.35),
            '鍒嗘憡浜烘暟': parseSkillDesignerIntegerInputValue(params['shareCount'], 1, 1),
          }));
          break;
        case '鏇胯韩鎶垫秷':
          effects.push(buildSkillDesignerRuntimeObject({
            '鏈哄埗': '鏇胯韩鎶垫秷',
            '鐩爣': grantableTarget,
            '鎸佺画鍥炲悎': parseSkillDesignerIntegerInputValue(params['duration'], 2, 1),
            '鎶垫秷娆℃暟': parseSkillDesignerIntegerInputValue(params['substituteCount'], 1, 1),
            '瑙﹀彂鏉′欢': normalizeSkillUiText(params['substituteRule'], ''),
          }));
          break;
        case '澶嶈嫃':
          effects.push(buildSkillDesignerRuntimeObject({
            '鏈哄埗': '澶嶈嫃',
            '鐩爣': resolveSkillDesignerMechanismTargetForRuntime(draft && draft.target, '澶嶈嫃'),
            '鎸佺画鍥炲悎': parseSkillDesignerIntegerInputValue(params['duration'], 3, 1),
            '澶嶈嫃娆℃暟': parseSkillDesignerIntegerInputValue(params['reviveCount'], 1, 1),
            '澶嶈嫃鍥炶姣斾緥': parseSkillDesignerPercentRatio(params['reviveHealRatio'], 0.25),
          }));
          break;
        case '娌荤枟鍙嶈浆':
          effects.push(buildSkillDesignerRuntimeObject({
            '鏈哄埗': '娌荤枟鍙嶈浆',
            '鐩爣': offensiveTarget,
            '鎸佺画鍥炲悎': parseSkillDesignerIntegerInputValue(params['duration'], 2, 1),
            '鍙嶈浆姣斾緥': parseSkillDesignerFactorInputValue(params['invertRatio'], 1.0),
          }));
          break;
        case '灏佹妧':
          effects.push(buildSkillDesignerRuntimeObject({
            '鏈哄埗': '灏佹妧',
            '鐩爣': offensiveTarget,
            '鎸佺画鍥炲悎': parseSkillDesignerIntegerInputValue(params['duration'], 2, 1),
            '闄愬埗鑼冨洿': normalizeSkillUiText(params['muteScope'], ''),
          }));
          break;
        case '鐘舵€佽浆绉?:
          effects.push(buildSkillDesignerRuntimeObject({
            '鏈哄埗': '鐘舵€佽浆绉?,
            '鐩爣': offensiveTarget,
            '杞Щ绫诲瀷': normalizeSkillUiText(params['transferMode'], '鑷韩璐熼潰->鐩爣'),
            '瑙﹀彂鏉′欢': normalizeSkillUiText(params['triggerRule'], ''),
          }));
          break;
        case '寮曠垎鎸佺画浼ゅ':
          effects.push(buildSkillDesignerRuntimeObject({
            '鏈哄埗': '寮曠垎鎸佺画浼ゅ',
            '鐩爣': offensiveTarget,
            '寮曠垎鍊嶇巼': parseSkillDesignerFactorInputValue(params['detonateRatio'], 1.2),
            '娑堣€楁寔缁激瀹?: !/涓嶆秷鑰梶淇濈暀/.test(normalizeSkillUiText(params['consumeMode'], '')),
          }));
          break;
        case '鏂╃浘':
          effects.push(buildSkillDesignerRuntimeObject({
            '鏈哄埗': '鏂╃浘',
            '鐩爣': offensiveTarget,
            '鏂╃浘鍊嶇巼': parseSkillDesignerFactorInputValue(params['shieldBreakRatio'], 0.6),
          }));
          break;
        case '绐冨彇鎶ょ浘':
          effects.push(buildSkillDesignerRuntimeObject({
            '鏈哄埗': '绐冨彇鎶ょ浘',
            '鐩爣': offensiveTarget,
            '鎸佺画鍥炲悎': parseSkillDesignerIntegerInputValue(params['duration'], 2, 1),
            '绐冪浘姣斾緥': parseSkillDesignerPercentRatio(params['shieldStealRatio'], 0.45),
          }));
          break;
        case '鍚炲櫖':
          effects.push(buildSkillDesignerRuntimeObject({
            '鏈哄埗': '鍚炲櫖',
            '鐩爣': offensiveTarget,
            '璧勬簮绫诲瀷': normalizeSkillUiText(params['resourceType'], '榄傚姏'),
            '澶哄彇姣斾緥': parseSkillDesignerPercentRatio(params['drainRatio'], 0.18),
            '杞寲姣斾緥': parseSkillDesignerFactorInputValue(params['convertRatio'], 1),
          }));
          break;
        case '鑳藉姏鍏变韩':
          effects.push(buildSkillDesignerRuntimeObject({
            '鏈哄埗': '鑳藉姏鍏变韩',
            '鐩爣': grantableTarget,
            '璧勬簮绫诲瀷': normalizeSkillUiText(params['resourceType'], '榄傚姏'),
            '鍙嶇亴姣斾緥': parseSkillDesignerPercentRatio(params['refeedRatio'], 0.2),
          }));
          break;
        case '鏈哄埗鎶规秷':
          effects.push(buildSkillDesignerRuntimeObject({
            '鏈哄埗': '鏈哄埗鎶规秷',
            '鐩爣': offensiveTarget,
            '鎶规秷鐩爣': normalizeSkillUiText(params['suppressTarget'], '澶嶈嫃'),
            '鎶规秷鏂瑰紡': normalizeSkillUiText(params['suppressMode'], '绉婚櫎骞跺皝閿?),
            '鎸佺画鍥炲悎': parseSkillDesignerIntegerInputValue(params['duration'], 2, 1),
          }));
          break;
        default:
          break;
      }
      return effects.filter(Boolean).filter(effect => safeEntries(effect).length);
    }

    function normalizeSkillDesignerPassiveEffects(effectArray = [], draft = {}, previewMeta = {}) {
      const isPassive = /琚姩/.test(normalizeSkillUiText(draft && draft.type, '')) || toText(previewMeta && previewMeta.scope, '') === 'blood_passive';
      if (!isPassive) return Array.isArray(effectArray) ? effectArray : [];
      return (Array.isArray(effectArray) ? effectArray : []).map(effect => {
        if (!effect || typeof effect !== 'object') return effect;
        const cloned = cloneJsonValue(effect);
        const mechanism = toText(cloned && cloned['鏈哄埗'], '').trim();
        if (['灞炴€у彉鍖?, '鎸佺画鎭㈠', '娑堣€椾慨姝?, '鍓嶆憞淇', '鎺屾帶淇', '閫熷害淇'].includes(mechanism)) {
          cloned['瀵硅薄'] = cloned['瀵硅薄'] || '鑷韩';
          if (cloned['鎸佺画'] === undefined || Number(cloned['鎸佺画'] || 0) <= 0) cloned['鎸佺画'] = 999;
          if (mechanism === '鎸佺画鎭㈠') cloned['瑙﹀彂'] = '姣忓洖鍚?;
          else if (!toText(cloned['瑙﹀彂'], '').trim() || cloned['瑙﹀彂'] === '绔嬪嵆鐢熸晥') cloned['瑙﹀彂'] = '甯搁┗';
        }
        if (['鎶ょ浘', '鍑忎激', '鏍兼尅', '闇镐綋', '鍏嶆', '鍏变韩瑙嗛噹', '鍙楀嚮鍙嶅嚮', '鍙嶅埗', '鍒嗚韩', '鏃犳晫閲戣韩', '浼ゅ鍙嶅皠', '浼ゅ鍒嗘憡', '鏇胯韩鎶垫秷', '澶嶈嫃', '闅愯韩', '鎶ゅ崼', '杩藉嚮'].includes(mechanism)) {
          cloned['鐩爣'] = cloned['鐩爣'] || '鑷韩';
          if (cloned['鎸佺画鍥炲悎'] === undefined || Number(cloned['鎸佺画鍥炲悎'] || 0) <= 0) cloned['鎸佺画鍥炲悎'] = 999;
        }
        return cloned;
      });
    }

    function buildSkillDesignerConstructEffect(draft = {}, runtimeEffects = []) {
      const usageEffects = cloneJsonValue(Array.isArray(runtimeEffects) ? runtimeEffects : []).map(effect => {
        if (!effect || typeof effect !== 'object') return effect;
        const cloned = cloneJsonValue(effect);
        if (normalizeSkillUiText(draft && draft.type, '') === '椋熺墿绯?) {
          if ('鐩爣' in cloned) cloned['鐩爣'] = '椋熺敤鑰?;
          if ('瀵硅薄' in cloned) cloned['瀵硅薄'] = '椋熺敤鑰?;
        }
        return cloned;
      });
      const isFood = normalizeSkillUiText(draft && draft.type, '') === '椋熺墿绯?;
      const triggerMode = isFood ? '椋熺敤' : '浣跨敤';
      const itemType = isFood ? '椋熺墿' : '榄傛妧閫犵墿';
      return buildSkillDesignerRuntimeObject({
        '鏈哄埗': '閫犵墿鐢熸垚',
        '鐩爣': '鑷韩',
        '榄傛妧鍚?: normalizeSkillUiText(draft && draft.name, '鏈懡鍚嶆妧鑳?),
        '浜х墿绫诲瀷': itemType,
        '鏁伴噺': 1,
        '瑙﹀彂鏂瑰紡': triggerMode,
        '浣跨敤鏁堟灉': usageEffects,
        '鑳屽寘妯℃澘': {
          '鏁伴噺': 1,
          '绫诲瀷': itemType,
          '鎻忚堪': buildSkillDesignerCompactSummary(draft) || '浣跨敤鍚庤Е鍙戝搴旈瓊鎶€鏁堟灉',
          '鏉ユ簮鎶€鑳?: normalizeSkillUiText(draft && draft.name, '鏈懡鍚嶆妧鑳?),
        },
      });
    }

    function buildSkillDesignerRuntimeEffects(draft = {}, skillSource = {}, previewMeta = {}) {
      const runtimeState = {
        target: normalizeSkillDesignerTargetForRuntime(draft && draft.target, 'effect'),
        supportTarget: resolveSkillDesignerSupportTargetForRuntime(draft && draft.target),
        grantableTarget: resolveSkillDesignerGrantableMechanismTargetForRuntime(draft && draft.target),
        sharedVisionTarget: resolveSkillDesignerSharedVisionTargetForRuntime(draft && draft.target),
        effects: [],
        primaryDamageEffect: null,
      };
      const primaryEffects = buildSkillDesignerPrimaryEffects(draft, runtimeState);
      runtimeState.effects.push(...primaryEffects);
      runtimeState.primaryDamageEffect = runtimeState.effects.find(effect =>
        ['鐩存帴浼ゅ', '澶氭浼ゅ', '鎸佺画浼ゅ', '寤惰繜鐖嗗彂'].includes(toText(effect && effect['鏈哄埗'], '').trim())
      ) || null;
      normalizeSkillDesignerArray(draft && draft.secondaryMechanics).forEach(label => {
        const secondaryEffects = buildSkillDesignerSecondaryEffects(draft, runtimeState, label);
        runtimeState.effects.push(...secondaryEffects);
      });
      const normalizedEffects = normalizeSkillDesignerPassiveEffects(runtimeState.effects, draft, previewMeta);
      return draft && draft.deliveryForm === '閫犵墿鎵胯浇'
        ? [buildSkillDesignerConstructEffect(draft, normalizedEffects)]
        : normalizedEffects;
    }

    function buildSkillDesignerUpdatedSkill(skillSource = {}, formState = {}, previewMeta = {}) {
      const safeSkill = skillSource && typeof skillSource === 'object' ? cloneJsonValue(skillSource) : {};
      const normalized = buildSkillDesignerFormStateFromDraft(formState, previewMeta);
      const normalizedFusionFields = previewMeta && previewMeta.scope === '姝﹂瓊铻嶅悎鎶€'
        ? assertSkillDesignerFusionState(normalized)
        : null;
      const designSummary = buildSkillDesignerCompactSummary(normalized);
      const skillSourceCategory = resolveSkillDesignerSourceCategory(previewMeta);
      safeSkill['榄傛妧鍚?] = normalized.name;
      safeSkill['name'] = normalized.name;
      safeSkill['鎶€鑳芥潵婧?] = skillSourceCategory;
      safeSkill['鎶€鑳界被鍨?] = normalized.type;
      safeSkill['娑堣€?] = normalized.cost;
      delete safeSkill['鍔犳垚灞炴€?];
      safeSkill['涓诲畾浣?] = normalized.mainRole;
      safeSkill['鏍囩'] = compactSkillDesignerStoredTags(normalized.tags);
      safeSkill['鐢婚潰鎻忚堪'] = normalized.visualDesc;
      safeSkill['鏁堟灉鎻忚堪'] = normalized.effectDesc;
      if ('鎻忚堪' in safeSkill || (previewMeta && ['art', '姝﹂瓊铻嶅悎鎶€'].includes(previewMeta.scope))) safeSkill['鎻忚堪'] = normalized.effectDesc;
      if (previewMeta && previewMeta.scope === 'art') {
        safeSkill['澧冪晫'] = normalizeSkillUiText(normalized.artStage, '鏈叆闂?);
        safeSkill['lv'] = Math.max(0, toNumber(normalized.artLevel, 0));
        safeSkill['exp'] = Math.max(0, toNumber(normalized.artExp, 0));
      }
      if (previewMeta && previewMeta.scope === '姝﹂瓊铻嶅悎鎶€') {
        safeSkill['铻嶅悎妯″紡'] = normalizedFusionFields.fusionMode;
        safeSkill['铻嶅悎瀵硅薄'] = normalizedFusionFields.fusionPartner;
        safeSkill['鏉ユ簮姝﹂瓊'] = [...normalizedFusionFields.fusionSourceSpirits];
        safeSkill['铻嶅悎鍙備笌鑰?] = cloneJsonValue(normalizedFusionFields.fusionParticipants);
      }
      safeSkill['闄勫甫灞炴€?] = [...normalized.attachedAttributes];
      safeSkill['鐗规晥閲忓寲鍙傛暟'] = designSummary;
      safeSkill['璁捐绋?] = {
        '鎶€鑳芥潵婧?: skillSourceCategory,
        '涓绘満鍒?: normalized.primaryMain,
        '缁嗗垎鏈哄埗': normalized.primarySub,
        '閲婃斁褰㈠紡': normalized.deliveryForm,
        '闄勫姞鏈哄埗': [...normalized.secondaryMechanics],
        '鏈哄埗鍙傛暟': cloneJsonValue(normalizeSkillDesignerMechanicParamMap(normalized.mechanicParams, normalized)),
        '鍓綔鐢ㄥ垪琛?: cloneJsonValue(normalizeSkillDesignerSideEffectList(normalized['鍓綔鐢ㄥ垪琛?])),
        '闄勫甫灞炴€?: [...normalized.attachedAttributes],
        '鏍囩': compactSkillDesignerStoredTags(normalized.tags),
        '鎶€鑳界被鍨?: normalized.type,
        '鐩爣妯″瀷': normalizeSkillDesignerTargetForForm(normalized.target, '鏁屾柟鍗曚綋'),
        '缁撶畻绛栫暐': ['鏁屾柟缇や綋', '鍙嬫柟缇や綋', '鍏ㄥ満'].includes(normalizeSkillDesignerTargetForForm(normalized.target, '鏁屾柟鍗曚綋'))
          ? '鍏ㄧ洰鏍囩嫭绔?
          : '鍗曠洰鏍囩嫭绔?,
        '娑堣€?: normalized.cost,
        '娑堣€楄祫婧?: normalized.costType,
        '娑堣€楁暟鍊?: normalized.costValue,
        '涓诲畾浣?: normalized.mainRole,
        '澧冪晫': normalizeSkillUiText(normalized.artStage, '鏈叆闂?),
        'lv': Math.max(0, toNumber(normalized.artLevel, 0)),
        'exp': Math.max(0, toNumber(normalized.artExp, 0)),
        ...(previewMeta && previewMeta.scope === '姝﹂瓊铻嶅悎鎶€' ? {
          '铻嶅悎妯″紡': normalizedFusionFields.fusionMode,
          '铻嶅悎瀵硅薄': normalizedFusionFields.fusionPartner,
          '鏉ユ簮姝﹂瓊': [...normalizedFusionFields.fusionSourceSpirits],
          '铻嶅悎鍙備笌鑰?: cloneJsonValue(normalizedFusionFields.fusionParticipants),
        } : {}),
        '璁捐鎽樿': designSummary,
      };
      const systemBase = buildSkillDesignerSystemBaseEffect(skillSource, normalized, previewMeta);
      let runtimeEffects = buildSkillDesignerRuntimeEffects(normalized, skillSource, previewMeta);
      safeSkill['_鏁堟灉鏁扮粍'] = normalizeSkillDesignerExecutionEffectArray(
        [cloneJsonValue(systemBase), ...cloneJsonValue(runtimeEffects)],
        normalizeSkillDesignerTargetForForm(normalized.target, '鏁屾柟鍗曚綋'),
      );
      const 宸叉敹鍙ｇ郴缁熷熀纭€ = safeSkill['_鏁堟灉鏁扮粍'][0] && typeof safeSkill['_鏁堟灉鏁扮粍'][0] === 'object'
        ? safeSkill['_鏁堟灉鏁扮粍'][0]
        : {};
      safeSkill['鎶€鑳芥潵婧?] = normalizeSkillUiText(宸叉敹鍙ｇ郴缁熷熀纭€['鎶€鑳芥潵婧?], safeSkill['鎶€鑳芥潵婧?]);
      safeSkill['鎶€鑳界被鍨?] = normalizeSkillUiText(宸叉敹鍙ｇ郴缁熷熀纭€['鎶€鑳界被鍨?], safeSkill['鎶€鑳界被鍨?]);
      safeSkill['鐩爣妯″瀷'] = normalizeSkillUiText(宸叉敹鍙ｇ郴缁熷熀纭€['鐩爣妯″瀷'], safeSkill['鐩爣妯″瀷'] || normalized.target);
      safeSkill['缁撶畻绛栫暐'] = normalizeSkillUiText(宸叉敹鍙ｇ郴缁熷熀纭€['缁撶畻绛栫暐'], safeSkill['缁撶畻绛栫暐'] || '鍗曠洰鏍囩嫭绔?);
      safeSkill['娑堣€?] = 宸叉敹鍙ｇ郴缁熷熀纭€['娑堣€?] === undefined ? safeSkill['娑堣€?] : cloneJsonValue(宸叉敹鍙ｇ郴缁熷熀纭€['娑堣€?]);
      safeSkill['cast_time'] = Math.max(0, toNumber(宸叉敹鍙ｇ郴缁熷熀纭€['cast_time'], safeSkill['cast_time']));
      return safeSkill;
    }

    function buildSkillDesignerWriteUpdates(previewMeta = {}, nextSkill = {}, rootData = {}) {
      const path = Array.isArray(previewMeta && previewMeta.path) ? previewMeta.path : [];
      if (!path.length) return [];
      const updates = [{ path, value: nextSkill }];
      if (toText(previewMeta && previewMeta.scope, '') === '姝﹂瓊铻嶅悎鎶€' && toText(path[path.length - 1], '') === '鎶€鑳芥暟鎹?) {
        const fusionRecordPath = path.slice(0, -1);
        const existingFusion = deepGet(rootData, fusionRecordPath, {});
        const fusionName = normalizeSkillUiText(nextSkill && (nextSkill['榄傛妧鍚?] || nextSkill.name), toText(previewMeta && previewMeta.label, '鏈懡鍚嶈瀺鍚堟妧'));
        const designDraft = nextSkill && nextSkill['璁捐绋?] && typeof nextSkill['璁捐绋?] === 'object' ? nextSkill['璁捐绋?] : {};
        const charKey = path[0] === 'char' ? toText(path[1], '') : '';
        const spiritOptions = getSkillDesignerSpiritNameOptions(charKey ? deepGet(rootData, ['char', charKey], {}) : {});
        const canUseSelfFusion = spiritOptions.length >= 2;
        let fusionMode = normalizeSkillDesignerFusionMode(designDraft['铻嶅悎妯″紡'] || nextSkill['铻嶅悎妯″紡'] || existingFusion.铻嶅悎妯″紡 || 'partner');
        if (fusionMode === 'self' && !canUseSelfFusion) fusionMode = 'partner';
        const rawParticipants = getSkillDesignerRawFusionParticipants(designDraft, nextSkill).length
          ? getSkillDesignerRawFusionParticipants(designDraft, nextSkill)
          : getSkillDesignerRawFusionParticipants({}, existingFusion);
        const fusionParticipants = normalizeSkillDesignerFusionParticipantList(rawParticipants, rootData);
        const fusionFields = deriveSkillDesignerFusionFields(
          fusionMode,
          fusionParticipants,
          normalizeSkillUiText(designDraft['铻嶅悎瀵硅薄'] || nextSkill['铻嶅悎瀵硅薄'] || existingFusion['铻嶅悎瀵硅薄'], '鏈煡鎼。'),
        );
        const partnerValue = fusionFields.fusionPartner;
        let sourceSpirits = fusionFields.fusionSourceSpirits.length
          ? fusionFields.fusionSourceSpirits
          : normalizeSkillDesignerArray(designDraft['鏉ユ簮姝﹂瓊'] || nextSkill['鏉ユ簮姝﹂瓊'] || existingFusion.鏉ユ簮姝﹂瓊);
        if (fusionMode === 'self' && !sourceSpirits.length) sourceSpirits = spiritOptions.slice(0, 2);
        updates.push({ path: [...fusionRecordPath, '鍚嶇О'], value: fusionName });
        updates.push({ path: [...fusionRecordPath, '铻嶅悎妯″紡'], value: fusionMode });
        updates.push({ path: [...fusionRecordPath, '铻嶅悎瀵硅薄'], value: partnerValue });
        updates.push({ path: [...fusionRecordPath, '鏉ユ簮姝﹂瓊'], value: sourceSpirits });
        updates.push({ path: [...fusionRecordPath, '铻嶅悎鍙備笌鑰?], value: cloneJsonValue(fusionFields.fusionParticipants) });
      }
      return updates;
    }

    function summarizeSkillEffectArray(effectArray, skill = null, cachedDraft = null) {
      const effectNames = (Array.isArray(effectArray) ? effectArray : [])
        .map(effect => {
          const name = toText(effect && effect['鏈哄埗'], '').trim();
          if (!name || name === '绯荤粺鍩虹' || isSkillSummaryEffect(effect)) return '';
          if (name === '鐢熸垚閫犵墿' || name === '閫犵墿鐢熸垚') return summarizeConstructEffectUi(effect);
          if (name === '鍒嗚韩') return summarizeCloneEffectUi(effect);
          if (name === '鐘舵€佹寕杞?) return normalizeSkillUiText(effect && (effect['鐘舵€佸悕绉?] || effect['鐗规畩鏈哄埗鏍囪瘑']), '鐘舵€佹寕杞?);
          return name;
        })
        .filter(Boolean);
      if (effectNames.length) return effectNames.join(' / ');
      const draft = cachedDraft || (skill ? readSkillDesignerDraft(skill) : null);
      const designSummary = buildSkillDesignerMechanicSummary(draft || {});
      return designSummary || '鏈煡';
    }

    function resolveExpiryUiText(item, fallback = '') {
      const expiryTick = toNumber(item && item['鏈夋晥鏈熻嚦tick'], 0);
      if (expiryTick > 0) return formatTickToCalendarDateText(expiryTick);
      const explicitText = normalizeSkillUiText(item && item['鏈夋晥鏈熻嚦'], '');
      if (explicitText) return explicitText;
      return fallback;
    }

    function extractConstructCreateEffects(effectArray = []) {
      return (Array.isArray(effectArray) ? effectArray : []).filter(effect =>
        ['鐢熸垚閫犵墿', '閫犵墿鐢熸垚'].includes(toText(effect && effect['鏈哄埗'], '').trim())
      );
    }

    function buildConstructSkillActionHtml(skillPath = [], itemType = '') {
      if (!Array.isArray(skillPath) || !skillPath.length) return '';
      const actionLabel = /椋熺墿/.test(toText(itemType, '')) ? '鏃ュ父鍒朵綔' : '鏃ュ父鏂藉睍';
      return `
        <div class="tag-cloud armory-quick-actions" style="margin-top:8px;justify-content:flex-end;">
          <button type="button" class="tag-chip live skill-inline-action-btn" data-skill-action="cast-construct" data-skill-path="${escapeHtmlAttr(JSON.stringify(skillPath))}">${actionLabel}</button>
        </div>
      `;
    }

    function buildSkillList(skillObj, options = {}) {
      const basePath = Array.isArray(options && options.basePath) ? options.basePath : [];
      const category = normalizeSkillUiText(options && options.category, '鎶€鑳?);
      const scope = normalizeSkillUiText(options && options.scope, 'skill');
      const skills = safeEntries(skillObj).map(([rawName, skill]) => {
        const effectArray = Array.isArray(skill && skill['_鏁堟灉鏁扮粍']) ? skill['_鏁堟灉鏁扮粍'] : [];
        const tacticalTags = Array.isArray(deepGet(skill, '鎴樻枟璇箟.鎴樻湳鏍囩', [])) ? deepGet(skill, '鎴樻枟璇箟.鎴樻湳鏍囩', []) : [];
        const systemBase = effectArray.find(effect => toText(effect && effect['鏈哄埗'], '').trim() === '绯荤粺鍩虹') || {};
        const draft = readSkillDesignerDraft(skill, rawName);
        const effectCount = effectArray.filter(effect => {
          const name = toText(effect && effect['鏈哄埗'], '').trim();
          return name && name !== '绯荤粺鍩虹' && !isSkillSummaryEffect(effect);
        }).length;
        const visualDesc = normalizeSkillUiText(skill && skill['鐢婚潰鎻忚堪'], '鏈煡');
        const effectDesc = normalizeSkillUiText(skill && (skill['鏁堟灉鎻忚堪'] || skill['鎻忚堪']), '鏈煡');
        const effectSummaryCore = summarizeSkillEffectArray(effectArray, skill, draft);
        const constructMeta = extractConstructEffectMeta(effectArray);
        const mechanicSummary = buildSkillDesignerMechanicSummary(draft);
        const attributeSummary = buildSkillDesignerAttributeSummary(draft);
        const effectSummary = effectSummaryCore === '鏈煡'
          ? '鏈煡'
          : (effectCount > 0 ? `${effectSummaryCore} (${effectCount}椤?` : effectSummaryCore);
        const type = normalizeSkillUiText(systemBase['鎶€鑳界被鍨?], '鏈煡');
        const target = normalizeSkillUiText(systemBase['鐩爣妯″瀷'] || systemBase['瀵硅薄'], '鏈煡');
        const cost = normalizeSkillUiText(systemBase['娑堣€?], '鏈煡');
        const mainRole = normalizeSkillUiText((draft && draft.mainRole) || (skill && skill['涓诲畾浣?]), '鏈煡');
        const compatParamDesc = normalizeSkillUiText(skill && skill['鐗规晥閲忓寲鍙傛暟'], '');
        const descSegments = [];
        const pushDesc = text => {
          const safeText = toText(text, '').trim();
          if (!safeText || descSegments.includes(safeText)) return;
          descSegments.push(safeText);
        };
        pushDesc(constructMeta ? `閫犵墿锛?{constructMeta.summary}` : '');
        pushDesc(mechanicSummary ? `鏈哄埗锛?{mechanicSummary}` : '');
        pushDesc(attributeSummary ? `灞炴€э細${attributeSummary}` : '');
        pushDesc(visualDesc !== '鏈煡' ? `鐢婚潰锛?{visualDesc}` : '');
        pushDesc(effectDesc !== '鏈煡' ? `鏁堟灉锛?{effectDesc}` : '');
        if (effectSummary !== '鏈煡' && effectSummary !== mechanicSummary) pushDesc(`鏁堟灉鏁扮粍锛?{effectSummary}`);
        pushDesc(compatParamDesc);
        let desc = descSegments.join('<br/>');
        const clash = deepGet(skill, '浠茶閫昏緫.鐬棿浜ら攱妯″潡');
        const state = deepGet(skill, '浠茶閫昏緫.鐘舵€佹寕杞芥ā鍧?);
        if (clash && clash['鍩虹濞佸姏鍊嶇巼'] > 0) desc += `${desc ? '<br/>' : ''}<span style="color:var(--cyan);">[濞佸姏鍊嶇巼: ${clash['鍩虹濞佸姏鍊嶇巼']}% | 浼ゅ绫诲瀷: ${clash['浼ゅ绫诲瀷'] || '鏃?} | 鎶ょ浘: ${clash['鎶ょ浘缁濆鍊?] || 0}]</span>`;
        if (state && state['鐘舵€佸悕绉?] !== '鏃?) desc += `${desc ? '<br/>' : ''}<span style="color:var(--gold);">[闄勫姞鐘舵€? ${state['鐘舵€佸悕绉?]} | 鏈哄埗: ${state['鐗规畩鏈哄埗鏍囪瘑'] || '鏃?} | 鎸佺画: ${state['鎸佺画鍥炲悎']}鍥瀅</span>`;
        const tags = Array.from(new Set(
          (Array.isArray(skill && skill['鏍囩']) ? skill['鏍囩'] : [])
            .map(tag => normalizeSkillUiText(tag, ''))
            .concat(tacticalTags.map(tag => normalizeSkillUiText(tag, '')))
            .concat(
              effectArray
                .map(effect => normalizeSkillUiText(effect && effect['鏈哄埗'], ''))
                .filter(name => name && name !== '绯荤粺鍩虹' && !SKILL_SUMMARY_EFFECT_MECHANISM_SET.has(name))
            )
            .concat(type && type !== '鏈煡' ? [type] : [])
            .concat(mainRole && mainRole !== '鏈煡' ? [mainRole] : [])
            .concat(draft.primaryMain ? [draft.primaryMain] : [])
            .concat(draft.primarySub ? [draft.primarySub] : [])
            .concat(Array.isArray(draft.secondaryMechanics) ? draft.secondaryMechanics : [])
            .concat(Array.isArray(draft.attachedAttributes) ? draft.attachedAttributes : [])
        )).filter(Boolean).slice(0, 8);

        const cleanSkillName = draft.name;
        const skillPath = basePath.length ? [...basePath, rawName] : [];
        const preview = buildSkillDesignerPreviewKey({
          path: skillPath,
          label: cleanSkillName,
          category,
          scope,
        });
        const constructCreateEffects = extractConstructCreateEffects(effectArray);
        const constructItemType = toText(constructCreateEffects[0] && constructCreateEffects[0]['浜х墿绫诲瀷'], '');
        const canDailyCastConstruct = skillPath.length > 0 && skillPath[0] === 'char' && constructCreateEffects.length > 0;
        const constructActionHtml = canDailyCastConstruct ? buildConstructSkillActionHtml(skillPath, constructItemType) : '';
        if (constructActionHtml) desc += constructActionHtml;

        return {
          name: cleanSkillName,
          type,
          target,
          cost,
          desc: desc || '鏈煡',
          visualDesc,
          effectDesc,
          effectSummary,
          status: normalizeSkillUiText(skill && skill['鐘舵€?], '宸茶閱?),
          mainRole,
          tags,
          preview,
          path: skillPath,
          category,
          scope,
          designDraft: draft,
          constructActionHtml
        };
      });

      if (skills.length) return skills;

      return [{
        name: '榄傛妧鏈樉鐜?,
        type: '鏈閱?,
        target: '鏈煡',
        bonus: '鏈煡',
        cost: '鏈煡',
        desc: '榄傛妧淇℃伅灏氭湭鏄庢櫚銆?,
        visualDesc: '鏈煡',
        effectDesc: '鏈煡',
        effectSummary: '鏈煡',
        status: '鏈閱?,
        mainRole: '鏈煡',
        tags: [],
        preview: '',
        path: [],
        category,
        scope,
        designDraft: null
      }];
    }

    function formatPermanentBonusSummary(bonusMap) {
      const labels = { str: '鍔涢噺', def: '闃插尽', agi: '鏁忔嵎', vit_max: '浣撳姏涓婇檺', men_max: '绮剧鍔涗笂闄?, sp_max: '榄傚姏涓婇檺' };
      const segments = [];
      safeEntries(bonusMap).forEach(([key, value]) => {
        const amount = Math.floor(toNumber(value, 0));
        if (amount > 0 && labels[key]) segments.push(`${labels[key]}+${amount}`);
      });
      return segments.join('锛?) || '鏃?;
    }

    function buildPermanentBonusList(bonusObj) {
      return safeEntries(bonusObj).map(([rawName, bonus]) => {
        const safeBonus = bonus && typeof bonus === 'object' ? bonus : {};
        const name = normalizeSkillUiText(safeBonus['鍚嶇О'] || rawName, '姘镐箙鎴愰暱');
        const bonusSummary = formatPermanentBonusSummary(safeBonus['灞炴€у姞鎴?] || {});
        const effectDesc = normalizeSkillUiText(safeBonus['鏁堟灉鎻忚堪'] || safeBonus['鎻忚堪'], bonusSummary !== '鏃? ? `鍥哄畾鍔犳垚锛?{bonusSummary}` : '鏈煡');
        return {
          name,
          type: '姘镐箙鎴愰暱',
          target: '鑷韩',
          bonus: bonusSummary === '鏃? ? '鍥哄畾鎴愰暱' : bonusSummary,
          cost: '鏃?,
          desc: [
            effectDesc !== '鏈煡' ? `鏁堟灉锛?{effectDesc}` : '',
            bonusSummary !== '鏃? ? `鍥哄畾鍔犳垚锛?{bonusSummary}` : ''
          ].filter(Boolean).join('<br/>') || effectDesc || '姘镐箙鎴愰暱',
          visualDesc: '浣撳姏灏佸嵃瑙ｅ紑鍚庯紝鎴愰暱宸叉案涔呭浐鍖栥€?,
          effectDesc,
          effectSummary: bonusSummary === '鏃? ? '鏈煡' : `鍥哄畾鍔犳垚锛?{bonusSummary}`,
          status: normalizeSkillUiText(safeBonus['鐘舵€?], '宸插浐鍖?),
          mainRole: '姘镐箙鎴愰暱',
          tags: ['姘镐箙鎴愰暱', '鍥哄畾澧炵泭']
        };
      });
    }

    function buildSpiritRingInfo(ringIndex, ring, skills, options = {}) {
      const ringPath = Array.isArray(options.path) ? options.path : [];
      const forceGold = !!(options && options.forceGold);
      const colorText = getRingDisplayColorLabel(ring && ring['棰滆壊'], ring && ring['骞撮檺'], { forceGold });
      const ageText = formatAge(ring && ring['骞撮檺']);
      return {
        glyph: ringGlyph(ring && ring['棰滆壊'], ringIndex, ring && ring['骞撮檺'], { forceGold }),
        ringClass: mapRingClass(ring && ring['棰滆壊'], ring && ring['骞撮檺'], { forceGold }),
        title: toText(options && options.title, `绗?{ringIndex}榄傜幆`),
        desc: [colorText, ageText].filter(Boolean).join(' / '),
        skills,
        path: ringPath,
        colorText,
        colorValue: colorText,
        ageText,
        sourceText: normalizeSkillUiText(options && options.sourceText, ''),
        sourceValue: toText(options && options.sourceValue, ''),
        sourcePath: Array.isArray(options && options.sourcePath) ? options.sourcePath : [],
        carrierText: normalizeSkillUiText(options && options.carrierText, ''),
      };
    }

    function buildRingHoverMetaMarkup(ring) {
      const rows = [];
      const colorMarkup = ring && Array.isArray(ring.path) && ring.path.length
        ? makeInlineEditableValue(toText(ring.colorText, '鏈畾'), {
            path: [...ring.path, '棰滆壊'],
            kind: 'enum_select',
            rawValue: toText(ring.colorValue, toText(ring.colorText, '鏈畾')),
            editorMeta: { options: RING_COLOR_OPTIONS }
          })
        : htmlEscape(toText(ring && ring.colorText, '鏈畾'));
      rows.push(`<div class="ring-hover-meta-row"><em>棰滆壊</em><strong>${colorMarkup}</strong></div>`);
      rows.push(`<div class="ring-hover-meta-row"><em>骞撮檺</em><strong>${htmlEscape(toText(ring && ring.ageText, '鏈畾'))}</strong></div>`);
      if (ring && ring.carrierText) {
        rows.push(`<div class="ring-hover-meta-row"><em>鎵胯浇</em><strong>${htmlEscape(toText(ring.carrierText, '鏈畾'))}</strong></div>`);
      }
      if ((ring && ring.sourcePath && ring.sourcePath.length) || (ring && ring.sourceText)) {
        const sourceMarkup = ring && ring.sourcePath && ring.sourcePath.length
          ? makeInlineEditableValue(toText(ring.sourceText, '鏈褰?), {
              path: ring.sourcePath,
              kind: 'string',
              rawValue: toText(ring.sourceValue, toText(ring.sourceText, '鏈褰?)),
            })
          : htmlEscape(toText(ring && ring.sourceText, '鏈褰?));
        rows.push(`<div class="ring-hover-meta-row"><em>鏉ユ簮</em><strong>${sourceMarkup}</strong></div>`);
      }
      return rows.length ? `<div class="ring-hover-meta">${rows.join('')}</div>` : '';
    }

    function buildRingHoverCardMarkup(ring) {
      const skills = (ring && Array.isArray(ring.skills) ? ring.skills : []).map(skill => `
        <div class=\"ring-hover-skill ${skill && skill.preview ? 'clickable' : ''}\"${skill && skill.preview ? ` data-preview=\"${escapeHtmlAttr(skill.preview)}\"` : ''}>
          <b>${htmlEscape(skill && skill.name ? skill.name : '鏈懡鍚嶆妧鑳?)}</b>
          <div class=\"ring-hover-copy\"><em>鐢婚潰鎻忚堪</em><span>${htmlEscape(skill && skill.visualDesc ? skill.visualDesc : '鏈煡')}</span></div>
          <div class=\"ring-hover-copy\"><em>鏁堟灉鎻忚堪</em><span>${htmlEscape(skill && skill.effectDesc ? skill.effectDesc : '鏈煡')}</span></div>
          ${skill && skill.constructActionHtml ? skill.constructActionHtml : ''}
        </div>
      `).join('');
      return `<div class=\"ring-hover-card\"><div class=\"ring-hover-title\">${htmlEscape(toText(ring && ring.title, '榄傜幆鎶€鑳?))}</div><div class=\"ring-hover-desc\">${htmlEscape(toText(ring && ring.desc, ''))}</div>${buildRingHoverMetaMarkup(ring)}${skills}</div>`;
    }

    function buildSpiritConfig(slotName, spiritData, previewKey, badgeText, badgeClass, spiritBasePath = []) {
      const spiritPath = Array.isArray(spiritBasePath) ? spiritBasePath : [];
      const soulEntries = safeEntries(spiritData && spiritData.榄傜伒);
      const summaryRings = [];
      const souls = soulEntries.map(([soulName, soulData]) => {
        const soulPath = [...spiritPath, '榄傜伒', soulName];
        const soulDisplayName = normalizeSkillUiText(soulData && soulData['琛ㄨ薄鍚嶇О'], soulName);
        const ringEntries = safeEntries(soulData && soulData.榄傜幆)
          .sort((a, b) => toNumber(a[0], 0) - toNumber(b[0], 0))
          .map(([ringIndex, ring]) => {
            const skills = buildSkillList(ring && ring['榄傛妧'], {
              basePath: [...soulPath, '榄傜幆', ringIndex, '榄傛妧'],
              category: '榄傜幆榄傛妧',
            scope: '榄傛妧',
            });
            const ringInfo = buildSpiritRingInfo(ringIndex, ring, skills, {
              path: [...soulPath, '榄傜幆', ringIndex],
              title: `绗?{ringIndex}榄傜幆 路 ${skills[0] ? skills[0].name : soulDisplayName}`,
              sourceText: soulDisplayName,
              carrierText: `榄傜伒 路 ${soulDisplayName}`,
            });
            summaryRings.push(ringInfo);
            return ringInfo;
          });

        const spiritName = soulDisplayName;
        const description = normalizeSkillUiText(soulData && soulData['鎻忚堪'], '鏈缃?);
        const quality = normalizeSkillUiText(soulData && soulData['鍝佽川'], '鏈缃?);
        const state = normalizeSkillUiText(soulData && soulData['鐘舵€?], '鏈煡');
        const ageValue = toNumber(soulData && soulData['骞撮檺'], 0);
        const compValue = toNumber(soulData && soulData['濂戝悎搴?], 100);
        return {
          name: soulName,
          desc: spiritName,
          spiritName,
          description,
          quality,
          state,
          age: formatAge(ageValue),
          ageValue,
          comp: `${compValue}%`,
          compValue,
          path: soulPath,
          榄傜幆: ringEntries
        };
      });
      const independentRings = safeEntries(spiritData && spiritData['鐙珛榄傜幆'])
        .sort((a, b) => toNumber(a[0], 0) - toNumber(b[0], 0))
        .map(([ringIndex, ring]) => {
          const ringPath = [...spiritPath, '鐙珛榄傜幆', ringIndex];
          const skills = buildSkillList(ring && ring['榄傛妧'], {
            basePath: [...ringPath, '榄傛妧'],
            category: '鐙珛榄傜幆榄傛妧',
            scope: 'independent_ring_skill',
          });
          const ringInfo = buildSpiritRingInfo(ringIndex, ring, skills, {
            path: ringPath,
            title: `鐙珛榄傜幆 路 绗?{ringIndex}鐜痐,
            sourceText: normalizeSkillUiText(ring && ring['鏉ユ簮'], '鏈褰?),
            sourceValue: toText(ring && ring['鏉ユ簮'], ''),
            sourcePath: [...ringPath, '鏉ユ簮'],
            carrierText: '鐙珛榄傜幆',
          });
          summaryRings.push(ringInfo);
          return ringInfo;
        });

      if (!souls.length) {
        souls.push({
          name: '榄傜伒妲戒綅',
          desc: '灏氭湭鎺ュ叆榄傜伒銆?,
          spiritName: '鏈缃?,
          description: '灏氭湭鎺ュ叆榄傜伒銆?,
          quality: '鏈缃?,
          state: '鏈縺娲?,
          age: '--',
          ageValue: 0,
          comp: '--',
          compValue: 0,
          path: [],
          榄傜幆: []
        });
      }

      const soulCount = soulEntries.length;
      const independentRingCount = independentRings.length;
      const displayName = normalizeSkillUiText(spiritData && spiritData['琛ㄨ薄鍚嶇О'], slotName);
      const spiritType = normalizeSkillUiText(spiritData && spiritData['type'], '鏈煡绯?);
      const spiritDesc = normalizeSkillUiText(spiritData && spiritData['鎻忚堪'], '鏈缃?);
      const attributeState = resolveSpiritAttributeUiState(spiritData);

      return {
        preview: previewKey,
        badge: badgeText,
        badgeClass,
        name: `${displayName}锛?{spiritType}锛塦,
        desc: `灞炴€т綋绯伙細${attributeState.attributeSystem} / 榄傜伒锛?{soulCount} / 鐙珛鐜細${independentRingCount}`,
        榄傜幆: summaryRings.slice(0, 10),
        souls,
        independentRings,
        soulCount,
        independentRingCount,
        slotName,
        spiritPath,
        spiritName: displayName,
        spiritDesc,
        spiritType,
        spiritElement: attributeState.attributeSystem,
        spiritUnlockedAttrs: attributeState.unlockedAttrs,
        spiritCapacityAttrs: attributeState.capacityAttrs
      };
    }

    function createDefaultRingSkillShellForUi() {
      return {
        榄傛妧鍚? '寰呰ˉ鍏?,
        鐢婚潰鎻忚堪: '鏈煡',
        鏁堟灉鎻忚堪: '鏈煡',
        _鏁堟灉鏁扮粍: [],
      };
    }

    function buildDefaultRingSkillMapForUi(ringIndex = 1, ringAge = 0) {
      const safeRingIndex = Math.max(1, Math.floor(Number(ringIndex) || 1));
      const baseSkillKey = `绗?{safeRingIndex}榄傛妧`;
      const skillMap = {
        [baseSkillKey]: createDefaultRingSkillShellForUi(),
      };
      if (Math.floor(Number(ringAge) || 0) >= 100000) {
        skillMap[`${baseSkillKey}路鍏朵簩`] = createDefaultRingSkillShellForUi();
      }
      return skillMap;
    }

    function getSoulSpiritRingCapacityForUi(ageValue = 0) {
      const years = Math.max(0, toNumber(ageValue, 0));
      if (years >= 10000) return 4;
      if (years >= 1000) return 3;
      if (years >= 100) return 2;
      return 1;
    }

    function buildPendingSoulRingState(snapshot) {
      const safeSnapshot = snapshot && typeof snapshot === 'object' ? snapshot : null;
      if (!safeSnapshot || !safeSnapshot.rootData) return null;
      const activeChar = safeSnapshot.activeChar && typeof safeSnapshot.activeChar === 'object' ? safeSnapshot.activeChar : null;
      const pending = activeChar && activeChar.鐘舵€?&& typeof activeChar.鐘舵€?寰呴€夐瓊鐜?=== 'object'
        ? activeChar.鐘舵€?寰呴€夐瓊鐜?        : null;
      if (!pending) return null;
      const status = toText(pending.鐘舵€? '').trim();
      const spiritSlot = toText(pending.姝﹂瓊妲戒綅, '').trim();
      const ringIndex = Math.max(1, toNumber(pending.寰呯敓鎴愰瓊鐜綅, 1));
      if (!status || !spiritSlot || !ringIndex) return null;
      const charKey = resolveSnapshotCharKey(safeSnapshot, toText(safeSnapshot.activeName, '')) || toText(safeSnapshot.activeName, '');
      if (!charKey) return null;
      const spiritData = deepGet(safeSnapshot, ['rootData', 'char', charKey, '姝﹂瓊', spiritSlot], {});
      const spiritPreviewKey = spiritSlot === '绗竴姝﹂瓊'
        ? '绗竴姝﹂瓊璇︾粏椤?
        : (spiritSlot === '绗簩姝﹂瓊' ? '绗簩姝﹂瓊璇︾粏椤? : '绗竴姝﹂瓊璇︾粏椤?);
      const candidateNames = Array.isArray(pending.鍊欓€夐瓊鐏?
        ? pending.鍊欓€夐瓊鐏?map(name => toText(name, '').trim()).filter(Boolean)
        : [];
      const candidates = candidateNames.map(name => {
        const soulData = deepGet(spiritData, ['榄傜伒', name], {});
        const ageValue = toNumber(soulData && soulData.骞撮檺, 0);
        const displayName = normalizeSkillUiText(soulData && soulData.琛ㄨ薄鍚嶇О, name || '鍊欓€夐瓊鐏?);
        const ringCount = safeEntries(soulData && soulData.榄傜幆).length;
        const ringCapacity = getSoulSpiritRingCapacityForUi(ageValue);
        const nextRingColorLabel = getRingDisplayColorLabel('', ageValue);
        return {
          soulName: name,
          displayName,
          ageValue,
          ageText: formatAge(ageValue),
          quality: normalizeSkillUiText(soulData && soulData.鍝佽川, '鏈缃?),
          state: normalizeSkillUiText(soulData && soulData.鐘舵€? '鏈煡'),
          compValue: toNumber(soulData && soulData.濂戝悎搴? 100),
          description: normalizeSkillUiText(soulData && soulData.鎻忚堪, '鏆傛棤鎻忚堪'),
          ringCount,
          ringCapacity,
          remainingSlots: Math.max(0, ringCapacity - ringCount),
          remainingAfterGenerate: Math.max(0, ringCapacity - ringCount - 1),
          nextRingColorLabel,
          nextRingClass: mapRingClass('', ageValue),
          sourcePath: ['char', charKey, '姝﹂瓊', spiritSlot, '榄傜伒', name],
        };
      });
      const signature = [
        charKey,
        spiritSlot,
        ringIndex,
        status,
        candidates.map(item => item.soulName).join('|'),
      ].join('::');
      return {
        charKey,
        charName: toText(safeSnapshot.activeName, charKey),
        spiritSlot,
        spiritName: normalizeSkillUiText(spiritData && spiritData.琛ㄨ薄鍚嶇О, spiritSlot),
        spiritPreviewKey,
        ringIndex,
        status,
        source: toText(pending.鏉ユ簮, ''),
        candidates,
        signature,
      };
    }

    function shouldRenderBloodline(activeChar, activeName = '') {
      const displayName = String(activeName || deepGet(activeChar, '灞炴€?name', '') || '').trim();
      return displayName === '鍞愯垶楹?;
    }

    function buildBloodlineConfig(activeChar, activeName = '', bloodlineBasePath = []) {
      if (!shouldRenderBloodline(activeChar, activeName)) {
        return {
          kind: 'bloodline',
          valid: false,
          preview: '琛€鑴夊皝鍗拌缁嗛〉',
          badge: '琛€鑴夊皝鍗?,
          badgeClass: 'gold',
          name: '',
          desc: '',
          榄傜幆: [],
          bloodSkills: [],
          bloodPassives: [],
          bloodPermanentBonuses: [],
          sealLv: 0,
          core: '鏈嚌鑱?,
          lifeFire: false,
          bloodline: '鏃?
        };
      }
      const bloodline = normalizeSkillUiText(deepGet(activeChar, '琛€鑴変箣鍔?琛€鑴?, '鏃?), '鏃?);
      const sealLv = toNumber(deepGet(activeChar, '琛€鑴変箣鍔?瑙ｅ皝灞傛暟', 0), 0);
      const core = normalizeSkillUiText(deepGet(activeChar, '琛€鑴変箣鍔?鏍稿績', '鏈嚌鑱?), '鏈嚌鑱?);
      const rawSkills = deepGet(activeChar, '琛€鑴変箣鍔?鎶€鑳?, {});
      const rawRings = deepGet(activeChar, '琛€鑴変箣鍔?姘旇榄傜幆', {});
      const rawPassives = deepGet(activeChar, '琛€鑴変箣鍔?琚姩', {});
      const rawPermanentBonuses = deepGet(activeChar, '琛€鑴変箣鍔?姘镐箙鍔犳垚', {});
      const hasBloodlineData = toText(bloodline, '鏃?) !== '鏃?
        || sealLv > 0
        || safeEntries(rawRings).length > 0
        || safeEntries(rawSkills).length > 0
        || safeEntries(rawPassives).length > 0
        || safeEntries(rawPermanentBonuses).length > 0;
      const ringEntries = safeEntries(deepGet(activeChar, '琛€鑴変箣鍔?姘旇榄傜幆', {}))
        .sort((a, b) => toNumber(a[0], 0) - toNumber(b[0], 0))
        .map(([index, ring]) => buildSpiritRingInfo(index, ring, buildSkillList(ring && ring['榄傛妧'], {
            basePath: [...bloodlineBasePath, '姘旇榄傜幆', index, '榄傛妧'],
            category: '琛€鑴夐瓊鐜?,
            scope: 'blood_ring_skill',
          }), {
            path: [...bloodlineBasePath, '姘旇榄傜幆', index],
            title: `琛€鑴夌幆浣?路 ${index}`,
            sourceText: toText(activeChar && activeChar['琛€鑴変箣鍔?] && activeChar['琛€鑴変箣鍔?]['琛€鑴?], '琛€鑴夋湰浣?),
            carrierText: '琛€鑴夐瓊鐜?,
          }));
      const normalizedRingEntries = ringEntries.slice(0, 10);

      return {
        kind: 'bloodline',
        valid: hasBloodlineData,
        preview: '琛€鑴夊皝鍗拌缁嗛〉',
        badge: '琛€鑴夊皝鍗?,
        badgeClass: 'gold',
        name: `${bloodline === '鏃? ? '鏈閱掕鑴? : bloodline}`,
        desc: `瑙ｅ皝灞傛暟锛?{sealLv} / 浣撳姏榄傛牳锛?{core}`,
        榄傜幆: normalizedRingEntries,
        bloodSkills: buildSkillList(rawSkills, {
          basePath: [...bloodlineBasePath, 'skills'],
          category: '琛€鑴夋暎鎶€',
          scope: 'blood_skill',
        }),
        bloodPassives: buildSkillList(rawPassives, {
          basePath: [...bloodlineBasePath, '琚姩'],
          category: '琛€鑴夌壒鎬?,
          scope: 'blood_passive',
        }),
        bloodPermanentBonuses: buildPermanentBonusList(rawPermanentBonuses),
        sealLv,
        core,
        lifeFire: !!deepGet(activeChar, '琛€鑴変箣鍔?鐢熷懡涔嬬伀', false),
        bloodline: bloodline === '鏃? ? '鏈閱掕鑴? : bloodline
      };
    }

    function getPrimaryFactionEntry(snapshot) {
      const preferredName = toText(
        deepGet(snapshot, 'locationData.鎺屾帶鍔垮姏', snapshot && snapshot.鍔垮姏 && snapshot.鍔垮姏[0] ? snapshot.鍔垮姏[0][0] : ''),
        snapshot && snapshot.鍔垮姏 && snapshot.鍔垮姏[0] ? snapshot.鍔垮姏[0][0] : ''
      );
      const fallbackEntry = snapshot && Array.isArray(snapshot.orgEntries) && snapshot.orgEntries.length ? snapshot.orgEntries[0] : ['', {}];
      const matchedEntry = snapshot && Array.isArray(snapshot.orgEntries)
        ? (snapshot.orgEntries.find(([name]) => name === preferredName) || fallbackEntry)
        : fallbackEntry;
      return {
        name: toText(matchedEntry && matchedEntry[0], preferredName || '鏈煡'),
        data: matchedEntry && matchedEntry[1] ? matchedEntry[1] : {}
      };
    }

    function buildFactionRelationMeta(relName, relData) {
      const safeName = toText(relName, '鏈煡鍔垮姏');
      const safeRelData = relData && typeof relData === 'object' ? relData : {};
      const attitude = toText(deepGet(safeRelData, '鎬佸害', deepGet(safeRelData, '鍏崇郴', '鏈煡')), '鏈煡');
      const detailParts = safeEntries(safeRelData)
        .filter(([key]) => !['鎬佸害', '鍏崇郴'].includes(toText(key, '')))
        .map(([key, value]) => {
          if (value && typeof value === 'object') {
            const detailCount = safeEntries(value).length;
            return `${toText(key, '鍏崇郴椤?)} ${detailCount}椤筦;
          }
          const textValue = toText(value, '');
          return textValue ? `${toText(key, '鍏崇郴椤?)} ${textValue}` : toText(key, '鍏崇郴椤?);
        })
        .filter(Boolean);
      return {
        name: safeName,
        attitude,
        desc: detailParts.length ? `${attitude} 锝?${detailParts.join(' / ')}` : attitude,
        className: /鏁屽|姝绘晫|浠囪|鎴樹簤/.test(attitude) ? 'warn' : (/鍚岀洘|鐩熷弸|鍙嬪ソ|鍚堜綔|鐢熸鍚岀洘/.test(attitude) ? 'highlight' : '')
      };
    }

    function buildFactionRelationEditorGrid(orgName, orgData, options = {}) {
      const {
        max = 6,
        emptyTitle = '鏆傛棤鍔垮姏鍏崇郴',
        emptyDesc = '褰撳墠鏈褰曞澶栧叧绯汇€?
      } = options || {};
      const safeOrgName = toText(orgName, '').trim();
      const relationEntries = safeEntries(deepGet(orgData, '鍏崇郴', {})).slice(0, Math.max(1, max));
      if (!relationEntries.length) {
        return `<div class="relation-card"><b>${htmlEscape(emptyTitle)}</b><span>${htmlEscape(emptyDesc)}</span></div>`;
      }
      return `
        <div style="display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px;margin-top:12px;">
          ${relationEntries.map(([relationName, relationData]) => {
            const safeRelData = relationData && typeof relationData === 'object' ? relationData : {};
            const relationMeta = buildFactionRelationMeta(relationName, safeRelData);
            const relationField = Object.prototype.hasOwnProperty.call(safeRelData, '鎬佸害')
              ? '鎬佸害'
              : (Object.prototype.hasOwnProperty.call(safeRelData, '鍏崇郴') ? '鍏崇郴' : '鎬佸害');
            const relationValue = toText(deepGet(safeRelData, relationField, relationMeta.attitude), relationMeta.attitude);
            const detailText = safeEntries(safeRelData)
              .filter(([key]) => !['鎬佸害', '鍏崇郴'].includes(toText(key, '')))
              .map(([key, value]) => {
                if (value && typeof value === 'object') return `${toText(key, '鍏崇郴椤?)} ${safeEntries(value).length}椤筦;
                const textValue = toText(value, '');
                return textValue ? `${toText(key, '鍏崇郴椤?)} ${textValue}` : '';
              })
              .filter(Boolean)
              .join(' / ');
            const editableValue = safeOrgName
              ? makeInlineEditableValue(relationValue, {
                  path: ['org', safeOrgName, '鍏崇郴', relationName, relationField],
                  kind: 'string',
                  rawValue: relationValue,
                })
              : htmlEscape(relationValue);
            return `
              <div class="archive-tile" style="min-height:96px;justify-content:flex-start;">
                <b>${htmlEscape(relationMeta.name)}</b>
                <span style="color:var(--cyan);font-size:16px;font-weight:700;line-height:1.35;">${editableValue}</span>
                ${detailText ? `<div style="margin-top:8px;font-size:12px;line-height:1.45;color:var(--color-text-secondary);">${htmlEscape(detailText)}</div>` : ''}
              </div>
            `;
          }).join('')}
        </div>
      `;
    }

    function buildFactionRelationSummary(orgData, max = 3) {
      return safeEntries(deepGet(orgData, '鍏崇郴', {}))
        .slice(0, Math.max(1, max))
        .map(([name, relData]) => {
          const meta = buildFactionRelationMeta(name, relData);
          return `${meta.name}:${meta.attitude}`;
        })
        .join(' / ');
    }

    function getPrimaryFactionPowerStats(snapshot) {
      const preferredName = toText(
        deepGet(snapshot, 'locationData.鎺屾帶鍔垮姏', snapshot && snapshot.鍔垮姏 && snapshot.鍔垮姏[0] ? snapshot.鍔垮姏[0][0] : ''),
        snapshot && snapshot.鍔垮姏 && snapshot.鍔垮姏[0] ? snapshot.鍔垮姏[0][0] : ''
      );
      const fallbackEntry = snapshot && Array.isArray(snapshot.orgEntries) && snapshot.orgEntries.length ? snapshot.orgEntries[0] : ['', {}];
      const matchedEntry = snapshot && Array.isArray(snapshot.orgEntries)
        ? (snapshot.orgEntries.find(([name]) => name === preferredName) || fallbackEntry)
        : fallbackEntry;
      const matchedName = toText(matchedEntry && matchedEntry[0], preferredName || '鏈煡');
      const matchedData = matchedEntry && matchedEntry[1] ? matchedEntry[1] : {};
      return {
        name: matchedName,
        limit: toNumber(deepGet(matchedData, '鎴樺姏缁熻.鏋侀檺鏂楃綏', 0), 0),
        super: toNumber(deepGet(matchedData, '鎴樺姏缁熻.瓒呯骇鏂楃綏', 0), 0),
        title: toNumber(deepGet(matchedData, '鎴樺姏缁熻.灏佸彿鏂楃綏', 0), 0)
      };
    }

    function collectPendingRequests(activeChar, sd) {
      const result = [];
      const push = (text) => {
        if (text) result.push(text);
      };

      const quest = activeChar && activeChar.浠诲姟璇锋眰;
      if (quest && toText(quest.action, '鏃?) !== '鏃?) {
        push(`浠诲姟锛?{toText(quest.浠诲姟鍚嶇О, '鏈懡鍚嶄换鍔?)}`);
      }

      const promotion = activeChar && activeChar.鏅嬪崌璇锋眰;
      if (promotion && toText(promotion.鐩爣鍔垮姏, '鏃?) !== '鏃?) {
        push(`鏅嬪崌锛?{toText(promotion.鐩爣鍔垮姏)} / ${toText(promotion.鐩爣鑱屼綅, '')}`.trim());
      }

      const donate = activeChar && activeChar.鎹愮尞璇锋眰;
      if (donate && toText(donate.鐗╁搧鍚嶇О, '鏃?) !== '鏃?) {
        push(`鎹愮尞锛?{toText(donate.鐗╁搧鍚嶇О)} 脳 ${toNumber(donate.鏁伴噺, 1)}`);
      }

      const interact = activeChar && activeChar.浜掑姩璇锋眰;
      if (interact && toText(interact.鍔ㄤ綔, '鏃?) !== '鏃?) {
        push(`浜掑姩锛?{toText(interact.鐩爣浜虹墿, '鏈煡瀵硅薄')} / ${toText(interact.鍔ㄤ綔, '浜掑姩')}`);
      }

      return result;
    }

    function 鑾峰彇鏁版嵁搴撻€夐」琛ㄥ揩鐓?) {
      const 宸茶闂獥鍙?= new Set();
      for (const 鐩爣绐楀彛 of 鑾峰彇杞藉叆鍣ㄥ€欓€夌獥鍙?)) {
        if (!鐩爣绐楀彛 || 宸茶闂獥鍙?has(鐩爣绐楀彛)) continue;
        宸茶闂獥鍙?add(鐩爣绐楀彛);
        try {
          const 鏁版嵁搴撴帴鍙?= 鐩爣绐楀彛.AutoCardUpdaterAPI;
          if (!鏁版嵁搴撴帴鍙?|| typeof 鏁版嵁搴撴帴鍙?exportTableAsJson !== 'function') continue;
          const 瀵煎嚭缁撴灉 = 鏁版嵁搴撴帴鍙?exportTableAsJson();
          if (瀵煎嚭缁撴灉 && typeof 瀵煎嚭缁撴灉 === 'object') return 瀵煎嚭缁撴灉;
        } catch (閿欒) {}
      }
      return null;
    }

    function 浠庢暟鎹簱蹇収瀹氫綅閫夐」琛?鏁版嵁搴撳揩鐓? {
      if (!鏁版嵁搴撳揩鐓?|| typeof 鏁版嵁搴撳揩鐓?!== 'object') return null;
      const 琛ㄦ潯鐩垪琛?= safeEntries(鏁版嵁搴撳揩鐓?
        .filter(([閿悕, 琛ㄥ璞) => /^sheet_/i.test(toText(閿悕, '')) && 琛ㄥ璞?&& typeof 琛ㄥ璞?=== 'object');
      for (const [, 琛ㄥ璞 of 琛ㄦ潯鐩垪琛? {
        const 琛ㄥ悕 = toText(琛ㄥ璞?&& 琛ㄥ璞?name, '').trim();
        if (琛ㄥ悕 === '閫夐」琛?) return 琛ㄥ璞?
      }
      for (const [, 琛ㄥ璞 of 琛ㄦ潯鐩垪琛? {
        const 琛ㄥ悕 = toText(琛ㄥ璞?&& 琛ㄥ璞?name, '').trim();
        if (琛ㄥ悕.includes('閫夐」')) return 琛ㄥ璞?
      }
      return null;
    }

    function 璇诲彇閫夐」琛ㄩ琛屽€欓€夋枃鏈?閫夐」琛ㄥ璞? {
      const 鍒楀悕鍒楄〃 = ['閫夐」涓€', '閫夐」浜?, '閫夐」涓?, '閫夐」鍥?];
      const 缁撴灉鍒楄〃 = [];
      if (!閫夐」琛ㄥ璞?|| typeof 閫夐」琛ㄥ璞?!== 'object') return 缁撴灉鍒楄〃;

      const 鍐呭琛屽垪琛?= Array.isArray(閫夐」琛ㄥ璞?content) ? 閫夐」琛ㄥ璞?content : [];
      if (鍐呭琛屽垪琛?length) {
        const 鏍囬琛?= Array.isArray(鍐呭琛屽垪琛╗0])
          ? 鍐呭琛屽垪琛╗0].map(鍊?=> toText(鍊? '').trim())
          : [];
        const 鏁扮粍鏁版嵁琛?= 鍐呭琛屽垪琛?find((琛? 搴忓彿) => 搴忓彿 > 0 && Array.isArray(琛? && 琛?length);
        if (鏍囬琛?length && 鏁扮粍鏁版嵁琛? {
          鍒楀悕鍒楄〃.forEach(鍒楀悕 => {
            const 鍒楃储寮?= 鏍囬琛?indexOf(鍒楀悕);
            if (鍒楃储寮?>= 0) 缁撴灉鍒楄〃.push(toText(鏁扮粍鏁版嵁琛孾鍒楃储寮昡, '').trim());
          });
        }
        if (!缁撴灉鍒楄〃.some(Boolean)) {
          const 瀵硅薄鏁版嵁琛?= 鍐呭琛屽垪琛?find((琛? 搴忓彿) => 搴忓彿 > 0 && 琛?&& typeof 琛?=== 'object' && !Array.isArray(琛?);
          if (瀵硅薄鏁版嵁琛? {
            鍒楀悕鍒楄〃.forEach(鍒楀悕 => {
              缁撴灉鍒楄〃.push(toText(瀵硅薄鏁版嵁琛孾鍒楀悕], '').trim());
            });
          }
        }
      }

      if (!缁撴灉鍒楄〃.some(Boolean)) {
        const 琛屽璞?= Array.isArray(閫夐」琛ㄥ璞?rows) ? (閫夐」琛ㄥ璞?rows[0] || null) : null;
        if (琛屽璞?&& typeof 琛屽璞?=== 'object' && !Array.isArray(琛屽璞?) {
          鍒楀悕鍒楄〃.forEach(鍒楀悕 => {
            缁撴灉鍒楄〃.push(toText(琛屽璞鍒楀悕], '').trim());
          });
        }
      }
      return 缁撴灉鍒楄〃.filter(Boolean);
    }

    function 璇诲彇閫夐」琛ㄥ€欓€夋枃鏈垪琛?) {
      const 鏁版嵁搴撳揩鐓?= 鑾峰彇鏁版嵁搴撻€夐」琛ㄥ揩鐓?);
      if (!鏁版嵁搴撳揩鐓? return [];
      const 閫夐」琛ㄥ璞?= 浠庢暟鎹簱蹇収瀹氫綅閫夐」琛?鏁版嵁搴撳揩鐓?;
      if (!閫夐」琛ㄥ璞? return [];
      return 璇诲彇閫夐」琛ㄩ琛屽€欓€夋枃鏈?閫夐」琛ㄥ璞?;
    }

    function 褰掍竴鍖栧€欓€夐€夐」鍘熸枃(鍘熷鍊?= '') {
      const 鍘熷鏂囨湰 = toText(鍘熷鍊? '').trim();
      if (!鍘熷鏂囨湰) return '';
      const 鍘诲墠缂€鏂囨湰 = 鍘熷鏂囨湰
        .replace(/^\s*(?:閫夐」)?[涓€浜屼笁鍥?234鈶犫憽鈶⑩懀]\s*[锛?銆?)锛塢\s*/i, '')
        .replace(/^\s*[-鈥⒙穄\s*/, '')
        .trim();
      if (!鍘诲墠缂€鏂囨湰) return '';
      if (hasUiPlaceholderToken(鍘诲墠缂€鏂囨湰)) return '';
      if (isShellPlaceholderText(鍘诲墠缂€鏂囨湰)) return '';
      return 鍘诲墠缂€鏂囨湰;
    }

    function 褰掍竴鍖栧€欓€夐€夐」閿?鍘熷鍊?= '') {
      let 瑙勮寖鏂囨湰 = toText(鍘熷鍊? '').toLowerCase();
      if (!瑙勮寖鏂囨湰) return '';
      const 鍚屼箟璇嶈鍒?= [
        { 鏍硅瘝: '鍓嶅線', 鍖归厤: /(鍓嶅線|鍘诲線|鍔ㄨ韩|鍚▼|鍑哄彂|鍓嶅幓|璧惰荡|璧跺線|濂旇荡)/g },
        { 鏍硅瘝: '鎺㈢储', 鍖归厤: /(鎺㈢储|鎺㈡煡|渚︽煡|宸℃煡|鎼滃|鏌ユ帰)/g },
        { 鏍硅瘝: '鏁寸悊', 鍖归厤: /(鏁寸悊|鏁村|绛瑰|鍑嗗|鐩樼偣|褰掓暣)/g },
        { 鏍硅瘝: '浼戞暣', 鍖归厤: /(浼戞暣|浼戞伅|璋冩伅|鎭㈠|鐤椾激|淇吇)/g },
        { 鏍硅瘝: '浜ゆ槗', 鍖归厤: /(浜ゆ槗|涔板崠|閲囪喘|鍑哄敭|鍏戞崲)/g },
        { 鏍硅瘝: '鎴樻枟', 鍖归厤: /(鎴樻枟|浜ゆ垬|杩庢垬|鍑烘墜|鏀诲嚮|瀵瑰喅)/g },
      ];
      鍚屼箟璇嶈鍒?forEach(瑙勫垯 => {
        瑙勮寖鏂囨湰 = 瑙勮寖鏂囨湰.replace(瑙勫垯.鍖归厤, 瑙勫垯.鏍硅瘝);
      });
      瑙勮寖鏂囨湰 = 瑙勮寖鏂囨湰
        .replace(/[\s\u3000]+/g, '')
        .replace(/[`~!@#$%^&*()_\-+=|\\[\]{};:'",.<>/?锛屻€傦紒锛熴€侊紱锛氣€溾€濃€樷€欙紙锛夈€愩€戙€娿€嬄封€/g, '');
      return 瑙勮寖鏂囨湰.trim();
    }

    function 鏋勫缓鍊欓€夐€夐」鏉＄洰(鍘熸枃 = '', 搴忓彿 = 0) {
      const 娓呮礂鍚庢枃鏈?= 褰掍竴鍖栧€欓€夐€夐」鍘熸枃(鍘熸枃);
      if (!娓呮礂鍚庢枃鏈? return null;
      const 瑙勮寖閿?= 褰掍竴鍖栧€欓€夐€夐」閿?娓呮礂鍚庢枃鏈? || `閫夐」_${搴忓彿}_${娓呮礂鍚庢枃鏈瑌`;
      return {
        搴忓彿: Math.max(1, toNumber(搴忓彿, 1)),
        鍘熸枃: 娓呮礂鍚庢枃鏈?
        瑙勮寖閿?
        灞曠ず鏂囨: shortenText(娓呮礂鍚庢枃鏈? 26)
      };
    }

    function 鏋勫缓鍙帺閫夐」灞曠ず鍒楄〃(蹇収) {
      const 閫夐」鍘熸枃鍒楄〃 = 璇诲彇閫夐」琛ㄥ€欓€夋枃鏈垪琛?);
      const 宸插綍鍏ラ敭闆嗗悎 = new Set();
      const 灞曠ず鏉＄洰鍒楄〃 = [];
      (Array.isArray(閫夐」鍘熸枃鍒楄〃) ? 閫夐」鍘熸枃鍒楄〃 : []).forEach((鍘熸枃, 绱㈠紩) => {
        const 鏉＄洰 = 鏋勫缓鍊欓€夐€夐」鏉＄洰(鍘熸枃, 绱㈠紩 + 1);
        if (!鏉＄洰) return;
        const 瑙勮寖閿?= toText(鏉＄洰.瑙勮寖閿? '').trim();
        if (!瑙勮寖閿?|| 宸插綍鍏ラ敭闆嗗悎.has(瑙勮寖閿?) return;
        宸插綍鍏ラ敭闆嗗悎.add(瑙勮寖閿?;
        灞曠ず鏉＄洰鍒楄〃.push(鏉＄洰);
      });
      return 灞曠ず鏉＄洰鍒楄〃.slice(0, 4);
    }

    function 鎻愬彇鍙欎簨鍙ユ(source = '') {
      const 鍘熸枃 = String(source || '')
        .replace(/\s+/g, ' ')
        .trim();
      if (!鍘熸枃) return [];
      return 鍘熸枃
        .split(/[銆傦紒锛??锛?\n]+/)
        .map(segment => String(segment || '').trim())
        .filter(Boolean);
    }

    function 鏋勫缓鍙欎簨涓夋鏂囨({ 鐜拌薄 = '', 鍚庢灉 = '', 閽╁瓙 = '', 鍥為€€ = '鏆傛棤璁板綍' } = {}) {
      const 鐜拌薄鏂囨湰 = toText(鐜拌薄, '').trim() || 鍥為€€;
      const 鍚庢灉鏂囨湰 = toText(鍚庢灉, '').trim() || '灞€鍔夸粛鍦ㄦ紨鍖?;
      const 閽╁瓙鏂囨湰 = toText(閽╁瓙, '').trim() || '鍙户缁窡杩涚浉鍏充汉鐗╂垨鍦扮偣';
      return `${鐜拌薄鏂囨湰} 锝?鍚庢灉锛?{鍚庢灉鏂囨湰} 锝?涓嬩竴閽╁瓙锛?{閽╁瓙鏂囨湰}`;
    }

    function 褰掍竴鍖栨儏鎶ュ彊浜嬫枃妗?source = '') {
      const 鍙ユ = 鎻愬彇鍙欎簨鍙ユ(source);
      return 鏋勫缓鍙欎簨涓夋鏂囨({
        鐜拌薄: 鍙ユ[0] || toText(source, '鏆傛棤璁板綍'),
        鍚庢灉: 鍙ユ[1] || '鎯呮姤宸插叆妗?,
        閽╁瓙: 鍙ユ[2] || '寤鸿缁х画鏍稿疄绾跨储鏉ユ簮',
        鍥為€€: '鏆傛棤璁板綍'
      });
    }

    function buildRecentNewsSummary(snapshot, options = {}) {
      const { seqLimit = 2, intelLimit = 2 } = options || {};
      const globalNews = (snapshot.timelineEntries || [])
        .filter(([, item]) => /done|handled|瀹屾垚|宸插鐞?.test(toText(deepGet(item, '鐘舵€?, ''), '')))
        .slice(0, Math.max(1, seqLimit))
        .map(([key, item]) => ({
        title: `鍏ㄥ眬 / ${key}`,
        desc: 鏋勫缓鍙欎簨涓夋鏂囨({
          鐜拌薄: toText(deepGet(item, '浜嬩欢', ''), '鏆傛棤璁板綍'),
          鍚庢灉: toText(deepGet(item, '鍚庢灉', deepGet(item, '褰卞搷', deepGet(item, '缁撴灉', ''))), ''),
          閽╁瓙: toText(deepGet(item, '涓嬩竴姝?, deepGet(item, '鍚庣画', deepGet(item, '绾跨储', ''))), ''),
          鍥為€€: '鏆傛棤璁板綍'
        })
      }));
      const personalNews = (snapshot.unlockedKnowledges || []).slice(-Math.max(1, intelLimit)).reverse().map((text, index) => ({
        title: `涓汉 / 瑙侀椈 ${index + 1}`,
        desc: 褰掍竴鍖栨儏鎶ュ彊浜嬫枃妗?text)
      }));
      return {
        globalNews,
        personalNews,
        cards: globalNews.concat(personalNews).length ? globalNews.concat(personalNews) : [{ title: '杩戞湡瑙侀椈', desc: '鏆傛棤鏂扮殑鍏ㄥ眬鎴栦釜浜鸿闂汇€? }],
        summary: globalNews[0] ? globalNews[0].desc : (personalNews[0] ? personalNews[0].desc : '鏆傛棤杩戞湡瑙侀椈')
      };
    }

    function buildRecentPlanSummary(snapshot, options = {}) {
      const { worldLimit = 2, recordLimit = 2 } = options || {};
      const worldPlans = (snapshot.timelineEntries || []).filter(([, item]) => !/done|handled|瀹屾垚|宸插鐞唡鍙栨秷|cancel/i.test(toText(deepGet(item, '鐘舵€?, 'pending'), 'pending'))).slice(0, Math.max(1, worldLimit)).map(([name, item]) => ({
        title: `涓栫晫 / ${name}`,
        desc: 鏋勫缓鍙欎簨涓夋鏂囨({
          鐜拌薄: toText(deepGet(item, '浜嬩欢', '鏃?), '鏃?),
          鍚庢灉: `棰勮 Tick ${toText(deepGet(item, '瑙﹀彂tick', 0), '0')} 瑙﹀彂锛屽綋鍓?${toText(deepGet(item, '鐘舵€?, 'pending'), 'pending')}`,
          閽╁瓙: toText(deepGet(item, '涓嬩竴姝?, deepGet(item, '鍚庣画', '')), ''),
          鍥為€€: '鏆傛棤瀹夋帓'
        })
      }));
      const personalPlans = (snapshot.recordEntries || []).filter(([, item]) => toText(item && item['鐘舵€?], '杩涜涓?) !== '宸插畬鎴? && toText(item && item['鐘舵€?], '杩涜涓?) !== '宸叉斁寮?).slice(0, Math.max(1, recordLimit)).map(([name, item]) => ({
        title: `涓汉 / ${name}`,
        desc: 鏋勫缓鍙欎簨涓夋鏂囨({
          鐜拌薄: toText(item && item['鎻忚堪'], '鏃犳弿杩?),
          鍚庢灉: `杩涘害 ${toNumber(item && item['褰撳墠杩涘害'], 0)}/${toNumber(item && item['鐩爣杩涘害'], 1)}锛屽綋鍓?${toText(item && item['鐘舵€?], '杩涜涓?)}`,
          閽╁瓙: toText(item && (item['涓嬩竴姝?] || item['鍚庣画'] || item['寤鸿']), ''),
          鍥為€€: '鏆傛棤瀹夋帓'
        })
      }));
      return {
        worldPlans,
        personalPlans,
        cards: worldPlans.concat(personalPlans).length ? worldPlans.concat(personalPlans) : [{ title: '杩戞湡瀹夋帓', desc: '鏆傛棤寰呭鐞嗙殑涓栫晫鏃ョ▼鎴栦釜浜轰换鍔°€? }],
        summary: worldPlans[0] ? worldPlans[0].desc : (personalPlans[0] ? personalPlans[0].desc : '鏆傛棤杩戞湡瀹夋帓')
      };
    }

    function 鏀堕泦鏈湴鍟嗗簵鍟嗗搧鏍锋湰(locationData = {}) {
      const 鏍锋湰 = [];
      safeEntries(deepGet(locationData, '鍟嗗簵', {})).forEach(([鍟嗗簵鍚? 鍟嗗簵鏁版嵁]) => {
        safeEntries(deepGet(鍟嗗簵鏁版嵁, '搴撳瓨', {})).forEach(([鍟嗗搧鍚? 鍟嗗搧鏁版嵁]) => {
          if (!鍟嗗搧鏁版嵁 || typeof 鍟嗗搧鏁版嵁 !== 'object') return;
          鏍锋湰.push({
            鍟嗗簵鍚? toText(鍟嗗簵鍚? '鏈煡鍟嗗簵'),
            鍟嗗搧鍚? toText(鍟嗗搧鍚? '鏈煡鍟嗗搧'),
            搴撳瓨: Math.max(0, toNumber(鍟嗗搧鏁版嵁 && 鍟嗗搧鏁版嵁['搴撳瓨'], 0)),
            浠锋牸: Math.max(0, toNumber(鍟嗗搧鏁版嵁 && 鍟嗗搧鏁版嵁['浠锋牸'], 0)),
          });
        });
      });
      return 鏍锋湰;
    }

    function 璁＄畻鏈湴渚涚粰鎸囨爣(locationData = {}) {
      const 鍟嗗搧鏍锋湰 = 鏀堕泦鏈湴鍟嗗簵鍟嗗搧鏍锋湰(locationData);
      if (!鍟嗗搧鏍锋湰.length) return '鏃犱緵缁?;
      const 鎬诲簱瀛?= 鍟嗗搧鏍锋湰.reduce((鎬诲拰, 椤? => 鎬诲拰 + Math.max(0, toNumber(椤?&& 椤筟'搴撳瓨'], 0)), 0);
      const 缂鸿揣鏁?= 鍟嗗搧鏍锋湰.filter(椤?=> Math.max(0, toNumber(椤?&& 椤筟'搴撳瓨'], 0)) <= 0).length;
      const 鍧囧€煎簱瀛?= 鎬诲簱瀛?/ Math.max(1, 鍟嗗搧鏍锋湰.length);
      const 缂鸿揣鍗犳瘮 = 缂鸿揣鏁?/ Math.max(1, 鍟嗗搧鏍锋湰.length);
      if (鎬诲簱瀛?<= 0 || 缂鸿揣鍗犳瘮 >= 0.55 || 鍧囧€煎簱瀛?< 1.5) return '绱х己';
      if (缂鸿揣鍗犳瘮 >= 0.3 || 鍧囧€煎簱瀛?< 3.5) return '鍋忕揣';
      if (鍧囧€煎簱瀛?>= 9) return '鍏呰';
      return '骞宠　';
    }

    function 璁＄畻鏈湴浠锋牸甯︽寚鏍?locationData = {}) {
      const 鍟嗗搧鏍锋湰 = 鏀堕泦鏈湴鍟嗗簵鍟嗗搧鏍锋湰(locationData);
      const 浠锋牸鏍锋湰 = 鍟嗗搧鏍锋湰.map(椤?=> Math.max(0, toNumber(椤?&& 椤筟'浠锋牸'], 0))).filter(浠锋牸 => 浠锋牸 > 0);
      if (!浠锋牸鏍锋湰.length) return '鏃?;
      const 鍧囦环 = 浠锋牸鏍锋湰.reduce((鎬诲拰, 浠锋牸) => 鎬诲拰 + 浠锋牸, 0) / Math.max(1, 浠锋牸鏍锋湰.length);
      if (鍧囦环 < 100000) return '浣庝环';
      if (鍧囦环 < 2000000) return '涓环';
      if (鍧囦环 < 50000000) return '楂樹环';
      return '瓒呴珮浠?;
    }

    function 鎻愬彇鏈€杩戞垚浜ゅ奖鍝嶆寚鏍?绯荤粺鎾姤 = '', rootData = null) {
      const 鎾姤鏂囨湰 = toText(绯荤粺鎾姤, '');
      const 鏍囩鍖归厤 = Array.from(鎾姤鏂囨湰.matchAll(/\[(涔板叆鐑瓅鍗栧嚭鐑瓅绔炴媿鐑瓅鍏戞崲鐑瓅甯傚満娉㈠姩)\]/g))
        .map(match => toText(match && match[1], '').trim())
        .filter(Boolean);
      if (鏍囩鍖归厤.length) return 鏍囩鍖归厤[鏍囩鍖归厤.length - 1];
      const 鎯呮姤鏉＄洰 = safeEntries(deepGet(rootData, 'world.鏈哄瘑鎯呮姤', {}))
        .filter(([, item]) => item && typeof item === 'object')
        .map(([, item]) => ({
          鏉ユ簮: toText(item && item['瑙﹀彂鏉ユ簮'], ''),
          璇佹嵁鏉ユ簮鍒楄〃: Array.isArray(item && item['璇佹嵁鏉ユ簮鍒楄〃']) ? item['璇佹嵁鏉ユ簮鍒楄〃'] : [],
          鏈€鍚庤瘉鎹畉ick: toNumber(item && item['鏈€鍚庤瘉鎹畉ick'], 0),
          鏈€杩戞牳瀹瀟ick: toNumber(item && item['鏈€杩戞牳瀹瀟ick'], 0),
          鏍囬: toText(item && item['鏍囬'], ''),
          鍐呭: toText(item && item['鍐呭'], ''),
        }))
        .filter(item => item.鏉ユ簮 === '浜ゆ槗' || item.璇佹嵁鏉ユ簮鍒楄〃.some(鏉ユ簮 => toText(鏉ユ簮, '') === '浜ゆ槗'))
        .sort((a, b) => Math.max(b.鏈€鍚庤瘉鎹畉ick, b.鏈€杩戞牳瀹瀟ick) - Math.max(a.鏈€鍚庤瘉鎹畉ick, a.鏈€杩戞牳瀹瀟ick));
      if (!鎯呮姤鏉＄洰.length) return '骞崇ǔ';
      const 鏂囨湰 = `${鎯呮姤鏉＄洰[0].鏍囬} ${鎯呮姤鏉＄洰[0].鍐呭}`;
      if (/绔炴媿|鎷嶅崠/.test(鏂囨湰)) return '绔炴媿鐑?;
      if (/鍏戞崲|榄傜伒濉?.test(鏂囨湰)) return '鍏戞崲鐑?;
      if (/鍗栧嚭|鍑哄敭|鎶涘敭|鍥炴敹/.test(鏂囨湰)) return '鍗栧嚭鐑?;
      if (/涔板叆|璐拱|閲囪喘|鎶㈣喘|姹傝喘/.test(鏂囨湰)) return '涔板叆鐑?;
      return '甯傚満娉㈠姩';
    }

    function clampMarketMultiplier(value) {
      const 鏁板€?= Number(value);
      if (!Number.isFinite(鏁板€?) return 1;
      return Math.max(0.7, Math.min(1.6, 鏁板€?);
    }

    function 鑾峰彇甯傚満渚涚粰鍊嶇巼(渚涚粰 = '骞宠　') {
      const 鏂囨湰 = toText(渚涚粰, '骞宠　');
      if (鏂囨湰 === '绱х己') return 1.25;
      if (鏂囨湰 === '鍋忕揣') return 1.12;
      if (鏂囨湰 === '鍏呰') return 0.92;
      return 1;
    }

    function 鑾峰彇甯傚満缁忔祹鍊嶇巼(缁忔祹鐘跺喌 = '鏈煡') {
      const 鏂囨湰 = toText(缁忔祹鐘跺喌, '鏈煡');
      if (鏂囨湰 === '绻佽崳') return 0.95;
      if (鏂囨湰 === '钀ф潯') return 1.12;
      return 1;
    }

    function 鑾峰彇甯傚満瓒嬪娍鍊嶇巼(鏈€杩戞垚浜ゅ奖鍝?= '骞崇ǔ') {
      const 鏂囨湰 = toText(鏈€杩戞垚浜ゅ奖鍝? '骞崇ǔ');
      if (鏂囨湰 === '涔板叆鐑?) return 1.08;
      if (鏂囨湰 === '鍗栧嚭鐑?) return 0.92;
      if (鏂囨湰 === '绔炴媿鐑?) return 1.12;
      if (鏂囨湰 === '甯傚満娉㈠姩') return 1.06;
      return 1;
    }

    function 鏋勫缓甯傚満鍊嶇巼璇存槑(甯傚満娲剧敓 = {}) {
      const 鏍囩 = [];
      const 渚涚粰 = toText(甯傚満娲剧敓.鏈湴渚涚粰, '骞宠　');
      const 缁忔祹 = toText(甯傚満娲剧敓.缁忔祹鐘跺喌, '鏈煡');
      const 瓒嬪娍 = toText(甯傚満娲剧敓.鏈€杩戞垚浜ゅ奖鍝? '骞崇ǔ');
      if (渚涚粰 === '绱х己') 鏍囩.push('绱х己涓婃诞');
      else if (渚涚粰 === '鍋忕揣') 鏍囩.push('渚涚粰鍋忕揣');
      else if (渚涚粰 === '鍏呰') 鏍囩.push('渚涚粰鍏呰');
      if (缁忔祹 === '绻佽崳') 鏍囩.push('绻佽崳鍥炶惤');
      else if (缁忔祹 === '钀ф潯') 鏍囩.push('钀ф潯涓婃诞');
      if (瓒嬪娍 === '涔板叆鐑?) 鏍囩.push('涔板叆鐑笂娴?);
      else if (瓒嬪娍 === '鍗栧嚭鐑?) 鏍囩.push('鍗栧嚭鐑洖钀?);
      else if (瓒嬪娍 === '绔炴媿鐑?) 鏍囩.push('绔炴媿鐑?);
      else if (瓒嬪娍 === '甯傚満娉㈠姩') 鏍囩.push('甯傚満娉㈠姩');
      return 鏍囩.join(' / ') || '骞崇ǔ';
    }

    function 鏋勫缓甯傚満娲剧敓妯″瀷(locationData = {}, rootData = null) {
      const 鏈湴渚涚粰 = 璁＄畻鏈湴渚涚粰鎸囨爣(locationData);
      const 浠锋牸甯?= 璁＄畻鏈湴浠锋牸甯︽寚鏍?locationData);
      const 鏈€杩戞垚浜ゅ奖鍝?= 鎻愬彇鏈€杩戞垚浜ゅ奖鍝嶆寚鏍?deepGet(rootData, 'sys.绯荤粺鎾姤', ''), rootData);
      const 缁忔祹鐘跺喌 = toText(locationData && locationData['缁忔祹鐘跺喌'], '鏈煡');
      const 渚涚粰鍊嶇巼 = 鑾峰彇甯傚満渚涚粰鍊嶇巼(鏈湴渚涚粰);
      const 缁忔祹鍊嶇巼 = 鑾峰彇甯傚満缁忔祹鍊嶇巼(缁忔祹鐘跺喌);
      const 瓒嬪娍鍊嶇巼 = 鑾峰彇甯傚満瓒嬪娍鍊嶇巼(鏈€杩戞垚浜ゅ奖鍝?;
      const 涔板叆鍊嶇巼 = clampMarketMultiplier(渚涚粰鍊嶇巼 * 缁忔祹鍊嶇巼 * 瓒嬪娍鍊嶇巼);
      const 鍗栧嚭鍊嶇巼 = clampMarketMultiplier(渚涚粰鍊嶇巼 * 瓒嬪娍鍊嶇巼);
      const 鎴愪氦鐜囦慨姝?= Math.round((1 - 涔板叆鍊嶇巼) * 35);
      const 娲剧敓 = {
        鏈湴渚涚粰,
        浠锋牸甯?
        鏈€杩戞垚浜ゅ奖鍝?
        缁忔祹鐘跺喌,
        渚涚粰鍊嶇巼,
        缁忔祹鍊嶇巼,
        瓒嬪娍鍊嶇巼,
        涔板叆鍊嶇巼,
        鍗栧嚭鍊嶇巼,
        鎴愪氦鐜囦慨姝?
      };
      娲剧敓.璇存槑 = 鏋勫缓甯傚満鍊嶇巼璇存槑(娲剧敓);
      return 娲剧敓;
    }

    function buildSnapshot(sd) {
      const rawRootData = sd && typeof sd === 'object' ? sd : {};
      const contextMeta = getCurrentChatContextMeta();
      const runtimeReady = true;
      sd = rawRootData;
      const [activeName, activeChar] = resolveActiveCharacter(sd || {});
      const currentLoc = toText(deepGet(activeChar, '鐘舵€?浣嶇疆', '鏈煡'), '鏈煡').replace(/^鏂楃綏澶ч檰-/, '').replace(/^鏂楃伒澶ч檰-/, '');
      const locationInfo = resolveLocationData(sd, currentLoc);
      const locationData = locationInfo.data || {};
      const storeNames = safeEntries(locationData && locationData.鍟嗗簵).map(([name]) => name);
      const dynamicLocationNames = safeEntries(deepGet(sd, 'world.鍔ㄦ€佸湴鐐?, {})).map(([name]) => name);
      const timelineEntries = safeEntries(deepGet(sd, 'world.鏃堕棿绾?, {})).sort((a, b) => toNumber(deepGet(b[1], '瑙﹀彂tick', 0), 0) - toNumber(deepGet(a[1], '瑙﹀彂tick', 0), 0));
      const factions = safeEntries(deepGet(activeChar, '绀句氦.鍔垮姏', {}));
      const relations = safeEntries(deepGet(activeChar, '绀句氦.鍏崇郴', {})).sort((a, b) => toNumber(deepGet(b[1], '濂芥劅搴?, 0), 0) - toNumber(deepGet(a[1], '濂芥劅搴?, 0), 0));
      const unlockedKnowledges = Array.isArray(activeChar && activeChar.宸叉帉鎻℃儏鎶? ? activeChar.宸叉帉鎻℃儏鎶?: [];
      const inventoryEntries = safeEntries(activeChar && activeChar.鑳屽寘);
      const flagEntries = [];
      if (deepGet(sd, 'world.鍏芥疆宸茶Е鍙?, false)) flagEntries.push(['鍏芥疆宸茶Е鍙?, true]);
      if (deepGet(sd, 'world.浼犵伒濉斿崈骞撮瓊鐏靛紑鏀?, false)) flagEntries.push(['浼犵伒濉斿崈骞撮瓊鐏靛紑鏀?, true]);
      const orgEntries = safeEntries(sd && sd.org).sort((a, b) => {
        const aFav = factions.some(([name]) => name === a[0]) ? 1 : 0;
        const bFav = factions.some(([name]) => name === b[0]) ? 1 : 0;
        if (aFav !== bFav) return bFav - aFav;
        return toNumber(deepGet(b[1], '褰卞搷鍔?, 0), 0) - toNumber(deepGet(a[1], '褰卞搷鍔?, 0), 0);
      });

      const spiritEntries = safeEntries(activeChar && activeChar.姝﹂瓊);
      const primarySpirit = spiritEntries[0]
        ? buildSpiritConfig(spiritEntries[0][0], spiritEntries[0][1], '绗竴姝﹂瓊璇︾粏椤?, '绗竴姝﹂瓊', 'cyan', ['char', activeName, '姝﹂瓊', spiritEntries[0][0]])
        : buildSpiritConfig('绗竴姝﹂瓊', {}, '绗竴姝﹂瓊璇︾粏椤?, '绗竴姝﹂瓊', 'cyan');
      const secondarySpirit = spiritEntries[1]
        ? buildSpiritConfig(spiritEntries[1][0], spiritEntries[1][1], '绗簩姝﹂瓊璇︾粏椤?, '绗簩姝﹂瓊', 'gold', ['char', activeName, '姝﹂瓊', spiritEntries[1][0]])
        : null;
      const bloodline = buildBloodlineConfig(activeChar || {}, activeName, ['char', activeName, '琛€鑴変箣鍔?]);

      // --- 鏀堕泦棰濆鑳藉姏涓庡姛娉曪紝鏀惧埌澶栧眰缁欑敓鍛藉浘璋变娇鐢?---
      const safeRecords = (obj) => {
        return Object.entries(obj || {}).filter(([k, v]) => k !== '鏃? && typeof v === 'object' && v !== null).map(([k, v]) => ({
          recordKey: k,
          name: k,
          ...v
        }));
      };
      const extraSkills = [];
      const artSkillNameSet = new Set(safeEntries(deepGet(activeChar, '鍔熸硶', {})).map(([name]) => toText(name, '').trim()).filter(Boolean));
      safeRecords(deepGet(activeChar, '鍔熸硶', {})).forEach(art => {
        extraSkills.push({
          category: '鍔熸硶缁濆',
          name: art.name,
          level: `Lv.${art.lv || 1} / ${toText(art.澧冪晫, '鏈叆闂?)}`,
          desc: toText(art.鎻忚堪, '鏆傛棤鎻忚堪'),
          preview: ''
        });
      });
      if (bloodline.valid) {
        safeRecords(deepGet(activeChar, '琛€鑴変箣鍔?鎶€鑳?, {})).forEach(skill => {
          extraSkills.push({
            category: '琛€鑴夋暎鎶€',
            name: skill.name,
            level: toText(skill.鐘舵€? '宸叉帉鎻?),
            desc: toText(skill.鎻忚堪 || skill.鏁堟灉鎻忚堪 || skill.effectDesc, '鏃犺鏄?),
            preview: buildSkillDesignerPreviewKey({
              path: ['char', activeName, '琛€鑴変箣鍔?, '鎶€鑳?, skill.recordKey || skill.name],
              label: toText(skill.name, skill.recordKey || '琛€鑴夋暎鎶€'),
              category: '琛€鑴夋暎鎶€',
              scope: 'blood_skill',
            })
          });
        });
        safeRecords(deepGet(activeChar, '琛€鑴変箣鍔?琚姩', {})).forEach(skill => {
          extraSkills.push({
            category: '琛€鑴夌壒鎬?,
            name: skill.name,
            level: toText(skill.鐘舵€? '宸插浐鍖?),
            desc: toText(skill.鎻忚堪 || skill.鏁堟灉鎻忚堪 || skill.effectDesc, '鏃犺鏄?),
            preview: buildSkillDesignerPreviewKey({
              path: ['char', activeName, '琛€鑴変箣鍔?, '琚姩', skill.recordKey || skill.name],
              label: toText(skill.name, skill.recordKey || '琛€鑴夌壒鎬?),
              category: '琛€鑴夌壒鎬?,
              scope: 'blood_passive',
            })
          });
        });
        safeEntries(deepGet(activeChar, '琛€鑴変箣鍔?姘镐箙鍔犳垚', {})).forEach(([name, bonus]) => {
          extraSkills.push({
            category: '琛€鑴夋案涔呮垚闀?,
            name: toText(bonus && bonus['鍚嶇О'], name || '姘镐箙鎴愰暱'),
            level: toText(bonus && bonus['鐘舵€?], '宸插浐鍖?),
            desc: toText(bonus && bonus['鏁堟灉鎻忚堪'], toText(bonus && bonus['鎻忚堪'], '鏃犺鏄?))
          });
        });
      }
      safeRecords(deepGet(activeChar, '姝﹂瓊铻嶅悎鎶€', {})).forEach(fusion => {
        const fusionName = toText(fusion['鍚嶇О'], fusion.name || fusion.recordKey || '姝﹂瓊铻嶅悎鎶€');
        extraSkills.push({
          category: '姝﹂瓊铻嶅悎鎶€',
          name: fusionName,
          level: `鎼。: ${toText(fusion['铻嶅悎瀵硅薄'], '鏈煡')}`,
          desc: toText(deepGet(fusion, '鎶€鑳芥暟鎹?鎻忚堪', '鏃?), '鐖嗗彂鎶€'),
          preview: buildSkillDesignerPreviewKey({
            path: ['char', activeName, '姝﹂瓊铻嶅悎鎶€', fusion.recordKey || fusionName, '鎶€鑳芥暟鎹?],
            label: toText(fusionName, fusion.recordKey || '姝﹂瓊铻嶅悎鎶€'),
            category: '姝﹂瓊铻嶅悎鎶€',
            scope: '姝﹂瓊铻嶅悎鎶€',
          })
        });
      });
      safeRecords(deepGet(activeChar, '鑷垱榄傛妧', {})).forEach(abi => {
        if (artSkillNameSet.has(toText(abi.recordKey || abi.name, '').trim())) return;
        extraSkills.push({
          category: '鑷垱榄傛妧',
          name: abi.name,
          level: toText(abi.鎶€鑳界被鍨?|| abi.涓诲畾浣? '琚姩'),
          desc: toText(abi.鏁堟灉鎻忚堪 || abi.鎴樻枟鎽樿?.涓€鍙ヨ瘽瀹氫綅, '鏆傛棤鎻忚堪'),
          preview: buildSkillDesignerPreviewKey({
            path: ['char', activeName, '鑷垱榄傛妧', abi.recordKey || abi.name],
            label: toText(abi.name, abi.recordKey || '鑷垱榄傛妧'),
            category: '鑷垱榄傛妧',
            scope: '鑷垱榄傛妧',
          })
        });
      });

      const nextNodeCandidates = [
        locationInfo.name,
        currentLoc,
        toText(deepGet(sd, 'world.鎷嶅崠.鍦扮偣', ''), ''),
        ...storeNames,
        ...safeEntries(deepGet(sd, 'world.鍦扮偣', {})).map(([name]) => name),
        ...dynamicLocationNames
      ].filter(Boolean);

      const mapNodeLabels = [];
      nextNodeCandidates.forEach(name => {
        if (!mapNodeLabels.includes(name)) mapNodeLabels.push(name);
      });

      const recentTitles = safeEntries(deepGet(activeChar, '绀句氦.绉板彿', {})).map(([name]) => name);
      const titleEntries = safeEntries(deepGet(activeChar, '绀句氦.绉板彿', {}));
      const recordEntries = safeEntries(deepGet(activeChar, '璁板綍', {}));
      const conditionEntries = safeEntries(deepGet(activeChar, '灞炴€?鐘舵€佹晥鏋?, {}));
      const soulBoneEntries = safeEntries(deepGet(activeChar, '榄傞', {}));
      const artEntries = safeEntries(deepGet(activeChar, '鍔熸硶', {})).sort((a, b) => toNumber(deepGet(b[1], 'lv', 0), 0) - toNumber(deepGet(a[1], 'lv', 0), 0));
      const customSkillEntries = safeEntries(deepGet(activeChar, '鑷垱榄傛妧', {})).filter(([name]) => !artSkillNameSet.has(toText(name, '').trim()));
      const combatHistoryEntries = safeEntries(deepGet(activeChar, '鎴樻枟鍘嗗彶', {})).sort((a, b) => toNumber(deepGet(b[1], '鏈€杩憈ick', 0), 0) - toNumber(deepGet(a[1], '鏈€杩憈ick', 0), 0));
      const pendingRequests = collectPendingRequests(activeChar || {}, sd || {});
      const bestiaryEntries = safeEntries(deepGet(sd, 'world.鍥鹃壌', {}));
      const forestKilledAge = toNumber(deepGet(sd, 'world.绱鍑绘潃骞撮檺', 0), 0);
      const 浠诲姟杩涜涓垪琛?= recordEntries.filter(([, item]) => item && typeof item === 'object' && !['宸插畬鎴?, '宸叉斁寮?, '澶辫触', '宸插け璐?].includes(toText(item && item['鐘舵€?], '杩涜涓?)));
      const 褰撳墠浠诲姟鑱氱劍鏉＄洰 = 浠诲姟杩涜涓垪琛╗0] || recordEntries[0] || null;
      const 褰撳墠浠诲姟鍚嶇О = 褰撳墠浠诲姟鑱氱劍鏉＄洰 ? toText(褰撳墠浠诲姟鑱氱劍鏉＄洰[0], '鏆傛棤浠诲姟') : '鏆傛棤浠诲姟';
      const 褰撳墠浠诲姟鏁版嵁 = 褰撳墠浠诲姟鑱氱劍鏉＄洰 && 褰撳墠浠诲姟鑱氱劍鏉＄洰[1] && typeof 褰撳墠浠诲姟鑱氱劍鏉＄洰[1] === 'object'
        ? 褰撳墠浠诲姟鑱氱劍鏉＄洰[1]
        : {};
      const 褰撳墠浠诲姟闃舵 = Math.max(1, toNumber(褰撳墠浠诲姟鏁版嵁 && 褰撳墠浠诲姟鏁版嵁['闃舵'], 1));
      const 褰撳墠浠诲姟闃舵鏂囨湰 = 褰撳墠浠诲姟鑱氱劍鏉＄洰 ? `绗?{褰撳墠浠诲姟闃舵}闃舵` : '鏃?;
      const 褰撳墠浠诲姟涓嬩竴鑺傜偣杩涘害 = 褰撳墠浠诲姟鑱氱劍鏉＄洰
        ? `${Math.max(0, toNumber(褰撳墠浠诲姟鏁版嵁 && 褰撳墠浠诲姟鏁版嵁['褰撳墠杩涘害'], 0))}/${Math.max(1, toNumber(褰撳墠浠诲姟鏁版嵁 && 褰撳墠浠诲姟鏁版嵁['鐩爣杩涘害'], 1))}`
        : '--';
      const 褰撳墠浠诲姟璺嚎 = 褰撳墠浠诲姟鑱氱劍鏉＄洰 ? toText(褰撳墠浠诲姟鏁版嵁 && 褰撳墠浠诲姟鏁版嵁['鍒嗘敮'], '鏈垽瀹?) : '鏃?;
      const 褰撳墠浠诲姟椋庨櫓绾у埆 = 褰撳墠浠诲姟鑱氱劍鏉＄洰 ? toText(褰撳墠浠诲姟鏁版嵁 && 褰撳墠浠诲姟鏁版嵁['椋庨櫓绾у埆'], '鏃?) : '鏃?;
      const 鏈哄瘑鎯呮姤鏉＄洰 = safeEntries(deepGet(sd, 'world.鏈哄瘑鎯呮姤', {})).filter(([, item]) => item && typeof item === 'object');
      const 寰呮牳瀹炴儏鎶ュ垪琛?= 鏈哄瘑鎯呮姤鏉＄洰.filter(([, item]) => toText(item && item['鏍稿疄鐘舵€?], '鍙枒') === '寰呮牳瀹?);
      const 鏈€杩戞牳瀹炴儏鎶?= 鏈哄瘑鎯呮姤鏉＄洰
        .map(([name, item]) => ({ 鍚嶇О: name, 鏁版嵁: item }))
        .sort((a, b) => toNumber(deepGet(b, '鏁版嵁.鏈€杩戞牳瀹瀟ick', 0), 0) - toNumber(deepGet(a, '鏁版嵁.鏈€杩戞牳瀹瀟ick', 0), 0))[0] || null;
      const 鎯呮姤鏈€杩戞牳瀹炵粨鏋?= 鏈€杩戞牳瀹炴儏鎶?        ? `${toText(deepGet(鏈€杩戞牳瀹炴儏鎶? '鏁版嵁.鏍囬', 鏈€杩戞牳瀹炴儏鎶?鍚嶇О), 鏈€杩戞牳瀹炴儏鎶?鍚嶇О)}:${toText(deepGet(鏈€杩戞牳瀹炴儏鎶? '鏁版嵁.鏈€杩戞牳瀹炵粨鏋?, '鏃?), '鏃?)}`
        : '鏆傛棤鏍稿疄璁板綍';
      const 鎯呮姤璇佹嵁鍑€鍊?= 鏈€杩戞牳瀹炴儏鎶?        ? Number((
            Math.max(0, toNumber(deepGet(鏈€杩戞牳瀹炴儏鎶? '鏁版嵁.璇佹嵁鏉冮噸', 0), 0))
            - Math.max(0, toNumber(deepGet(鏈€杩戞牳瀹炴儏鎶? '鏁版嵁.鍙嶈瘉鏉冮噸', 0), 0))
          ).toFixed(2))
        : 0;
      const 鍥鹃壌妗ｄ綅椤哄簭 = ['鍒濊瘑', '鐔熸倝', '绮剧爺', '閫氭檽', '鐚庣帇'];
      const 鍥鹃壌鑱氱劍鏉＄洰 = (bestiaryEntries || [])
        .filter(([, item]) => item && typeof item === 'object')
        .sort((a, b) => {
          const aData = a[1] || {};
          const bData = b[1] || {};
          const aTick = toNumber(aData && aData['鏈€杩戞椿璺僼ick'], 0);
          const bTick = toNumber(bData && bData['鏈€杩戞椿璺僼ick'], 0);
          if (aTick !== bTick) return bTick - aTick;
          const a妗ｄ綅 = 鍥鹃壌妗ｄ綅椤哄簭.indexOf(toText(aData && aData['鍥鹃壌妗ｄ綅'], '鍒濊瘑'));
          const b妗ｄ綅 = 鍥鹃壌妗ｄ綅椤哄簭.indexOf(toText(bData && bData['鍥鹃壌妗ｄ綅'], '鍒濊瘑'));
          if (a妗ｄ綅 !== b妗ｄ綅) return b妗ｄ綅 - a妗ｄ綅;
          return toNumber(bData && bData['浜ゆ墜娆℃暟'], 0) - toNumber(aData && aData['浜ゆ墜娆℃暟'], 0);
        })[0] || null;
      const 鍥鹃壌褰撳墠妗ｄ綅 = 鍥鹃壌鑱氱劍鏉＄洰
        ? toText(deepGet(鍥鹃壌鑱氱劍鏉＄洰, '1.鍥鹃壌妗ｄ綅', '鍒濊瘑'), '鍒濊瘑')
        : '鏃?;
      const 鍥鹃壌褰撳墠缁忛獙 = 鍥鹃壌鑱氱劍鏉＄洰
        ? Math.max(0, toNumber(deepGet(鍥鹃壌鑱氱劍鏉＄洰, '1.褰撳墠妗ｇ粡楠?, 0), 0))
        : 0;
      const 鍥鹃壌涓嬫。闇€姹?= 鍥鹃壌鑱氱劍鏉＄洰
        ? Math.max(0, toNumber(deepGet(鍥鹃壌鑱氱劍鏉＄洰, '1.涓嬫。闇€姹?, 0), 0))
        : 0;
      const 鍥鹃壌涓嬩竴妗ｈ繘搴?= 鍥鹃壌鑱氱劍鏉＄洰
        ? (鍥鹃壌涓嬫。闇€姹?> 鍥鹃壌褰撳墠缁忛獙 ? `${鍥鹃壌褰撳墠缁忛獙}/${鍥鹃壌涓嬫。闇€姹倉` : '宸茶揪涓婇檺')
        : '--';
      const 鍥鹃壌鑱氱劍鍚嶇О = 鍥鹃壌鑱氱劍鏉＄洰 ? toText(鍥鹃壌鑱氱劍鏉＄洰[0], '鏆傛棤') : '鏆傛棤';
      const 鍥鹃壌鎴愰暱鍊惧悜 = 鍥鹃壌鑱氱劍鏉＄洰 ? toText(deepGet(鍥鹃壌鑱氱劍鏉＄洰, '1.鎴愰暱鍊惧悜', '鍧囪　'), '鍧囪　') : '鏃?;
      const pendingSoulRing = buildPendingSoulRingState({ rootData: sd, activeChar, activeName });
      const mapData = sd && sd.map && typeof sd.map === 'object' ? sd.map : {};
      const sheepSnapshot = window.__sheepMapSnapshot && typeof window.__sheepMapSnapshot === 'object'
        ? window.__sheepMapSnapshot
        : null;
      const normalizeMapEntries = source => {
        if (Array.isArray(source)) {
          return source
            .map(item => {
              if (Array.isArray(item) && item.length >= 2) {
                const key = toText(item[0], '').trim();
                const value = item[1] && typeof item[1] === 'object' ? item[1] : {};
                return key ? [key, value] : null;
              }
              if (item && typeof item === 'object') {
                const key = toText(item.name || item.loc || item.id, '').trim();
                return key ? [key, item] : null;
              }
              return null;
            })
            .filter(entry => entry && entry[0]);
        }
        if (source && typeof source === 'object') {
          return safeEntries(source);
        }
        return [];
      };
      const normalizeTravelCandidates = source => {
        if (!Array.isArray(source)) return [];
        return source
          .map(item => {
            if (typeof item === 'string') return item.trim();
            if (Array.isArray(item)) return toText(item[0], '').trim();
            if (item && typeof item === 'object') return toText(item.loc || item.name || item.target, '').trim();
            return '';
          })
          .filter(Boolean);
      };
      const mapVisibleNodeEntriesCore = normalizeMapEntries(deepGet(mapData, 'visible_nodes', {}));
      const mapVisibleNodeEntriesSheep = normalizeMapEntries(sheepSnapshot && sheepSnapshot.visibleNodes);
      const mapVisibleNodeEntries = mapVisibleNodeEntriesCore.length ? mapVisibleNodeEntriesCore : mapVisibleNodeEntriesSheep;
      const mapVisibleDynamicEntriesCore = normalizeMapEntries(deepGet(mapData, 'visible_dynamic_locations', {}));
      const mapVisibleDynamicEntriesSheep = normalizeMapEntries(
        sheepSnapshot && (sheepSnapshot.visibleDynamics || sheepSnapshot.visibleDynamicLocations)
      );
      const mapVisibleDynamicEntries = mapVisibleDynamicEntriesCore.length ? mapVisibleDynamicEntriesCore : mapVisibleDynamicEntriesSheep;
      const mapAvailableChildMapsCore = deepGet(mapData, 'available_child_maps', {}) || {};
      const mapAvailableChildMapsSheep = sheepSnapshot && sheepSnapshot.availableChildMaps && typeof sheepSnapshot.availableChildMaps === 'object'
        ? sheepSnapshot.availableChildMaps
        : {};
      const mapAvailableChildMaps = safeEntries(mapAvailableChildMapsCore).length ? mapAvailableChildMapsCore : mapAvailableChildMapsSheep;
      const mapTravelCandidatesCore = normalizeTravelCandidates(deepGet(mapData, 'travel_candidates', []));
      const mapTravelCandidatesSheep = normalizeTravelCandidates(sheepSnapshot && sheepSnapshot.travelCandidates);
      const mapTravelCandidates = mapTravelCandidatesCore.length ? mapTravelCandidatesCore : mapTravelCandidatesSheep;
      const mapCurrentFocusCore = deepGet(mapData, 'current_focus', {});
      const mapCurrentFocus = mapCurrentFocusCore && typeof mapCurrentFocusCore === 'object'
        ? mapCurrentFocusCore
        : ((sheepSnapshot && sheepSnapshot.currentFocus && typeof sheepSnapshot.currentFocus === 'object') ? sheepSnapshot.currentFocus : {});
      const mapCurrentMapId = toText(
        deepGet(mapData, 'current_map_id', sheepSnapshot && sheepSnapshot.currentMapId ? sheepSnapshot.currentMapId : 'map_douluo_world'),
        'map_douluo_world'
      );
      const mapZoomHint = toNumber(
        deepGet(mapData, 'current_zoom_hint', sheepSnapshot && Number.isFinite(Number(sheepSnapshot.currentZoomHint)) ? Number(sheepSnapshot.currentZoomHint) : 0),
        0
      );
      const primaryFaction = factions[0] || null;
      const topRelation = relations[0] || null;
      const questRecordCount = recordEntries.filter(([, item]) => toText(item && item['鐘舵€?], '杩涜涓?) !== '宸叉斁寮?).length;
      const warningText = toNumber(deepGet(sd, 'world.鍋忓樊鍊?, 0), 0) >= 40
        ? `鍋忓樊 ${toNumber(deepGet(sd, 'world.鍋忓樊鍊?, 0), 0)} / 楂樺嵄`
        : (deepGet(sd, 'world.鍏芥疆宸茶Е鍙?, false)
          ? '鍏芥疆璀︽姤 / 宸茶Е鍙?
          : (flagEntries.length ? `涓栫晫鐘舵€?${flagEntries.length} 椤筦 : '鏃?));
      const auctionStatus = toText(deepGet(sd, 'world.鎷嶅崠.鐘舵€?, '浼戝競'), '浼戝競');
      const auctionLocation = toText(deepGet(sd, 'world.鎷嶅崠.鍦扮偣', '鏃?), '鏃?);
      const 甯傚満娲剧敓 = 鏋勫缓甯傚満娲剧敓妯″瀷(locationData, sd);
      const 鏈湴渚涚粰 = 甯傚満娲剧敓.鏈湴渚涚粰;
      const 浠锋牸甯?= 甯傚満娲剧敓.浠锋牸甯?
      const 鏈€杩戞垚浜ゅ奖鍝?= 甯傚満娲剧敓.鏈€杩戞垚浜ゅ奖鍝?

      const chars = sd && sd.char ? sd.char : {};
      const charEntries = safeEntries(chars);
      const 蹇収鏁版嵁 = {
        runtimeReady,
        rawRootData,
        rootData: sd,
        activeName: toText(activeName, contextMeta.name1 || contextMeta.name2 || '褰撳墠瑙掕壊'),
        appearanceMeta: formatAppearanceMeta(deepGet(activeChar, '澶栬矊', {})),
        appearanceText: formatAppearanceText(deepGet(activeChar, '澶栬矊', {})),
        personalityText: toText(deepGet(activeChar, '鎬ф牸', '鏈瀹?), '鏈瀹?),
        activeChar: activeChar || {},
        currentLoc,
        normalizedLoc: locationInfo.name,
        locationData,
        storeNames,
        dynamicLocationNames,
        timelineEntries,
        latestTimeline: timelineEntries[0] || null,
        鍔垮姏: factions,
        relations,
        unlockedKnowledges,
        inventoryEntries,
        flagEntries,
        charEntries,
        orgEntries,
        titleEntries,
        recordEntries,
        conditionEntries,
        soulBoneEntries,
        artEntries,
        customSkillEntries,
        combatHistoryEntries,
        primarySpirit,
        secondaryTrack: secondarySpirit || (bloodline.valid ? bloodline : null),
        bloodline,
        mapNodeLabels: mapNodeLabels.slice(0, 4),
        pendingRequests,
        primaryFaction,
        topRelation,
        pendingSoulRing,
        鍏崇郴鍒嗘瀽: deepGet(activeChar, '绀句氦.鍏崇郴鍒嗘瀽', {}),

        questRecordCount,
        褰撳墠浠诲姟鍚嶇О,
        褰撳墠浠诲姟闃舵鏂囨湰,
        褰撳墠浠诲姟涓嬩竴鑺傜偣杩涘害,
        褰撳墠浠诲姟璺嚎,
        褰撳墠浠诲姟椋庨櫓绾у埆,
        鎯呮姤寰呮牳瀹炴暟閲? 寰呮牳瀹炴儏鎶ュ垪琛?length,
        鎯呮姤鏈€杩戞牳瀹炵粨鏋?
        鎯呮姤璇佹嵁鍑€鍊?
        鍥鹃壌褰撳墠妗ｄ綅,
        鍥鹃壌涓嬩竴妗ｈ繘搴?
        鍥鹃壌鑱氱劍鍚嶇О,
        鍥鹃壌鎴愰暱鍊惧悜,
        recentTitles,
        worldAlert: warningText,
        鏈湴渚涚粰,
        浠锋牸甯?
        鏈€杩戞垚浜ゅ奖鍝?
        甯傚満娲剧敓,
        bestiaryEntries,
        forestKilledAge,
        auctionStatus,
        auctionLocation,
        mapData,
        mapCurrentMapId,
        mapZoomHint,
        mapCurrentFocus,
        mapVisibleNodeEntries,
        mapVisibleDynamicEntries,
        mapAvailableChildMaps,
        mapTravelCandidates,
        publicIntel: !!deepGet(activeChar, 'social.鍏紑鎯呮姤', deepGet(activeChar, 'social.public_intel', false)),
        extraSkills
      };
      蹇収鏁版嵁.鍙帺閫夐」 = 鏋勫缓鍙帺閫夐」灞曠ず鍒楄〃(蹇収鏁版嵁);
      return 蹇収鏁版嵁;
    }

    function maybeAutoOpenPendingSoulRingPreview(snapshot, options = {}) {
      const pending = buildPendingSoulRingState(snapshot);
      if (!pending || pending.status !== '寰呴€夋嫨') {
        if (!pending) lastAutoOpenedPendingSoulRingSignature = '';
        return;
      }
      if (!pending.signature || pending.signature === lastAutoOpenedPendingSoulRingSignature) return;
      lastAutoOpenedPendingSoulRingSignature = pending.signature;
      const shouldPreferFloating = options && options.force !== false;
      openModal(PENDING_SOUL_RING_PREVIEW_KEY, {
        preserveMapDispatchContext: true,
        displayMode: shouldPreferFloating ? 'floating' : undefined,
      });
    }

    async function handlePendingSoulRingGenerate(snapshot, candidateSoulName = '') {
      const pending = buildPendingSoulRingState(snapshot);
      if (!pending) throw new Error('褰撳墠娌℃湁寰呭鐞嗙殑榄傜幆鐢熸垚璇锋眰銆?);
      const wantedSoulName = toText(candidateSoulName, '').trim() || toText(deepGet(pending, 'candidates.0.soulName', ''), '').trim();
      if (!wantedSoulName) throw new Error('褰撳墠寰呴€夐瓊鐏典负绌恒€?);
      await mutateStatDataByEditor(statData => {
        const charData = deepGet(statData, ['char', pending.charKey], null);
        if (!charData || typeof charData !== 'object') throw new Error('鏈壘鍒板搴旇鑹层€?);
        const currentPending = deepGet(charData, ['鐘舵€?, '寰呴€夐瓊鐜?], null);
        if (!currentPending || typeof currentPending !== 'object') throw new Error('褰撳墠寰呴€夐瓊鐜姸鎬佸凡澶辨晥銆?);
        const spiritSlot = toText(currentPending.姝﹂瓊妲戒綅, pending.spiritSlot).trim();
        const ringIndex = Math.max(1, toNumber(currentPending.寰呯敓鎴愰瓊鐜綅, pending.ringIndex));
        const spiritData = deepGet(charData, ['姝﹂瓊', spiritSlot], null);
        if (!spiritData || typeof spiritData !== 'object') throw new Error('鏈壘鍒扮洰鏍囨榄傛Ы浣嶃€?);
        const soulData = deepGet(spiritData, ['榄傜伒', wantedSoulName], null);
        if (!soulData || typeof soulData !== 'object') throw new Error(`鏈壘鍒板€欓€夐瓊鐏点€?{wantedSoulName}銆戙€俙);
        if (!soulData.榄傜幆 || typeof soulData.榄傜幆 !== 'object') soulData.榄傜幆 = {};
        const ringKey = String(ringIndex);
        if (soulData.榄傜幆[ringKey] && typeof soulData.榄傜幆[ringKey] === 'object' && (toNumber(soulData.榄傜幆[ringKey].骞撮檺, 0) > 0 || safeEntries(soulData.榄傜幆[ringKey].榄傛妧).length > 0)) {
          throw new Error(`绗?{ringIndex}榄傜幆宸插瓨鍦ㄣ€俙);
        }
        const ringAge = Math.max(0, toNumber(soulData.骞撮檺, 0));
        soulData.榄傜幆[ringKey] = {
          骞撮檺: ringAge,
          棰滆壊: getRingDisplayColorLabel('', ringAge),
          榄傛妧: buildDefaultRingSkillMapForUi(ringIndex, ringAge),
        };
        if (!charData.鐘舵€?|| typeof charData.鐘舵€?!== 'object') charData.鐘舵€?= {};
        delete charData.鐘舵€?寰呴€夐瓊鐜?
        if (!statData.sys || typeof statData.sys !== 'object') statData.sys = {};
        statData.sys.绯荤粺鎾姤 = `[榄傜幆鐢熸垚] ${pending.charName} 閫夋嫨鐢便€?{wantedSoulName}銆戜负銆?{pending.spiritName}銆戣鐢熺 ${ringIndex} 榄傜幆銆俙;
      }, { force: true });
      lastAutoOpenedPendingSoulRingSignature = '';
      await refreshLiveSnapshot({ force: true });
      const shouldJumpPreview = pending.spiritPreviewKey || '';
      if ((currentModalPreviewKey || currentUnifiedPreviewKey) === PENDING_SOUL_RING_PREVIEW_KEY) closeModal();
      if (shouldJumpPreview) openModal(shouldJumpPreview, { preserveMapDispatchContext: true });
      if (window.MVU_Toast && typeof window.MVU_Toast.show === 'function') {
        window.MVU_Toast.show('宸茬敓鎴愬搴旈瓊鐜€?, 'info');
      }
    }

    async function handlePendingSoulRingDefer(snapshot) {
      const pending = buildPendingSoulRingState(snapshot);
      if (!pending) throw new Error('褰撳墠娌℃湁寰呭鐞嗙殑榄傜幆鐢熸垚璇锋眰銆?);
      await mutateStatDataByEditor(statData => {
        const charData = deepGet(statData, ['char', pending.charKey], null);
        if (!charData || typeof charData !== 'object') throw new Error('鏈壘鍒板搴旇鑹层€?);
        if (!charData.鐘舵€?|| typeof charData.鐘舵€?!== 'object') charData.鐘舵€?= {};
        if (!charData.鐘舵€?寰呴€夐瓊鐜?|| typeof charData.鐘舵€?寰呴€夐瓊鐜?!== 'object') throw new Error('寰呴€夐瓊鐜姸鎬佸凡澶辨晥銆?);
        charData.鐘舵€?寰呴€夐瓊鐜?鐘舵€?= '宸叉殏缂?;
        if (!statData.sys || typeof statData.sys !== 'object') statData.sys = {};
        statData.sys.绯荤粺鎾姤 = `[榄傜幆鐢熸垚] ${pending.charName} 鏆傜紦澶勭悊銆?{pending.spiritName}銆戠殑绗?${pending.ringIndex} 榄傜幆銆俙;
      }, { force: true });
      await refreshLiveSnapshot({ force: true });
      if ((currentModalPreviewKey || currentUnifiedPreviewKey) === PENDING_SOUL_RING_PREVIEW_KEY) {
        closeModal();
      }
      if (window.MVU_Toast && typeof window.MVU_Toast.show === 'function') {
        window.MVU_Toast.show('宸叉殏缂撴湰娆￠瓊鐜敓鎴愩€?, 'info');
      }
    }

    function applyActiveCharacterSelection(nextName, options = {}) {
      cancelActiveInlineEdit();
      const targetName = toText(nextName, '').trim();
      const rootData = deepGet(liveSnapshot, 'rootData', null);
      const chars = deepGet(rootData, 'char', {});
      if (!targetName || !rootData || !chars || !chars[targetName]) return false;

      window.__MVU_MANUAL_CHAR_SET = true; // 鏍囪杩欐槸鐢ㄦ埛鎵嬪姩鍒囨崲鐨勮鑹诧紝涓嶅啀鑷姩琚悗缁殑鐜╁椤舵帀
      setPreferredActiveCharacterName(targetName);
      liveSnapshot = buildSnapshot(rootData);
      lastHeaderRenderSignature = buildHeaderRenderSignature(liveSnapshot);
      const nextDashboardSectionRenderSignatures = buildDashboardSectionRenderSignatures(liveSnapshot);
      lastDashboardRenderSignature = buildDashboardRenderSignature(liveSnapshot, nextDashboardSectionRenderSignatures);
      lastDashboardSectionRenderSignatures = null;
      renderHeader(liveSnapshot);
      renderLiveCards(liveSnapshot, nextDashboardSectionRenderSignatures);
      syncPrivateArchiveLongPressTargets(liveSnapshot);
      maybeAutoOpenPendingSoulRingPreview(liveSnapshot, { force: true });

      syncBattleUiForSnapshot(liveSnapshot, { force: true });

      const activeDetailPreviewKey = getEffectiveUnifiedPreviewKey() || currentModalPreviewKey;
      if ((detailModal.classList.contains('show') || isShellInlinePreviewActive() || isUnifiedInlinePreviewActive()) && activeDetailPreviewKey) {
        if (options.closeModal !== false) {
          if (isUnifiedInlinePreviewActive() && getEffectiveUnifiedPreviewKey()) closeDetailSurface({ surface: 'unified' });
          else closeModal();
        } else {
          rerenderDetailSurface(activeDetailPreviewKey, { surface: getEffectiveUnifiedPreviewKey() ? 'unified' : 'modal' });
        }
      }

      return true;
    }

    function buildRenderSignature(value) {
      try {
        return JSON.stringify(value);
      } catch (error) {
        return `${Date.now()}|${Math.random()}`;
      }
    }

    function buildHeaderRenderSignature(snapshot) {
      const status = deepGet(snapshot, 'activeChar.鐘舵€?, {});
      const bloodline = snapshot && snapshot.bloodline && snapshot.bloodline.valid
        ? { bloodline: snapshot.bloodline.bloodline, sealLv: snapshot.bloodline.sealLv }
        : null;
      return buildRenderSignature({
        activeName: toText(snapshot && snapshot.activeName, ''),
        currentLoc: toText(snapshot && snapshot.currentLoc, ''),
        worldTimeText: toText(deepGet(snapshot, 'rootData.world.鏃堕棿._calendar', deepGet(snapshot, 'rootData.world.鏃堕棿.calendar', '')), ''),
        action: toText(status && status.琛屽姩, ''),
        activeDomain: toText(status && status.褰撳墠棰嗗煙, ''),
        wound: getDisplayWoundLabel(deepGet(snapshot, 'activeChar.灞炴€?, {})),
        alive: deepGet(status, '瀛樻椿', true) !== false,
        bloodline,
        armorStatus: toText(deepGet(snapshot, 'activeChar.瑁呭.鏂楅摖.瑁呭鐘舵€?, ''), ''),
        mechLv: toText(deepGet(snapshot, 'activeChar.瑁呭.鏈虹敳.绛夌骇', ''), ''),
        worldAlert: toText(snapshot && snapshot.worldAlert, '')
      });
    }

    function buildDashboardSectionRenderSignatures(snapshot) {
      return {
        archive: buildRenderSignature({
          activeName: toText(snapshot && snapshot.activeName, ''),
          activeChar: deepGet(snapshot, 'activeChar', {}),
          bloodline: snapshot && snapshot.bloodline ? snapshot.bloodline : null,
          primarySpirit: snapshot && snapshot.primarySpirit ? snapshot.primarySpirit : null,
          secondaryTrack: snapshot && snapshot.secondaryTrack ? snapshot.secondaryTrack : null,
          soulBoneEntries: Array.isArray(snapshot && snapshot.soulBoneEntries) ? snapshot.soulBoneEntries : [],
          inventoryEntries: Array.isArray(snapshot && snapshot.inventoryEntries) ? snapshot.inventoryEntries : []
        }),
        map: buildRenderSignature({
          currentLoc: toText(snapshot && snapshot.currentLoc, ''),
          normalizedLoc: toText(snapshot && snapshot.normalizedLoc, ''),
          locationData: snapshot && snapshot.locationData ? snapshot.locationData : null,
          mapZoomHint: toNumber(snapshot && snapshot.mapZoomHint, 0),
          mapNodeLabels: Array.isArray(snapshot && snapshot.mapNodeLabels) ? snapshot.mapNodeLabels : [],
          mapVisibleNodeEntries: Array.isArray(snapshot && snapshot.mapVisibleNodeEntries) ? snapshot.mapVisibleNodeEntries : [],
          mapVisibleDynamicEntries: Array.isArray(snapshot && snapshot.mapVisibleDynamicEntries) ? snapshot.mapVisibleDynamicEntries : [],
          mapAvailableChildMaps: snapshot && snapshot.mapAvailableChildMaps && typeof snapshot.mapAvailableChildMaps === 'object'
            ? snapshot.mapAvailableChildMaps
            : {},
          鏈湴浜虹墿: getShellLocalCharacterEntries(snapshot, 99).map(([鍚嶇О, 瑙掕壊]) => [
            鍚嶇О,
            deepGet(瑙掕壊, '鐘舵€?浣嶇疆', ''),
            deepGet(瑙掕壊, '鐘舵€?琛屽姩', ''),
            deepGet(瑙掕壊, '绀句氦.涓昏韩浠?, ''),
          ]),
          latestTimeline: snapshot && snapshot.latestTimeline ? snapshot.latestTimeline : null,
          timelineEntries: Array.isArray(snapshot && snapshot.timelineEntries) ? snapshot.timelineEntries : []
        }),
        social: buildRenderSignature({
          social: deepGet(snapshot, 'activeChar.绀句氦', {}),
          primaryFaction: snapshot && snapshot.primaryFaction ? snapshot.primaryFaction : null,
          topRelation: snapshot && snapshot.topRelation ? snapshot.topRelation : null,
          relations: Array.isArray(snapshot && snapshot.relations) ? snapshot.relations : [],
          unlockedKnowledges: Array.isArray(snapshot && snapshot.unlockedKnowledges) ? snapshot.unlockedKnowledges : []
        }),
        world: buildRenderSignature({
          rootWorld: deepGet(snapshot, 'rootData.world', {}),
          worldAlert: toText(snapshot && snapshot.worldAlert, ''),
          factions: Array.isArray(snapshot && snapshot.鍔垮姏) ? snapshot.鍔垮姏 : [],
          locationData: snapshot && snapshot.locationData ? snapshot.locationData : null,
          latestTimeline: snapshot && snapshot.latestTimeline ? snapshot.latestTimeline : null
        }),
        terminal: buildRenderSignature({
          rootSys: deepGet(snapshot, 'rootData.sys', {}),
          rootWorld: deepGet(snapshot, 'rootData.world', {}),
          activeChar: deepGet(snapshot, 'activeChar', {}),
          worldAlert: toText(snapshot && snapshot.worldAlert, ''),
          inventoryEntries: Array.isArray(snapshot && snapshot.inventoryEntries) ? snapshot.inventoryEntries : [],
          bestiaryEntries: Array.isArray(snapshot && snapshot.bestiaryEntries) ? snapshot.bestiaryEntries : [],
          recordEntries: Array.isArray(snapshot && snapshot.recordEntries) ? snapshot.recordEntries : [],
          timelineEntries: Array.isArray(snapshot && snapshot.timelineEntries) ? snapshot.timelineEntries : [],
          unlockedKnowledges: Array.isArray(snapshot && snapshot.unlockedKnowledges) ? snapshot.unlockedKnowledges : []
        })
      };
    }

    function buildDashboardRenderSignature(snapshot, sectionSignatures = null) {
      const sections = sectionSignatures || buildDashboardSectionRenderSignatures(snapshot);
      return [
        sections.archive || '',
        sections.map || '',
        sections.social || '',
        sections.world || '',
        sections.terminal || ''
      ].join('|');
    }

    function getLiveUiElements(selector) {
      if (!(liveUiRefCache instanceof Map)) liveUiRefCache = new Map();
      const cached = liveUiRefCache.get(selector);
      if (Array.isArray(cached) && cached.length && cached.every(el => el && el.isConnected)) {
        return cached;
      }
      const elements = Array.from(document.querySelectorAll(selector));
      if (elements.length) liveUiRefCache.set(selector, elements);
      else liveUiRefCache.delete(selector);
      return elements;
    }

    function setLiveNodeText(node, value) {
      if (!node) return;
      const nextValue = value == null ? '' : String(value);
      if (node.textContent === nextValue) return;
      node.textContent = nextValue;
    }

    function setLiveNodeHtml(node, value) {
      if (!node) return;
      const nextValue = value == null ? '' : String(value);
      if (node.innerHTML === nextValue) return;
      node.innerHTML = nextValue;
    }

    function setLiveText(selector, value) {
      getLiveUiElements(selector).forEach(node => setLiveNodeText(node, value));
    }

    function setLiveHtml(selector, value) {
      getLiveUiElements(selector).forEach(node => setLiveNodeHtml(node, value));
    }

    function 鏋勫缓鎱㈠埛鏂伴鏋剁粓绔崱() {
      return `
        <div class="module-name">绯荤粺鎾姤</div>
        <div class="terminal-log terminal-log--single-pane" data-terminal-tab-host="terminal-hero">
          <div class="terminal-tab-strip">
            <button type="button" class="terminal-tab active terminal-skeleton-row" data-terminal-tab="鐘舵€?>鐘舵€?/button>
            <button type="button" class="terminal-tab terminal-skeleton-row" data-terminal-tab="鎾姤">鎾姤</button>
            <button type="button" class="terminal-tab terminal-skeleton-row" data-terminal-tab="鎬昏">鎬昏</button>
          </div>
          <div class="terminal-tab-panels">
            <div class="terminal-tab-panel active terminal-skeleton-row" data-terminal-tab-panel="鐘舵€?>
              <div class="log-line sys log-line--single"><b>[鐘舵€乚</b> 鍔犺浇涓?..</div>
            </div>
            <div class="terminal-tab-panel terminal-skeleton-row" data-terminal-tab-panel="鎾姤">
              <div class="log-line roll log-line--single"><b>[鎾姤]</b> 鍔犺浇涓?..</div>
            </div>
            <div class="terminal-tab-panel terminal-skeleton-row" data-terminal-tab-panel="鎬昏">
              <div class="log-line sys log-line--single"><b>[鎬昏]</b> 鍔犺浇涓?..</div>
            </div>
          </div>
        </div>
        <div class="module-foot">
          <span class="foot-hint">鏁版嵁鍒锋柊涓?/span>
          <span class="enter-chip">鍚屾涓?/span>
        </div>
      `;
    }

    function 鏋勫缓鎱㈠埛鏂伴鏋朵晶鍗?鏍囬 = '鍔犺浇涓?) {
      return `
        <div class="simple-head"><div class="simple-title">${htmlEscape(toText(鏍囬, '鍔犺浇涓?))}</div></div>
        <div class="simple-list">
          <div class="simple-row"><b>鐘舵€?/b><span>鍔犺浇涓?..</span></div>
          <div class="simple-row"><b>鏁版嵁</b><span>姝ｅ湪鍚屾</span></div>
        </div>
      `;
    }

    function 搴旂敤鎱㈠埛鏂伴鏋跺崱鐗?) {
      setLiveHtml('[data-preview="绯荤粺鎾姤涓庢棩蹇?].terminal-hero-card', 鏋勫缓鎱㈠埛鏂伴鏋剁粓绔崱());
      setLiveHtml('[data-preview="鎿嶄綔鎬荤嚎"].terminal-side-card, [data-preview="鎿嶄綔鎬荤嚎"].mvu-simple-card, [data-preview="鎿嶄綔鎬荤嚎"].simple-card', 鏋勫缓鎱㈠埛鏂伴鏋朵晶鍗?'杩戞湡瀹夋帓'));
      setLiveHtml('[data-preview="璇曠偧涓庢儏鎶?].terminal-side-card, [data-preview="璇曠偧涓庢儏鎶?].mvu-simple-card, [data-preview="璇曠偧涓庢儏鎶?].simple-card', 鏋勫缓鎱㈠埛鏂伴鏋朵晶鍗?'璇曠偧涓庢儏鎶?));
    }

    function 璁剧疆鎱㈠埛鏂伴鏋剁姸鎬?鍚敤 = false, 閫夐」 = {}) {
      const 闇€鍒锋柊璇︽儏 = Object.prototype.hasOwnProperty.call(閫夐」 || {}, '鍒锋柊璇︽儏')
        ? !!閫夐」.鍒锋柊璇︽儏
        : true;
      if (鍚敤) {
        if (!鎱㈠埛鏂伴鏋跺凡鍚敤) {
          鎱㈠埛鏂伴鏋跺凡鍚敤 = true;
          搴旂敤鎱㈠埛鏂伴鏋跺崱鐗?);
        }
      } else if (鎱㈠埛鏂伴鏋跺凡鍚敤) {
        鎱㈠埛鏂伴鏋跺凡鍚敤 = false;
      }
      if (!闇€鍒锋柊璇︽儏) return;
      const 褰撳墠璇︽儏棰勮閿?= toText(currentUnifiedPreviewKey || currentModalPreviewKey, '').trim();
      if (!褰撳墠璇︽儏棰勮閿?|| !鎱㈠埛鏂伴鏋堕瑙堥敭闆嗗悎.has(褰撳墠璇︽儏棰勮閿?) return;
      rerenderDetailSurface(褰撳墠璇︽儏棰勮閿? {
        surface: currentUnifiedPreviewKey ? 'unified' : 'modal',
        force: true,
      });
    }

    function renderHeader(snapshot) {
      const worldTimeText = toText(deepGet(snapshot, 'rootData.world.鏃堕棿._calendar', deepGet(snapshot, 'rootData.world.鏃堕棿.calendar', '鏂楃綏鍘嗘湭鍚屾')), '鏂楃綏鍘嗘湭鍚屾');
      const headerComboHtml = `<span style="opacity:1;font-size:12px;color:#fff;">${worldTimeText}</span><span style="opacity:0.65;font-size:11px;">${snapshot.currentLoc}</span>`;
      setLiveHtml('.header-loc span', headerComboHtml);
      setLiveText('.char-name', snapshot.activeName);
      syncPrivateArchiveLongPressTargets(snapshot);

      const statusChips = getLiveUiElements('.header-status-row .header-status-chip');
      if (statusChips[0]) setLiveNodeText(statusChips[0].querySelector('span'), `${toText(deepGet(snapshot, 'activeChar.鐘舵€?琛屽姩', '鏃ュ父'), '鏃ュ父')} / ${toText(deepGet(snapshot, 'activeChar.鐘舵€?褰撳墠棰嗗煙', '鏃?), '鏃?)}`);
      if (statusChips[1]) {
        const activeStatus = deepGet(snapshot, 'activeChar.鐘舵€?, {});
        const injurySummary = buildInjurySummaryText(activeStatus, { limit: 1, empty: '' });
        setLiveNodeText(
          statusChips[1].querySelector('span'),
          `${getDisplayWoundLabel(deepGet(snapshot, 'activeChar.灞炴€?, {}))} / ${injurySummary || (deepGet(snapshot, 'activeChar.鐘舵€?瀛樻椿', true) ? '鐘舵€佺ǔ瀹? : '宸查櫒钀?)}`,
        );
      }
      if (statusChips[2]) {
        const shouldShowBloodline = !!(snapshot.bloodline && snapshot.bloodline.valid);
        if (statusChips[2].style.display !== (shouldShowBloodline ? '' : 'none')) {
          statusChips[2].style.display = shouldShowBloodline ? '' : 'none';
        }
        if (snapshot.bloodline && snapshot.bloodline.valid) {
          setLiveNodeText(statusChips[2].querySelector('span'), `${snapshot.bloodline.bloodline} / ${snapshot.bloodline.sealLv}灞俙);
        }
      }
      if (statusChips[3]) setLiveNodeText(statusChips[3].querySelector('span'), `鏂楅摖${toText(deepGet(snapshot, 'activeChar.瑁呭.鏂楅摖.瑁呭鐘舵€?, '鏈澶?), '鏈澶?)} / 鏈虹敳${toText(deepGet(snapshot, 'activeChar.瑁呭.鏈虹敳.绛夌骇', '鏃?), '鏃?)}`);
      if (statusChips[4]) setLiveNodeText(statusChips[4].querySelector('span'), snapshot.worldAlert);
    }

    const PRIVATE_ARCHIVE_PREVIEW_KEY = '绉佸瘑妗ｆ璇︾粏椤?;

    function isFemaleGenderText(value) {
      const normalized = String(value == null ? '' : value).trim().toLowerCase();
      if (!normalized) return false;
      if (normalized.includes('濂?)) return true;
      return normalized === 'female'
        || normalized === 'woman'
        || normalized === 'girl'
        || normalized === 'f';
    }

    function getActiveSnapshotCharacter(snapshot) {
      const activeChar = deepGet(snapshot, 'activeChar', null);
      if (activeChar && typeof activeChar === 'object') return activeChar;
      const activeCharKey = resolveSnapshotCharKey(snapshot, toText(snapshot && snapshot.activeName, ''));
      return activeCharKey ? deepGet(snapshot, ['rootData', 'char', activeCharKey], {}) : {};
    }

    function getPrivateArchiveAgeValue(value) {
      if (typeof value === 'number') return Number.isFinite(value) ? value : NaN;
      const rawText = String(value == null ? '' : value).trim();
      if (!rawText) return NaN;
      const directNumber = Number(rawText);
      if (Number.isFinite(directNumber)) return directNumber;
      const numericText = rawText.match(/-?\d+(?:\.\d+)?/);
      return numericText ? Number(numericText[0]) : NaN;
    }

    const SOUL_TOWER_MAX_AGE = 30;
    const SOUL_TOWER_TEAM_LIMIT = 7;
    const SOUL_TOWER_MAX_AGE_GAP = 3;
    const SOUL_TOWER_TOTAL_FLOORS = 108;
    const SOUL_TOWER_DISCOUNT_STORE_NAME = '榄傜伒濉旂壒璁稿厬鎹?;
    const 璇曠偧鍦扮偣鍓嶇紑 = '璇曠偧-';
    const 鍗囩伒鍙伴€€鍑哄湴鐐?= '浼犵伒濉斿叆鍙?;
    const 榄傜伒濉旈€€鍑哄湴鐐?= '鍙茶幈鍏嬪煄浼犵伒濉旀€婚儴';
    const SOUL_TOWER_LAYER_RULES = Object.freeze([
      Object.freeze({
        key: 'thousand',
        label: '鍗冨勾榄傜伒鍖?,
        rewardTier: '鍗冨勾',
        gateStart: 1,
        gateEnd: 18,
        minAge: 1000,
        maxAge: 9999,
        qualitySteps: Object.freeze(['C', 'B', 'A']),
      }),
      Object.freeze({
        key: 'ten_thousand',
        label: '涓囧勾榄傜伒鍖?,
        rewardTier: '涓囧勾',
        gateStart: 19,
        gateEnd: 36,
        minAge: 10000,
        maxAge: 99999,
        qualitySteps: Object.freeze(['B', 'A', 'S']),
      }),
      Object.freeze({
        key: 'pre_beast',
        label: '涓囧勾浠ヤ笂榄傜伒鍖?,
        rewardTier: '涓囧勾浠ヤ笂',
        gateStart: 37,
        gateEnd: 99,
        minAge: 10000,
        maxAge: 99999,
        qualitySteps: Object.freeze(['A', 'S']),
      }),
      Object.freeze({
        key: 'beast',
        label: '鍑跺吔榄傜伒鍖?,
        rewardTier: '鍑跺吔绾?,
        gateStart: 100,
        gateEnd: 108,
        minAge: 100000,
        maxAge: 200000,
        qualitySteps: Object.freeze(['S+']),
      }),
    ]);
    const SOUL_SPIRIT_QUALITY_VALUES = Object.freeze(['F', 'D', 'C', 'B', 'A', 'S', 'S+']);
    const SOUL_SPIRIT_QUALITY_MULTIPLIER_MAP = Object.freeze({
      F: 0.82,
      D: 0.9,
      C: 1.0,
      B: 1.08,
      A: 1.18,
      S: 1.32,
      'S+': 1.48,
    });
    const SOUL_SPIRIT_QUALITY_LEVEL_OFFSET_MAP = Object.freeze({
      F: -6,
      D: -3,
      C: 0,
      B: 2,
      A: 5,
      S: 8,
      'S+': 12,
    });

    function normalizeSoulSpiritQuality(value = '') {
      const text = toText(value, '')
        .toUpperCase()
        .replace('锛?, '+')
        .replace(/\s+/g, '');
      return SOUL_SPIRIT_QUALITY_VALUES.includes(text) ? text : '';
    }

    function createEmptySoulTowerDiscountSpiritRecord() {
      return {
        灞傛暟: 0,
        鍚嶇О: '',
        鏍囧噯鐗╃: '',
        骞撮檺: 0,
        鍝佽川: '',
        宸蹭娇鐢? false,
      };
    }

    function normalizeSoulTowerDiscountSpiritRecord(record = {}) {
      if (!record || typeof record !== 'object' || Array.isArray(record)) return createEmptySoulTowerDiscountSpiritRecord();
      const next = createEmptySoulTowerDiscountSpiritRecord();
      next.灞傛暟 = Math.max(0, Math.floor(toNumber(record.灞傛暟, 0)));
      next.鍚嶇О = toText(record.鍚嶇О, '');
      next.鏍囧噯鐗╃ = toText(record.鏍囧噯鐗╃, '');
      next.骞撮檺 = Math.max(0, Math.floor(toNumber(record.骞撮檺, 0)));
      next.鍝佽川 = normalizeSoulSpiritQuality(record.鍝佽川 || '');
      next.宸蹭娇鐢?= record.宸蹭娇鐢?=== true;
      if (!(next.灞傛暟 > 0 && next.鏍囧噯鐗╃ && next.骞撮檺 > 0 && next.鍝佽川 && next.宸蹭娇鐢?=== false)) {
        return createEmptySoulTowerDiscountSpiritRecord();
      }
      if (!next.鍚嶇О) next.鍚嶇О = `${next.鏍囧噯鐗╃}榄傜伒`;
      return next;
    }

    function hasActiveSoulTowerDiscountSpirit(record = {}) {
      return normalizeSoulTowerDiscountSpiritRecord(record).灞傛暟 > 0;
    }

    function buildSoulTowerDiscountSpiritDisplay(record = {}) {
      const normalized = normalizeSoulTowerDiscountSpiritRecord(record);
      if (!(normalized.灞傛暟 > 0)) return '鏆傛棤';
      return `绗?{normalized.灞傛暟}灞?路 ${normalized.鏍囧噯鐗╃} 路 ${formatAge(normalized.骞撮檺)} 路 ${normalized.鍝佽川}`;
    }

    function getSoulTowerAgeValueFromCharacter(charData = {}) {
      return getPrivateArchiveAgeValue(deepGet(charData, '灞炴€?骞撮緞', null));
    }

    function isSoulTowerEligibleCharacterData(charData = {}) {
      const ageValue = getSoulTowerAgeValueFromCharacter(charData);
      return Number.isFinite(ageValue) && ageValue > 0 && ageValue <= SOUL_TOWER_MAX_AGE;
    }

    function isTransmissionTowerEntryLocation(location = '') {
      const normalized = normalizeLocForMatch(location);
      if (!normalized.raw) return false;
      return normalized.segments.some(seg => /浼犵伒濉?.test(seg));
    }

    function isSoulTowerHeadquartersLocation(location = '') {
      const normalized = normalizeLocForMatch(location);
      if (!normalized.raw) return false;
      if (/浼犵伒濉旀€婚儴/.test(normalized.raw)) return true;
      const hasSpiritPagoda = isTransmissionTowerEntryLocation(normalized.raw);
      const hasShrekCity = normalized.segments.some(seg => /鍙茶幈鍏嬪煄/.test(seg));
      return hasSpiritPagoda && hasShrekCity;
    }

    function 璇诲彇褰撳墠瑙掕壊浣嶇疆(snapshot) {
      return toText(
        deepGet(snapshot, 'activeChar.鐘舵€?浣嶇疆', snapshot && snapshot.currentLoc),
        toText(snapshot && snapshot.currentLoc, ''),
      ).trim();
    }

    function 璇诲彇璇曠偧鐘舵€佹枃鏈?snapshot) {
      return toText(deepGet(snapshot, 'rootData.world.鎴樻枟.璇曠偧鐘舵€?, ''), '').trim();
    }

    function 鏄惁璇曠偧鍐呭湴鐐?location = '') {
      return toText(location, '').trim().startsWith(璇曠偧鍦扮偣鍓嶇紑);
    }

    function 鏋勫缓鍗囩伒鍙拌瘯鐐煎湴鐐?妗ｄ綅 = '鍒濈骇', 鏆村姩鏈?= false) {
      const safeTier = ['鍒濈骇', '涓骇', '楂樼骇'].includes(toText(妗ｄ綅, '鍒濈骇')) ? toText(妗ｄ綅, '鍒濈骇') : '鍒濈骇';
      return `${璇曠偧鍦扮偣鍓嶇紑}${鏆村姩鏈?? '鏆村姩鏈? : ''}${safeTier}鍗囩伒鍙癭;
    }

    function 鏋勫缓榄傜伒濉旇瘯鐐煎湴鐐?灞傛暟 = 1) {
      const floor = Math.min(SOUL_TOWER_TOTAL_FLOORS, Math.max(1, Math.floor(toNumber(灞傛暟, 1))));
      return `${璇曠偧鍦扮偣鍓嶇紑}榄傜伒濉?绗?{floor}灞俙;
    }

    function 瑙ｆ瀽璇曠偧鍦扮偣(location = '') {
      const safeLocation = toText(location, '').trim();
      if (!鏄惁璇曠偧鍐呭湴鐐?safeLocation)) return null;
      const 濉斿尮閰?= safeLocation.match(/^璇曠偧-榄傜伒濉?绗?\d+)灞?/);
      if (濉斿尮閰? {
        const floor = Math.min(SOUL_TOWER_TOTAL_FLOORS, Math.max(1, Math.floor(toNumber(濉斿尮閰峓1], 1))));
        return {
          璇曠偧绫诲瀷: '榄傜伒濉?,
          闂ㄧエ鍚? '榄傜伒濉旈棬绁?,
          妗ｄ綅: '榄傜伒濉?,
          鏆村姩鏈? false,
          鍏ュ満鍦扮偣: 榄傜伒濉旈€€鍑哄湴鐐?
          褰撳墠灞? floor,
          璇曠偧鍐呭湴鐐? 鏋勫缓榄傜伒濉旇瘯鐐煎湴鐐?floor),
        };
      }
      if (/鍗囩伒鍙?.test(safeLocation)) {
        const body = safeLocation.replace(/^璇曠偧-/, '');
        const 鏆村姩鏈?= /鏆村姩鏈?.test(body);
        const 妗ｄ綅 = 瑙ｆ瀽鍗囩伒鍙版。浣?body);
        return {
          璇曠偧绫诲瀷: '鍗囩伒鍙?,
          闂ㄧエ鍚? `${鏆村姩鏈?? '鏆村姩鏈? : ''}${妗ｄ綅}鍗囩伒鍙伴棬绁╜,
          妗ｄ綅,
          鏆村姩鏈?
          鍏ュ満鍦扮偣: 鍗囩伒鍙伴€€鍑哄湴鐐?
          褰撳墠灞? 0,
          璇曠偧鍐呭湴鐐? 鏋勫缓鍗囩伒鍙拌瘯鐐煎湴鐐?妗ｄ綅, 鏆村姩鏈?,
        };
      }
      return null;
    }

    function isSoulTowerCombatType(detail = {}) {
      const battleType = toText(detail && (detail['鎴樻枟绫诲瀷'] || detail.combatType || detail.type), '');
      return battleType === '榄傜伒濉斿啿濉?;
    }

    function getSoulTowerGateMeta(floor = 0) {
      const safeFloor = Math.max(1, Math.floor(Number(floor) || 1));
      const rule =
        SOUL_TOWER_LAYER_RULES.find(item => safeFloor >= item.gateStart && safeFloor <= item.gateEnd) ||
        SOUL_TOWER_LAYER_RULES[SOUL_TOWER_LAYER_RULES.length - 1];
      const layerInGate = safeFloor - rule.gateStart + 1;
      const gateIndex = SOUL_TOWER_LAYER_RULES.findIndex(item => item.key === rule.key) + 1;
      const isGateBoss = safeFloor === rule.gateEnd;
      const nextGateBossFloor = rule.gateEnd;
      const layerProgress = rule.gateEnd > rule.gateStart ? (safeFloor - rule.gateStart) / (rule.gateEnd - rule.gateStart) : 1;
      return {
        floor: safeFloor,
        gateIndex,
        gateStart: rule.gateStart,
        gateEnd: rule.gateEnd,
        layerInGate,
        isGateBoss,
        gateLabel: rule.label,
        gateRangeLabel: `${rule.gateStart}-${rule.gateEnd}灞俙,
        nextGateBossFloor,
        rewardTier: rule.rewardTier,
        layerProgress,
        minAge: rule.minAge,
        maxAge: rule.maxAge,
        qualitySteps: rule.qualitySteps,
        isTopNine: safeFloor >= 100,
        totalFloors: SOUL_TOWER_TOTAL_FLOORS,
        key: rule.key,
      };
    }

    function getSoulTowerStageTextByHighestFloor(highestFloor = 0) {
      const safeHighestFloor = Math.max(0, Math.floor(Number(highestFloor) || 0));
      const nextFloor = Math.max(1, safeHighestFloor + 1);
      const meta = getSoulTowerGateMeta(nextFloor);
      return `${meta.gateLabel} 路 ${meta.gateRangeLabel}`;
    }

    function getSoulTowerNextBossFloorText(highestFloor = 0) {
      const safeHighestFloor = Math.max(0, Math.floor(Number(highestFloor) || 0));
      const nextFloor = Math.max(1, safeHighestFloor + 1);
      const meta = getSoulTowerGateMeta(nextFloor);
      if (safeHighestFloor >= SOUL_TOWER_TOTAL_FLOORS) return '宸茬櫥椤?;
      return `${meta.nextGateBossFloor}灞俙;
    }

    function getSoulTowerGuardianQualityForFloor(floor = 1) {
      const meta = getSoulTowerGateMeta(floor);
      const steps = Array.isArray(meta.qualitySteps) && meta.qualitySteps.length ? meta.qualitySteps : ['C'];
      if (steps.length === 1) return steps[0];
      const progress = Math.max(0, Math.min(1, Number(meta.layerProgress || 0)));
      const index = Math.min(steps.length - 1, Math.floor(progress * steps.length));
      return steps[index] || steps[steps.length - 1];
    }

    function buildSoulTowerGuardianAgeForFloor(floor = 1, rng = Math.random) {
      const meta = getSoulTowerGateMeta(floor);
      const minAge = Math.max(1, Math.floor(Number(meta.minAge || 1)));
      const maxAge = Math.max(minAge, Math.floor(Number(meta.maxAge || minAge)));
      if (meta.isGateBoss) return maxAge;
      const gateLength = Math.max(1, meta.gateEnd - meta.gateStart);
      const progress = Math.max(0, Math.min(1, Number(meta.layerProgress || 0)));
      const anchor = minAge + Math.round((maxAge - minAge) * progress);
      const bucket = Math.max(1, Math.floor((maxAge - minAge) / Math.max(2, gateLength + 1)));
      const variance = Math.max(0, Math.floor(bucket * 0.45));
      const offset = variance > 0 ? pickTemporaryBattleInt(-variance, variance, rng) : 0;
      return Math.max(minAge, Math.min(maxAge, anchor + offset));
    }

    function buildSoulTowerGuardianSeed(floor = 1, options = {}) {
      const safeFloor = Math.max(1, Math.floor(toNumber(floor, 1)));
      const rng = typeof options.rng === 'function' ? options.rng : Math.random;
      const speciesPool =
        Array.isArray(options.speciesPool) && options.speciesPool.length ? options.speciesPool : BATTLE_SOUL_BEAST_STANDARD_SPECIES;
      const species = toText(options.species, pickTemporaryBattleItem(speciesPool, rng) || '榫欑被') || '榫欑被';
      const age = Math.max(1, Math.floor(toNumber(options.age, buildSoulTowerGuardianAgeForFloor(safeFloor, rng))));
      const quality = normalizeSoulSpiritQuality(options.鍝佽川 || options.quality || getSoulTowerGuardianQualityForFloor(safeFloor)) || 'C';
      const name = toText(options.name, `${species}瀹堝榄傚吔`) || `${species}瀹堝榄傚吔`;
      return {
        name,
        鍚嶇О: name,
        鏉ユ簮: '涓存椂鍗曚綅',
        鍗曚綅鎬ц川: '榄傚吔',
        鏁伴噺: 1,
        鏍囧噯鐗╃: species,
        骞撮檺: age,
        鍝佽川: quality,
      };
    }

    function buildSoulTowerDiscountSpiritRecordFromSeed(floor = 1, seed = {}) {
      const safeFloor = Math.max(1, Math.floor(toNumber(floor, 1)));
      const species = toText(seed.鏍囧噯鐗╃, '');
      const age = Math.max(0, Math.floor(toNumber(seed.骞撮檺, 0)));
      const quality = normalizeSoulSpiritQuality(seed.鍝佽川 || '');
      if (!(species && age > 0 && quality)) return createEmptySoulTowerDiscountSpiritRecord();
      return {
        灞傛暟: safeFloor,
        鍚嶇О: `${species}榄傜伒`,
        鏍囧噯鐗╃: species,
        骞撮檺: age,
        鍝佽川: quality,
        宸蹭娇鐢? false,
      };
    }

    function buildSoulTowerTeamValidationResult(members = []) {
      const safeMembers = Array.isArray(members) ? members.filter(member => member && typeof member === 'object') : [];
      if (!safeMembers.length) return { ok: false, message: '榄傜伒濉旈槦浼嶄负绌恒€? };
      if (safeMembers.length > SOUL_TOWER_TEAM_LIMIT) {
        return { ok: false, message: `榄傜伒濉旈槦浼嶆渶澶?${SOUL_TOWER_TEAM_LIMIT} 浜恒€俙 };
      }
      const invalidAgeMember = safeMembers.find(member => !(member.age > 0) || member.age > SOUL_TOWER_MAX_AGE);
      if (invalidAgeMember) {
        return { ok: false, message: `${toText(invalidAgeMember.name, '闃熷憳')} 宸茶秴杩?${SOUL_TOWER_MAX_AGE} 宀侊紝涓嶈兘鍙備笌榄傜伒濉斻€俙 };
      }
      const ages = safeMembers.map(member => Number(member.age || 0)).filter(age => age > 0);
      const minAge = Math.min(...ages);
      const maxAge = Math.max(...ages);
      if (maxAge - minAge > SOUL_TOWER_MAX_AGE_GAP) {
        return { ok: false, message: `榄傜伒濉旈槦浼嶆垚鍛樺勾榫勫樊涓嶈兘瓒呰繃 ${SOUL_TOWER_MAX_AGE_GAP} 宀併€俙 };
      }
      return {
        ok: true,
        count: safeMembers.length,
        minAge,
        maxAge,
        totalFloors: SOUL_TOWER_TOTAL_FLOORS,
      };
    }

    function resolveSoulTowerStatCharKeyByName(statData, rawName = '') {
      const wanted = toText(rawName, '').trim();
      if (!wanted) return '';
      const charMap = statData && statData.char && typeof statData.char === 'object' ? statData.char : {};
      const direct = charMap[wanted];
      if (direct && typeof direct === 'object') return wanted;
      const foundEntry = Object.entries(charMap).find(([charKey, charData]) => {
        const displayName = toText(charData && charData.name, charKey);
        return displayName === wanted || charKey === wanted;
      });
      return foundEntry ? toText(foundEntry[0], '') : '';
    }

    function buildSoulTowerTeamMembersFromStatBattle(statData, charKey = '') {
      const safeCharKey = toText(charKey, '').trim();
      const battleData = deepGet(statData, 'world.鎴樻枟', null);
      if (!safeCharKey || !battleData || toText(battleData && battleData['鎴樻枟绫诲瀷'], '') !== '榄傜伒濉斿啿濉?) return null;
      const participantMap = battleData && battleData['鍙傛垬鑰?] && typeof battleData['鍙傛垬鑰?] === 'object' ? battleData['鍙傛垬鑰?] : null;
      if (!participantMap) return null;
      const playerName = toText(deepGet(participantMap, 'player.name', ''), '');
      const selfNames = new Set([
        safeCharKey,
        toText(deepGet(statData, ['char', safeCharKey, 'name'], ''), safeCharKey),
      ].filter(Boolean));
      if (playerName && !selfNames.has(playerName)) return null;
      const seeds = [participantMap.player, ...(Array.isArray(participantMap.team_player) ? participantMap.team_player : [])].filter(Boolean);
      return seeds.map(seed => {
        const memberName = toText(seed && seed.name, '');
        const memberKey = resolveSoulTowerStatCharKeyByName(statData, memberName);
        const charData = memberKey ? deepGet(statData, ['char', memberKey], null) : null;
        return {
          key: memberKey,
          name: toText(charData && charData.name, memberName || memberKey || '闃熷憳'),
          age: getSoulTowerAgeValueFromCharacter(charData || {}),
        };
      });
    }

    function buildTrialBattleDispatchDetail(snapshot, dispatchDetail = {}, trialContext = null) {
      const sourceSnapshot = snapshot && snapshot.rootData ? snapshot : (liveSnapshot || lastRenderableSnapshot);
      if (!sourceSnapshot || !sourceSnapshot.rootData) return null;
      const baseDetail = { ...cloneJsonValue(dispatchDetail, {}) };
      const 鏄惧紡鍙傛垬鑰?= baseDetail.鍙傛垬鑰?&& typeof baseDetail.鍙傛垬鑰?=== 'object'
        ? cloneJsonValue(baseDetail.鍙傛垬鑰? {})
        : null;
      const 鏄惧紡宸辨柟闃熶紞 = Array.isArray(鏄惧紡鍙傛垬鑰?&& 鏄惧紡鍙傛垬鑰?team_player)
        ? cloneJsonValue(鏄惧紡鍙傛垬鑰?team_player, [])
        : [];
      const 鐘舵€佷笂涓嬫枃 = 瑙ｆ瀽璇曠偧鍦扮偣(璇诲彇璇曠偧鐘舵€佹枃鏈?sourceSnapshot));
      const safeContext = 瑙勮寖鍖栬瘯鐐肩姸鎬佷笂涓嬫枃(trialContext || 鐘舵€佷笂涓嬫枃 || null);
      if (!safeContext) return null;
      const activeKey = resolveSnapshotCharKey(sourceSnapshot, toText(sourceSnapshot && sourceSnapshot.activeName, ''));
      if (!activeKey) return null;
      const activeName = toText(deepGet(sourceSnapshot, ['rootData', 'char', activeKey, 'name'], ''), toText(sourceSnapshot && sourceSnapshot.activeName, activeKey));
      if (safeContext.璇曠偧绫诲瀷 === '鍗囩伒鍙?) {
        const ascensionResult = 鐢熸垚鍗囩伒鍙版垬鏂楀弬鏁?sourceSnapshot, safeContext);
        return {
          ...baseDetail,
          action: 'battle',
          currentLoc: safeContext.璇曠偧鍐呭湴鐐?|| 璇诲彇褰撳墠瑙掕壊浣嶇疆(sourceSnapshot),
          target: safeContext.璇曠偧鍐呭湴鐐?|| 璇诲彇褰撳墠瑙掕壊浣嶇疆(sourceSnapshot),
          鎴樻枟绫诲瀷: '鍗囩伒鍙拌櫄鎷熸垬鏂?,
          鎴樻枟鎰忓浘: '鍗囩伒璇曠偧',
          source: toText(baseDetail.source, 'trial_entry'),
          璇曠偧鐘舵€? safeContext.璇曠偧鍐呭湴鐐?|| 鏋勫缓鍗囩伒鍙拌瘯鐐煎湴鐐?safeContext.妗ｄ綅, safeContext.鏆村姩鏈?,
          ascension_ticket: toText(baseDetail.ascension_ticket, safeContext.闂ㄧエ鍚?,
          killed_age: Math.max(1, Math.floor(toNumber(baseDetail.killed_age, ascensionResult.killedAge))),
          party_size: Math.max(1, Math.floor(toNumber(baseDetail.party_size, 1))),
          鍗囩伒瀵规墜绫诲瀷: toText(baseDetail.鍗囩伒瀵规墜绫诲瀷, ascensionResult.瀵规墜绫诲瀷),
          鍗囩伒妗ｄ綅鐧惧垎浣? Math.max(0, Math.min(1, toNumber(baseDetail.鍗囩伒妗ｄ綅鐧惧垎浣? ascensionResult.鐧惧垎浣?)),
          鍙傛垬鑰? {
            player: { name: activeName || activeKey },
            enemy: cloneJsonValue(ascensionResult.瀵规墜绉嶅瓙, {}),
            team_player: 鏄惧紡宸辨柟闃熶紞,
            team_enemy: [],
          },
        };
      }
      if (safeContext.璇曠偧绫诲瀷 === '榄傜伒濉?) {
        const 濉斿眰鍩哄噯鍊?= toNumber(
          baseDetail.floor,
          toNumber(
            deepGet(sourceSnapshot, 'rootData.world.鎴樻枟.floor', safeContext.褰撳墠灞?|| getDefaultSoulTowerChallengeFloor(sourceSnapshot)),
            safeContext.褰撳墠灞?|| getDefaultSoulTowerChallengeFloor(sourceSnapshot),
          ),
        );
        const requestedFloor = Math.min(
          SOUL_TOWER_TOTAL_FLOORS,
          Math.max(1, Math.floor(濉斿眰鍩哄噯鍊?),
        );
        const guardianSeed = 鏋勫缓榄傜伒濉斿畧濉旂瀛?{ ...safeContext, 褰撳墠灞? requestedFloor });
        return {
          ...baseDetail,
          action: 'battle',
          currentLoc: safeContext.璇曠偧鍐呭湴鐐?|| 鏋勫缓榄傜伒濉旇瘯鐐煎湴鐐?requestedFloor),
          target: safeContext.璇曠偧鍐呭湴鐐?|| 鏋勫缓榄傜伒濉旇瘯鐐煎湴鐐?requestedFloor),
          鎴樻枟绫诲瀷: '榄傜伒濉斿啿濉?,
          鎴樻枟鎰忓浘: '榄傜伒濉旇瘯鐐?,
          source: toText(baseDetail.source, 'trial_entry'),
          璇曠偧鐘舵€? safeContext.璇曠偧鍐呭湴鐐?|| 鏋勫缓榄傜伒濉旇瘯鐐煎湴鐐?requestedFloor),
          floor: requestedFloor,
          soulTowerTicket: toText(baseDetail.soulTowerTicket, safeContext.闂ㄧエ鍚?,
          闂ㄧエ宸叉墸闄? true,
          鍙傛垬鑰? {
            player: { name: activeName || activeKey },
            enemy: cloneJsonValue(guardianSeed, {}),
            team_player: 鏄惧紡宸辨柟闃熶紞,
            team_enemy: [],
          },
        };
      }
      return null;
    }

    function getTrialEntranceText(snapshot) {
      const soulTowerEligible = isSoulTowerEligibleCharacterData(getActiveSnapshotCharacter(snapshot));
      const hasSoulTowerTicket = soulTowerEligible && listSoulTowerTicketNames(snapshot).length > 0;
      const hasAscensionTicket = listAscensionTicketNames(snapshot).length > 0;
      if (hasSoulTowerTicket) return '鍗囩伒鍙?/ 榄傜伒濉?/ 鐙╃寧';
      if (hasAscensionTicket) return '鍗囩伒鍙?/ 鐙╃寧';
      return '褰撳墠鏃犻棬绁紝浠呭父瑙勭嫨鐚?;
    }

    function canOpenPrivateArchive(snapshot) {
      const activeChar = getActiveSnapshotCharacter(snapshot);
      const genderText = toText(deepGet(activeChar, '灞炴€?鎬у埆', ''), '');
      if (!isFemaleGenderText(genderText)) return false;
      const ageValue = getPrivateArchiveAgeValue(deepGet(activeChar, '灞炴€?骞撮緞', null));
      return Number.isFinite(ageValue) && ageValue >= 6;
    }

    function getActiveNsfwPath(snapshot) {
      const activeCharKey = resolveSnapshotCharKey(snapshot, toText(snapshot && snapshot.activeName, ''));
      return activeCharKey ? ['char', activeCharKey, '绉佸瘑妗ｆ'] : [];
    }

    function getActiveNsfwData(snapshot) {
      const activeNsfw = deepGet(snapshot, 'activeChar.绉佸瘑妗ｆ', null);
      if (activeNsfw && typeof activeNsfw === 'object' && safeEntries(activeNsfw).length) return activeNsfw;
      const activeCharKey = resolveSnapshotCharKey(snapshot, toText(snapshot && snapshot.activeName, ''));
      const rootNsfw = activeCharKey ? deepGet(snapshot, ['rootData', 'char', activeCharKey, '绉佸瘑妗ｆ'], null) : null;
      if (rootNsfw && typeof rootNsfw === 'object') return rootNsfw;
      return activeNsfw && typeof activeNsfw === 'object' ? activeNsfw : {};
    }

    function setPrivateArchiveLongPressTarget(node, enabled, options = {}) {
      if (!node) return;
      const useVisualClass = options.visualClass !== false;
      node.classList.toggle('nsfw-trigger-title', !!(enabled && useVisualClass));
      node.classList.remove('active-press');
      if (enabled) {
        node.setAttribute('data-longpress', PRIVATE_ARCHIVE_PREVIEW_KEY);
        node.setAttribute('data-longpress-delay', '600');
      } else {
        node.removeAttribute('data-longpress');
        node.removeAttribute('data-longpress-delay');
      }
    }

    function syncPrivateArchiveLongPressTargets(snapshot) {
      const enabled = canOpenPrivateArchive(snapshot);
      getLiveUiElements('.char-name').forEach(node => {
        setPrivateArchiveLongPressTarget(node, enabled);
      });
      getLiveUiElements('#mvu-unified-mount .mvu-unified-section-title').forEach(node => {
        if (toText(node.textContent, '').trim() === '璇︾粏妗ｆ') {
          setPrivateArchiveLongPressTarget(node, enabled);
        }
      });
      getLiveUiElements('#mvu-unified-mount [data-unified-card="archive-core"][data-unified-surface="shell"]').forEach(node => {
        setPrivateArchiveLongPressTarget(node, enabled, { visualClass: false });
      });
    }

    function buildArchiveCoreCard(snapshot) {
      const stat = deepGet(snapshot, 'activeChar.灞炴€?, {});
      const status = deepGet(snapshot, 'activeChar.鐘舵€?, {});
      const hpPair = getDisplayHpPair(stat);
      const woundLabel = getDisplayWoundLabel(stat);
      const activeCharKey = resolveSnapshotCharKey(snapshot, toText(snapshot.activeName, '')) || toText(snapshot.activeName, '褰撳墠瑙掕壊');
      const nextLevelSoul = getNextLevelSoulRequirementWithCap(stat, deepGet(snapshot, 'activeChar', {}));
      const allowNsfwLongPress = canOpenPrivateArchive(snapshot);
      const mentalRealmText = toText(stat._men_realm, toText(stat.绮剧鍔沖realm, '鐏靛厓澧?));
      const actionText = toText(status.琛屽姩, '鏃ュ父');
      const actionDisplay = activeCharKey
        ? makeInlineEditableValue(actionText, {
            path: ['char', activeCharKey, '鐘舵€?, '琛屽姩'],
            kind: 'enum_select',
            rawValue: actionText,
            editorMeta: { options: getStatusActionEditorOptions(actionText) },
          })
        : htmlEscape(actionText);
      const nextSoulDisplayText = nextLevelSoul.isMax
        ? '涓嬬骇榄傚姏 宸叉弧绾?
        : nextLevelSoul.blocked
          ? (nextLevelSoul.blockedBy === 'ring'
            ? `涓嬬骇榄傚姏 鍙楅瓊鐜檺鍒禶
            : `涓嬬骇榄傚姏 鍙楅瓊鏍搁檺鍒禶)
        : (toNumber(nextLevelSoul.needed, 0) <= 0
          ? `涓嬬骇榄傚姏 宸茶揪${formatNumber(nextLevelSoul.nextLevel)}绾ч棬妲沗
          : `涓嬬骇榄傚姏 ${formatNumber(nextLevelSoul.needed)}`);
      const nextSoulValueText = nextSoulDisplayText.replace(/^涓嬬骇榄傚姏\s*/, '');
      const 璧勬簮鎸囨爣鍒楄〃 = [
        { 鍚嶇О: '榄傚姏', 鏁板€? `${formatNumber(stat.榄傚姏)}/${formatNumber(stat.榄傚姏涓婇檺)}`, 姣斾緥: ratioPercent(stat.榄傚姏, stat.榄傚姏涓婇檺), 鏍峰紡: 'cyan' },
        { 鍚嶇О: `绮剧鍔?路 ${mentalRealmText}`, 鏁板€? `${formatNumber(stat.绮剧鍔?}/${formatNumber(stat.绮剧鍔涗笂闄?}`, 姣斾緥: ratioPercent(stat.绮剧鍔? stat.绮剧鍔涗笂闄?, 鏍峰紡: 'white' },
        { 鍚嶇О: '鐢熷懡', 鏁板€? `${formatNumber(hpPair.hp)}/${formatNumber(hpPair.hpMax)}`, 姣斾緥: ratioPercent(hpPair.hp, hpPair.hpMax), 鏍峰紡: 'red' },
        { 鍚嶇О: '浣撳姏', 鏁板€? `${formatNumber(stat.浣撳姏)}/${formatNumber(stat.浣撳姏涓婇檺)}`, 姣斾緥: ratioPercent(stat.浣撳姏, stat.浣撳姏涓婇檺), 鏍峰紡: 'red' },
      ];
      return `
        <div class="panel-head panel-head--archive archive-core-head">
          <div class="panel-title-row">
            <div class="panel-title${allowNsfwLongPress ? ' nsfw-trigger-title' : ''}"${allowNsfwLongPress ? ` data-longpress="${PRIVATE_ARCHIVE_PREVIEW_KEY}" data-longpress-delay="600"` : ''}>璇︾粏妗ｆ</div>
          </div>
        </div>
        <div class="archive-core-grid">
          <div class="archive-core-tile">
            <b>骞撮緞 / 鎬у埆</b>
            <strong>${htmlEscape(`${toText(stat.骞撮緞, '0')}宀?/ ${toText(stat.鎬у埆, '鏈煡')}`)}</strong>
          </div>
          <div class="archive-core-tile">
            <b>淇负绛夌骇</b>
            <strong class="cyan">${htmlEscape(formatCultivationLevelBadge(stat.绛夌骇, '0'))}</strong>
          </div>
          <div class="archive-core-tile">
            <b>鐘舵€佹瑙?/b>
            <strong>${actionDisplay} / ${htmlEscape(woundLabel)}</strong>
          </div>
          <div class="archive-core-tile">
            <b>涓嬬骇榄傚姏</b>
            <strong class="cyan">${htmlEscape(nextSoulValueText)}</strong>
          </div>
          ${璧勬簮鎸囨爣鍒楄〃.map(鎸囨爣 => `
            <div class="archive-core-tile archive-core-tile--resource is-${htmlEscape(鎸囨爣.鏍峰紡)}">
              <b>${htmlEscape(鎸囨爣.鍚嶇О)}</b>
              <strong>${htmlEscape(鎸囨爣.鏁板€?}</strong>
              <i><span style="width:${鎸囨爣.姣斾緥}%;"></span></i>
            </div>
          `).join('')}
        </div>
        `;
    }

    function buildArmoryCard(snapshot) {
      const armor = deepGet(snapshot, 'activeChar.瑁呭.鏂楅摖', {});
      const mech = deepGet(snapshot, 'activeChar.瑁呭.鏈虹敳', {});
      const weapon = deepGet(snapshot, 'activeChar.瑁呭.姝﹀櫒', {});
      const jobs = safeEntries(deepGet(snapshot, 'activeChar.鑱屼笟', {}));
      const jobSummary = jobs.length ? `${jobs[0][0]} Lv.${toText(deepGet(jobs[0][1], 'lv', 0), '0')}` : '鏈睍寮€';
      const jobCoreTechSummary = jobs.length ? (Object.keys(deepGet(jobs[0][1], '鏍稿績鎶€鑹?, {})).slice(0, 2).join(' / ') || '鏆傛棤鏍稿績鎶€鏈?) : '鏆傛棤鏍稿績鎶€鏈?;
      const jobLimitSummary = jobs.length ? `铻嶉敾涓婇檺 ${toText(deepGet(jobs[0][1], '闄愬埗.鏈€澶ц瀺鍚堟暟', 1), '1')} / 鎴愬姛鐜?${toText(deepGet(jobs[0][1], '闄愬埗.鎴愬姛鐜?, 0), '0')}%` : '鏆傛棤宸ュ潑涓婇檺';
      const armorSummary = toNumber(armor.绛夌骇, 0) > 0 ? `${toText(armor.鍚嶇О, `${armor.绛夌骇}瀛楁枟閾燻)} / ${toText(armor.瑁呭鐘舵€? '鏈澶?)}` : '鏈澶?;
      const mechSummary = toText(mech.绛夌骇, '鏃?) !== '鏃? ? `${toText(mech.绛夌骇, '鏃?)}路${toText(mech.鍨嬪彿, '鏈畾鍨?)} / ${toText(mech.瑁呭鐘舵€? '鏈澶?)}` : '鏃?;
      const boneCount = snapshot.soulBoneEntries ? snapshot.soulBoneEntries.length : 0;
      const 姝﹀櫒鎽樿 = toText(weapon.鍚嶇О, '') || toText(weapon.绫诲瀷, '') || '鏈澶?;
      return `
        <div class="module-name">姝﹁宸ュ潑</div>
        <div class="module-grid armory-grid">
          <div class="mini-box"><b>褰撳墠鏂楅摖</b><span>${htmlEscape(armorSummary)}</span></div>
          <div class="mini-box"><b>褰撳墠鏈虹敳</b><span>${htmlEscape(mechSummary)}</span></div>
          <div class="mini-box"><b>姝﹀櫒</b><span>${htmlEscape(shortenText(姝﹀櫒鎽樿, 28))}</span></div>
          <div class="mini-box"><b>瑁呰浇榄傞</b><span>${htmlEscape(boneCount ? `${boneCount} 鍧梎 : '鏈閰?)}</span></div>
        </div>
        <div class="module-foot">
          <span class="foot-hint">${htmlEscape(jobSummary)} 路 ${htmlEscape(jobCoreTechSummary)}</span>
          <span class="enter-chip">${htmlEscape(jobLimitSummary)}</span>
        </div>
      `;
    }

    function buildVaultCard(snapshot) {
      const wealth = deepGet(snapshot, 'activeChar.璐㈠瘜', {});
      const activeCharKey = resolveSnapshotCharKey(snapshot, toText(snapshot.activeName, '')) || toText(snapshot.activeName, '褰撳墠瑙掕壊');
      return `
        <div class="module-name">鍌ㄧ墿浠撳簱</div>
        <div class="module-grid vault-grid">
          <div class="mini-box"><b>鐗╁搧绉嶇被</b><span>${htmlEscape(String(snapshot.inventoryEntries.length || 0))}</span></div>
          <div class="mini-box"><b>鑱旈偊甯?/b><span>${htmlEscape(formatNumber(wealth.鑱旈偊甯?)}</span></div>
          <div class="mini-box"><b>鏄熺綏甯?/b><span>${htmlEscape(formatNumber(wealth.鏄熺綏甯?)}</span></div>
          <div class="mini-box"><b>鍞愰棬绉垎</b><span>${htmlEscape(formatNumber(wealth.鍞愰棬绉垎))}</span></div>
        </div>
        <div class="module-foot">
          <span class="foot-hint">璁板綍鐗╄祫鎬昏 ${htmlEscape(String(snapshot.inventoryEntries.length || 0))} 浠?/span>
          <span class="enter-chip gold-chip">瀛﹂櫌/鎴樺姛锛?{htmlEscape(formatNumber(wealth.瀛﹂櫌绉垎))} / ${htmlEscape(formatNumber(wealth.鎴樺姛))}</span>
        </div>
      `;
    }

    function buildUnifiedSocialCard(snapshot) {
      const social = deepGet(snapshot, 'activeChar.绀句氦', {});
      const primaryFactionName = snapshot.primaryFaction ? snapshot.primaryFaction[0] : '鏃?;
      const primaryFactionRole = snapshot.primaryFaction ? toText(deepGet(snapshot.primaryFaction[1], '韬唤', '鏃?), '鏃?) : '鏈姞鍏?;
      const topRelationText = snapshot.topRelation
        ? `${shortenText(snapshot.topRelation[0], 8)} / ${toText(deepGet(snapshot.topRelation[1], '鍏崇郴', '闄岀敓'), '闄岀敓')} / ${toNumber(deepGet(snapshot.topRelation[1], '濂芥劅搴?, 0), 0)}`
        : `${snapshot.relations.length} 鏉;
      const latestIntelText = getLatestUnlockedIntelText(snapshot, 12, '鏆傛棤');
      return `
        <div class="mvu-social-entry-grid">
          ${鏋勫缓绀句氦鍏ュ彛鍧?'鍚嶆湜', `${toText(social.鍚嶆湜绛夌骇, toText(social.鍚嶆湜绛夌骇, '绫嶇睄鏃犲悕'))} / ${formatNumber(social.澹版湜)}`, toText(social.涓昏韩浠? '绀句細妗ｆ'), '绀句細妗ｆ璇︾粏椤?, 'gold')}
          ${鏋勫缓绀句氦鍏ュ彛鍧?'鍔垮姏', `${shortenText(primaryFactionName, 10)} / ${shortenText(primaryFactionRole, 10)}`, `${(snapshot.鍔垮姏 || []).length || 0} 椤瑰綊灞瀈, '鎵€灞炲娍鍔涜缁嗛〉', 'live')}
          ${鏋勫缓绀句氦鍏ュ彛鍧?'鍏崇郴', topRelationText, `${snapshot.relations.length} 鏉″叧绯籤, '浜虹墿鍏崇郴璇︾粏椤?)}
          ${鏋勫缓绀句氦鍏ュ彛鍧?'鎯呮姤', `${snapshot.unlockedKnowledges.length} / ${latestIntelText}`, snapshot.publicIntel ? '鍏紑鎯呮姤' : '鎯呮姤搴?, '鎯呮姤搴撹缁嗛〉')}
        </div>
      `;
    }

    function buildUnifiedSpiritCard(config, options = {}) {
      const primary = !!(options && options.primary);
      if (!config) {
        return `
          <div class="mvu-unified-card-head">
            <div class="mvu-unified-card-title">${primary ? '涓绘榄? : '绗簩姝﹂瓊'}</div>
          </div>
          <div class="mvu-unified-empty-note mvu-unified-empty-note--compact">${primary ? '褰撳墠鏈姞杞芥榄備俊鎭€? : '鏈惎鐢ㄧ浜屾榄傛垨琛€鑴?}</div>
        `;
      }
      const content = config.kind === 'bloodline'
        ? renderArchiveBloodlineEntry(config)
        : renderArchiveSpiritEntry(config, primary);
      return `<div class="mvu-unified-spirit-card">${content}</div>`;
    }

    function 鏋勫缓缁熶竴姝﹂瓊鍙岃建鍗?snapshot) {
      const 涓昏建 = snapshot && snapshot.primarySpirit ? snapshot.primarySpirit : null;
      const 绗簩杞?= snapshot && snapshot.secondaryTrack ? snapshot.secondaryTrack : null;
      const 铻嶅悎璧勬枡 = getFusionArchiveMeta(snapshot || {});
      const 榄傞鏁伴噺 = Array.isArray(snapshot && snapshot.soulBoneEntries) ? snapshot.soulBoneEntries.length : 0;
      const 绗簩杞ㄩ瑙堥敭 = 绗簩杞?? toText(绗簩杞?preview, '琛€鑴夊皝鍗拌缁嗛〉') : '琛€鑴夊皝鍗拌缁嗛〉';
      const 绗簩杞ㄥ唴瀹?= 绗簩杞?        ? (绗簩杞?kind === 'bloodline' ? renderArchiveBloodlineEntry(绗簩杞? : renderArchiveSpiritEntry(绗簩杞? false))
        : `
          <div class="mvu-spirit-empty-compact">
            <b>绗簩姝﹂瓊 / 琛€鑴?/b>
            <span>鏈惎鐢ㄧ浜屾榄傦紝琛€鑴?灏佸嵃浠嶅彲鏌ョ湅銆?/span>
            <em>铻嶅悎鎶€ ${htmlEscape(String(铻嶅悎璧勬枡.fusionEntries.length || 0))} 椤?路 榄傞 ${htmlEscape(String(榄傞鏁伴噺 || 0))} 鍧?/em>
          </div>
        `;
      return `
        <div class="mvu-spirit-pair-grid">
          <div class="mvu-spirit-pair-side mvu-spirit-pair-side--primary ${涓昏建 ? 'clickable' : 'is-empty'}"${涓昏建 ? ` data-preview="${escapeHtmlAttr(toText(涓昏建.preview, '绗竴姝﹂瓊璇︾粏椤?))}"` : ''}>
            ${涓昏建 ? renderArchiveSpiritEntry(涓昏建, true) : '<div class="mvu-unified-empty-note">褰撳墠鏈姞杞界涓€姝﹂瓊銆?/div>'}
          </div>
          <div class="mvu-spirit-pair-side mvu-spirit-pair-side--secondary clickable ${绗簩杞?? '' : 'is-empty'}" data-preview="${escapeHtmlAttr(绗簩杞ㄩ瑙堥敭)}">
            ${绗簩杞ㄥ唴瀹箎
          </div>
        </div>
      `;
    }

    function normalizeUnifiedSurfaceKey(surface) {
      const value = toText(surface, '').trim().toLowerCase();
      if (value === 'shell') return 'shell';
      if (value === 'panel') return 'panel';
      return '';
    }

    function collectShellBadgeItems(items = [], max = 4) {
      const result = [];
      (Array.isArray(items) ? items : []).forEach(item => {
        if (result.length >= max) return;
        const entry = item && typeof item === 'object'
          ? { text: toText(item.text, '').trim(), tone: toText(item.tone, '').trim() }
          : { text: toText(item, '').trim(), tone: '' };
        if (!entry.text) return;
        if (result.some(candidate => candidate.text === entry.text)) return;
        result.push(entry);
      });
      return result;
    }

    function collectShellMetricItems(items = [], max = 4) {
      return (Array.isArray(items) ? items : [])
        .map(item => ({
          label: toText(item && item.label, '').trim(),
          value: toText(item && item.value, '').trim(),
          tone: toText(item && item.tone, '').trim(),
        }))
        .filter(item => item.label && item.value)
        .slice(0, max);
    }

    function collectShellLineItems(items = [], max = 3) {
      return (Array.isArray(items) ? items : [])
        .map(item => ({
          label: toText(item && item.label, '').trim(),
          value: toText(item && item.value, '').trim(),
        }))
        .filter(item => item.label && item.value)
        .slice(0, max);
    }

    function formatShellPair(current, max) {
      return `${formatNumber(current)} / ${formatNumber(max)}`;
    }

    function buildShellSummaryCard(options = {}) {
      const kicker = toText(options.kicker, '').trim();
      const title = toText(options.title, '绌?).trim() || '绌?;
      const value = toText(options.value, '').trim();
      const meta = toText(options.meta, '').trim();
      const rawNote = toText(options.note, '').trim();
      const tone = toText(options.tone, '').trim();
      const requestedSize = toText(options.size, '').trim();
      const size = requestedSize || 'compact';
      const longPressPreview = toText(options.longPressPreview, '').trim();
      const longPressDelay = Math.max(250, toNumber(options.longPressDelay, 600));
      const badgeLimit = options.badgeLimit != null
        ? Math.max(0, toNumber(options.badgeLimit, size === 'hero' ? 2 : 1))
        : (size === 'hero' ? 2 : 1);
      const metricLimit = options.metricLimit != null
        ? Math.max(0, toNumber(options.metricLimit, size === 'hero' ? 3 : 1))
        : (size === 'hero' ? 3 : 1);
      const rowLimit = options.rowLimit != null
        ? Math.max(0, toNumber(options.rowLimit, size === 'hero' ? 2 : 1))
        : (size === 'hero' ? 2 : 1);
      const badges = collectShellBadgeItems(options.badges || [], badgeLimit);
      const metrics = collectShellMetricItems(options.metrics || [], metricLimit);
      const rows = collectShellLineItems(options.rows || [], rowLimit);
      const note = size === 'hero' || !rows.length ? rawNote : '';
      const className = [
        'mvu-shell-summary',
        tone ? `mvu-shell-summary--${tone}` : '',
        size ? `mvu-shell-summary--${size}` : '',
        value ? 'has-value' : '',
        longPressPreview ? 'nsfw-trigger-title' : '',
      ].filter(Boolean).join(' ');
      const longPressAttrs = longPressPreview
        ? ` data-longpress="${escapeHtmlAttr(longPressPreview)}" data-longpress-delay="${escapeHtmlAttr(String(longPressDelay))}"`
        : '';

      return `
        <div class="${className}"${longPressAttrs}>
          <div class="mvu-shell-card-top">
            <div class="mvu-shell-card-copy">
              <div class="mvu-shell-title">${htmlEscape(title)}</div>
              ${meta ? `<div class="mvu-shell-meta">${htmlEscape(meta)}</div>` : ''}
            </div>
            ${value ? `<div class="mvu-shell-value">${htmlEscape(value)}</div>` : ''}
          </div>
          ${badges.length ? `
            <div class="mvu-shell-chip-row">
              ${badges.map(item => `<span class="mvu-shell-chip${item.tone ? ` is-${htmlEscape(item.tone)}` : ''}">${htmlEscape(item.text)}</span>`).join('')}
            </div>
          ` : ''}
          ${metrics.length ? `
            <div class="mvu-shell-metric-grid">
              ${metrics.map(item => `
                <div class="mvu-shell-metric${item.tone ? ` is-${htmlEscape(item.tone)}` : ''}">
                  <span class="mvu-shell-metric-label">${htmlEscape(item.label)}</span>
                  <strong class="mvu-shell-metric-value">${htmlEscape(item.value)}</strong>
                </div>
              `).join('')}
            </div>
          ` : ''}
          ${rows.length ? `
            <div class="mvu-shell-line-list">
              ${rows.map(item => `
                <div class="mvu-shell-line">
                  <span class="mvu-shell-line-label">${htmlEscape(item.label)}</span>
                  <span class="mvu-shell-line-value">${htmlEscape(item.value)}</span>
                </div>
              `).join('')}
            </div>
          ` : ''}
          ${note ? `<div class="mvu-shell-card-note">${htmlEscape(note)}</div>` : ''}
        </div>
      `;
    }

    function buildShellEmptyCard(title, note, options = {}) {
      return buildShellSummaryCard({
        kicker: options.kicker || '',
        title,
        value: note || '绌?,
        tone: options.tone || 'muted',
      });
    }

    function buildShellAppPeek(options = {}) {
      const value = toText(options.value, '鏆傛棤淇℃伅').trim() || '鏆傛棤淇℃伅';
      const meta = toText(options.meta, '').trim();
      const note = toText(options.note, '').trim();
      const tone = toText(options.tone, '').trim();
      return `
        <div class="mvu-shell-app-peek${tone ? ` is-${htmlEscape(tone)}` : ''}">
          <div class="mvu-shell-app-peek-value">${htmlEscape(value)}</div>
          ${meta ? `<div class="mvu-shell-app-peek-meta">${htmlEscape(meta)}</div>` : ''}
          ${note ? `<div class="mvu-shell-app-peek-note">${htmlEscape(note)}</div>` : ''}
        </div>
      `;
    }

    function isShellPlaceholderText(value) {
      const text = toText(value, '').trim();
      if (!text) return true;
      const normalized = text.replace(/\s+/g, '');
      return /^(鏃爘鏆傛棤|鏆傛棤璁板綍|鏆傛棤鏀跺綍|鏆傛棤寰呭姙|鏆傛棤濮旀墭|鏆傛棤寰呰|鏆傛棤鏃堕棿绾縷鏆傛棤浜嬩欢|鏆傛棤鎾姤|鏆傛棤鏂扮嚎绱鏆傛棤璀︽姤|鏆傛棤鍙樺寲|鏆傛棤瑙侀椈|寰呭懡涓瓅鏈悓姝鏃堕棿鏈悓姝鏈缃畖鏈褰晐鏈敹褰晐--)$/.test(normalized);
    }

    function resolveShellText(value, fallback = '') {
      const text = toText(value, '').trim();
      if (!isShellPlaceholderText(text)) return text;
      const fallbackText = toText(fallback, '').trim();
      return isShellPlaceholderText(fallbackText) ? '' : fallbackText;
    }

    function normalizeUserFacingTrackLabel(value, fallback = '') {
      const text = toText(value, fallback).trim() || toText(fallback, '').trim();
      if (!text) return '';
      return text
        .replace(/琛€鑴夊壇杞?g, '琛€鑴?)
        .replace(/姝﹂瓊鍓建/g, '绗簩姝﹂瓊')
        .replace(/鍓建/g, '绗簩姝﹂瓊');
    }

    function summarizeShellIdentityText(value, options = {}) {
      const limit = Math.max(4, toNumber(options.limit, 12));
      const preferFirst = options.preferFirst !== false;
      const rawText = resolveShellText(value, '').trim();
      if (!rawText) return '';
      const cleaned = rawText
        .replace(/锛圼^锛塢*锛?g, '')
        .replace(/\([^)]*\)/g, '')
        .replace(/[锛?锛?銆俔.*$/, '')
        .replace(/\s+/g, ' ')
        .trim();
      if (!cleaned) return '';
      const pieces = cleaned
        .split(/[锝渱/锛忋€乚/)
        .map(item => toText(item, '').trim())
        .filter(Boolean);
      const resolved = preferFirst && pieces.length ? pieces[0] : cleaned;
      return shortenText(resolved || cleaned, limit);
    }

    function isRenderableShellRing(ring) {
      if (!ring || typeof ring !== 'object') return false;
      const title = resolveShellText(ring.title, '');
      const desc = resolveShellText(ring.desc, '');
      const skills = Array.isArray(ring.skills) ? ring.skills.filter(skill => resolveShellText(skill && skill.name, '')).length : 0;
      return !!title || !!desc || skills > 0;
    }

    function isRenderableShellSoul(soul) {
      if (!soul || typeof soul !== 'object') return false;
      const spiritName = resolveShellText(soul.spiritName || soul.desc || soul.name, '');
      const description = resolveShellText(soul.description, '');
      const state = toText(soul.state, '').trim();
      const ringCount = Array.isArray(soul.榄傜幆) ? soul.榄傜幆.filter(isRenderableShellRing).length : 0;
      if (ringCount > 0) return true;
      if (spiritName && !/^(鏈缃畖榄傜伒妲戒綅)$/.test(spiritName)) return true;
      if (description && !/^(灏氭湭鎺ュ叆榄傜伒銆倈鏈缃?$/.test(description)) return true;
      return !!state && !/^(鏈煡|鏈縺娲粅鏈缃?$/.test(state);
    }

    function buildShellSpiritSummaryCard(config, options = {}) {
      const primary = !!(options && options.primary);
      if (!config) {
        return buildShellEmptyCard(primary ? '涓绘榄? : normalizeUserFacingTrackLabel('', '绗簩姝﹂瓊'), primary ? '鏈帴鍏? : '鏈惎鐢?);
      }

      if (config.kind === 'bloodline') {
        return buildShellSummaryCard({
          kicker: '琛€鑴?,
          title: shortenText(toText(config.bloodline, '鏈閱?), 20),
          value: `灏佸嵃 ${toText(config.sealLv, '0')} 灞俙,
          meta: `鏍稿績 ${shortenText(toText(config.core, '鏈嚌鏍?), 16)}`,
          badges: [
            config.lifeFire ? { text: '鍛界伀宸茬噧', tone: 'gold' } : { text: '鍛界伀鏈噧' },
            ...((config.榄傜幆 || []).slice(0, 2).map(item => shortenText(toText(item && item.title, ''), 16))),
          ],
          metrics: [
            { label: '琛€鐜?, value: String((config.榄傜幆 || []).length || 0), tone: 'gold' },
            { label: '鎶€鑳?, value: String((config.bloodSkills || []).length || 0) },
            { label: '鐗规€?, value: String((config.bloodPassives || []).length || 0) },
            { label: '鎴愰暱', value: String((config.bloodPermanentBonuses || []).length || 0) },
          ],
          rows: [
            { label: '鐘舵€?, value: config.lifeFire ? '宸茬偣鐕? : '绛夊緟鍛界伀' },
            { label: '姒傝', value: shortenText(toText(config.desc, '鏈褰?), 28) },
          ],
          tone: 'gold',
        });
      }

      const renderableSouls = Array.isArray(config.souls) ? config.souls.filter(isRenderableShellSoul) : [];
      const primarySoul = renderableSouls[0] || null;
      const unlockedAttrs = Array.isArray(config.spiritUnlockedAttrs) ? config.spiritUnlockedAttrs : [];
      const soulCount = renderableSouls.length || Math.max(0, toNumber(config.soulCount, 0));
      const ringCount = Array.isArray(config.榄傜幆) ? config.榄傜幆.filter(isRenderableShellRing).length : 0;
      const hasSpiritProgress = !!primarySoul || soulCount > 0 || ringCount > 0;
      if (!hasSpiritProgress) {
        return buildShellSummaryCard({
          kicker: primary ? '涓绘榄? : normalizeUserFacingTrackLabel(config.badge, '绗簩姝﹂瓊'),
          title: shortenText(toText(config.spiritName || config.name, primary ? '涓绘榄? : '绗簩姝﹂瓊'), 20),
          value: '寰呮帴榄傜幆',
          meta: shortenText(toText(config.spiritType, '鏈畾'), 18),
          rows: [
            { label: '鐘舵€?, value: '鏈帴榄傜幆' },
          ],
          tone: primary ? 'live' : 'cyan',
        });
      }
      const renderableRings = (config.榄傜幆 || []).filter(isRenderableShellRing);
      return buildShellSummaryCard({
        kicker: primary ? '涓绘榄? : normalizeUserFacingTrackLabel(config.badge, '绗簩姝﹂瓊'),
        title: shortenText(toText(config.spiritName || config.name, primary ? '涓绘榄? : normalizeUserFacingTrackLabel(config.badge, '绗簩姝﹂瓊')), 20),
        value: `${soulCount} 鏋氶瓊鐏礰,
        meta: `${shortenText(toText(config.spiritType, '鏈畾'), 12)} 路 ${shortenText(toText(config.spiritElement, '鏈畾'), 12)}`,
        badges: [
          primarySoul ? shortenText(toText(primarySoul.spiritName || primarySoul.desc, ''), 16) : '',
          ...renderableRings.slice(0, 2).map(item => shortenText(toText(item && item.title, ''), 16)),
          ...unlockedAttrs.slice(0, 1).map(item => shortenText(toText(item, ''), 14)),
        ],
        metrics: [
          { label: '濂戝悎', value: primarySoul ? toText(primarySoul.comp, '--') : '--', tone: 'live' },
          { label: '骞撮檺', value: primarySoul ? shortenText(toText(primarySoul.age, '--'), 12) : '--' },
          { label: '榄傜幆', value: String(ringCount || 0) },
          { label: '涓婇檺', value: String((Array.isArray(config.spiritCapacityAttrs) ? config.spiritCapacityAttrs.length : 0) || 0) },
        ],
        rows: [
          { label: '涓婚瓊鐏?, value: primarySoul ? shortenText(toText(primarySoul.spiritName || primarySoul.desc, '鏈帴鍏?), 24) : '鏈帴鍏? },
          { label: '鐘舵€?, value: primarySoul ? toText(primarySoul.state, '鏈煡') : '绛夊緟鍞ら啋' },
        ],
        tone: primary ? 'live' : 'cyan',
      });
    }

    function buildShellHomeArchiveCard(snapshot) {
      if (!snapshot) {
        return buildShellAppPeek({
          value: toText(snapshot && snapshot.activeName, '褰撳墠鑱婂ぉ'),
          meta: '寰呭悓姝?,
          note: '褰撳墠鑱婂ぉ',
          tone: 'gold',
        });
      }
      const stat = deepGet(snapshot, 'activeChar.灞炴€?, {});
      const status = deepGet(snapshot, 'activeChar.鐘舵€?, {});
      const placeText = buildShellLocationLabel(snapshot, { fullLimit: 18, trailLimit: 8 });
      return buildShellAppPeek({
        value: toText(snapshot.activeName, '褰撳墠瑙掕壊'),
        meta: `${formatCultivationLevelBadge(stat.绛夌骇, '0')} 路 ${placeText}`,
        note: toText(status.琛屽姩, '鏃ュ父'),
        tone: 'live',
      });
    }

    function buildShellLocationLabel(snapshot, options = {}) {
      const normalizedLoc = toText(snapshot && snapshot.normalizedLoc, '').trim();
      const currentLoc = toText(snapshot && snapshot.currentLoc, '').trim();
      const fullLimit = Math.max(8, toNumber(options.fullLimit, 18));
      const trailLimit = Math.max(6, toNumber(options.trailLimit, 8));
      if (!normalizedLoc && !currentLoc) return '鏈煡鍦扮偣';
      if (
        !normalizedLoc
        || normalizedLoc === currentLoc
        || currentLoc.startsWith(`${normalizedLoc}-`)
        || currentLoc.startsWith(`${normalizedLoc}路`)
      ) {
        return shortenText(currentLoc || normalizedLoc, fullLimit);
      }
      return `${shortenText(normalizedLoc, trailLimit)} 路 ${shortenText(currentLoc || normalizedLoc, trailLimit)}`;
    }

    function buildShellHomeMapCard(snapshot) {
      if (!snapshot) {
        return buildShellAppPeek({
          value: '鍦板浘寰呭悓姝?,
          meta: '寰呭悓姝?,
          note: '褰撳墠鑱婂ぉ',
        });
      }
      const 鏈湴浜虹墿鏁伴噺 = getShellLocalCharacterEntries(snapshot, 99).length;
      return buildShellAppPeek({
        value: shortenText(toText(snapshot.normalizedLoc || snapshot.currentLoc, '鏈懡鍚嶈妭鐐?), 14),
        meta: `${鏈湴浜虹墿鏁伴噺} 鍚嶅湪鍦?路 ${toText(snapshot && snapshot.浠锋牸甯? '骞崇ǔ')}`,
        note: shortenText(getMapDisplayName(snapshot), 18),
        tone: 'live',
      });
    }

    function buildShellHomeWorldCard(snapshot) {
      if (!snapshot) {
        return buildShellAppPeek({
          value: '涓栫晫寰呭悓姝?,
          meta: '寰呭悓姝?,
          note: '褰撳墠鑱婂ぉ',
        });
      }
      const rawWorldTime = toText(deepGet(snapshot, 'rootData.world.鏃堕棿._calendar', deepGet(snapshot, 'rootData.world.鏃堕棿.calendar', '鏃堕棿鏈悓姝?)), '鏃堕棿鏈悓姝?);
      const worldTime = resolveShellText(rawWorldTime, '绛夊緟鍚屾');
      const deviation = toNumber(deepGet(snapshot, 'rootData.world.鍋忓樊鍊?, 0), 0);
      const deviationState = deviation >= 40 ? '楂樺嵄' : (deviation >= 10 ? '娉㈠姩' : '骞崇ǔ');
      const worldAlertText = resolveShellText(snapshot.worldAlert, '');
      const timelineHeadline = snapshot.latestTimeline ? snapshot.latestTimeline[0] : '';
      const timelineNote = snapshot.latestTimeline
        ? toText(deepGet(snapshot.latestTimeline[1], '浜嬩欢', snapshot.latestTimeline[0]), snapshot.latestTimeline[0])
        : '';
      const headline = worldAlertText
        || timelineHeadline
        || (rawWorldTime !== '鏃堕棿鏈悓姝? ? '涓栫晫鏃堕挓' : '涓栫晫骞崇ǔ');
      return buildShellAppPeek({
        value: shortenText(toText(headline, '涓栫晫骞崇ǔ'), 14),
        meta: shortenText(worldTime || '绛夊緟鍚屾', 18),
        note: shortenText(worldAlertText || timelineNote || `鍋忓樊 ${deviationState}`, 18),
        tone: worldAlertText ? (deviation >= 40 ? 'warn' : 'gold') : (deviation >= 10 ? 'gold' : 'live'),
      });
    }

    function buildShellHomeOrgCard(snapshot) {
      if (!snapshot) {
        return buildShellAppPeek({
          value: '鍔垮姏寰呭悓姝?,
          meta: '寰呭悓姝?,
          note: '褰撳墠鑱婂ぉ',
          tone: 'gold',
        });
      }
      const primaryFactionEntry = getPrimaryFactionEntry(snapshot);
      const primaryFaction = snapshot.鍔垮姏[0] || null;
      const factionName = primaryFaction ? primaryFaction[0] : primaryFactionEntry.name || toText(deepGet(snapshot, 'locationData.鎺屾帶鍔垮姏', '鏈姞鍏?), '鏈姞鍏?);
      const roleText = primaryFaction
        ? toText(deepGet(primaryFaction[1], '韬唤', '鏈姞鍏?), '鏈姞鍏?)
        : toText(deepGet(primaryFactionEntry.data, '韬唤', '鏈姞鍏?), '鏈姞鍏?);
      return buildShellAppPeek({
        value: shortenText(toText(factionName, '鏈姞鍏?), 14),
        meta: `韬唤 ${shortenText(roleText, 10)}`,
        note: `${(snapshot.orgEntries || []).length || 0} 涓娍鍔涚劍鐐筦,
        tone: 'gold',
      });
    }

    function buildShellHomeTerminalCard(snapshot) {
      if (!snapshot) {
        return buildShellAppPeek({
          value: '缁堢寰呭悓姝?,
          meta: '寰呭悓姝?,
          note: '褰撳墠鑱婂ぉ',
        });
      }
      const latestRequest = resolveShellText((snapshot.pendingRequests || [])[0], resolveShellText(deepGet(snapshot, 'rootData.sys.绯荤粺鎾姤', ''), '寰呭懡涓?));
      const intelCount = (snapshot.unlockedKnowledges || []).length || 0;
      return buildShellAppPeek({
        value: intelCount ? `${intelCount} 鏉℃儏鎶 : (snapshot.questRecordCount ? `${snapshot.questRecordCount} 椤逛换鍔 : '绯荤粺鍦ㄧ嚎'),
        meta: `${snapshot.questRecordCount || 0} 椤逛换鍔?路 ${(snapshot.bestiaryEntries || []).length || 0} 鏉″浘閴碻,
        note: shortenText(latestRequest || '寰呭懡涓?, 18),
        tone: intelCount ? 'gold' : (snapshot.questRecordCount ? 'gold' : 'live'),
      });
    }

    function buildShellArchiveCoreCard(snapshot) {
      if (!snapshot) {
        return buildShellSummaryCard({
          kicker: '瑙掕壊棣栭〉',
          title: toText(snapshot && snapshot.activeName, '褰撳墠鑱婂ぉ'),
          value: '寰呭悓姝?,
          meta: '褰撳墠鑱婂ぉ',
          metrics: [
            { label: '鐘舵€?, value: '寰呭悓姝? },
            { label: '鍦扮偣', value: '--' },
            { label: '闃佃惀', value: '--' },
            { label: '鍏崇郴', value: '--' },
          ],
          rows: [
            { label: '椤甸潰', value: '妗ｆ' },
            { label: '鏁版嵁', value: '鏈帴鍏? },
          ],
          tone: 'hero',
          size: 'hero',
        });
      }
      const stat = deepGet(snapshot, 'activeChar.灞炴€?, {});
      const social = deepGet(snapshot, 'activeChar.绀句氦', {});
      const status = deepGet(snapshot, 'activeChar.鐘舵€?, {});
      const primaryFactionName = snapshot.primaryFaction ? snapshot.primaryFaction[0] : '鏈姞鍏?;
      const primaryFactionRole = snapshot.primaryFaction ? toText(deepGet(snapshot.primaryFaction[1], '韬唤', '鏈姞鍏?), '鏈姞鍏?) : '鏈姞鍏?;
      const titleText = Array.isArray(snapshot.recentTitles) && snapshot.recentTitles.length ? snapshot.recentTitles[0] : '';
      const nextLevelSoul = getNextLevelSoulRequirementWithCap(stat, deepGet(snapshot, 'activeChar', {}));
      const placeText = buildShellLocationLabel(snapshot, { fullLimit: 18, trailLimit: 8 });
      const hasSoulPowerMeter = toNumber(stat.榄傚姏涓婇檺, 0) > 0;
      const hasVitalityMeter = toNumber(stat.浣撳姏涓婇檺, 0) > 0;
      const hasMentalMeter = toNumber(stat.绮剧鍔涗笂闄? 0) > 0;
      const ageMetric = toNumber(stat.骞撮緞, 0);
      const talentMetric = shortenText(toText(stat.talent_tier, '鏈畾'), 8);
      const typeMetric = shortenText(toText(stat.绯诲埆, '鏈畾'), 8);
      const factionJoined = primaryFactionName !== '鏈姞鍏?;
      const nextLevelNeeded = Math.max(0, toNumber(nextLevelSoul && nextLevelSoul.needed, 0));
      const hpPair = getDisplayHpPair(stat);
      const woundText = getDisplayWoundLabel(stat);
      const injurySummary = buildInjurySummaryText(status, { limit: 2, empty: '鏃? });
      const statusSummary = !woundText || /^(鏃?$/i.test(woundText)
        ? toText(status.琛屽姩, '鏃ュ父')
        : `${toText(status.琛屽姩, '鏃ュ父')} / ${woundText}`;
      const identitySummary = summarizeShellIdentityText(social.涓昏韩浠? { limit: 12 }) || shortenText(toText(social.涓昏韩浠? ''), 14);
      const factionSummary = factionJoined
        ? `${shortenText(primaryFactionName, 8)} / ${shortenText(primaryFactionRole, 8)}`
        : (identitySummary || '鐙');
      const fameLevelText = shortenText(toText(social.鍚嶆湜绛夌骇, toText(social.鍚嶆湜绛夌骇, '绫嶇睄鏃犲悕')), 8);
      const allowPrivateArchiveLongPress = canOpenPrivateArchive(snapshot);
      return buildShellSummaryCard({
        kicker: '瑙掕壊棣栭〉',
        title: toText(snapshot.activeName, '褰撳墠瑙掕壊'),
        value: formatCultivationLevelBadge(stat.绛夌骇, '0'),
        meta: placeText,
        badges: [
          titleText ? { text: shortenText(titleText, 10), tone: 'gold' } : '',
          factionJoined ? { text: shortenText(primaryFactionName, 10), tone: 'live' } : '',
          fameLevelText && fameLevelText !== '绫嶇睄鏃犲悕' ? { text: fameLevelText, tone: 'gold' } : '',
        ],
        metrics: [
          hpPair.hpMax > 0 ? { label: 'HP', value: formatShellPair(hpPair.hp, hpPair.hpMax), tone: 'warn' } : { label: '骞撮緞', value: ageMetric > 0 ? String(ageMetric) : '--' },
          hasVitalityMeter ? { label: '浣撳姏', value: formatShellPair(stat.浣撳姏, stat.浣撳姏涓婇檺), tone: 'gold' } : { label: '澶╄祴', value: talentMetric || '--', tone: 'gold' },
          hasSoulPowerMeter ? { label: '榄傚姏', value: formatShellPair(stat.榄傚姏, stat.榄傚姏涓婇檺), tone: 'live' } : { label: '绯诲埆', value: typeMetric || '--', tone: 'live' },
          hasMentalMeter ? { label: '绮剧', value: formatShellPair(stat.绮剧鍔? stat.绮剧鍔涗笂闄? } : { label: '骞撮緞', value: ageMetric > 0 ? String(ageMetric) : '--' },
        ],
        rows: [
          { label: '鐘舵€?, value: statusSummary || '--' },
          { label: factionJoined ? '闃佃惀' : '韬唤', value: factionSummary },
          { label: '鍚嶆湜', value: social.澹版湜 ? `${fameLevelText} 路 ${formatNumber(social.澹版湜)}` : fameLevelText },
          { label: '浼ゅ', value: injurySummary },
        ],
        tone: 'hero',
        size: 'hero',
        metricLimit: 4,
        rowLimit: 3,
        longPressPreview: allowPrivateArchiveLongPress ? PRIVATE_ARCHIVE_PREVIEW_KEY : '',
        longPressDelay: 600,
      });
    }

    function buildShellArmoryCard(snapshot) {
      if (!snapshot) {
        return buildShellSummaryCard({
          kicker: '姝﹁',
          title: '瑁呴厤鎽樿',
          value: '寰呭悓姝?,
          meta: '褰撳墠鑱婂ぉ',
          rows: [
            { label: '鏂楅摖', value: '--' },
            { label: '鏈虹敳', value: '--' },
            { label: '涓绘鍣?, value: '--' },
          ],
        });
      }
      const armor = deepGet(snapshot, 'activeChar.瑁呭.鏂楅摖', {});
      const mech = deepGet(snapshot, 'activeChar.瑁呭.鏈虹敳', {});
      const weapon = deepGet(snapshot, 'activeChar.瑁呭.姝﹀櫒', {});
      const jobs = safeEntries(deepGet(snapshot, 'activeChar.鑱屼笟', {}));
      const jobSummary = jobs.length ? `${jobs[0][0]} Lv.${toText(deepGet(jobs[0][1], 'lv', 0), '0')}` : '鏈紑鍚?;
      const weaponName = toText(weapon.鍚嶇О, '').trim();
      const hasArmor = toNumber(armor.绛夌骇, 0) > 0;
      const hasWeapon = !!weaponName && !/^(鏃爘鏈褰?$/.test(weaponName);
      const hasMech = !!toText(mech.绛夌骇, '').trim();
      const hasLoadout = hasArmor || hasWeapon || hasMech || (snapshot.soulBoneEntries || []).length;
      const armorSummary = toNumber(armor.绛夌骇, 0) > 0
        ? `${toText(armor.鍚嶇О, `${armor.绛夌骇}瀛楁枟閾燻)} / ${toText(armor.瑁呭鐘舵€? '宸茶閰?)}`
        : '鏈閰?;
      const mechSummary = toText(mech.绛夌骇, '') ? `${toText(mech.绛夌骇, '')} 路 ${toText(mech.鍨嬪彿, '鏈畾鍨?)}` : '鏈寕杞?;
      if (!hasLoadout) {
        return buildShellSummaryCard({
          title: jobs.length ? '宸ュ潑' : '姝﹁',
          value: jobs.length ? shortenText(jobSummary, 16) : '0',
          meta: jobs.length ? '鍓亴宸茶褰? : '鏃犺澶?,
          rows: [
            { label: jobs.length ? '鍓亴' : '鏂楅摖', value: jobs.length ? shortenText(jobSummary, 16) : '鏃? },
          ],
        });
      }
      return buildShellSummaryCard({
        kicker: '姝﹁',
        title: '姝﹁',
        value: toNumber(armor.绛夌骇, 0) > 0 ? shortenText(toText(armor.鍚嶇О, `${armor.绛夌骇}瀛楁枟閾燻), 12) : '鏈閰?,
        meta: shortenText(armorSummary, 24),
        metrics: [
          { label: '鏈虹敳', value: toText(mech.绛夌骇, '--') || '--' },
          { label: '榄傞', value: String((snapshot.soulBoneEntries || []).length || 0), tone: 'gold' },
        ],
        rows: [
          { label: '涓绘鍣?, value: shortenText(toText(weapon.鍚嶇О, '鏈褰?), 14) },
          { label: '宸ュ潑', value: shortenText(jobSummary, 14) },
          { label: '鏈虹敳', value: shortenText(mechSummary, 14) },
        ],
      });
    }

    function buildShellVaultCard(snapshot) {
      if (!snapshot) {
        return buildShellSummaryCard({
          kicker: '浠撳簱',
          title: '璧勬簮姒傝',
          value: '寰呭悓姝?,
          meta: '褰撳墠鑱婂ぉ',
          rows: [
            { label: '鑱旈偊', value: '--' },
            { label: '鏄熺綏', value: '--' },
          ],
        });
      }
      const wealth = deepGet(snapshot, 'activeChar.璐㈠瘜', {});
      const inventoryCount = (snapshot.inventoryEntries || []).length || 0;
      const fedCoin = toNumber(wealth.鑱旈偊甯? 0);
      const starCoin = toNumber(wealth.鏄熺綏甯? 0);
      const tangPt = toNumber(wealth.鍞愰棬绉垎, 0);
      const bloodPt = toNumber(wealth.鎴樺姛, 0);
      const hasStoredResources = inventoryCount > 0 || fedCoin > 0 || starCoin > 0 || tangPt > 0 || bloodPt > 0;
      if (!hasStoredResources) {
        return buildShellSummaryCard({
          title: '浠撳簱',
          value: '0',
          meta: '鏃犵墿璧?,
          rows: [
            { label: '璐у竵', value: '0' },
          ],
        });
      }
      return buildShellSummaryCard({
        kicker: '浠撳簱',
        title: '浠撳簱',
        value: `${formatNumber(inventoryCount)} 鐗╄祫`,
        meta: `娴佸姩璧勯噾 ${formatNumber(fedCoin)} / 鏄熺綏 ${formatNumber(starCoin)}`,
        metrics: [
          { label: '鑱旈偊', value: formatNumber(fedCoin), tone: 'live' },
          { label: '鏄熺綏', value: formatNumber(starCoin) },
          { label: '绉垎', value: formatNumber(tangPt), tone: 'gold' },
        ],
        rows: [
          { label: '琛€绁?, value: formatNumber(bloodPt) },
          { label: '鐗╄祫', value: `${formatNumber(inventoryCount)} 绉峘 },
        ],
      });
    }

    function 鏋勫缓绀句氦鍏ュ彛鍧?鏍囬, 涓诲€? 鍓€? 棰勮閿? 鑹茶皟 = '') {
      return `
        <button type="button" class="mvu-social-entry clickable ${鑹茶皟 ? `is-${鑹茶皟}` : ''}" data-preview="${escapeHtmlAttr(棰勮閿?}">
          <b>${htmlEscape(toText(鏍囬, ''))}</b>
          <strong>${htmlEscape(shortenText(toText(涓诲€? '鏆傛棤'), 36))}</strong>
          <span>${htmlEscape(shortenText(toText(鍓€? ''), 30))}</span>
        </button>
      `;
    }

    function buildShellSocialCard(snapshot) {
      if (!snapshot) {
        return `
          <div class="mvu-social-entry-grid">
            ${鏋勫缓绀句氦鍏ュ彛鍧?'鍚嶆湜', '寰呭悓姝?, '绀句細妗ｆ', '绀句細妗ｆ璇︾粏椤?)}
            ${鏋勫缓绀句氦鍏ュ彛鍧?'鍔垮姏', '寰呭悓姝?, '鎵€灞炲娍鍔?, '鎵€灞炲娍鍔涜缁嗛〉')}
            ${鏋勫缓绀句氦鍏ュ彛鍧?'鍏崇郴', '寰呭悓姝?, '浜虹墿鍏崇郴', '浜虹墿鍏崇郴璇︾粏椤?)}
            ${鏋勫缓绀句氦鍏ュ彛鍧?'鎯呮姤', '寰呭悓姝?, '鎯呮姤搴?, '鎯呮姤搴撹缁嗛〉')}
          </div>
        `;
      }
      const social = deepGet(snapshot, 'activeChar.绀句氦', {});
      const primaryFactionName = snapshot.primaryFaction ? snapshot.primaryFaction[0] : '鏈姞鍏?;
      const primaryFactionRole = snapshot.primaryFaction ? toText(deepGet(snapshot.primaryFaction[1], '韬唤', '鏈姞鍏?), '鏈姞鍏?) : '鏈姞鍏?;
      const topRelationText = snapshot.topRelation
        ? `${shortenText(snapshot.topRelation[0], 8)} / ${toText(deepGet(snapshot.topRelation[1], '鍏崇郴', '闄岀敓'), '闄岀敓')} / ${toNumber(deepGet(snapshot.topRelation[1], '濂芥劅搴?, 0), 0)}`
        : '鏆傛棤楂樹寒鍏崇郴';
      const latestIntelText = getLatestUnlockedIntelText(snapshot, 12, '鏆傛棤');
      const fameLevel = shortenText(toText(social.鍚嶆湜绛夌骇, toText(social.鍚嶆湜绛夌骇, '绫嶇睄鏃犲悕')), 12);
      const reputationText = formatNumber(social.澹版湜);
      const mainIdentity = summarizeShellIdentityText(social.涓昏韩浠? { limit: 12 }) || shortenText(toText(social.涓昏韩浠? ''), 14);
      const factionSummary = primaryFactionName === '鏈姞鍏?
        ? (mainIdentity || '鏈粨鐩?)
        : `${shortenText(primaryFactionName, 8)} / ${shortenText(primaryFactionRole, 8)}`;
      const intelCount = (snapshot.unlockedKnowledges || []).length || 0;
      return `
        <div class="mvu-social-entry-grid">
          ${鏋勫缓绀句氦鍏ュ彛鍧?'鍚嶆湜', `${fameLevel} / ${reputationText}`, mainIdentity || '绀句細妗ｆ', '绀句細妗ｆ璇︾粏椤?, 'gold')}
          ${鏋勫缓绀句氦鍏ュ彛鍧?'鍔垮姏', factionSummary, `${(snapshot.鍔垮姏 || []).length || 0} 椤瑰綊灞瀈, '鎵€灞炲娍鍔涜缁嗛〉', 'live')}
          ${鏋勫缓绀句氦鍏ュ彛鍧?'鍏崇郴', topRelationText, `${(snapshot.relations || []).length || 0} 鏉″叧绯籤, '浜虹墿鍏崇郴璇︾粏椤?)}
          ${鏋勫缓绀句氦鍏ュ彛鍧?'鎯呮姤', `${intelCount} / ${latestIntelText}`, snapshot.publicIntel ? '鍏紑鎯呮姤' : '鎯呮姤搴?, '鎯呮姤搴撹缁嗛〉')}
        </div>
      `;
    }

    function getShellLocalCharacterEntries(snapshot, limit = 6) {
      if (!snapshot) return [];
      const activeCharKey = resolveSnapshotCharKey(snapshot, toText(snapshot.activeName, ''));
      const activeDisplayName = toText(deepGet(snapshot, 'activeChar.name', deepGet(snapshot, 'activeChar.base.name', snapshot.activeName)), snapshot.activeName);
      const currentLoc = toText(deepGet(snapshot, 'activeChar.鐘舵€?浣嶇疆', snapshot.currentLoc), snapshot.currentLoc);
      const normalizedLoc = toText(snapshot.normalizedLoc, currentLoc);
      return safeEntries(deepGet(snapshot, 'rootData.char', {}))
        .filter(([name, char]) => {
          const displayName = toText(deepGet(char, 'name', deepGet(char, 'base.name', name)), name);
          if ((activeCharKey && name === activeCharKey) || (activeDisplayName && displayName === activeDisplayName)) return false;
          const npcLoc = toText(deepGet(char, '鐘舵€?浣嶇疆', ''), '');
          if (!npcLoc) return false;
          return isLocationCompatible(currentLoc, npcLoc) || isLocationCompatible(normalizedLoc, npcLoc);
        })
        .slice(0, Math.max(1, toNumber(limit, 6)));
    }

    function buildShellMapCurrentCard(snapshot) {
      const currentMapDisplayName = getMapDisplayName(snapshot);
      const localFaction = toText(deepGet(snapshot, 'locationData.鎺屾帶鍔垮姏', '鏈煡'), '鏈煡');
      const nodeStores = safeEntries(deepGet(snapshot, 'locationData.鍟嗗簵', {}));
      return buildShellSummaryCard({
        kicker: '鍦扮偣',
        title: shortenText(toText(snapshot.normalizedLoc, snapshot.currentLoc), 22),
        value: shortenText(currentMapDisplayName, 16),
        meta: snapshot.normalizedLoc !== snapshot.currentLoc ? shortenText(snapshot.currentLoc, 22) : '',
        rows: [
          { label: '鎺屾帶', value: shortenText(localFaction, 22) },
          { label: '搴楅摵', value: nodeStores.length ? `${nodeStores.length} 澶刞 : '0' },
          { label: '渚涚粰', value: toText(snapshot && snapshot.鏈湴渚涚粰, '鏃犱緵缁?) },
          { label: '浠锋牸', value: toText(snapshot && snapshot.浠锋牸甯? '鏃?) },
          { label: '鎴愪氦', value: toText(snapshot && snapshot.鏈€杩戞垚浜ゅ奖鍝? '骞崇ǔ') },
        ],
      });
    }

    function buildShellLocalRosterRows(localEntries, limit = 4, 閫夐」 = {}) {
      const entries = (Array.isArray(localEntries) ? localEntries : []).slice(0, Math.max(1, toNumber(limit, 4)));
      const 鍚敤鎿嶄綔 = !!(閫夐」 && 閫夐」.鍚敤鎿嶄綔);
      const 褰撳墠鍦扮偣 = toText(閫夐」 && 閫夐」.褰撳墠鍦扮偣, '');
      if (!entries.length) {
        return '<div class="mvu-shell-roster-empty">鏆傛棤鏈湴瑙掕壊</div>';
      }
      return entries.map(([name, char]) => {
        const displayName = toText(deepGet(char, 'name', deepGet(char, 'base.name', name)), name);
        const identity = summarizeShellIdentityText(deepGet(char, '绀句氦.涓昏韩浠?, ''), { limit: 14 })
          || shortenText(toText(deepGet(char, '绀句氦.涓昏韩浠?, '鏈煡韬唤'), '鏈煡韬唤'), 14);
        const action = resolveShellText(deepGet(char, '鐘舵€?琛屽姩', ''), '鏃ュ父') || '鏃ュ父';
        const 鍙鎵?= 鑾峰彇瑙掕壊宸ュ潑濮旀墭鑳藉姏鎽樿(char).length > 0;
        const 鎿嶄綔鎸夐挳 = 鍚敤鎿嶄綔
          ? `
            <div class="mvu-shell-roster-actions">
              <button type="button" class="mvu-shell-roster-action clickable" data-preview="瑙掕壊妗ｆ锛?{escapeHtmlAttr(displayName)}">妗ｆ</button>
              <button type="button" class="mvu-shell-roster-action map-dispatch-action-btn live" data-action="talk" data-target="${escapeHtmlAttr(褰撳墠鍦扮偣)}" data-current-loc="${escapeHtmlAttr(褰撳墠鍦扮偣)}" data-npc-target="${escapeHtmlAttr(displayName)}">瀵硅瘽</button>
              <button type="button" class="mvu-shell-roster-action map-dispatch-action-btn" data-action="intel" data-target="${escapeHtmlAttr(褰撳墠鍦扮偣)}" data-current-loc="${escapeHtmlAttr(褰撳墠鍦扮偣)}" data-npc-target="${escapeHtmlAttr(displayName)}">璇锋暀</button>
              <button type="button" class="mvu-shell-roster-action map-dispatch-action-btn warn" data-action="battle" data-target="${escapeHtmlAttr(褰撳墠鍦扮偣)}" data-current-loc="${escapeHtmlAttr(褰撳墠鍦扮偣)}" data-npc-target="${escapeHtmlAttr(displayName)}">鍒囩</button>
              <button type="button" class="mvu-shell-roster-action map-dispatch-action-btn" data-action="trade" data-target="${escapeHtmlAttr(褰撳墠鍦扮偣)}" data-current-loc="${escapeHtmlAttr(褰撳墠鍦扮偣)}" data-npc-target="${escapeHtmlAttr(displayName)}">浜ゆ槗</button>
              ${鍙鎵?? `<button type="button" class="mvu-shell-roster-action map-dispatch-action-btn warn" data-action="commission" data-target="${escapeHtmlAttr(褰撳墠鍦扮偣)}" data-current-loc="${escapeHtmlAttr(褰撳墠鍦扮偣)}" data-npc-target="${escapeHtmlAttr(displayName)}" data-executor-type="private">濮旀墭</button>` : ''}
            </div>
          `
          : '';
        return `
          <div class="mvu-shell-roster-row${鍚敤鎿嶄綔 ? ' mvu-shell-roster-row--interactive' : ''}">
            <button type="button" class="mvu-shell-roster-name clickable" data-preview="瑙掕壊妗ｆ锛?{escapeHtmlAttr(displayName)}">${htmlEscape(shortenText(displayName, 12))}</button>
            <div class="mvu-shell-roster-meta">${htmlEscape(shortenText(identity, 18))}</div>
            <div class="mvu-shell-roster-state">${htmlEscape(shortenText(action, 16))}</div>
            ${鎿嶄綔鎸夐挳}
          </div>
        `;
      }).join('');
    }

    function buildShellMapLocalCharactersCard(snapshot) {
      const localEntries = getShellLocalCharacterEntries(snapshot, 4);
      const currentLoc = toText(deepGet(snapshot, 'activeChar.鐘舵€?浣嶇疆', snapshot.currentLoc), snapshot.currentLoc);
      return `
        <div class="mvu-shell-roster-card">
          <div class="mvu-shell-roster-head">
            <div>
              <span>鏈湴瑙掕壊</span>
              <strong>${htmlEscape(shortenText(currentLoc, 22))}</strong>
            </div>
            <b>${htmlEscape(localEntries.length ? `${localEntries.length} 鍚峘 : '0')}</b>
          </div>
          <div class="mvu-shell-roster-list">
            ${buildShellLocalRosterRows(localEntries, 4, { 褰撳墠鍦扮偣: currentLoc })}
          </div>
        </div>
      `;
    }

    function buildShellMapRouteCard(snapshot) {
      const routeCount = snapshot.mapTravelCandidates.length || snapshot.mapNodeLabels.length || 0;
      return buildShellSummaryCard({
        kicker: '璺戝浘',
        title: '璺嚎',
        value: `${routeCount} 鏉,
        meta: `${shortenText(getMapDisplayName(snapshot), 18)} 路 ${safeEntries(snapshot.mapAvailableChildMaps).length} 涓瓙鍥綻,
        rows: [
          { label: '褰撳墠鍦板浘', value: shortenText(getMapDisplayName(snapshot), 22) },
          { label: '瀛愬浘', value: safeEntries(snapshot.mapAvailableChildMaps).length ? `${safeEntries(snapshot.mapAvailableChildMaps).length} 涓叆鍙 : '鏆傛棤瀛愬浘' },
          { label: '鍏ュ彛', value: routeCount ? `${routeCount} 鏉″彲鐢ㄨ矾绾縛 : '鏆傛棤璺嚎' },
        ],
      });
    }

    function buildShellMapDynamicCard(snapshot) {
      if (!snapshot || (!snapshot.mapVisibleDynamicEntries.length && !snapshot.latestTimeline)) {
        return buildShellSummaryCard({
          title: '鍔ㄦ€?,
          value: '0',
          meta: '鏃犲湴鍥惧姩鎬?,
          rows: [],
        });
      }
      return buildShellSummaryCard({
        kicker: '鍔ㄦ€?,
        title: '鍔ㄦ€?,
        value: `${snapshot.mapVisibleDynamicEntries.length || 0} 澶刞,
        meta: snapshot.latestTimeline ? shortenText(snapshot.latestTimeline[0], 20) : '绛夊緟鏂板彉鍖?,
        rows: [
          { label: '鍦板浘淇℃伅', value: snapshot.latestTimeline ? shortenText(toText(deepGet(snapshot.latestTimeline[1], '浜嬩欢', snapshot.latestTimeline[0]), snapshot.latestTimeline[0]), 28) : '鏆傛棤鏂板彉鍖? },
        ],
      });
    }

    function buildShellWorldHeroCard(snapshot) {
      if (!snapshot) {
        return buildShellSummaryCard({
          kicker: '涓栫晫',
          title: '鏃剁┖涓灑',
          value: '寰呭悓姝?,
          meta: '褰撳墠鑱婂ぉ',
          metrics: [
            { label: '鍋忓樊', value: '--' },
            { label: '浜嬩欢', value: '--' },
            { label: '瑙侀椈', value: '--' },
            { label: '妫€?, value: '--' },
          ],
          note: '绛夊緟鍚屾',
          tone: 'hero',
          size: 'hero',
        });
      }
      const rawWorldTime = toText(deepGet(snapshot, 'rootData.world.鏃堕棿._calendar', deepGet(snapshot, 'rootData.world.鏃堕棿.calendar', '鏃堕棿鏈悓姝?)), '鏃堕棿鏈悓姝?);
      const worldTime = resolveShellText(rawWorldTime, '绛夊緟鍚屾');
      const deviation = toNumber(deepGet(snapshot, 'rootData.world.鍋忓樊鍊?, 0), 0);
      const deviationState = deviation >= 40 ? '楂樺嵄' : (deviation >= 10 ? '娉㈠姩' : '骞崇ǔ');
      const forestRatio = Math.max(0, Math.min(100, Number(((toNumber(snapshot.forestKilledAge, 0) / 1000000) * 100).toFixed(1))));
      const recentNews = buildRecentNewsSummary(snapshot, { seqLimit: 2, intelLimit: 2 });
      const worldAlertText = resolveShellText(snapshot.worldAlert, '');
      const timelineEventText = snapshot.latestTimeline
        ? toText(deepGet(snapshot.latestTimeline[1], '浜嬩欢', snapshot.latestTimeline[0]), snapshot.latestTimeline[0])
        : '';
      return buildShellSummaryCard({
        kicker: '涓栫晫',
        title: '涓栫晫',
        value: deviationState,
        meta: shortenText(worldTime || '绛夊緟鍚屾', 24),
        badges: [
          snapshot.latestTimeline ? shortenText(snapshot.latestTimeline[0], 10) : '',
          worldAlertText ? { text: shortenText(worldAlertText, 10), tone: deviation >= 40 ? 'warn' : 'live' } : '',
        ],
        metrics: [
          { label: '鍋忓樊', value: String(deviation), tone: deviation >= 40 ? 'warn' : (deviation >= 10 ? 'gold' : 'live') },
          { label: '浜嬩欢', value: String((snapshot.timelineEntries || []).length || 0) },
          { label: '瑙侀椈', value: String((recentNews.cards || []).length || 0) },
          { label: '妫€?, value: `${forestRatio}%`, tone: forestRatio >= 70 ? 'warn' : (forestRatio >= 30 ? 'gold' : 'live') },
        ],
        note: shortenText(worldAlertText || timelineEventText || '绛夊緟鏂颁笘鐣屼簨浠?, 34),
        tone: 'hero',
        size: 'hero',
      });
    }

    function buildShellWorldTimelineCard(snapshot) {
      const latestTimeline = snapshot.latestTimeline;
      if (!latestTimeline) {
        return buildShellSummaryCard({
          title: '鏃堕棿绾?,
          value: '0',
          meta: '鏃犵紪骞磋褰?,
        });
      }
      const timelineStatus = latestTimeline
        ? `${toText(deepGet(latestTimeline[1], '鐘舵€?, 'pending'), 'pending')} / Tick ${toText(deepGet(latestTimeline[1], '瑙﹀彂tick', 0), '0')}`
        : '绛夊緟涓嬩竴鏉℃椂闂寸嚎';
      const latestTimelineText = latestTimeline
        ? toText(deepGet(latestTimeline[1], '浜嬩欢', latestTimeline[0]), latestTimeline[0])
        : '';
      return buildShellSummaryCard({
        kicker: '缂栧勾',
        title: '鏃堕棿绾?,
        value: latestTimeline ? shortenText(latestTimeline[0], 12) : '寰呮洿鏂?,
        meta: shortenText(latestTimelineText || '绛夊緟涓嬩竴鏉℃椂闂寸嚎', 22),
        rows: [
          { label: '鐘舵€?, value: shortenText(timelineStatus, 18) },
        ],
      });
    }

    function buildShellWorldAlertCard(snapshot) {
      const worldAlertText = resolveShellText(snapshot.worldAlert, '');
      const auctionStatus = resolveShellText(snapshot.auctionStatus, '浼戝競');
      const auctionLocation = resolveShellText(snapshot.auctionLocation, '');
      const hasAuctionActivity = auctionStatus !== '浼戝競' || !!auctionLocation;
      if (!worldAlertText && !hasAuctionActivity) {
        return buildShellSummaryCard({
          title: '璀︽姤',
          value: '0',
          meta: '鏃犺鎶?,
        });
      }
      return buildShellSummaryCard({
        kicker: '璀︽姤',
        title: '璀︽姤',
        value: worldAlertText ? shortenText(worldAlertText, 12) : (hasAuctionActivity ? shortenText(auctionStatus, 12) : '寰呰瀵?),
        meta: auctionLocation ? shortenText(auctionLocation, 18) : '鎷嶅崠 / 鐢熸€?,
        rows: [
          hasAuctionActivity ? { label: '鎷嶅崠', value: auctionLocation ? `${auctionStatus} / ${shortenText(auctionLocation, 10)}` : auctionStatus } : null,
          worldAlertText ? { label: '鐢熸€?, value: shortenText(worldAlertText, 16) } : null,
        ],
        tone: worldAlertText && /楂樺嵄|璀︽姤/i.test(worldAlertText) ? 'warn' : '',
      });
    }

    function buildShellOrgHeroCard(snapshot) {
      if (!snapshot) {
        return buildShellSummaryCard({
          kicker: '鍔垮姏',
          title: '闃佃惀寰呭悓姝?,
          value: '寰呭悓姝?,
          meta: '褰撳墠鑱婂ぉ',
          rows: [
            { label: '闃佃惀', value: '--' },
            { label: '韬唤', value: '--' },
          ],
        });
      }
      const primaryFactionEntry = getPrimaryFactionEntry(snapshot);
      const factionStats = getPrimaryFactionPowerStats(snapshot);
      const localFaction = toText(deepGet(snapshot, 'locationData.鎺屾帶鍔垮姏', primaryFactionEntry.name || '鏈煡'), primaryFactionEntry.name || '鏈煡');
      return buildShellSummaryCard({
        kicker: '鍔垮姏',
        title: shortenText(primaryFactionEntry.name || '鍔垮姏鐭╅樀', 14),
        value: `${(snapshot.orgEntries || []).length || 0} 涓劍鐐筦,
        meta: `鏈湴 ${shortenText(localFaction, 12)}`,
        badges: [
          snapshot.鍔垮姏[0] ? { text: shortenText(snapshot.鍔垮姏[0][0], 10), tone: 'gold' } : '',
          buildFactionRelationSummary(primaryFactionEntry.data || {}, 2),
        ],
        metrics: [
          { label: '鏋侀檺', value: String(factionStats.limit || 0), tone: 'gold' },
          { label: '瓒呯骇', value: String(factionStats.super || 0) },
          { label: '灏佸彿', value: String(factionStats.title || 0), tone: 'live' },
          { label: '鐒︾偣', value: String((snapshot.orgEntries || []).length || 0) },
        ],
        note: shortenText(buildFactionRelationSummary(primaryFactionEntry.data || {}, 3) || '鏆傛棤鍔垮姏鍏崇郴', 34),
        tone: 'hero',
        size: 'hero',
      });
    }

    function buildShellOrgFactionCard(snapshot) {
      const primaryFaction = snapshot.鍔垮姏[0] || null;
      const primaryFactionEntry = getPrimaryFactionEntry(snapshot);
      const factionName = primaryFaction ? primaryFaction[0] : primaryFactionEntry.name;
      const factionData = primaryFaction ? primaryFaction[1] : primaryFactionEntry.data;
      return buildShellSummaryCard({
        kicker: '鎴戠殑闃佃惀',
        title: shortenText(toText(factionName, '鏈姞鍏?), 14),
        value: shortenText(toText(deepGet(factionData, '韬唤', '鏈姞鍏?), '鏈姞鍏?), 10),
        meta: shortenText(toText(deepGet(factionData, '鐘舵€?, '姝ｅ父'), '姝ｅ父'), 18),
        rows: [
          { label: '褰卞搷鍔?, value: formatNumber(deepGet(factionData, '褰卞搷鍔?, 0)) },
          { label: '鍏崇郴', value: shortenText(buildFactionRelationSummary(primaryFactionEntry.data || {}, 2) || '鏆傛棤', 16) },
        ],
      });
    }

    function buildShellOrgNodeCard(snapshot) {
      return buildShellSummaryCard({
        kicker: '鏈湴鎹偣',
        title: buildShellLocationLabel(snapshot, { fullLimit: 14, trailLimit: 7 }),
        value: shortenText(toText(deepGet(snapshot, 'locationData.鎺屾帶鍔垮姏', '鏈煡'), '鏈煡'), 12),
        meta: `${shortenText(toText(deepGet(snapshot, 'locationData.缁忔祹鐘跺喌', '鏈煡'), '鏈煡'), 8)} 路 ${shortenText(toText(deepGet(snapshot, 'locationData.瀹堟姢鍐涘洟', '鏈煡'), '鏈煡'), 8)}`,
        rows: [
          { label: '搴楅摵', value: String((snapshot.storeNames || []).length || 0) },
          { label: '鍔ㄦ€佺偣', value: String((snapshot.dynamicLocationNames || []).length || 0) },
        ],
      });
    }

    function buildShellTerminalHeroCard(snapshot) {
      if (!snapshot) {
        return buildShellSummaryCard({
          kicker: '缁堢',
          title: '绯荤粺寰呭悓姝?,
          value: '寰呭悓姝?,
          meta: '褰撳墠鑱婂ぉ',
          rows: [
            { label: '鎯呮姤', value: '--' },
            { label: '浠诲姟', value: '--' },
            { label: '鏀跺綍', value: '--' },
          ],
        });
      }
      const sys = deepGet(snapshot, 'rootData.sys', {});
      const recentNews = buildRecentNewsSummary(snapshot, { seqLimit: 2, intelLimit: 2 });
      const worldAlertText = resolveShellText(snapshot.worldAlert, '');
      const intelCount = (snapshot.unlockedKnowledges || []).length || 0;
      return buildShellSummaryCard({
        kicker: '缁堢',
        title: '缁堢',
        value: intelCount ? `${intelCount} 鎯呮姤` : (snapshot.questRecordCount ? `${snapshot.questRecordCount} 浠诲姟` : '鍦ㄧ嚎'),
        meta: shortenText(resolveShellText(sys.绯荤粺鎾姤, '寰呭懡涓?) || '寰呭懡涓?, 26),
        badges: [
          worldAlertText ? { text: shortenText(worldAlertText, 10), tone: 'warn' } : '',
          intelCount ? { text: `宸插綍 ${intelCount}`, tone: 'gold' } : (snapshot.questRecordCount ? '浠诲姟寰呰窡杩? : '寰呭懡涓?),
        ],
        metrics: [
          { label: '鎯呮姤', value: String(intelCount), tone: 'gold' },
          { label: '浠诲姟', value: String(snapshot.questRecordCount || 0), tone: 'live' },
          { label: '鏀跺綍', value: String((snapshot.bestiaryEntries || []).length || 0) },
        ],
        note: shortenText(resolveShellText(recentNews.summary, '寰呭懡涓?), 34),
        tone: 'hero',
        size: 'hero',
      });
    }

    function buildShellTerminalPlanCard(snapshot) {
      const planSummary = buildRecentPlanSummary(snapshot, { worldLimit: 2, recordLimit: 2 });
      const worldText = resolveShellText(planSummary.worldPlans[0] && planSummary.worldPlans[0].desc, '鏆傛棤涓栫晫瀹夋帓');
      const personalText = resolveShellText(planSummary.personalPlans[0] && planSummary.personalPlans[0].desc, '鏆傛棤涓汉寰呭姙');
      return buildShellSummaryCard({
        kicker: '瀹夋帓',
        title: '瀹夋帓',
        value: `${(planSummary.worldPlans || []).length + (planSummary.personalPlans || []).length} 椤筦,
        meta: shortenText(worldText, 22),
        rows: [
          { label: '涓栫晫', value: shortenText(worldText, 18) },
          { label: '涓汉', value: shortenText(personalText, 18) },
        ],
        note: shortenText(resolveShellText(planSummary.summary, '鏆傛棤瀹夋帓'), 26),
      });
    }

    function buildShellTerminalIntelCard(snapshot) {
      if (!(snapshot.unlockedKnowledges || []).length && !(snapshot.pendingRequests || []).length) {
        return buildShellSummaryCard({
          title: '鎯呮姤',
          value: '0',
          meta: `寰呮牳瀹?${toNumber(snapshot && snapshot.鎯呮姤寰呮牳瀹炴暟閲? 0)} 鏉?路 鍑€鍊?${toNumber(snapshot && snapshot.鎯呮姤璇佹嵁鍑€鍊? 0)}`,
          note: shortenText(toText(snapshot && snapshot.鎯呮姤鏈€杩戞牳瀹炵粨鏋? '鏆傛棤鏍稿疄璁板綍'), 24),
        });
      }
      const latestIntelText = getLatestUnlockedIntelText(snapshot, 12, '鏆傛棤鎯呮姤');
      const pendingRequestText = resolveShellText((snapshot.pendingRequests || [])[0], '');
      return buildShellSummaryCard({
        kicker: '鎯呮姤',
        title: '鎯呮姤',
        value: (snapshot.unlockedKnowledges || []).length ? `${(snapshot.unlockedKnowledges || []).length} 宸插綍` : '寰呭懡涓?,
        meta: shortenText(`寰呮牳瀹?${toNumber(snapshot && snapshot.鎯呮姤寰呮牳瀹炴暟閲? 0)} 鏉?路 鍑€鍊?${toNumber(snapshot && snapshot.鎯呮姤璇佹嵁鍑€鍊? 0)}`, 20),
        note: shortenText(
          toText(snapshot && snapshot.鎯呮姤鏈€杩戞牳瀹炵粨鏋? '') || pendingRequestText || latestIntelText || '鏆傛棤鏍稿疄璁板綍',
          24,
        ),
      });
    }

    function buildShellTerminalNewsCard(snapshot) {
      const newsSummary = buildRecentNewsSummary(snapshot, { seqLimit: 1, intelLimit: 1 });
      if (!(newsSummary.cards || []).length) {
        return buildShellSummaryCard({
          title: '瑙侀椈',
          value: '0',
          meta: '鏃犺闂?,
        });
      }
      const globalNews = resolveShellText((newsSummary.globalNews[0] || {}).desc, '');
      const personalNews = resolveShellText((newsSummary.personalNews[0] || {}).desc, '');
      const summaryText = resolveShellText(newsSummary.summary, '绛夊緟鏂拌闂?);
      return buildShellSummaryCard({
        kicker: '鎾姤',
        title: '鎾姤',
        value: `${(newsSummary.cards || []).length || 0} 鏉,
        meta: shortenText(globalNews || personalNews || summaryText || '绛夊緟鏂拌闂?, 20),
        note: globalNews && personalNews ? shortenText(personalNews, 24) : '',
      });
    }

    function buildShellTerminalBestiaryCard(snapshot) {
      const bestiaryNames = (snapshot.bestiaryEntries || []).map(([name]) => name).filter(Boolean);
      if (!bestiaryNames.length) {
        return buildShellSummaryCard({
          title: '鍥鹃壌',
          value: '0',
          meta: `妗ｄ綅 ${toText(snapshot && snapshot.鍥鹃壌褰撳墠妗ｄ綅, '鏃?)}`,
          note: `涓嬩竴妗?${toText(snapshot && snapshot.鍥鹃壌涓嬩竴妗ｈ繘搴? '--')}`,
        });
      }
      return buildShellSummaryCard({
        kicker: '鏀跺綍',
        title: '鏀跺綍',
        value: bestiaryNames.length ? `${bestiaryNames.length} 绉峘 : '寰呮敹褰?,
        meta: shortenText(`${toText(snapshot && snapshot.鍥鹃壌鑱氱劍鍚嶇О, bestiaryNames[0] || '鏆傛棤')} 路 ${toText(snapshot && snapshot.鍥鹃壌褰撳墠妗ｄ綅, '鍒濊瘑')}`, 20),
        note: shortenText(`涓嬩竴妗?${toText(snapshot && snapshot.鍥鹃壌涓嬩竴妗ｈ繘搴? '--')} 路 鍊惧悜 ${toText(snapshot && snapshot.鍥鹃壌鎴愰暱鍊惧悜, '鍧囪　')}`, 24),
      });
    }

    function buildShellTerminalQuestCard(snapshot) {
      const questBoardEntries = safeEntries(deepGet(snapshot, 'rootData.world.濮旀墭鏉?, {})).filter(([, item]) => item && typeof item === 'object');
      const pendingRequestText = resolveShellText((snapshot.pendingRequests || [])[0], '');
      if (!snapshot.questRecordCount && !questBoardEntries.length && !pendingRequestText) {
        return buildShellSummaryCard({
          title: '浠诲姟',
          value: '0',
          meta: `${toText(snapshot && snapshot.褰撳墠浠诲姟璺嚎, '鏃?)} 路 ${toText(snapshot && snapshot.褰撳墠浠诲姟椋庨櫓绾у埆, '鏃?)}`,
          note: toText(snapshot && snapshot.褰撳墠浠诲姟鍚嶇О, '鏆傛棤浠诲姟'),
        });
      }
      return buildShellSummaryCard({
        kicker: '浠诲姟',
        title: '浠诲姟',
        value: snapshot.questRecordCount ? `${snapshot.questRecordCount} 椤筦 : '寰呭懡涓?,
        meta: `${toText(snapshot && snapshot.褰撳墠浠诲姟璺嚎, '鏃?)} 路 ${toText(snapshot && snapshot.褰撳墠浠诲姟椋庨櫓绾у埆, '鏃?)} 路 涓嬭妭鐐?${toText(snapshot && snapshot.褰撳墠浠诲姟涓嬩竴鑺傜偣杩涘害, '--')}`,
        note: shortenText(toText(snapshot && snapshot.褰撳墠浠诲姟鍚嶇О, '') || pendingRequestText, 24),
      });
    }

    function buildShellCurrentAreaView(snapshot) {
      const currentMapDisplayName = getMapDisplayName(snapshot);
      const currentLoc = toText(deepGet(snapshot, 'activeChar.鐘舵€?浣嶇疆', snapshot.currentLoc), snapshot.currentLoc);
      const localFaction = toText(deepGet(snapshot, 'locationData.鎺屾帶鍔垮姏', '鏈煡'), '鏈煡');
      const nodeStores = safeEntries(deepGet(snapshot, 'locationData.鍟嗗簵', {}));
      const localEntries = getShellLocalCharacterEntries(snapshot, 8);
      const localCards = buildShellLocalRosterRows(localEntries, 8);
      return {
        title: '褰撳墠鍦板尯',
        body: `
          <div class="mvu-shell-lite-root" data-shell-light-view="area">
            <section class="mvu-shell-lite-card">
              <div class="mvu-shell-lite-section-title">鏈湴瑙掕壊</div>
              <div class="mvu-shell-roster-list">${localCards}</div>
            </section>
            <section class="mvu-shell-lite-card mvu-shell-lite-card--hero">
              <div class="mvu-shell-lite-head">
                <span>${htmlEscape(shortenText(currentMapDisplayName, 18))}</span>
                <strong>${htmlEscape(shortenText(currentLoc, 28))}</strong>
              </div>
              <div class="mvu-shell-lite-grid">
                <div class="mvu-shell-lite-stat"><span>鎺屾帶</span><b>${htmlEscape(shortenText(localFaction, 14))}</b></div>
                <div class="mvu-shell-lite-stat"><span>瑙掕壊</span><b>${htmlEscape(String(localEntries.length))}</b></div>
                <div class="mvu-shell-lite-stat"><span>鍟嗗簵</span><b>${htmlEscape(String(nodeStores.length))}</b></div>
              </div>
            </section>
          </div>
        `
      };
    }

    function buildShellQuestOverviewView(snapshot) {
      const questRecords = (snapshot.recordEntries || []).filter(([, item]) => item && typeof item === 'object' && (
        Object.prototype.hasOwnProperty.call(item, '鐘舵€?)
        || Object.prototype.hasOwnProperty.call(item, '鐩爣杩涘害')
        || Object.prototype.hasOwnProperty.call(item, '濂栧姳甯?)
        || Object.prototype.hasOwnProperty.call(item, '濂栧姳澹版湜')
      ));
      const questBoardEntries = safeEntries(deepGet(snapshot, 'rootData.world.濮旀墭鏉?, {})).filter(([, item]) => item && typeof item === 'object');
      const renderQuest = ([name, item]) => {
        const status = toText(item && item['鐘舵€?], '杩涜涓?);
        const current = toNumber(item && item['褰撳墠杩涘害'], 0);
        const target = Math.max(1, toNumber(item && item['鐩爣杩涘害'], 1));
        const desc = toText(item && item['鎻忚堪'], '鏆傛棤璇存槑');
        return `
          <div class="mvu-shell-lite-item">
            <b>${htmlEscape(shortenText(name, 18))}</b>
            <span>${htmlEscape(status)} 路 ${htmlEscape(`${current}/${target}`)}</span>
            <div class="mvu-shell-lite-track"><i style="width:${ratioPercent(current, target)}%;"></i></div>
            <em>${htmlEscape(shortenText(desc, 28))}</em>
          </div>
        `;
      };
      const renderBoard = ([id, item]) => {
        const title = toText(item && item['鏍囬'], id);
        const status = toText(item && item['鐘舵€?], '寰呮帴鍙?);
        const publisher = toText(item && item['鍙戝竷鑰?], '绯荤粺');
        return `
          <div class="mvu-shell-lite-item">
            <b>${htmlEscape(shortenText(title, 18))}</b>
            <span>${htmlEscape(status)} 路 ${htmlEscape(shortenText(publisher, 12))}</span>
          </div>
        `;
      };
      const questList = questRecords.length
        ? questRecords.slice(0, 5).map(renderQuest).join('')
        : '<div class="mvu-shell-lite-empty">鏆傛棤浠诲姟</div>';
      const boardList = questBoardEntries.length
        ? questBoardEntries.slice(0, 4).map(renderBoard).join('')
        : '<div class="mvu-shell-lite-empty">鏆傛棤濮旀墭</div>';
      return {
        title: '浠诲姟',
        body: `
          <div class="mvu-shell-lite-root" data-shell-light-view="quest">
            <section class="mvu-shell-lite-card mvu-shell-lite-card--hero">
              <div class="mvu-shell-lite-head">
                <span>浠诲姟</span>
                <strong>${htmlEscape(questRecords[0] ? questRecords[0][0] : '鏆傛棤杩涜涓换鍔?)}</strong>
              </div>
              <div class="mvu-shell-lite-grid">
                <div class="mvu-shell-lite-stat"><span>浠诲姟</span><b>${htmlEscape(String(questRecords.length))}</b></div>
                <div class="mvu-shell-lite-stat"><span>濮旀墭</span><b>${htmlEscape(String(questBoardEntries.length))}</b></div>
                <div class="mvu-shell-lite-stat"><span>寰呭姙</span><b>${htmlEscape(String((snapshot.pendingRequests || []).length || 0))}</b></div>
              </div>
            </section>
            <section class="mvu-shell-lite-card">
              <div class="mvu-shell-lite-section-title">鎴戠殑浠诲姟</div>
              <div class="mvu-shell-lite-list">${questList}</div>
            </section>
            <section class="mvu-shell-lite-card">
              <div class="mvu-shell-lite-section-title">濮旀墭鏉?/div>
              <div class="mvu-shell-lite-list">${boardList}</div>
            </section>
          </div>
        `
      };
    }

    function buildShellLiteStats(items = [], options = {}) {
      const className = toText(options.className, '').trim();
      const list = (Array.isArray(items) ? items : [])
        .map(item => ({
          label: toText(item && item.label, '').trim(),
          value: toText(item && item.value, '').trim(),
        }))
        .filter(item => item.label || item.value);
      if (!list.length) return '';
      return `
        <div class="mvu-shell-lite-grid ${htmlEscape(className)}">
          ${list.map(item => `
            <div class="mvu-shell-lite-stat">
              <span>${htmlEscape(item.label)}</span>
              <b>${htmlEscape(item.value)}</b>
            </div>
          `).join('')}
        </div>
      `;
    }

    function buildShellLiteTags(items = []) {
      const list = (Array.isArray(items) ? items : [])
        .map(item => toText(item, '').trim())
        .filter(Boolean)
        .slice(0, 5);
      if (!list.length) return '';
      return `<div class="mvu-shell-lite-tags">${list.map(item => `<span>${htmlEscape(shortenText(item, 16))}</span>`).join('')}</div>`;
    }

    function buildShellLiteItemList(items = [], fallback = '鏆傛棤璁板綍') {
      const list = (Array.isArray(items) ? items : []).filter(item => item && (item.title || item.meta || item.note));
      if (!list.length) return `<div class="mvu-shell-lite-empty">${htmlEscape(fallback)}</div>`;
      return list.map(item => `
        <div class="mvu-shell-lite-item">
          <b>${htmlEscape(shortenText(item.title || '鏈懡鍚?, 22))}</b>
          ${item.meta ? `<span>${htmlEscape(shortenText(item.meta, 30))}</span>` : ''}
          ${item.note ? `<em>${htmlEscape(shortenText(item.note, 34))}</em>` : ''}
        </div>
      `).join('');
    }

    function buildShellLifeView(snapshot) {
      const stat = deepGet(snapshot, 'activeChar.灞炴€?, {});
      const status = deepGet(snapshot, 'activeChar.鐘舵€?, {});
      const social = deepGet(snapshot, 'activeChar.绀句氦', {});
      const hpPair = getDisplayHpPair(stat);
      const woundLabel = getDisplayWoundLabel(stat);
      const placeText = buildShellLocationLabel(snapshot, { fullLimit: 24, trailLimit: 10 });
      return {
        title: '璇︾粏妗ｆ',
        body: `
          <div class="mvu-shell-lite-root" data-shell-light-view="life">
            <section class="mvu-shell-lite-card mvu-shell-lite-card--hero">
              <div class="mvu-shell-lite-head">
                <span>${htmlEscape(placeText || '褰撳墠浣嶇疆')}</span>
                <strong>${htmlEscape(toText(snapshot && snapshot.activeName, '褰撳墠瑙掕壊'))}</strong>
              </div>
              ${buildShellLiteStats([
                { label: '骞撮緞 / 鎬у埆', value: `${toText(stat.骞撮緞, '--')} / ${toText(stat.鎬у埆, '鏈煡')}` },
                { label: '淇负绛夌骇', value: formatCultivationLevelBadge(stat.绛夌骇, '0') },
                { label: 'HP', value: formatShellPair(hpPair.hp, hpPair.hpMax) },
                { label: '浣撳姏', value: formatShellPair(stat.浣撳姏, stat.浣撳姏涓婇檺) },
                { label: '榄傚姏', value: formatShellPair(stat.榄傚姏, stat.榄傚姏涓婇檺) },
                { label: '绮剧', value: formatShellPair(stat.绮剧鍔? stat.绮剧鍔涗笂闄? },
                { label: '鐘舵€?, value: shortenText(toText(status.琛屽姩, '鏃ュ父'), 16) },
                { label: '浼ゅ娍', value: woundLabel },
              ], { className: 'mvu-shell-lite-grid--two' })}
            </section>
            <section class="mvu-shell-lite-card">
              <div class="mvu-shell-lite-section-title">灞炴€?/div>
              ${buildShellLiteStats([
                { label: '鍔涢噺', value: formatNumber(stat.鍔涢噺) },
                { label: '闃插尽', value: formatNumber(stat.闃插尽) },
                { label: '鏁忔嵎', value: formatNumber(stat.鏁忔嵎) },
                { label: '绮剧澧冪晫', value: toText(stat.绮剧澧冪晫, '鐏靛厓澧?) },
              ], { className: 'mvu-shell-lite-grid--two' })}
            </section>
            <section class="mvu-shell-lite-card">
              <div class="mvu-shell-lite-section-title">鎽樿</div>
              <div class="mvu-shell-lite-list">${buildShellLiteItemList([
                { title: '韬唤', meta: summarizeShellIdentityText(social.涓昏韩浠? { limit: 22 }) || toText(social.涓昏韩浠? '鏈褰?) },
                { title: '闃佃惀', meta: snapshot.primaryFaction ? `${snapshot.primaryFaction[0]} / ${toText(deepGet(snapshot.primaryFaction[1], '韬唤', '鎴愬憳'), '鎴愬憳')}` : '鏈姞鍏? },
                { title: '鍚嶆湜', meta: `${toText(social.鍚嶆湜绛夌骇, toText(social.鍚嶆湜绛夌骇, '绫嶇睄鏃犲悕'))} 路 ${formatNumber(social.澹版湜)}` },
              ], '鏆傛棤鎽樿')}</div>
            </section>
          </div>
        `,
      };
    }

    function buildShellArmoryView(snapshot) {
      const armor = deepGet(snapshot, 'activeChar.瑁呭.鏂楅摖', {});
      const mech = deepGet(snapshot, 'activeChar.瑁呭.鏈虹敳', {});
      const weapon = deepGet(snapshot, 'activeChar.瑁呭.姝﹀櫒', {});
      const jobs = safeEntries(deepGet(snapshot, 'activeChar.鑱屼笟', {}));
      const soulBoneEntries = Array.isArray(snapshot && snapshot.soulBoneEntries) ? snapshot.soulBoneEntries : [];
      const armorText = toNumber(armor.绛夌骇, 0) > 0
        ? `${toText(armor.鍚嶇О, `${armor.绛夌骇}瀛楁枟閾燻)} / ${toText(armor.瑁呭鐘舵€? '鏈澶?)}`
        : '鏈閰?;
      const mechText = toText(mech.绛夌骇, '') && toText(mech.绛夌骇, '鏃?) !== '鏃?
        ? `${toText(mech.绛夌骇, '鏃?)} 路 ${toText(mech.鍨嬪彿, '鏈畾鍨?)}`
        : '鏈寕杞?;
      const weaponText = toText(weapon.鍚嶇О || weapon['鍚嶇О'], '鏃?);
      const jobItems = jobs.slice(0, 4).map(([name, info]) => ({
        title: name,
        meta: `Lv.${toText(info && info.绛夌骇, 0)} 路 ${toText(info && info.绉板彿, '鏈畾绾?)}`,
        note: '',
      }));
      return {
        title: '姝﹁',
        body: `
          <div class="mvu-shell-lite-root" data-shell-light-view="armory">
            <section class="mvu-shell-lite-card mvu-shell-lite-card--hero">
              <div class="mvu-shell-lite-head">
                <span>${htmlEscape(shortenText(weaponText, 18))}</span>
                <strong>${htmlEscape(shortenText(armorText, 24))}</strong>
              </div>
              ${buildShellLiteStats([
                { label: '鏈虹敳', value: shortenText(mechText, 10) },
                { label: '榄傞', value: String(soulBoneEntries.length || 0) },
              ])}
            </section>
            <section class="mvu-shell-lite-card">
              <div class="mvu-shell-lite-section-title">鍓亴</div>
              <div class="mvu-shell-lite-list">${buildShellLiteItemList(jobItems, '鏆傛棤鍓亴')}</div>
            </section>
          </div>
        `,
      };
    }

    function buildShellSpiritView(snapshot, previewKey) {
      const config = previewKey === '绗簩姝﹂瓊璇︾粏椤?
        ? (snapshot && snapshot.secondaryTrack)
        : (snapshot && snapshot.primarySpirit);
      if (!config) {
        return {
          title: '姝﹂瓊',
          body: `<div class="mvu-shell-lite-root"><section class="mvu-shell-lite-card"><div class="mvu-shell-lite-empty">鏆傛棤姝﹂瓊</div></section></div>`,
        };
      }
      const souls = Array.isArray(config.souls) ? config.souls : [];
      const rings = Array.isArray(config.榄傜幆) ? config.榄傜幆 : [];
      const skillItems = rings.flatMap(ring => (Array.isArray(ring.skills) ? ring.skills : []).map(skill => ({
        title: toText(skill && skill.name, '榄傛妧'),
        meta: shortenText(toText(ring && ring.title, '榄傜幆'), 24),
        note: shortenText(toText(skill && skill.effectSummary, toText(skill && skill.effectDesc, '')), 30),
      }))).slice(0, 5);
      return {
        title: '姝﹂瓊',
        body: `
          <div class="mvu-shell-lite-root" data-shell-light-view="spirit">
            <section class="mvu-shell-lite-card mvu-shell-lite-card--hero">
              <div class="mvu-shell-lite-head">
                <span>${htmlEscape(shortenText(toText(config.spiritType, config.badge), 16))}</span>
                <strong>${htmlEscape(shortenText(toText(config.spiritName || config.name, '姝﹂瓊'), 22))}</strong>
              </div>
              ${buildShellLiteStats([
                { label: '榄傜伒', value: String(souls.length || 0) },
                { label: '榄傜幆', value: String(rings.length || 0) },
                { label: '灞炴€?, value: shortenText(toText(config.spiritElement, '鏃?), 8) },
              ])}
            </section>
            <section class="mvu-shell-lite-card">
              <div class="mvu-shell-lite-section-title">榄傛妧</div>
              <div class="mvu-shell-lite-list">${buildShellLiteItemList(skillItems, '鏆傛棤榄傛妧')}</div>
            </section>
          </div>
        `,
      };
    }

    function buildShellFusionView(snapshot) {
      const fusionMeta = getFusionArchiveMeta(snapshot || {});
      const entries = (fusionMeta.fusionEntries || []).slice(0, 5).map(([name, item]) => ({
        title: toText(deepGet(item, '鎶€鑳芥暟鎹?榄傛妧鍚?, deepGet(item, '鎶€鑳芥暟鎹?name', name)), name),
        meta: `${toText(deepGet(item, '铻嶅悎妯″紡', 'partner'), 'partner') === 'self' ? '鑷綋' : '鍙屼汉'} 路 ${toText(deepGet(item, '铻嶅悎瀵硅薄', '鏃?), '鏃?)}`,
        note: toText(deepGet(item, '鎶€鑳芥暟鎹?鏁堟灉鎻忚堪', deepGet(item, '鎶€鑳芥暟鎹?effectDesc', '')), ''),
      }));
      return {
        title: '姝﹂瓊铻嶅悎鎶€',
        body: `
          <div class="mvu-shell-lite-root" data-shell-light-view="fusion">
            <section class="mvu-shell-lite-card mvu-shell-lite-card--hero">
              <div class="mvu-shell-lite-head">
                <span>姝﹂瓊铻嶅悎鎶€</span>
                <strong>${htmlEscape(`${fusionMeta.fusionEntries.length || 0} 椤筦)}</strong>
              </div>
              ${buildShellLiteStats([
                { label: '鍙屼汉', value: String(fusionMeta.partnerCount || 0) },
                { label: '鑷綋', value: String(fusionMeta.selfCount || 0) },
              ])}
            </section>
            <section class="mvu-shell-lite-card">
              <div class="mvu-shell-lite-section-title">鏉＄洰</div>
              <div class="mvu-shell-lite-list">${buildShellLiteItemList(entries, '鏆傛棤姝﹂瓊铻嶅悎鎶€')}</div>
            </section>
          </div>
        `,
      };
    }

    function buildShellBloodlineView(snapshot) {
      const config = snapshot && snapshot.bloodline ? snapshot.bloodline : buildBloodlineConfig(deepGet(snapshot, 'activeChar', {}), toText(snapshot && snapshot.activeName, ''));
      const rings = Array.isArray(config.榄傜幆) ? config.榄傜幆 : [];
      const skills = [
        ...(Array.isArray(config.bloodSkills) ? config.bloodSkills : []),
        ...(Array.isArray(config.bloodPassives) ? config.bloodPassives : []),
      ].slice(0, 5).map(skill => ({
        title: toText(skill && skill.name, '琛€鑴夎兘鍔?),
        meta: toText(skill && skill.category, '鑳藉姏'),
        note: toText(skill && skill.effectSummary, toText(skill && skill.effectDesc, '')),
      }));
      return {
        title: '琛€鑴?,
        body: `
          <div class="mvu-shell-lite-root" data-shell-light-view="bloodline">
            <section class="mvu-shell-lite-card mvu-shell-lite-card--hero">
              <div class="mvu-shell-lite-head">
                <span>${htmlEscape(toText(config.core, '鏈嚌鑱?))}</span>
                <strong>${htmlEscape(toText(config.bloodline || config.name, '鏈閱掕鑴?))}</strong>
              </div>
              ${buildShellLiteStats([
                { label: '灏佸嵃', value: String(config.sealLv || 0) },
                { label: '琛€鐜?, value: String(rings.length || 0) },
              ])}
            </section>
            <section class="mvu-shell-lite-card">
              <div class="mvu-shell-lite-section-title">鑳藉姏</div>
              <div class="mvu-shell-lite-list">${buildShellLiteItemList(skills, '鏆傛棤鑳藉姏')}</div>
            </section>
          </div>
        `,
      };
    }

    function buildShellWorldStateView(snapshot) {
      const tick = toText(deepGet(snapshot, 'rootData.world.鏃堕棿.tick', '0'), '0');
      const latest = snapshot && snapshot.latestTimeline ? snapshot.latestTimeline : null;
      const auction = deepGet(snapshot, 'rootData.world.鎷嶅崠', {});
      return {
        title: '涓栫晫',
        body: `
          <div class="mvu-shell-lite-root" data-shell-light-view="world">
            <section class="mvu-shell-lite-card mvu-shell-lite-card--hero">
              <div class="mvu-shell-lite-head">
                <span>Tick ${htmlEscape(tick)}</span>
                <strong>${htmlEscape(toText(deepGet(snapshot, 'rootData.world.name', '鏂楃綏澶ч檰'), '鏂楃綏澶ч檰'))}</strong>
              </div>
              ${buildShellLiteStats([
                { label: '瑙掕壊', value: String(safeEntries(deepGet(snapshot, 'rootData.char', {})).length || 0) },
                { label: '鍔垮姏', value: String((snapshot.orgEntries || []).length || 0) },
                { label: '璀︽姤', value: shortenText(toText(snapshot.worldAlert, '鏃?), 8) },
              ])}
            </section>
            <section class="mvu-shell-lite-card">
              <div class="mvu-shell-lite-section-title">鍔ㄦ€?/div>
              <div class="mvu-shell-lite-list">${buildShellLiteItemList([
                latest ? { title: latest[0], meta: `Tick ${toText(deepGet(latest[1], '瑙﹀彂tick', 0), 0)}`, note: toText(deepGet(latest[1], '浜嬩欢', ''), '') } : null,
                { title: '鎷嶅崠', meta: `${toText(auction.鐘舵€? '浼戝競')} 路 ${toText(auction.鍦扮偣, '鏃?)}` },
              ], '鏆傛棤鍔ㄦ€?)}</div>
            </section>
          </div>
        `,
      };
    }

    function buildShellAlertView(snapshot) {
      const auction = deepGet(snapshot, 'rootData.world.鎷嶅崠', {});
      return {
        title: '璀︽姤',
        body: `
          <div class="mvu-shell-lite-root" data-shell-light-view="alert">
            <section class="mvu-shell-lite-card mvu-shell-lite-card--hero">
              <div class="mvu-shell-lite-head">
                <span>${htmlEscape(toText(auction.鍦扮偣, '鏃?))}</span>
                <strong>${htmlEscape(toText(auction.鐘舵€? '浼戝競'))}</strong>
              </div>
              ${buildShellLiteTags([snapshot.worldAlert])}
            </section>
            <section class="mvu-shell-lite-card">
              <div class="mvu-shell-lite-section-title">浠诲姟璁板綍</div>
              <div class="mvu-shell-lite-list">${buildShellLiteItemList([
                { title: '鐢熸€?, meta: toText(snapshot.worldAlert, '鏃?) },
                { title: '鎷嶅崠', meta: `${toText(auction.鐘舵€? '浼戝競')} 路 ${toText(auction.鍦扮偣, '鏃?)}` },
              ], '鏆傛棤璀︽姤')}</div>
            </section>
          </div>
        `,
      };
    }

    function buildShellFactionView(snapshot) {
      const primaryFaction = snapshot && snapshot.primaryFaction ? snapshot.primaryFaction : null;
      const factionName = primaryFaction ? primaryFaction[0] : '鏈姞鍏?;
      const factionData = primaryFaction ? primaryFaction[1] : {};
      const orgEntry = getPrimaryFactionEntry(snapshot || {});
      const relations = safeEntries(deepGet(orgEntry.data, '鍏崇郴', {})).slice(0, 5).map(([name, item]) => ({
        title: name,
        meta: toText(deepGet(item, '鎬佸害', deepGet(item, '鍏崇郴', '鏈煡')), '鏈煡'),
        note: '',
      }));
      return {
        title: '闃佃惀',
        body: `
          <div class="mvu-shell-lite-root" data-shell-light-view="faction">
            <section class="mvu-shell-lite-card mvu-shell-lite-card--hero">
              <div class="mvu-shell-lite-head">
                <span>${htmlEscape(toText(deepGet(factionData, '韬唤', '鏃?), '鏃?))}</span>
                <strong>${htmlEscape(factionName)}</strong>
              </div>
              ${buildShellLiteStats([
                { label: '鏉冮檺', value: toText(deepGet(factionData, '鏉冮檺绾?, '--'), '--') },
                { label: '鍏崇郴', value: String(relations.length || 0) },
              ])}
            </section>
            <section class="mvu-shell-lite-card">
              <div class="mvu-shell-lite-section-title">鍏崇郴</div>
              <div class="mvu-shell-lite-list">${buildShellLiteItemList(relations, '鏆傛棤鍏崇郴')}</div>
            </section>
          </div>
        `,
      };
    }

    function buildShellNodeView(snapshot) {
      const currentLoc = toText(deepGet(snapshot, 'activeChar.鐘舵€?浣嶇疆', snapshot.currentLoc), snapshot.currentLoc);
      const localFaction = toText(deepGet(snapshot, 'locationData.鎺屾帶鍔垮姏', '鏈煡'), '鏈煡');
      const 鍟嗗簵鍒楄〃 = safeEntries(deepGet(snapshot, 'locationData.鍟嗗簵', {})).slice(0, 5).map(([name, item]) => ({
        title: name,
        meta: toText(deepGet(item, '绫诲瀷', deepGet(item, 'type', '鍟嗗簵')), '鍟嗗簵'),
        note: '',
      }));
      return {
        title: '鎹偣',
        body: `
          <div class="mvu-shell-lite-root" data-shell-light-view="node">
            <section class="mvu-shell-lite-card mvu-shell-lite-card--hero">
              <div class="mvu-shell-lite-head">
                <span>${htmlEscape(shortenText(localFaction, 18))}</span>
                <strong>${htmlEscape(shortenText(currentLoc, 28))}</strong>
              </div>
              ${buildShellLiteStats([
                { label: '缁忔祹', value: shortenText(toText(deepGet(snapshot, 'locationData.缁忔祹鐘跺喌', '鏈煡'), '鏈煡'), 8) },
                { label: '瀹堝', value: shortenText(toText(deepGet(snapshot, 'locationData.瀹堟姢鍐涘洟', '鏈煡'), '鏈煡'), 8) },
                { label: '鍟嗗簵', value: String(鍟嗗簵鍒楄〃.length || 0) },
              ])}
            </section>
            <section class="mvu-shell-lite-card">
              <div class="mvu-shell-lite-section-title">鍟嗗簵</div>
              <div class="mvu-shell-lite-list">${buildShellLiteItemList(鍟嗗簵鍒楄〃, '鏆傛棤鍟嗗簵')}</div>
            </section>
          </div>
        `,
      };
    }

    function buildShellTerminalView(snapshot) {
      const requestText = resolveShellText((snapshot.pendingRequests || [])[0], resolveShellText(deepGet(snapshot, 'rootData.sys.绯荤粺鎾姤', ''), '寰呭懡涓?));
      return {
        title: '缁堢',
        body: `
          <div class="mvu-shell-lite-root" data-shell-light-view="terminal">
            <section class="mvu-shell-lite-card mvu-shell-lite-card--hero">
              <div class="mvu-shell-lite-head">
                <span>绯荤粺</span>
                <strong>${htmlEscape(shortenText(requestText, 28))}</strong>
              </div>
              ${buildShellLiteStats([
                { label: '浠诲姟', value: String(snapshot.questRecordCount || 0) },
                { label: '鎯呮姤', value: String((snapshot.unlockedKnowledges || []).length || 0) },
                { label: '鍥鹃壌', value: String((snapshot.bestiaryEntries || []).length || 0) },
              ])}
            </section>
          </div>
        `,
      };
    }

    function buildShellIntelView(snapshot) {
      const unlocked = (snapshot.unlockedKnowledges || []).slice(-5).reverse().map(item => ({
        title: item,
        meta: '宸叉帉鎻?,
        note: '',
      }));
      return {
        title: '鎯呮姤',
        body: `
          <div class="mvu-shell-lite-root" data-shell-light-view="intel">
            <section class="mvu-shell-lite-card mvu-shell-lite-card--hero">
              <div class="mvu-shell-lite-head">
                <span>鎯呮姤</span>
                <strong>${htmlEscape(`${(snapshot.unlockedKnowledges || []).length || 0} 鏉)}</strong>
              </div>
              ${buildShellLiteStats([
                { label: '鏈€鏂?, value: getLatestUnlockedIntelText(snapshot, 8, '鏆傛棤') },
                { label: '璇曠偧', value: String((snapshot.trialEntries || []).length || 0) },
              ])}
            </section>
            <section class="mvu-shell-lite-card">
              <div class="mvu-shell-lite-section-title">鏉＄洰</div>
              <div class="mvu-shell-lite-list">${buildShellLiteItemList(unlocked, '鏆傛棤鎯呮姤')}</div>
            </section>
          </div>
        `,
      };
    }

    function buildShellNewsView(snapshot) {
      const newsSummary = buildRecentNewsSummary(snapshot || {}, { seqLimit: 3, intelLimit: 3 });
      const items = [
        ...(newsSummary.globalNews || []).map(item => ({ title: '鍏ㄥ眬', meta: item.desc, note: '' })),
        ...(newsSummary.personalNews || []).map(item => ({ title: '涓汉', meta: item.desc, note: '' })),
      ].slice(0, 6);
      return {
        title: '瑙侀椈',
        body: `
          <div class="mvu-shell-lite-root" data-shell-light-view="news">
            <section class="mvu-shell-lite-card">
              <div class="mvu-shell-lite-section-title">杩戞湡</div>
              <div class="mvu-shell-lite-list">${buildShellLiteItemList(items, '鏆傛棤瑙侀椈')}</div>
            </section>
          </div>
        `,
      };
    }

    function buildShellBestiaryView(snapshot) {
      const entries = (snapshot.bestiaryEntries || []).slice(0, 6).map(([name, item]) => ({
        title: name,
        meta: toText(deepGet(item, '绫诲瀷', deepGet(item, 'type', '鏈煡')), '鏈煡'),
        note: toText(deepGet(item, '鎻忚堪', deepGet(item, 'desc', '')), ''),
      }));
      return {
        title: '鍥鹃壌',
        body: `
          <div class="mvu-shell-lite-root" data-shell-light-view="bestiary">
            <section class="mvu-shell-lite-card mvu-shell-lite-card--hero">
              <div class="mvu-shell-lite-head">
                <span>鍥鹃壌</span>
                <strong>${htmlEscape(`${(snapshot.bestiaryEntries || []).length || 0} 绉峘)}</strong>
              </div>
            </section>
            <section class="mvu-shell-lite-card">
              <div class="mvu-shell-lite-section-title">浠诲姟璁板綍</div>
              <div class="mvu-shell-lite-list">${buildShellLiteItemList(entries, '鏆傛棤璁板綍')}</div>
            </section>
          </div>
        `,
      };
    }

    function buildShellVaultView(snapshot) {
      const wealth = deepGet(snapshot, 'activeChar.璐㈠瘜', {});
      const currencies = [
        { key: 'fed_coin', label: '鑱旈偊甯?, value: toNumber(wealth.fed_coin, 0) },
        { key: 'star_coin', label: '鏄熺綏甯?, value: toNumber(wealth.star_coin, 0) },
        { key: 'tang_pt', label: '鍞愰棬绉垎', value: toNumber(wealth.tang_pt, 0) },
        { key: 'shrek_pt', label: '瀛﹂櫌绉垎', value: toNumber(wealth.shrek_pt, 0) },
        { key: 'blood_pt', label: '鎴樺姛', value: toNumber(wealth.blood_pt, 0) },
      ];
      const primaryCurrency = currencies.find(item => item.value > 0) || currencies[0];
      const secondaryCurrencies = currencies.filter(item => item !== primaryCurrency && item.value > 0);
      const inventoryEntries = Array.isArray(snapshot && snapshot.inventoryEntries) ? snapshot.inventoryEntries : [];
      const inventoryItems = inventoryEntries.slice(0, 6).map(([name, item]) => {
        const qty = toNumber(item && item['鏁伴噺'], 1);
        const type = toText(item && item['绫诲瀷'], '鐗╁搧');
        const desc = resolveShellText(item && item['鎻忚堪'], type) || type;
        return {
          title: `${name}${qty > 1 ? ` 脳${formatNumber(qty)}` : ''}`,
          meta: type,
          note: desc,
        };
      });
      const otherCurrencyText = secondaryCurrencies.map(item => `${item.label} ${formatNumber(item.value)}`);
      return {
        title: '浠撳簱',
        body: `
          <div class="mvu-shell-lite-root" data-shell-light-view="vault">
            <section class="mvu-shell-lite-card mvu-shell-lite-card--hero mvu-shell-lite-card--wallet">
              <div class="mvu-shell-lite-head">
                <span>${htmlEscape(primaryCurrency.label)}</span>
                <strong>${htmlEscape(formatNumber(primaryCurrency.value))}</strong>
              </div>
              ${buildShellLiteTags(otherCurrencyText)}
            </section>
            <section class="mvu-shell-lite-card">
              <div class="mvu-shell-lite-section-title">鐗╄祫</div>
              <div class="mvu-shell-lite-list">${buildShellLiteItemList(inventoryItems, '鏆傛棤鐗╁搧')}</div>
            </section>
          </div>
        `,
      };
    }

    function buildShellSocialView(snapshot) {
      const social = deepGet(snapshot, 'activeChar.绀句氦', {});
      const primaryFactionName = snapshot.primaryFaction ? snapshot.primaryFaction[0] : '';
      const primaryFactionRole = snapshot.primaryFaction ? toText(deepGet(snapshot.primaryFaction[1], '韬唤', ''), '') : '';
      const mainIdentity = summarizeShellIdentityText(social.涓昏韩浠? { limit: 18 }) || toText(social.涓昏韩浠? '鏃?);
      const fame = toText(social.鍚嶆湜绛夌骇, toText(social.鍚嶆湜绛夌骇, '绫嶇睄鏃犲悕'));
      const titleText = Array.isArray(snapshot.recentTitles) && snapshot.recentTitles.length ? snapshot.recentTitles[0] : '';
      const longPressAttrs = canOpenPrivateArchive(snapshot)
        ? ` data-longpress="${escapeHtmlAttr(PRIVATE_ARCHIVE_PREVIEW_KEY)}" data-longpress-delay="600"`
        : '';
      return {
        title: '绀句細',
        body: `
          <div class="mvu-shell-lite-root" data-shell-light-view="social">
            <section class="mvu-shell-lite-card mvu-shell-lite-card--hero nsfw-trigger-title"${longPressAttrs}>
              <div class="mvu-shell-lite-head">
                <span>${htmlEscape(fame)}</span>
                <strong>${htmlEscape(toText(snapshot.activeName, '褰撳墠瑙掕壊'))}</strong>
              </div>
              ${buildShellLiteStats([
                { label: '韬唤', value: shortenText(mainIdentity, 18) },
                { label: '鍚嶆湜', value: formatNumber(social.澹版湜) },
              ])}
              ${buildShellLiteTags([titleText, primaryFactionName ? `${primaryFactionName} / ${primaryFactionRole || '鎴愬憳'}` : '', snapshot.publicIntel ? '鍏紑鎯呮姤' : '鏈叕寮€'])}
            </section>
            <section class="mvu-shell-lite-card">
              <div class="mvu-shell-lite-section-title">鍏崇郴</div>
              <div class="mvu-shell-lite-list">${buildShellLiteItemList((snapshot.relations || []).slice(0, 5).map(([name, rel]) => ({
                title: name,
                meta: `${toText(deepGet(rel, '鍏崇郴', '闄岀敓'), '闄岀敓')} 路 ${toNumber(deepGet(rel, '濂芥劅搴?, 0), 0)}`,
                note: toText(deepGet(rel, '_鎺ㄨ繘鎻愮ず', ''), ''),
              })), '鏆傛棤鍏崇郴')}</div>
            </section>
          </div>
        `,
      };
    }

    function buildShellTimelineView(snapshot) {
      const entries = Array.isArray(snapshot && snapshot.timelineEntries) ? snapshot.timelineEntries : [];
      const latest = snapshot && snapshot.latestTimeline ? snapshot.latestTimeline : entries[0];
      const latestName = latest ? latest[0] : '';
      const latestData = latest ? latest[1] : {};
      const eventText = toText(deepGet(latestData, '浜嬩欢', latestName), latestName || '鏆傛棤缂栧勾');
      const nodes = entries.slice(0, 6).map(([name, item]) => ({
        title: name,
        meta: `Tick ${toText(item && item.瑙﹀彂tick, 0)} 路 ${toText(item && item.鐘舵€? 'pending')}`,
        note: toText(item && item.浜嬩欢, ''),
      }));
      return {
        title: '缂栧勾',
        body: `
          <div class="mvu-shell-lite-root" data-shell-light-view="timeline">
            <section class="mvu-shell-lite-card mvu-shell-lite-card--hero">
              <div class="mvu-shell-lite-head">
                <span>${htmlEscape(latest ? `Tick ${toText(deepGet(latestData, '瑙﹀彂tick', 0), 0)}` : 'Tick 0')}</span>
                <strong>${htmlEscape(shortenText(latestName || '鏆傛棤缂栧勾', 24))}</strong>
              </div>
              <div class="mvu-shell-lite-copy">${htmlEscape(shortenText(eventText, 60))}</div>
            </section>
            <section class="mvu-shell-lite-card">
              <div class="mvu-shell-lite-section-title">杩戞湡</div>
              <div class="mvu-shell-lite-list">${buildShellLiteItemList(nodes, '鏆傛棤缂栧勾')}</div>
            </section>
          </div>
        `,
      };
    }

    function buildShellOrgMatrixView(snapshot) {
      const entries = Array.isArray(snapshot && snapshot.orgEntries) ? snapshot.orgEntries : [];
      const primaryFactionEntry = getPrimaryFactionEntry(snapshot || {});
      const factionStats = getPrimaryFactionPowerStats(snapshot || {});
      const rows = entries.slice(0, 5).map(([name, item]) => ({
        title: name,
        meta: `${toText(item && item.鐘舵€? '姝ｅ父')} 路 ${formatNumber(item && item.瑙勬ā || 0)}`,
        note: '',
      }));
      return {
        title: '鍔垮姏',
        body: `
          <div class="mvu-shell-lite-root" data-shell-light-view="org">
            <section class="mvu-shell-lite-card mvu-shell-lite-card--hero">
              <div class="mvu-shell-lite-head">
                <span>${htmlEscape(toText(primaryFactionEntry.name, '鏈湴鍔垮姏'))}</span>
                <strong>${htmlEscape(`${entries.length || 0} 涓娍鍔沗)}</strong>
              </div>
              ${buildShellLiteStats([
                { label: '鏋侀檺', value: String(factionStats.limit || 0) },
                { label: '瓒呯骇', value: String(factionStats.super || 0) },
                { label: '灏佸彿', value: String(factionStats.title || 0) },
              ])}
            </section>
            <section class="mvu-shell-lite-card">
              <div class="mvu-shell-lite-section-title">姊樀</div>
              <div class="mvu-shell-lite-list">${buildShellLiteItemList(rows, '鏆傛棤鍔垮姏')}</div>
            </section>
          </div>
        `,
      };
    }

    function renderUnifiedSpiritCardsBySurface(snapshot, surface) {
      const normalizedSurface = normalizeUnifiedSurfaceKey(surface) || 'panel';
      const primary = snapshot.primarySpirit || null;
      const secondary = snapshot.secondaryTrack || null;
      const spiritBuilder = normalizedSurface === 'shell' ? buildShellSpiritSummaryCard : buildUnifiedSpiritCard;
      if (normalizedSurface === 'panel') {
        setUnifiedCardMarkup('primary-spirit', 鏋勫缓缁熶竴姝﹂瓊鍙岃建鍗?snapshot), {
          enabled: true,
          surface: normalizedSurface,
        });
        setUnifiedCardMarkup('secondary-spirit', '', {
          enabled: false,
          empty: true,
          surface: normalizedSurface,
        });
        return;
      }
      setUnifiedCardMarkup('primary-spirit', spiritBuilder(primary, { primary: true }), {
        preview: primary ? toText(primary.preview, '') : '',
        enabled: !!primary,
        surface: normalizedSurface,
      });
      if (secondary) {
        setUnifiedCardMarkup('secondary-spirit', spiritBuilder(secondary, { primary: false }), {
          preview: toText(secondary.preview, ''),
          enabled: true,
          surface: normalizedSurface,
        });
      } else {
        setUnifiedCardMarkup('secondary-spirit', spiritBuilder(null, { primary: false }), {
          preview: '',
          enabled: false,
          empty: false,
          surface: normalizedSurface,
        });
      }
    }

    function renderUnifiedCardsBySurface(snapshot, sectionSignatures, previousSectionSignatures, surface) {
      const normalizedSurface = normalizeUnifiedSurfaceKey(surface) || 'panel';
      const isShellSurface = normalizedSurface === 'shell';

      if (sectionSignatures.archive !== previousSectionSignatures.archive) {
        setUnifiedCardMarkup('archive-core', isShellSurface ? buildShellArchiveCoreCard(snapshot) : buildArchiveCoreCard(snapshot), {
          preview: '鐢熷懡鍥捐氨璇︾粏椤?,
          surface: normalizedSurface,
        });
        setUnifiedCardMarkup('armory', isShellSurface ? buildShellArmoryCard(snapshot) : buildArmoryCard(snapshot), {
          preview: '姝﹁宸ュ潑璇︾粏椤?,
          surface: normalizedSurface,
        });
        setUnifiedCardMarkup('vault', isShellSurface ? buildShellVaultCard(snapshot) : buildVaultCard(snapshot), {
          preview: '鍌ㄧ墿浠撳簱璇︾粏椤?,
          surface: normalizedSurface,
        });
        setUnifiedCardMarkup('social', isShellSurface ? buildShellSocialCard(snapshot) : buildUnifiedSocialCard(snapshot), {
          surface: normalizedSurface,
        });
        if (isShellSurface) {
          setUnifiedCardMarkup('home-archive', buildShellHomeArchiveCard(snapshot), { surface: normalizedSurface });
        }
        renderUnifiedSpiritCardsBySurface(snapshot, normalizedSurface);
      }

      if (sectionSignatures.map !== previousSectionSignatures.map) {
        if (isShellSurface) {
          setUnifiedMapStageMarkup('shell', '');
          setUnifiedCardMarkup('home-map', buildShellHomeMapCard(snapshot), { surface: normalizedSurface });
          setUnifiedCardMarkup('map-hero', '', { enabled: false, surface: normalizedSurface });
          setUnifiedCardMarkup('map-current', buildShellMapCurrentCard(snapshot), { preview: '褰撳墠鑺傜偣璇︽儏', surface: normalizedSurface });
          setUnifiedCardMarkup('map-locals', buildShellMapLocalCharactersCard(snapshot), { surface: normalizedSurface });
          setUnifiedCardMarkup('map-route', buildShellMapRouteCard(snapshot), { preview: '鍥惧眰鎺у埗涓庤窇鍥?, surface: normalizedSurface });
          setUnifiedCardMarkup('map-dynamic', buildShellMapDynamicCard(snapshot), { preview: '鍔ㄦ€佸湴鐐逛笌鎵╁睍鑺傜偣', surface: normalizedSurface });
        } else {
          ensureUnifiedSheepMapStage('panel');
          setUnifiedCardMarkup('map-hero', '', { enabled: false, surface: normalizedSurface });
          const 鏈湴浜虹墿鏉＄洰鍒楄〃 = getShellLocalCharacterEntries(snapshot, 99);
          const 鏈湴浜虹墿灞曠ず鍒楄〃 = 鏈湴浜虹墿鏉＄洰鍒楄〃;
          const 鏈湴浜虹墿浣嶇疆 = toText(deepGet(snapshot, 'activeChar.鐘舵€?浣嶇疆', snapshot.currentLoc), snapshot.currentLoc);
          setUnifiedCardMarkup('map-current', buildSimpleCard('褰撳墠浣嶇疆', { text: '褰撳墠' }, [
            { label: '鍦扮偣', value: snapshot.normalizedLoc !== snapshot.currentLoc ? `${snapshot.normalizedLoc} / ${snapshot.currentLoc}` : snapshot.currentLoc },
            { label: '鍦板浘', value: getMapDisplayName(snapshot) },
            { label: '鏈湴渚涚粰', value: toText(snapshot && snapshot.鏈湴渚涚粰, '鏃犱緵缁?) },
            { label: '浠锋牸甯?, value: toText(snapshot && snapshot.浠锋牸甯? '鏃?) },
            { label: '鏈€杩戞垚浜?, value: toText(snapshot && snapshot.鏈€杩戞垚浜ゅ奖鍝? '骞崇ǔ') },
          ]), { preview: '褰撳墠鑺傜偣璇︽儏', surface: normalizedSurface });
          setUnifiedCardMarkup('map-locals', `
            <div class="mvu-shell-roster-card">
              <div class="mvu-shell-roster-head">
                <div>
                  <span>鍦ㄥ満浜虹墿</span>
                  <strong>${htmlEscape(shortenText(鏈湴浜虹墿浣嶇疆, 26))}</strong>
                </div>
                <b>${htmlEscape(`${鏈湴浜虹墿鏉＄洰鍒楄〃.length} 鍚峘)}</b>
              </div>
              <div class="mvu-shell-roster-list">
                ${buildShellLocalRosterRows(鏈湴浜虹墿灞曠ず鍒楄〃, 99, { 鍚敤鎿嶄綔: true, 褰撳墠鍦扮偣: 鏈湴浜虹墿浣嶇疆 })}
              </div>
            </div>
          `, { surface: normalizedSurface });
        }
      }

      if (sectionSignatures.world !== previousSectionSignatures.world) {
        if (isShellSurface) {
          setUnifiedCardMarkup('home-world', buildShellHomeWorldCard(snapshot), { surface: normalizedSurface });
          setUnifiedCardMarkup('home-org', buildShellHomeOrgCard(snapshot), { surface: normalizedSurface });
          setUnifiedCardMarkup('world-hero', buildShellWorldHeroCard(snapshot), { preview: '涓栫晫鐘舵€佹€昏', surface: normalizedSurface });
          setUnifiedCardMarkup('world-timeline', buildShellWorldTimelineCard(snapshot), { preview: '缂栧勾鍙叉。妗?, surface: normalizedSurface });
          setUnifiedCardMarkup('world-alerts', buildShellWorldAlertCard(snapshot), { preview: '鎷嶅崠涓庤鎶?, surface: normalizedSurface });
          setUnifiedCardMarkup('org-hero', buildShellOrgHeroCard(snapshot), { preview: '鍔垮姏鐭╅樀鎬昏', surface: normalizedSurface });
          setUnifiedCardMarkup('org-faction', buildShellOrgFactionCard(snapshot), { preview: '鎴戠殑闃佃惀璇︽儏', surface: normalizedSurface });
          setUnifiedCardMarkup('org-node', buildShellOrgNodeCard(snapshot), { preview: '鏈湴鎹偣璇︽儏', surface: normalizedSurface });
        } else {
          setUnifiedCardMarkup('world-hero', buildWorldHeroCard(snapshot), { preview: '涓栫晫鐘舵€佹€昏', surface: normalizedSurface });
          const 涓栫晫瑙侀椈鎽樿 = buildRecentNewsSummary(snapshot, { seqLimit: 1, intelLimit: 1 });
          const 涓栫晫鏃堕棿绾挎暟閲?= (snapshot.timelineEntries || []).length || 0;
          const 涓栫晫鍟嗗簵鏁伴噺 = (snapshot.storeNames || []).length || 0;
          setUnifiedCardMarkup('world-timeline', buildSimpleCard('缂栧勾鍙叉。妗?, null, [
            { label: '鏈€杩戜簨浠?, value: snapshot.latestTimeline ? toText(deepGet(snapshot.latestTimeline[1], '浜嬩欢', snapshot.latestTimeline[0]), snapshot.latestTimeline[0]) : '鏆傛棤' },
            { label: '鐘舵€?, value: snapshot.latestTimeline ? `${toText(deepGet(snapshot.latestTimeline[1], '鐘舵€?, 'pending'), 'pending')} / Tick ${toText(deepGet(snapshot.latestTimeline[1], '瑙﹀彂tick', 0), '0')}` : '鏆傛棤鏃堕棿绾? },
            { label: '璁板綍鏁?, value: `${涓栫晫鏃堕棿绾挎暟閲弣 鏉 },
            { label: '杩戞湡瑙侀椈', value: shortenText(涓栫晫瑙侀椈鎽樿.summary, 34) },
          ]), { preview: '缂栧勾鍙叉。妗?, surface: normalizedSurface });
          setUnifiedCardMarkup('world-alerts', buildSimpleCard('鎷嶅崠涓庤鎶?, null, [
            { label: '鎷嶅崠琛?, value: `${toText(deepGet(snapshot, 'rootData.world.鎷嶅崠.鐘舵€?, '浼戝競'), '浼戝競')} / ${toText(deepGet(snapshot, 'rootData.world.鎷嶅崠.鍦扮偣', '鏃?), '鏃?)}` },
            { label: '鐢熸€佽鎶?, value: snapshot.worldAlert },
            { label: '鏈湴渚涚粰', value: toText(snapshot && snapshot.鏈湴渚涚粰, '鏃犱緵缁?) },
            { label: '浠锋牸甯?, value: toText(snapshot && snapshot.浠锋牸甯? '鏃?) },
            { label: '鏈€杩戞垚浜?, value: toText(snapshot && snapshot.鏈€杩戞垚浜ゅ奖鍝? '骞崇ǔ') },
            { label: '鏈湴鍟嗗簵', value: `${涓栫晫鍟嗗簵鏁伴噺} 澶刞 },
          ]), { preview: '鎷嶅崠涓庤鎶?, surface: normalizedSurface });
          setUnifiedCardMarkup('org-hero', buildOrgHeroCard(snapshot), { preview: '鍔垮姏鐭╅樀鎬昏', surface: normalizedSurface });
          const 褰撳墠闃佃惀鏉＄洰 = getPrimaryFactionEntry(snapshot);
          const 鏈湴浜虹墿鏁伴噺 = getShellLocalCharacterEntries(snapshot, 12).length;
          setUnifiedCardMarkup('org-faction', buildSimpleCard('鎴戠殑闃佃惀', null, [
            { label: '褰撳墠鎵€灞?, value: snapshot.鍔垮姏[0] ? snapshot.鍔垮姏[0][0] : '鏃? },
            { label: '韬唤', value: snapshot.鍔垮姏[0] ? toText(deepGet(snapshot.鍔垮姏[0][1], '韬唤', '鏃?), '鏃?) : '鏈姞鍏? },
            { label: '鏉冮檺绾?, value: snapshot.鍔垮姏[0] ? toText(deepGet(snapshot.鍔垮姏[0][1], '鏉冮檺绾?, 0), '0') : '0' },
            { label: '褰卞搷鍔?, value: formatNumber(deepGet(褰撳墠闃佃惀鏉＄洰, 'data.褰卞搷鍔?, 0)) },
            { label: '鍚嶆湜', value: `${toText(deepGet(snapshot, 'activeChar.绀句氦.鍚嶆湜绛夌骇', '鏃?), '鏃?)} / ${formatNumber(deepGet(snapshot, 'activeChar.绀句氦.澹版湜', 0))}` },
            { label: '鍏崇郴鐒︾偣', value: snapshot.topRelation ? `${shortenText(snapshot.topRelation[0], 8)} / ${toText(deepGet(snapshot.topRelation[1], '鍏崇郴', '闄岀敓'), '闄岀敓')}` : '鏆傛棤' },
          ]), { preview: '鎴戠殑闃佃惀璇︽儏', surface: normalizedSurface });
          setUnifiedCardMarkup('org-node', buildSimpleCard('鏈湴鎹偣', null, [
            { label: '鎺屾帶鍔垮姏', value: toText(deepGet(snapshot, 'locationData.鎺屾帶鍔垮姏', '鏈煡'), '鏈煡') },
            { label: '鎹偣鐘舵€?, value: `${toText(deepGet(snapshot, 'locationData.缁忔祹鐘跺喌', '鏈煡'), '鏈煡')} / ${toText(deepGet(snapshot, 'locationData.瀹堟姢鍐涘洟', '鏈煡'), '鏈煡')}` },
            { label: '搴楅摵', value: `${(snapshot.storeNames || []).length || 0} 澶刞 },
            { label: '鏈湴浜虹墿', value: `${鏈湴浜虹墿鏁伴噺} 鍚峘 },
            { label: '鍔ㄦ€佺偣', value: `${(snapshot.dynamicLocationNames || []).length || 0} 澶刞 },
            { label: '渚涢渶', value: `${toText(snapshot && snapshot.鏈湴渚涚粰, '鏃犱緵缁?)} / ${toText(snapshot && snapshot.浠锋牸甯? '鏃?)}` },
          ]), { preview: '鏈湴鎹偣璇︽儏', surface: normalizedSurface });
        }
      }

      if (sectionSignatures.terminal !== previousSectionSignatures.terminal) {
        if (isShellSurface) {
          setUnifiedCardMarkup('home-terminal', buildShellHomeTerminalCard(snapshot), { surface: normalizedSurface });
          setUnifiedCardMarkup('terminal-hero', buildShellTerminalHeroCard(snapshot), { preview: '绯荤粺鎾姤涓庢棩蹇?, surface: normalizedSurface });
          setUnifiedCardMarkup('terminal-plan', buildShellTerminalPlanCard(snapshot), { preview: '鎿嶄綔鎬荤嚎', surface: normalizedSurface });
          setUnifiedCardMarkup('terminal-intel', buildShellTerminalIntelCard(snapshot), { preview: '璇曠偧涓庢儏鎶?, surface: normalizedSurface });
          setUnifiedCardMarkup('terminal-news', buildShellTerminalNewsCard(snapshot), { preview: '杩戞湡瑙侀椈', surface: normalizedSurface });
          setUnifiedCardMarkup('terminal-bestiary', buildShellTerminalBestiaryCard(snapshot), { preview: '鎬墿鍥鹃壌', surface: normalizedSurface });
          setUnifiedCardMarkup('terminal-quest', buildShellTerminalQuestCard(snapshot), { preview: '浠诲姟鐣岄潰', surface: normalizedSurface });
        } else {
          setUnifiedCardMarkup('terminal-hero', buildTerminalHeroCard(snapshot), { preview: '绯荤粺鎾姤涓庢棩蹇?, surface: normalizedSurface });
          setUnifiedCardMarkup('terminal-plan', buildTerminalPlanCard(snapshot), { preview: '鎿嶄綔鎬荤嚎', surface: normalizedSurface });
          setUnifiedCardMarkup('terminal-intel', `
            <div class="simple-head"><div class="simple-title">璇曠偧涓庢儏鎶?/div></div>
            <div class="simple-list">
              <div class="simple-row"><b>璇曠偧鐘舵€?/b><span>${htmlEscape(getTrialEntranceText(snapshot))}</span></div>
              <div class="simple-row"><b>寰呮牳瀹?/b><span>${htmlEscape(`${toNumber(snapshot && snapshot.鎯呮姤寰呮牳瀹炴暟閲? 0)} 鏉)}</span></div>
              <div class="simple-row"><b>璇佹嵁鍑€鍊?/b><span>${htmlEscape(String(toNumber(snapshot && snapshot.鎯呮姤璇佹嵁鍑€鍊? 0)))}</span></div>
              <div class="simple-row"><b>鏈€杩戞牳瀹?/b><span>${htmlEscape(toText(snapshot && snapshot.鎯呮姤鏈€杩戞牳瀹炵粨鏋? '鏆傛棤鏍稿疄璁板綍'))}</span></div>
            </div>
          `, { preview: '璇曠偧涓庢儏鎶?, surface: normalizedSurface });
          const newsSummary = buildRecentNewsSummary(snapshot, { seqLimit: 1, intelLimit: 1 });
          setUnifiedCardMarkup('terminal-news', `
            <div class="simple-head"><div class="simple-title">杩戞湡瑙侀椈</div></div>
            <div class="simple-list">
              <div class="simple-row"><b>鍏ㄥ眬</b><span>${htmlEscape((newsSummary.globalNews[0] || {}).desc || '鏆傛棤')}</span></div>
              <div class="simple-row"><b>涓汉</b><span>${htmlEscape((newsSummary.personalNews[0] || {}).desc || '鏆傛棤')}</span></div>
              <div class="simple-row"><b>浜嬩欢</b><span>${htmlEscape(snapshot.latestTimeline ? snapshot.latestTimeline[0] : '鏆傛棤')}</span></div>
            </div>
          `, { preview: '杩戞湡瑙侀椈', surface: normalizedSurface });
          setUnifiedCardMarkup('terminal-bestiary', `
            <div class="simple-head"><div class="simple-title">鎬墿鍥鹃壌</div></div>
            <div class="simple-list">
              <div class="simple-row"><b>宸茶褰?/b><span>${htmlEscape(`${snapshot.bestiaryEntries.length} 绉峘)}</span></div>
              <div class="simple-row"><b>褰撳墠妗ｄ綅</b><span>${htmlEscape(`${toText(snapshot && snapshot.鍥鹃壌鑱氱劍鍚嶇О, '鏆傛棤')} / ${toText(snapshot && snapshot.鍥鹃壌褰撳墠妗ｄ綅, '鏃?)}`)}</span></div>
              <div class="simple-row"><b>涓嬩竴妗?/b><span>${htmlEscape(toText(snapshot && snapshot.鍥鹃壌涓嬩竴妗ｈ繘搴? '--'))}</span></div>
              <div class="simple-row"><b>鎴愰暱鍊惧悜</b><span>${htmlEscape(toText(snapshot && snapshot.鍥鹃壌鎴愰暱鍊惧悜, '鍧囪　'))}</span></div>
            </div>
          `, { preview: '鎬墿鍥鹃壌', surface: normalizedSurface });
          setUnifiedCardMarkup('terminal-quest', `
            <div class="simple-head"><div class="simple-title">浠诲姟鐣岄潰</div></div>
            <div class="simple-list">
              <div class="simple-row"><b>鎴戠殑浠诲姟</b><span>${htmlEscape(`${(snapshot.recordEntries || []).length} 椤逛换鍔)}</span></div>
              <div class="simple-row"><b>濮旀墭鏉?/b><span>${htmlEscape(`${safeEntries(deepGet(snapshot, 'rootData.world.濮旀墭鏉?, {})).length} 鏉″鎵榒)}</span></div>
              <div class="simple-row"><b>褰撳墠璺嚎</b><span>${htmlEscape(toText(snapshot && snapshot.褰撳墠浠诲姟璺嚎, '鏃?))}</span></div>
              <div class="simple-row"><b>椋庨櫓绾у埆</b><span>${htmlEscape(toText(snapshot && snapshot.褰撳墠浠诲姟椋庨櫓绾у埆, '鏃?))}</span></div>
              <div class="simple-row"><b>涓嬩竴鑺傜偣</b><span>${htmlEscape(toText(snapshot && snapshot.褰撳墠浠诲姟涓嬩竴鑺傜偣杩涘害, '--'))}</span></div>
            </div>
          `, { preview: '浠诲姟鐣岄潰', surface: normalizedSurface });
        }
      }
      鍚屾AI缁存姢棣栭〉閰嶇疆鍏ュ彛();
    }

    function ensureUnifiedSheepMapStage(stage = 'panel') {
      const stageKey = toText(stage, '').trim();
      if (!stageKey) return;
      const selector = `#mvu-unified-mount [data-mvu-map-stage="${stageKey}"]`;
      getLiveUiElements(selector).forEach(node => {
        if (!(node instanceof Element)) return;
        const hasSheepMap = !!node.querySelector('.map-layout .map-canvas.interactive-map [data-map-node-layer]');
        if (!hasSheepMap) setLiveNodeHtml(node, '');
      });
      if (typeof window.__sheepMapResync === 'function') {
        window.setTimeout(() => {
          try {
            window.__sheepMapResync({ center: false, syncVisual: false });
          } catch (err) {}
          if (typeof scheduleUnifiedMapCanvasClamp === 'function') scheduleUnifiedMapCanvasClamp();
        }, 0);
      }
    }

    function setUnifiedCardMarkup(slot, html, options = {}) {
      const surface = normalizeUnifiedSurfaceKey(options.surface);
      const selector = surface
        ? `#mvu-unified-mount [data-unified-card="${slot}"][data-unified-surface="${surface}"]`
        : `#mvu-unified-mount [data-unified-card="${slot}"]`;
      const preview = toText(options.preview, '');
      const hasMarkup = !!toText(html, '').trim();
      const enabled = options.enabled !== false && hasMarkup;
      const 鏄惧紡绌哄崱 = !!options.empty;
      getLiveUiElements(selector).forEach(node => {
        setLiveNodeHtml(node, html);
        if (preview && enabled) node.setAttribute('data-preview', preview);
        else node.removeAttribute('data-preview');
        node.classList.toggle('clickable', !!(preview && enabled));
        node.classList.toggle('is-empty', 鏄惧紡绌哄崱 || !enabled);
      });
    }

    function setUnifiedMapStageMarkup(stage, html) {
      const stageKey = toText(stage, '').trim();
      if (!stageKey) return;
      const selector = `#mvu-unified-mount [data-mvu-map-stage="${stageKey}"]`;
      getLiveUiElements(selector).forEach(node => setLiveNodeHtml(node, html));
    }

    function 璁剧疆缁熶竴椤堕儴鐘舵€佹潯(snapshot) {
      getLiveUiElements('#mvu-unified-mount [data-unified-top-status="panel"]').forEach(node => {
        node.removeAttribute('data-preview');
        node.classList.remove('clickable');
        setLiveNodeHtml(node, 鏋勫缓缁熶竴椤堕儴鐘舵€佹潯(snapshot));
      });
    }

    function 璁剧疆缁熶竴瑙掕壊鍒囨崲鍏ュ彛(snapshot) {
      const 瑙掕壊鍚?= snapshot
        ? toText(deepGet(snapshot, 'activeChar.name', deepGet(snapshot, 'activeChar.base.name', snapshot.activeName)), snapshot.activeName || '瑙掕壊')
        : '瑙掕壊';
      getLiveUiElements('#mvu-unified-mount [data-unified-role-switch="panel"]').forEach(node => {
        setLiveNodeHtml(node, `瑙掕壊锝?{htmlEscape(shortenText(瑙掕壊鍚? 10))}`);
        node.setAttribute('title', '鍒囨崲褰撳墠鏌ョ湅瑙掕壊');
      });
    }

    function renderUnifiedCards(snapshot, precomputedSectionSignatures = null, previousSectionSignaturesOverride = null) {
      const sectionSignatures = precomputedSectionSignatures || buildDashboardSectionRenderSignatures(snapshot);
      const previousSectionSignatures = previousSectionSignaturesOverride || lastDashboardSectionRenderSignatures || Object.create(null);
      璁剧疆缁熶竴椤堕儴鐘舵€佹潯(snapshot);
      璁剧疆缁熶竴瑙掕壊鍒囨崲鍏ュ彛(snapshot);
      renderUnifiedCardsBySurface(snapshot, sectionSignatures, previousSectionSignatures, 'panel');
      renderUnifiedCardsBySurface(snapshot, sectionSignatures, previousSectionSignatures, 'shell');
    }

    function getFusionArchiveMeta(snapshot) {
      const fusionEntries = safeEntries(deepGet(snapshot, 'activeChar.姝﹂瓊铻嶅悎鎶€', {}));
      const partnerCount = fusionEntries.filter(([, fusion]) => toText(deepGet(fusion, '铻嶅悎妯″紡', 'partner'), 'partner') !== 'self').length;
      const selfCount = fusionEntries.length - partnerCount;
      const leadFusion = fusionEntries[0] && fusionEntries[0][1] && typeof fusionEntries[0][1] === 'object' ? fusionEntries[0][1] : {};
      const leadParticipants = normalizeSkillDesignerFusionParticipantList(getSkillDesignerRawFusionParticipants({}, leadFusion), snapshot && snapshot.rootData ? snapshot.rootData : {});
      const leadPartnerNames = leadParticipants
        .filter(participant => participant.role !== 'self')
        .map(participant => participant.charName || participant.charKey)
        .filter(Boolean);
      const headlineFusion = fusionEntries[0]
        ? toText(deepGet(fusionEntries[0][1], '鎶€鑳芥暟鎹?榄傛妧鍚?, deepGet(fusionEntries[0][1], '鎶€鑳芥暟鎹?name', fusionEntries[0][0])), fusionEntries[0][0])
        : '鏈綍鍏?;
      const partnerLabel = fusionEntries[0]
        ? (toText(deepGet(leadFusion, '铻嶅悎妯″紡', 'partner'), 'partner') === 'self'
          ? '鑷綋铻嶅悎'
          : `${leadParticipants.length > 2 ? '澶氫汉' : '鎼。'} ${leadPartnerNames.length ? Array.from(new Set(leadPartnerNames)).join('銆?) : toText(deepGet(leadFusion, '铻嶅悎瀵硅薄', '鏈煡'), '鏈煡')}`)
        : '绛夊緟鏀跺綍';
      return {
        fusionEntries,
        partnerCount,
        selfCount,
        headlineFusion,
        partnerLabel,
      };
    }

    function buildFusionArchiveListItem(fusionMeta) {
      const meta = fusionMeta && typeof fusionMeta === 'object' ? fusionMeta : getFusionArchiveMeta({});
      if (!meta.fusionEntries.length) {
        return {
          title: '姝﹂瓊铻嶅悎鎶€',
          desc: '<small>鏈敹褰?/small>',
          preview: '姝﹂瓊铻嶅悎鎶€璇︾粏椤?
        };
      }
      return {
        title: '姝﹂瓊铻嶅悎鎶€',
        desc: `<strong>${htmlEscape(meta.headlineFusion)}</strong><small>宸叉敹褰?${htmlEscape(String(meta.fusionEntries.length))} 椤?锝?澶栭儴 ${htmlEscape(String(meta.partnerCount))} / 鑷綋 ${htmlEscape(String(meta.selfCount))}</small><small>${htmlEscape(meta.partnerLabel)}</small>`,
        preview: '姝﹂瓊铻嶅悎鎶€璇︾粏椤?
      };
    }

    function renderSpiritStrips(snapshot) {
      const primary = snapshot.primarySpirit;
      const secondary = snapshot.secondaryTrack;
      getLiveUiElements('.dual-spirit-strip').forEach(strip => {
        const isSecondaryOnly = strip.classList.contains('secondary-track') || strip.classList.contains('split-secondary-left');
        const isSinglePrimary = strip.classList.contains('single-track') && !strip.classList.contains('split-secondary-left');

        if (isSecondaryOnly) {
          if (!secondary) {
            if (strip.style.display !== 'none') strip.style.display = 'none';
            return;
          }
          if (strip.style.display !== '') strip.style.display = '';
          setLiveNodeHtml(strip, `<div class="dual-spirit-body"><div class="spirit-side primary-side"></div><div class="spirit-side secondary-side clickable" data-preview="${htmlEscape(secondary.preview)}">${secondary.kind === 'bloodline' ? renderArchiveBloodlineEntry(secondary) : renderArchiveSpiritEntry(secondary, false)}</div></div>`);
          return;
        }

        if (isSinglePrimary) {
          if (strip.style.display !== '') strip.style.display = '';
          setLiveNodeHtml(strip, `<div class="dual-spirit-body"><div class="spirit-side primary-side clickable" data-preview="${htmlEscape(primary.preview)}">${renderArchiveSpiritEntry(primary, true)}</div><div class="spirit-side secondary-side clickable"></div></div>`);
          return;
        }

        if (!secondary) {
          if (strip.style.display !== 'none') strip.style.display = 'none';
          return;
        }

        if (strip.style.display !== '') strip.style.display = '';
        setLiveNodeHtml(strip, `
          <div class="dual-spirit-body">
            <div class="spirit-side primary-side clickable" data-preview="${htmlEscape(primary.preview)}">${renderArchiveSpiritEntry(primary, true)}</div>
            <div class="spirit-side secondary-side clickable" data-preview="${htmlEscape(secondary.preview)}">${secondary.kind === 'bloodline' ? renderArchiveBloodlineEntry(secondary) : renderArchiveSpiritEntry(secondary, false)}</div>
          </div>
        `);
      });
    }


    function getSheepMapSnapshot(snapshot, mapId = null) {
      const sheepSnapshot = window.__sheepMapSnapshot;
      if (!sheepSnapshot || typeof sheepSnapshot !== 'object') return null;
      const expectedMapId = toText(mapId || (snapshot && snapshot.mapCurrentMapId), '');
      const sheepMapId = toText(sheepSnapshot.currentMapId, '');
      if (expectedMapId && sheepMapId && expectedMapId !== sheepMapId) return null;
      return sheepSnapshot;
    }

    function getMapMeta(snapshot, mapId = null) {
      // Map metadata is sourced from sheep_map_restore.js snapshots.
      const sheepSnapshot = getSheepMapSnapshot(snapshot, mapId);
      const mapMeta = sheepSnapshot && sheepSnapshot.mapMeta && typeof sheepSnapshot.mapMeta === 'object'
        ? sheepSnapshot.mapMeta
        : null;
      return mapMeta || {};
    }

    function getMapDisplayName(snapshot, mapId = null) {
      const safeMapId = toText(mapId || (snapshot && snapshot.mapCurrentMapId), 'map_douluo_world');
      const mapMeta = getMapMeta(snapshot, safeMapId);
      const sheepMapName = toText(mapMeta.name, '').trim();
      if (sheepMapName) return sheepMapName;
      if (safeMapId === 'map_douluo_world') return '鏂楃綏澶ч檰鎬诲浘';
      if (/^map_debug_/i.test(safeMapId)) return '鍖哄煙瀛愬浘';
      if (/^map_/i.test(safeMapId)) return '鏈懡鍚嶅瓙鍥?;
      return '鏈懡鍚嶅湴鍥?;
    }

    function resolveDisplayMapNode(snapshot, nodeName) {
      const node = snapshot.mapVisibleNodeEntries.find(([name]) => name === nodeName);
      if (node) {
        const data = node[1] && typeof node[1] === 'object' ? node[1] : {};
        const childMapValue = data.child_map_id || data['child_map_id'] || data['瀛愬浘ID'] || (snapshot.mapAvailableChildMaps && snapshot.mapAvailableChildMaps[nodeName]);
        return {
          name: nodeName,
          source: toText(data.source, 'static'),
          type: toText(data.type || data['绫诲瀷'], '鍦板浘鑺傜偣'),
          state: data.level ? `Lv.${data.level}` : toText(data.state || data['鐘舵€?], '鍙'),
          childMapId: toText(childMapValue, '鏃?),
          x: data.x,
          y: data.y,
          desc: toText(data.desc || data['鎻忚堪'], '鏃?),
          faction: toText(data.faction || data['鎺屾帶鍔垮姏'], '鏈煡')
        };
      }
      const dynamicNode = snapshot.mapVisibleDynamicEntries.find(([name]) => name === nodeName);
      if (dynamicNode) {
        const data = dynamicNode[1] && typeof dynamicNode[1] === 'object' ? dynamicNode[1] : {};
        const childMapValue = data.child_map_id || data['child_map_id'] || data['瀛愬浘ID'] || (snapshot.mapAvailableChildMaps && snapshot.mapAvailableChildMaps[nodeName]);
        return {
          name: nodeName,
          source: 'dynamic',
          type: toText(data.type || data['绫诲瀷'], '鍔ㄦ€佸湴鐐?),
          state: toText(data.state || data['鐘舵€?], '鍔ㄦ€?),
          childMapId: toText(childMapValue, '鏃?),
          x: data.x,
          y: data.y,
          desc: toText(data.desc || data['鎻忚堪'], '鏃?),
          faction: toText(data.faction || data['鎺屾帶鍔垮姏'], '鏈煡')
        };
      }
      return null;
    }

    function isMapOverviewPreviewKey(previewKey) {
      const key = toText(previewKey, '');
      if (!key) return false;
      if (key === '\u5168\u606f\u661f\u56fe\u4e3b\u753b\u5e03') return true;
      return key.includes('\u661f\u56fe') && key.includes('\u4e3b\u753b\u5e03');
    }

    function buildSimpleCard(title, badge, rows) {
      return `
        <div class="simple-head">
          <div class="simple-title">${htmlEscape(title)}</div>
          ${badge ? `<span class="map-side-badge ${badge.className || ''}">${htmlEscape(badge.text)}</span>` : ''}
        </div>
        <div class="simple-list">
          ${rows.map(row => `<div class="simple-row"><b>${htmlEscape(row.label)}</b><span>${htmlEscape(row.value)}</span></div>`).join('')}
        </div>
      `;
    }

    function buildWorldHeroCard(snapshot) {
      const worldTime = toText(deepGet(snapshot, 'rootData.world.鏃堕棿._calendar', deepGet(snapshot, 'rootData.world.鏃堕棿.calendar', '鏂楃綏鍘嗘湭鍚屾')), '鏂楃綏鍘嗘湭鍚屾');
      const deviation = toNumber(deepGet(snapshot, 'rootData.world.鍋忓樊鍊?, 0), 0);
      const forestRatio = Math.max(0, Math.min(100, Number(((toNumber(snapshot.forestKilledAge, 0) / 1000000) * 100).toFixed(1))));
      const forestStage = forestRatio >= 100 ? '鍏芥疆涓寸晫' : (forestRatio >= 70 ? '楂樺害绱у紶' : (forestRatio >= 30 ? '鎸佺画鍗囨俯' : '鐩稿瀹夊叏'));
      const latest = snapshot.latestTimeline;
      const stageLabel = latest
        ? toText(deepGet(latest[1], '浜嬩欢', latest[0]), latest[0])
        : snapshot.currentLoc;
      const newsSummary = buildRecentNewsSummary(snapshot, { seqLimit: 1, intelLimit: 1 });
      const latestEventText = latest
        ? toText(deepGet(latest[1], '浜嬩欢', latest[0]), latest[0])
        : '鏆傛棤缂栧勾浜嬩欢';

      return `
        <div class="module-name">鏃剁┖涓灑</div>
        <div class="world-core-grid">
          <div class="world-core-tile world-core-tile--wide"><b>褰撳墠鏃堕棿</b><strong>${htmlEscape(worldTime)}</strong></div>
          <div class="world-core-tile"><b>涓栫晫鍋忓樊</b><strong>${htmlEscape(`${deviation} / ${deviation >= 40 ? '楂樺嵄' : deviation >= 10 ? '娉㈠姩' : '骞崇ǔ'}`)}</strong></div>
          <div class="world-core-tile"><b>褰撳墠闃舵</b><strong>${htmlEscape(stageLabel)}</strong></div>
          <div class="world-core-tile world-core-tile--wide"><b>褰撳墠浜嬩欢</b><strong>${htmlEscape(latestEventText)}</strong></div>
          <div class="world-core-tile world-core-tile--wide"><b>杩戞湡瑙侀椈</b><strong>${htmlEscape(newsSummary.summary)}</strong></div>
          <div class="world-core-tile world-core-tile--progress">
            <b>妫灄浠囨仺鍊?/b>
            <strong>${htmlEscape(`${forestStage} / ${formatNumber(snapshot.forestKilledAge)} / 1000000`)}</strong>
            <i><span style="width:${forestRatio}%;"></span></i>
          </div>
        </div>
        <div class="module-foot world-core-foot">
          <span class="foot-hint">椋庨櫓鎽樿锛?{htmlEscape(snapshot.worldAlert)}</span>
        </div>
      `;
    }

    function buildOrgHeroCard(snapshot) {
      const topOrgs = snapshot.orgEntries.slice(0, 4);
      const primaryFactionEntry = getPrimaryFactionEntry(snapshot);
      const relationSummary = buildFactionRelationSummary(primaryFactionEntry && primaryFactionEntry.data ? primaryFactionEntry.data : {}, 3) || '鏆傛棤';
      return `
        <div class="module-name">鍔垮姏鐭╅樀</div>
        <div class="matrix-board org-matrix-board">
          ${topOrgs.map(([name, data], index) => `<div class="matrix-node ${snapshot.鍔垮姏.some(([fac]) => fac === name) || index === 0 ? 'gold' : ''}"><b>${htmlEscape(name)}</b><span>${htmlEscape(`褰卞搷鍔?${formatNumber(data && data.褰卞搷鍔?} / ${toText(data && data.鐘舵€? '姝ｅ父')}`)}</span></div>`).join('')}
        </div>
        ${(() => { const factionStats = getPrimaryFactionPowerStats(snapshot); return `
        <div class="org-power-grid">
          <div><b>鏋侀檺鏂楃綏</b><strong>${htmlEscape(String(factionStats.limit))}</strong></div>
          <div><b>瓒呯骇鏂楃綏</b><strong>${htmlEscape(String(factionStats.super))}</strong></div>
          <div><b>灏佸彿鏂楃綏</b><strong>${htmlEscape(String(factionStats.title))}</strong></div>
          <div class="org-power-grid__wide"><b>鍔垮姏鍏崇郴</b><strong>${htmlEscape(relationSummary)}</strong></div>
        </div>
        `; })()}
        <div class="module-foot org-core-foot">
          <span class="foot-hint">鏈湴楂樹寒锛?{htmlEscape(toText(deepGet(snapshot, 'locationData.鎺屾帶鍔垮姏', snapshot.鍔垮姏[0] ? snapshot.鍔垮姏[0][0] : '鏈煡'), '鏈煡'))}</span>
          <span class="enter-chip gold-chip">${htmlEscape(`${snapshot.orgEntries.length || 0} 涓娍鍔沗)}</span>
        </div>
      `;
    }

    function buildTerminalHeroCard(snapshot) {
      const sys = deepGet(snapshot, 'rootData.sys', {});
      const recentNews = buildRecentNewsSummary(snapshot, { seqLimit: 2, intelLimit: 1 });
      const latestBroadcast = toText(sys.绯荤粺鎾姤, '鏆傛棤鎾姤');
      const latestEvent = recentNews.cards[0] ? recentNews.cards[0].desc : '鏆傛棤浜嬩欢';
      const 鏍稿績鐘舵€佹憳瑕?= shortenText(`${snapshot.worldAlert.includes('楂樺嵄') ? '楂樺帇鍚屾' : '绋冲畾鍚屾'} 路 ${toText(snapshot.currentLoc, '鏈煡鍦扮偣')}`, 44);
      const 鏈€杩戞挱鎶ユ憳瑕?= shortenText(latestBroadcast, 58);
      const 鏈€杩戜簨浠舵憳瑕?= shortenText(latestEvent, 58);
      const 鎯呮姤浠诲姟鎽樿 = shortenText(`寰呮牳瀹?${toNumber(snapshot && snapshot.鎯呮姤寰呮牳瀹炴暟閲? 0)} 路 浠诲姟 ${(snapshot.recordEntries || []).length} 路 鍥鹃壌 ${(snapshot.bestiaryEntries || []).length}`, 58);
      const 缁堢琛屽€欓€?= [
        { 鏍囩: '[鐘舵€乚', 鍐呭: 鏍稿績鐘舵€佹憳瑕? 棰勮: '涓栫晫鐘舵€佹€昏', 鏍峰紡: 'sys' },
        { 鏍囩: '[鎾姤]', 鍐呭: 鏈€杩戞挱鎶ユ憳瑕? 棰勮: '绯荤粺鎾姤涓庢棩蹇?, 鏍峰紡: 'roll' },
        { 鏍囩: '[浜嬩欢]', 鍐呭: 鏈€杩戜簨浠舵憳瑕? 棰勮: '杩戞湡瑙侀椈', 鏍峰紡: 'sys' },
        { 鏍囩: '[鎬昏]', 鍐呭: 鎯呮姤浠诲姟鎽樿, 棰勮: '璇曠偧涓庢儏鎶?, 鏍峰紡: 'sys' },
      ];
      const 缁堢琛屽幓閲?= new Set();
      const 缁堢琛屽垪琛?= 缁堢琛屽€欓€?filter(鏉＄洰 => {
        const 鍐呭閿?= toText(鏉＄洰 && 鏉＄洰.鍐呭, '').replace(/\s+/g, ' ').trim();
        if (!鍐呭閿? return false;
        if (缁堢琛屽幓閲?has(鍐呭閿?) return false;
        缁堢琛屽幓閲?add(鍐呭閿?;
        return true;
      });
      return `
        <div class="module-name">绯荤粺鎾姤</div>
        <div class="terminal-home-log">
          ${缁堢琛屽垪琛?map(鏉＄洰 => `<button type="button" class="terminal-home-line ${htmlEscape(鏉＄洰.鏍峰紡)} clickable" data-preview="${escapeHtmlAttr(鏉＄洰.棰勮)}"><b>${htmlEscape(鏉＄洰.鏍囩)}</b><span>${htmlEscape(鏉＄洰.鍐呭)}</span></button>`).join('')}
        </div>
        <div class="terminal-home-metrics">
          <span><b>寰呮牳瀹?/b><strong>${htmlEscape(String(toNumber(snapshot && snapshot.鎯呮姤寰呮牳瀹炴暟閲? 0)))}</strong></span>
          <span><b>浠诲姟</b><strong>${htmlEscape(String((snapshot.recordEntries || []).length))}</strong></span>
          <span><b>瑙侀椈</b><strong>${htmlEscape(String((recentNews.cards || []).length))}</strong></span>
          <span><b>鍥鹃壌</b><strong>${htmlEscape(String((snapshot.bestiaryEntries || []).length))}</strong></span>
        </div>
      `;
    }

    function buildTerminalPlanCard(snapshot) {
      const planSummary = buildRecentPlanSummary(snapshot, { worldLimit: 2, recordLimit: 2 });
      const worldText = planSummary.worldPlans[0] ? planSummary.worldPlans[0].desc : '鏆傛棤涓栫晫瀹夋帓';
      const personalText = planSummary.personalPlans[0] ? planSummary.personalPlans[0].desc : '鏆傛棤涓汉寰呭姙';
      return `
        <div class="simple-head"><div class="simple-title">杩戞湡瀹夋帓</div></div>
        <div class="terminal-plan-grid">
          <div class="terminal-plan-item terminal-plan-item--wide"><b>涓栫晫</b><span>${htmlEscape(shortenText(worldText, 72))}</span></div>
          <div class="terminal-plan-item terminal-plan-item--wide"><b>涓汉</b><span>${htmlEscape(shortenText(personalText, 72))}</span></div>
          <div class="terminal-plan-item"><b>涓栫晫寰呭姙</b><span>${htmlEscape(String((planSummary.worldPlans || []).length))}</span></div>
          <div class="terminal-plan-item"><b>涓汉寰呭姙</b><span>${htmlEscape(String((planSummary.personalPlans || []).length))}</span></div>
        </div>
      `;
    }

    function 鏋勫缓缁熶竴椤堕儴鐘舵€佹潯(snapshot) {
      if (!snapshot) {
        return `
          <div class="mvu-unified-top-name">褰撳墠鑱婂ぉ</div>
          <div class="mvu-unified-top-chip-row">
            <span class="is-time">鏃堕棿鏈悓姝?/span>
            <span class="is-place">鍦扮偣鏈悓姝?/span>
          </div>
          <div class="mvu-ai-maintenance-home-api mvu-ai-maintenance-home-api--top" data-ai-maintenance-home-api>${鏋勫缓AI缁存姢API閫夋嫨鍣?)}</div>
        `;
      }
      const 瑙掕壊鍚?= toText(
        deepGet(snapshot, 'activeChar.name', deepGet(snapshot, 'activeChar.base.name', snapshot.activeName)),
        snapshot.activeName || '褰撳墠瑙掕壊',
      );
      const 涓栫晫鏃堕棿 = getSnapshotWorldTimeText(snapshot);
      const 褰撳墠浣嶇疆 = toText(deepGet(snapshot, 'activeChar.鐘舵€?浣嶇疆', snapshot.currentLoc), snapshot.currentLoc || '鏈煡鍦扮偣')
        .replace(/^鏂楃綏澶ч檰-/, '')
        .replace(/^鏂楃伒澶ч檰-/, '');
      return `
        <button type="button" class="mvu-unified-top-name mvu-unified-top-name-button clickable" data-preview="瑙掕壊鍒囨崲鍣? title="鍒囨崲褰撳墠鏌ョ湅瑙掕壊">${htmlEscape(shortenText(瑙掕壊鍚? 16))}</button>
        <div class="mvu-unified-top-chip-row">
          <span class="is-time" title="${escapeHtmlAttr(涓栫晫鏃堕棿)}">${htmlEscape(涓栫晫鏃堕棿)}</span>
          <span class="is-place" title="${escapeHtmlAttr(褰撳墠浣嶇疆)}">${htmlEscape(褰撳墠浣嶇疆)}</span>
        </div>
        <div class="mvu-ai-maintenance-home-api mvu-ai-maintenance-home-api--top" data-ai-maintenance-home-api>${鏋勫缓AI缁存姢API閫夋嫨鍣?)}</div>
      `;
    }

    function renderEmptyUnifiedShellCards() {
      璁剧疆缁熶竴椤堕儴鐘舵€佹潯(null);
      璁剧疆缁熶竴瑙掕壊鍒囨崲鍏ュ彛(null);
      const emptyShellCards = {
        'archive-core': ['瑙掕壊', '鏃犳暟鎹?],
        'primary-spirit': ['涓绘榄?, '鏃犳暟鎹?],
        'armory': ['姝﹁', '0'],
        'vault': ['浠撳簱', '0'],
        'social': ['绀句氦', '0'],
        'map-current': ['褰撳墠浣嶇疆', '鏃犳暟鎹?],
        'map-locals': ['鏈湴瑙掕壊', '0'],
        'map-route': ['璺嚎', '0'],
        'map-dynamic': ['鍔ㄦ€?, '0'],
        'world-hero': ['涓栫晫', '鏃犳暟鎹?],
        'world-timeline': ['鏃堕棿绾?, '0'],
        'world-alerts': ['璀︽姤', '0'],
        'org-hero': ['鍔垮姏', '0'],
        'org-faction': ['闃佃惀', '鏃?],
        'org-node': ['鎹偣', '鏃犳暟鎹?],
        'terminal-hero': ['缁堢', '鏃犳暟鎹?],
        'terminal-plan': ['瀹夋帓', '0'],
        'terminal-intel': ['鎯呮姤', '0'],
        'terminal-news': ['瑙侀椈', '0'],
        'terminal-bestiary': ['鍥鹃壌', '0'],
        'terminal-quest': ['浠诲姟', '0'],
      };
      Object.entries(emptyShellCards).forEach(([slot, [title, value]]) => {
        setUnifiedCardMarkup(slot, buildShellEmptyCard(title, value), { surface: 'shell', enabled: true });
      });
      setUnifiedCardMarkup('secondary-spirit', '', { surface: 'shell', enabled: false });
      setUnifiedMapStageMarkup('shell', '');
    }

    function rerenderUnifiedCardsFromLive(options = {}) {
      const snapshot = liveSnapshot || lastRenderableSnapshot;
      if (!snapshot) {
        renderEmptyUnifiedShellCards();
        return false;
      }
      const force = !!(options && options.force);
      const sectionSignatures = buildDashboardSectionRenderSignatures(snapshot);
      renderUnifiedCards(
        snapshot,
        sectionSignatures,
        force ? Object.create(null) : (lastDashboardSectionRenderSignatures || Object.create(null)),
      );
      return true;
    }

    window.__MVU_RERENDER_UNIFIED_CARDS__ = rerenderUnifiedCardsFromLive;

    function clampUnifiedMapCanvas(root = document) {
      if (!document.body || !document.body.classList.contains('mvu-layout-unified')) return;
      const scope = root && typeof root.querySelectorAll === 'function' ? root : document;
      const canvases = scope.querySelectorAll('#mvu-unified-mount .mvu-unified-map-stage .map-canvas.map-canvas-large, #mvu-unified-mount .mvu-unified-detail-host .map-canvas.map-canvas-large');
      const viewportHeight = Math.max(520, Number(window.innerHeight) || Number(document.documentElement.clientHeight) || 720);
      canvases.forEach(canvas => {
        if (!(canvas instanceof HTMLElement)) return;
        if (canvas.closest('.mvu-mobile-shell, .mvu-mobile-map-stage, .mvu-shell-map-stage')) return;
        const isDetail = !!canvas.closest('.mvu-unified-detail-host');
        const 鏄瑙堟槦鍥?= !isDetail && !!canvas.closest('.mvu-unified-map-stage');
        if (鏄瑙堟槦鍥? {
          canvas.style.setProperty('height', '100%', 'important');
          canvas.style.setProperty('width', '100%', 'important');
          canvas.style.setProperty('min-height', '0px', 'important');
          canvas.style.setProperty('max-height', 'none', 'important');
          canvas.style.setProperty('aspect-ratio', 'auto', 'important');
          canvas.style.setProperty('flex-basis', 'auto', 'important');
          return;
        }
        const 鎵€灞炶垶鍙?= canvas.closest('.mvu-unified-map-stage, .mvu-unified-detail-host');
        const 鑸炲彴楂樺害 = 鎵€灞炶垶鍙?instanceof HTMLElement ? Math.round(鎵€灞炶垶鍙?getBoundingClientRect().height || 鎵€灞炶垶鍙?clientHeight || 0) : 0;
        const 瑙嗗彛鐩爣 = isDetail
          ? Math.round(viewportHeight * 0.56)
          : Math.round(viewportHeight * 0.70);
        const 瀹瑰櫒鐩爣 = 鑸炲彴楂樺害 > 120 ? Math.max(220, 鑸炲彴楂樺害 - (isDetail ? 18 : 20)) : 0;
        const targetHeight = Math.max(
          isDetail ? 360 : 420,
          Math.min(isDetail ? 620 : 680, 瀹瑰櫒鐩爣 || 瑙嗗彛鐩爣),
        );
        canvas.style.setProperty('height', `${targetHeight}px`, 'important');
        canvas.style.setProperty('width', '100%', 'important');
        canvas.style.setProperty('min-height', '0px', 'important');
        canvas.style.setProperty('max-height', `${targetHeight}px`, 'important');
        canvas.style.setProperty('aspect-ratio', 'auto', 'important');
        canvas.style.setProperty('flex-basis', 'auto', 'important');
      });
    }

    function scheduleUnifiedMapCanvasClamp(root = document) {
      const run = () => clampUnifiedMapCanvas(root);
      if (typeof window.requestAnimationFrame === 'function') {
        window.requestAnimationFrame(() => window.requestAnimationFrame(run));
      } else {
        window.setTimeout(run, 0);
      }
      window.setTimeout(run, 80);
      window.setTimeout(run, 220);
    }

    window.__MVU_CLAMP_UNIFIED_MAP_CANVAS__ = scheduleUnifiedMapCanvasClamp;
    window.addEventListener('resize', () => scheduleUnifiedMapCanvasClamp());

    function renderLiveCards(snapshot, precomputedSectionSignatures = null) {
      const social = deepGet(snapshot, 'activeChar.绀句氦', {});
      const primaryFactionName = snapshot.primaryFaction ? snapshot.primaryFaction[0] : '鏃?;
      const primaryFactionRole = snapshot.primaryFaction ? toText(deepGet(snapshot.primaryFaction[1], '韬唤', '鏃?), '鏃?) : '鏈姞鍏?;
      const topRelationText = snapshot.topRelation
        ? `${shortenText(snapshot.topRelation[0], 8)} / ${toText(deepGet(snapshot.topRelation[1], '鍏崇郴', '闄岀敓'), '闄岀敓')} 路 ${toNumber(deepGet(snapshot.topRelation[1], '濂芥劅搴?, 0), 0)}`
        : `${snapshot.relations.length} 鏉;
      const latestIntelText = getLatestUnlockedIntelText(snapshot, 12, '鏆傛棤');
      const sectionSignatures = precomputedSectionSignatures || buildDashboardSectionRenderSignatures(snapshot);
      const previousSectionSignatures = lastDashboardSectionRenderSignatures || Object.create(null);
      renderUnifiedCards(snapshot, sectionSignatures, previousSectionSignatures);

      if (sectionSignatures.archive !== previousSectionSignatures.archive) {
        setLiveHtml('[data-preview="鐢熷懡鍥捐氨璇︾粏椤?].mvu-panel.core-card', buildArchiveCoreCard(snapshot));
        setLiveHtml('[data-preview="姝﹁宸ュ潑璇︾粏椤?].mvu-module-card', buildArmoryCard(snapshot));
        setLiveHtml('[data-preview="鍌ㄧ墿浠撳簱璇︾粏椤?].mvu-module-card', buildVaultCard(snapshot));
        renderSpiritStrips(snapshot);
      }

      if (sectionSignatures.map !== previousSectionSignatures.map) {
        if (typeof window.__sheepMapResync === 'function') {
          try {
            window.__sheepMapResync({ center: false, syncVisual: false });
          } catch (err) {}
          scheduleUnifiedMapCanvasClamp();
        }
      }

      if (sectionSignatures.social !== previousSectionSignatures.social) {
        setLiveText('.archive-social-card .social-chip[data-preview="绀句細妗ｆ璇︾粏椤?] span', `${toText(social.鍚嶆湜绛夌骇, toText(social.鍚嶆湜绛夌骇, '绫嶇睄鏃犲悕'))} / ${formatNumber(social.澹版湜)}`);
        setLiveText('.archive-social-card .social-chip[data-preview="鎵€灞炲娍鍔涜缁嗛〉"] span', `${shortenText(primaryFactionName, 8)} / ${shortenText(primaryFactionRole, 8)}`);
        setLiveText('.archive-social-card .social-chip[data-preview="浜虹墿鍏崇郴璇︾粏椤?] span', topRelationText);
        setLiveText('.archive-social-card .social-chip[data-preview="鎯呮姤搴撹缁嗛〉"] span', `${snapshot.unlockedKnowledges.length} / ${latestIntelText}`);
      }

      if (sectionSignatures.world !== previousSectionSignatures.world) {
        getLiveUiElements('[data-preview="涓栫晫鐘舵€佹€昏"].hero-card').forEach(el => {
          el.classList.remove('clickable');
          el.removeAttribute('data-preview');
          setLiveNodeHtml(el, buildWorldHeroCard(snapshot));
        });
        getLiveUiElements('[data-preview="缂栧勾鍙叉。妗?].mvu-simple-card').forEach(el => {
          el.style.overflow = 'hidden';
          el.style.flex = '1.2 1 0';
          setLiveNodeHtml(el, buildSimpleCard('缂栧勾鍙?, null, [
            { label: '鏈€杩戜簨浠?, value: snapshot.latestTimeline ? toText(deepGet(snapshot.latestTimeline[1], '浜嬩欢', snapshot.latestTimeline[0]), snapshot.latestTimeline[0]) : '鏆傛棤' },
            { label: '鎺ㄨ繘鐘舵€?, value: snapshot.latestTimeline ? `${toText(deepGet(snapshot.latestTimeline[1], '鐘舵€?, 'pending'), 'pending')} / Tick ${toText(deepGet(snapshot.latestTimeline[1], '瑙﹀彂tick', 0), '0')}` : '鏆傛棤鏃堕棿绾? }
          ]));
          const simpleList = el.querySelector('.simple-list');
          if (simpleList) {
            simpleList.style.overflow = 'hidden';
            simpleList.style.maxHeight = 'none';
          }
        });
        setLiveHtml('[data-preview="鎷嶅崠涓庤鎶?].mvu-simple-card', buildSimpleCard('鎷嶅崠琛屼笌璀︽姤', null, [
          { label: '鎷嶅崠琛?, value: `${toText(deepGet(snapshot, 'rootData.world.鎷嶅崠.鐘舵€?, '浼戝競'), '浼戝競')} / ${toText(deepGet(snapshot, 'rootData.world.鎷嶅崠.鍦扮偣', '鏃?), '鏃?)}` },
          { label: '鐢熸€佽鎶?, value: snapshot.worldAlert }
        ]));
        setLiveHtml('[data-preview="鍔垮姏鐭╅樀鎬昏"].hero-card', buildOrgHeroCard(snapshot));
        setLiveHtml('[data-preview="鎴戠殑闃佃惀璇︽儏"].mvu-simple-card', buildSimpleCard('鎴戠殑闃佃惀', null, [
          { label: '褰撳墠鎵€灞?, value: snapshot.鍔垮姏[0] ? snapshot.鍔垮姏[0][0] : '鏃? },
          { label: '韬唤', value: snapshot.鍔垮姏[0] ? toText(deepGet(snapshot.鍔垮姏[0][1], '韬唤', '鏃?), '鏃?) : '鏈姞鍏? }
        ]));
        setLiveHtml('[data-preview="鏈湴鎹偣璇︽儏"].mvu-simple-card', buildSimpleCard('鏈湴鎹偣', null, [
          { label: '鎺屾帶鍔垮姏', value: toText(deepGet(snapshot, 'locationData.鎺屾帶鍔垮姏', '鏈煡'), '鏈煡') },
          { label: '缁忔祹鐘跺喌', value: `${toText(deepGet(snapshot, 'locationData.缁忔祹鐘跺喌', '鏈煡'), '鏈煡')} / ${toText(deepGet(snapshot, 'locationData.瀹堟姢鍐涘洟', '瀹堟姢鍐涘洟鏈煡'), '瀹堟姢鍐涘洟鏈煡')}` }
        ]));
      }

      if (sectionSignatures.terminal !== previousSectionSignatures.terminal) {
        setLiveHtml('[data-preview="绯荤粺鎾姤涓庢棩蹇?].terminal-hero-card', buildTerminalHeroCard(snapshot));
        setLiveHtml('[data-preview="鎿嶄綔鎬荤嚎"].terminal-side-card, [data-preview="鎿嶄綔鎬荤嚎"].mvu-simple-card, [data-preview="鎿嶄綔鎬荤嚎"].simple-card', `
        <div class="simple-head"><div class="simple-title">杩戞湡瀹夋帓</div></div>
        <div class="simple-list">
          ${(() => { const planSummary = buildRecentPlanSummary(snapshot, { worldLimit: 1, recordLimit: 1 }); return `
          <div class="simple-row"><b>涓栫晫瀹夋帓</b><span>${htmlEscape(shortenText(planSummary.worldPlans[0] ? planSummary.worldPlans[0].desc : '鏆傛棤', 24))}</span></div>
          <div class="simple-row"><b>涓汉寰呭姙</b><span>${htmlEscape(shortenText(planSummary.personalPlans[0] ? planSummary.personalPlans[0].desc : '鏆傛棤', 24))}</span></div>
          `; })()}
        </div>
      `);
        setLiveHtml('[data-preview="璇曠偧涓庢儏鎶?].terminal-side-card, [data-preview="璇曠偧涓庢儏鎶?].mvu-simple-card, [data-preview="璇曠偧涓庢儏鎶?].simple-card', `
        <div class="simple-head"><div class="simple-title">璇曠偧涓庢儏鎶?/div></div>
        <div class="simple-list">
          <div class="simple-row"><b>璇曠偧鍏ュ彛</b><span>${htmlEscape(getTrialEntranceText(snapshot))}</span></div>
          <div class="simple-row"><b>寰呮牳瀹?/b><span>${htmlEscape(`${toNumber(snapshot && snapshot.鎯呮姤寰呮牳瀹炴暟閲? 0)} 鏉)}</span></div>
          <div class="simple-row"><b>璇佹嵁鍑€鍊?/b><span>${htmlEscape(String(toNumber(snapshot && snapshot.鎯呮姤璇佹嵁鍑€鍊? 0)))}</span></div>
          <div class="simple-row"><b>鏈€杩戞牳瀹?/b><span>${htmlEscape(shortenText(toText(snapshot && snapshot.鎯呮姤鏈€杩戞牳瀹炵粨鏋? '鏆傛棤鏍稿疄璁板綍'), 24))}</span></div>
        </div>
      `);
        setLiveHtml('[data-preview="杩戞湡瑙侀椈"].terminal-side-card, [data-preview="杩戞湡瑙侀椈"].mvu-simple-card, [data-preview="杩戞湡瑙侀椈"].simple-card', `
        <div class="simple-head"><div class="simple-title">杩戞湡瑙侀椈</div></div>
        <div class="simple-list">
          ${(() => { const newsSummary = buildRecentNewsSummary(snapshot, { seqLimit: 1, intelLimit: 1 }); return `
          <div class="simple-row"><b>鍏ㄥ眬瑙侀椈</b><span>${htmlEscape(newsSummary.globalNews[0] ? newsSummary.globalNews[0].desc : '鏆傛棤')}</span></div>
          <div class="simple-row"><b>涓汉瑙侀椈</b><span>${htmlEscape(newsSummary.personalNews[0] ? newsSummary.personalNews[0].desc : '鏆傛棤')}</span></div>
          `; })()}
        </div>
      `);
        setLiveHtml('[data-preview="鎬墿鍥鹃壌"].terminal-side-card, [data-preview="鎬墿鍥鹃壌"].mvu-simple-card, [data-preview="鎬墿鍥鹃壌"].simple-card', `
        <div class="simple-head"><div class="simple-title">鎬墿鍥鹃壌</div></div>
        <div class="simple-list">
          <div class="simple-row"><b>宸茶褰?/b><span>${htmlEscape(`${snapshot.bestiaryEntries.length} 绉峘)}</span></div>
          <div class="simple-row"><b>褰撳墠妗ｄ綅</b><span>${htmlEscape(`${toText(snapshot && snapshot.鍥鹃壌鑱氱劍鍚嶇О, '鏆傛棤')} / ${toText(snapshot && snapshot.鍥鹃壌褰撳墠妗ｄ綅, '鏃?)}`)}</span></div>
          <div class="simple-row"><b>涓嬩竴妗?/b><span>${htmlEscape(toText(snapshot && snapshot.鍥鹃壌涓嬩竴妗ｈ繘搴? '--'))}</span></div>
          <div class="simple-row"><b>鎴愰暱鍊惧悜</b><span>${htmlEscape(toText(snapshot && snapshot.鍥鹃壌鎴愰暱鍊惧悜, '鍧囪　'))}</span></div>
        </div>
      `);
        const questRecords = (snapshot.recordEntries || []).filter(([, item]) => item && typeof item === 'object' && (Object.prototype.hasOwnProperty.call(item, '鐘舵€?) || Object.prototype.hasOwnProperty.call(item, '鐩爣杩涘害') || Object.prototype.hasOwnProperty.call(item, '濂栧姳甯?) || Object.prototype.hasOwnProperty.call(item, '濂栧姳澹版湜')));
        const activeQuestEntry = questRecords.find(([, item]) => !['宸插畬鎴?, '宸叉斁寮?, '澶辫触', '宸插け璐?].includes(toText(item && item['鐘舵€?], '杩涜涓?))) || questRecords[0] || null;
        const activeQuestName = activeQuestEntry ? activeQuestEntry[0] : '';
        const activeQuestState = activeQuestEntry ? toText(activeQuestEntry[1] && activeQuestEntry[1]['鐘舵€?], '杩涜涓?) : '鏆傛棤';
        const questBoardEntries = safeEntries(deepGet(snapshot, 'rootData.world.濮旀墭鏉?, {})).filter(([, item]) => item && typeof item === 'object');
        const openBoardCount = questBoardEntries.filter(([, item]) => toText(item && item['鐘舵€?], '寰呮帴鍙?) === '寰呮帴鍙?).length;
        setLiveHtml('[data-preview="浠诲姟鐣岄潰"].terminal-side-card, [data-preview="浠诲姟鐣岄潰"].mvu-simple-card, [data-preview="浠诲姟鐣岄潰"].simple-card', `
          <div class="simple-head"><div class="simple-title">浠诲姟鐣岄潰</div></div>
          <div class="simple-list">
            <div class="simple-row"><b>鎴戠殑浠诲姟</b><span>${htmlEscape(questRecords.length ? `${questRecords.length} 椤?/ 褰撳墠 ${activeQuestName || '宸插綊妗?}` : '鏆傛棤浠诲姟')}</span></div>
            <div class="simple-row"><b>濮旀墭鏉?/b><span>${htmlEscape(questBoardEntries.length ? `${questBoardEntries.length} 鏉?/ 寰呮帴鍙?${openBoardCount}` : '鏆傛棤濮旀墭')}</span></div>
            <div class="simple-row"><b>褰撳墠璺嚎</b><span>${htmlEscape(toText(snapshot && snapshot.褰撳墠浠诲姟璺嚎, activeQuestState))}</span></div>
            <div class="simple-row"><b>椋庨櫓绾у埆</b><span>${htmlEscape(toText(snapshot && snapshot.褰撳墠浠诲姟椋庨櫓绾у埆, '鏃?))}</span></div>
            <div class="simple-row"><b>涓嬩竴鑺傜偣</b><span>${htmlEscape(toText(snapshot && snapshot.褰撳墠浠诲姟涓嬩竴鑺傜偣杩涘害, '--'))}</span></div>
          </div>
        `);
      }
      lastDashboardSectionRenderSignatures = sectionSignatures;
    }

    function 鏋勫缓鍙帺閫夐」鍗″尯(蹇収, 鏉ユ簮鏍囪 = '') {
      const 閫夐」鍒楄〃 = Array.isArray(蹇収 && 蹇収.鍙帺閫夐」) ? 蹇収.鍙帺閫夐」.slice(0, 4) : [];
      if (!閫夐」鍒楄〃.length) return '';
      const 鎸夐挳Html = 閫夐」鍒楄〃.length
        ? 閫夐」鍒楄〃.map(鏉＄洰 => `
            <button
              type="button"
              class="tag-chip live"
              data-lwcs-option-fill="${escapeHtmlAttr(toText(鏉＄洰 && 鏉＄洰.鍘熸枃, ''))}"
              data-lwcs-option-key="${escapeHtmlAttr(toText(鏉＄洰 && 鏉＄洰.瑙勮寖閿? ''))}"
            >${htmlEscape(toText(鏉＄洰 && 鏉＄洰.灞曠ず鏂囨, toText(鏉＄洰 && 鏉＄洰.鍘熸枃, '鍙€夎鍔?)))}</button>
          `).join('')
        : '';
      return `
        <div class="archive-card full" data-lwcs-option-panel="${escapeHtmlAttr(toText(鏉ユ簮鏍囪, ''))}">
          <div class="archive-card-head"><div class="archive-card-title">鍙€夎鍔?/div></div>
          <div class="request-console-row" style="display:flex; gap:8px; flex-wrap:wrap;">
            ${鎸夐挳Html}
          </div>
        </div>
      `;
    }

    function buildLiveArchiveModal(previewKey) {
      if (!liveSnapshot) return null;
      const snapshot = liveSnapshot;
      if (鎱㈠埛鏂伴鏋跺凡鍚敤 && 鎱㈠埛鏂伴鏋堕瑙堥敭闆嗗悎.has(previewKey)) {
        const 楠ㄦ灦瑙嗗浘 = buildArchiveSkeletonModal(previewKey);
        if (楠ㄦ灦瑙嗗浘) {
          return {
            title: 楠ㄦ灦瑙嗗浘.title,
            summary: '',
            body: 楠ㄦ灦瑙嗗浘.body,
          };
        }
      }
      const tradeLaunchOptions = buildMapTradeModalOptions(snapshot, mapDispatchContext);
      const stat = deepGet(snapshot, 'activeChar.灞炴€?, {});
      const social = deepGet(snapshot, 'activeChar.绀句氦', {});
      const status = deepGet(snapshot, 'activeChar.鐘舵€?, {});
      const wealth = deepGet(snapshot, 'activeChar.璐㈠瘜', {});
      const 绌挎惌鏁版嵁 = deepGet(snapshot, 'activeChar.绌挎惌', {});
      const wardrobeEntries = safeEntries(deepGet(snapshot, 'activeChar.绌挎惌.琛ｆ煖', {}));
      const armor = deepGet(snapshot, 'activeChar.瑁呭.鏂楅摖', {});
      const mech = deepGet(snapshot, 'activeChar.瑁呭.鏈虹敳', {});
      const jobs = safeEntries(deepGet(snapshot, 'activeChar.鑱屼笟', {}));
      if (previewKey === '鎵€灞炲娍鍔涜缁嗛〉' || previewKey === '鎴戠殑闃佃惀璇︽儏') {
        return buildFactionDossierModal(snapshot, previewKey);
      }
      if (isMapOverviewPreviewKey(previewKey)) {
        return {
          title: `${'\u5168\u606f\u661f\u56fe'} / ${getMapDisplayName(snapshot)}`,
          summary: '',
          body: `
            <div class="archive-modal-grid">
              <div class="archive-card full mvu-map-detail-card">
                <div class="mvu-unified-map-stage mvu-archive-map-stage" data-mvu-map-stage="detail"></div>
              </div>
            </div>
          `,
          onMount: mountEl => {
            const stage = mountEl && mountEl.querySelector ? mountEl.querySelector('[data-mvu-map-stage="detail"]') : null;
            if (stage) setLiveNodeHtml(stage, '');
            if (typeof window.__sheepMapResync === 'function') {
              window.setTimeout(() => {
                try { window.__sheepMapResync({ center: false, syncVisual: false }); } catch (err) {}
                scheduleUnifiedMapCanvasClamp();
              }, 0);
            }
            return null;
          }
        };
      }

      if (String(previewKey || '').startsWith(SKILL_DESIGNER_PREVIEW_PREFIX)) {
        const previewMeta = parseSkillDesignerPreviewKey(previewKey) || { path: [], label: '鎶€鑳?, category: '鎶€鑳?, scope: '榄傛妧' };
        const skillSource = previewMeta.path.length ? (deepGet(snapshot.rootData, previewMeta.path, {}) || {}) : {};
        const cachedDesignerDraft = readCachedSkillDesignerDraft(previewKey);
        const rawDesignerDraft = cachedDesignerDraft || readSkillDesignerDraft(skillSource, previewMeta.label);
        const fusionDraftSeed = getSkillDesignerFusionDraftSeed(snapshot, previewMeta, rawDesignerDraft);
        const designerDraft = buildSkillDesignerFormStateFromDraft(
          { ...rawDesignerDraft, ...fusionDraftSeed },
          previewMeta
        );
        const pathTail = formatSkillDesignerWritebackLabel(previewMeta);
        const scopeLabels = getSkillDesignerScopeLabels(previewMeta);
        const recommendedAttrs = new Set(normalizeSkillDesignerArray(SKILL_DESIGNER_ATTRIBUTE_HINTS_BY_TYPE[designerDraft.type] || []));
        const isFusionDesigner = previewMeta.scope === '姝﹂瓊铻嶅悎鎶€';
        const { charData: fusionCharData } = getSkillDesignerCharByPreviewPath(snapshot.rootData, previewMeta);
        const fusionSpiritOptions = isFusionDesigner ? getSkillDesignerSpiritNameOptions(fusionCharData) : [];
        const fusionModeOptions = isFusionDesigner
          ? [
              { value: 'partner', label: '鍙屼汉/澶氫汉铻嶅悎' },
              ...(fusionSpiritOptions.length >= 2 || designerDraft.fusionMode === 'self'
                ? [{ value: 'self', label: '鑷綋铻嶅悎' }]
                : []),
            ]
          : [];
        const fusionStructureSection = isFusionDesigner ? `
                    <section class=\"mvu-editor-section skill-designer-fusion-section\">
                      <div class=\"mvu-editor-section-title\">铻嶅悎瀵硅薄</div>
                      <div class=\"mvu-editor-field-grid skill-designer-fusion-mode-grid\">
                        <label class=\"mvu-editor-field\">
                          <span class=\"mvu-editor-label\">妯″紡</span>
                          <select class=\"mvu-editor-select\" data-skill-designer-field=\"fusionMode\" data-skill-designer-disableable>
                            ${buildSkillDesignerSelectOptions(fusionModeOptions, designerDraft.fusionMode, '')}
                          </select>
                        </label>
                      </div>
                      <div class=\"skill-designer-fusion-participant-list\" data-skill-designer-fusion-participants>
                        ${buildSkillDesignerFusionParticipantRows(designerDraft.fusionParticipants, snapshot.rootData, previewMeta)}
                      </div>
                      <div class=\"mvu-editor-actions skill-designer-fusion-actions\"${designerDraft.fusionMode === 'self' ? ' hidden' : ''}>
                        <button type=\"button\" class=\"tag-chip\" data-skill-designer-add-fusion-participant data-skill-designer-disableable>鏂板瀵硅薄</button>
                      </div>
                    </section>
                  ` : '';
        return {
          title: `${scopeLabels.studioTitle} / ${designerDraft.name}`,
          summary: '',
          onMount: mountEl => {
            const form = mountEl.querySelector('[data-skill-designer-form]');
            const refreshBtn = mountEl.querySelector('[data-skill-designer-refresh]');
            const primaryMainInput = mountEl.querySelector('[data-skill-designer-field=\"primaryMain\"]');
            const primarySubInput = mountEl.querySelector('[data-skill-designer-field=\"primarySub\"]');
            const typeInput = mountEl.querySelector('[data-skill-designer-field=\"type\"]');
            const deliveryFormInput = mountEl.querySelector('[data-skill-designer-field=\"deliveryForm\"]');
            const fusionModeInput = mountEl.querySelector('[data-skill-designer-field=\"fusionMode\"]');
            const fusionParticipantList = mountEl.querySelector('[data-skill-designer-fusion-participants]');
            const addFusionParticipantBtn = mountEl.querySelector('[data-skill-designer-add-fusion-participant]');
            const secondaryGrid = mountEl.querySelector('[data-skill-designer-secondary-grid]');
            const attributeGrid = mountEl.querySelector('[data-skill-designer-attribute-grid]');
            const mechanicParamGrid = mountEl.querySelector('[data-skill-designer-mechanic-param-grid]');
            let destroyed = false;
            let busy = false;

            const setBusy = nextBusy => {
              busy = !!nextBusy;
              mountEl.querySelectorAll('[data-skill-designer-disableable]').forEach(node => {
                node.disabled = busy;
              });
            };

            const readCheckedValues = name => Array.from(mountEl.querySelectorAll(`input[name=\"${name}\"]:checked`)).map(node => toText(node.value, '').trim()).filter(Boolean);
            const syncDraftCache = () => {
              const formState = readSkillDesignerFormState(mountEl, previewMeta);
              const fusionSeed = isFusionDesigner ? getSkillDesignerFusionDraftSeed(snapshot, previewMeta, formState) : {};
              const nextDraft = buildSkillDesignerFormStateFromDraft({ ...formState, ...fusionSeed }, previewMeta);
              writeCachedSkillDesignerDraft(previewKey, nextDraft);
              return nextDraft;
            };
            const syncChipState = () => {
              mountEl.querySelectorAll('.skill-designer-check-chip').forEach(label => {
                const input = label.querySelector('input');
                label.classList.toggle('active', !!(input && input.checked));
              });
            };

            const syncAttributeHints = () => {
              const hintSet = new Set(normalizeSkillDesignerArray(SKILL_DESIGNER_ATTRIBUTE_HINTS_BY_TYPE[typeInput ? typeInput.value : ''] || []));
              mountEl.querySelectorAll('[data-skill-designer-attribute-grid] .skill-designer-check-chip').forEach(label => {
                const input = label.querySelector('input');
                label.classList.toggle('recommended', !!(input && hintSet.has(input.value)));
              });
            };

            const syncFusionFields = () => {
              if (!fusionModeInput) return;
              const formState = readSkillDesignerFormState(mountEl, previewMeta);
              const fusionSeed = getSkillDesignerFusionDraftSeed(snapshot, previewMeta, formState);
              if (fusionParticipantList) {
                fusionParticipantList.innerHTML = buildSkillDesignerFusionParticipantRows(fusionSeed.fusionParticipants, snapshot.rootData, previewMeta);
              }
              if (addFusionParticipantBtn) {
                const actions = addFusionParticipantBtn.closest('.skill-designer-fusion-actions');
                if (actions) actions.hidden = fusionSeed.fusionMode === 'self';
              }
              return buildSkillDesignerFormStateFromDraft({ ...formState, ...fusionSeed }, previewMeta);
            };

            const applyFusionParticipants = participants => {
              if (!fusionModeInput || !fusionParticipantList) return null;
              const formState = readSkillDesignerFormState(mountEl, previewMeta);
              const fusionSeed = getSkillDesignerFusionDraftSeed(snapshot, previewMeta, {
                ...formState,
                fusionMode: fusionModeInput.value,
                fusionParticipants: participants,
              });
              fusionParticipantList.innerHTML = buildSkillDesignerFusionParticipantRows(fusionSeed.fusionParticipants, snapshot.rootData, previewMeta);
              if (addFusionParticipantBtn) {
                const actions = addFusionParticipantBtn.closest('.skill-designer-fusion-actions');
                if (actions) actions.hidden = fusionSeed.fusionMode === 'self';
              }
              const nextDraft = buildSkillDesignerFormStateFromDraft({ ...formState, ...fusionSeed }, previewMeta);
              writeCachedSkillDesignerDraft(previewKey, nextDraft);
              refreshPreview(nextDraft);
              return nextDraft;
            };

            const handleFusionAdd = () => {
              if (!fusionModeInput) return;
              fusionModeInput.value = 'partner';
              const currentDraft = syncDraftCache();
              const nextParticipant = createSkillDesignerDefaultPartnerParticipant(
                snapshot.rootData,
                previewMeta,
                currentDraft.fusionParticipants.map(participant => participant.charKey),
              );
              applyFusionParticipants([...currentDraft.fusionParticipants, nextParticipant]);
            };

            const handleFusionRemove = button => {
              const row = button ? button.closest('[data-fusion-participant-row]') : null;
              if (!row) return;
              const rows = Array.from(mountEl.querySelectorAll('[data-fusion-participant-row]'));
              const removeIndex = rows.indexOf(row);
              const currentDraft = syncDraftCache();
              const nextParticipants = currentDraft.fusionParticipants.filter((participant, index) => index !== removeIndex);
              applyFusionParticipants(nextParticipants);
            };

            const refreshPreview = (draftOverride = null) => {
              const formState = draftOverride || syncDraftCache();
              const previewMap = {
                fusion: buildSkillDesignerFusionSummary(formState) || '鏈缃?,
                mechanic: buildSkillDesignerMechanicSummary(formState) || '鏈缃?,
                mechanicParams: buildSkillDesignerMechanicParamSummary(formState) || '鏈缃?,
                construct: buildSkillDesignerConstructSummary(formState) || '鏈缃?,
                execution: buildSkillDesignerExecutionSummary(formState) || '鏈缃?,
                sideEffects: buildSkillDesignerSideEffectSummary(formState) || '鏃?,
                progress: buildSkillDesignerArtProgressSummary(formState) || '鏈缃?,
                attribute: buildSkillDesignerAttributeSummary(formState) || '鏈缃?,
                summary: buildSkillDesignerCompactSummary(formState) || '鏈缃?,
                tags: normalizeSkillDesignerArray(formState.tags).join(' / ') || '鏃?,
              };
              mountEl.querySelectorAll('[data-skill-designer-preview]').forEach(node => {
                const key = node.getAttribute('data-skill-designer-preview') || '';
                node.textContent = previewMap[key] || '鏈缃?;
              });
              mountEl.querySelectorAll('[data-skill-designer-construct-row]').forEach(node => {
                node.hidden = normalizeSkillUiText(formState.deliveryForm, '') !== '閫犵墿鎵胯浇';
              });
            };

            const rebuildMechanicParamEditor = () => {
              if (!mechanicParamGrid) return;
              const currentDraft = syncDraftCache();
              mechanicParamGrid.innerHTML = buildSkillDesignerMechanicParamEditor(currentDraft);
            };

            const rebuildPrimarySubOptions = () => {
              if (!primarySubInput || !primaryMainInput) return;
              const optionList = getSkillDesignerChildMechanicOptions(primaryMainInput.value);
              const currentValue = toText(primarySubInput.value, '').trim();
              const targetInput = mountEl.querySelector('[data-skill-designer-field=\"target\"]');
              const nextValue = optionList.includes(currentValue)
                ? currentValue
                : getSkillDesignerDefaultPrimarySub(
                  primaryMainInput.value,
                  targetInput ? targetInput.value : '',
                  typeInput ? typeInput.value : '',
                );
              primarySubInput.innerHTML = buildSkillDesignerSelectOptions(
                optionList,
                nextValue,
                '鏈缃?
              );
            };

            const rebuildDeliveryOptions = () => {
              if (!deliveryFormInput || !typeInput) return;
              const currentValue = toText(deliveryFormInput.value, '').trim();
              deliveryFormInput.innerHTML = buildSkillDesignerSelectOptions(
                getSkillDesignerDeliveryOptions(typeInput.value),
                currentValue,
                '鏈缃?
              );
            };

            const rebuildSecondaryOptions = () => {
              if (!secondaryGrid || !primaryMainInput) return;
              const selectedValues = readCheckedValues('skill-secondary');
              secondaryGrid.innerHTML = buildSkillDesignerCheckChipList(
                getSkillDesignerSecondaryOptionList(
                  primaryMainInput.value,
                  selectedValues,
                  typeInput ? typeInput.value : '',
                ),
                selectedValues,
                'skill-secondary',
                label => isSkillDesignerRecommendedSecondaryOption(
                  label,
                  primaryMainInput.value,
                  typeInput ? typeInput.value : '',
                )
                  ? 'recommended'
                  : '',
              ) || '<span class=\"tag-chip\">鏆傛棤闄勫姞鏈哄埗</span>';
              syncChipState();
            };

            const handleInteractiveRefresh = () => {
              rebuildPrimarySubOptions();
              rebuildDeliveryOptions();
              rebuildSecondaryOptions();
              rebuildMechanicParamEditor();
              syncFusionFields();
              syncChipState();
              syncAttributeHints();
              refreshPreview();
            };

            const runDesignerTask = async (task, successMessage = '') => {
              if (busy) return;
              setBusy(true);
              try {
                await task();
                if (successMessage) showUiToast(successMessage, 'info');
              } catch (error) {
                showUiToast(error && error.message ? error.message : '鎶€鑳借璁″啓鍥炲け璐ャ€?, 'error', 4200);
              } finally {
                if (!destroyed) setBusy(false);
              }
            };

            const handleRefresh = async () => {
              await runDesignerTask(async () => {
                clearCachedSkillDesignerDraft(previewKey);
                await refreshLiveSnapshot({ force: true });
                if (!destroyed) rerenderDetailSurface(currentUnifiedPreviewKey || currentModalPreviewKey, { surface: currentUnifiedPreviewKey ? 'unified' : 'modal', force: true });
              }, '宸茬粡閲嶆柊璇诲彇褰撳墠鎶€鑳姐€?);
            };

            const handleSubmit = async event => {
              event.preventDefault();
              await runDesignerTask(async () => {
                if (!Array.isArray(previewMeta.path) || !previewMeta.path.length) throw new Error('褰撳墠鎶€鑳界己灏戝彲鍐欏洖璺緞銆?);
                const formState = readSkillDesignerFormState(mountEl, previewMeta);
                const nextSkill = buildSkillDesignerUpdatedSkill(skillSource, formState, previewMeta);
                await replaceStatDataByEditor(buildSkillDesignerWriteUpdates(previewMeta, nextSkill, snapshot.rootData));
                clearCachedSkillDesignerDraft(previewKey);
                await refreshLiveSnapshot({ force: true });
                if (!destroyed) rerenderDetailSurface(currentUnifiedPreviewKey || currentModalPreviewKey, { surface: currentUnifiedPreviewKey ? 'unified' : 'modal', force: true });
              }, `宸叉洿鏂?{previewMeta.label || '鎶€鑳?}銆俙);
            };

            const handleChange = event => {
              const target = event && event.target instanceof HTMLElement ? event.target : null;
              if (event && event.target === primaryMainInput) {
                rebuildPrimarySubOptions();
                rebuildSecondaryOptions();
              }
              if (event && event.target === typeInput) {
                rebuildDeliveryOptions();
              }
              const shouldRebuildFusion = !!(target && (
                target === fusionModeInput
                || target.matches('[data-skill-designer-fusion-char]')
              ));
              if (
                event
                && (
                  event.target === primaryMainInput
                  || event.target === primarySubInput
                  || event.target === deliveryFormInput
                  || (event.target instanceof HTMLElement && event.target.matches('input[name=\"skill-secondary\"]'))
                )
              ) {
                rebuildMechanicParamEditor();
              }
              syncChipState();
              syncAttributeHints();
              const fusionDraft = shouldRebuildFusion ? syncFusionFields() : null;
              refreshPreview(fusionDraft);
            };

            const handleInput = () => {
              syncChipState();
              refreshPreview();
            };

            const handleClick = event => {
              const target = event && event.target instanceof HTMLElement ? event.target : null;
              const addButton = target ? target.closest('[data-skill-designer-add-fusion-participant]') : null;
              if (addButton) {
                event.preventDefault();
                handleFusionAdd();
                return;
              }
              const removeButton = target ? target.closest('[data-skill-designer-remove-fusion-participant]') : null;
              if (removeButton) {
                event.preventDefault();
                handleFusionRemove(removeButton);
              }
            };

            handleInteractiveRefresh();
            if (form) form.addEventListener('submit', handleSubmit);
            if (refreshBtn) refreshBtn.addEventListener('click', handleRefresh);
            mountEl.addEventListener('change', handleChange);
            mountEl.addEventListener('input', handleInput);
            mountEl.addEventListener('click', handleClick);

            return {
              destroy() {
                destroyed = true;
                if (form) form.removeEventListener('submit', handleSubmit);
                if (refreshBtn) refreshBtn.removeEventListener('click', handleRefresh);
                mountEl.removeEventListener('change', handleChange);
                mountEl.removeEventListener('input', handleInput);
                mountEl.removeEventListener('click', handleClick);
              }
            };
          },
          body: `
            <div class=\"archive-modal-grid skill-designer-layout skill-designer-shell\">
              <div class=\"archive-card full skill-designer-anchor-card\">
                <div class=\"archive-card-head\">
                  <div class=\"archive-card-title\">${htmlEscape(scopeLabels.anchorTitle)}</div>
                  <span class=\"state-tag live\">${htmlEscape(designerDraft.typeDisplay || designerDraft.type || previewMeta.category || '鎶€鑳?)}</span>
                </div>
                ${makeTileGrid([
                  { label: scopeLabels.nameCardLabel, value: designerDraft.name },
                  { label: scopeLabels.typeCardLabel, value: designerDraft.typeDisplay || designerDraft.type },
                  { label: scopeLabels.targetLabel, value: designerDraft.target },
                  { label: '鍐欏洖浣嶇疆', value: pathTail }
                ], 'two')}
              </div>

              <form class=\"archive-card full mvu-editor-form skill-designer-form skill-designer-form-card\" data-skill-designer-form>
                <div class=\"archive-card-head\">
                  <div class=\"archive-card-title\">${htmlEscape(scopeLabels.parameterTitle)}</div>
                  <div class=\"mvu-editor-actions\">
                    <button type=\"button\" class=\"tag-chip\" data-skill-designer-refresh data-skill-designer-disableable>閲嶆柊璇诲彇</button>
                    <button type=\"submit\" class=\"tag-chip live\" data-skill-designer-disableable>淇濆瓨璁捐</button>
                  </div>
                </div>
                <div class=\"mvu-editor-grid\">
                  <section class=\"mvu-editor-section\">
                    <div class=\"mvu-editor-section-title\">鍩虹淇℃伅</div>
                    <div class=\"mvu-editor-field-grid\">
                      <input type=\"hidden\" value=\"${escapeHtmlAttr(designerDraft.type || '鏈缃?)}\" data-skill-designer-field=\"type\" data-skill-designer-disableable />
                      <label class=\"mvu-editor-field\">
                        <span class=\"mvu-editor-label\">${htmlEscape(scopeLabels.nameFieldLabel)}</span>
                        <input class=\"mvu-editor-input\" type=\"text\" value=\"${escapeHtmlAttr(designerDraft.name)}\" data-skill-designer-field=\"name\" data-skill-designer-disableable />
                      </label>
                      <label class=\"mvu-editor-field\">
                        <span class=\"mvu-editor-label\">${htmlEscape(scopeLabels.targetLabel)}</span>
                        <select class=\"mvu-editor-select\" data-skill-designer-field=\"target\" data-skill-designer-disableable>
                          ${buildSkillDesignerSelectOptions(SKILL_DESIGNER_TARGET_OPTIONS, designerDraft.target, '鏈缃?)}
                        </select>
                      </label>
                      <label class=\"mvu-editor-field mvu-editor-field-wide\">
                        <span class=\"mvu-editor-label\">鏍囩</span>
                        <input class=\"mvu-editor-input\" type=\"text\" value=\"${escapeHtmlAttr((designerDraft.tags || []).join('銆?))}\" placeholder=\"鐢ㄣ€佹垨閫楀彿鍒嗛殧\" data-skill-designer-field=\"tags\" data-skill-designer-disableable />
                      </label>
                    </div>
                  </section>

                  ${fusionStructureSection}

                  ${previewMeta.scope === 'art' ? `
                    <section class=\"mvu-editor-section\">
                      <div class=\"mvu-editor-section-title\">鍔熸硶杩涘害</div>
                      <div class=\"mvu-editor-field-grid\">
                        <label class=\"mvu-editor-field\">
                          <span class=\"mvu-editor-label\">澧冪晫</span>
                          <input class=\"mvu-editor-input\" type=\"text\" value=\"${escapeHtmlAttr(designerDraft.artStage || '鏈叆闂?)}\" data-skill-designer-field=\"artStage\" data-skill-designer-disableable />
                        </label>
                        <label class=\"mvu-editor-field\">
                          <span class=\"mvu-editor-label\">绛夌骇</span>
                          <input class=\"mvu-editor-input\" type=\"number\" min=\"0\" step=\"1\" value=\"${escapeHtmlAttr(String(Math.max(0, toNumber(designerDraft.artLevel, 0))))}\" data-skill-designer-field=\"artLevel\" data-skill-designer-disableable />
                        </label>
                        <label class=\"mvu-editor-field\">
                          <span class=\"mvu-editor-label\">鐔熺粌搴?/span>
                          <input class=\"mvu-editor-input\" type=\"number\" min=\"0\" step=\"1\" value=\"${escapeHtmlAttr(String(Math.max(0, toNumber(designerDraft.artExp, 0))))}\" data-skill-designer-field=\"artExp\" data-skill-designer-disableable />
                        </label>
                      </div>
                    </section>
                  ` : ''}

                  <section class=\"mvu-editor-section\">
                    <div class=\"mvu-editor-section-title\">鏈哄埗璁捐</div>
                    <div class=\"mvu-editor-field-grid\">
                      <label class=\"mvu-editor-field\">
                        <span class=\"mvu-editor-label\">涓绘満鍒?/span>
                        <select class=\"mvu-editor-select\" data-skill-designer-field=\"primaryMain\" data-skill-designer-disableable>
                          ${buildSkillDesignerSelectOptions(Object.keys(SKILL_DESIGNER_MAIN_MECHANIC_POOL), designerDraft.primaryMain, '鏈缃?)}
                        </select>
                      </label>
                      <label class=\"mvu-editor-field\">
                        <span class=\"mvu-editor-label\">缁嗗垎鏈哄埗</span>
                        <select class=\"mvu-editor-select\" data-skill-designer-field=\"primarySub\" data-skill-designer-disableable>
                          ${buildSkillDesignerSelectOptions(getSkillDesignerChildMechanicOptions(designerDraft.primaryMain), designerDraft.primarySub, '鏈缃?)}
                        </select>
                      </label>
                      <label class=\"mvu-editor-field\">
                        <span class=\"mvu-editor-label\">${htmlEscape(scopeLabels.deliveryLabel)}</span>
                        <select class=\"mvu-editor-select\" data-skill-designer-field=\"deliveryForm\" data-skill-designer-disableable>
                          ${buildSkillDesignerSelectOptions(getSkillDesignerDeliveryOptions(designerDraft.type), designerDraft.deliveryForm, '鏈缃?)}
                        </select>
                      </label>
                    </div>
                    <div class=\"skill-designer-subsection\">
                      <div class=\"mvu-editor-label\">闄勫姞鏈哄埗锛堝彲澶氶€夛級</div>
                      <div class=\"skill-designer-chip-grid\" data-skill-designer-secondary-grid>
                        ${buildSkillDesignerCheckChipList(
                          getSkillDesignerSecondaryOptionList(designerDraft.primaryMain, designerDraft.secondaryMechanics, designerDraft.type),
                          designerDraft.secondaryMechanics,
                          'skill-secondary',
                          option => isSkillDesignerRecommendedSecondaryOption(option, designerDraft.primaryMain, designerDraft.type) ? 'recommended' : '',
                        ) || '<span class=\"tag-chip\">鏆傛棤闄勫姞鏈哄埗</span>'}
                      </div>
                    </div>
                  </section>

                  <section class=\"mvu-editor-section\">
                    <div class=\"mvu-editor-section-title\">鏈哄埗鍙傛暟</div>
                    <div class=\"skill-designer-preview-stack\" data-skill-designer-mechanic-param-grid>
                      ${buildSkillDesignerMechanicParamEditor(designerDraft)}
                    </div>
                  </section>

                  <section class=\"mvu-editor-section\">
                    <div class=\"mvu-editor-section-title\">鍓綔鐢?/div>
                    <div class=\"mvu-editor-field-grid\">
                      <label class=\"mvu-editor-field mvu-editor-field-wide\">
                        <span class=\"mvu-editor-label\">鍓綔鐢ㄥ垪琛?/span>
                        <textarea class=\"mvu-editor-textarea\" data-skill-designer-field=\"sideEffectJson\" data-skill-designer-disableable>${htmlEscape(formatSkillDesignerSideEffectJson(designerDraft['鍓綔鐢ㄥ垪琛?]))}</textarea>
                      </label>
                    </div>
                  </section>

                  <section class=\"mvu-editor-section\">
                    <div class=\"mvu-editor-section-title\">鎵ц鍙傛暟</div>
                    <div class=\"mvu-editor-field-grid\">
                      <label class=\"mvu-editor-field\">
                        <span class=\"mvu-editor-label\">娑堣€楄祫婧?/span>
                        <select class=\"mvu-editor-select\" data-skill-designer-field=\"costType\" data-skill-designer-disableable>
                          ${buildSkillDesignerSelectOptions(SKILL_DESIGNER_RESOURCE_TYPE_OPTIONS, designerDraft.costType, '鏈缃?)}
                        </select>
                      </label>
                      <label class=\"mvu-editor-field\">
                        <span class=\"mvu-editor-label\">娑堣€楁暟鍊?/span>
                        <input class=\"mvu-editor-input\" type=\"text\" value=\"${escapeHtmlAttr(designerDraft.costValue || '')}\" placeholder=\"20 / 15% / 榄傚姏30+绮剧鍔?0\" data-skill-designer-field=\"costValue\" data-skill-designer-disableable />
                      </label>
                    </div>
                  </section>

                  <section class=\"mvu-editor-section\">
                    <div class=\"mvu-editor-section-title\">灞炴€ц璁?/div>
                    <div class=\"skill-designer-subsection\">
                      <div class=\"mvu-editor-label\">闄勫甫灞炴€?/div>
                      <div class=\"skill-designer-chip-grid\" data-skill-designer-attribute-grid>
                        ${buildSkillDesignerCheckChipList(SKILL_DESIGNER_ATTRIBUTE_OPTIONS, designerDraft.attachedAttributes, 'skill-attribute', option => recommendedAttrs.has(option) ? 'recommended' : '')}
                      </div>
                    </div>
                  </section>

                  <section class=\"mvu-editor-section\">
                    <div class=\"mvu-editor-section-title\">鏂囨湰鎻忚堪</div>
                    <div class=\"mvu-editor-field-grid\">
                      <label class=\"mvu-editor-field mvu-editor-field-wide\">
                        <span class=\"mvu-editor-label\">${htmlEscape(scopeLabels.visualLabel)}</span>
                        <textarea class=\"mvu-editor-textarea\" data-skill-designer-field=\"visualDesc\" data-skill-designer-disableable>${htmlEscape(designerDraft.visualDesc)}</textarea>
                      </label>
                      <label class=\"mvu-editor-field mvu-editor-field-wide\">
                        <span class=\"mvu-editor-label\">${htmlEscape(scopeLabels.effectLabel)}</span>
                        <textarea class=\"mvu-editor-textarea\" data-skill-designer-field=\"effectDesc\" data-skill-designer-disableable>${htmlEscape(designerDraft.effectDesc)}</textarea>
                      </label>
                    </div>
                  </section>
                </div>
              </form>

              <div class=\"archive-card full skill-designer-summary-card\">
                <div class=\"archive-card-head\"><div class=\"archive-card-title\">${htmlEscape(scopeLabels.summaryTitle)}</div></div>
                <div class=\"skill-designer-preview-stack skill-designer-summary-list\">
                  ${isFusionDesigner
                    ? `<div class=\"skill-designer-summary-row\"><em>铻嶅悎瀵硅薄</em><span data-skill-designer-preview=\"fusion\">${htmlEscape(buildSkillDesignerFusionSummary(designerDraft) || '鏈缃?)}</span></div>`
                    : ''}
                  <div class=\"skill-designer-summary-row\"><em>鏈哄埗缁勫悎</em><span data-skill-designer-preview=\"mechanic\">${htmlEscape(buildSkillDesignerMechanicSummary(designerDraft) || '鏈缃?)}</span></div>
                  <div class=\"skill-designer-summary-row\"><em>鏈哄埗鍙傛暟</em><span data-skill-designer-preview=\"mechanicParams\">${htmlEscape(buildSkillDesignerMechanicParamSummary(designerDraft) || '鏈缃?)}</span></div>
                  <div class=\"skill-designer-summary-row\" data-skill-designer-construct-row ${normalizeSkillUiText(designerDraft.deliveryForm, '') === '閫犵墿鎵胯浇' ? '' : 'hidden'}>
                    <em>閫犵墿瑙勫垯</em><span data-skill-designer-preview=\"construct\">${htmlEscape(buildSkillDesignerConstructSummary(designerDraft) || '鏈缃?)}</span>
                  </div>
                  <div class=\"skill-designer-summary-row\"><em>鎵ц鎽樿</em><span data-skill-designer-preview=\"execution\">${htmlEscape(buildSkillDesignerExecutionSummary(designerDraft) || '鏈缃?)}</span></div>
                  <div class=\"skill-designer-summary-row\"><em>鍓綔鐢?/em><span data-skill-designer-preview=\"sideEffects\">${htmlEscape(buildSkillDesignerSideEffectSummary(designerDraft) || '鏃?)}</span></div>
                  ${previewMeta.scope === 'art'
                    ? `<div class=\"skill-designer-summary-row\"><em>鍔熸硶杩涘害</em><span data-skill-designer-preview=\"progress\">${htmlEscape(buildSkillDesignerArtProgressSummary(designerDraft) || '鏈缃?)}</span></div>`
                    : ''}
                  <div class=\"skill-designer-summary-row\"><em>闄勫甫灞炴€?/em><span data-skill-designer-preview=\"attribute\">${htmlEscape(buildSkillDesignerAttributeSummary(designerDraft) || '鏈缃?)}</span></div>
                  <div class=\"skill-designer-summary-row\"><em>鏈€缁堟憳瑕?/em><span data-skill-designer-preview=\"summary\">${htmlEscape(buildSkillDesignerCompactSummary(designerDraft) || '鏈缃?)}</span></div>
                  <div class=\"skill-designer-summary-row\"><em>鏍囩</em><span data-skill-designer-preview=\"tags\">${htmlEscape((designerDraft.tags || []).join(' / ') || '鏃?)}</span></div>
                </div>
              </div>
            </div>
          `
        };
      }

      if (previewKey === '瑙掕壊鍒囨崲鍣?) {
        const playerName = toText(deepGet(snapshot, 'rootData.sys.鐜╁鍚?, ''), '');
        const currentCharKey = resolveSnapshotCharKey(snapshot, toText(snapshot.activeName, ''));
        const charEntries = sortCharacterEntries(safeEntries(deepGet(snapshot, 'rootData.char', {})), { playerName, currentName: snapshot.activeName });
        const switchCards = charEntries.length
          ? charEntries.map(([name, char]) => {
              const displayName = toText(char && (char.name || deepGet(char, 'base.name', '')), name);
              const activeDisplayName = toText(snapshot.activeName, '');
              const isCurrent = (currentCharKey && name === currentCharKey) || (!currentCharKey && !!activeDisplayName && displayName === activeDisplayName);
              const isPlayer = isPlayerCharacterEntry(name, char, playerName);
              const lvText = formatCultivationLevelBadge(deepGet(char, '灞炴€?绛夌骇', 0), '0');
              const identityText = toText(deepGet(char, '绀句氦.涓昏韩浠?, deepGet(char, 'base.identity', '鏃?)), '鏃?);
              const locText = toText(deepGet(char, '鐘舵€?浣嶇疆', '鏈煡鍦扮偣'), '鏈煡鍦扮偣').replace(/^鏂楃綏澶ч檰-/, '').replace(/^鏂楃伒澶ч檰-/, '');
              const actionText = toText(deepGet(char, '鐘舵€?琛屽姩', '鏃ュ父'), '鏃ュ父');
              const searchText = [
                name,
                isPlayer ? '鐜╁' : '瑙掕壊',
                identityText,
                locText,
                actionText,
                isTangWulinCharacter(name) ? '鍞愯垶楹? : ''
              ].filter(Boolean).join(' ').toLowerCase();
              return `
                <button type="button" class="role-switch-tile${isCurrent ? ' active' : ''}" data-mvu-switch-char="${escapeHtmlAttr(name)}" data-switch-search="${escapeHtmlAttr(searchText)}">
                  <div class="role-switch-head"><b>${htmlEscape(name)}</b><span class="state-tag ${isCurrent ? 'live' : (isPlayer ? 'warn' : '')}">${isCurrent ? '褰撳墠鏌ョ湅' : (isPlayer ? '鐜╁' : '瑙掕壊')}</span></div>
                  <div class="role-switch-meta">${htmlEscape(`${lvText} 锝?${identityText}`)}</div>
                  <div class="role-switch-meta">${htmlEscape(`${locText} 锝?${actionText}`)}</div>
                </button>
              `;
            }).join('')
          : '<div class="role-switch-empty">鏆傛棤鍙垏鎹㈣鑹层€?/div>';
        return {
          title: '瑙掕壊娴忚 / 鍒囨崲寮圭獥',
          summary: '鏌ョ湅鍏ㄩ儴瑙掕壊鎽樿锛屽苟鍒囨崲褰撳墠鏌ョ湅瑙掕壊銆?,
          onMount: mountEl => {
            const inputEl = mountEl.querySelector('.role-switch-search-input');
            const countEl = mountEl.querySelector('.role-switch-search-count');
            const prevBtn = mountEl.querySelector('[data-role-page-nav="prev"]');
            const nextBtn = mountEl.querySelector('[data-role-page-nav="next"]');
            const pageIndicatorEl = mountEl.querySelector('.role-switch-page-indicator');
            const tileEls = Array.from(mountEl.querySelectorAll('.role-switch-tile'));
            let currentPage = 1;
            const pageSize = 50;
            const applyFilter = () => {
              const keyword = toText(inputEl && inputEl.value, '').trim().toLowerCase();
              const matchedTiles = [];
              tileEls.forEach(tile => {
                const haystack = toText(tile.getAttribute('data-switch-search'), '').toLowerCase();
                const matched = !keyword || haystack.includes(keyword);
                if (matched) matchedTiles.push(tile);
                tile.style.display = 'none';
              });
              const totalPages = Math.max(1, Math.ceil(matchedTiles.length / pageSize));
              currentPage = Math.max(1, Math.min(totalPages, currentPage));
              const start = (currentPage - 1) * pageSize;
              matchedTiles.slice(start, start + pageSize).forEach(tile => { tile.style.display = ''; });
              if (countEl) {
                countEl.textContent = keyword ? `鍖归厤 ${matchedTiles.length} / ${tileEls.length} 鍚峘 : `鍏?${tileEls.length} 鍚峘;
              }
              if (pageIndicatorEl) pageIndicatorEl.textContent = `绗?${currentPage} / ${totalPages} 椤礰;
              if (prevBtn) prevBtn.disabled = currentPage <= 1;
              if (nextBtn) nextBtn.disabled = currentPage >= totalPages;
            };
            if (inputEl) {
              inputEl.addEventListener('input', () => { currentPage = 1; applyFilter(); });
              setTimeout(() => {
                try { inputEl.focus(); } catch (_) {}
              }, 0);
            }
            if (prevBtn) prevBtn.addEventListener('click', () => { currentPage = Math.max(1, currentPage - 1); applyFilter(); });
            if (nextBtn) nextBtn.addEventListener('click', () => { currentPage += 1; applyFilter(); });
            applyFilter();
            return {
              destroy() {
                if (inputEl) inputEl.removeEventListener('input', applyFilter);
              }
            };
          },
          body: `
            <div class="archive-modal-grid" style="grid-template-columns:1fr;">
              <div class="archive-card full"><div class="archive-card-head"><div class="archive-card-title">鏌ョ湅鐘舵€?/div></div>${makeTileGrid([{ label: '褰撳墠鏌ョ湅', value: snapshot.activeName }, { label: '鐜╁瑙掕壊', value: playerName || snapshot.activeName }, { label: '瑙掕壊鎬绘暟', value: `${charEntries.length} 鍚峘 }, { label: '褰撳墠鍦扮偣', value: snapshot.currentLoc }], 'two')}</div>
              <div class="archive-card full">
                <div class="archive-card-head"><div class="archive-card-title">瑙掕壊鍒楄〃</div></div>
                <div style="display:flex;align-items:center;gap:10px;flex-wrap:wrap;margin:0 0 12px;">
                  <input type="search" class="role-switch-search-input" placeholder="鎼滅储瑙掕壊 / 韬唤 / 鍦扮偣 / 鐘舵€? style="flex:1 1 240px;min-width:220px;height:36px;padding:0 12px;border-radius:10px;border:1px solid rgba(120,220,255,0.24);background:rgba(8,20,30,0.72);color:#dff7ff;outline:none;" />
                  <span class="role-switch-search-count" style="font-size:12px;color:rgba(220,245,255,0.72);">鍏?${charEntries.length} 鍚?/span>
                </div>
                <div class="role-switch-grid">${switchCards}</div>
                <div class="tag-cloud armory-quick-actions" style="margin-top:12px;justify-content:flex-end;">
                  <button type="button" class="tag-chip role-switch-page-btn" data-role-page-nav="prev">涓婁竴椤?/button>
                  <span class="tag-chip role-switch-page-indicator">绗?1 / 1 椤?/span>
                  <button type="button" class="tag-chip role-switch-page-btn" data-role-page-nav="next">涓嬩竴椤?/button>
                </div>
              </div>
            </div>
          `
        };
      }

      if (previewKey === PRIVATE_ARCHIVE_PREVIEW_KEY) {
        if (!canOpenPrivateArchive(snapshot)) return null;
        const nsfwPath = getActiveNsfwPath(snapshot);
        const nsfw = getActiveNsfwData(snapshot);
        const bodyPartEntries = safeEntries(deepGet(nsfw, '韬綋閮ㄤ綅', {}));

        return {
          title: '绉佸瘑妗ｆ',
          onMount: (modalBody) => {
            const panel = modalBody.closest('.mvu-modal-panel');
            if (panel) {
              panel.classList.add('nsfw-theme-active');
              const observer = new MutationObserver(() => {
                if (!modalBody.querySelector('.nsfw-modal-grid')) {
                  panel.classList.remove('nsfw-theme-active');
                  observer.disconnect();
                }
              });
              observer.observe(modalBody, { childList: true });
            }
          },
          body: `
            <div class="nsfw-modal-grid">
              
              <!-- 1. 鏍稿績娆插康 -->
              <div class="nsfw-theme-card">
                <div class="nsfw-theme-head">
                  <div class="nsfw-theme-title">馃挄 鏍稿績娆插康</div>
                </div>
                <div class="nsfw-theme-body">
                  <div class="nsfw-progress-container">
                    <div class="nsfw-progress-label">
                      <span>鍙戞儏搴?/span>
                      <span>${makeInlineEditableValue(toText(nsfw.鍙戞儏搴? '0'), { path: [...nsfwPath, '鍙戞儏搴?], kind: 'number' })} / 100</span>
                    </div>
                    <div class="nsfw-progress-track arousal">
                      <div class="nsfw-fill-arousal" style="width: ${Math.min(100, Math.max(0, toNumber(nsfw.鍙戞儏搴? 0)))}%;"></div>
                    </div>
                  </div>
                  <div class="nsfw-progress-container">
                    <div class="nsfw-progress-label">
                      <span>寮€鍙戝害</span>
                      <span>${makeInlineEditableValue(toText(nsfw.寮€鍙戝害, '0'), { path: [...nsfwPath, '寮€鍙戝害'], kind: 'number' })} / 100</span>
                    </div>
                    <div class="nsfw-progress-track dev">
                      <div class="nsfw-fill-dev" style="width: ${Math.min(100, Math.max(0, toNumber(nsfw.寮€鍙戝害, 0)))}%;"></div>
                    </div>
                  </div>
                  <div class="identity-growth-grid" style="grid-template-columns: 1fr;">
                    <div class="nsfw-meta-item">
                      <b>鏁忔劅搴﹀熀纭€鍊?/b>
                      <span>${makeInlineEditableValue(toText(nsfw.鏁忔劅搴? '10'), { path: [...nsfwPath, '鏁忔劅搴?], kind: 'number' })}</span>
                    </div>
                  </div>
                </div>
              </div>

              <!-- 2. 鑲変綋瀵嗙爜 & 鐫€瑁?-->
              <div class="nsfw-theme-card">
                <div class="nsfw-theme-head">
                  <div class="nsfw-theme-title">馃憴 鑲変綋瀵嗙爜涓庣瀵嗙潃瑁?/div>
                </div>
                <div class="nsfw-theme-body">
                  <div class="identity-growth-grid" style="grid-template-columns: 1fr 1fr;">
                    <div class="nsfw-meta-item">
                      <b>缃╂澂</b>
                      <span>${makeInlineEditableValue(toText(deepGet(nsfw, '韬潗鏁版嵁.缃╂澂'), '鏈煡'), { path: [...nsfwPath, '韬潗鏁版嵁', '缃╂澂'], kind: 'string' })}</span>
                    </div>
                    <div class="nsfw-meta-item">
                      <b>涓夊洿 (鑳?鑵?鑷€)</b>
                      <span>
                        ${makeInlineEditableValue(toText(deepGet(nsfw, '韬潗鏁版嵁.鑳稿洿'), '0'), { path: [...nsfwPath, '韬潗鏁版嵁', '鑳稿洿'], kind: 'number' })} /
                        ${makeInlineEditableValue(toText(deepGet(nsfw, '韬潗鏁版嵁.鑵板洿'), '0'), { path: [...nsfwPath, '韬潗鏁版嵁', '鑵板洿'], kind: 'number' })} /
                        ${makeInlineEditableValue(toText(deepGet(nsfw, '韬潗鏁版嵁.鑷€鍥?), '0'), { path: [...nsfwPath, '韬潗鏁版嵁', '鑷€鍥?], kind: 'number' })}
                      </span>
                    </div>
                    <div class="nsfw-meta-item">
                      <b>鍐呰。</b>
                      <span>${makeInlineEditableValue(toText(deepGet(nsfw, '璐磋韩琛ｇ墿.鍐呰。'), '鏈煡'), { path: [...nsfwPath, '璐磋韩琛ｇ墿', '鍐呰。'], kind: 'string' })}</span>
                    </div>
                    <div class="nsfw-meta-item">
                      <b>鍐呰￥</b>
                      <span>${makeInlineEditableValue(toText(deepGet(nsfw, '璐磋韩琛ｇ墿.鍐呰￥'), '鏈煡'), { path: [...nsfwPath, '璐磋韩琛ｇ墿', '鍐呰￥'], kind: 'string' })}</span>
                    </div>
                  </div>
                  <div class="nsfw-meta-item" style="margin-top:8px;">
                    <b>韬潗鎻忚堪</b>
                    <span class="nsfw-text-wrap">${makeInlineEditableValue(toText(deepGet(nsfw, '韬潗鏁版嵁.韬潗鎻忚堪'), '鏃?), { path: [...nsfwPath, '韬潗鏁版嵁', '韬潗鎻忚堪'], kind: 'string' })}</span>
                  </div>
                </div>
              </div>

              <!-- 3. 鐢熺悊鑺傚緥 -->
              <div class="nsfw-theme-card">
                <div class="nsfw-theme-head">
                  <div class="nsfw-theme-title">馃└ 鐢熺悊鑺傚緥</div>
                </div>
                <div class="nsfw-theme-body">
                  <div class="identity-growth-grid" style="grid-template-columns: 1fr 1fr;">
                    <div class="nsfw-meta-item">
                      <b>褰撳墠鐢熺悊闃舵</b>
                      <span class="nsfw-highlight-red">${makeInlineEditableValue(toText(nsfw._鐢熺悊闃舵, '瀹夊叏鏈?), { path: [...nsfwPath, '_鐢熺悊闃舵'], kind: 'string' })}</span>
                    </div>
                    <div class="nsfw-meta-item">
                      <b>鏄惁鍒濇疆</b>
                      <span>${makeInlineEditableValue(nsfw._宸叉潵鍒濇疆 ? '宸插垵娼? : '鏈垵娼?, { path: [...nsfwPath, '_宸叉潵鍒濇疆'], kind: 'boolean' })}</span>
                    </div>
                    <div class="nsfw-meta-item">
                      <b>鍙楀瓡鑺傜偣</b>
                      <span>${makeInlineEditableValue(String(toNumber(nsfw.鍙楀瓡tick, -1)), { path: [...nsfwPath, '鍙楀瓡tick'], kind: 'number' })}</span>
                    </div>
                    <div class="nsfw-meta-item">
                      <b>褰撳墠鎬€瀛曞ぉ鏁?/b>
                      <span>${makeInlineEditableValue(String(toNumber(nsfw._鎬€瀛曞ぉ鏁? 0)), { path: [...nsfwPath, '_鎬€瀛曞ぉ鏁?], kind: 'number' })}</span>
                    </div>
                    <div class="nsfw-meta-item" style="grid-column: span 2;">
                      <b>瀛曠埗</b>
                      <span>${makeInlineEditableValue(toText(nsfw.鍙楀瓡瀵硅薄, '鏃?), { path: [...nsfwPath, '鍙楀瓡瀵硅薄'], kind: 'string' })}</span>
                    </div>
                  </div>
                </div>
              </div>

              <!-- 4. 鍫曡惤灞ュ巻 -->
              <div class="nsfw-theme-card">
                <div class="nsfw-theme-head">
                  <div class="nsfw-theme-title">馃搱 寮€鍙戝饱鍘?/div>
                </div>
                <div class="nsfw-theme-body">
                  <div class="identity-growth-grid" style="grid-template-columns: repeat(3, 1fr);">
                    ${['楂樻疆', '棰滃皠', '鍐呭皠', '鍚炵簿', '瀹犵墿鎵紨', '鑷叞', '鎬т氦', '鎵嬩氦', '瓒充氦', '鍙ｄ氦', '绱犺偂', '鍙戜氦', 'SM', 'COSPLAY'].map(exp => `
                      <div class="nsfw-meta-item">
                        <b>${exp}</b>
                        <span>${makeInlineEditableValue(String(toNumber(deepGet(nsfw, ['缁忓巻娆℃暟', exp]), 0)), { path: [...nsfwPath, '缁忓巻娆℃暟', exp], kind: 'number' })}</span>
                      </div>
                    `).join('')}
                  </div>
                </div>
              </div>

              <!-- 5. 娣卞眰鎬х櫀 -->
              <div class="nsfw-theme-card">
                <div class="nsfw-theme-head">
                  <div class="nsfw-theme-title">馃槇 娣卞眰鎬х櫀</div>
                </div>
                <div class="nsfw-theme-body">
                  <div style="margin-bottom: 8px;">
                    <b style="font-size: 11px; color: #ffb3c1; display: block; margin-bottom: 6px;">鐧栧ソ</b>
                    <div style="display:flex; flex-wrap:wrap; gap:6px;">
                      ${(Array.isArray(nsfw.鐧栧ソ) ? nsfw.鐧栧ソ : []).map((f, i) => `
                        <span class="nsfw-tag">${makeInlineEditableValue(toText(f), { path: [...nsfwPath, '鐧栧ソ', i], kind: 'string' })}</span>
                      `).join('')}
                    </div>
                  </div>
                  <div>
                    <b style="font-size: 11px; color: #ffb3c1; display: block; margin-bottom: 6px;">骞绘兂</b>
                    <div style="display:flex; flex-wrap:wrap; gap:6px;">
                      ${(Array.isArray(nsfw.骞绘兂) ? nsfw.骞绘兂 : []).map((f, i) => `
                        <span class="nsfw-tag">${makeInlineEditableValue(toText(f), { path: [...nsfwPath, '骞绘兂', i], kind: 'string' })}</span>
                      `).join('')}
                    </div>
                  </div>
                </div>
              </div>

              <!-- 6. 韬綋閮ㄤ綅 -->
              <div class="nsfw-theme-card">
                <div class="nsfw-theme-head">
                  <div class="nsfw-theme-title">馃挔 鏁忔劅閮ㄤ綅妗ｆ</div>
                </div>
                <div class="nsfw-theme-body">
                  ${bodyPartEntries.length ? `
                    <div class="nsfw-bodypart-grid">
                      ${bodyPartEntries.map(([partName, partData]) => `
                        <div class="nsfw-bodypart-card">
                          <div class="nsfw-bodypart-head">
                            <div class="nsfw-bodypart-name">${htmlEscape(partName)}</div>
                            <span class="nsfw-bodypart-badge">寮€鍙?${makeInlineEditableValue(String(toNumber(deepGet(partData, '寮€鍙戝害', 0), 0)), { path: [...nsfwPath, '韬綋閮ㄤ綅', partName, '寮€鍙戝害'], kind: 'number' })}</span>
                          </div>
                          <div class="nsfw-bodypart-meta">
                            <div class="nsfw-meta-item">
                              <b>澶栬鐗瑰緛</b>
                              <span class="nsfw-text-wrap">${makeInlineEditableValue(toText(deepGet(partData, '澶栬鐗瑰緛', '鏃?), '鏃?), { path: [...nsfwPath, '韬綋閮ㄤ綅', partName, '澶栬鐗瑰緛'], kind: 'string' })}</span>
                            </div>
                            <div class="identity-growth-grid" style="grid-template-columns: 1fr 1fr;">
                              <div class="nsfw-meta-item">
                                <b>鏁忔劅搴﹀姞鎴?/b>
                                <span>${makeInlineEditableValue(String(toNumber(deepGet(partData, '鏁忔劅搴﹀姞鎴?, 0), 0)), { path: [...nsfwPath, '韬綋閮ㄤ綅', partName, '鏁忔劅搴﹀姞鎴?], kind: 'number' })}</span>
                              </div>
                              <div class="nsfw-meta-item">
                                <b>鐘舵€佹弿杩?/b>
                                <span>${makeInlineEditableValue(toText(deepGet(partData, '鐘舵€佹弿杩?, '姝ｅ父'), '姝ｅ父'), { path: [...nsfwPath, '韬綋閮ㄤ綅', partName, '鐘舵€佹弿杩?], kind: 'string' })}</span>
                              </div>
                            </div>
                            <div class="nsfw-meta-item">
                              <b>浣撴恫娈嬬暀</b>
                              <span>${makeInlineEditableValue(toText(deepGet(partData, '浣撴恫娈嬬暀', '鏃?), '鏃?), { path: [...nsfwPath, '韬綋閮ㄤ綅', partName, '浣撴恫娈嬬暀'], kind: 'string' })}</span>
                            </div>
                          </div>
                        </div>
                      `).join('')}
                    </div>
                  ` : '<div class="dossier-empty-note">褰撳墠瑙掕壊灏氭湭褰曞叆韬綋閮ㄤ綅缁撴瀯銆?/div>'}
                </div>
              </div>

            </div>
          `
        };
      }



      if (previewKey === '鐢熷懡鍥捐氨璇︾粏椤?) {
        const activeCharKey = resolveSnapshotCharKey(snapshot, toText(snapshot.activeName, '')) || toText(snapshot.activeName, '');
        const trainedBonusPath = activeCharKey ? ['char', activeCharKey, '灞炴€?, '璁粌鍔犳垚'] : [];
        const trainedBonusItems = buildEditableStatBonusItems(trainedBonusPath, deepGet(snapshot, 'activeChar.灞炴€?璁粌鍔犳垚', {}), { includeSoulPower: false });
        const lifePreviewStats = activeCharKey ? resolveTrackedStatPreview(activeCharKey, stat) : createTrackedStatPreviewMap(stat);
        const currentLocText = toText(deepGet(snapshot, 'activeChar.鐘舵€?浣嶇疆', snapshot.currentLoc), snapshot.currentLoc || '鏈煡');
        const worldTimeText = getSnapshotWorldTimeText(snapshot);
        const actionText = toText(status.琛屽姩, '鏃ュ父');
        const activeWardrobeKey = toText(绌挎惌鏁版嵁.鐫€瑁? '').trim();
        const activeWardrobePath = activeWardrobeKey && activeWardrobeKey !== '鏃?
          ? ['char', activeCharKey, '绌挎惌', '琛ｆ煖', activeWardrobeKey]
          : null;
        const createAbilityPreview = activeCharKey
          ? buildSkillDesignerPreviewKey({
            path: ['char', activeCharKey, '鑷垱榄傛妧', `鑷垱榄傛妧_${Date.now().toString(36)}_${Math.random().toString(36).slice(2, 6)}`],
            label: '鏂板缓鑷垱榄傛妧',
            category: '鑷垱榄傛妧',
            scope: '鑷垱榄傛妧',
          })
          : '';
        const fusionArchiveMeta = getFusionArchiveMeta(snapshot);
        const fusionArchiveEntry = buildFusionArchiveListItem(fusionArchiveMeta);
        const nonFusionSkillEntries = (Array.isArray(snapshot.extraSkills) ? snapshot.extraSkills : [])
          .filter(skill => toText(skill && skill.category, '') !== '姝﹂瓊铻嶅悎鎶€');
        const artArchiveEntries = nonFusionSkillEntries
          .filter(skill => /鍔熸硶/.test(toText(skill && skill.category, '')))
          .map(skill => ({
            title: `${skill.category} - ${skill.name}`,
            desc: `${skill.level} 锝?${skill.desc}`,
            preview: ''
          }));
        const customSkillArchiveEntries = nonFusionSkillEntries
          .filter(skill => toText(skill && skill.category, '') === '鑷垱榄傛妧')
          .map(skill => ({
            title: `${skill.category} - ${skill.name}`,
            desc: `${skill.level} 锝?${skill.desc}`,
            preview: skill.preview || ''
          }));
        const bloodlineArchiveEntries = nonFusionSkillEntries
          .filter(skill => /^琛€鑴?.test(toText(skill && skill.category, '')))
          .map(skill => ({
            title: `${skill.category} - ${skill.name}`,
            desc: `${skill.level} 锝?${skill.desc}`,
            preview: skill.preview || ''
          }));
        return {
          title: '璇︾粏妗ｆ',
          summary: '瑙掕壊鐘舵€併€佷慨涓恒€佸瑙備笌鎴愰暱璁板綍銆?,
          body: `
            <div class="archive-modal-grid dossier-shell dossier-shell--life">
              <div class="archive-card dossier-card dossier-card--life-core">
                <div class="archive-card-head">
                  <div class="archive-card-title">鏍稿績鎴愰暱</div>
                  <span class="dossier-pill live">瀹炴椂浣撳緛</span>
                </div>
                <div class="dossier-columns dossier-columns--life-core">
                  <section class="dossier-section">
                    <div class="dossier-section-title">鎴愰暱淇℃伅</div>
                    ${makeDossierRows([
                      { label: '绛夌骇', value: htmlEscape(formatCultivationLevelBadge(stat.绛夌骇, '0')) },
                      { label: '绮剧澧冪晫', value: htmlEscape(toText(stat._men_realm, toText(stat.绮剧鍔沖realm, '鐏靛厓澧?))) },
                      { label: '澶╄祴姊槦', value: htmlEscape(toText(stat.talent_tier, '鏈畾')) },
                      { label: '绯诲埆', value: activeCharKey
                        ? makeInlineEditableValue(toText(stat.绯诲埆, '鏈煡'), {
                            path: ['char', activeCharKey, '灞炴€?, '绯诲埆'],
                            kind: 'string',
                            rawValue: toText(stat.绯诲埆, '鏈煡'),
                          })
                        : htmlEscape(toText(stat.绯诲埆, '鏈煡')) },
                      { label: '鍚嶆湜', value: htmlEscape(`${toText(social.鍚嶆湜绛夌骇, toText(social.鍚嶆湜绛夌骇, '绫嶇睄鏃犲悕'))} / ${formatNumber(social.澹版湜)}`) },
                    ], 'dossier-row-grid--two')}
                  </section>
                  <section class="dossier-section dossier-section--radar">
                    <div class="dossier-section-title">鎴樻枟杞粨</div>
                    <div class="battle-radar-wrap dossier-radar-wrap">
                      ${(() => {
                        const fStr = Math.max(0, toNumber(lifePreviewStats.鍔涢噺, stat.鍔涢噺));
                        const fDef = Math.max(0, toNumber(lifePreviewStats.闃插尽, stat.闃插尽));
                        const fAgi = Math.max(0, toNumber(lifePreviewStats.鏁忔嵎, stat.鏁忔嵎));
                        const fMen = Math.max(0, toNumber(lifePreviewStats.绮剧鍔涗笂闄? stat.绮剧鍔涗笂闄?);
                        const fVit = Math.max(0, toNumber(lifePreviewStats.浣撳姏涓婇檺, stat.浣撳姏涓婇檺));
                        const maxAxis = Math.max(1, fStr, fDef, fAgi, fMen, fVit);
                        return makeRadarSvg(['鍔涢噺', '闃插尽', '鏁忔嵎', '绮剧', '浣撳姏'], [
                          Math.max(12, Math.min(100, Math.round(fStr / maxAxis * 100))),
                          Math.max(12, Math.min(100, Math.round(fDef / maxAxis * 100))),
                          Math.max(12, Math.min(100, Math.round(fAgi / maxAxis * 100))),
                          Math.max(12, Math.min(100, Math.round(fMen / maxAxis * 100))),
                          Math.max(12, Math.min(100, Math.round(fVit / maxAxis * 100)))
                        ], [formatNumber(fStr), formatNumber(fDef), formatNumber(fAgi), formatNumber(fMen), formatNumber(fVit)]);
                      })()}
                    </div>
                  </section>
                </div>
                <section class="dossier-section">
                  <div class="dossier-section-title">褰撳墠鐘舵€?/div>
                  ${makeDossierRows([
                    { label: '琛屽姩鐘舵€?, value: activeCharKey
                      ? makeInlineEditableValue(actionText, {
                          path: ['char', activeCharKey, '鐘舵€?, '琛屽姩'],
                          kind: 'enum_select',
                          rawValue: actionText,
                          editorMeta: { options: getStatusActionEditorOptions(actionText) },
                        })
                      : htmlEscape(actionText) },
                    { label: '褰撳墠鍦扮偣', value: activeCharKey
                      ? makeInlineEditableValue(currentLocText, {
                          path: ['char', activeCharKey, '鐘舵€?, '浣嶇疆'],
                          kind: 'string',
                          rawValue: currentLocText,
                        })
                      : htmlEscape(currentLocText) },
                    { label: '褰撳墠鏃堕棿', value: htmlEscape(worldTimeText) },
                    { label: '浼ゅ娍', value: htmlEscape(getDisplayWoundLabel(stat)) },
                    { label: '榄傛牳鐘舵€?, value: activeCharKey
                      ? `${makeInlineEditableValue(String(toNumber(deepGet(snapshot, 'activeChar.榄傛牳.鏍稿績.鏁伴噺', 0), 0)), {
                          path: ['char', activeCharKey, '榄傛牳', '鏍稿績', '鏁伴噺'],
                          kind: 'number',
                          rawValue: toNumber(deepGet(snapshot, 'activeChar.榄傛牳.鏍稿績.鏁伴噺', 0), 0),
                          editorMeta: { min: 0, integer: true, hint: '鏈€灏?0 路 鏁存暟' },
                        })} 鏋?/ 杩涘害 ${makeInlineEditableValue(String(toNumber(deepGet(snapshot, 'activeChar.榄傛牳.鏍稿績.杩涘害', 0), 0)), {
                          path: ['char', activeCharKey, '榄傛牳', '鏍稿績', '杩涘害'],
                          kind: 'number',
                          rawValue: toNumber(deepGet(snapshot, 'activeChar.榄傛牳.鏍稿績.杩涘害', 0), 0),
                          editorMeta: { min: 0, max: 100, integer: true, hint: '鑼冨洿 0 - 100 路 鏁存暟' },
                        })}%`
                      : htmlEscape(`${toText(deepGet(snapshot, 'activeChar.榄傛牳.鏍稿績.鏁伴噺', 0), '0')} 鏋?/ 杩涘害 ${toText(deepGet(snapshot, 'activeChar.榄傛牳.鏍稿績.杩涘害', 0), '0')}%`) },
                    ...(snapshot.bloodline && snapshot.bloodline.valid ? [{ label: '琛€鑴夌姸鎬?, value: htmlEscape(`${snapshot.bloodline.bloodline} / ${snapshot.bloodline.sealLv}灞俙) }] : []),
                    { label: '鐏电墿鍚告敹', value: activeCharKey
                      ? `${makeInlineEditableValue(String(toNumber(status.鍚告敹鐏电墿骞撮檺, 0), 0), {
                          path: ['char', activeCharKey, '鐘舵€?, '鍚告敹鐏电墿骞撮檺'],
                          kind: 'number',
                          rawValue: toNumber(status.鍚告敹鐏电墿骞撮檺, 0),
                          editorMeta: { min: 0, integer: true, hint: '鏈€灏?0 路 鏁存暟' },
                        })} 骞碻
                      : htmlEscape(toNumber(status.鍚告敹鐏电墿骞撮檺, 0) > 0 ? `${formatNumber(toNumber(status.鍚告敹鐏电墿骞撮檺, 0))} 骞碻 : '褰撳墠鏃犲惛鏀?) },
                    { label: '鍙椾激閮ㄤ綅', value: buildInjuryCrudHtml(activeCharKey, status), className: 'dossier-row--wide' },
                  ], 'dossier-row-grid--two')}
                </section>
              </div>
              <div class="archive-card dossier-card dossier-card--life-profile">
                <div class="archive-card-head archive-card-head--profile">
                  <div class="archive-card-title">瑙掕壊妗ｆ</div>
                  <div class="dossier-profile-strip" aria-label="瑙掕壊褰撳墠鐘舵€佹憳瑕?>
                    <span class="dossier-profile-chip"><b>琛屽姩</b><em>${htmlEscape(actionText)}</em></span>
                    <span class="dossier-profile-chip dossier-profile-chip--wide"><b>鍦扮偣</b><em>${htmlEscape(currentLocText)}</em></span>
                    <span class="dossier-profile-chip"><b>鏃堕棿</b><em>${htmlEscape(worldTimeText)}</em></span>
                    <span class="dossier-profile-chip"><b>浼ゅ娍</b><em>${htmlEscape(getDisplayWoundLabel(stat))}</em></span>
                  </div>
                </div>
                <section class="dossier-section">
                  <div class="dossier-section-title">鍩虹鎻忚堪</div>
                  ${makeDossierRows([
                    { label: '骞撮緞 / 鎬у埆', value: `${makeInlineEditableValue(`${toText(stat.骞撮緞, '0')}宀乣, { path: ['char', activeCharKey, '灞炴€?, '骞撮緞'], kind: 'number', rawValue: stat.骞撮緞, editorMeta: { min: 0, integer: true, hint: '鏈€灏?0 路 鏁存暟' } })} / ${makeInlineEditableValue(toText(stat.鎬у埆, '鏈煡'), { path: ['char', activeCharKey, '灞炴€?, '鎬у埆'], kind: 'string', rawValue: stat.鎬у埆 })}` },
                    { label: '鎬ф牸', value: makeInlineEditableValue(snapshot.personalityText, { path: ['char', activeCharKey, '鎬ф牸'], kind: 'string', rawValue: deepGet(snapshot, 'activeChar.鎬ф牸', snapshot.personalityText) }) },
                  ], 'dossier-row-grid--two')}
                </section>
                <section class="dossier-section">
                  <div class="dossier-section-title">澶栬矊涓庣潃瑁?/div>
                  ${makeDossierRows([
                    { label: '鍙戣壊', value: makeInlineEditableValue(snapshot.appearanceMeta.hairColor, { path: ['char', activeCharKey, '澶栬矊', '鍙戣壊'], kind: 'string', rawValue: deepGet(snapshot, 'activeChar.澶栬矊.鍙戣壊', snapshot.appearanceMeta.hairColor) }) },
                    { label: '鍙戝瀷', value: makeInlineEditableValue(snapshot.appearanceMeta.hairStyle, { path: ['char', activeCharKey, '澶栬矊', '鍙戝瀷'], kind: 'string', rawValue: deepGet(snapshot, 'activeChar.澶栬矊.鍙戝瀷', snapshot.appearanceMeta.hairStyle) }) },
                    { label: '鐬宠壊', value: makeInlineEditableValue(snapshot.appearanceMeta.eyes, { path: ['char', activeCharKey, '澶栬矊', '鐬宠壊'], kind: 'string', rawValue: deepGet(snapshot, 'activeChar.澶栬矊.鐬宠壊', snapshot.appearanceMeta.eyes) }) },
                    { label: '韬珮', value: makeInlineEditableValue(snapshot.appearanceMeta.height, { path: ['char', activeCharKey, '澶栬矊', '韬珮'], kind: 'string', rawValue: deepGet(snapshot, 'activeChar.澶栬矊.韬珮', snapshot.appearanceMeta.height) }) },
                    { label: '浣撳瀷', value: makeInlineEditableValue(snapshot.appearanceMeta.build, { path: ['char', activeCharKey, '澶栬矊', '浣撳瀷'], kind: 'string', rawValue: deepGet(snapshot, 'activeChar.澶栬矊.浣撳瀷', snapshot.appearanceMeta.build) }) },
                    { label: '闀跨浉鎻忚堪', value: makeInlineEditableValue(snapshot.appearanceMeta.looks, { path: ['char', activeCharKey, '澶栬矊', '闀跨浉鎻忚堪'], kind: 'string', rawValue: deepGet(snapshot, 'activeChar.澶栬矊.闀跨浉鎻忚堪', snapshot.appearanceMeta.looks) }), className: 'dossier-row--wide' },
                    { label: '鐗瑰緛', value: makeInlineEditableValue(snapshot.appearanceMeta.features, { path: ['char', activeCharKey, '澶栬矊', '鐗规畩鐗瑰緛'], kind: 'string_list', rawValue: deepGet(snapshot, 'activeChar.澶栬矊.鐗规畩鐗瑰緛', []) }), className: 'dossier-row--wide' },
                    { label: '褰撳墠濂楄', value: makeInlineEditableValue(toText(绌挎惌鏁版嵁.鐫€瑁? '鏃?), { path: ['char', activeCharKey, '绌挎惌', '鐫€瑁?], kind: 'string', rawValue: 绌挎惌鏁版嵁.鐫€瑁?}), className: 'dossier-row--wide' },
                    { label: '琛ｆ煖濂楁暟', value: String(wardrobeEntries.length) },
                    { label: '涓婅', value: activeWardrobePath ? makeInlineEditableValue(toText(deepGet(绌挎惌鏁版嵁, ['琛ｆ煖', activeWardrobeKey, '涓婅'], '鏃?), '鏃?), { path: [...activeWardrobePath, '涓婅'], kind: 'string', rawValue: deepGet(绌挎惌鏁版嵁, ['琛ｆ煖', activeWardrobeKey, '涓婅'], '') }) : '鏃? },
                    { label: '涓嬭', value: activeWardrobePath ? makeInlineEditableValue(toText(deepGet(绌挎惌鏁版嵁, ['琛ｆ煖', activeWardrobeKey, '涓嬭'], '鏃?), '鏃?), { path: [...activeWardrobePath, '涓嬭'], kind: 'string', rawValue: deepGet(绌挎惌鏁版嵁, ['琛ｆ煖', activeWardrobeKey, '涓嬭'], '') }) : '鏃? },
                    { label: '闉嬪瓙', value: activeWardrobePath ? makeInlineEditableValue(toText(deepGet(绌挎惌鏁版嵁, ['琛ｆ煖', activeWardrobeKey, '闉嬪瓙'], '鏃?), '鏃?), { path: [...activeWardrobePath, '闉嬪瓙'], kind: 'string', rawValue: deepGet(绌挎惌鏁版嵁, ['琛ｆ煖', activeWardrobeKey, '闉嬪瓙'], '') }) : '鏃? },
                    { label: '濂楄鎻忚堪', value: activeWardrobePath ? makeInlineEditableValue(toText(deepGet(绌挎惌鏁版嵁, ['琛ｆ煖', activeWardrobeKey, '鎻忚堪'], '鏆傛棤鎻忚堪'), '鏆傛棤鎻忚堪'), { path: [...activeWardrobePath, '鎻忚堪'], kind: 'string', rawValue: deepGet(绌挎惌鏁版嵁, ['琛ｆ煖', activeWardrobeKey, '鎻忚堪'], '') }) : '鏆傛棤鎻忚堪', className: 'dossier-row--wide' },
                  ], 'dossier-row-grid--two')}
                </section>
                <section class="dossier-section">
                  <div class="dossier-section-title">琛ｆ煖璁板綍</div>
                  ${wardrobeEntries.length
                    ? makeDossierList(wardrobeEntries.map(([name, item]) => ({
                        title: name,
                        desc: `${toText(item && item['涓婅'], '鏃?)} / ${toText(item && item['涓嬭'], '鏃?)} / ${toText(item && item['闉嬪瓙'], '鏃?)} 锝?${toText(item && item['鎻忚堪'], '鏆傛棤鎻忚堪')}`
                      })), 'dossier-list--compact')
                    : '<div class="dossier-empty-note">褰撳墠瑙掕壊灏氭湭璁板綍鍙垏鎹㈠瑁呫€?/div>'}
                </section>
              </div>
              <div class="archive-card dossier-card dossier-card--life-record full">
                <div class="archive-card-head">
                  <div class="archive-card-title">鑳藉姏涓庢垚闀胯褰?/div>
                  <div class="dossier-tag-row dossier-head-actions">
                    <button type="button" class="dossier-pill ${fusionArchiveMeta.fusionEntries.length ? 'live' : 'warn'} clickable" data-preview="姝﹂瓊铻嶅悎鎶€璇︾粏椤?>姝﹂瓊铻嶅悎鎶€</button>
                    ${createAbilityPreview ? `<button type="button" class="dossier-pill live clickable" data-preview="${escapeHtmlAttr(createAbilityPreview)}">鏂板缓鑷垱榄傛妧</button>` : ''}
                  </div>
                </div>
                <div class="dossier-columns dossier-columns--life-record">
                  <section class="dossier-section">
                    <div class="dossier-section-title">棰濆鎴愰暱鍔犳垚</div>
                    ${makeDossierRows(trainedBonusItems, 'dossier-row-grid--three')}
                  </section>
                  <section class="dossier-section">
                    <div class="dossier-section-title">鍔熸硶</div>
                    ${makeDossierList(artArchiveEntries, 'dossier-list--compact') || '<div class="dossier-empty-note">鏆傛棤鍔熸硶璁板綍銆?/div>'}
                  </section>
                  <section class="dossier-section">
                    <div class="dossier-section-title">鑷垱榄傛妧</div>
                    ${makeDossierList(customSkillArchiveEntries, 'dossier-list--compact') || '<div class="dossier-empty-note">鏆傛棤鑷垱榄傛妧銆?/div>'}
                  </section>
                  <section class="dossier-section">
                    <div class="dossier-section-title">琛€鑴夎兘鍔?/div>
                    ${makeDossierList(bloodlineArchiveEntries, 'dossier-list--compact') || '<div class="dossier-empty-note">鏆傛棤琛€鑴夎兘鍔涖€?/div>'}
                  </section>
                </div>
              </div>
            </div>
          `
        };
      }

      if (previewKey === '绀句細妗ｆ璇︾粏椤?) {
        const activeCharKey = resolveSnapshotCharKey(snapshot, toText(snapshot.activeName, '')) || toText(snapshot.activeName, '');
        return {
          title: '绀句細妗ｆ寮圭獥',
          summary: '褰撳墠瑙掕壊鐨勫叕寮€韬唤銆佸悕鏈涖€佸ご琛斾笌绀句細鍙搴︽憳瑕併€?,
          body: `
            <div class="archive-modal-grid dossier-shell dossier-shell--social">
              <div class="archive-card dossier-card dossier-card--social-main">
                <div class="archive-card-head">
                  <div>
                    <div class="archive-card-title">韬唤鍗峰畻</div>
                    <div class="dossier-head-name">${htmlEscape(snapshot.activeName)}</div>
                  </div>
                  ${makeDossierTags([
                    { text: toText(social.鍚嶆湜绛夌骇, toText(social.鍚嶆湜绛夌骇, '绫嶇睄鏃犲悕')), className: 'warn' },
                    { text: (social.鍏紑鎯呮姤 ?? social.public_intel) ? '鍏紑鎯呮姤' : '鏈叕寮€' },
                    { text: `绉板彿 ${snapshot.recentTitles.length}` },
                  ])}
                </div>
                <section class="dossier-section">
                  <div class="dossier-section-title">鍏紑韬唤</div>
                  ${makeDossierRows([
                    { label: '褰撳墠韬唤', value: makeInlineEditableValue(toText(social.涓昏韩浠? '鏃?), { path: ['char', activeCharKey, '绀句氦', '涓昏韩浠?], kind: 'string', rawValue: social.涓昏韩浠?}) },
                    { label: '鍚嶆湜灞傜骇', value: htmlEscape(toText(social.鍚嶆湜绛夌骇, toText(social.鍚嶆湜绛夌骇, '绫嶇睄鏃犲悕'))) },
                    { label: '鍏紑鎯呮姤', value: htmlEscape(snapshot.publicIntel ? '宸插叕寮€' : '鏈叕寮€') },
                    { label: '涓昏鍦堝眰', value: htmlEscape(snapshot.鍔垮姏.map(([name]) => name).join(' / ') || '鏆傛棤') },
                    { label: '褰撳墠涓荤О鍙?, value: htmlEscape(snapshot.recentTitles[0] || '鏆傛棤') },
                    { label: '涓婚樀钀ヨ韩浠?, value: htmlEscape(snapshot.primaryFaction ? `${snapshot.primaryFaction[0]} / ${toText(deepGet(snapshot.primaryFaction[1], '韬唤', '鏃?), '鏃?)}` : '鏃?) },
                  ], 'dossier-row-grid--two')}
                </section>
                <section class="dossier-section">
                  <div class="dossier-section-title">绀句細鏍囩</div>
                  ${makeDossierTags([
                    { text: snapshot.activeName, className: 'live' },
                    { text: toText(social.涓昏韩浠? '鏃?) },
                    { text: snapshot.鍔垮姏.map(([name]) => name).join(' / ') || '鏃犲湀灞? },
                    { text: snapshot.recentTitles[0] || '鏆傛棤涓荤О鍙? },
                  ], 'dossier-tag-row--wrap')}
                </section>
              </div>
              <div class="archive-card dossier-card dossier-card--social-side">
                <div class="archive-card-head">
                  <div class="archive-card-title">鍚嶆湜鎽樿</div>
                  <span class="dossier-pill ${snapshot.publicIntel ? 'live' : 'warn'}">${htmlEscape(snapshot.publicIntel ? '妗ｆ宸插叕寮€' : '鏈叕寮€')}</span>
                </div>
                <section class="dossier-section">
                  <div class="dossier-section-title">涓汉澹版湜閲忕骇</div>
                  ${makeDossierMeter(
                    '褰撳墠澹版湜',
                    makeInlineEditableValue(`${formatNumber(social.澹版湜)} / 5,000 闃堝€糮, {
                      path: ['char', activeCharKey, '绀句氦', '澹版湜'],
                      kind: 'number',
                      rawValue: social.澹版湜,
                      editorMeta: { min: 0, integer: true, hint: '鏈€灏?0 路 鏁存暟 路 5,000 涓哄綋鍓嶉噺绾ч槇鍊煎弬鑰冿紝涓嶆槸纭笂闄? }
                    }),
                    ratioPercent(social.澹版湜, 5000),
                    'dossier-meter--accent'
                  )}
                </section>
                <section class="dossier-section">
                  <div class="dossier-section-title">绀句細浣嶇疆</div>
                  ${makeDossierRows([
                    { label: '澶磋鎬绘暟', value: String(snapshot.titleEntries.length) },
                    { label: '涓婚樀钀ヨ韩浠?, value: htmlEscape(snapshot.primaryFaction ? `${snapshot.primaryFaction[0]} / ${toText(deepGet(snapshot.primaryFaction[1], '韬唤', '鏃?), '鏃?)}` : '鏃?) },
                    { label: '妗ｆ鐘舵€?, value: htmlEscape(snapshot.publicIntel ? '鍏紑鍙' : '浠呭唴閮ㄥ彲瑙?) },
                    { label: '褰撳墠鎽樿', value: htmlEscape(toText(social.涓昏韩浠? '鏃?)) },
                  ], 'dossier-row-grid--single')}
                </section>
              </div>
            </div>
          `
        };
      }

      if (previewKey === '浜虹墿鍏崇郴璇︾粏椤?) {
        const activeCharKey = resolveSnapshotCharKey(snapshot, toText(snapshot.activeName, '')) || toText(snapshot.activeName, '');
        const relationPositions = [
          { left: 20, top: 25, className: 'hover-right' },
          { left: 50, top: 18, className: 'gold' },
          { left: 82, top: 25, className: 'hover-left' },
          { left: 25, top: 75, className: 'warn hover-up hover-right' },
          { left: 75, top: 75, className: 'hover-up hover-left' }
        ];
        const currentLocFull = toText(deepGet(snapshot, 'activeChar.鐘舵€?浣嶇疆', snapshot.currentLoc || '鏈煡鍦扮偣'), snapshot.currentLoc || '鏈煡鍦扮偣');
        const relationNodes = snapshot.relations.slice(0, 5);
        const relationLinks = relationPositions.slice(0, relationNodes.length).map((pos, index) => {
          const lineClass = index === 1 ? 'gold' : index === 3 ? 'alert' : 'cyan';
          return `<line class="topology-link ${lineClass}" x1="50" y1="50" x2="${pos.left}" y2="${pos.top}"></line>`;
        }).join('');
        const relationHtml = relationNodes.map(([name, rel], index) => {
          const pos = relationPositions[index];
          const 鐩稿叧搴﹁鍥?= 鏋勫缓鍏崇郴鐩稿叧搴﹁鍥炬暟鎹甠妗ユ帴(rel);
          return `
            <div class="topology-node interactive-ring ${pos.className}" data-relation-focus="${escapeHtmlAttr(name)}" style="left:${pos.left}%;top:${pos.top}%">
              <b>${htmlEscape(name)}</b><span>${htmlEscape(toText(rel && rel['鍏崇郴'], '闄岀敓'))}</span>
              <div class="relation-hover-card">
                <span class="relation-hover-title">${htmlEscape(name)}</span>
                <span class="relation-hover-desc">${htmlEscape(`${toText(rel && rel['鍏崇郴'], '闄岀敓')} / ${toText(rel && rel['鍏崇郴璺嚎'], '鏈嬪弸绾?)}`)}</span>
                <div class="relation-hover-tags">
                  <span class="relation-hover-chip">${htmlEscape(`濂芥劅 ${鐩稿叧搴﹁鍥?濂芥劅搴`)}</span>
                  <span class="relation-hover-chip">${htmlEscape(toText(rel && rel['瀵规柟韬唤'], '鏈煡韬唤'))}</span>
                  <span class="relation-hover-chip">${htmlEscape(`鐩稿叧搴?${鐩稿叧搴﹁鍥?鎬诲垎鍊紏`)}</span>
                </div>
                <div class="relation-hover-skill">
                  <span>${htmlEscape(toText(rel && rel['濂芥劅鍔犳垚'], '鏆傛棤棰濆鍏崇郴鍔犳垚璇存槑'))}</span>
                  <span>${htmlEscape(toText(rel && rel['_鎺ㄨ繘鎻愮ず'], '鏆傛棤鎺ㄨ繘鎻愮ず'))}</span>
                  <span>${htmlEscape(`涓嬩竴闃舵锛?{toText(rel && rel['_涓嬩竴闃舵'], '鏃?)} / ${toNumber(rel && rel['_涓嬩竴闃舵闃堝€?], 0)}`)}</span>
                  <span>${htmlEscape(`鏈€杩戜簰鍔細${toText(rel && rel['涓婃浜掑姩鍔ㄤ綔'], '鏃?)} / ${toNumber(rel && rel['鏈€杩戝ソ鎰熷彉鍖?], 0)}`)}</span>
                  <span>${htmlEscape(`褰撳墠鍔犳垚锛?{toText(rel && rel['_褰撳墠鍏崇郴鍔犳垚'], toText(rel && rel['濂芥劅鍔犳垚'], '鏃?))}`)}</span>
                  <span>${htmlEscape(`涓嬩竴瑙ｉ攣锛?{toText(rel && rel['_涓嬫。瑙ｉ攣鍔犳垚'], '鏃?)}`)}</span>
                  <span>${htmlEscape(`鍏崇郴鐘舵€侊細${toText(rel && rel['_缁存姢浼樺厛绾?], '鏈煡')} / ${toText(rel && rel['_鍒囩嚎闄愬埗鍘熷洜'], '鏃?)}`)}</span>
                  <span>${htmlEscape(`鍩虹鐩稿叧搴︼細${鐩稿叧搴﹁鍥?鍩虹鏄剧ず鍊紏 / 鍏崇郴鍔犳垚锛?${鐩稿叧搴﹁鍥?鍏崇郴鍔犳垚鍊紏`)}</span>
                  <span>${htmlEscape(`铻嶅悎鍒ゅ畾锛?{鐩稿叧搴﹁鍥?铻嶅悎瑙﹀彂鐘舵€亇 (鎬诲垎${鐩稿叧搴﹁鍥?鎬诲垎鍊紏)`)}</span>
                  <span>${htmlEscape(`鍚屼慨鏁堢巼锛歺${鐩稿叧搴﹁鍥?鍚屼慨鏁堢巼鍊嶇巼}`)}</span>
                </div>
                <div class="energy-stack">
                  <div class="energy-row-block">
                    <div class="energy-headline"><b>濂芥劅搴?/b><span>${htmlEscape(`${鐩稿叧搴﹁鍥?濂芥劅搴 / ${toText(rel && rel['鍏崇郴'], '闄岀敓')}`)}</span></div>
                    <div class="energy-track"><div class="energy-fill ${鐩稿叧搴﹁鍥?濂芥劅搴?>= 80 ? 'red' : 鐩稿叧搴﹁鍥?濂芥劅搴?>= 50 ? 'gold' : 'cyan'}" style="width:${Math.max(5, Math.min(100, 鐩稿叧搴﹁鍥?濂芥劅搴?)}%"></div></div>
                  </div>
                </div>
              </div>
            </div>
          `;
        }).join('');

        const relationFocusTargets = Array.isArray(deepGet(snapshot, '鍏崇郴鍒嗘瀽.閲嶇偣瀵硅薄', []))
          ? deepGet(snapshot, '鍏崇郴鍒嗘瀽.閲嶇偣瀵硅薄', [])
          : [];
        const relationDirectoryPage = paginateModalItems(snapshot.relations, previewKey, 'relation-directory', 6);
        const relationFocusStateKey = `${previewKey}::relation-focus`;
        let relationDetailName = toText(modalFocusState[relationFocusStateKey], toText(deepGet(snapshot, '鍏崇郴鍒嗘瀽.鍏虫敞瀵硅薄', deepGet(relationFocusTargets, '0.target', relationNodes[0] ? relationNodes[0][0] : '')), ''));
        if (!snapshot.relations.some(([entryName]) => entryName === relationDetailName)) relationDetailName = snapshot.relations[0] ? snapshot.relations[0][0] : '';
        if (relationDetailName) modalFocusState[relationFocusStateKey] = relationDetailName;
        else delete modalFocusState[relationFocusStateKey];

        const relationDetailEntry = relationDetailName ? snapshot.relations.find(([entryName]) => entryName === relationDetailName) : null;
        const relationDetail = relationDetailEntry && relationDetailEntry[1];
        const relationDetailPath = relationDetailName && activeCharKey ? ['char', activeCharKey, '绀句氦', '鍏崇郴', relationDetailName] : [];
        const resolvedTarget = relationDetailName ? resolveSnapshotCharacter(snapshot, relationDetailName) : { key: '', displayName: '', char: null };
        const relationTargetChar = resolvedTarget.char || null;
        const relationTargetLoc = toText(deepGet(relationTargetChar, '鐘舵€?浣嶇疆', ''), '');
        const relationTargetLocLabel = relationTargetLoc ? relationTargetLoc.replace(/^鏂楃綏澶ч檰-/, '').replace(/^鏂楃伒澶ч檰-/, '') : '鏈煡鍦扮偣';
        const 鍏崇郴鐩稿叧搴﹁鍥?= 鏋勫缓鍏崇郴鐩稿叧搴﹁鍥炬暟鎹甠妗ユ帴(relationDetail);
        const relationFavor = 鍏崇郴鐩稿叧搴﹁鍥?濂芥劅搴?
        const relationRoute = toText(relationDetail && relationDetail['鍏崇郴璺嚎'], '鏈嬪弸绾?);
        const 姝﹂瓊鐩稿叧搴﹀熀纭€鍊?= 鍏崇郴鐩稿叧搴﹁鍥?鍩虹鍊?
        const 姝﹂瓊鐩稿叧搴﹀熀纭€鏄剧ず鍊?= 鍏崇郴鐩稿叧搴﹁鍥?鍩虹鏄剧ず鍊?
        const 姝﹂瓊鐩稿叧搴﹀叧绯诲姞鎴愬€?= 鍏崇郴鐩稿叧搴﹁鍥?鍏崇郴鍔犳垚鍊?
        const 姝﹂瓊鐩稿叧搴︽€诲垎鍊?= 鍏崇郴鐩稿叧搴﹁鍥?鎬诲垎鍊?
        const 铻嶅悎瑙﹀彂鐘舵€佸€?= 鍏崇郴鐩稿叧搴﹁鍥?铻嶅悎瑙﹀彂鐘舵€?
        const 鍚屼慨鏁堢巼鍊嶇巼鍊?= 鍏崇郴鐩稿叧搴﹁鍥?鍚屼慨鏁堢巼鍊嶇巼;
        const routeSwitchable = !!deepGet(relationDetail, '_鍙垏绾?, false);
        const isContactable = !!relationTargetChar && deepGet(relationTargetChar, '鐘舵€?瀛樻椿', true) !== false;
        const isSameLocation = !!relationTargetChar && isLocationCompatible(currentLocFull, relationTargetLoc);
        const isPlayerControlled = isSnapshotPlayerControlled(snapshot);
        const canTalk = isSameLocation && isContactable;
        const canAsk = isSameLocation && isContactable && relationFavor >= 30;
        const canBattle = isSameLocation && isContactable;
        const canConfess = isSameLocation && isContactable && (relationRoute === '鎭嬩汉绾? || routeSwitchable || relationFavor >= 80);
        const canDual = isSameLocation && isContactable && relationRoute === '鎭嬩汉绾? && relationFavor >= 80;
        const giftableItems = (snapshot.inventoryEntries || []).filter(([, item]) => toNumber(item && item['鏁伴噺'], 0) > 0).slice(0, 30);
        const canGift = isSameLocation && isContactable && giftableItems.length > 0;
        const giftOptionsHtml = giftableItems.length
          ? giftableItems.map(([itemName, item]) => `<option value="${escapeHtmlAttr(itemName)}">${htmlEscape(`${itemName} 脳${toNumber(item && item['鏁伴噺'], 0)}`)}</option>`).join('')
          : '<option value="">鏆傛棤鍙€佺墿鍝?/option>';

        const relationDirectoryHtml = relationDirectoryPage.items.length
          ? relationDirectoryPage.items.map(([name, rel]) => {
              const resolved = resolveSnapshotCharacter(snapshot, name);
              const targetChar = resolved.char || null;
              const targetLoc = toText(deepGet(targetChar, '鐘舵€?浣嶇疆', ''), '');
              const alive = !!targetChar && deepGet(targetChar, '鐘舵€?瀛樻椿', true) !== false;
              const sameLocation = !!targetChar && isLocationCompatible(currentLocFull, targetLoc);
              const favor = toNumber(rel && rel['濂芥劅搴?], 0);
              const stage = toText(rel && rel['鍏崇郴'], '闄岀敓');
              const route = toText(rel && rel['鍏崇郴璺嚎'], '鏈嬪弸绾?);
              const availability = sameLocation ? '鍦ㄥ満鍙帴瑙? : (alive ? '杩滅瀛樻椿' : '涓嶅彲鎺ヨЕ');
              const availabilityClass = sameLocation ? 'live' : (alive ? 'warn' : '');
              const progressNote = toText(rel && rel['_鎺ㄨ繘鎻愮ず'], '鏆傛棤鎺ㄨ繘鎻愮ず');
              return `
                <button type="button" class="dossier-list-row dossier-list-row--button relation-directory-item ${name === relationDetailName ? 'active' : ''}" data-relation-focus="${escapeHtmlAttr(name)}">
                  <b>${htmlEscape(name)}</b>
                  <span>
                    <strong>${htmlEscape(toText(rel && rel['瀵规柟韬唤'], '鏈煡韬唤'))}</strong>
                    <em>${htmlEscape(`${stage} / ${route} / 濂芥劅 ${favor}`)}</em>
                    <small class="${availabilityClass}">${htmlEscape(`${availability} 锝?${progressNote}`)}</small>
                  </span>
                </button>
              `;
            }).join('')
          : '<div class="dossier-empty-note">鍏崇郴缃戠粶灏氭湭灞曞紑銆?/div>';

        const relationFocusHtml = (relationFocusTargets.length
          ? makeDossierList(relationFocusTargets.slice(0, 2).map(item => {
              const targetName = toText(item && item.瀵硅薄, '鏈煡瀵硅薄');
              const detailEntry = snapshot.relations.find(([entryName]) => entryName === targetName);
              const detail = detailEntry && detailEntry[1];
              return {
                title: `${targetName} / ${toText(item && item.鍏崇郴, '闄岀敓')}`,
                desc: `${toText(item && item.鍘熷洜, '鏆傛棤鎺ㄨ繘寤鸿')} 锝?寤鸿锛?{toText(item && item.寤鸿琛屽姩, '缁х画瑙傚療')} 锝?涓嬩竴闃舵锛?{toText(detail && detail['_涓嬩竴闃舵'], '鏃?)} / ${toNumber(detail && detail['_涓嬩竴闃舵闃堝€?], 0)}`,
              };
            }), 'dossier-list--compact')
          : makeDossierList(snapshot.relations.slice(0, 2).map(([name, rel]) => ({
              title: `${name} / ${toText(rel && rel['鍏崇郴'], '闄岀敓')}`,
              desc: `璺嚎锛?{toText(rel && rel['鍏崇郴璺嚎'], '鏈嬪弸绾?)} / 濂芥劅锛?{toText(rel && rel['濂芥劅搴?], 0)} 锝?涓嬩竴闃舵锛?{toText(rel && rel['_涓嬩竴闃舵'], '鏃?)} / ${toNumber(rel && rel['_涓嬩竴闃舵闃堝€?], 0)}`,
            })), 'dossier-list--compact')) || '<div class="dossier-empty-note">鍏崇郴绾跨储浠嶅湪閾洪檲銆?/div>';

        const sameLocationTargets = Array.isArray(deepGet(snapshot, '鍏崇郴鍒嗘瀽.鍚屽湴瀵硅薄', [])) ? deepGet(snapshot, '鍏崇郴鍒嗘瀽.鍚屽湴瀵硅薄', []) : [];
        const trustTargets = Array.isArray(deepGet(snapshot, '鍏崇郴鍒嗘瀽.淇′换瀵硅薄', [])) ? deepGet(snapshot, '鍏崇郴鍒嗘瀽.淇′换瀵硅薄', []) : [];
        const recommendedActions = Array.from(new Set(
          relationFocusTargets
            .map(item => toText(item && item.寤鸿琛屽姩, ''))
            .filter(Boolean),
        )).slice(0, 3);
        const relationSummaryText = relationFocusTargets.length
          ? `褰撳墠閲嶇偣鍏崇郴瀵硅薄锛?{relationFocusTargets.slice(0, 2).map(item => `${toText(item && item.瀵硅薄, '鏃?)}(${toText(item && item.鍏崇郴, '闄岀敓')}/${toNumber(item && item.濂芥劅搴? 0)})`).join('銆?)}`
          : sameLocationTargets.length
            ? `褰撳墠鍙珛鍗虫帴瑙︼細${sameLocationTargets.slice(0, 3).join('銆?)}`
            : recommendedActions.length
              ? `褰撳墠寤鸿锛?{recommendedActions.slice(0, 2).join('锛?)}`
              : (trustTargets.length ? `楂樹俊浠诲璞★細${trustTargets.slice(0, 3).join('銆?)}` : '褰撳墠闆疯揪鏈壂鎻忓埌瓒冲鐨勭ぞ浼氶摼鎺ユ暟鎹紝鏆傛棤鎬讳綋鍒嗘瀽鍊惧悜銆?);
        const relationOverviewRowsHtml = makeDossierRows([
          { label: '鐒︾偣瀵硅薄', value: htmlEscape(toText(deepGet(snapshot, '鍏崇郴鍒嗘瀽.鍏虫敞瀵硅薄', relationDetailName || '鏃?), relationDetailName || '鏃?)) },
          { label: '绯荤粺寤鸿', value: htmlEscape(recommendedActions.slice(0, 3).join(' / ') || '鏃?) },
          { label: '鍦ㄥ満鐩爣', value: htmlEscape(sameLocationTargets.slice(0, 4).join(' / ') || '鏃?) },
        ], 'dossier-row-grid--two');
        const 鍏崇郴蹇収鎸囨爣HTML = [
          ['瀵硅薄', `${snapshot.relations.length}鍚峘],
          ['鍚屽湴', `${sameLocationTargets.length}鍚峘],
          ['淇′换', `${trustTargets.length}鍚峘],
        ].map(([鏍囩, 鏁板€糫) => `
          <span class="relation-overview-meter"><b>${htmlEscape(鏍囩)}</b><em>${htmlEscape(鏁板€?}</em></span>
        `).join('');
        const relationTargetSummaryHtml = relationDetail
          ? makeDossierRows([
              { label: '鐩爣瀵硅薄', value: htmlEscape(relationDetailName) },
              { label: '鍏崇郴', value: relationDetailPath.length
                ? makeInlineEditableValue(toText(relationDetail && relationDetail['鍏崇郴'], '闄岀敓'), {
                    path: [...relationDetailPath, '鍏崇郴'],
                    kind: 'string',
                    rawValue: toText(relationDetail && relationDetail['鍏崇郴'], '闄岀敓'),
                  })
                : htmlEscape(toText(relationDetail && relationDetail['鍏崇郴'], '闄岀敓')) },
              { label: '璺嚎', value: relationDetailPath.length
                ? makeInlineEditableValue(relationRoute, {
                    path: [...relationDetailPath, '鍏崇郴璺嚎'],
                    kind: 'enum_select',
                    rawValue: relationRoute,
                    editorMeta: { options: ['鏈嬪弸绾?, '鎭嬩汉绾?] },
                  })
                : htmlEscape(relationRoute) },
              { label: '濂芥劅搴?, value: relationDetailPath.length
                ? makeInlineEditableValue(String(relationFavor), {
                    path: [...relationDetailPath, '濂芥劅搴?],
                    kind: 'number',
                    rawValue: relationFavor,
                    editorMeta: { integer: true, hint: '鏁存暟 路 鍙鍙礋 路 涓嶈纭笂涓嬮檺' },
                  })
                : htmlEscape(String(relationFavor)) },
              { label: '瀵规柟韬唤', value: relationDetailPath.length
                ? makeInlineEditableValue(toText(relationDetail && relationDetail['瀵规柟韬唤'], '鏃?), {
                    path: [...relationDetailPath, '瀵规柟韬唤'],
                    kind: 'string',
                    rawValue: toText(relationDetail && relationDetail['瀵规柟韬唤'], '鏃?),
                  })
                : htmlEscape(toText(relationDetail && relationDetail['瀵规柟韬唤'], '鏃?)) },
              { label: '鍩虹鐩稿叧搴?, value: relationDetailPath.length
                ? (姝﹂瓊鐩稿叧搴﹀熀纭€鍊?== null
                  ? makeInlineEditableValue(姝﹂瓊鐩稿叧搴﹀熀纭€鏄剧ず鍊? {
                      path: [...relationDetailPath, '姝﹂瓊鐩稿叧搴﹀熀纭€'],
                      kind: 'string',
                      rawValue: 姝﹂瓊鐩稿叧搴﹀熀纭€鏄剧ず鍊?
                    })
                  : makeInlineEditableValue(String(姝﹂瓊鐩稿叧搴﹀熀纭€鍊?, {
                      path: [...relationDetailPath, '姝﹂瓊鐩稿叧搴﹀熀纭€'],
                      kind: 'number',
                      rawValue: 姝﹂瓊鐩稿叧搴﹀熀纭€鍊?
                      editorMeta: { min: 0, max: 100, integer: true, hint: '0-100鏁存暟' },
                    }))
                : htmlEscape(姝﹂瓊鐩稿叧搴﹀熀纭€鏄剧ず鍊? },
              { label: '鍏崇郴鍔犳垚', value: htmlEscape(`+${姝﹂瓊鐩稿叧搴﹀叧绯诲姞鎴愬€紏`) },
              { label: '鎬荤浉鍏冲害', value: htmlEscape(`${姝﹂瓊鐩稿叧搴︽€诲垎鍊紏`) },
              { label: '铻嶅悎瑙﹀彂', value: htmlEscape(`${铻嶅悎瑙﹀彂鐘舵€佸€紏 / 闃堝€?0`) },
              { label: '鍚屼慨鏁堢巼', value: htmlEscape(`x${鍚屼慨鏁堢巼鍊嶇巼鍊紏`) },
              { label: '浣嶇疆鐘舵€?, value: htmlEscape(`${relationTargetLocLabel} / ${isSameLocation ? '鍚屽湴鍙帴瑙? : (isContactable ? '杩滅鍙仈绯? : '褰撳墠涓嶅彲鎺ヨЕ')}`) },
              { label: '鎺ㄨ繘鎻愮ず', value: htmlEscape(toText(relationDetail && relationDetail['_鎺ㄨ繘鎻愮ず'], '鏆傛棤')) },
              { label: '鍏崇郴鐘舵€?, value: htmlEscape(`${toText(relationDetail && relationDetail['_缁存姢浼樺厛绾?], '鏈煡')} / ${toText(relationDetail && relationDetail['_鍒囩嚎闄愬埗鍘熷洜'], '鏃?)}`) },
            ], 'dossier-row-grid--two')
          : '<div class="dossier-empty-note">鍏堜粠瀵硅薄鍒楄〃閲岄€夋嫨涓€涓洰鏍囥€?/div>';
        const relationRecordRowsHtml = relationDetail
          ? makeDossierRows([
              { label: '鍏崇郴鍔犳垚', value: relationDetailPath.length
                ? makeInlineEditableValue(toText(relationDetail && relationDetail['濂芥劅鍔犳垚'], '鏃?), {
                    path: [...relationDetailPath, '濂芥劅鍔犳垚'],
                    kind: 'string',
                    rawValue: toText(relationDetail && relationDetail['濂芥劅鍔犳垚'], '鏃?),
                  })
                : htmlEscape(toText(relationDetail && relationDetail['濂芥劅鍔犳垚'], '鏃?)) },
              { label: '褰撳墠鍔犳垚', value: htmlEscape(toText(relationDetail && relationDetail['_褰撳墠鍏崇郴鍔犳垚'], '鏃?)) },
              { label: '涓嬩竴瑙ｉ攣', value: htmlEscape(toText(relationDetail && relationDetail['_涓嬫。瑙ｉ攣鍔犳垚'], '鏃?)) },
              { label: '涓嬩竴闃舵', value: htmlEscape(`${toText(relationDetail && relationDetail['_涓嬩竴闃舵'], '鏃?)} / ${toNumber(relationDetail && relationDetail['_涓嬩竴闃舵闃堝€?], 0)}`) },
              { label: '鏈€杩戜簰鍔?, value: relationDetailPath.length
                ? makeInlineEditableValue(toText(relationDetail && relationDetail['涓婃浜掑姩鍔ㄤ綔'], '鏃?), {
                    path: [...relationDetailPath, '涓婃浜掑姩鍔ㄤ綔'],
                    kind: 'string',
                    rawValue: toText(relationDetail && relationDetail['涓婃浜掑姩鍔ㄤ綔'], '鏃?),
                  })
                : htmlEscape(toText(relationDetail && relationDetail['涓婃浜掑姩鍔ㄤ綔'], '鏃?)) },
              { label: '鏈€杩戝彉鍔?, value: relationDetailPath.length
                ? makeInlineEditableValue(String(toNumber(relationDetail && relationDetail['鏈€杩戝ソ鎰熷彉鍖?], 0)), {
                    path: [...relationDetailPath, '鏈€杩戝ソ鎰熷彉鍖?],
                    kind: 'number',
                    rawValue: toNumber(relationDetail && relationDetail['鏈€杩戝ソ鎰熷彉鍖?], 0),
                    editorMeta: { integer: true, hint: '鏁存暟 路 鍙鍙礋 路 涓嶈纭笂涓嬮檺' },
                  })
                : htmlEscape(String(toNumber(relationDetail && relationDetail['鏈€杩戝ソ鎰熷彉鍖?], 0))) },
              { label: '涓婃浜掑姩Tick', value: relationDetailPath.length
                ? makeInlineEditableValue(String(toNumber(relationDetail && relationDetail['涓婃浜掑姩tick'], 0)), {
                    path: [...relationDetailPath, '涓婃浜掑姩tick'],
                    kind: 'number',
                    rawValue: toNumber(relationDetail && relationDetail['涓婃浜掑姩tick'], 0),
                    editorMeta: { min: 0, integer: true, hint: '鏈€灏?0 路 鏁存暟' },
                  })
                : htmlEscape(String(toNumber(relationDetail && relationDetail['涓婃浜掑姩tick'], 0))) },
              { label: '鎺ㄨ繘璺嚎', value: htmlEscape(routeSwitchable ? '鍙垏鎭嬩汉绾? : relationRoute) },
            ], 'dossier-row-grid--two')
          : '';
        const relationActionSummaryHtml = relationDetail
          ? (isPlayerControlled
            ? `
                ${makeDossierTags([
                  { text: isSameLocation ? '鍚屽湴' : '鏈湪韬竟', className: isSameLocation ? 'live' : 'warn' },
                  { text: isContactable ? '鍙帴瑙? : '涓嶅彲鎺ヨЕ', className: isContactable ? 'live' : 'warn' },
                  { text: routeSwitchable ? '鍙垏鎭嬩汉绾? : relationRoute },
                ], 'dossier-tag-row--wrap')}
                <div class="dossier-action-toolbar">
                  <button type="button" class="relation-action-btn action-primary" data-relation-action="talk" data-relation-target="${escapeHtmlAttr(relationDetailName)}" ${!canTalk ? 'disabled' : ''}>闂茶亰</button>
                  <button type="button" class="relation-action-btn action-primary" data-relation-action="ask" data-relation-target="${escapeHtmlAttr(relationDetailName)}" ${!canAsk ? 'disabled' : ''}>璇锋暀</button>
                  <button type="button" class="relation-action-btn action-warn" data-relation-action="battle" data-relation-target="${escapeHtmlAttr(relationDetailName)}" ${!canBattle ? 'disabled' : ''}>鍒囩</button>
                  <button type="button" class="relation-action-btn action-accent" data-relation-action="confess" data-relation-target="${escapeHtmlAttr(relationDetailName)}" ${!canConfess ? 'disabled' : ''}>琛ㄧ櫧</button>
                  <button type="button" class="relation-action-btn action-accent" data-relation-action="dual" data-relation-target="${escapeHtmlAttr(relationDetailName)}" ${!canDual ? 'disabled' : ''}>鍙屼慨</button>
                </div>
                <div class="relation-gift-row dossier-form-row">
                  <select class="relation-gift-select" ${!canGift ? 'disabled' : ''}>
                    ${giftOptionsHtml}
                  </select>
                  <button type="button" class="relation-action-btn action-primary" data-relation-action="gift" data-relation-target="${escapeHtmlAttr(relationDetailName)}" ${!canGift ? 'disabled' : ''}>閫佺ぜ</button>
                </div>
              `
            : `
                ${makeDossierTags([
                  { text: relationRoute },
                  { text: isSameLocation ? '鍚屽湴鍙' : '杩滅瑙傚療', className: isSameLocation ? 'live' : 'warn' },
                ], 'dossier-tag-row--wrap')}
              `)
          : '<div class="dossier-empty-note">褰撳墠娌℃湁鍙搷浣滅殑鍏崇郴鐩爣銆?/div>';

        return {
          title: '浜虹墿鍏崇郴寮圭獥',
          summary: '鍏ㄥ眬绀句細閾捐矾鎵弿銆佸綋鍓嶉噸鐐瑰璞＄洃鎺т笌鏅鸿兘鍏崇郴琛屽姩鎺ㄨ崘銆?,
          onMount: () => {
            const 褰撳墠蹇収 = liveSnapshot || lastRenderableSnapshot;
            if (!褰撳墠蹇収 || !activeCharKey || !relationDetailName) return null;
            void 灏濊瘯瑙﹀彂姝﹂瓊鐩稿叧搴﹁嚜鍔ㄥ垽瀹?褰撳墠蹇収, activeCharKey, relationDetailName);
            return null;
          },
          body: `
            <div class="archive-modal-grid dossier-shell dossier-shell--relation">
              <div class="archive-card dossier-card dossier-card--relation-overview full">
                <div class="archive-card-head"><div class="archive-card-title">鍏崇郴姒傝</div><span class="dossier-pill live">瀹炴椂鎷撴墤</span></div>
                <div class="dossier-columns dossier-columns--relation-overview">
                  <div class="relation-topology-panel">
                    <div class="topology-board relation-topology-board dossier-topology-board">
                      <svg class="topology-svg" viewBox="0 0 100 100" preserveAspectRatio="none">${relationLinks}</svg>
                      <div class="topology-node center" style="left:50%;top:50%"><b>${htmlEscape(snapshot.activeName)}</b><span>鑷垜</span></div>
                      ${relationHtml || '<div class="topology-node" style="left:50%;top:18%"><b>鏆傛棤鍏崇郴瀵硅薄</b><span>鍏崇郴缃戠粶灏氭湭灞曞紑</span></div>'}
                    </div>
                    <div class="relation-overview-meter-row">${鍏崇郴蹇収鎸囨爣HTML}</div>
                  </div>
                  <div class="dossier-section-stack">
                    <section class="dossier-section">
                      <div class="dossier-section-title">鍏崇郴鎬佸娍</div>
                      <div class="dossier-note dossier-note--dense">${htmlEscape(toText(relationSummaryText, '鍏崇郴鏁版嵁涓嶈冻'))}</div>
                      ${relationOverviewRowsHtml}
                    </section>
                    <section class="dossier-section">
                      <div class="dossier-section-title">鎺ㄨ繘绾跨储</div>
                      ${relationFocusHtml}
                    </section>
                  </div>
                </div>
              </div>
              <div class="archive-card dossier-card dossier-card--relation-directory">
                <div class="archive-card-head"><div class="archive-card-title">瀵硅薄鍒楄〃</div><span class="dossier-pill ${relationDirectoryPage.total ? 'live' : 'warn'}">${relationDirectoryPage.total ? `宸叉敹褰?${relationDirectoryPage.total} 鍚峘 : '鏆傛棤瀵硅薄'}</span></div>
                <div class="dossier-section">
                  ${relationDirectoryHtml}
                </div>
                ${makeModalPaginationControls('relation-directory', relationDirectoryPage.page, relationDirectoryPage.totalPages, relationDirectoryPage.total)}
              </div>
              <div class="archive-card dossier-card dossier-card--relation-focus">
                <div class="archive-card-head"><div class="archive-card-title">鐩爣鍗峰畻</div><span class="dossier-pill ${relationDetailName ? 'live' : 'warn'}">${htmlEscape(relationDetailName || '鏈€変腑')}</span></div>
                <section class="dossier-section">
                  <div class="dossier-section-title">鍏崇郴鎽樿</div>
                  ${relationTargetSummaryHtml}
                </section>
                <section class="dossier-section">
                  <div class="dossier-section-title">${htmlEscape(isPlayerControlled ? '浜掑姩鎿嶄綔' : '瑙傚療澶囨敞')}</div>
                  ${relationActionSummaryHtml}
                </section>
                <section class="dossier-section">
                  <div class="dossier-section-title">鍏崇郴璁板綍</div>
                  ${relationRecordRowsHtml || '<div class="dossier-empty-note">褰撳墠娌℃湁鍙睍绀虹殑鎺ㄨ繘璁板綍銆?/div>'}
                </section>
              </div>
            </div>
          `
        };
      }


      if (previewKey === '鎯呮姤搴撹缁嗛〉') {
        const activeCharKey = resolveSnapshotCharKey(snapshot, toText(snapshot.activeName, '')) || toText(snapshot.activeName, '');
        const intelNodes = snapshot.unlockedKnowledges.slice(-5).reverse().filter(item => item && item !== '鏃?);
        const coreIntel = intelNodes[0] || '鏆傛棤鏍稿績鎯呮姤';
        const sideIntels = intelNodes.slice(1, 5);
        const unlockedIntelEntries = (snapshot.unlockedKnowledges || []).map((text, index) => ({ text, index })).slice().reverse();
        const unlockedIntelPageSize = 6;
        const unlockedIntelPage = paginateModalItems(unlockedIntelEntries, previewKey, 'intel-records', unlockedIntelPageSize);
        const latestUnlockedIntel = snapshot.unlockedKnowledges.length
          ? toText(snapshot.unlockedKnowledges[snapshot.unlockedKnowledges.length - 1], '鏆傛棤')
          : '鏆傛棤';
        const intelOverviewText = latestUnlockedIntel;
        const combatHistoryTotalCount = snapshot.combatHistoryEntries.reduce((total, [, info]) => total + Math.max(0, toNumber(info && info.娆℃暟, 0)), 0);
        const latestCombatEntry = snapshot.combatHistoryEntries.length ? snapshot.combatHistoryEntries[0] : null;
        const latestCombatTarget = latestCombatEntry ? toText(latestCombatEntry[0], '鏆傛棤') : '鏆傛棤';
        const latestCombatResult = latestCombatEntry ? toText(latestCombatEntry[1] && latestCombatEntry[1].last_result, '鏈褰?) : '鏈褰?;
        const combatHistoryCards = (snapshot.combatHistoryEntries.length ? snapshot.combatHistoryEntries.slice(0, 8) : [['鏆傛棤浜ゆ垬妗ｆ', { empty: true }]]).map(([name, info]) => {
          if (info && info.empty) {
            return {
              title: name,
              desc: '褰撳墠瑙掕壊杩樻病鏈変换浣曚氦鎴樻。妗堛€?
            };
          }
          const countValue = Math.max(0, toNumber(info && info.娆℃暟, 0));
          const tickValue = toText(info && info.鏈€杩憈ick, '鏈褰?);
          const lastResult = toText(info && info.last_result, '');
          return {
            title: `${name}`,
            desc: [
              `绱浜ゆ垬 ${countValue} 娆,
              `鏈€杩戣褰曡疆娆?${tickValue}`,
              lastResult ? `鏈€杩戠粨鏋滐細${lastResult}` : ''
            ].filter(Boolean).join(' 路 ')
          };
        });
        const unlockedIntelHtml = unlockedIntelPage.items.length
          ? unlockedIntelPage.items.map(item => `
              <div class="intel-card" style="min-height:132px; display:flex; flex-direction:column; justify-content:space-between;">
                <div style="display:flex; gap:8px; align-items:flex-start; justify-content:space-between;">
                  <b style="flex:1 1 auto;">${activeCharKey
                    ? makeInlineEditableValue(toText(item && item.text, '鏈煡'), {
                        path: ['char', activeCharKey, '宸叉帉鎻℃儏鎶?, toNumber(item && item.index, 0)],
                        kind: 'string',
                        rawValue: toText(item && item.text, '鏈煡'),
                        multiline: true,
                      })
                    : htmlEscape(toText(item && item.text, '鏈煡'))}</b>
                  ${activeCharKey
                    ? `<button type="button" class="tag-chip" data-collection-action="delete-intel" data-collection-char="${escapeHtmlAttr(activeCharKey)}" data-collection-index="${escapeHtmlAttr(String(toNumber(item && item.index, 0)))}">鍒犻櫎</button>`
                    : ''}
                </div>
                <span style="margin-top:10px; color:var(--color-text-secondary); font-size:12px; line-height:1.55;">鎯呮姤搴忓彿 #${toNumber(item && item.index, 0) + 1}</span>
              </div>
            `).join('')
          : '<div class="intel-card" style="min-height:132px;"><b>鏆傛棤鎯呮姤</b></div>';
        return {
          title: '鎯呮姤搴撳脊绐?,
          summary: '褰撳墠宸茶В閿佹儏鎶ャ€佷换鍔¤褰曚笌浜ゆ垬妗ｆ姒傝銆?,
          body: `
            <div class="intel-layout-dashboard is-empty">
              <div class="archive-card intel-pending-card intel-pending-card--empty">
                <div class="archive-card-head">
                  <div class="archive-card-title">鏈€鏂版儏鎶?/div>
                  <span class="state-tag" style="opacity: 0.5;">宸叉暣鐞?/span>
                </div>

                <div class="intel-pending-shell is-empty">
                  ${snapshot.unlockedKnowledges.length ? `
                    <div class="intel-pending-editor">${htmlEscape(intelOverviewText)}</div>
                  ` : `
                    <div class="intel-empty-state">
                      <div class="intel-empty-state-mark">鈼?/div>
                      <div class="intel-empty-state-copy">褰撳墠杩樻病鏈夊凡鏀跺綍鎯呮姤</div>
                    </div>
                  `}
                </div>
              </div>

              <div class="archive-card intel-overview-card">
                <div class="archive-card-head"><div class="archive-card-title">鎯呮姤姒傝</div></div>
                ${makeTileGrid([
                  { label: '宸茶В閿?, value: String(snapshot.unlockedKnowledges.length) },
                  { label: '鏈€鏂?, value: htmlEscape(shortenText(intelOverviewText, 10) || '鏆傛棤') },
                  { label: '浠诲姟璁板綍', value: String(snapshot.questRecordCount) },
                  { label: '浜ゆ垬鐩爣', value: String(snapshot.combatHistoryEntries.length) }
                ], 'two')}
              </div>

              <div class="archive-card intel-combat-card">
                <div class="archive-card-head"><div class="archive-card-title">浜ゆ垬妗ｆ</div></div>
                ${makeTileGrid([
                  { label: '璁板綍鐩爣', value: String(snapshot.combatHistoryEntries.length) },
                  { label: '绱浜ゆ垬', value: String(combatHistoryTotalCount) },
                  { label: '鏈€杩戠洰鏍?, value: latestCombatTarget },
                  { label: '鏈€杩戠粨鏋?, value: latestCombatResult }
                ], 'two')}
                <div class="intel-combat-summary">
                  <div class="simple-sub" style="margin: 0 0 6px;">鎸夌洰鏍囪仛鍚?/div>
                  <div class="intel-combat-list">
                    ${combatHistoryCards.map(c => `
                      <div class="intel-combat-row">
                        <b>${htmlEscape(c.title)}</b>
                        <span>${htmlEscape(c.desc)}</span>
                      </div>
                    `).join('')}
                  </div>
                </div>
              </div>
            </div>

            <div class="archive-card intel-unlocked-card" style="margin-top: 12px;">
              <div class="archive-card-head">
                <div class="archive-card-title">宸茶В閿佹儏鎶ュ垪琛?/div>
                <span class="state-tag live">${unlockedIntelPage.items.length} 鏉?/span>
              </div>
              <div class="intel-list-grid" style="padding: 10px; display: grid; gap: 10px;">
                ${activeCharKey ? `
                  <div class="intel-card intel-card-add">
                    <div class="request-console-row intel-add-row" data-collection-panel="intel-create">
                      <input type="text" class="request-console-input intel-add-input" data-collection-input="intel-text" placeholder="鏂板鎯呮姤鍐呭锛堟敮鎸侀暱鏂囨湰锛? />
                      <button type="button" class="tag-chip live intel-add-btn" data-collection-action="add-intel" data-collection-char="${escapeHtmlAttr(activeCharKey)}">鏂板</button>
                    </div>
                  </div>
                ` : ''}
                ${unlockedIntelHtml}
                
                ${unlockedIntelPage.totalPages > 1 ? `
                  <div class="tag-cloud" style="margin-top: 8px; justify-content: flex-end;">
                    <button type="button" class="tag-chip" data-pagination="intel-records" data-page="${unlockedIntelPage.currentPage - 1}" ${unlockedIntelPage.currentPage <= 1 ? 'disabled style="opacity:0.3;"' : ''}>涓婁竴椤?/button>
                    <span class="tag-chip">绗?${unlockedIntelPage.currentPage} / ${unlockedIntelPage.totalPages} 椤?/span>
                    <button type="button" class="tag-chip" data-pagination="intel-records" data-page="${unlockedIntelPage.currentPage + 1}" ${unlockedIntelPage.currentPage >= unlockedIntelPage.totalPages ? 'disabled style="opacity:0.3;"' : ''}>涓嬩竴椤?/button>
                  </div>
                ` : ''}
              </div>
            </div>
          `
        };
	  }





      if (
        previewKey === '姝﹁宸ュ潑璇︾粏椤?
        || previewKey === '姝﹁璇︽儏锛氭枟閾?
        || previewKey === '姝﹁璇︽儏锛氭満鐢?
        || previewKey === '姝﹁璇︽儏锛氫富姝﹀櫒'
        || String(previewKey || '').startsWith('鏂楅摖閮ㄤ欢锛?)
      ) {
        const activeCharKey = resolveSnapshotCharKey(snapshot, toText(snapshot.activeName, '')) || toText(snapshot.activeName, '');
        const armor = deepGet(snapshot, 'activeChar.瑁呭.鏂楅摖', {});
        const mech = deepGet(snapshot, 'activeChar.瑁呭.鏈虹敳', {});
        const weapon = deepGet(snapshot, 'activeChar.瑁呭.姝﹀櫒', {});
        const armorPath = activeCharKey ? ['char', activeCharKey, '瑁呭', '鏂楅摖'] : [];
        const mechPath = activeCharKey ? ['char', activeCharKey, '瑁呭', '鏈虹敳'] : [];
        const weaponPath = activeCharKey ? ['char', activeCharKey, '瑁呭', '姝﹀櫒'] : [];
        const jobs = safeEntries(deepGet(snapshot, 'activeChar.鑱屼笟', {}));
        const isPlayerControlled = isSnapshotPlayerControlled(snapshot);
        const armorSummary = toNumber(armor.绛夌骇, 0) > 0 ? `${toText(armor.鍚嶇О, `${armor.绛夌骇}瀛楁枟閾燻)} / ${toText(armor.瑁呭鐘舵€? '鏈澶?)}` : '鏈澶?;
        const mechSummary = toText(mech.绛夌骇, '鏃?) !== '鏃? ? `${toText(mech.绛夌骇, '鏃?)}路${toText(mech.鍨嬪彿, '鏈畾鍨?)} / ${toText(mech.瑁呭鐘舵€? '鏈澶?)}` : '鏈儴缃?;
        const weaponSummary = weapon && (weapon.鍚嶇О || weapon['鍚嶇О']) ? `${toText(weapon.鍚嶇О || weapon['鍚嶇О'], '鏃?)} / ${toText(weapon.鍝侀樁 || weapon['鍝侀樁'], '鏃犲搧闃?)}` : '鏃?;
        const jobSummary = jobs.length ? jobs.slice(0, 2).map(([name, info]) => `${name} Lv.${toText(info && info.绛夌骇, 0)}`).join(' / ') : '鏈帉鎻?;
        const jobCoreTechSummary = jobs.length ? (Object.keys(deepGet(jobs[0][1], '鏍稿績鎶€鑹?, {})).slice(0, 2).join(' / ') || '鏆傛棤鏍稿績鎶€鏈?) : '鏆傛棤鏍稿績鎶€鏈?;
        const jobLimitSummary = jobs.length ? `铻嶉敾涓婇檺 ${toText(deepGet(jobs[0][1], '闄愬埗.鏈€澶ц瀺鍚堟暟', 1), '1')} / 鎴愬姛鐜?${toText(deepGet(jobs[0][1], '闄愬埗.鎴愬姛鐜?, 0), '0')}%` : '鏆傛棤宸ュ潑涓婇檺';
        const battleForm = `${toText(deepGet(snapshot, 'activeChar.鐘舵€?琛屽姩', '鏃ュ父'), '鏃ュ父')} / ${toText(deepGet(snapshot, 'activeChar.鐘舵€?褰撳墠棰嗗煙', '甯告€?), '甯告€?)}`;
        const armorExists = toNumber(armor.绛夌骇, 0) > 0 || !!toText(armor.鍚嶇О || armor['鍚嶇О'], '');
        const mechExists = toText(mech.绛夌骇, '鏃?) !== '鏃? || !!toText(mech.鍨嬪彿, '');
        const armorEquipped = toText(armor.瑁呭鐘舵€? '鏈澶?) === '宸茶澶?;
        const mechEquipped = toText(mech.瑁呭鐘舵€? '鏈澶?) === '宸茶澶?;
        const armorBonusItems = buildStatsBonusItems(deepGet(armor, '_灞炴€у姞鎴?, deepGet(armor, '灞炴€у姞鎴?, {})), { includeLvEquiv: true });
        const mechBonusItems = buildStatsBonusItems(deepGet(mech, '_灞炴€у姞鎴?, deepGet(mech, '灞炴€у姞鎴?, {})));
        const weaponBonusItems = buildStatsBonusItems(deepGet(weapon, '灞炴€у姞鎴?, {}));
        const armorSlotDefs = [
          { x: 50, y: 12, label: '澶寸洈', preview: '鏂楅摖閮ㄤ欢锛氬ご鐩? },
          { x: 50, y: 34, label: '鑳搁摖', preview: '鏂楅摖閮ㄤ欢锛氳兏閾? },
          { x: 24, y: 34, label: '宸﹁偐', preview: '鏂楅摖閮ㄤ欢锛氬乏鑲? },
          { x: 76, y: 34, label: '鍙宠偐', preview: '鏂楅摖閮ㄤ欢锛氬彸鑲? },
          { x: 18, y: 52, label: '宸﹁噦', preview: '鏂楅摖閮ㄤ欢锛氬乏鑷? },
          { x: 82, y: 52, label: '鍙宠噦', preview: '鏂楅摖閮ㄤ欢锛氬彸鑷? },
          { x: 34, y: 82, label: '宸﹁吙', preview: '鏂楅摖閮ㄤ欢锛氬乏鑵? },
          { x: 66, y: 82, label: '鍙宠吙', preview: '鏂楅摖閮ㄤ欢锛氬彸鑵? },
          { x: 50, y: 68, label: '鎴樿', preview: '鏂楅摖閮ㄤ欢锛氭垬瑁? },
          { x: 50, y: 94, label: '鎴橀澊', preview: '鏂楅摖閮ㄤ欢锛氭垬闈? }
        ];
        const armorSlots = armorSlotDefs.map(slot => ({
          ...slot,
          className: (() => {
            const partData = resolveArmorPartData(armor, slot.label);
            const partStatus = toText(partData && partData['鐘舵€?], '鏈墦閫?);
            if (!partData || partStatus === '鏈墦閫?) return 'off';
            if (partStatus === '閲嶅垱') return 'warn';
            return 'on';
          })()
        }));

        if (previewKey === '姝﹁璇︽儏锛氭枟閾?) {
          return {
            title: toText(armor.鍚嶇О, '鏂楅摖璇︽儏'),
            summary: '',
            body: `
              <div class="equipment-layout">
                <div class="archive-card full">
                  <div class="archive-card-head"><div class="archive-card-title">鏂楅摖璇︽儏</div></div>
                  ${makeTileGrid([
                    { label: '鍚嶇О', value: armorPath.length
                      ? makeInlineEditableValue(toText(armor.鍚嶇О, '鏃?), {
                          path: [...armorPath, '鍚嶇О'],
                          kind: 'string',
                          rawValue: toText(armor.鍚嶇О, '鏃?),
                        })
                      : htmlEscape(toText(armor.鍚嶇О, '鏃?)) },
                    { label: '瀛楃骇', value: armorPath.length
                      ? makeInlineEditableValue(toNumber(armor.绛夌骇, 0) > 0 ? `${toNumber(armor.绛夌骇, 0)}瀛楁枟閾燻 : '鏃?, {
                          path: [...armorPath, '绛夌骇'],
                          kind: 'number',
                          rawValue: toNumber(armor.绛夌骇, 0),
                          editorMeta: { min: 0, max: 4, integer: true, hint: '鑼冨洿 0 - 4 路 鏁存暟' },
                        })
                      : htmlEscape(toNumber(armor.绛夌骇, 0) > 0 ? `${toNumber(armor.绛夌骇, 0)}瀛楁枟閾燻 : '鏃?) },
                    { label: '棰嗗煙', value: armorPath.length
                      ? makeInlineEditableValue(toText(armor.棰嗗煙, '鏃?), {
                          path: [...armorPath, '棰嗗煙'],
                          kind: 'string',
                          rawValue: toText(armor.棰嗗煙, '鏃?),
                        })
                      : htmlEscape(toText(armor.棰嗗煙, '鏃?)) },
                    { label: '瑁呭鐘舵€?, value: armorPath.length
                      ? makeInlineEditableValue(toText(armor.瑁呭鐘舵€? '鏈澶?), {
                          path: [...armorPath, '瑁呭鐘舵€?],
                          kind: 'enum_select',
                          rawValue: toText(armor.瑁呭鐘舵€? '鏈澶?),
                          editorMeta: { options: ['鏈澶?, '宸茶澶?] },
                        })
                      : htmlEscape(toText(armor.瑁呭鐘舵€? '鏈澶?)) }
                  ])}
                </div>
                <div class="archive-card full">
                  <div class="archive-card-head"><div class="archive-card-title">鏂楅摖鍔犳垚</div></div>
                  ${makeTileGrid(armorBonusItems)}
                </div>
                <div class="archive-card">
                  <div class="archive-card-head"><div class="archive-card-title">鏂楅摖閮ㄤ欢</div></div>
                  ${makeInteractiveFigureBoard(toText(armor.鍚嶇О, '鏂楅摖'), armorSlots, { preview: '姝﹁璇︽儏锛氭枟閾? })}
                </div>
                ${isPlayerControlled && armorExists ? `
                  <div class="archive-card full">
                    <div class="tag-cloud armory-quick-actions" style="justify-content:flex-end;">
                      <button type="button" class="relation-action-btn equipment-action-btn" data-equipment-action="unequip" data-equipment-char="${escapeHtmlAttr(activeCharKey)}" data-equipment-kind="armor">鍗镐笅鏂楅摖</button>
                    </div>
                  </div>
                ` : ''}
              </div>
            `
          };
        }

        if (previewKey === '姝﹁璇︽儏锛氭満鐢?) {
          return {
            title: mechExists ? toText(mech.鍨嬪彿, '鏈虹敳璇︽儏') : '鏈虹敳璇︽儏',
            summary: '',
            body: `
              <div class="archive-modal-grid">
                <div class="archive-card full">
                  <div class="archive-card-head"><div class="archive-card-title">鏈虹敳璇︽儏</div></div>
                  ${makeTileGrid([
                    { label: '绾у埆', value: mechPath.length
                      ? makeInlineEditableValue(toText(mech.绛夌骇, '鏃?), {
                          path: [...mechPath, '绛夌骇'],
                          kind: 'string',
                          rawValue: toText(mech.绛夌骇, '鏃?),
                        })
                      : htmlEscape(toText(mech.绛夌骇, '鏃?)) },
                    { label: '绫诲瀷', value: mechPath.length
                      ? makeInlineEditableValue(toText(mech.鍨嬪彿, '鏃?), {
                          path: [...mechPath, '鍨嬪彿'],
                          kind: 'string',
                          rawValue: toText(mech.鍨嬪彿, '鏃?),
                        })
                      : htmlEscape(toText(mech.鍨嬪彿, '鏃?)) },
                    { label: '鏈轰綋鐘舵€?, value: mechPath.length
                      ? makeInlineEditableValue(toText(mech.鐘舵€? '瀹屽ソ'), {
                          path: [...mechPath, '鐘舵€?],
                          kind: 'string',
                          rawValue: toText(mech.鐘舵€? '瀹屽ソ'),
                        })
                      : htmlEscape(toText(mech.鐘舵€? '瀹屽ソ')) },
                    { label: '瑁呭鐘舵€?, value: mechPath.length
                      ? makeInlineEditableValue(toText(mech.瑁呭鐘舵€? '鏈澶?), {
                          path: [...mechPath, '瑁呭鐘舵€?],
                          kind: 'enum_select',
                          rawValue: toText(mech.瑁呭鐘舵€? '鏈澶?),
                          editorMeta: { options: ['鏈澶?, '宸茶澶?] },
                        })
                      : htmlEscape(toText(mech.瑁呭鐘舵€? '鏈澶?)) },
                    { label: '鏈鸿浇姝﹀櫒', value: mechPath.length
                      ? makeInlineEditableValue(toText(mech.姝﹁, '鏃?), {
                          path: [...mechPath, '姝﹁'],
                          kind: 'string',
                          rawValue: toText(mech.姝﹁, '鏃?),
                        })
                      : htmlEscape(toText(mech.姝﹁, '鏃?)) },
                    { label: '鍝佽川绯绘暟', value: mechPath.length
                      ? makeInlineEditableValue(String(toNumber(deepGet(mech, '鍝佽川绯绘暟', 1), 1)), {
                          path: [...mechPath, '鍝佽川绯绘暟'],
                          kind: 'number',
                          rawValue: toNumber(deepGet(mech, '鍝佽川绯绘暟', 1), 1),
                          editorMeta: { min: 0.8, max: 2, step: 0.1, hint: '鑼冨洿 0.8 - 2.0 路 鍙緭鍏ュ皬鏁?路 姝ラ暱 0.1' },
                        })
                      : htmlEscape(String(toNumber(deepGet(mech, '鍝佽川绯绘暟', 1), 1))) }
                  ])}
                </div>
                <div class="archive-card full">
                  <div class="archive-card-head"><div class="archive-card-title">鏈虹敳鍔犳垚</div></div>
                  ${makeTileGrid(mechBonusItems)}
                </div>
                ${isPlayerControlled && mechExists ? `
                  <div class="archive-card full">
                    <div class="tag-cloud armory-quick-actions" style="justify-content:flex-end;">
                      <button type="button" class="relation-action-btn equipment-action-btn" data-equipment-action="unequip" data-equipment-char="${escapeHtmlAttr(activeCharKey)}" data-equipment-kind="mech">鍗镐笅鏈虹敳</button>
                    </div>
                  </div>
                ` : ''}
              </div>
            `
          };
        }

        if (previewKey === '姝﹁璇︽儏锛氫富姝﹀櫒') {
          return {
            title: toText(weapon.鍚嶇О || weapon['鍚嶇О'], '涓绘鍣ㄨ鎯?),
            summary: '',
            body: `
              <div class="archive-modal-grid">
                <div class="archive-card full">
                  <div class="archive-card-head"><div class="archive-card-title">涓绘鍣ㄨ鎯?/div></div>
                  ${makeTileGrid([
                    { label: '鍚嶇О', value: weaponPath.length
                      ? makeInlineEditableValue(toText(weapon.鍚嶇О, '鏃?), {
                          path: [...weaponPath, '鍚嶇О'],
                          kind: 'string',
                          rawValue: toText(weapon.鍚嶇О, '鏃?),
                        })
                      : htmlEscape(toText(weapon.鍚嶇О, '鏃?)) },
                    { label: '鍝侀樁', value: weaponPath.length
                      ? makeInlineEditableValue(toText(weapon.鍝侀樁, '鏃?), {
                          path: [...weaponPath, '鍝侀樁'],
                          kind: 'string',
                          rawValue: toText(weapon.鍝侀樁, '鏃?),
                        })
                      : htmlEscape(toText(weapon.鍝侀樁, '鏃?)) },
                    { label: '鐗规€ф暟', value: String(Object.keys(deepGet(weapon, '鐗规€?, {})).length) },
                    { label: '涓昏鐗规€?, value: Object.keys(deepGet(weapon, '鐗规€?, {})).slice(0, 2).join(' / ') || '鏃? }
                  ])}
                </div>
                <div class="archive-card full">
                  <div class="archive-card-head"><div class="archive-card-title">姝﹀櫒鍔犳垚</div></div>
                  ${makeTileGrid(weaponPath.length ? buildEditableStatBonusItems([...weaponPath, '灞炴€у姞鎴?], deepGet(weapon, '灞炴€у姞鎴?, {})) : weaponBonusItems)}
                </div>
                <div class="archive-card full">
                  <div class="archive-card-head"><div class="archive-card-title">姝﹀櫒鐗规€?/div></div>
                  ${makeTimelineStack(safeEntries(deepGet(weapon, '鐗规€?, {})).length ? safeEntries(deepGet(weapon, '鐗规€?, {})).map(([name, item]) => ({
                    title: htmlEscape(name),
                    desc: weaponPath.length
                      ? makeInlineEditableValue(toText(deepGet(item, '鎻忚堪', '鏃?), '鏃?), {
                          path: [...weaponPath, '鐗规€?, name, '鎻忚堪'],
                          kind: 'string',
                          rawValue: toText(deepGet(item, '鎻忚堪', '鏃?), '鏃?),
                          multiline: true,
                        })
                      : htmlEscape(toText(deepGet(item, '鎻忚堪', '鏃?), '鏃?))
                  })) : [{ title: '鏆傛棤姝﹀櫒鐗规€?, desc: '褰撳墠涓绘鍣ㄦ湭璁板綍棰濆鐗规€с€? }])}
                </div>
                ${isPlayerControlled && (weapon && (weapon.鍚嶇О || weapon['鍚嶇О'])) ? `
                  <div class="archive-card full">
                    <div class="tag-cloud armory-quick-actions" style="justify-content:flex-end;">
                      <button type="button" class="relation-action-btn equipment-action-btn" data-equipment-action="unequip" data-equipment-char="${escapeHtmlAttr(activeCharKey)}" data-equipment-kind="wpn">鍗镐笅涓绘鍣?/button>
                    </div>
                  </div>
                ` : ''}
              </div>
            `
          };
        }

        if (String(previewKey || '').startsWith('鏂楅摖閮ㄤ欢锛?)) {
          const slotName = String(previewKey).replace('鏂楅摖閮ㄤ欢锛?, '');
          const partData = resolveArmorPartData(armor, slotName);
          const partPath = armorPath.length ? [...armorPath, '閮ㄤ欢', slotName] : [];
          const fallbackPartItems = [
            { label: '閮ㄤ綅', value: slotName },
            { label: '璁板綍', value: '褰撳墠鏈褰曠嫭绔嬮儴浠跺睘鎬? },
            { label: '鎵€灞炴枟閾?, value: armorSummary },
            { label: '瑁呴厤鐘舵€?, value: toText(armor.瑁呭鐘舵€? '鏈澶?) }
          ];
          return {
            title: `${slotName}璇︽儏`,
            summary: '',
            body: `
              <div class="archive-modal-grid" style="grid-template-columns:1fr;">
                <div class="archive-card full">
                  <div class="archive-card-head"><div class="archive-card-title">${slotName}</div></div>
                  ${makeTileGrid(partData ? [
                    { label: '鐘舵€?, value: partPath.length
                      ? makeInlineEditableValue(toText(partData['鐘舵€?], '鏈墦閫?), {
                          path: [...partPath, '鐘舵€?],
                          kind: 'string',
                          rawValue: toText(partData['鐘舵€?], '鏈墦閫?),
                        })
                      : htmlEscape(toText(partData['鐘舵€?], '鏈墦閫?)) },
                    { label: '鍝佽川绯绘暟', value: partPath.length
                      ? makeInlineEditableValue(String(toNumber(partData['鍝佽川绯绘暟'], 1)), {
                          path: [...partPath, '鍝佽川绯绘暟'],
                          kind: 'number',
                          rawValue: toNumber(partData['鍝佽川绯绘暟'], 1),
                          editorMeta: { min: 0.8, max: 2, step: 0.1, hint: '鑼冨洿 0.8 - 2.0 路 鍙緭鍏ュ皬鏁?路 姝ラ暱 0.1' },
                        })
                      : htmlEscape(String(toNumber(partData['鍝佽川绯绘暟'], 1))) },
                    { label: '鎵€灞炴枟閾?, value: toText(armor.鍚嶇О, '鏃?) },
                    { label: '瑁呴厤鐘舵€?, value: armorPath.length
                      ? makeInlineEditableValue(toText(armor.瑁呭鐘舵€? '鏈澶?), {
                          path: [...armorPath, '瑁呭鐘舵€?],
                          kind: 'enum_select',
                          rawValue: toText(armor.瑁呭鐘舵€? '鏈澶?),
                          editorMeta: { options: ['鏈澶?, '宸茶澶?] },
                        })
                      : htmlEscape(toText(armor.瑁呭鐘舵€? '鏈澶?)) }
                  ] : fallbackPartItems)}
                </div>
              </div>
            `
          };
        }

        const getBoneBonus = (age, part) => {
          let rb = {
            鍔涢噺: Math.floor(age * 0.05),
            闃插尽: Math.floor(age * 0.05),
            鏁忔嵎: Math.floor(age * 0.05),
            浣撳姏涓婇檺: Math.floor(age * 0.05),
            绮剧鍔涗笂闄? Math.floor(age * 0.01),
            榄傚姏涓婇檺: Math.floor(age * 0.1),
          };
          let bonus = { ...rb };
          if (part === "韬共榄傞") { bonus.鍔涢噺 *= 2; bonus.闃插尽 *= 2; bonus.鏁忔嵎 *= 2; bonus.浣撳姏涓婇檺 *= 2; bonus.榄傚姏涓婇檺 *= 2; }
          else if (part === "澶撮儴榄傞") { bonus.绮剧鍔涗笂闄?*= 2; }
          else if (part === "宸﹁吙榄傞" || part === "鍙宠吙榄傞") { bonus.鏁忔嵎 *= 2; }
          else if (part === "宸﹁噦榄傞" || part === "鍙宠噦榄傞") { bonus.鍔涢噺 *= 2; }
          return bonus;
        };

        const soulBoneCards = (snapshot.soulBoneEntries && snapshot.soulBoneEntries.length ? snapshot.soulBoneEntries.slice(0, 6) : [['鏆傛棤榄傞瑁呰浇', { empty: true }]]).map(([slot, bone]) => {
          if (bone && bone.empty) {
            return { title: slot, desc: '灏氭湭瑁呰浇榄傞銆? };
          }
          const boneName = toText(bone && (bone.name || bone['鍚嶇О'] || bone['琛ㄨ薄鍚嶇О']), slot);
          const age = toText(bone && (bone['骞撮檺'] || bone.age), '');
          const quality = toText(bone && (bone['鍝佽川'] || bone['鍝侀樁']), '');
          let descText = `${toText(bone && (bone['鐘舵€?]), '宸茶杞?)}`;
          if (age || quality) descText += ` 锝?${[age ? `${age}骞碻 : '', quality].filter(Boolean).join(' ')}`;
          
          const realAge = toNumber(age, 0);
          if (realAge > 0) {
            const stats = getBoneBonus(realAge, slot);
            const statParts = [];
            if (stats.鍔涢噺) statParts.push(`鍔?${formatNumber(stats.鍔涢噺)}`);
            if (stats.闃插尽) statParts.push(`闃?${formatNumber(stats.闃插尽)}`);
            if (stats.鏁忔嵎) statParts.push(`鏁?${formatNumber(stats.鏁忔嵎)}`);
            if (stats.浣撳姏涓婇檺) statParts.push(`浣?${formatNumber(stats.浣撳姏涓婇檺)}`);
            if (stats.绮剧鍔涗笂闄? statParts.push(`绮?${formatNumber(stats.绮剧鍔涗笂闄?}`);
            if (statParts.length) descText += ` 锝?灞炴€у姞鎴? ${statParts.join(' ')}`;
          }

          const skillsObj = bone && bone['闄勫甫鎶€鑳?];
          if (skillsObj && Object.keys(skillsObj).length) {
            const parsedSkills = buildSkillList(skillsObj, {
              basePath: ['char', snapshot.activeName, '榄傞', slot, '闄勫甫鎶€鑳?],
              category: '榄傞闄勫甫鎶€鑳?,
              scope: 'soul_bone_skill',
            });
            const skillsHtml = parsedSkills.map(skill => `
              <div
                class="ring-hover-skill${skill && skill.preview ? ' clickable' : ''}"
                ${skill && skill.preview ? `data-preview="${escapeHtmlAttr(skill.preview)}"` : ''}
                style="background:rgba(0,0,0,0.2); border:1px solid rgba(255,255,255,0.05); margin-top:8px; padding:8px; border-radius:8px;"
              >
                <b style="color:var(--gold);font-size:11px;display:block;margin-bottom:6px;">${htmlEscape(skill && skill.name ? skill.name : '鏈懡鍚嶆妧鑳?)}</b>
                <div class="ring-hover-copy"><em>鐢婚潰鎻忚堪</em><span>${htmlEscape(skill && skill.visualDesc ? skill.visualDesc : '鏈煡')}</span></div>
                <div class="ring-hover-copy"><em>鏁堟灉鎻忚堪</em><span>${htmlEscape(skill && skill.effectDesc ? skill.effectDesc : '鏈煡')}</span></div>
              </div>
            `).join('');
            descText += skillsHtml;
          }

          return { title: `${boneName} / ${slot}`, desc: descText };
        });

        return {
          title: '姝﹁宸ュ潑寮圭獥',
          summary: '',
          body: `
            <div class="equipment-layout armory-layout-single">
              <div class="archive-card full">
                <div class="archive-card-head"><div class="archive-card-title">褰撳墠姝﹁</div></div>
                ${makeInteractiveTileGrid([
                  { label: '鏂楅摖', value: armorSummary, preview: '姝﹁璇︽儏锛氭枟閾? },
                  { label: '鏈虹敳', value: mechSummary, preview: '姝﹁璇︽儏锛氭満鐢? },
                  { label: '涓绘鍣?, value: weaponSummary, preview: '姝﹁璇︽儏锛氫富姝﹀櫒' },
                  { label: '鍓亴涓?, value: jobSummary },
                  { label: '鎴樻枟褰㈡€?, value: battleForm }
                ])}
              </div>
              <div class="archive-card full">
                <div class="archive-card-head"><div class="archive-card-title">榄傞瑁呰浇</div></div>
                ${makeTimelineStack(soulBoneCards)}
              </div>
              <div class="archive-card full">
                <div class="archive-card-head"><div class="archive-card-title">鍓亴涓氬伐鍧?/div></div>
                ${makeTileGrid(jobs.length ? [
                  { label: '涓诲壇鑱?, value: jobSummary },
                  { label: '鏍稿績鎶€鏈?, value: jobCoreTechSummary },
                  { label: '宸ュ潑涓婇檺', value: jobLimitSummary }
                ] : [{ label: '涓诲壇鑱?, value: '鏈睍寮€' }, { label: '鏍稿績鎶€鏈?, value: '鏆傛棤' }, { label: '宸ュ潑涓婇檺', value: '鏆傛棤' }], 'three')}
                ${isPlayerControlled ? '<div id="armoryProfessionMount"></div>' : '<div class="tag-cloud armory-quick-actions"><span class="tag-chip">浠呯帺瀹惰鑹插彲杩涜閿婚€?宸ュ潑鎿嶄綔</span></div>'}
              </div>
            </div>
          `,
          onMount: (container) => {
            if (!isPlayerControlled) {
              return null;
            }
            const professionMount = container.querySelector('#armoryProfessionMount');
            if (!professionMount) return null;
            const 鎸傝浇鑱屼笟宸ュ潑 = () => window.mountProfessionUI(professionMount, snapshot, {
              dispatchContext: mapDispatchContext,
              onAction: (actionData) => {
                dispatchUiAiRequest(actionData.playerInput, actionData.systemPrompt, {
                  requestKind: actionData.requestKind,
                  patchOps: actionData.patchOps
                });
              }
            });
            if (typeof window.mountProfessionUI === 'function') {
              return 鎸傝浇鑱屼笟宸ュ潑();
            }
            professionMount.innerHTML = '<div class="dossier-empty-note">宸ュ潑妯″潡鍔犺浇涓?..</div>';
            void 璇锋眰棰勭儹棰勮渚濊禆('姝﹁宸ュ潑璇︾粏椤?, 'armory_profession_mount')
              .then(() => {
                if (!professionMount.isConnected) return;
                if (typeof window.mountProfessionUI !== 'function') {
                  professionMount.innerHTML = '<div class="dossier-empty-note">宸ュ潑妯″潡鏆備笉鍙敤</div>';
                  return;
                }
                professionMount.innerHTML = '';
                activeSubUI = 鎸傝浇鑱屼笟宸ュ潑();
              })
              .catch(() => {
                if (professionMount.isConnected) professionMount.innerHTML = '<div class="dossier-empty-note">宸ュ潑妯″潡鍔犺浇澶辫触</div>';
              });
            return null;
          }
        };
      }


      if (previewKey === '鍌ㄧ墿浠撳簱璇︾粏椤?) {
        const activeCharKey = resolveSnapshotCharKey(snapshot, toText(snapshot.activeName, '')) || toText(snapshot.activeName, '');
        const inventoryCells = snapshot.inventoryEntries
          .sort((a, b) => toNumber(deepGet(b[1], '甯傚満浼板€?浠锋牸', b[1] && b[1]['鏁伴噺']), 0) - toNumber(deepGet(a[1], '甯傚満浼板€?浠锋牸', a[1] && a[1]['鏁伴噺']), 0))
          .slice(0, 18)
          .map(([name, item]) => ({
            charKey: activeCharKey,
            name,
            trigger: toText(item && item['瑙﹀彂鏂瑰紡'], /椋熺墿/.test(toText(item && item['绫诲瀷'], '')) ? '椋熺敤' : '浣跨敤'),
            expiry: resolveExpiryUiText(item, Number(deepGet(item, '鏈夋晥鏈熻嚦tick', 0)) > 0 ? '涓存椂鐗╁搧' : '鏃犳湡闄?),
            qty: toNumber(item && item['鏁伴噺'], 1),
            meta: `${toText(item && item['绫诲瀷'], '鐗╁搧')} 路 ${toText(item && (item['鍝佽川'] || item['鍝侀樁']), '甯歌')}`,
            className: /鍗佷竾|澶╅敾|绁瀨绾?.test(name + toText(item && item['鍝佽川'], '')) ? 'gold' : (/涓囧勾|榄傞|榄傜伒|绱?.test(name + toText(item && item['鍝佽川'], '')) ? 'purple' : ''),
            type: `${toText(item && item['绫诲瀷'], '鐗╁搧')} / ${toText(item && item['鍝侀樁'], toText(item && item['鍝佽川'], '鏅€?))}`,
            rarity: toText(item && (item['鍝佽川'] || item['鍝侀樁']), '鏅€?),
            source: toText(item && item['鏉ユ簮鎶€鑳?], toText(item && item['缁戝畾鑰?], '鑳屽寘鎸佹湁')),
            usage: toText(item && item['鎻忚堪'], '鏆傛棤璇存槑'),
            canEquip: !!(window.EquipmentManager && window.EquipmentManager.parseEquipSlot(name, item)),
            canUse: Array.isArray(item && item['浣跨敤鏁堟灉']) && item['浣跨敤鏁堟灉'].length > 0,
            actionLabel: /椋熺墿/.test(toText(item && item['绫诲瀷'], '')) || /椋熺敤/.test(toText(item && item['瑙﹀彂鏂瑰紡'], '')) ? '椋熺敤' : '浣跨敤',
            tags: [
              toText(item && item['绫诲瀷'], ''),
              toText(item && item['鍝佽川'], ''),
              toText(item && item['鍝侀樁'], ''),
              toText(item && item['瑙﹀彂鏂瑰紡'], ''),
              Number(deepGet(item, '鏈夋晥鏈熻嚦tick', 0)) > 0 ? '涓存椂閬撳叿' : ''
            ].filter(Boolean)
          }));
        const inventoryOverviewPage = paginateModalItems(snapshot.inventoryEntries, previewKey, 'inventory-overview', 8);
        const inventoryOverviewCards = (inventoryOverviewPage.items.length ? inventoryOverviewPage.items : [['鏆傛棤鐗╁搧', null]]).map(([name, item]) => {
          if (!item || typeof item !== 'object') {
            return {
              title: '鏆傛棤鐗╁搧',
              desc: '鏈煡'
            };
          }
          const itemPath = activeCharKey ? ['char', activeCharKey, '鑳屽寘', name] : [];
          const typeValue = toText(item && item['绫诲瀷'], '鐗╁搧');
          const qtyValue = toNumber(item && item['鏁伴噺'], 1);
          const qualityValue = toText(item && item['鍝佽川'], '鏃?);
          const tierValue = toText(item && item['鍝侀樁'], '鏃?);
          const triggerValue = toText(item && item['瑙﹀彂鏂瑰紡'], /椋熺墿/.test(typeValue) ? '椋熺敤' : '甯歌');
          const slotValue = toText(item && item['瑁呭妲戒綅'], '鏃?);
          const sourceValue = toText(item && item['鏉ユ簮鎶€鑳?], '鏃?);
          const binderValue = toText(item && item['缁戝畾鑰?], '鏃?);
          const tagsValue = Array.isArray(item && item['鏍囩']) ? item['鏍囩'] : [];
          const descValue = toText(item && item['鎻忚堪'], '鏆傛棤璇存槑');
          const priceValue = toNumber(deepGet(item, ['甯傚満浼板€?, '浠锋牸'], 0), 0);
          const currencyValue = toText(deepGet(item, ['甯傚満浼板€?, '璐у竵'], '鑱旈偊甯?), '鑱旈偊甯?);
          const durabilityCurrent = toNumber(deepGet(item, ['鑰愪箙', '褰撳墠'], 0), 0);
          const durabilityMax = toNumber(deepGet(item, ['鑰愪箙', '涓婇檺'], 0), 0);
          const canEquipItem = !!(window.EquipmentManager && window.EquipmentManager.parseEquipSlot(name, item));
          const lineOne = [
            `鏁伴噺 ${itemPath.length ? makeInlineEditableValue(String(qtyValue), { path: [...itemPath, '鏁伴噺'], kind: 'number', rawValue: qtyValue, editorMeta: { min: 1, integer: true, hint: '鏈€灏?1 路 鏁存暟' } }) : htmlEscape(String(qtyValue))}`,
            `绫诲瀷 ${itemPath.length ? makeInlineEditableValue(typeValue, { path: [...itemPath, '绫诲瀷'], kind: 'string', rawValue: typeValue }) : htmlEscape(typeValue)}`,
            `妲戒綅 ${itemPath.length ? makeInlineEditableValue(slotValue, { path: [...itemPath, '瑁呭妲戒綅'], kind: 'string', rawValue: slotValue }) : htmlEscape(slotValue)}`,
            `瑙﹀彂 ${itemPath.length ? makeInlineEditableValue(triggerValue, { path: [...itemPath, '瑙﹀彂鏂瑰紡'], kind: 'string', rawValue: triggerValue }) : htmlEscape(triggerValue)}`
          ].join(' 锝?');
          const lineTwo = [
            `鍝佽川 ${itemPath.length ? makeInlineEditableValue(qualityValue, { path: [...itemPath, '鍝佽川'], kind: 'string', rawValue: qualityValue }) : htmlEscape(qualityValue)}`,
            `鍝侀樁 ${itemPath.length ? makeInlineEditableValue(tierValue, { path: [...itemPath, '鍝侀樁'], kind: 'string', rawValue: tierValue }) : htmlEscape(tierValue)}`,
            `鏉ユ簮鎶€鑳?${itemPath.length ? makeInlineEditableValue(sourceValue, { path: [...itemPath, '鏉ユ簮鎶€鑳?], kind: 'string', rawValue: sourceValue }) : htmlEscape(sourceValue)}`,
            `缁戝畾鑰?${itemPath.length ? makeInlineEditableValue(binderValue, { path: [...itemPath, '缁戝畾鑰?], kind: 'string', rawValue: binderValue }) : htmlEscape(binderValue)}`
          ].join(' 锝?');
          const lineThree = [
            `鏍囩 ${itemPath.length ? makeInlineEditableValue(tagsValue.join('銆?) || '鏃?, { path: [...itemPath, '鏍囩'], kind: 'string_list', rawValue: tagsValue }) : htmlEscape(tagsValue.join('銆?) || '鏃?)}`,
            `甯備环 ${itemPath.length ? makeInlineEditableValue(String(priceValue), { path: [...itemPath, '甯傚満浼板€?, '浠锋牸'], kind: 'number', rawValue: priceValue, editorMeta: { min: 0, integer: true, hint: '鏈€灏?0 路 鏁存暟' } }) : htmlEscape(String(priceValue))}`,
            `璐у竵 ${htmlEscape(({
              fed_coin: '鑱旈偊甯?,
              star_coin: '鏄熺綏甯?,
              tang_pt: '鍞愰棬绉垎',
              shrek_pt: '瀛﹂櫌绉垎',
              blood_pt: '琛€绁炲姛鍕?
            }[currencyValue] || currencyValue))}`
          ].join(' 锝?');
          const lineFour = (durabilityCurrent > 0 || durabilityMax > 0)
            ? [
                `鑰愪箙褰撳墠 ${itemPath.length ? makeInlineEditableValue(String(durabilityCurrent), { path: [...itemPath, '鑰愪箙', '褰撳墠'], kind: 'number', rawValue: durabilityCurrent, editorMeta: { min: 0, max: durabilityMax > 0 ? durabilityMax : null, integer: true, hint: durabilityMax > 0 ? `鑼冨洿 0 - ${formatNumber(durabilityMax)} 路 鏁存暟` : '鏈€灏?0 路 鏁存暟' } }) : htmlEscape(String(durabilityCurrent))}`,
                `鑰愪箙涓婇檺 ${itemPath.length ? makeInlineEditableValue(String(durabilityMax), { path: [...itemPath, '鑰愪箙', '涓婇檺'], kind: 'number', rawValue: durabilityMax, editorMeta: { min: Math.max(0, durabilityCurrent), integer: true, hint: `鏈€灏?${formatNumber(Math.max(0, durabilityCurrent))} 路 鏁存暟` } }) : htmlEscape(String(durabilityMax))}`
              ].join(' 锝?')
            : '';
          const descLine = `鎻忚堪 ${itemPath.length
            ? makeInlineEditableValue(descValue, {
                path: [...itemPath, '鎻忚堪'],
                kind: 'string',
                rawValue: descValue,
                multiline: true,
              })
            : htmlEscape(descValue)}`;
          const canUseItem = Array.isArray(item && item['浣跨敤鏁堟灉']) && item['浣跨敤鏁堟灉'].length > 0;
          const useActionLabel = /椋熺墿/.test(typeValue) || /椋熺敤/.test(triggerValue) ? '椋熺敤' : '浣跨敤';
          const actionLine = itemPath.length
            ? `
              <div class="request-console-row" style="display:flex; gap:8px; flex-wrap:wrap; margin-top:10px;">
                ${canUseItem
                  ? `<button type="button" class="tag-chip live inventory-hover-action-btn" data-inventory-action="use" data-inventory-char="${escapeHtmlAttr(activeCharKey)}" data-inventory-item="${escapeHtmlAttr(name)}">${useActionLabel}</button>`
                  : ''}
                ${canEquipItem
                  ? `<button type="button" class="tag-chip live inventory-hover-action-btn" data-inventory-action="equip" data-inventory-char="${escapeHtmlAttr(activeCharKey)}" data-inventory-item="${escapeHtmlAttr(name)}">瑁呭</button>`
                  : ''}
                <button type="button" class="tag-chip inventory-hover-action-btn" data-inventory-action="discard" data-inventory-mode="one" data-inventory-char="${escapeHtmlAttr(activeCharKey)}" data-inventory-item="${escapeHtmlAttr(name)}">涓㈠純1浠?/button>
                <button type="button" class="tag-chip inventory-hover-action-btn" data-inventory-action="discard" data-inventory-mode="all" data-inventory-char="${escapeHtmlAttr(activeCharKey)}" data-inventory-item="${escapeHtmlAttr(name)}">${qtyValue > 1 ? '鍏ㄩ儴涓㈠純' : '涓㈠純'}</button>
              </div>
            `
            : '';
          return {
            title: `${htmlEscape(name)} / ${htmlEscape(typeValue)}`,
            desc: [lineOne, lineTwo, lineThree, lineFour, descLine, actionLine].filter(Boolean).join('<br>')
          };
        });
        return {
          title: '鍌ㄧ墿浠撳簱',
          summary: '褰撳墠鑳屽寘銆佽揣甯佷笌鏍稿績鐗╄祫銆?,
          body: `
            <div class="archive-modal-grid">
              <div class="archive-card full">
                <div class="archive-card-head"><div class="archive-card-title">閽卞寘鏉?/div></div>
                ${makeWalletStrip([
                  { label: '鑱旈偊甯?, value: makeInlineEditableValue(formatNumber(wealth.鑱旈偊甯?, { path: ['char', activeCharKey, '璐㈠瘜', '鑱旈偊甯?], kind: 'number', rawValue: wealth.鑱旈偊甯? editorMeta: { min: 0, integer: true, hint: '鏈€灏?0 路 鏁存暟' } }), className: 'gold' },
                  { label: '鏄熺綏甯?, value: makeInlineEditableValue(formatNumber(wealth.鏄熺綏甯?, { path: ['char', activeCharKey, '璐㈠瘜', '鏄熺綏甯?], kind: 'number', rawValue: wealth.鏄熺綏甯? editorMeta: { min: 0, integer: true, hint: '鏈€灏?0 路 鏁存暟' } }), className: 'cyan' },
                  { label: '鍞愰棬绉垎', value: makeInlineEditableValue(formatNumber(wealth.鍞愰棬绉垎), { path: ['char', activeCharKey, '璐㈠瘜', '鍞愰棬绉垎'], kind: 'number', rawValue: wealth.鍞愰棬绉垎, editorMeta: { min: 0, integer: true, hint: '鏈€灏?0 路 鏁存暟' } }), className: 'cyan' },
                  { label: '瀛﹂櫌绉垎', value: makeInlineEditableValue(formatNumber(wealth.瀛﹂櫌绉垎), { path: ['char', activeCharKey, '璐㈠瘜', '瀛﹂櫌绉垎'], kind: 'number', rawValue: wealth.瀛﹂櫌绉垎, editorMeta: { min: 0, integer: true, hint: '鏈€灏?0 路 鏁存暟' } }), className: 'cyan' },
                  { label: '鎴樺姛', value: makeInlineEditableValue(formatNumber(wealth.鎴樺姛), { path: ['char', activeCharKey, '璐㈠瘜', '鎴樺姛'], kind: 'number', rawValue: wealth.鎴樺姛, editorMeta: { min: 0, integer: true, hint: '鏈€灏?0 路 鏁存暟' } }), className: 'red' }
                ])}
              </div>
              <div class="archive-card full">
                <div class="archive-card-head"><div class="archive-card-title">鐗╄祫缁嗛」鎬昏</div><span class="state-tag ${snapshot.inventoryEntries.length ? 'live' : 'warn'}">${snapshot.inventoryEntries.length ? `${snapshot.inventoryEntries.length} 浠禶 : '绌?}</span></div>
                ${activeCharKey ? `
                  <div class="request-console-row" data-collection-panel="inventory-create" style="display:flex; gap:8px; flex-wrap:wrap; margin-bottom:12px;">
                    <input class="request-console-input" style="margin:0; flex:1 1 180px;" data-collection-input="item-name" value="" placeholder="鐗╁搧鍚? />
                    <input type="number" min="1" class="request-console-input" style="margin:0; width:90px; flex:0 0 90px;" data-collection-input="item-qty" value="1" placeholder="鏁伴噺" />
                    <input class="request-console-input" style="margin:0; flex:0 1 140px;" data-collection-input="item-type" value="鐗╁搧" placeholder="绫诲瀷" />
                    <input class="request-console-input" style="margin:0; flex:1 1 220px;" data-collection-input="item-desc" value="" placeholder="鎻忚堪" />
                    <button type="button" class="tag-chip live" data-collection-action="add-item" data-collection-char="${escapeHtmlAttr(activeCharKey)}">鏂板</button>
                  </div>
                ` : ''}
                ${makeTimelineStack(inventoryOverviewCards)}
                ${makeModalPaginationControls('inventory-overview', inventoryOverviewPage.page, inventoryOverviewPage.totalPages, inventoryOverviewPage.total)}
              </div>
              <div class="archive-card full vault-main-card">
                <div class="archive-card-head"><div class="archive-card-title">鑳屽寘鏍奸樀鍒?/div></div>
                <div class="inventory-inline-tools">
                  ${makeTagCloud([
                    { text: '鍏ㄩ儴', className: 'live' },
                    { text: '鏉愭枡' },
                    { text: '娑堣€? },
                    { text: '鎴樺埄鍝?, className: 'warn' },
                    { text: '绁ㄦ嵁' },
                    { text: '鐗规畩', className: 'warn' }
                  ])}
                  ${makeTileGrid([
                    { label: '褰撳墠瑙嗗浘', value: `鍏ㄩ儴 / ${snapshot.inventoryEntries.length}浠禶 },
                    { label: '鎺掑簭', value: '浠峰€?/ 绋€鏈夊害浼樺厛' }
                  ], 'two compact')}
                </div>
                <div class="inventory-scroll-shell">${makeInventoryGrid(inventoryCells)}</div>
              </div>
            </div>
          `
        };
      }

      if (previewKey === PENDING_SOUL_RING_PREVIEW_KEY) {
        const pendingSoulRing = buildPendingSoulRingState(snapshot);
        if (!pendingSoulRing) {
          return {
            title: '榄傜幆鐢熸垚',
            summary: '褰撳墠娌℃湁寰呭鐞嗙殑榄傜幆鐢熸垚璇锋眰銆?,
            body: `
              <div class="archive-modal-grid" style="grid-template-columns: 1fr;">
                <div class="archive-card full">
                  <div class="archive-card-head"><div class="archive-card-title">鏆傛棤寰呭鐞嗚姹?/div></div>
                  <div class="dossier-empty-note">褰撳墠娌℃湁寰呭鐞嗙殑榄傜幆鐢熸垚閫夋嫨銆?/div>
                </div>
              </div>
            `
          };
        }
        const candidateCards = pendingSoulRing.candidates.length
          ? pendingSoulRing.candidates.map(candidate => `
              <div class="soul-expand-card">
                <div class="soul-expand-head">
                  <div>
                    <div class="soul-expand-title">${htmlEscape(candidate.displayName)}</div>
                  </div>
                </div>
                <div class="soul-meta">
                  <div class="meta-item"><b>骞撮檺</b><span>${htmlEscape(candidate.ageText)}</span></div>
                  <div class="meta-item"><b>鍝佽川</b><span>${htmlEscape(candidate.quality)}</span></div>
                  <div class="meta-item"><b>鐘舵€?/b><span>${htmlEscape(candidate.state)}</span></div>
                  <div class="meta-item"><b>濂戝悎搴?/b><span>${htmlEscape(`${candidate.compValue}%`)}</span></div>
                  <div class="meta-item"><b>褰撳墠榄傜幆</b><span>${htmlEscape(String(candidate.ringCount))}</span></div>
                  <div class="meta-item"><b>涓婇檺</b><span>${htmlEscape(String(candidate.ringCapacity))}</span></div>
                  <div class="meta-item"><b>鏈棰滆壊</b><span>${htmlEscape(candidate.nextRingColorLabel)}</span></div>
                  <div class="meta-item"><b>鐢熸垚鍚庡墿浣?/b><span>${htmlEscape(String(candidate.remainingAfterGenerate))}</span></div>
                </div>
                <div style="margin-top:12px;font-size:12px;line-height:1.8;color:var(--color-text-secondary);">
                  <b style="display:block;margin-bottom:6px;color:var(--white);font-size:12px;">榄傜伒鎻忚堪</b>
                  ${htmlEscape(candidate.description)}
                </div>
                <div class="ring-hover-actions" style="margin-top:14px;">
                  <button type="button" class="tag-chip live" data-soul-ring-action="generate" data-soul-ring-candidate="${escapeHtmlAttr(candidate.soulName)}">浣跨敤璇ラ瓊鐏电敓鎴?/button>
                </div>
              </div>
            `).join('')
          : '<div class="dossier-empty-note">褰撳墠娌℃湁鍙€夐瓊鐏碉紝璇峰厛閫氳繃鍓ф儏琛ヨ冻鍊欓€夐瓊鐏点€?/div>';
        return {
          title: '榄傜幆鐢熸垚',
          summary: '妫€娴嬪埌淇负绐佺牬鍚庣殑榄傜幆鐢熸垚閫夋嫨銆?,
          body: `
            <div class="archive-modal-grid" style="grid-template-columns: 1fr;">
              <div class="archive-card full">
                <div class="archive-card-head"><div class="archive-card-title">寰呭鐞嗛瓊鐜?/div></div>
                ${makeDossierRows([
                  { label: '瑙掕壊', value: htmlEscape(pendingSoulRing.charName) },
                  { label: '姝﹂瓊', value: htmlEscape(pendingSoulRing.spiritName) },
                  { label: '榄傜幆浣?, value: htmlEscape(`绗?${pendingSoulRing.ringIndex} 榄傜幆`) },
                  { label: '鐘舵€?, value: htmlEscape(pendingSoulRing.status) },
                ], 'dossier-row-grid--two')}
                <div style="margin-top:12px;font-size:12px;line-height:1.8;color:var(--color-text-secondary);">
                  褰撳墠宸茶揪鍒伴瓊鐜棬妲涖€傝閫夋嫨鏄惁浣跨敤鐜版湁榄傜伒琛嶇敓杩欎竴鏋氶瓊鐜紱鑻ユ殏缂撳鐞嗭紝绯荤粺浼氫繚鎸佹寕璧凤紝涓嶄細缁х画鑷姩绐佺牬銆?                </div>
                <div style="margin-top:10px;font-size:12px;line-height:1.7;color:var(--color-text-secondary);">
                  纭鐢熸垚鍚庝細鑷姩璺宠浆鍥炪€?{htmlEscape(pendingSoulRing.spiritName)}銆戝搴旀榄傝鎯呴〉銆?                </div>
              </div>
              <div class="archive-card full">
                <div class="archive-card-head"><div class="archive-card-title">鍊欓€夐瓊鐏?/div></div>
                <div class="soul-expand-stack">
                  ${candidateCards}
                </div>
                <div class="ring-hover-actions" style="margin-top:14px;">
                  <button type="button" class="tag-chip" data-soul-ring-action="defer">鏆備笉鐢熸垚</button>
                </div>
              </div>
            </div>
          `
        };
      }

      if (previewKey === '绗竴姝﹂瓊璇︾粏椤? || previewKey === '绗簩姝﹂瓊璇︾粏椤?) {
        const secondaryTrack = snapshot.secondaryTrack || null;
        const config = previewKey === '绗竴姝﹂瓊璇︾粏椤?
          ? snapshot.primarySpirit
          : (secondaryTrack && secondaryTrack.kind === 'bloodline' ? null : secondaryTrack);

        if (!config) return null;
        const pendingSoulRing = buildPendingSoulRingState(snapshot);
        const hasPendingSoulRingForCurrentSpirit = !!(pendingSoulRing && pendingSoulRing.spiritSlot === config.slotName);
        return {
          title: '姝﹂瓊',
          summary: '姝﹂瓊銆侀瓊鐏点€侀瓊鐜笌榄傛妧鐨勫疄鏃跺睍寮€銆?,
          body: `
            <div class="archive-modal-grid">
              <div class="archive-card full">
                <div class="archive-card-head"><div class="archive-card-title">姝﹂瓊</div></div>
                <div class="spirit-main-card">
                  <h4>${config.spiritPath && config.spiritPath.length
                    ? makeInlineEditableValue(config.spiritName, { path: [...config.spiritPath, '琛ㄨ薄鍚嶇О'], kind: 'string', rawValue: config.spiritName })
                    : htmlEscape(config.spiritName)}</h4>
                  <div class="spirit-head-tags">
                    <span class="tag-chip ${config.badgeClass === 'gold' ? 'warn' : 'live'}">${htmlEscape(config.badge)}</span>
                    <span class="tag-chip">${config.spiritPath && config.spiritPath.length
                      ? makeInlineEditableValue(config.spiritType, { path: [...config.spiritPath, 'type'], kind: 'string', rawValue: config.spiritType })
                      : htmlEscape(config.spiritType)}</span>
                    <span class="tag-chip">${config.spiritPath && config.spiritPath.length
                      ? makeInlineEditableValue(`灞炴€э細${config.spiritElement}`, {
                        path: [...config.spiritPath, '灞炴€т綋绯?],
                        kind: 'enum_select',
                        rawValue: config.spiritElement,
                        editorMeta: { options: SPIRIT_ATTRIBUTE_SYSTEM_OPTIONS }
                      })
                      : htmlEscape(`灞炴€э細${config.spiritElement}`)}</span>
                    <span class="tag-chip">${htmlEscape(`榄傜伒锛?{toNumber(config.soulCount, 0)}`)}</span>
                    <span class="tag-chip">${htmlEscape(`鐙珛鐜細${toNumber(config.independentRingCount, 0)}`)}</span>
                    ${hasPendingSoulRingForCurrentSpirit ? `<button type="button" class="tag-chip warn clickable" data-preview="${escapeHtmlAttr(PENDING_SOUL_RING_PREVIEW_KEY)}">寰呯敓鎴愰瓊鐜?/button>` : ''}
                  </div>
                  <div style="margin-top:12px;font-size:12px;line-height:1.8;color:var(--color-text-secondary);">
                    <b style="display:block;margin-bottom:6px;color:var(--white);font-size:12px;">姝﹂瓊鎻忚堪</b>
                    ${config.spiritPath && config.spiritPath.length
                      ? makeInlineEditableValue(config.spiritDesc, { path: [...config.spiritPath, '鎻忚堪'], kind: 'string', rawValue: config.spiritDesc, multiline: true })
                      : htmlEscape(config.spiritDesc)}
                  </div>
                  <div class="soul-meta" style="margin-top:12px;">
                    <div class="meta-item"><b>宸茶В閿佸睘鎬?/b><span>${config.spiritPath && config.spiritPath.length
                      ? makeInlineEditableValue(config.spiritUnlockedAttrs.join('銆?) || '鏈缃?, {
                        path: [...config.spiritPath, '宸茶В閿佸睘鎬?],
                        kind: 'token_multi',
                        rawValue: config.spiritUnlockedAttrs,
                        editorMeta: { options: config.spiritElement === '浜旇' ? WUXING_ATTRIBUTE_TOKEN_OPTIONS : SPIRIT_ATTRIBUTE_TOKEN_OPTIONS }
                      })
                      : htmlEscape(config.spiritUnlockedAttrs.join('銆?) || '鏈缃?)}</span></div>
                    <div class="meta-item"><b>鍙绾冲睘鎬?/b><span>${config.spiritPath && config.spiritPath.length
                      ? makeInlineEditableValue(config.spiritCapacityAttrs.join('銆?) || '鏈缃?, {
                        path: [...config.spiritPath, '鍙绾冲睘鎬?],
                        kind: 'token_multi',
                        rawValue: config.spiritCapacityAttrs,
                        editorMeta: { options: config.spiritElement === '浜旇' ? WUXING_ATTRIBUTE_TOKEN_OPTIONS : SPIRIT_ATTRIBUTE_TOKEN_OPTIONS }
                      })
                      : htmlEscape(config.spiritCapacityAttrs.join('銆?) || '鏈缃?)}</span></div>
                  </div>
                </div>
              </div>
              <div class="archive-card full spirit-flow-card">
                <div class="archive-card-head"><div class="archive-card-title">榄傜伒</div></div>
                <div class="soul-expand-stack">
                  ${config.souls.map(soul => `
                    <div class="soul-expand-card">
                      <div class="soul-expand-head">
                        <div>
                          <div class="soul-expand-title">${htmlEscape(soul.name)}</div>
                        </div>
                      </div>
                      <div class="soul-meta">
                        <div class="meta-item"><b>榄傜伒鍚?/b><span>${soul.path && soul.path.length
                          ? makeInlineEditableValue(soul.spiritName, { path: [...soul.path, '琛ㄨ薄鍚嶇О'], kind: 'string', rawValue: soul.spiritName })
                          : htmlEscape(soul.spiritName)}</span></div>
                        <div class="meta-item"><b>鍝佽川</b><span>${soul.path && soul.path.length
                          ? makeInlineEditableValue(soul.quality, { path: [...soul.path, '鍝佽川'], kind: 'string', rawValue: soul.quality })
                          : htmlEscape(soul.quality)}</span></div>
                        <div class="meta-item"><b>鐘舵€?/b><span>${soul.path && soul.path.length
                          ? makeInlineEditableValue(soul.state, { path: [...soul.path, '鐘舵€?], kind: 'string', rawValue: soul.state })
                          : htmlEscape(soul.state)}</span></div>
                        <div class="meta-item"><b>骞撮檺</b><span>${soul.path && soul.path.length
                          ? makeInlineEditableValue(soul.age, { path: [...soul.path, '骞撮檺'], kind: 'number', rawValue: soul.ageValue, editorMeta: { min: 0, integer: true, hint: '鏈€灏?0 路 鏁存暟' } })
                          : htmlEscape(soul.age)}</span></div>
                        <div class="meta-item"><b>濂戝悎搴?/b><span>${soul.path && soul.path.length
                          ? makeInlineEditableValue(soul.comp, { path: [...soul.path, '濂戝悎搴?], kind: 'number', rawValue: soul.compValue, editorMeta: { min: 0, max: 100, integer: true, hint: '鑼冨洿 0 - 100 路 鏁存暟' } })
                          : htmlEscape(soul.comp)}</span></div>
                      </div>
                      <div style="margin-top:12px;font-size:12px;line-height:1.8;color:var(--color-text-secondary);">
                        <b style="display:block;margin-bottom:6px;color:var(--white);font-size:12px;">榄傜伒鎻忚堪</b>
                        ${soul.path && soul.path.length
                          ? makeInlineEditableValue(soul.description, { path: [...soul.path, '鎻忚堪'], kind: 'string', rawValue: soul.description, multiline: true })
                          : htmlEscape(soul.description)}
                      </div>
                      ${soul.榄傜幆.length ? `
                        <div class="soul-ring-section">
                          <div class="rings soul-ring-lane">
                            ${soul.榄傜幆.map(ring => `<div class="ring ${ring.ringClass} interactive-ring">${htmlEscape(ring.glyph)}${buildRingHoverMarkup(ring)}</div>`).join('')}
                          </div>
                        </div>
                      ` : ''}
                    </div>
                  `).join('')}
                  ${config.independentRings && config.independentRings.length ? `
                    <div class="soul-expand-card">
                      <div class="soul-expand-head">
                        <div>
                          <div class="soul-expand-title">鐙珛榄傜幆</div>
                        </div>
                      </div>
                      <div class="soul-meta">
                        <div class="meta-item"><b>鏁伴噺</b><span>${htmlEscape(String(config.independentRings.length))}</span></div>
                        <div class="meta-item"><b>璇存槑</b><span>鐙珛鎵胯浇锛屼笉缁戝畾榄傜伒妲戒綅</span></div>
                      </div>
                      <div class="soul-ring-section">
                        <div class="rings soul-ring-lane">
                          ${config.independentRings.map(ring => `<div class="ring ${ring.ringClass} interactive-ring">${htmlEscape(ring.glyph)}${buildRingHoverMarkup(ring)}</div>`).join('')}
                        </div>
                      </div>
                    </div>
                  ` : ''}
                </div>
              </div>
            </div>
          `
        };
      }

      if (previewKey === '姝﹂瓊铻嶅悎鎶€璇︾粏椤?) {
        const activeCharKey = resolveSnapshotCharKey(snapshot, toText(snapshot.activeName, '')) || toText(snapshot.activeName, '');
        const fusionArchiveMeta = getFusionArchiveMeta(snapshot);
        const { fusionEntries, partnerCount, selfCount } = fusionArchiveMeta;
        const getFusionRecordParticipants = fusion => normalizeSkillDesignerFusionParticipantList(
          getSkillDesignerRawFusionParticipants({}, fusion && typeof fusion === 'object' ? fusion : {}),
          snapshot.rootData,
        );
        const getFusionRecordModeText = fusion => {
          const safeFusion = fusion && typeof fusion === 'object' ? fusion : {};
          const participants = getFusionRecordParticipants(safeFusion);
          if (toText(safeFusion.铻嶅悎妯″紡, 'partner') === 'self') return '鑷綋铻嶅悎';
          return participants.length > 2 ? '澶氫汉铻嶅悎' : '鍙屼汉铻嶅悎';
        };
        const getFusionRecordPartnerText = fusion => {
          const safeFusion = fusion && typeof fusion === 'object' ? fusion : {};
          const participants = getFusionRecordParticipants(safeFusion);
          if (toText(safeFusion.铻嶅悎妯″紡, 'partner') === 'self') return SKILL_DESIGNER_SELF_FUSION_PARTNER;
          const names = participants
            .filter(participant => participant.role !== 'self')
            .map(participant => participant.charName || participant.charKey)
            .filter(Boolean);
          return names.length ? Array.from(new Set(names)).join('銆?) : toText(safeFusion['铻嶅悎瀵硅薄'], '鏈煡鎼。');
        };
        const getFusionRecordParticipantText = fusion => {
          const safeFusion = fusion && typeof fusion === 'object' ? fusion : {};
          const participants = getFusionRecordParticipants(safeFusion);
          if (participants.length) {
            return participants
              .map(participant => `${participant.charName || participant.charKey || '鏈缃?}:${participant.spirit || '鏈缃?}`)
              .join(' + ');
          }
          return (Array.isArray(safeFusion.鏉ユ簮姝﹂瓊) ? safeFusion.鏉ユ簮姝﹂瓊 : []).filter(Boolean).join(' / ') || '鏈褰?;
        };
        const createFusionPreview = activeCharKey
          ? buildSkillDesignerPreviewKey({
              path: ['char', activeCharKey, '姝﹂瓊铻嶅悎鎶€', `姝﹂瓊铻嶅悎鎶€_${Date.now().toString(36)}_${Math.random().toString(36).slice(2, 6)}`, '鎶€鑳芥暟鎹?],
              label: '鏂板缓姝﹂瓊铻嶅悎鎶€',
              category: '姝﹂瓊铻嶅悎鎶€',
              scope: '姝﹂瓊铻嶅悎鎶€',
            })
          : '';
        const leadFusionEntry = fusionEntries[0] || null;
        const leadRecordKey = leadFusionEntry ? leadFusionEntry[0] : '';
        const leadFusion = leadFusionEntry && leadFusionEntry[1] && typeof leadFusionEntry[1] === 'object' ? leadFusionEntry[1] : {};
        const leadSkillData = leadFusion.鎶€鑳芥暟鎹?&& typeof leadFusion.鎶€鑳芥暟鎹?=== 'object' ? leadFusion.鎶€鑳芥暟鎹?: {};
        const leadFusionName = leadFusionEntry
          ? toText(leadSkillData['榄傛妧鍚?] || leadSkillData.name || leadFusion['鍚嶇О'] || leadFusion.name || leadRecordKey, leadRecordKey || '鏈懡鍚嶈瀺鍚堟妧')
          : '';
        const leadModeValue = toText(leadFusion.铻嶅悎妯″紡, 'partner');
        const leadModeText = getFusionRecordModeText(leadFusion);
        const leadPartnerRaw = getFusionRecordPartnerText(leadFusion);
        const leadSourceSpiritText = getFusionRecordParticipantText(leadFusion);
        const leadEffectDesc = toText(leadSkillData['鏁堟灉鎻忚堪'] || leadSkillData['鎻忚堪'] || '', '');
        const leadVisualDesc = toText(leadSkillData['鐢婚潰鎻忚堪'] || '', '');
        const leadPreview = activeCharKey && leadRecordKey
          ? buildSkillDesignerPreviewKey({
              path: ['char', activeCharKey, '姝﹂瓊铻嶅悎鎶€', leadRecordKey, '鎶€鑳芥暟鎹?],
              label: leadFusionName,
              category: '姝﹂瓊铻嶅悎鎶€',
              scope: '姝﹂瓊铻嶅悎鎶€',
            })
          : '';
        const fusionList = fusionEntries.map(([recordKey, fusion]) => {
          const safeFusion = fusion && typeof fusion === 'object' ? fusion : {};
          const skillData = safeFusion.鎶€鑳芥暟鎹?&& typeof safeFusion.鎶€鑳芥暟鎹?=== 'object' ? safeFusion.鎶€鑳芥暟鎹?: {};
          const fusionName = toText(skillData['榄傛妧鍚?] || skillData.name || safeFusion['鍚嶇О'] || safeFusion.name || recordKey, recordKey || '鏈懡鍚嶈瀺鍚堟妧');
          const modeText = getFusionRecordModeText(safeFusion);
          const partnerText = getFusionRecordPartnerText(safeFusion);
          const sourceSpiritText = getFusionRecordParticipantText(safeFusion);
          const effectDesc = toText(skillData['鏁堟灉鎻忚堪'] || skillData['鎻忚堪'] || '鏈煡', '鏈煡');
          const visualDesc = toText(skillData['鐢婚潰鎻忚堪'] || '', '');
          const tags = (Array.isArray(skillData['鏍囩']) ? skillData['鏍囩'] : []).filter(Boolean);
          const preview = activeCharKey
            ? buildSkillDesignerPreviewKey({
                path: ['char', activeCharKey, '姝﹂瓊铻嶅悎鎶€', recordKey, '鎶€鑳芥暟鎹?],
                label: fusionName,
                category: '姝﹂瓊铻嶅悎鎶€',
                scope: '姝﹂瓊铻嶅悎鎶€',
              })
            : '';
          const descParts = [
            `<strong>${htmlEscape(modeText)} 路 ${htmlEscape(partnerText)}</strong>`,
            `<small>鍙備笌姝﹂瓊锛?{htmlEscape(sourceSpiritText)}</small>`,
            effectDesc !== '鏈煡' ? `<small>${htmlEscape(effectDesc)}</small>` : '',
            visualDesc ? `<small>${htmlEscape(visualDesc)}</small>` : '',
            tags.length ? `<small>${htmlEscape(tags.slice(0, 4).join(' / '))}</small>` : ''
          ].filter(Boolean);
          return {
            title: fusionName,
            desc: descParts.join(''),
            preview,
          };
        });
        return {
          title: '姝﹂瓊铻嶅悎鎶€妗ｆ',
          summary: '闆嗕腑鏌ョ湅褰撳墠瑙掕壊鐨勮嚜浣撹瀺鍚堜笌澶氫汉铻嶅悎鎶€銆?,
          body: `
            <div class="archive-modal-grid dossier-shell">
              <div class="archive-card dossier-card">
                <div class="archive-card-head">
                  <div class="archive-card-title">姝﹂瓊铻嶅悎鎶€姒傝</div>
                  <span class="dossier-pill ${fusionEntries.length ? 'live' : 'warn'}">${fusionEntries.length ? `${fusionEntries.length} 椤筦 : '鏈敹褰?}</span>
                </div>
                <section class="dossier-section">
                  <div class="dossier-section-title">鍩虹缁熻</div>
                  ${makeDossierRows([
                    { label: '褰撳墠瑙掕壊', value: htmlEscape(snapshot.activeName) },
                    { label: '姝﹂瓊铻嶅悎鎶€鎬绘暟', value: htmlEscape(String(fusionEntries.length)) },
                    { label: '澶栭儴铻嶅悎', value: htmlEscape(String(partnerCount)) },
                    { label: '鑷綋铻嶅悎', value: htmlEscape(String(selfCount)) }
                  ], 'dossier-row-grid--two')}
                </section>
                <section class="dossier-section">
                  <div class="dossier-section-title">褰撳墠涓绘。</div>
                  ${fusionEntries.length
                    ? `
                      ${makeDossierRows([
                        { label: '褰撳墠鎷涚墝', value: leadPreview ? `<button type="button" class="dossier-pill live clickable" data-preview="${escapeHtmlAttr(leadPreview)}">${htmlEscape(leadFusionName)}</button>` : htmlEscape(leadFusionName) },
                        { label: '铻嶅悎妯″紡', value: activeCharKey && leadRecordKey
                          ? makeInlineEditableValue(leadModeText, {
                              path: ['char', activeCharKey, '姝﹂瓊铻嶅悎鎶€', leadRecordKey, '铻嶅悎妯″紡'],
                              kind: 'string',
                              rawValue: leadModeValue,
                              editorMeta: { hint: '鍙～ self 鎴?partner' },
                            })
                          : htmlEscape(leadModeText) },
                        { label: '鎼。', value: activeCharKey && leadRecordKey
                          ? makeInlineEditableValue(leadPartnerRaw, {
                              path: ['char', activeCharKey, '姝﹂瓊铻嶅悎鎶€', leadRecordKey, '铻嶅悎瀵硅薄'],
                              kind: 'string',
                              rawValue: toText(leadFusion['铻嶅悎瀵硅薄'], ''),
                            })
                          : htmlEscape(leadPartnerRaw) },
                        { label: '鍙備笌姝﹂瓊', value: htmlEscape(leadSourceSpiritText), className: 'dossier-row--wide' },
                        { label: '鏁堟灉鎽樿', value: htmlEscape(leadEffectDesc || leadVisualDesc || '鏈褰?), className: 'dossier-row--wide' }
                      ])}
                    `
                    : `
                      ${makeDossierRows([
                        { label: '涓绘。璁板綍', value: '鏈敹褰? },
                        { label: '缂栬緫鍏ュ彛', value: createFusionPreview ? `<button type="button" class="dossier-pill live clickable" data-preview="${escapeHtmlAttr(createFusionPreview)}">鏂板缓姝﹂瓊铻嶅悎鎶€</button>` : '鏆傛棤鍙紪杈戝璞? }
                      ])}
                    `}
                </section>
              </div>
              <div class="archive-card dossier-card">
                <div class="archive-card-head">
                  <div class="archive-card-title">姝﹂瓊铻嶅悎鎶€娓呭崟</div>
                  <div class="dossier-tag-row dossier-head-actions">
                    ${createFusionPreview ? `<button type="button" class="dossier-pill live clickable" data-preview="${escapeHtmlAttr(createFusionPreview)}">鏂板缓姝﹂瓊铻嶅悎鎶€</button>` : ''}
                  </div>
                </div>
                <section class="dossier-section">
                  <div class="dossier-section-title">宸叉敹褰曟潯鐩?/div>
                  ${fusionList.length
                    ? makeDossierList(fusionList, 'dossier-list--fusion')
                    : '<div class="dossier-empty-note">鏈敹褰曟榄傝瀺鍚堟妧銆?/div>'}
                </section>
              </div>
            </div>
          `
        };
      }




      if (previewKey === '琛€鑴夊皝鍗拌缁嗛〉') {
        const activeCharKey = resolveSnapshotCharKey(snapshot, toText(snapshot.activeName, '')) || toText(snapshot.activeName, '');
        const bloodlinePath = activeCharKey ? ['char', activeCharKey, '琛€鑴変箣鍔?] : [];
        const bloodMainSkill = snapshot.bloodline.bloodSkills[0] || null;
        const bloodlineRawName = toText(deepGet(snapshot, 'activeChar.琛€鑴変箣鍔?琛€鑴?, '鏃?), '鏃?);
        return {
          title: '琛€鑴夊皝鍗板脊绐?,
          summary: '琛€鑴夊眰绾с€佷綋鍔涢瓊鐜笌褰撳墠宸插浐鍖栬兘鍔涖€?,
          body: `
            <div class="archive-modal-grid">
              <div class="archive-card full">
                <div class="archive-card-head"><div class="archive-card-title">琛€鑴夋湰浣?/div></div>
                <div class="spirit-main-card">
                  <h4>${bloodlinePath.length
                    ? makeInlineEditableValue(snapshot.bloodline.bloodline, {
                        path: [...bloodlinePath, 'bloodline'],
                        kind: 'string',
                        rawValue: bloodlineRawName,
                      })
                    : htmlEscape(snapshot.bloodline.bloodline)}</h4>
                  <div class="spirit-head-tags">
                    <span class="tag-chip warn">琛€鑴夊皝鍗?/span>
                    <span class="tag-chip">${bloodlinePath.length
                      ? makeInlineEditableValue(`绗?{snapshot.bloodline.sealLv}灞俙, {
                          path: [...bloodlinePath, 'seal_lv'],
                          kind: 'number',
                          rawValue: snapshot.bloodline.sealLv,
                          editorMeta: { min: 0, integer: true, hint: '鏈€灏?0 路 鏁存暟' },
                        })
                      : htmlEscape(`绗?{snapshot.bloodline.sealLv}灞俙)}</span>
                    <span class="tag-chip">${bloodlinePath.length
                      ? makeInlineEditableValue(snapshot.bloodline.core, {
                          path: [...bloodlinePath, 'core'],
                          kind: 'string',
                          rawValue: snapshot.bloodline.core,
                        })
                      : htmlEscape(snapshot.bloodline.core)}</span>
                    <span class="tag-chip">${htmlEscape(snapshot.bloodline.lifeFire ? '鐢熷懡涔嬬伀' : '鏈偣鐕?) }</span>
                  </div>
                </div>
              </div>
              <div class="archive-card">
                <div class="archive-card-head"><div class="archive-card-title">灏佸嵃灞傜骇</div></div>
                ${makeSealColumn(Array.from({ length: Math.max(4, snapshot.bloodline.sealLv + 2) }).map((_, index) => ({
                  label: `绗?{index + 1}灞傚皝鍗癭,
                  state: index < snapshot.bloodline.sealLv ? '宸茶В' : (index % 2 === 1 ? '閲戠幆浣? : '鏈В'),
                  className: index < snapshot.bloodline.sealLv ? 'active' : 'locked'
                })))}
              </div>
              <div class="archive-card">
                <div class="archive-card-head"><div class="archive-card-title">褰撳墠涓诲姩鑳藉姏</div></div>
                <div class="ability-detail-card${bloodMainSkill && bloodMainSkill.preview ? ' clickable' : ''}"${bloodMainSkill && bloodMainSkill.preview ? ` data-preview="${escapeHtmlAttr(bloodMainSkill.preview)}"` : ''}>
                  <div class="ability-detail-title">${htmlEscape(bloodMainSkill ? bloodMainSkill.name : '鏆傛棤涓诲姩鑳藉姏')}</div>
                  <div class="ring-hover-copy"><em>鐢婚潰鎻忚堪</em><span>${htmlEscape(bloodMainSkill ? bloodMainSkill.visualDesc : '鏈煡')}</span></div>
                  <div class="ring-hover-copy"><em>鏁堟灉鎻忚堪</em><span>${htmlEscape(bloodMainSkill ? bloodMainSkill.effectDesc : '鏈煡')}</span></div>
                </div>
              </div>
              ${snapshot.bloodline.bloodPermanentBonuses && snapshot.bloodline.bloodPermanentBonuses.length ? `
                <div class="archive-card">
                  <div class="archive-card-head"><div class="archive-card-title">姘镐箙鎴愰暱</div></div>
                  <div class="ability-detail-card">
                    ${snapshot.bloodline.bloodPermanentBonuses.slice(0, 4).map(item => `<div class="ring-hover-copy"><em>${htmlEscape(item.name || '姘镐箙鎴愰暱')}</em><span>${htmlEscape(item.effectDesc || item.effectSummary || '姘镐箙鎴愰暱')}</span></div>`).join('')}
                  </div>
                </div>
              ` : ''}
              ${snapshot.bloodline.bloodPassives && snapshot.bloodline.bloodPassives.length ? `
                <div class="archive-card">
                  <div class="archive-card-head"><div class="archive-card-title">琚姩鐗规€?/div></div>
                  <div class="ability-detail-card">
                    ${snapshot.bloodline.bloodPassives.slice(0, 4).map(skill => `<div class="ring-hover-copy"><em>${htmlEscape(skill.name || '琚姩鐗规€?)}</em><span>${htmlEscape(skill.effectDesc || skill.visualDesc || '琚姩鐗规€?)}</span></div>`).join('')}
                  </div>
                </div>
              ` : ''}
              ${snapshot.bloodline.榄傜幆.length ? `
                <div class="archive-card full spirit-flow-card">
                  <div class="archive-card-head"><div class="archive-card-title">浣撳姏榄傜幆杞ㄩ亾</div></div>
                  <div class="orbit-track">
                    ${snapshot.bloodline.榄傜幆.map(ring => `<div class="ring ${ring.ringClass || 'ring-gold'} interactive-ring">${htmlEscape(ring.glyph)}${buildRingHoverMarkup(ring)}</div>`).join('')}
                  </div>
                </div>
              ` : ''}
            </div>
          `
        };
      }


      if (previewKey === '缂栧勾鍙叉。妗?) {
        const nodes = snapshot.timelineEntries.slice(0, 6).reverse();
        const focus = snapshot.latestTimeline;
        return {
          title: '缂栧勾鍙插脊绐?,
          summary: '妯悜鏃堕棿杞翠笌鏈€杩戜簨浠躲€?,
          body: `
            <div class="archive-modal-grid" style="grid-template-columns: 1fr;">
              <div class="archive-card full">
                <div class="archive-card-head"><div class="archive-card-title">瀹忚鏃堕棿杞?/div></div>
                <div class="history-timeline-wrap"><div class="history-timeline-track">
                  ${nodes.map(([name, item], index) => `
                    <div class="history-node ${index === nodes.length - 1 ? 'major' : ''}">
                      <div class="history-node-date">Tick ${htmlEscape(toText(item && item.瑙﹀彂tick, 0))}</div>
                      <div class="history-node-dot"></div>
                      <div class="history-node-label">${htmlEscape(name)}</div>
                    </div>
                  `).join('') || '<div class="history-node major"><div class="history-node-date">Tick 0</div><div class="history-node-dot"></div><div class="history-node-label">缂栧勾鍙叉湭灞曞紑</div></div>'}
                </div></div>
                <div class="history-floating-card">
                  <div class="history-floating-title">${htmlEscape(focus ? `銆愬綋鍓嶉敋鐐广€?{focus[0]}` : '銆愬綋鍓嶉敋鐐广€戝緟寮€鍚?)}</div>
                  <div class="history-floating-desc">${htmlEscape(focus ? toText(deepGet(focus[1], '浜嬩欢', '鏆傛棤鎻忚堪'), '鏆傛棤鎻忚堪') : '涓栫晫鏃堕棿绾垮皻鏈睍寮€銆?)}</div>
                </div>
              </div>
              <div class="archive-card full">
                <div class="archive-card-head"><div class="archive-card-title">杩戞湡璇︾粏鏃ュ織</div></div>
                ${makeTimelineStack(snapshot.timelineEntries.slice(0, 5).map(([name, item]) => ({ title: `${name} / Tick ${toText(item && item.瑙﹀彂tick, 0)}`, desc: `${toText(item && item.鐘舵€? 'pending')} 锝?${toText(item && item.浜嬩欢, '鏆傛棤鎻忚堪')}` }))) }
              </div>
            </div>
          `
        };
      }

      if (String(previewKey || '').startsWith('瑙掕壊妗ｆ锛?)) {
        const targetName = String(previewKey).replace('瑙掕壊妗ｆ锛?, '').trim();
        const rootChars = deepGet(snapshot, 'rootData.char', {});
        let targetCharKey = targetName;
        let targetChar = deepGet(snapshot, ['sd', 'char', targetName], null) || (rootChars && typeof rootChars === 'object' ? (rootChars[targetName] || null) : null);
        if (!targetChar && rootChars && typeof rootChars === 'object') {
          for (const [charKey, charInfo] of Object.entries(rootChars)) {
            const displayName = toText(charInfo && (charInfo.name || deepGet(charInfo, 'base.name', '')), charKey);
            if (displayName === targetName) { targetCharKey = charKey; targetChar = charInfo; break; }
          }
        }
        const targetCharPath = targetChar ? ['char', targetCharKey] : [];
        const targetStat = deepGet(targetChar, '灞炴€?, {});
        const targetSocial = deepGet(targetChar, '绀句氦', {});
        const targetArmor = deepGet(targetChar, '瑁呭.鏂楅摖', {});
        const targetMech = deepGet(targetChar, '瑁呭.鏈虹敳', {});
        const targetWeapon = deepGet(targetChar, '瑁呭.姝﹀櫒', {});
        const targetSpiritEntries = safeEntries(deepGet(targetChar, '姝﹂瓊', {}));
        return {
          title: `${targetName} / 瑙掕壊鍩烘湰淇℃伅`,
          summary: '瑙掕壊杞婚噺淇℃伅闈㈡澘锛氬睘鎬с€佹榄備笌瑁呭姒傝銆?,
          body: `
            <div class="archive-modal-grid">
              <div class="archive-card full">
                <div class="archive-card-head"><div class="archive-card-title">鍩虹淇℃伅</div></div>
                ${makeTileGrid([
                  { label: '瑙掕壊鍚?, value: targetName || '鏈煡' },
                  { label: '绛夌骇', value: targetChar ? formatCultivationLevelBadge(targetStat.绛夌骇, '0') : '鏈敹褰? },
                  { label: '绯诲埆', value: targetCharPath.length
                    ? makeInlineEditableValue(toText(targetStat.绯诲埆, '鏈煡'), {
                        path: [...targetCharPath, '灞炴€?, '绯诲埆'],
                        kind: 'string',
                        rawValue: toText(targetStat.绯诲埆, '鏈煡'),
                      })
                    : (targetChar ? toText(targetStat.绯诲埆, '鏈煡') : '鏈敹褰?) },
                  { label: '鍏紑韬唤', value: targetCharPath.length
                    ? makeInlineEditableValue(toText(targetSocial.涓昏韩浠? '鏃?), {
                        path: [...targetCharPath, '绀句氦', '涓昏韩浠?],
                        kind: 'string',
                        rawValue: toText(targetSocial.涓昏韩浠? '鏃?),
                      })
                    : (targetChar ? toText(targetSocial.涓昏韩浠? '鏃?) : '鏈敹褰?) },
                  { label: '鍚嶆湜', value: targetCharPath.length
                    ? makeInlineEditableValue(formatNumber(targetSocial.澹版湜), {
                        path: [...targetCharPath, '绀句氦', '澹版湜'],
                        kind: 'number',
                        rawValue: toNumber(targetSocial.澹版湜, 0),
                        editorMeta: { min: 0, integer: true, hint: '鏈€灏?0 路 鏁存暟' },
                      })
                    : (targetChar ? formatNumber(targetSocial.澹版湜) : '鏈敹褰?) },
                  { label: '榄傚姏', value: targetChar ? `${formatNumber(targetStat.榄傚姏)} / ${formatNumber(targetStat.榄傚姏涓婇檺)}` : '鏈敹褰? },
                  { label: '浣撳姏', value: targetChar ? `${formatNumber(targetStat.浣撳姏)} / ${formatNumber(targetStat.浣撳姏涓婇檺)}` : '鏈敹褰? },
                  { label: '绮剧鍔?, value: targetChar ? `${formatNumber(targetStat.绮剧鍔?} / ${formatNumber(targetStat.绮剧鍔涗笂闄?}` : '鏈敹褰? },
                  { label: '鍔涢噺', value: targetChar ? formatNumber(targetStat.鍔涢噺) : '鏈敹褰? },
                  { label: '闃插尽', value: targetChar ? formatNumber(targetStat.闃插尽) : '鏈敹褰? },
                  { label: '鏁忔嵎', value: targetChar ? formatNumber(targetStat.鏁忔嵎) : '鏈敹褰? }
                ], 'three')}
              </div>
              <div class="archive-card full"><div class="archive-card-head"><div class="archive-card-title">姝﹂瓊姒傝</div></div>${makeTimelineStack(targetSpiritEntries.length ? targetSpiritEntries.map(([spiritName, spirit]) => {
                const spiritPath = targetCharPath.length ? [...targetCharPath, '姝﹂瓊', spiritName] : [];
                return {
                  title: spiritName,
                  desc: [
                    `绫诲瀷 ${spiritPath.length
                      ? makeInlineEditableValue(toText(spirit && spirit.type, '鏈煡'), {
                          path: [...spiritPath, 'type'],
                          kind: 'string',
                          rawValue: toText(spirit && spirit.type, '鏈煡'),
                        })
                      : htmlEscape(toText(spirit && spirit.type, '鏈煡'))}`,
                    `鍏冪礌 ${spiritPath.length
                      ? makeInlineEditableValue(toText(spirit && spirit['灞炴€т綋绯?], '鏃?), {
                          path: [...spiritPath, '灞炴€т綋绯?],
                          kind: 'string',
                          rawValue: toText(spirit && spirit['灞炴€т綋绯?], '鏃?),
                        })
                      : htmlEscape(toText(spirit && spirit['灞炴€т綋绯?], '鏃?))}`,
                    `榄傜伒 ${safeEntries(deepGet(spirit, '榄傜伒', {})).length}`,
                  ].join(' / ')
                };
              }) : [{ title: '鏆傛棤姝﹂瓊璁板綍', desc: targetChar ? '褰撳墠瑙掕壊鏈褰曟榄傛暟鎹€? : '璇ヨ鑹叉殏鏃犺鑹叉。妗堛€? }])}</div>
              <div class="archive-card full"><div class="archive-card-head"><div class="archive-card-title">瑁呭姒傝</div></div>${makeTileGrid([
                { label: '鏂楅摖', value: targetCharPath.length
                  ? `${makeInlineEditableValue(toText(targetArmor.鍚嶇О, '鏃?), {
                      path: [...targetCharPath, '瑁呭', '鏂楅摖', '鍚嶇О'],
                      kind: 'string',
                      rawValue: toText(targetArmor.鍚嶇О, '鏃?),
                    })} / ${makeInlineEditableValue(toText(targetArmor.瑁呭鐘舵€? '鏈澶?), {
                      path: [...targetCharPath, '瑁呭', '鏂楅摖', '瑁呭鐘舵€?],
                      kind: 'string',
                      rawValue: toText(targetArmor.瑁呭鐘舵€? '鏈澶?),
                    })}`
                  : (targetChar ? (toText(targetArmor.鍚嶇О, '鏃?) !== '鏃? ? `${toText(targetArmor.鍚嶇О, '鏃?)} / ${toText(targetArmor.瑁呭鐘舵€? '鏈澶?)}` : '鏃?) : '鏈敹褰?) },
                { label: '鏈虹敳', value: targetCharPath.length
                  ? `${makeInlineEditableValue(toText(targetMech.绛夌骇, '鏃?), {
                      path: [...targetCharPath, '瑁呭', '鏈虹敳', '绛夌骇'],
                      kind: 'string',
                      rawValue: toText(targetMech.绛夌骇, '鏃?),
                    })}路${makeInlineEditableValue(toText(targetMech.鍨嬪彿, '鏈畾鍨?), {
                      path: [...targetCharPath, '瑁呭', '鏈虹敳', '鍨嬪彿'],
                      kind: 'string',
                      rawValue: toText(targetMech.鍨嬪彿, '鏈畾鍨?),
                    })} / ${makeInlineEditableValue(toText(targetMech.瑁呭鐘舵€? '鏈澶?), {
                      path: [...targetCharPath, '瑁呭', '鏈虹敳', '瑁呭鐘舵€?],
                      kind: 'string',
                      rawValue: toText(targetMech.瑁呭鐘舵€? '鏈澶?),
                    })}`
                  : (targetChar ? (toText(targetMech.绛夌骇, '鏃?) !== '鏃? ? `${toText(targetMech.绛夌骇, '鏃?)}路${toText(targetMech.鍨嬪彿, '鏈畾鍨?)} / ${toText(targetMech.瑁呭鐘舵€? '鏈澶?)}` : '鏃?) : '鏈敹褰?) },
                { label: '涓绘鍣?, value: targetCharPath.length
                  ? `${makeInlineEditableValue(toText(targetWeapon['鍚嶇О'], '鏃?), {
                      path: [...targetCharPath, '瑁呭', '姝﹀櫒', '鍚嶇О'],
                      kind: 'string',
                      rawValue: toText(targetWeapon['鍚嶇О'], '鏃?),
                    })} / ${makeInlineEditableValue(toText(targetWeapon.鍝侀樁 || targetWeapon['鍝侀樁'], '鏃犲搧闃?), {
                      path: [...targetCharPath, '瑁呭', '姝﹀櫒', '鍝侀樁'],
                      kind: 'string',
                      rawValue: toText(targetWeapon.鍝侀樁 || targetWeapon['鍝侀樁'], '鏃犲搧闃?),
                    })}`
                  : (targetChar ? (targetWeapon && targetWeapon['鍚嶇О'] ? `${toText(targetWeapon['鍚嶇О'], '鏃?)} / ${toText(targetWeapon.鍝侀樁 || targetWeapon['鍝侀樁'], '鏃犲搧闃?)}` : '鏃?) : '鏈敹褰?) }
              ], 'two')}</div>
            </div>
          `
        };
      }

      if (previewKey === '鎷嶅崠涓庤鎶?) {
        const auctionItems = safeEntries(deepGet(snapshot, 'rootData.world.鎷嶅崠.鎷嶅搧', {})).slice(0, 6);
        const auctionPath = ['world', '鎷嶅崠'];
        return {
          title: '鎷嶅崠琛?/ 涓栫晫璀︽姤寮圭獥',
          summary: '鎷嶅崠鐘舵€併€佹媿鍝佷笌褰撳墠涓栫晫璀︽姤銆?,
          body: `
            <div class="archive-modal-grid">
              <div class="archive-card">
                <div class="archive-card-head"><div class="archive-card-title">鎷嶅崠鐘舵€?/div></div>
                ${makeTileGrid([
                  { label: '鐘舵€?, value: makeInlineEditableValue(toText(deepGet(snapshot, 'rootData.world.鎷嶅崠.鐘舵€?, '浼戝競'), '浼戝競'), {
                      path: [...auctionPath, '鐘舵€?],
                      kind: 'string',
                      rawValue: toText(deepGet(snapshot, 'rootData.world.鎷嶅崠.鐘舵€?, '浼戝競'), '浼戝競'),
                    }) },
                  { label: '鍦扮偣', value: makeInlineEditableValue(toText(deepGet(snapshot, 'rootData.world.鎷嶅崠.鍦扮偣', '鏃?), '鏃?), {
                      path: [...auctionPath, '鍦扮偣'],
                      kind: 'string',
                      rawValue: toText(deepGet(snapshot, 'rootData.world.鎷嶅崠.鍦扮偣', '鏃?), '鏃?),
                    }) },
                  { label: '涓嬫鍒锋柊', value: makeInlineEditableValue(String(toNumber(deepGet(snapshot, 'rootData.world.鎷嶅崠.涓嬫鍒锋柊tick', 0), 0)), {
                      path: [...auctionPath, '涓嬫鍒锋柊tick'],
                      kind: 'number',
                      rawValue: toNumber(deepGet(snapshot, 'rootData.world.鎷嶅崠.涓嬫鍒锋柊tick', 0), 0),
                      editorMeta: { min: 0, integer: true, hint: '鏈€灏?0 路 鏁存暟' },
                    }) },
                  { label: '褰撳墠鎷嶅搧', value: `${auctionItems.length} 浠禶 }
                ], 'two')}
              </div>
              <div class="archive-card full">
                <div class="archive-card-head"><div class="archive-card-title">鎷嶅搧鍒楄〃</div></div>
                ${makeTimelineStack(auctionItems.map(([name, item]) => {
                  const itemPath = [...auctionPath, '鎷嶅搧', name];
                  return {
                    title: name,
                    desc: [
                      `鍝侀樁 ${makeInlineEditableValue(toText(item && item.tier, '浣庨樁'), {
                        path: [...itemPath, 'tier'],
                        kind: 'string',
                        rawValue: toText(item && item.tier, '浣庨樁'),
                      })}`,
                      `浠锋牸 ${makeInlineEditableValue(String(toNumber(item && item.浠锋牸, 0)), {
                        path: [...itemPath, '浠锋牸'],
                        kind: 'number',
                        rawValue: toNumber(item && item.浠锋牸, 0),
                        editorMeta: { min: 0, integer: true, hint: '鏈€灏?0 路 鏁存暟' },
                      })}`,
                      `璇存槑 ${makeInlineEditableValue(toText(item && item.lore, '鏆傛棤璇存槑'), {
                        path: [...itemPath, 'lore'],
                        kind: 'string',
                        rawValue: toText(item && item.lore, '鏆傛棤璇存槑'),
                        multiline: true,
                      })}`,
                    ].join(' / ')
                  };
                }))}
              </div>
            </div>
          `
        };
      }

      if (previewKey === '鍔垮姏鐭╅樀鎬昏') {
        return {
          title: '鍔垮姏鐭╅樀寮圭獥',
          summary: '鐐瑰嚮鍔垮姏鍚嶇О锛屾煡鐪嬭鍔垮姏鐨勮缁嗙粨鏋勪笌鍦ㄥ満浜虹墿妗ｆ銆?,
          body: `
            <div class="archive-modal-grid">
              <div class="archive-card full">
                <div class="archive-card-head"><div class="archive-card-title">鍔垮姏姊樀</div></div>
                <div style="display: grid; grid-template-columns: repeat(auto-fill, minmax(280px, 1fr)); gap: 12px; margin-top: 10px;">
                  ${snapshot.orgEntries.map(([name, item]) => `
                    <button type="button" class="role-switch-tile clickable" data-preview="org_detail_${escapeHtmlAttr(name)}" style="text-align:left; border:1px solid rgba(255,255,255,0.08); background:rgba(0,0,0,0.2); padding:12px; border-radius:8px; cursor:pointer;">
                      <div class="role-switch-head" style="margin-bottom:6px;font-size:15px;"><b>${htmlEscape(name)}</b><span class="state-tag">鐐瑰嚮璇︽儏</span></div>
                      <div class="role-switch-meta" style="font-size:12px;color:var(--text-muted);">
                        瑙勬ā锛?{formatNumber(item && item.瑙勬ā || 0)} 锝?鐘舵€侊細${toText(item && item.鐘舵€? '姝ｅ父')} <br>
                        椤跺皷鎴樺姏锛?{toText(deepGet(item, '鎴樺姏缁熻.灏佸彿鏂楃綏', 0), '0')} 浣嶅皝鍙锋枟缃?                      </div>
                    </button>
                  `).join('')}
                </div>
              </div>
            </div>
          `
        };
      }

      if (String(previewKey || '').startsWith('org_detail_')) {
        const targetOrgName = String(previewKey).replace('org_detail_', '').trim();
        const targetOrgEntry = snapshot.orgEntries.find(([name]) => name === targetOrgName) || [targetOrgName, {}];
        const targetOrgData = targetOrgEntry[1] || {};
        const targetOrgPath = targetOrgName ? ['org', targetOrgName] : [];

        const factionRelationCards = buildFactionRelationEditorGrid(targetOrgName, targetOrgData, {
          max: 8,
          emptyTitle: '鏆傛棤瀵瑰鍏崇郴',
          emptyDesc: '褰撳墠鍔垮姏鏈褰曞澶栧叧绯汇€?
        });

        // Automatically gather characters belonging to this organization
        const orgMembers = [];
        for (const [charName, charInfo] of snapshot.charEntries) {
           const factions = safeEntries(deepGet(charInfo, '绀句氦.鍔垮姏', {}));
           const matchingFaction = factions.find(([fName]) => fName === targetOrgName);
           if (matchingFaction) {
               orgMembers.push({
                 name: charName,
                 desc: `韬唤锛?{toText(matchingFaction[1] && matchingFaction[1]['韬唤'], '鎴愬憳')} 锝?鏉冮檺锛歀v.${toText(matchingFaction[1] && matchingFaction[1]['鏉冮檺绾?], '1')}`
               });
           }
        }

        return {
          title: `${targetOrgName} / 鍔垮姏妗ｆ`,
          summary: '灞曠ず璇ュ娍鍔涚殑瑙勬ā鎴樺姏銆佸澶栧叧绯讳互鍙婂凡鐭ユ垚鍛樺悕鍐屻€?,
          body: `
            <div class="archive-modal-grid" style="grid-template-columns: 1fr;">
              <div class="archive-card full">
                <div class="archive-card-head"><div class="archive-card-title">鍔垮姏姒傚喌</div></div>
                ${makeTileGrid([
                  { label: '褰卞搷鍔?, value: targetOrgPath.length
                    ? makeInlineEditableValue(formatNumber(targetOrgData.褰卞搷鍔?|| 0), {
                        path: [...targetOrgPath, '褰卞搷鍔?],
                        kind: 'number',
                        rawValue: toNumber(targetOrgData.褰卞搷鍔? 0),
                        editorMeta: { min: 0, integer: true, hint: '鏈€灏?0 路 鏁存暟' },
                      })
                    : formatNumber(targetOrgData.褰卞搷鍔?|| 0) },
                  { label: '鍔垮姏瑙勬ā', value: targetOrgPath.length
                    ? makeInlineEditableValue(formatNumber(targetOrgData.瑙勬ā || 0), {
                        path: [...targetOrgPath, '瑙勬ā'],
                        kind: 'number',
                        rawValue: toNumber(targetOrgData.瑙勬ā, 0),
                        editorMeta: { min: 0, integer: true, hint: '鏈€灏?0 路 鏁存暟' },
                      })
                    : formatNumber(targetOrgData.瑙勬ā || 0) },
                  { label: '褰撳墠鐘舵€?, value: targetOrgPath.length
                    ? makeInlineEditableValue(toText(targetOrgData.鐘舵€? '姝ｅ父'), {
                        path: [...targetOrgPath, '鐘舵€?],
                        kind: 'string',
                        rawValue: toText(targetOrgData.鐘舵€? '姝ｅ父'),
                      })
                    : toText(targetOrgData.鐘舵€? '姝ｅ父') },
                  { label: '涓婄骇鍔垮姏', value: targetOrgPath.length
                    ? makeInlineEditableValue(toText(targetOrgData.涓婄骇鍔垮姏, '鏃?), {
                        path: [...targetOrgPath, '涓婄骇鍔垮姏'],
                        kind: 'string',
                        rawValue: toText(targetOrgData.涓婄骇鍔垮姏, '鏃?),
                      })
                    : toText(targetOrgData.涓婄骇鍔垮姏, '鏃?) },
                  { label: '鏋侀檺鏂楃綏', value: targetOrgPath.length
                    ? makeInlineEditableValue(String(toNumber(deepGet(targetOrgData, '鎴樺姏缁熻.鏋侀檺鏂楃綏', 0), 0)), {
                        path: [...targetOrgPath, '鎴樺姏缁熻', '鏋侀檺鏂楃綏'],
                        kind: 'number',
                        rawValue: toNumber(deepGet(targetOrgData, '鎴樺姏缁熻.鏋侀檺鏂楃綏', 0), 0),
                        editorMeta: { min: 0, integer: true, hint: '鏈€灏?0 路 鏁存暟' },
                      })
                    : deepGet(targetOrgData, '鎴樺姏缁熻.鏋侀檺鏂楃綏', 0) },
                  { label: '瓒呯骇鏂楃綏', value: targetOrgPath.length
                    ? makeInlineEditableValue(String(toNumber(deepGet(targetOrgData, '鎴樺姏缁熻.瓒呯骇鏂楃綏', 0), 0)), {
                        path: [...targetOrgPath, '鎴樺姏缁熻', '瓒呯骇鏂楃綏'],
                        kind: 'number',
                        rawValue: toNumber(deepGet(targetOrgData, '鎴樺姏缁熻.瓒呯骇鏂楃綏', 0), 0),
                        editorMeta: { min: 0, integer: true, hint: '鏈€灏?0 路 鏁存暟' },
                      })
                    : deepGet(targetOrgData, '鎴樺姏缁熻.瓒呯骇鏂楃綏', 0) },
                  { label: '灏佸彿鏂楃綏', value: targetOrgPath.length
                    ? makeInlineEditableValue(String(toNumber(deepGet(targetOrgData, '鎴樺姏缁熻.灏佸彿鏂楃綏', 0), 0)), {
                        path: [...targetOrgPath, '鎴樺姏缁熻', '灏佸彿鏂楃綏'],
                        kind: 'number',
                        rawValue: toNumber(deepGet(targetOrgData, '鎴樺姏缁熻.灏佸彿鏂楃綏', 0), 0),
                        editorMeta: { min: 0, integer: true, hint: '鏈€灏?0 路 鏁存暟' },
                      })
                    : deepGet(targetOrgData, '鎴樺姏缁熻.灏佸彿鏂楃綏', 0) }
                ], 'two')}
              </div>
              <div class="archive-card full">
                <div class="archive-card-head"><div class="archive-card-title">瀵瑰鍏崇郴</div></div>
                ${factionRelationCards}
              </div>
              <div class="archive-card full">
                <div class="archive-card-head"><div class="archive-card-title">瑙掕壊鍚嶅唽</div></div>
                <div style="display: grid; grid-template-columns: repeat(auto-fill, minmax(260px, 1fr)); gap: 12px; margin-top: 10px;">
                  ${orgMembers.length > 0 ? orgMembers.map(m => `
                    <button type="button" class="role-switch-tile clickable" data-preview="瑙掕壊妗ｆ锛?{escapeHtmlAttr(m.name)}" style="text-align:left; border:1px solid rgba(255,255,255,0.08); background:rgba(0,0,0,0.2); padding:12px; border-radius:8px; cursor:pointer;">
                      <div class="role-switch-head" style="margin-bottom:6px;font-size:15px;"><b>${htmlEscape(m.name)}</b><span class="state-tag">瑙掕壊</span></div>
                      <div class="role-switch-meta" style="font-size:12px;color:var(--text-muted);">
                        ${htmlEscape(m.desc)}
                      </div>
                    </button>
                  `).join('') : '<div style="padding:12px; color:var(--text-muted);">鏈悳绱㈠埌灞炰簬璇ュ娍鍔涚殑宸茬煡瑙掕壊銆?/div>'}
                </div>
              </div>
            </div>
          `
        };
      }

      if (previewKey === '鏈湴鎹偣璇︽儏' || previewKey === '褰撳墠鑺傜偣璇︽儏' || previewKey.startsWith('鍦板浘鑺傜偣锛?)) {
        const nodeName = previewKey.startsWith('鍦板浘鑺傜偣锛?) ? previewKey.replace('鍦板浘鑺傜偣锛?, '') : snapshot.currentLoc;
        const mapNode = resolveDisplayMapNode(snapshot, nodeName);
        const nodeInfo = resolveLocationData(snapshot.rootData, nodeName);
        const locationBasePath = nodeInfo && nodeInfo.data ? ['world', '鍦扮偣', nodeInfo.name] : [];
        const nodeStores = safeEntries(nodeInfo.data && nodeInfo.data.鍟嗗簵);
        const activeCharKey = resolveSnapshotCharKey(snapshot, toText(snapshot.activeName, ''));
        const activeDisplayName = toText(deepGet(snapshot, 'activeChar.name', deepGet(snapshot, 'activeChar.base.name', snapshot.activeName)), snapshot.activeName);
        const localNpcEntries = safeEntries(deepGet(snapshot, 'rootData.char', {}))
          .filter(([name, char]) => {
            const displayName = toText(deepGet(char, 'name', deepGet(char, 'base.name', name)), name);
            if ((activeCharKey && name === activeCharKey) || (activeDisplayName && displayName === activeDisplayName)) return false;
            const npcLoc = toText(deepGet(char, '鐘舵€?浣嶇疆', ''), '');
            return npcLoc ? isLocationCompatible(nodeName, npcLoc) : false;
          })
          .slice(0, 4);
        const primaryNpc = localNpcEntries[0]
          ? toText(deepGet(localNpcEntries[0][1], 'name', deepGet(localNpcEntries[0][1], 'base.name', localNpcEntries[0][0])), localNpcEntries[0][0])
          : '';
        const 涓诲璞″鎵樿兘鍔?= primaryNpc
          ? 鑾峰彇瑙掕壊宸ュ潑濮旀墭鑳藉姏鎽樿(resolveSnapshotCharacter(snapshot, primaryNpc).char)
          : [];
        const officialCommissionLocationName = ['閿婚€犲笀鍗忎細', '鍒堕€犲笀鍗忎細', '璁捐甯堝崗浼?, '淇悊甯堝崗浼?]
          .find(locationName => isLocationCompatible(nodeName, locationName) || isLocationCompatible(locationName, nodeName)) || '';
        const currentNodeLocFull = toText(deepGet(snapshot, 'activeChar.鐘舵€?浣嶇疆', snapshot.currentLoc), snapshot.currentLoc);
        const canDispatchHere = !!toText(currentNodeLocFull, '') && isLocationCompatible(nodeName, currentNodeLocFull);
        const isPlayerControlled = isSnapshotPlayerControlled(snapshot);
        const isTransmissionTowerNode = isTransmissionTowerEntryLocation(nodeName);
        const isSoulTowerHeadquartersNode = isSoulTowerHeadquartersLocation(nodeName);
        const ascensionTickets = listAscensionTicketNames(snapshot);
        const soulTowerTickets = listSoulTowerTicketNames(snapshot);
        const soulTowerEligible = isSoulTowerEligibleCharacterData(getActiveSnapshotCharacter(snapshot));
        const preferredLocalStoreName = isTransmissionTowerNode
          ? toText((nodeStores.find(([name]) => /浼犵伒濉?.test(toText(name, ''))) || nodeStores[0] || [])[0], '')
          : toText((nodeStores[0] || [])[0], '');

        const availableActionButtons = [
          ...(isTransmissionTowerNode ? [{
            text: '杩涘叆鍗囩伒鍙?,
            action: 'ascension',
            className: 'live',
            disabled: ascensionTickets.length <= 0,
            desc: ascensionTickets.length > 0 ? `鍙敤闂ㄧエ ${ascensionTickets.length} 绉峘 : '缂哄皯鍗囩伒鍙伴棬绁?,
          }] : []),
          ...(isSoulTowerHeadquartersNode ? [{
            text: '杩涘叆榄傜伒濉?,
            action: 'tower',
            className: 'live',
            disabled: !soulTowerEligible || soulTowerTickets.length <= 0,
            desc: !soulTowerEligible
              ? '褰撳墠瑙掕壊涓嶆弧瓒冲叆濉旀潯浠?
              : soulTowerTickets.length > 0
                ? '榛樿鎸戞垬涓嬩竴灞?
                : '缂哄皯榄傜伒濉旈棬绁?,
          }] : []),
          ...(preferredLocalStoreName ? [{ text: '鍟嗗簵浜ゆ槗', action: 'shop', target: preferredLocalStoreName, className: 'live', desc: `杩涘叆 ${preferredLocalStoreName}` }] : []),
          ...(officialCommissionLocationName ? [{ text: '瀹樻柟宸ュ潑濮旀墭', action: 'commission', target: officialCommissionLocationName, executorType: 'official', className: 'live', desc: 鑾峰彇瀹樻柟宸ュ潑濮旀墭鑳藉姏鎽樿(officialCommissionLocationName).join(' / ') || officialCommissionLocationName }] : []),
          ...(primaryNpc ? [
            { text: `涓?{primaryNpc}浜ゆ槗`, action: 'trade', npcTarget: primaryNpc, className: 'warn', desc: '瑙掕壊浜ゆ槗' },
            ...(涓诲璞″鎵樿兘鍔?length ? [{ text: `濮旀墭${primaryNpc}宸ュ潑`, action: 'commission', npcTarget: primaryNpc, executorType: 'private', className: 'warn', desc: 涓诲璞″鎵樿兘鍔?join(' / ') }] : []),
            { text: `涓?{primaryNpc}瀵硅瘽`, action: 'talk', npcTarget: primaryNpc, className: 'live' },
            { text: `鍚?{primaryNpc}璇锋暀`, action: 'intel', npcTarget: primaryNpc, className: '' },
            { text: `鍚?{primaryNpc}鍒囩`, action: 'battle', npcTarget: primaryNpc, className: 'warn' }
          ] : [])
        ];
        const actionButtons = canDispatchHere ? availableActionButtons : [];
        const actionSummaryText = availableActionButtons.map(btn => btn.text).join(' / ');
        const actionCardTitle = isPlayerControlled ? '鍙墽琛屾搷浣? : '椹诲湴鎿嶄綔姒傝';
        const travelPlan = !canDispatchHere && isPlayerControlled && window.__sheepMapBridge && typeof window.__sheepMapBridge.describeTravelToNode === 'function'
          ? window.__sheepMapBridge.describeTravelToNode(nodeName)
          : null;
        const arrivalActionItems = availableActionButtons.map(btn => {
          const metaParts = [];
          if (btn.npcTarget) metaParts.push(`瀵硅薄 ${btn.npcTarget}`);
          if (btn.target && btn.target !== nodeName && !btn.npcTarget) metaParts.push(`鐩爣 ${btn.target}`);
          if (btn.executorType === 'official') metaParts.push('瀹樻柟濮旀墭');
          if (btn.executorType === 'private') metaParts.push('绉佷汉濮旀墭');
          return {
            title: btn.text,
            desc: toText(btn.desc, '') || (metaParts.length ? metaParts.join(' / ') : '鎶佃揪鍚庡彲鐩存帴鍙戣捣')
          };
        });
        const actionButtonsHtml = isPlayerControlled
          ? (canDispatchHere
            ? (actionButtons.length
              ? `<div class="map-action-grid">${actionButtons.map(btn => `<button type="button" class="map-dispatch-action-btn ${htmlEscape(btn.className || '')}" data-action="${htmlEscape(btn.action || '')}" data-target="${htmlEscape(btn.target || nodeName)}" data-current-loc="${htmlEscape(nodeName)}" data-npc-target="${htmlEscape(btn.npcTarget || '')}" data-executor-type="${htmlEscape(btn.executorType || '')}" data-services="${htmlEscape(Array.isArray(btn.services) ? btn.services.join('|') : '')}" ${btn.disabled ? 'disabled' : ''}>${htmlEscape(btn.text || '鎵ц鎿嶄綔')}</button>`).join('')}</div>`
              : `<div class="relation-card"><b>鏆傛棤鍙墽琛屾搷浣?/b><span>杩欓噷鏆傛椂娌℃湁鑳界珛鍒诲彂璧风殑浜ゆ槗鎴栦簰鍔ㄣ€?/span></div>`)
            : `
              <section class="dossier-section">
                <div class="dossier-section-title">绉诲姩鍒版澶?/div>
                ${travelPlan && travelPlan.ok
                  ? `${makeDossierRows([
                      { label: '绉诲姩鏂瑰紡', value: htmlEscape(toText(travelPlan.method, '鏈畾')) },
                      { label: '棰勮鑰楁椂', value: htmlEscape(toText(travelPlan.duration, '鏈畾')) },
                      { label: '璧勬簮娑堣€?, value: htmlEscape(toText(travelPlan.costText, '鏃?)) },
                      { label: '鍧愭爣', value: htmlEscape(toText(travelPlan.coordText, '鏈畾')) },
                      ...(travelPlan.routePlan ? [{ label: '璺緞璇存槑', value: htmlEscape(toText(travelPlan.routePlan, '')), className: 'dossier-row--wide' }] : []),
                      ...(!travelPlan.canAfford && travelPlan.reason ? [{ label: '褰撳墠闄愬埗', value: htmlEscape(toText(travelPlan.reason, '璧勬簮涓嶈冻')), className: 'dossier-row--wide' }] : [])
                    ], 'dossier-row-grid--two')}
                    <div class="tag-cloud armory-quick-actions" style="margin-top:12px;justify-content:flex-start;">
                      <button type="button" class="relation-action-btn action-primary" data-map-travel-node="${escapeHtmlAttr(nodeName)}" ${travelPlan.canAfford ? '' : 'disabled'}>${htmlEscape(travelPlan.canAfford ? `绉诲姩鍒?${nodeName}` : '璧勬簮涓嶈冻锛屾殏涓嶈兘绉诲姩')}</button>
                    </div>`
                  : `<div class="dossier-empty-note">${htmlEscape(toText(travelPlan && travelPlan.reason, '褰撳墠鏃犳硶瑙勫垝鍓嶅線璇ヨ妭鐐圭殑绉诲姩銆?))}</div>`}
              </section>
              <section class="dossier-section">
                <div class="dossier-section-title">鍒拌揪鍚庡彲鎵ц</div>
                ${arrivalActionItems.length
                  ? makeDossierList(arrivalActionItems, 'dossier-list--compact')
                  : '<div class="dossier-empty-note">鎶佃揪鍚庢殏鏈瘑鍒埌绔嬪嵆鍙墽琛岀殑浜ゆ槗鎴栫ぞ浜ゅ叆鍙ｃ€?/div>'}
              </section>
            `)
          : `<div class="relation-card"><b>鏃佽瑙嗚</b><span>${htmlEscape(actionSummaryText ? `鍙鍏ュ彛锛?{actionSummaryText}` : '鏃犳湰鍦版搷浣?)}</span></div>`;
        const localNpcCardsHtml = localNpcEntries.length
          ? `<div class="location-npc-list">${localNpcEntries.map(([name, char]) => {
              const displayName = toText(deepGet(char, 'name', deepGet(char, 'base.name', name)), name);
              const identityRaw = toText(deepGet(char, '绀句氦.涓昏韩浠?, '鏈煡韬唤'), '鏈煡韬唤');
              const identity = identityRaw && identityRaw !== displayName ? identityRaw : '鏈爣娉?;
              const npcLoc = toText(deepGet(char, '鐘舵€?浣嶇疆', '鏈煡鍦扮偣'), '鏈煡鍦扮偣');
              const npcAction = toText(deepGet(char, '鐘舵€?琛屽姩', '鏃ュ父'), '鏃ュ父');
              const 瀵硅薄濮旀墭鑳藉姏 = 鑾峰彇瑙掕壊宸ュ潑濮旀墭鑳藉姏鎽樿(char);
              const npcButtons = isPlayerControlled && canDispatchHere
                ? `<div class="location-npc-actions">
                    <button type="button" class="map-dispatch-action-btn live" data-action="trade" data-target="${escapeHtmlAttr(nodeName)}" data-current-loc="${escapeHtmlAttr(nodeName)}" data-npc-target="${escapeHtmlAttr(displayName)}">浜ゆ槗</button>
                    ${瀵硅薄濮旀墭鑳藉姏.length ? `<button type="button" class="map-dispatch-action-btn warn" data-action="commission" data-target="${escapeHtmlAttr(nodeName)}" data-current-loc="${escapeHtmlAttr(nodeName)}" data-npc-target="${escapeHtmlAttr(displayName)}" data-executor-type="private">濮旀墭</button>` : ''}
                    <button type="button" class="map-dispatch-action-btn live" data-action="talk" data-target="${escapeHtmlAttr(nodeName)}" data-current-loc="${escapeHtmlAttr(nodeName)}" data-npc-target="${escapeHtmlAttr(displayName)}">瀵硅瘽</button>
                    <button type="button" class="map-dispatch-action-btn" data-action="intel" data-target="${escapeHtmlAttr(nodeName)}" data-current-loc="${escapeHtmlAttr(nodeName)}" data-npc-target="${escapeHtmlAttr(displayName)}">璇锋暀</button>
                    <button type="button" class="map-dispatch-action-btn warn" data-action="battle" data-target="${escapeHtmlAttr(nodeName)}" data-current-loc="${escapeHtmlAttr(nodeName)}" data-npc-target="${escapeHtmlAttr(displayName)}">鍒囩</button>
                  </div>`
                : '';
              return `
                <article class="location-npc-card">
                  <div class="location-npc-card-head">
                    <b>${htmlEscape(shortenText(displayName, 16))}</b>
                    <span>${htmlEscape(shortenText(npcAction, 10))}</span>
                  </div>
                  <div class="location-npc-meta">
                    <span>${htmlEscape(`韬唤 ${shortenText(identity, 20)}`)}</span>
                    <span>${htmlEscape(`浣嶇疆 ${shortenText(npcLoc.replace(/^鏂楃綏澶ч檰-/, '').replace(/^鏂楃伒澶ч檰-/, ''), 24)}`)}</span>
                  </div>
                  ${npcButtons}
                </article>
              `;
            }).join('')}</div>`
          : '<div class="location-empty-note">褰撳墠鑺傜偣鏈壂鎻忓埌鍙洿鎺ヤ簰鍔ㄧ殑鏈湴瑙掕壊銆?/div>';
        const travelTags = (snapshot.mapTravelCandidates.length ? snapshot.mapTravelCandidates : snapshot.dynamicLocationNames).filter(name => name !== nodeName).slice(0, 6);
        return {
          title: `鏈湴鎹偣 / ${nodeName}`,
          summary: '褰撳墠鑺傜偣鐨勫湴鍥惧睘鎬с€佸娍鍔涜祫鏂欎笌鍙幓鏂瑰悜銆?,
          body: `
            <div class="archive-modal-grid location-detail-grid">
                <div class="archive-card">
                  <div class="archive-card-head"><div class="archive-card-title">鎹偣姒傝</div></div>
                  ${makeTileGrid([
                    { label: '鎵€鍦ㄥ湴鐐?, value: nodeName },
                    { label: '鎺屾帶鍔垮姏', value: locationBasePath.length
                      ? makeInlineEditableValue(toText(nodeInfo.data && nodeInfo.data['鎺屾帶鍔垮姏'], '鏈煡'), {
                          path: [...locationBasePath, '鎺屾帶鍔垮姏'],
                          kind: 'string',
                          rawValue: toText(nodeInfo.data && nodeInfo.data['鎺屾帶鍔垮姏'], '鏈煡'),
                        })
                      : htmlEscape(toText(nodeInfo.data && nodeInfo.data['鎺屾帶鍔垮姏'], '鏈煡')) },
                    { label: '甯镐綇浜哄彛', value: locationBasePath.length
                      ? makeInlineEditableValue(formatNumber(nodeInfo.data && nodeInfo.data['浜哄彛']), {
                          path: [...locationBasePath, '浜哄彛'],
                          kind: 'number',
                          rawValue: toNumber(nodeInfo.data && nodeInfo.data['浜哄彛'], 0),
                          editorMeta: { min: 0, integer: true, hint: '鏈€灏?0 路 鏁存暟' },
                        })
                      : htmlEscape(formatNumber(nodeInfo.data && nodeInfo.data['浜哄彛'])) },
                    { label: '缁忔祹鐘跺喌', value: locationBasePath.length
                      ? makeInlineEditableValue(toText(nodeInfo.data && nodeInfo.data['缁忔祹鐘跺喌'], '鏈煡'), {
                          path: [...locationBasePath, '缁忔祹鐘跺喌'],
                          kind: 'enum_select',
                          rawValue: toText(nodeInfo.data && nodeInfo.data['缁忔祹鐘跺喌'], '鏈煡'),
                          editorMeta: { options: ['绻佽崳', '鏅€?, '钀ф潯', '鏈煡'] },
                        })
                      : htmlEscape(toText(nodeInfo.data && nodeInfo.data['缁忔祹鐘跺喌'], '鏈煡')) },
                    { label: '瀹堟姢鍐涘洟', value: locationBasePath.length
                      ? makeInlineEditableValue(toText(nodeInfo.data && nodeInfo.data['瀹堟姢鍐涘洟'], '鏃?), {
                          path: [...locationBasePath, '瀹堟姢鍐涘洟'],
                          kind: 'string',
                          rawValue: toText(nodeInfo.data && nodeInfo.data['瀹堟姢鍐涘洟'], '鏃?),
                        })
                      : htmlEscape(toText(nodeInfo.data && nodeInfo.data['瀹堟姢鍐涘洟'], '鏃?)) },
                    { label: '鍟嗗簵鏁伴噺', value: `${nodeStores.length} 澶刞 },
                  ], 'two')}
                </div>
                <div class="archive-card">
                  <div class="archive-card-head"><div class="archive-card-title">椹诲湴鐘舵€?/div></div>
                  <div class="location-status-layout">
                    ${(() => {
                      const getScore = (text) => {
                        if (!text || text === '鏈煡') return 50;
                        if (/绻佸崕|鏍稿績|鏋佷匠|鍏呰/.test(text)) return 95;
                        if (/鑹ソ|绋冲畾|鍙戝睍/.test(text)) return 75;
                        if (/涓€鑸瑋鏅€殀甯告€?.test(text)) return 60;
                        if (/钀ф潯|娣蜂贡|鍖箯|鍗遍櫓/.test(text)) return 30;
                        if (/搴熷純|姣佺伃|鑽掕姕|鏋佸嵄/.test(text)) return 10;
                        return 50;
                      };
                      const ecoText = toText(nodeInfo.data && nodeInfo.data['缁忔祹鐘跺喌'], '鏈煡');
                      const ecoScore = getScore(ecoText);
                      const popScore = Math.min(100, Math.max(10, Math.round(Math.log10(Math.max(1, toNumber(nodeInfo.data && nodeInfo.data['浜哄彛'], 1000))) * 15)));
                      const guardText = toText(nodeInfo.data && nodeInfo.data['瀹堟姢鍐涘洟'], '鏃?);
                      const guardScore = guardText !== '鏃? ? (guardText.includes('绮鹃攼') || guardText.includes('涓诲姏') ? 95 : 70) : 20;
                      const tradeScore = nodeStores.length ? Math.min(100, 30 + nodeStores.length * 20) : 10;
                      return `
                        <div class="location-radar-panel">
                          ${makeRadarSvg(['缁忔祹鐘跺喌', '甯镐綇浜哄彛', '瀹堝鍔涢噺', '璐告槗琛ョ粰', '鎴樼暐浠峰€?], [ecoScore, popScore, guardScore, tradeScore, Math.round((ecoScore+popScore+guardScore+tradeScore)/4)])}
                        </div>
                      `;
                    })()}
                    <div class="location-status-summary">
                      <div class="location-summary-card location-summary-card--highlight"><b>缁煎悎璇勭骇</b><span>${htmlEscape(toText(nodeInfo.data && nodeInfo.data['缁忔祹鐘跺喌'], '鏈煡'))}</span></div>
                      <div class="location-summary-card"><b>瀹堝璇勪及</b><span>${htmlEscape(toText(nodeInfo.data && nodeInfo.data['瀹堟姢鍐涘洟'], '鏃犲啗鍥㈤┗鎵?))}</span></div>
                      <div class="location-summary-card"><b>琛ョ粰鑺傜偣</b><span>${htmlEscape(nodeStores.length ? `${nodeStores.length} 澶勮锤鏄撶珯鎴栬鏂絗 : '鏃犲彲瑙佸晢搴?)}</span></div>
                      <div class="location-summary-card"><b>鏈湴渚涚粰</b><span>${htmlEscape(toText(snapshot && snapshot.鏈湴渚涚粰, '鏃犱緵缁?))}</span></div>
                      <div class="location-summary-card"><b>浠锋牸甯?/b><span>${htmlEscape(toText(snapshot && snapshot.浠锋牸甯? '鏃?))}</span></div>
                      <div class="location-summary-card"><b>鏈€杩戞垚浜?/b><span>${htmlEscape(toText(snapshot && snapshot.鏈€杩戞垚浜ゅ奖鍝? '骞崇ǔ'))}</span></div>
                      <div class="location-summary-card location-summary-card--note"><b>鍦板浘绠€鎶?/b><span>${locationBasePath.length
                        ? makeInlineEditableValue(toText(deepGet(nodeInfo.data, 'desc', mapNode ? mapNode.desc : '鏃犳壂鎻忔暟鎹?), mapNode ? mapNode.desc : '鏃犳壂鎻忔暟鎹?), {
                            path: [...locationBasePath, 'desc'],
                            kind: 'string',
                            rawValue: toText(deepGet(nodeInfo.data, 'desc', mapNode ? mapNode.desc : '鏃犳壂鎻忔暟鎹?), mapNode ? mapNode.desc : '鏃犳壂鎻忔暟鎹?),
                            multiline: true,
                          })
                        : htmlEscape(mapNode ? mapNode.desc : '鏃犳壂鎻忔暟鎹?)}</span></div>
                    </div>
                  </div>
                </div>
              ${localNpcEntries.length > 0 ? `
                <div class="archive-card">
                  <div class="archive-card-head"><div class="archive-card-title">${htmlEscape(actionCardTitle)}</div></div>
                  ${actionButtonsHtml}
                </div>
                <div class="archive-card">
                  <div class="archive-card-head"><div class="archive-card-title">鏈湴鍙帴瑙︿汉鐗?/div></div>
                  ${localNpcCardsHtml}
                </div>
              ` : `
                <div class="archive-card full">
                  <div class="archive-card-head"><div class="archive-card-title">${htmlEscape(actionCardTitle)}</div></div>
                  ${actionButtonsHtml}
                  <div class="location-empty-note">褰撳墠鑺傜偣鏈壂鎻忓埌鍙洿鎺ヤ簰鍔ㄧ殑鏈湴瑙掕壊銆?/div>
                </div>
              `}
            </div>
          `,
          onMount: () => null
        };
      }

      if (previewKey === '鍥惧眰鎺у埗涓庤窇鍥?) {
        const statusLoc = toText(deepGet(snapshot, 'activeChar.鐘舵€?浣嶇疆', snapshot.currentLoc), snapshot.currentLoc);
        const statusCoordSystem = toText(deepGet(snapshot, 'activeChar.鐘舵€?鍧愭爣绯?, 'image'), 'image');
        return {
          title: '鍥惧眰鎺у埗 / 璺戝浘寮圭獥',
          summary: '褰撳墠浣嶇疆銆佸彲瑙佸眰绾т笌璺戝浘浠茶鐘舵€併€?,
          body: `
            <div class="archive-modal-grid" style="grid-template-columns: 1fr;">
              <div class="archive-card full">
                <div class="archive-card-head"><div class="archive-card-title">褰撳墠浣嶇疆涓庤鍥?/div></div>
                ${makeTileGrid([
                  { label: '褰撳墠閿氱偣', value: snapshot.currentLoc },
                  { label: '涓昏鍥?, value: snapshot.normalizedLoc },
                  { label: '瑙掕壊瀹氫綅', value: statusLoc },
                  { label: '鍧愭爣绯?, value: statusCoordSystem },
                  { label: '绉诲姩缁撶畻', value: '鍦板浘浠茶鍣ㄧ洿缁? },
                  { label: '鍔ㄦ€佽妭鐐?, value: `${snapshot.dynamicLocationNames.length} 涓猔 }
                ], 'two')}
              </div>
            </div>
          `
        };
      }

      if (previewKey === '鍔ㄦ€佸湴鐐逛笌鎵╁睍鑺傜偣') {
        const dynamicLocationEntries = snapshot.dynamicLocationNames.map(name => [name, deepGet(snapshot, ['rootData', 'world', '鍔ㄦ€佸湴鐐?, name], {})]);
        return {
          title: '鍔ㄦ€佸湴鐐瑰脊绐?,
          summary: '鍓ф儏鎺ㄨ繘鐢熸垚鐨勬墿灞曡妭鐐广€?,
          body: `
            <div class="archive-modal-grid" style="grid-template-columns: 1fr;">
              <div class="archive-card full">
                <div class="archive-card-head"><div class="archive-card-title">鍔ㄦ€佽妭鐐瑰垪琛?/div></div>
                ${makeTimelineStack(dynamicLocationEntries.length ? dynamicLocationEntries.map(([name, item]) => {
                  const basePath = ['world', '鍔ㄦ€佸湴鐐?, name];
                  return {
                    title: name,
                    desc: [
                      `褰掑睘 ${makeInlineEditableValue(toText(item && item['褰掑睘鐖惰妭鐐?], '鏈煡'), {
                        path: [...basePath, '褰掑睘鐖惰妭鐐?],
                        kind: 'string',
                        rawValue: toText(item && item['褰掑睘鐖惰妭鐐?], '鏈煡'),
                      })}`,
                      `鍔垮姏 ${makeInlineEditableValue(toText(item && item.鍔垮姏, '鏈煡'), {
                        path: [...basePath, '鍔垮姏'],
                        kind: 'string',
                        rawValue: toText(item && item.鍔垮姏, '鏈煡'),
                      })}`,
                      `绫诲瀷 ${makeInlineEditableValue(toText(item && item.鑺傜偣绫诲瀷, '鏈煡'), {
                        path: [...basePath, '鑺傜偣绫诲瀷'],
                        kind: 'string',
                        rawValue: toText(item && item.鑺傜偣绫诲瀷, '鏈煡'),
                      })}`,
                      `閲嶈搴?${makeInlineEditableValue(String(toNumber(item && item.importance, 0)), {
                        path: [...basePath, 'importance'],
                        kind: 'number',
                        rawValue: toNumber(item && item.importance, 0),
                        editorMeta: { min: 0, integer: true, hint: '鏈€灏?0 路 鏁存暟' },
                      })}`,
                      `鐘舵€?${makeInlineEditableValue(toText(item && item.state, 'intact'), {
                        path: [...basePath, 'state'],
                        kind: 'string',
                        rawValue: toText(item && item.state, 'intact'),
                      })}`,
                      `鎻忚堪 ${makeInlineEditableValue(toText(item && item['鎻忚堪'], '鏃?), {
                        path: [...basePath, '鎻忚堪'],
                        kind: 'string',
                        rawValue: toText(item && item['鎻忚堪'], '鏃?),
                        multiline: true,
                      })}`,
                    ].join(' / ')
                  };
                }) : [{ title: '鏆傛棤鍔ㄦ€佽妭鐐?, desc: '褰撳墠鍓ф儏灏氭湭鎵╁睍鍑烘柊鐨勫姩鎬佸湴鐐广€? }])}
              </div>
            </div>
          `
        };
      }

      if (previewKey === '涓栫晫鐘舵€佹€昏') {
        const worldTime = resolveShellText(deepGet(snapshot, 'rootData.world.鏃堕棿._calendar', deepGet(snapshot, 'rootData.world.鏃堕棿.calendar', '')), '');
        const deviation = toNumber(deepGet(snapshot, 'rootData.world.鍋忓樊鍊?, 0), 0);
        const forestRatio = Math.max(0, Math.min(100, Number(((toNumber(snapshot.forestKilledAge, 0) / 1000000) * 100).toFixed(1))));
        const worldAlertText = resolveShellText(snapshot.worldAlert, '');
        const recentNews = buildRecentNewsSummary(snapshot, { seqLimit: 4, intelLimit: 4 });
        const recentPlans = buildRecentPlanSummary(snapshot, { worldLimit: 4, recordLimit: 4 });
        const timelineCards = (snapshot.timelineEntries || [])
          .slice(0, 5)
          .map(([name, item]) => ({
            title: name,
            desc: toText(item && (item.浜嬩欢 || item['浜嬩欢'] || item.desc || item['鎻忚堪']), '')
          }))
          .filter(item => item.title && resolveShellText(item.desc, ''));
        const newsCards = (recentNews.cards || [])
          .slice(0, 5)
          .map(item => ({
            title: toText(item && item.title, ''),
            desc: toText(item && item.desc, '')
          }))
          .filter(item => item.title && resolveShellText(item.desc, ''));
        const planCards = (recentPlans.cards || [])
          .slice(0, 5)
          .map(item => ({
            title: toText(item && item.title, ''),
            desc: toText(item && item.desc, '')
          }))
          .filter(item => item.title && resolveShellText(item.desc, ''));
        const auctionStatus = resolveShellText(deepGet(snapshot, 'rootData.world.鎷嶅崠.鐘舵€?, ''), '');
        const auctionLocation = resolveShellText(deepGet(snapshot, 'rootData.world.鎷嶅崠.鍦扮偣', ''), '');
        const hasAuction = auctionStatus && auctionStatus !== '浼戝競';
        return {
          title: '涓栫晫',
          summary: '',
          body: `
            <div class="archive-modal-grid mvu-shell-compact-detail">
              <div class="archive-card full">
                <div class="archive-card-head"><div class="archive-card-title">涓栫晫鐘舵€?/div></div>
                ${makeTileGrid([
                  ...(worldTime ? [{ label: '鏃堕棿', value: htmlEscape(worldTime) }] : []),
                  { label: '鍋忓樊', value: htmlEscape(String(deviation)) },
                  { label: '妫€?, value: htmlEscape(`${forestRatio}%`) },
                  ...(worldAlertText ? [{ label: '璀︽姤', value: htmlEscape(worldAlertText) }] : []),
                  ...(hasAuction ? [{ label: '鎷嶅崠', value: htmlEscape(auctionLocation ? `${auctionStatus} / ${auctionLocation}` : auctionStatus) }] : []),
                ], 'two')}
              </div>
              ${timelineCards.length ? `
                <div class="archive-card full">
                  <div class="archive-card-head"><div class="archive-card-title">缂栧勾</div></div>
                  ${makeTimelineStack(timelineCards)}
                </div>
              ` : ''}
              ${newsCards.length ? `
                <div class="archive-card full">
                  <div class="archive-card-head"><div class="archive-card-title">瑙侀椈</div></div>
                  ${makeTimelineStack(newsCards)}
                </div>
              ` : ''}
              ${planCards.length ? `
                <div class="archive-card full">
                  <div class="archive-card-head"><div class="archive-card-title">瀹夋帓</div></div>
                  ${makeTimelineStack(planCards)}
                </div>
              ` : ''}
            </div>
          `
        };
      }

      if (previewKey === '褰撳墠鑺傜偣璇︽儏' || previewKey === '浜ゆ槗妯″潡寮圭獥' || previewKey === '浜ゆ槗缃戠粶') {
        return {
          title: '浜ゆ槗涓庨┗鍦颁俊鎭?,
          summary: '褰撳墠鑺傜偣鍟嗗簵涓庡彲瑙佸簱瀛樸€?,
          body: '',
          onMount: (container) => {
            if (!isSnapshotPlayerControlled(snapshot)) {
              container.innerHTML = '<div class="archive-card full"><div class="archive-card-head"><div class="archive-card-title">鏃佽瑙嗚</div></div><div class="intel-layout"><div class="intel-card"><b>搴楅摵姒傝</b><span>浜ゆ槗涓嶅彲鐢?/span></div></div></div>';
              return null;
            }
            const 鎸傝浇浜ゆ槗闈㈡澘 = () => window.mountTradeUI(container, snapshot, {
              initialTab: tradeLaunchOptions.initialTab,
              prefillNpc: tradeLaunchOptions.prefillNpc,
              lockNpc: tradeLaunchOptions.lockNpc,
              preferredStore: tradeLaunchOptions.preferredStore,
              prefillAction: tradeLaunchOptions.prefillAction,
              prefillItem: tradeLaunchOptions.prefillItem,
              prefillQty: tradeLaunchOptions.prefillQty,
              prefillPrice: tradeLaunchOptions.prefillPrice,
              autoExecute: tradeLaunchOptions.autoExecute,
              onTradeAction: (actionData) => {
                dispatchUiAiRequest(actionData.playerInput, actionData.systemPrompt, {
                  requestKind: actionData.requestKind,
                  patchOps: actionData.patchOps
                });
              }
            });
            if (typeof window.mountTradeUI === 'function') {
              return 鎸傝浇浜ゆ槗闈㈡澘();
            }
            container.innerHTML = '<div class="archive-card full"><div class="archive-card-head"><div class="archive-card-title">浜ゆ槗妯″潡</div></div><div class="dossier-empty-note">浜ゆ槗妯″潡鍔犺浇涓?..</div></div>';
            void 璇锋眰棰勭儹棰勮渚濊禆('浜ゆ槗缃戠粶', 'trade_preview_mount')
              .then(() => {
                if (!container.isConnected) return;
                if (typeof window.mountTradeUI !== 'function') {
                  container.innerHTML = '<div class="archive-card full"><div class="archive-card-head"><div class="archive-card-title">浜ゆ槗妯″潡</div></div><div class="dossier-empty-note">浜ゆ槗妯″潡鏆備笉鍙敤</div></div>';
                  return;
                }
                container.innerHTML = '';
                activeSubUI = 鎸傝浇浜ゆ槗闈㈡澘();
              })
              .catch(() => {
                if (container.isConnected) {
                  container.innerHTML = '<div class="archive-card full"><div class="archive-card-head"><div class="archive-card-title">浜ゆ槗妯″潡</div></div><div class="dossier-empty-note">浜ゆ槗妯″潡鍔犺浇澶辫触</div></div>';
                }
              });
            return null;
          }
        };
      }

      if (previewKey === '绯荤粺鎾姤涓庢棩蹇?) {
        const 缁堢鍘熷鏉＄洰 = [
          { title: '鏈€杩戞挱鎶?, desc: resolveShellText(deepGet(snapshot, 'rootData.sys.绯荤粺鎾姤', ''), '') },
          { title: '鏈€杩戜簨浠?, desc: snapshot.latestTimeline ? snapshot.latestTimeline[0] : '' },
          { title: '瀹夋帓鎽樿', desc: resolveShellText(snapshot.pendingRequests[0], '') },
          { title: '鎯呮姤鎽樿', desc: getLatestUnlockedIntelText(snapshot, 24, '') },
          ...snapshot.timelineEntries.slice(0, 4).map(([name, item]) => ({
            title: name,
            desc: toText(item && (item['event'] || item['浜嬩欢']), '')
          }))
        ].filter(item => item.title && resolveShellText(item.desc, ''));
        const 缁堢鍘婚噸閿?= new Set();
        const 缁堢鏉＄洰 = 缁堢鍘熷鏉＄洰.filter(鏉＄洰 => {
          const 鍐呭閿?= resolveShellText(鏉＄洰 && 鏉＄洰.desc, '') || toText(鏉＄洰 && 鏉＄洰.title, '');
          if (!鍐呭閿? return false;
          if (缁堢鍘婚噸閿?has(鍐呭閿?) return false;
          缁堢鍘婚噸閿?add(鍐呭閿?;
          return true;
        });
        return {
          title: '缁堢',
          summary: '',
          body: `
            <div class="archive-modal-grid terminal-detail-grid">
              <div class="archive-card full">
                <div class="archive-card-head"><div class="archive-card-title">绯荤粺骞挎挱</div></div>
                ${makeTimelineStack(缁堢鏉＄洰)}
              </div>
            </div>
          `
        };
      }

      if (previewKey === '鎿嶄綔鎬荤嚎') {
        const planSummary = buildRecentPlanSummary(snapshot, { worldLimit: 6, recordLimit: 6 });
        return {
          title: '杩戞湡瀹夋帓',
          summary: '',
          body: `
            <div class="archive-modal-grid" style="grid-template-columns: 1fr;">
              <div class="archive-card full">
                <div class="archive-card-head"><div class="archive-card-title">褰撳墠鐘舵€?/div></div>
                ${makeTileGrid([
                  { label: '褰撳墠琛屽姩', value: toText(status.琛屽姩, '鏃ュ父') },
                  { label: '鎵€鍦ㄤ綅缃?, value: snapshot.currentLoc },
                  { label: '浼ゅ娍', value: getDisplayWoundLabel(stat) },
                  { label: '涓栫晫瀹夋帓', value: String(planSummary.worldPlans.length) },
                  { label: '涓汉寰呭姙', value: String(planSummary.personalPlans.length) },
                  { label: '璇锋眰鎽樿', value: String(snapshot.pendingRequests.length) }
                ], 'two')}
              </div>
              ${鏋勫缓鍙帺閫夐」鍗″尯(snapshot, '鎿嶄綔鎬荤嚎')}
              <div class="archive-card full">
                <div class="archive-card-head"><div class="archive-card-title">涓栫晫鏃ョ▼</div></div>
                ${makeTimelineStack(planSummary.worldPlans.length ? planSummary.worldPlans : [{ title: '鏆傛棤涓栫晫瀹夋帓', desc: '鏆傛棤', className: 'timeline-card--single' }])}
              </div>
              <div class="archive-card full">
                <div class="archive-card-head"><div class="archive-card-title">涓汉寰呭姙</div></div>
                ${makeTimelineStack(planSummary.personalPlans.length ? planSummary.personalPlans : [{ title: '鏆傛棤涓汉寰呭姙', desc: '鏆傛棤', className: 'timeline-card--single' }])}
              </div>
              <div class="archive-card full">
                <div class="archive-card-head"><div class="archive-card-title">璇锋眰鎽樿</div></div>
                ${makeTimelineStack((snapshot.pendingRequests.length ? snapshot.pendingRequests : ['鏆傛棤璇锋眰鎽樿']).map((item, index) => ({
                  title: `璇锋眰 ${index + 1}`,
                  desc: shortenText(toText(item, '鏆傛棤璇锋眰鎽樿'), 28),
                  descTitle: toText(item, '鏆傛棤璇锋眰鎽樿'),
                  className: 'timeline-card--single',
                })))}
              </div>
            </div>
          `
        };
      }

      if (previewKey === '璇曠偧涓庢儏鎶?) {
        const activeCharKey = resolveSnapshotCharKey(snapshot, toText(snapshot.activeName, '')) || toText(snapshot.activeName, '');
        const activeChar = getActiveSnapshotCharacter(snapshot);
        const soulTowerEligible = isSoulTowerEligibleCharacterData(activeChar);
        const towerPath = soulTowerEligible && activeCharKey ? ['char', activeCharKey, '榄傜伒濉旇褰?] : [];
        const towerDiscountSpirit = soulTowerEligible
          ? normalizeSoulTowerDiscountSpiritRecord(deepGet(snapshot, 'activeChar.榄傜伒濉旇褰?褰撳墠浜旀姌榄傜伒', {}))
          : createEmptySoulTowerDiscountSpiritRecord();
        const ascensionTicketCount = (snapshot.inventoryEntries || [])
          .filter(([name]) => /鍗囩伒鍙?.test(toText(name, '')))
          .reduce((sum, [, item]) => sum + Math.max(0, toNumber(item && item['鏁伴噺'], 0)), 0);
        const trialTickets = (snapshot.inventoryEntries || [])
          .filter(([name]) => (soulTowerEligible ? /鍗囩伒鍙皘榄傜伒濉?.test(toText(name, '')) : /鍗囩伒鍙?.test(toText(name, ''))))
          .slice(0, 8);
        const towerHighestFloor = toNumber(deepGet(snapshot, 'activeChar.榄傜伒濉旇褰?鏈€楂樺眰', 0), 0);
        const towerGateText = getSoulTowerStageTextByHighestFloor(towerHighestFloor);
        const towerNextBossText = getSoulTowerNextBossFloorText(towerHighestFloor);
        const ticketCards = trialTickets.length
          ? trialTickets.map(([name, item]) => {
            const 鍘熷鎻忚堪 = toText(item && item['鎻忚堪'], '鍙敤浜庡搴旇瘯鐐煎満鏅€?);
            return {
              title: `${name} 脳${toNumber(item && item['鏁伴噺'], 0)}`,
              desc: shortenText(鍘熷鎻忚堪, 22),
              descTitle: 鍘熷鎻忚堪,
              className: 'intel-card--single',
            };
          })
          : [{ title: '鏆傛棤璇曠偧闂ㄧエ', desc: '鏆傛棤鍙敤闂ㄧエ', className: 'intel-card--single' }];
        delete modalFocusState[`${previewKey}::trial-focus`];
        return {
          title: '璇曠偧涓庢儏鎶?,
          summary: '',
          body: `
            <div class="archive-modal-grid" style="grid-template-columns: repeat(2, minmax(0, 1fr)); align-items:stretch;">
              <div class="archive-card full">
                <div class="archive-card-head"><div class="archive-card-title">璇曠偧鎬昏</div></div>
                ${makeTileGrid([
                  { label: '鍗囩伒鍙伴棬绁?, value: String(ascensionTicketCount) },
                  ...(soulTowerEligible
                    ? [{
                        label: '榄傜伒濉旀渶楂?,
                        value: towerPath.length
                          ? makeInlineEditableValue(String(toNumber(deepGet(snapshot, 'activeChar.榄傜伒濉旇褰?鏈€楂樺眰', 0), 0)), {
                              path: [...towerPath, '鏈€楂樺眰'],
                              kind: 'number',
                              rawValue: toNumber(deepGet(snapshot, 'activeChar.榄傜伒濉旇褰?鏈€楂樺眰', 0), 0),
                              editorMeta: { min: 0, integer: true, hint: '鏈€灏?0 路 鏁存暟' },
                            })
                          : htmlEscape(String(toNumber(deepGet(snapshot, 'activeChar.榄傜伒濉旇褰?鏈€楂樺眰', 0), 0))),
                      }, {
                        label: '褰撳墠鍒嗗尯',
                        value: towerGateText,
                      }, {
                        label: '涓嬩釜鍖洪棿涓婇檺',
                        value: towerNextBossText,
                      }]
                    : []),
                  { label: '绱鐚庢潃骞撮檺', value: formatNumber(snapshot.forestKilledAge || 0) },
                  { label: '鍥鹃壌鏉＄洰', value: String((snapshot.bestiaryEntries || []).length || 0) },
                  { label: '褰撳墠鎴樺姛', value: formatNumber(deepGet(snapshot, 'activeChar.璐㈠瘜.鎴樺姛', 0)) }
                ], 'two')}
              </div>

              <div class="archive-card full">
                <div class="archive-card-head"><div class="archive-card-title">闂ㄧエ涓庤祫鏍?/div></div>
                <div class="intel-layout" style="display:grid; grid-template-columns:repeat(2, minmax(0, 1fr)); gap:12px;">
                  ${ticketCards.map(item => `<div class="intel-card ${item.className || ''}" style="min-height:116px;"><b>${htmlEscape(item.title)}</b><span${item.descTitle ? ` title="${escapeHtmlAttr(item.descTitle)}"` : ''}>${htmlEscape(item.desc)}</span></div>`).join('')}
                </div>
                ${soulTowerEligible ? `
                  <div style="margin-top:12px;">
                    ${makeTimelineStack(hasActiveSoulTowerDiscountSpirit(towerDiscountSpirit)
                      ? [{
                        title: '褰撳墠鍙簲鎶橀瓊鐏?,
                        desc: shortenText(`${buildSoulTowerDiscountSpiritDisplay(towerDiscountSpirit)} 路 鍓嶅線浼犵伒濉斿悗鍙崐浠疯喘涔癭, 34),
                        descTitle: `${buildSoulTowerDiscountSpiritDisplay(towerDiscountSpirit)} 路 鍓嶅線浼犵伒濉斿悗鍙崐浠疯喘涔癭,
                        className: 'timeline-card--single',
                      }]
                      : [{ title: '鏆傛棤鎶樻墸璧勬牸', desc: '鏆傛棤鍙敤鐨勪簲鎶樼洰鏍?, className: 'timeline-card--single' }])}
                  </div>
                ` : ''}
              </div>
              ${鏋勫缓鍙帺閫夐」鍗″尯(snapshot, '璇曠偧涓庢儏鎶?)}

              <div class="archive-card full">
                <div class="archive-card-head"><div class="archive-card-title">杩戞湡鎯呮姤</div></div>
                <div class="intel-layout" style="display:grid; grid-template-columns:repeat(2, minmax(0, 1fr)); gap:12px;">
                  ${(snapshot.unlockedKnowledges.length ? snapshot.unlockedKnowledges.slice(-4).reverse() : ['鎯呮姤浠嶅緟鏀堕泦']).map(item => `<div class="intel-card intel-card--single" style="min-height:116px;"><b>${htmlEscape(shortenText(item, 20))}</b><span title="${escapeHtmlAttr(item)}">${htmlEscape(shortenText(item, 24))}</span></div>`).join('')}
                </div>
              </div>
            </div>
          `
        };
      }

      if (previewKey === '浠诲姟鐣岄潰') {
        const questRecords = (snapshot.recordEntries || []).filter(([, item]) => item && typeof item === 'object' && (
          Object.prototype.hasOwnProperty.call(item, '鐘舵€?)
          || Object.prototype.hasOwnProperty.call(item, '鐩爣杩涘害')
          || Object.prototype.hasOwnProperty.call(item, '濂栧姳甯?)
          || Object.prototype.hasOwnProperty.call(item, '濂栧姳澹版湜')
        ));
        const questBoardEntries = safeEntries(deepGet(snapshot, 'rootData.world.濮旀墭鏉?, {})).filter(([, item]) => item && typeof item === 'object');
        const questPage = paginateModalItems(questRecords, previewKey, 'quest-records', 6);
        const boardPage = paginateModalItems(questBoardEntries, previewKey, 'quest-board', 6);
        const questFocusStateKey = `${previewKey}::quest-focus`;
        const boardFocusStateKey = `${previewKey}::quest-board-focus`;
        let focusQuestName = toText(modalFocusState[questFocusStateKey], toText((questRecords[0] && questRecords[0][0]) || '', ''));
        if (focusQuestName && !questRecords.some(([name]) => name === focusQuestName)) focusQuestName = questRecords[0] ? questRecords[0][0] : '';
        if (focusQuestName) modalFocusState[questFocusStateKey] = focusQuestName;
        else delete modalFocusState[questFocusStateKey];
        let focusBoardId = toText(modalFocusState[boardFocusStateKey], toText((questBoardEntries[0] && questBoardEntries[0][0]) || '', ''));
        if (focusBoardId && !questBoardEntries.some(([name]) => name === focusBoardId)) focusBoardId = questBoardEntries[0] ? questBoardEntries[0][0] : '';
        if (focusBoardId) modalFocusState[boardFocusStateKey] = focusBoardId;
        else delete modalFocusState[boardFocusStateKey];
        const focusQuestEntry = focusQuestName ? questRecords.find(([name]) => name === focusQuestName) : null;
        const focusBoardEntry = focusBoardId ? questBoardEntries.find(([name]) => name === focusBoardId) : null;
        const isPlayerControlled = isSnapshotPlayerControlled(snapshot);
        const activeCharKey = resolveSnapshotCharKey(snapshot, toText(snapshot.activeName, '')) || toText(snapshot.activeName, '');
        const focusQuest = focusQuestEntry && focusQuestEntry[1];
        const focusBoard = focusBoardEntry && focusBoardEntry[1];
        const focusQuestStatus = toText(focusQuest && focusQuest['鐘舵€?], '杩涜涓?);
        const focusBoardStatus = toText(focusBoard && focusBoard['鐘舵€?], '寰呮帴鍙?);
        const focusQuestPath = focusQuestName && activeCharKey ? ['char', activeCharKey, '璁板綍', focusQuestName] : [];
        const focusBoardPath = focusBoardId ? ['world', '濮旀墭鏉?, focusBoardId] : [];
        const focusBoardDescPath = focusBoardPath.length ? [...focusBoardPath, focusBoardStatus === '寰呮帴鍙? ? '妗嗘灦鎻忚堪' : '鎻忚堪'] : [];
        const focusBoardDesc = focusBoard
          ? (focusBoardStatus === '寰呮帴鍙? ? toText(focusBoard['妗嗘灦鎻忚堪'], toText(focusBoard['鎻忚堪'], '鏈煡')) : toText(focusBoard['鎻忚堪'], toText(focusBoard['妗嗘灦鎻忚堪'], '鏈煡')))
          : '鏈煡';
        const 浠诲姟绫诲瀷閫夐」 = ['涓荤嚎浠诲姟', '鏀嚎浠诲姟', '鎮祻浠诲姟', '鎯呮姤浠诲姟', '淇偧浠诲姟', '宸ュ潑浠诲姟'];
        const 濮旀墭绫诲瀷閫夐」 = ['鎮祻濮旀墭', '鏀嚎濮旀墭', '璧勬簮濮旀墭', '鎯呮姤濮旀墭', '宸ュ潑濮旀墭', '璇曠偧濮旀墭'];
        const 涓嬫媺閫夐」 = (鍒楄〃, 褰撳墠鍊?= '') => 鍒楄〃.map(椤?=> `<option value="${escapeHtmlAttr(椤?}" ${椤?=== 褰撳墠鍊?? 'selected' : ''}>${htmlEscape(椤?}</option>`).join('');
        const questCreateFormHtml = activeCharKey ? `
          <div class="request-console-row quest-guided-row" data-collection-panel="quest-record-create" style="display:flex; gap:8px; flex-wrap:wrap; margin-bottom:12px;">
            <input class="request-console-input" style="margin:0; flex:1 1 180px;" data-collection-input="record-name" value="" placeholder="浠诲姟鍚嶏細璋冩煡澶辫釜鏈虹敳" aria-label="浠诲姟鍚? />
            <select class="request-console-input" style="margin:0; flex:0 1 132px;" data-collection-input="record-type" aria-label="浠诲姟绫诲瀷">${涓嬫媺閫夐」(浠诲姟绫诲瀷閫夐」, '鏀嚎浠诲姟')}</select>
            <input type="number" min="1" step="1" inputmode="numeric" class="request-console-input" style="margin:0; width:86px; flex:0 0 86px;" data-collection-input="record-target" value="3" placeholder="闃舵" aria-label="鐩爣闃舵" />
            <input type="number" min="0" step="1" inputmode="numeric" class="request-console-input" style="margin:0; width:96px; flex:0 0 96px;" data-collection-input="record-rewardCoin" value="0" placeholder="鑱旈偊甯? aria-label="濂栧姳鑱旈偊甯? />
            <input type="number" min="0" step="1" inputmode="numeric" class="request-console-input" style="margin:0; width:96px; flex:0 0 96px;" data-collection-input="record-rewardRep" value="0" placeholder="澹版湜" aria-label="濂栧姳澹版湜" />
            <input class="request-console-input" style="margin:0; flex:1 1 240px;" data-collection-input="record-desc" value="" placeholder="鐩爣锛氭壘鍒扮嚎绱?/ 鎶ら€佸埌涓滈棬" aria-label="浠诲姟鐩爣" />
            <button type="button" class="tag-chip live" data-collection-action="add-record" data-collection-char="${escapeHtmlAttr(activeCharKey)}">璁板綍浠诲姟</button>
          </div>
        ` : '';
        const boardCreateFormHtml = `
          <div class="request-console-row quest-guided-row" data-collection-panel="quest-board-create" style="display:flex; gap:8px; flex-wrap:wrap; margin-bottom:12px;">
            <input class="request-console-input" style="margin:0; flex:1 1 180px;" data-collection-input="board-name" value="" placeholder="濮旀墭鍚嶏細鎶ら€佹牱鏈? aria-label="濮旀墭鍚? />
            <select class="request-console-input" style="margin:0; flex:0 1 132px;" data-collection-input="board-type" aria-label="濮旀墭绫诲瀷">${涓嬫媺閫夐」(濮旀墭绫诲瀷閫夐」, '鎮祻濮旀墭')}</select>
            <input class="request-console-input" style="margin:0; flex:0 1 132px;" data-collection-input="board-publisher" value="浼犵伒濉? placeholder="鍙戝竷鑰? aria-label="鍙戝竷鑰? />
            <input type="number" min="1" step="1" inputmode="numeric" class="request-console-input" style="margin:0; width:86px; flex:0 0 86px;" data-collection-input="board-target" value="3" placeholder="闃舵" aria-label="棰勮闃舵" />
            <select class="request-console-input" style="margin:0; flex:0 1 88px;" data-collection-input="board-difficulty" aria-label="闅惧害">${涓嬫媺閫夐」(['浣?, '涓?, '楂?, '鏋侀珮'], '涓?)}</select>
            <select class="request-console-input" style="margin:0; flex:0 1 96px;" data-collection-input="board-resource" aria-label="璧勬簮绾у埆">${涓嬫媺閫夐」(['鏃?, '浣?, '涓?, '楂?, '绋€鏈?], '鏃?)}</select>
            <input type="number" min="0" step="1" inputmode="numeric" class="request-console-input" style="margin:0; width:96px; flex:0 0 96px;" data-collection-input="board-rewardCoin" value="0" placeholder="鑱旈偊甯? aria-label="濂栧姳鑱旈偊甯? />
            <input type="number" min="0" step="1" inputmode="numeric" class="request-console-input" style="margin:0; width:96px; flex:0 0 96px;" data-collection-input="board-rewardRep" value="0" placeholder="澹版湜" aria-label="濂栧姳澹版湜" />
            <input class="request-console-input" style="margin:0; flex:1 1 260px;" data-collection-input="board-desc" value="" placeholder="鐩爣锛氬彇鍥炴潗鏂?/ 鏍稿疄鎯呮姤" aria-label="濮旀墭鐩爣" />
            <button type="button" class="tag-chip live" data-collection-action="add-board">鍙戝竷濮旀墭</button>
          </div>
        `;
        const questListHtml = questPage.items.length
          ? questPage.items.map(([name, item]) => {
              const status = toText(item && item['鐘舵€?], '杩涜涓?);
              const progress = `${toNumber(item && item['褰撳墠杩涘害'], 0)}/${toNumber(item && item['鐩爣杩涘害'], 1)}`;
              const badgeClass = status === '宸插畬鎴? ? 'live' : (status === '宸叉斁寮? ? 'warn' : '');
              return `
                <button type="button" class="role-switch-tile ${name === focusQuestName ? 'active' : ''}" data-quest-focus="${escapeHtmlAttr(name)}" style="text-align:left;">
                  <div class="role-switch-head" style="margin-bottom:6px;font-size:15px;"><b>${htmlEscape(name)}</b><span class="state-tag ${badgeClass}">${htmlEscape(status)}</span></div>
                  <div style="display:grid;gap:4px;">
                    <span>${htmlEscape(`杩涘害锛?{progress}`)}</span>
                    <span>${htmlEscape(`濂栧姳锛?{formatNumber(toNumber(item && item['濂栧姳甯?], 0))} 甯?/ ${formatNumber(toNumber(item && item['濂栧姳澹版湜'], 0))} 澹版湜`)}</span>
                    <span>${htmlEscape(toText(item && item['鎻忚堪'], '鏃犺鏄?))}</span>
                  </div>
                </button>
              `;
            }).join('')
          : '<div style="padding: 12px 16px; color: var(--color-text-secondary); text-align: center; font-size: 13px;">鏆傛棤浠诲姟</div>';
        const boardListHtml = boardPage.items.length
          ? boardPage.items.map(([id, item]) => {
              const title = toText(item && item['鏍囬'], id);
              const status = toText(item && item['鐘舵€?], '寰呮帴鍙?);
              const publisher = toText(item && item['鍙戝竷鑰?], '绯荤粺');
              const badgeClass = status === '寰呮帴鍙? ? 'live' : (status === '杩涜涓? || status === '鍙彁浜? ? 'warn' : '');
              return `
                <button type="button" class="role-switch-tile ${id === focusBoardId ? 'active' : ''}" data-quest-board-focus="${escapeHtmlAttr(id)}" style="text-align:left;">
                  <div class="role-switch-head" style="margin-bottom:6px;font-size:15px;"><b>${htmlEscape(title)}</b><span class="state-tag ${badgeClass}">${htmlEscape(status)}</span></div>
                  <div style="display:grid;gap:4px;">
                    <span>${htmlEscape(`鍙戝竷鑰咃細${publisher}`)}</span>
                    <span>${htmlEscape(`闅惧害锛?{toText(item && item['闅惧害'], '涓?)} / 璧勬簮锛?{toText(item && item['璧勬簮绾у埆'], '鏃?)}`)}</span>
                    <span>${htmlEscape(`濂栧姳锛?{formatNumber(toNumber(item && item['濂栧姳甯?], 0))} 甯?/ ${formatNumber(toNumber(item && item['濂栧姳澹版湜'], 0))} 澹版湜`)}</span>
                  </div>
                </button>
              `;
            }).join('')
          : '<div style="padding: 12px 16px; color: var(--color-text-secondary); text-align: center; font-size: 13px;">鏆傛棤濮旀墭</div>';
        const questDetailHtml = focusQuest
          ? `
              ${makeTileGrid([
                { label: '浠诲姟鍚嶇О', value: focusQuestName },
                { label: '鐘舵€?, value: focusQuestPath.length
                  ? makeInlineEditableValue(focusQuestStatus, {
                      path: [...focusQuestPath, '鐘舵€?],
                      kind: 'string',
                      rawValue: focusQuestStatus,
                    })
                  : htmlEscape(focusQuestStatus) },
                { label: '浠诲姟绫诲瀷', value: focusQuestPath.length
                  ? makeInlineEditableValue(toText(focusQuest['绫诲瀷'], '浠诲姟'), {
                      path: [...focusQuestPath, '绫诲瀷'],
                      kind: 'string',
                      rawValue: toText(focusQuest['绫诲瀷'], '浠诲姟'),
                    })
                  : htmlEscape(toText(focusQuest['绫诲瀷'], '浠诲姟')) },
                { label: '褰撳墠杩涘害', value: focusQuestPath.length
                  ? makeInlineEditableValue(String(toNumber(focusQuest['褰撳墠杩涘害'], 0)), {
                      path: [...focusQuestPath, '褰撳墠杩涘害'],
                      kind: 'number',
                      rawValue: toNumber(focusQuest['褰撳墠杩涘害'], 0),
                      editorMeta: { min: 0, integer: true, hint: '鏈€灏?0 路 鏁存暟' },
                    })
                  : htmlEscape(String(toNumber(focusQuest['褰撳墠杩涘害'], 0))) },
                { label: '鐩爣杩涘害', value: focusQuestPath.length
                  ? makeInlineEditableValue(String(toNumber(focusQuest['鐩爣杩涘害'], 1)), {
                      path: [...focusQuestPath, '鐩爣杩涘害'],
                      kind: 'number',
                      rawValue: toNumber(focusQuest['鐩爣杩涘害'], 1),
                      editorMeta: { min: 1, integer: true, hint: '鏈€灏?1 路 鏁存暟' },
                    })
                  : htmlEscape(String(toNumber(focusQuest['鐩爣杩涘害'], 1))) },
                { label: '濂栧姳閲戝竵', value: focusQuestPath.length
                  ? makeInlineEditableValue(formatNumber(toNumber(focusQuest['濂栧姳甯?], 0)), {
                      path: [...focusQuestPath, '濂栧姳甯?],
                      kind: 'number',
                      rawValue: toNumber(focusQuest['濂栧姳甯?], 0),
                      editorMeta: { min: 0, integer: true, hint: '鏈€灏?0 路 鏁存暟' },
                    })
                  : htmlEscape(formatNumber(toNumber(focusQuest['濂栧姳甯?], 0))) },
                { label: '濂栧姳澹版湜', value: focusQuestPath.length
                  ? makeInlineEditableValue(formatNumber(toNumber(focusQuest['濂栧姳澹版湜'], 0)), {
                      path: [...focusQuestPath, '濂栧姳澹版湜'],
                      kind: 'number',
                      rawValue: toNumber(focusQuest['濂栧姳澹版湜'], 0),
                      editorMeta: { min: 0, integer: true, hint: '鏈€灏?0 路 鏁存暟' },
                    })
                  : htmlEscape(formatNumber(toNumber(focusQuest['濂栧姳澹版湜'], 0))) }
              ], 'two')}
              <div style="margin-top: 16px;">
                <div style="font-size: 11px; color: var(--color-text-secondary); margin-bottom: 6px; font-weight: 500;">褰撳墠杩涘害 <span style="float: right; color: var(--cyan);">${toNumber(focusQuest['褰撳墠杩涘害'], 0)} / ${toNumber(focusQuest['鐩爣杩涘害'], 1)}</span></div>
                <div style="height: 6px; background: rgba(0, 229, 255, 0.1); border-radius: 3px; overflow: hidden; box-shadow: inset 0 1px 3px rgba(0,0,0,0.5);">
                  <div style="height: 100%; background: linear-gradient(90deg, rgba(0,229,255,0.6), rgba(0,229,255,1)); width: ${ratioPercent(toNumber(focusQuest['褰撳墠杩涘害'], 0), toNumber(focusQuest['鐩爣杩涘害'], 1))}%; border-radius: 3px; box-shadow: 0 0 8px rgba(0,229,255,0.8); transition: width 0.3s ease;"></div>
                </div>
              </div>
            `
          : makeTileGrid([{ label: '鐘舵€?, value: '鏈€夋嫨' }], 'two');
        const questActionHtml = focusQuestPath.length
          ? `
              <div class="request-console-row" style="display:flex; gap:8px; flex-wrap:wrap; margin-top:16px;">
                <button type="button" class="tag-chip" data-collection-action="delete-record" data-collection-char="${escapeHtmlAttr(activeCharKey)}" data-collection-key="${escapeHtmlAttr(focusQuestName)}">鍒犻櫎浠诲姟</button>
                ${focusQuest && isPlayerControlled && !['宸插畬鎴?, '宸叉斁寮?, '澶辫触', '宸插け璐?].includes(focusQuestStatus)
                  ? `${focusQuestStatus === '鍙彁浜? ? `<button type="button" class="relation-action-btn quest-action-btn" data-quest-action="submit" data-quest-target="${escapeHtmlAttr(focusQuestName)}">鎻愪氦浠诲姟</button>` : ''}<button type="button" class="relation-action-btn quest-action-btn" data-quest-action="abandon" data-quest-target="${escapeHtmlAttr(focusQuestName)}">鏀惧純浠诲姟</button>`
                  : ''}
              </div>
            `
          : '';
        const boardDetailHtml = focusBoard
          ? makeTileGrid([
              { label: '濮旀墭鏍囬', value: focusBoardPath.length
                ? makeInlineEditableValue(toText(focusBoard['鏍囬'], focusBoardId), {
                    path: [...focusBoardPath, '鏍囬'],
                    kind: 'string',
                    rawValue: toText(focusBoard['鏍囬'], focusBoardId),
                  })
                : htmlEscape(toText(focusBoard['鏍囬'], focusBoardId)) },
              { label: '鐘舵€?, value: focusBoardPath.length
                ? makeInlineEditableValue(focusBoardStatus, {
                    path: [...focusBoardPath, '鐘舵€?],
                    kind: 'string',
                    rawValue: focusBoardStatus,
                  })
                : htmlEscape(focusBoardStatus) },
              { label: '绫诲瀷', value: focusBoardPath.length
                ? makeInlineEditableValue(toText(focusBoard['绫诲瀷'], '濮旀墭'), {
                    path: [...focusBoardPath, '绫诲瀷'],
                    kind: 'string',
                    rawValue: toText(focusBoard['绫诲瀷'], '濮旀墭'),
                  })
                : htmlEscape(toText(focusBoard['绫诲瀷'], '濮旀墭')) },
              { label: '鍙戝竷鑰?, value: focusBoardPath.length
                ? makeInlineEditableValue(toText(focusBoard['鍙戝竷鑰?], '绯荤粺'), {
                    path: [...focusBoardPath, '鍙戝竷鑰?],
                    kind: 'string',
                    rawValue: toText(focusBoard['鍙戝竷鑰?], '绯荤粺'),
                  })
                : htmlEscape(toText(focusBoard['鍙戝竷鑰?], '绯荤粺')) },
              { label: '闈㈠悜', value: focusBoardPath.length
                ? makeInlineEditableValue(toText(focusBoard['闈㈠悜'], '鍏紑'), {
                    path: [...focusBoardPath, '闈㈠悜'],
                    kind: 'string',
                    rawValue: toText(focusBoard['闈㈠悜'], '鍏紑'),
                  })
                : htmlEscape(toText(focusBoard['闈㈠悜'], '鍏紑')) },
              { label: '鎵挎帴鑰?, value: focusBoardPath.length
                ? makeInlineEditableValue(toText(focusBoard['鎵挎帴鑰?], '鏃?), {
                    path: [...focusBoardPath, '鎵挎帴鑰?],
                    kind: 'string',
                    rawValue: toText(focusBoard['鎵挎帴鑰?], '鏃?),
                  })
                : htmlEscape(toText(focusBoard['鎵挎帴鑰?], '鏃?)) },
              { label: '棰勮闃舵', value: focusBoardPath.length
                ? makeInlineEditableValue(formatNumber(toNumber(focusBoard['鐩爣杩涘害'], 1)), {
                    path: [...focusBoardPath, '鐩爣杩涘害'],
                    kind: 'number',
                    rawValue: toNumber(focusBoard['鐩爣杩涘害'], 1),
                    editorMeta: { min: 1, integer: true, hint: '鏈€灏?1 路 鏁存暟' },
                  })
                : htmlEscape(formatNumber(toNumber(focusBoard['鐩爣杩涘害'], 1))) },
              { label: '闅惧害', value: focusBoardPath.length
                ? makeInlineEditableValue(toText(focusBoard['闅惧害'], '涓?), {
                    path: [...focusBoardPath, '闅惧害'],
                    kind: 'string',
                    rawValue: toText(focusBoard['闅惧害'], '涓?),
                  })
                : htmlEscape(toText(focusBoard['闅惧害'], '涓?)) },
              { label: '璧勬簮绾у埆', value: focusBoardPath.length
                ? makeInlineEditableValue(toText(focusBoard['璧勬簮绾у埆'], '鏃?), {
                    path: [...focusBoardPath, '璧勬簮绾у埆'],
                    kind: 'string',
                    rawValue: toText(focusBoard['璧勬簮绾у埆'], '鏃?),
                  })
                : htmlEscape(toText(focusBoard['璧勬簮绾у埆'], '鏃?)) },
              { label: '濂栧姳閲戝竵', value: focusBoardPath.length
                ? makeInlineEditableValue(formatNumber(toNumber(focusBoard['濂栧姳甯?], 0)), {
                    path: [...focusBoardPath, '濂栧姳甯?],
                    kind: 'number',
                    rawValue: toNumber(focusBoard['濂栧姳甯?], 0),
                    editorMeta: { min: 0, integer: true, hint: '鏈€灏?0 路 鏁存暟' },
                  })
                : htmlEscape(formatNumber(toNumber(focusBoard['濂栧姳甯?], 0))) },
              { label: '濂栧姳澹版湜', value: focusBoardPath.length
                ? makeInlineEditableValue(formatNumber(toNumber(focusBoard['濂栧姳澹版湜'], 0)), {
                    path: [...focusBoardPath, '濂栧姳澹版湜'],
                    kind: 'number',
                    rawValue: toNumber(focusBoard['濂栧姳澹版湜'], 0),
                    editorMeta: { min: 0, integer: true, hint: '鏈€灏?0 路 鏁存暟' },
                  })
                : htmlEscape(formatNumber(toNumber(focusBoard['濂栧姳澹版湜'], 0))) }
            ], 'two')
          : makeTileGrid([{ label: '鐘舵€?, value: '鏈€夋嫨' }], 'two');
        const boardActionHtml = focusBoardPath.length
          ? `
              <div class="request-console-row" style="display:flex; gap:8px; flex-wrap:wrap; margin-top:16px;">
                <button type="button" class="tag-chip" data-collection-action="delete-board" data-collection-key="${escapeHtmlAttr(focusBoardId)}">鍒犻櫎濮旀墭</button>
                ${focusBoard && isPlayerControlled && focusBoardStatus === '寰呮帴鍙?
                  ? `<button type="button" class="relation-action-btn quest-action-btn" data-quest-action="accept" data-quest-target="${escapeHtmlAttr(focusBoardId)}">鎺ュ彇濮旀墭</button>`
                  : ''}
              </div>
            `
          : '';
        return {
          title: '浠诲姟鐣岄潰',
          summary: '鎴戠殑浠诲姟璁板綍涓庝笘鐣屽鎵樻澘銆?,
          body: `
            <div class="archive-modal-grid">
                <div class="archive-card">
                  <div class="archive-card-head"><div class="archive-card-title">鎴戠殑浠诲姟</div><span class="state-tag ${questRecords.length ? 'live' : 'warn'}">${questRecords.length ? `鍏?${questRecords.length} 椤筦 : '绌?}</span></div>
                  ${questCreateFormHtml}
                  <div class="relation-directory-grid">${questListHtml}</div>
                  ${makeModalPaginationControls('quest-records', questPage.page, questPage.totalPages, questPage.total)}
                </div>
                <div class="archive-card">
                  <div class="archive-card-head"><div class="archive-card-title">鐒︾偣浠诲姟璇︽儏</div><span class="state-tag ${focusQuestName ? 'live' : 'warn'}">${htmlEscape(focusQuestName || '鏈€変腑')}</span></div>
                  ${questDetailHtml}
                  ${questActionHtml}
                  <div style="margin-top: 16px; padding-top: 16px; border-top: 1px dashed rgba(255,255,255,0.1);">
                    <div style="font-size: 13px; color: var(--color-text-secondary); margin-bottom: 4px;">浠诲姟璇存槑</div>
                    <div style="font-size: 14px; line-height: 1.5; color: var(--color-text-primary);">${focusQuest && focusQuestPath.length
                      ? makeInlineEditableValue(toText(focusQuest['鎻忚堪'], '鏈煡'), {
                          path: [...focusQuestPath, '鎻忚堪'],
                          kind: 'string',
                          rawValue: toText(focusQuest['鎻忚堪'], '鏈煡'),
                          multiline: true,
                        })
                      : htmlEscape(focusQuest ? toText(focusQuest['鎻忚堪'], '鏈煡') : '鏈煡')}</div>
                  </div>
                </div>
                <div class="archive-card">
                  <div class="archive-card-head"><div class="archive-card-title">濮旀墭鏉?/div><span class="state-tag ${questBoardEntries.length ? 'live' : 'warn'}">${questBoardEntries.length ? `鍏?${questBoardEntries.length} 鏉 : '绌?}</span></div>
                  ${boardCreateFormHtml}
                  <div class="relation-directory-grid">${boardListHtml}</div>
                  ${makeModalPaginationControls('quest-board', boardPage.page, boardPage.totalPages, boardPage.total)}
                </div>
                <div class="archive-card">
                  <div class="archive-card-head"><div class="archive-card-title">鐒︾偣濮旀墭璇︽儏</div><span class="state-tag ${focusBoardId ? 'live' : 'warn'}">${htmlEscape(focusBoardId || '鏈€変腑')}</span></div>
                  ${boardDetailHtml}
                  ${boardActionHtml}
                  <div style="margin-top: 16px; padding-top: 16px; border-top: 1px dashed rgba(255,255,255,0.1);">
                    <div style="font-size: 13px; color: var(--color-text-secondary); margin-bottom: 4px;">${htmlEscape(focusBoardStatus === '寰呮帴鍙? ? '濮旀墭妗嗘灦' : '濮旀墭璇存槑')}</div>
                    <div style="font-size: 14px; line-height: 1.5; color: var(--color-text-primary);">${focusBoard && focusBoardDescPath.length
                      ? makeInlineEditableValue(focusBoardDesc, {
                          path: focusBoardDescPath,
                          kind: 'string',
                          rawValue: focusBoardDesc,
                          multiline: true,
                        })
                      : htmlEscape(focusBoard ? focusBoardDesc : '鏈煡')}</div>
                  </div>
                </div>
            </div>
          `
        };
      }

      if (previewKey === '杩戞湡瑙侀椈') {
        const newsSummary = buildRecentNewsSummary(snapshot, { seqLimit: 8, intelLimit: 8 });
        return {
          title: '杩戞湡瑙侀椈 / 鍩庡唴绠€鎶?,
          summary: '鍏ㄥ眬鍓ф儏澶囧繕褰曚笌涓汉鎯呮姤搴撶殑鍚堝苟绠€鎶ャ€?,
          body: `
            <div class="archive-modal-grid" style="grid-template-columns: 1fr;">
              <div class="archive-card full">
                <div class="archive-card-head"><div class="archive-card-title">鍏ㄥ眬瑙侀椈</div></div>
                ${makeTimelineStack(newsSummary.globalNews.length ? newsSummary.globalNews : [{ title: '鏆傛棤鍏ㄥ眬瑙侀椈', desc: '褰撳墠鏈褰曟柊鐨勪笘鐣屼簨浠躲€? }])}
              </div>
              <div class="archive-card full">
                <div class="archive-card-head"><div class="archive-card-title">涓汉瑙侀椈</div></div>
                ${makeTimelineStack(newsSummary.personalNews.length ? newsSummary.personalNews : [{ title: '鏆傛棤涓汉瑙侀椈', desc: '褰撳墠瑙掕壊灏氭湭鏀跺綍鏂扮殑鎯呮姤銆? }])}
              </div>
            </div>
          `
        };
      }

      if (previewKey === '鎬墿鍥鹃壌') {
        const bestiaryRecordToCard = ([name, item]) => {
          if (item && item.empty) {
            return { title: name, desc: '鏆傛湭璁板綍浠讳綍娣辨笂鐢熺墿鎴栭瓊鍏姐€? };
          }
          const scalarParts = safeEntries(item)
            .filter(([, value]) => value === null || typeof value !== 'object')
            .slice(0, 6)
            .map(([key, value]) => {
              const displayValue = value === undefined || value === null || value === '' ? '鏃? : String(value);
              const kind = typeof value === 'number' ? 'number' : (typeof value === 'boolean' ? 'boolean' : 'string');
              return `${htmlEscape(toText(key, '瀛楁'))}锛?{makeInlineEditableValue(displayValue, {
                path: ['world', '鍥鹃壌', name, key],
                kind,
                rawValue: value === undefined ? displayValue : value,
              })}`;
            });
          const objectParts = safeEntries(item)
            .filter(([, value]) => value && typeof value === 'object')
            .slice(0, 3)
            .map(([key, value]) => `${htmlEscape(toText(key, '瀛楁'))}锛?{htmlEscape(`${safeEntries(value).length}椤筦)}`);
          return {
            title: htmlEscape(name),
            desc: scalarParts.concat(objectParts).join(' / ') || '褰撳墠浠呭缓绔嬩簡绌虹櫧鍥鹃壌澹炽€?
          };
        };
        const bestiaryCards = (snapshot.bestiaryEntries.length ? snapshot.bestiaryEntries.slice(0, 24) : [['鏆傛棤鍥鹃壌璁板綍', { empty: true }]]).map(bestiaryRecordToCard);
        const latestBestiaryCard = snapshot.bestiaryEntries[0] ? bestiaryRecordToCard(snapshot.bestiaryEntries[0]) : null;
        return {
          title: '鎬墿鍥鹃壌',
          summary: '宸查伃閬囨繁娓婄敓鐗╀笌榄傚吔鐨勬爣鍑嗗寲璁板綍銆?,
          body: `
            <div class="archive-modal-grid" style="grid-template-columns: 1fr;">
              <div class="archive-card full">
                <div class="archive-card-head"><div class="archive-card-title">鍥鹃壌鎬昏</div></div>
                ${makeTileGrid([
                  { label: '宸茶褰曠墿绉?, value: `${snapshot.bestiaryEntries.length} 绉峘 },
                  { label: '鏈€杩戞敹褰?, value: snapshot.bestiaryEntries[0] ? snapshot.bestiaryEntries[0][0] : '鏆傛棤' },
                  { label: '鍙鐢ㄧ姸鎬?, value: snapshot.bestiaryEntries.length ? '鍙綔涓哄悗缁爣鍑嗘暟鎹簮' : '绛夊緟棣栨鏀跺綍' },
                  { label: '瀛楁瑕嗙洊', value: snapshot.bestiaryEntries[0] ? `${safeEntries(snapshot.bestiaryEntries[0][1]).length} 椤筦 : '0 椤? }
                ], 'two')}
              </div>
              <div class="archive-card full">
                <div class="archive-card-head"><div class="archive-card-title">鏈€杩戞敹褰曡鎯?/div></div>
                ${makeTimelineStack(latestBestiaryCard ? [latestBestiaryCard] : [{ title: '鏆傛棤鏈€杩戞敹褰?, desc: '褰撳墠鍥鹃壌浠嶄负绌恒€? }])}
              </div>
              <div class="archive-card full">
                <div class="archive-card-head"><div class="archive-card-title">宸茶褰曠墿绉?/div></div>
                ${makeTimelineStack(bestiaryCards)}
              </div>
            </div>
          `
        };
      }

      if (previewKey === '妫灄浠囨仺鍊?) {
        const remaining = Math.max(0, 1000000 - snapshot.forestKilledAge);
        const progress = Math.max(0, Math.min(100, Number(((snapshot.forestKilledAge / 1000000) * 100).toFixed(1))));
        const stage = progress >= 100 ? '鍏芥疆涓寸晫' : (progress >= 70 ? '楂樺害绱у紶' : (progress >= 30 ? '鎸佺画鍗囨俯' : '鐩稿瀹夊叏'));
        return {
          title: '妫灄浠囨仺鍊?,
          summary: '鏄熸枟澶ф．鏋楃疮璁″嚮鏉€骞撮檺涓庡吔娼槇鍊肩洃鎺с€?,
          body: `
            <div class="archive-modal-grid" style="grid-template-columns: 1fr;">
              <div class="archive-card full"><div class="archive-card-head"><div class="archive-card-title">浠囨仺绱</div></div>${makeTileGrid([{ label: '绱鍑绘潃骞撮檺', value: makeInlineEditableValue(formatNumber(snapshot.forestKilledAge), { path: ['world', '绱鍑绘潃骞撮檺'], kind: 'number', rawValue: snapshot.forestKilledAge, editorMeta: { min: 0, integer: true, hint: '鏈€灏?0 路 鏁存暟 路 1000000 涓哄吔娼槇鍊煎弬鑰冿紝涓嶆槸纭笂闄? } }) }, { label: '鍏芥疆闃堝€?, value: '1000000' }, { label: '鍓╀綑瀹夊叏绌洪棿', value: remaining > 0 ? formatNumber(remaining) : '0' }, { label: '褰撳墠闃舵', value: stage }], 'two')}<div style="margin-top:12px;"><div style="display:flex;justify-content:space-between;font-size:12px;color:#bfdde4;margin-bottom:6px;"><span>闃堝€艰繘搴?/span><span>${progress}%</span></div><div style="height:10px;border-radius:999px;background:rgba(255,255,255,0.08);overflow:hidden;border:1px solid rgba(150,217,228,0.12);"><div style="height:100%;width:${progress}%;background:${progress >= 100 ? 'linear-gradient(90deg,#ff6b6b,#ffb36b)' : (progress >= 70 ? 'linear-gradient(90deg,#ffd36b,#ff8a5b)' : 'linear-gradient(90deg,#72e6ff,#7dffb2)')};box-shadow:0 0 12px rgba(255,180,107,0.35);"></div></div></div></div>
              <div class="archive-card full">
                <div class="archive-card-head"><div class="archive-card-title">闃舵璇存槑</div></div>
                ${makeTimelineStack([
                  { title: '0% - 29%', desc: '鐩稿瀹夊叏 / 妫灄灏氭湭杩涘叆楂樺帇鐘舵€併€? },
                  { title: '30% - 69%', desc: '鎸佺画鍗囨俯 / 宸茬粡寮€濮嬬Н绱槑鏄句粐鎭ㄣ€? },
                  { title: '70% - 99%', desc: '楂樺害绱у紶 / 鎺ヨ繎鎭愭€栭槇鍊硷紝搴旈伩鍏嶇户缁睜鎴€? },
                  { title: '100%', desc: '鍏芥疆涓寸晫 / 宸茶揪鍒版亹鎬栦簨浠惰Е鍙戠嚎銆? }
                ])}
              </div>
            </div>
          `
        };
      }

      return null;
    }

    async function refreshLiveSnapshot(options = {}) {
      let 鎱㈠埛鏂伴鏋惰鏃跺櫒 = 0;
      let 宸茶Е鍙戞參鍒锋柊楠ㄦ灦 = false;
      let 宸插啓鍥炵湡瀹炲崱鐗?= false;
      try {
        const refreshInlineEditToken = inlineEditSessionToken;
        if (!options.force) {
          鎱㈠埛鏂伴鏋惰鏃跺櫒 = window.setTimeout(() => {
            if (refreshInlineEditToken !== inlineEditSessionToken) return;
            宸茶Е鍙戞參鍒锋柊楠ㄦ灦 = true;
            璁剧疆鎱㈠埛鏂伴鏋剁姸鎬?true, { 鍒锋柊璇︽儏: true });
          }, 300);
        }
        if (shouldPauseLiveRefresh(options)) {
          pendingLiveRefresh = true;
          return;
        }
        const vars = options.sharedVars === undefined ? await getAllVariablesSafe() : options.sharedVars;
        if (!options.force && (refreshInlineEditToken !== inlineEditSessionToken || shouldPauseLiveRefresh(options))) {
          pendingLiveRefresh = true;
          return;
        }
        const root = resolveRootData(vars);
        if (!root) return;
        const effective = buildEffectiveSd(root);
        if (!effective.rootData) return;
        if (!options.force && (refreshInlineEditToken !== inlineEditSessionToken || shouldPauseLiveRefresh(options))) {
          pendingLiveRefresh = true;
          return;
        }
        if (isRootDataRelevantToCurrentChat(effective.rootData)) {
          syncMvuEditorStoreFromRoot(effective.rootData);
        }
        liveSnapshot = buildSnapshot(effective.rootData);
        lastRenderableSnapshot = liveSnapshot;
        const snapshotPlayerName = toText(deepGet(liveSnapshot, 'rootData.sys.鐜╁鍚?, ''), '').trim();
        const repairedPlayerName = resolveEffectivePlayerName(
          deepGet(liveSnapshot, 'rootData', {}),
          toText(liveSnapshot && liveSnapshot.activeName, '')
        );
        if (isInvalidRuntimePlayerName(snapshotPlayerName) && repairedPlayerName) {
          if (!liveSnapshot.rootData.sys || typeof liveSnapshot.rootData.sys !== 'object') liveSnapshot.rootData.sys = {};
          liveSnapshot.rootData.sys.鐜╁鍚?= repairedPlayerName;
          if (!effective.rootData.sys || typeof effective.rootData.sys !== 'object') effective.rootData.sys = {};
          effective.rootData.sys.鐜╁鍚?= repairedPlayerName;
          schedulePlayerNameRepair(liveSnapshot, repairedPlayerName);
        }
        schedulePlayerNameRepair(liveSnapshot);
        const nextHeaderRenderSignature = buildHeaderRenderSignature(liveSnapshot);
        const nextDashboardSectionRenderSignatures = buildDashboardSectionRenderSignatures(liveSnapshot);
        const nextDashboardRenderSignature = buildDashboardRenderSignature(liveSnapshot, nextDashboardSectionRenderSignatures);
        const shouldRenderHeader = !!options.force || nextHeaderRenderSignature !== lastHeaderRenderSignature;
        const shouldRenderDashboard = !!options.force || nextDashboardRenderSignature !== lastDashboardRenderSignature;
        const 闇€瑕佹覆鏌撲华琛ㄧ洏 = shouldRenderDashboard || 宸茶Е鍙戞參鍒锋柊楠ㄦ灦;
        if (宸茶Е鍙戞參鍒锋柊楠ㄦ灦) {
          璁剧疆鎱㈠埛鏂伴鏋剁姸鎬?false, { 鍒锋柊璇︽儏: false });
        }

        if (shouldRenderHeader) {
          renderHeader(liveSnapshot);
          lastHeaderRenderSignature = nextHeaderRenderSignature;
        }
        if (闇€瑕佹覆鏌撲华琛ㄧ洏) {
          renderLiveCards(liveSnapshot, nextDashboardSectionRenderSignatures);
          lastDashboardRenderSignature = nextDashboardRenderSignature;
          宸插啓鍥炵湡瀹炲崱鐗?= true;
        }
        syncPrivateArchiveLongPressTargets(liveSnapshot);
        maybeOpenTradeRequestFromAI(liveSnapshot);
        maybeAutoOpenPendingSoulRingPreview(liveSnapshot, options);
        
        syncBattleUiForSnapshot(liveSnapshot, options);

        const effectiveUnifiedPreviewKey = getEffectiveUnifiedPreviewKey();
        const activeDetailPreviewKey = effectiveUnifiedPreviewKey || currentModalPreviewKey;
        if ((闇€瑕佹覆鏌撲华琛ㄧ洏 || !!options.force) && (detailModal.classList.contains('show') || isShellInlinePreviewActive() || isUnifiedInlinePreviewActive()) && activeDetailPreviewKey) {
          if (activeDetailPreviewKey === '瑙掕壊鍒囨崲鍣?) {
            return;
          }
          if (activeDetailPreviewKey === BATTLE_INLINE_PREVIEW_KEY) {
            const battleData = deepGet(liveSnapshot, 'rootData.world.鎴樻枟', {});
            const battleStillActive = !!(battleData && typeof battleData === 'object' && battleData.杩涜涓?;
            if (!battleStillActive) {
              if (effectiveUnifiedPreviewKey === BATTLE_INLINE_PREVIEW_KEY) clearUnifiedInlinePreview();
              if (currentModalPreviewKey === BATTLE_INLINE_PREVIEW_KEY && typeof closeModal === 'function') closeModal();
            }
            return;
          }
          const liveSubUiKeys = new Set(['姝﹁宸ュ潑璇︾粏椤?, '鍌ㄧ墿浠撳簱璇︾粏椤?, '褰撳墠鑺傜偣璇︽儏', '浜ゆ槗妯″潡寮圭獥', '浜ゆ槗缃戠粶']);
          if (
            liveSubUiKeys.has(activeDetailPreviewKey)
            && activeSubUI
            && typeof activeSubUI.updateData === 'function'
          ) {
            activeSubUI.updateData(liveSnapshot);
          } else {
            rerenderDetailSurface(activeDetailPreviewKey, { surface: effectiveUnifiedPreviewKey ? 'unified' : 'modal', force: !!options.force });
          }
        }
      } catch (error) {
        console.warn('[DragonUI] MVU 瀹炴椂娓叉煋澶辫触', error);
      } finally {
        if (鎱㈠埛鏂伴鏋惰鏃跺櫒) window.clearTimeout(鎱㈠埛鏂伴鏋惰鏃跺櫒);
        if (鎱㈠埛鏂伴鏋跺凡鍚敤) {
          璁剧疆鎱㈠埛鏂伴鏋剁姸鎬?false, { 鍒锋柊璇︽儏: false });
          if (!宸插啓鍥炵湡瀹炲崱鐗?&& lastRenderableSnapshot) {
            const 鍥為€€鍖烘绛惧悕 = buildDashboardSectionRenderSignatures(lastRenderableSnapshot);
            renderLiveCards(lastRenderableSnapshot, 鍥為€€鍖烘绛惧悕);
          }
          const 褰撳墠璇︽儏棰勮閿?= toText(currentUnifiedPreviewKey || currentModalPreviewKey, '').trim();
          if (褰撳墠璇︽儏棰勮閿?&& (detailModal.classList.contains('show') || isShellInlinePreviewActive() || isUnifiedInlinePreviewActive())) {
            rerenderDetailSurface(褰撳墠璇︽儏棰勮閿? {
              surface: currentUnifiedPreviewKey ? 'unified' : 'modal',
              force: true,
            });
          }
        }
      }
    }

    async function initLiveBindings() {
      bindInlineEditing();
      await waitForMvuReady();
      installDirectModuleIntentGuard();
      await refreshLiveSnapshot();
      bindMvuUpdates(vars => refreshLiveSnapshot({ sharedVars: vars }));
    }

    window.__MVU_REFRESH_LIVE_SNAPSHOT__ = options => refreshLiveSnapshot(options);
    window.__MVU_GET_LIVE_SNAPSHOT__ = () => liveSnapshot || lastRenderableSnapshot || null;
    window.__MVU_OPEN_BATTLE_UI__ = async () => {
      const 妯″潡鍔犺浇缁撴灉 = await 纭繚妯″潡渚濊禆宸插姞杞?'鎴樻枟妯″潡', 'open_battle_ui');
      if (!妯″潡鍔犺浇缁撴灉 || 妯″潡鍔犺浇缁撴灉.ok === false) {
        showUiToast('鎴樻枟妯″潡鍔犺浇澶辫触锛屾殏鏃舵棤娉曞紑鍚垬鏂楃粓绔€?, 'error', 4200);
        return false;
      }
      battleInlineDismissed = false;
      removeBattleReturnEntries();
      await openBattleInlineSurface();
      await refreshLiveSnapshot({ force: true, reopenBattle: true });
      return !!activeBattleUI;
    };
    window.__MVU_APPLY_PATCHES__ = (patches, options = {}) => applyJsonPatchOpsByEditor(patches, options);

    function createSkillFixtureSystemBase(target = '鏁屾柟/鍗曚綋', skillSource = '鑷垱榄傛妧') {
      return {
        鏈哄埗: '绯荤粺鍩虹',
        鎶€鑳芥潵婧? skillSource,
        鎶€鑳界被鍨? skillSource,
        瀵硅薄: target,
        cast_time: 10,
        娑堣€? '鏃?,
      };
    }

    function 鏋勫缓鎶€鑳藉す鍏锋仮澶嶆弿杩板垪琛?鏍规暟鎹?= {}, 琛ヤ竵鍒楄〃 = []) {
      const 鎭㈠鏄犲皠 = new Map();
      (Array.isArray(琛ヤ竵鍒楄〃) ? 琛ヤ竵鍒楄〃 : []).forEach(patch => {
        const 鎸囬拡璺緞 = String(patch && patch.path || '').trim();
        if (!鎸囬拡璺緞 || 鎭㈠鏄犲皠.has(鎸囬拡璺緞)) return;
        const 瑙ｇ爜璺緞 = decodeJsonPointerPath(鎸囬拡璺緞);
        if (!瑙ｇ爜璺緞.length) return;
        const 鍘熷鍊?= deepGet(鏍规暟鎹? 瑙ｇ爜璺緞);
        鎭㈠鏄犲皠.set(鎸囬拡璺緞, {
          path: 鎸囬拡璺緞,
          depth: 瑙ｇ爜璺緞.length,
          existed: 鍘熷鍊?!== undefined,
          value: cloneJsonValue(鍘熷鍊? 鍘熷鍊?,
        });
      });
      return Array.from(鎭㈠鏄犲皠.values());
    }

    function 鏋勫缓鎶€鑳藉す鍏锋仮澶嶈ˉ涓?鎭㈠鎻忚堪鍒楄〃 = []) {
      return [...(Array.isArray(鎭㈠鎻忚堪鍒楄〃) ? 鎭㈠鎻忚堪鍒楄〃 : [])]
        .sort((left, right) => Number(right && right.depth || 0) - Number(left && left.depth || 0))
        .map(descriptor =>
          descriptor && descriptor.existed
            ? { op: 'replace', path: descriptor.path, value: cloneJsonValue(descriptor.value, descriptor.value) }
            : { op: 'remove', path: descriptor && descriptor.path || '' });
    }

    function 鏀堕泦鎶€鑳藉す鍏风姸鎬佸樊寮?鍓嶇姸鎬?= {}, 鍚庣姸鎬?= {}) {
      const 鍓嶇姸鎬侀敭 = Object.keys(鍓嶇姸鎬?&& typeof 鍓嶇姸鎬?=== 'object' ? 鍓嶇姸鎬?: {});
      const 鍚庣姸鎬侀敭 = Object.keys(鍚庣姸鎬?&& typeof 鍚庣姸鎬?=== 'object' ? 鍚庣姸鎬?: {});
      const 鍓嶇姸鎬侀泦鍚?= new Set(鍓嶇姸鎬侀敭);
      const 鍚庣姸鎬侀泦鍚?= new Set(鍚庣姸鎬侀敭);
      return {
        鏂板: 鍚庣姸鎬侀敭.filter(key => !鍓嶇姸鎬侀泦鍚?has(key)),
        绉婚櫎: 鍓嶇姸鎬侀敭.filter(key => !鍚庣姸鎬侀泦鍚?has(key)),
        淇濈暀: 鍚庣姸鎬侀敭.filter(key => 鍓嶇姸鎬侀泦鍚?has(key)),
      };
    }

    function 姹囨€绘妧鑳藉す鍏锋墽琛岀粨鏋?澶瑰叿閰嶇疆, 鎵ц缁撴灉, 鐜╁鍚?= '') {
      const 鍓嶅揩鐓?= 鎵ц缁撴灉 && 鎵ц缁撴灉.beforeSnapshot || {};
      const 鍚庡揩鐓?= 鎵ц缁撴灉 && 鎵ц缁撴灉.afterSnapshot || {};
      const 鏍囧噯鐜╁鍚?= toText(鐜╁鍚? '').trim();
      const 鏍囧噯鐩爣鍚?= toText(澶瑰叿閰嶇疆 && 澶瑰叿閰嶇疆.targetName, '').trim();
      const 璇诲彇瑙掕壊灞炴€?= (蹇収鏁版嵁, 瑙掕壊鍚? =>
        cloneJsonValue(deepGet(蹇収鏁版嵁, ['rootData', 'char', 瑙掕壊鍚? '灞炴€?], {}), {});
      const 鐜╁鍓嶅睘鎬?= 鏍囧噯鐜╁鍚?? 璇诲彇瑙掕壊灞炴€?鍓嶅揩鐓? 鏍囧噯鐜╁鍚? : {};
      const 鐜╁鍚庡睘鎬?= 鏍囧噯鐜╁鍚?? 璇诲彇瑙掕壊灞炴€?鍚庡揩鐓? 鏍囧噯鐜╁鍚? : {};
      const 鐩爣鍓嶅睘鎬?= 鏍囧噯鐩爣鍚?? 璇诲彇瑙掕壊灞炴€?鍓嶅揩鐓? 鏍囧噯鐩爣鍚? : {};
      const 鐩爣鍚庡睘鎬?= 鏍囧噯鐩爣鍚?? 璇诲彇瑙掕壊灞炴€?鍚庡揩鐓? 鏍囧噯鐩爣鍚? : {};
      const 鐜╁鐘舵€佸樊寮?= 鏀堕泦鎶€鑳藉す鍏风姸鎬佸樊寮?鐜╁鍓嶅睘鎬?鐘舵€佹晥鏋?|| {}, 鐜╁鍚庡睘鎬?鐘舵€佹晥鏋?|| {});
      const 鐩爣鐘舵€佸樊寮?= 鏀堕泦鎶€鑳藉す鍏风姸鎬佸樊寮?鐩爣鍓嶅睘鎬?鐘舵€佹晥鏋?|| {}, 鐩爣鍚庡睘鎬?鐘舵€佹晥鏋?|| {});
      return {
        鐜╁鍚? 鏍囧噯鐜╁鍚?
        鐩爣鍚? 鏍囧噯鐩爣鍚?
        鐜╁璧勬簮鍙樺寲: {
          鐢熷懡: Number(鐜╁鍚庡睘鎬?HP || 0) - Number(鐜╁鍓嶅睘鎬?HP || 0),
          浣撳姏: Number(鐜╁鍚庡睘鎬?浣撳姏 || 0) - Number(鐜╁鍓嶅睘鎬?浣撳姏 || 0),
          榄傚姏: Number(鐜╁鍚庡睘鎬?榄傚姏 || 0) - Number(鐜╁鍓嶅睘鎬?榄傚姏 || 0),
          绮剧鍔? Number(鐜╁鍚庡睘鎬?绮剧鍔?|| 0) - Number(鐜╁鍓嶅睘鎬?绮剧鍔?|| 0),
        },
        鐩爣璧勬簮鍙樺寲: {
          鐢熷懡: Number(鐩爣鍚庡睘鎬?HP || 0) - Number(鐩爣鍓嶅睘鎬?HP || 0),
          浣撳姏: Number(鐩爣鍚庡睘鎬?浣撳姏 || 0) - Number(鐩爣鍓嶅睘鎬?浣撳姏 || 0),
          榄傚姏: Number(鐩爣鍚庡睘鎬?榄傚姏 || 0) - Number(鐩爣鍓嶅睘鎬?榄傚姏 || 0),
          绮剧鍔? Number(鐩爣鍚庡睘鎬?绮剧鍔?|| 0) - Number(鐩爣鍓嶅睘鎬?绮剧鍔?|| 0),
        },
        鐜╁鐘舵€佸彉鍖? 鐜╁鐘舵€佸樊寮?
        鐩爣鐘舵€佸彉鍖? 鐩爣鐘舵€佸樊寮?
        琛ヤ竵鏁伴噺: Array.isArray(鎵ц缁撴灉 && 鎵ц缁撴灉.mvuUpdate && 鎵ц缁撴灉.mvuUpdate.patchOps)
          ? 鎵ц缁撴灉.mvuUpdate.patchOps.length
          : 0,
        鍔ㄤ綔鏉ユ簮: toText(鎵ц缁撴灉 && 鎵ц缁撴灉.action && 鎵ц缁撴灉.action.source, ''),
        鎻愪氦妯″紡: toText(鎵ц缁撴灉 && 鎵ц缁撴灉.result && 鎵ц缁撴灉.result.mode, ''),
      };
    }

    function 鏍￠獙鎶€鑳藉す鍏锋憳瑕?澶瑰叿閰嶇疆, 鎽樿) {
      const 棰勬湡 = 澶瑰叿閰嶇疆 && 澶瑰叿閰嶇疆.棰勬湡 && typeof 澶瑰叿閰嶇疆.棰勬湡 === 'object' ? 澶瑰叿閰嶇疆.棰勬湡 : {};
      const 妫€鏌ラ」 = [];
      const 杩藉姞妫€鏌?= (label, passed, detail) => {
        妫€鏌ラ」.push({ 椤圭洰: label, 閫氳繃: !!passed, 璇︽儏: detail });
      };
      const 鐩爣绉婚櫎鐘舵€?= Array.isArray(棰勬湡.鐩爣绉婚櫎鐘舵€? ? 棰勬湡.鐩爣绉婚櫎鐘舵€?: [];
      鐩爣绉婚櫎鐘舵€?forEach(stateName => {
        杩藉姞妫€鏌?
          `鐩爣绉婚櫎鐘舵€?${stateName}`,
          (鎽樿 && 鎽樿.鐩爣鐘舵€佸彉鍖?&& 鎽樿.鐩爣鐘舵€佸彉鍖?绉婚櫎 || []).includes(stateName),
          `绉婚櫎=${(鎽樿 && 鎽樿.鐩爣鐘舵€佸彉鍖?&& 鎽樿.鐩爣鐘舵€佸彉鍖?绉婚櫎 || []).join(' / ') || '鏃?}`,
        );
      });
      const 鐩爣鏂板鐘舵€?= Array.isArray(棰勬湡.鐩爣鏂板鐘舵€? ? 棰勬湡.鐩爣鏂板鐘舵€?: [];
      鐩爣鏂板鐘舵€?forEach(stateName => {
        杩藉姞妫€鏌?
          `鐩爣鏂板鐘舵€?${stateName}`,
          (鎽樿 && 鎽樿.鐩爣鐘舵€佸彉鍖?&& 鎽樿.鐩爣鐘舵€佸彉鍖?鏂板 || []).includes(stateName),
          `鏂板=${(鎽樿 && 鎽樿.鐩爣鐘舵€佸彉鍖?&& 鎽樿.鐩爣鐘舵€佸彉鍖?鏂板 || []).join(' / ') || '鏃?}`,
        );
      });
      const 鐜╁绉婚櫎鐘舵€?= Array.isArray(棰勬湡.鐜╁绉婚櫎鐘舵€? ? 棰勬湡.鐜╁绉婚櫎鐘舵€?: [];
      鐜╁绉婚櫎鐘舵€?forEach(stateName => {
        杩藉姞妫€鏌?
          `鐜╁绉婚櫎鐘舵€?${stateName}`,
          (鎽樿 && 鎽樿.鐜╁鐘舵€佸彉鍖?&& 鎽樿.鐜╁鐘舵€佸彉鍖?绉婚櫎 || []).includes(stateName),
          `绉婚櫎=${(鎽樿 && 鎽樿.鐜╁鐘舵€佸彉鍖?&& 鎽樿.鐜╁鐘舵€佸彉鍖?绉婚櫎 || []).join(' / ') || '鏃?}`,
        );
      });
      const 鐜╁鏂板鐘舵€?= Array.isArray(棰勬湡.鐜╁鏂板鐘舵€? ? 棰勬湡.鐜╁鏂板鐘舵€?: [];
      鐜╁鏂板鐘舵€?forEach(stateName => {
        杩藉姞妫€鏌?
          `鐜╁鏂板鐘舵€?${stateName}`,
          (鎽樿 && 鎽樿.鐜╁鐘舵€佸彉鍖?&& 鎽樿.鐜╁鐘舵€佸彉鍖?鏂板 || []).includes(stateName),
          `鏂板=${(鎽樿 && 鎽樿.鐜╁鐘舵€佸彉鍖?&& 鎽樿.鐜╁鐘舵€佸彉鍖?鏂板 || []).join(' / ') || '鏃?}`,
        );
      });
      const 鐜╁榄傚姏鑷冲皯鍙樺寲 = Number(棰勬湡.鐜╁榄傚姏鑷冲皯鍙樺寲);
      if (Number.isFinite(鐜╁榄傚姏鑷冲皯鍙樺寲)) {
        杩藉姞妫€鏌?
          '鐜╁榄傚姏鑷冲皯鍙樺寲',
          Number(鎽樿 && 鎽樿.鐜╁璧勬簮鍙樺寲 && 鎽樿.鐜╁璧勬簮鍙樺寲.榄傚姏 || 0) >= 鐜╁榄傚姏鑷冲皯鍙樺寲,
          `鏈熸湜>=${鐜╁榄傚姏鑷冲皯鍙樺寲} / 瀹為檯=${Number(鎽樿 && 鎽樿.鐜╁璧勬簮鍙樺寲 && 鎽樿.鐜╁璧勬簮鍙樺寲.榄傚姏 || 0)}`,
        );
      }
      const 鐜╁榄傚姏鑷冲鍙樺寲 = Number(棰勬湡.鐜╁榄傚姏鑷冲鍙樺寲);
      if (Number.isFinite(鐜╁榄傚姏鑷冲鍙樺寲)) {
        杩藉姞妫€鏌?
          '鐜╁榄傚姏鑷冲鍙樺寲',
          Number(鎽樿 && 鎽樿.鐜╁璧勬簮鍙樺寲 && 鎽樿.鐜╁璧勬簮鍙樺寲.榄傚姏 || 0) <= 鐜╁榄傚姏鑷冲鍙樺寲,
          `鏈熸湜<=${鐜╁榄傚姏鑷冲鍙樺寲} / 瀹為檯=${Number(鎽樿 && 鎽樿.鐜╁璧勬簮鍙樺寲 && 鎽樿.鐜╁璧勬簮鍙樺寲.榄傚姏 || 0)}`,
        );
      }
      const 鐜╁绮剧鍔涜嚦灏戝彉鍖?= Number(棰勬湡.鐜╁绮剧鍔涜嚦灏戝彉鍖?;
      if (Number.isFinite(鐜╁绮剧鍔涜嚦灏戝彉鍖?) {
        杩藉姞妫€鏌?
          '鐜╁绮剧鍔涜嚦灏戝彉鍖?,
          Number(鎽樿 && 鎽樿.鐜╁璧勬簮鍙樺寲 && 鎽樿.鐜╁璧勬簮鍙樺寲.绮剧鍔?|| 0) >= 鐜╁绮剧鍔涜嚦灏戝彉鍖?
          `鏈熸湜>=${鐜╁绮剧鍔涜嚦灏戝彉鍖杴 / 瀹為檯=${Number(鎽樿 && 鎽樿.鐜╁璧勬簮鍙樺寲 && 鎽樿.鐜╁璧勬簮鍙樺寲.绮剧鍔?|| 0)}`,
        );
      }
      const 鐜╁绮剧鍔涜嚦澶氬彉鍖?= Number(棰勬湡.鐜╁绮剧鍔涜嚦澶氬彉鍖?;
      if (Number.isFinite(鐜╁绮剧鍔涜嚦澶氬彉鍖?) {
        杩藉姞妫€鏌?
          '鐜╁绮剧鍔涜嚦澶氬彉鍖?,
          Number(鎽樿 && 鎽樿.鐜╁璧勬簮鍙樺寲 && 鎽樿.鐜╁璧勬簮鍙樺寲.绮剧鍔?|| 0) <= 鐜╁绮剧鍔涜嚦澶氬彉鍖?
          `鏈熸湜<=${鐜╁绮剧鍔涜嚦澶氬彉鍖杴 / 瀹為檯=${Number(鎽樿 && 鎽樿.鐜╁璧勬簮鍙樺寲 && 鎽樿.鐜╁璧勬簮鍙樺寲.绮剧鍔?|| 0)}`,
        );
      }
      const 鐩爣榄傚姏鑷冲鍙樺寲 = Number(棰勬湡.鐩爣榄傚姏鑷冲鍙樺寲);
      if (Number.isFinite(鐩爣榄傚姏鑷冲鍙樺寲)) {
        杩藉姞妫€鏌?
          '鐩爣榄傚姏鑷冲鍙樺寲',
          Number(鎽樿 && 鎽樿.鐩爣璧勬簮鍙樺寲 && 鎽樿.鐩爣璧勬簮鍙樺寲.榄傚姏 || 0) <= 鐩爣榄傚姏鑷冲鍙樺寲,
          `鏈熸湜<=${鐩爣榄傚姏鑷冲鍙樺寲} / 瀹為檯=${Number(鎽樿 && 鎽樿.鐩爣璧勬簮鍙樺寲 && 鎽樿.鐩爣璧勬簮鍙樺寲.榄傚姏 || 0)}`,
        );
      }
      const 鐩爣绮剧鍔涜嚦澶氬彉鍖?= Number(棰勬湡.鐩爣绮剧鍔涜嚦澶氬彉鍖?;
      if (Number.isFinite(鐩爣绮剧鍔涜嚦澶氬彉鍖?) {
        杩藉姞妫€鏌?
          '鐩爣绮剧鍔涜嚦澶氬彉鍖?,
          Number(鎽樿 && 鎽樿.鐩爣璧勬簮鍙樺寲 && 鎽樿.鐩爣璧勬簮鍙樺寲.绮剧鍔?|| 0) <= 鐩爣绮剧鍔涜嚦澶氬彉鍖?
          `鏈熸湜<=${鐩爣绮剧鍔涜嚦澶氬彉鍖杴 / 瀹為檯=${Number(鎽樿 && 鎽樿.鐩爣璧勬簮鍙樺寲 && 鎽樿.鐩爣璧勬簮鍙樺寲.绮剧鍔?|| 0)}`,
        );
      }
      const 鐩爣淇濈暀鐘舵€?= Array.isArray(棰勬湡.鐩爣淇濈暀鐘舵€? ? 棰勬湡.鐩爣淇濈暀鐘舵€?: [];
      鐩爣淇濈暀鐘舵€?forEach(stateName => {
        杩藉姞妫€鏌?
          `鐩爣淇濈暀鐘舵€?${stateName}`,
          (鎽樿 && 鎽樿.鐩爣鐘舵€佸彉鍖?&& 鎽樿.鐩爣鐘舵€佸彉鍖?淇濈暀 || []).includes(stateName),
          `淇濈暀=${(鎽樿 && 鎽樿.鐩爣鐘舵€佸彉鍖?&& 鎽樿.鐩爣鐘舵€佸彉鍖?淇濈暀 || []).join(' / ') || '鏃?}`,
        );
      });
      if (棰勬湡.瑕佹眰琛ヤ竵杈撳嚭 === true) {
        杩藉姞妫€鏌?
          '瑕佹眰琛ヤ竵杈撳嚭',
          Number(鎽樿 && 鎽樿.琛ヤ竵鏁伴噺 || 0) > 0,
          `琛ヤ竵鏁伴噺=${Number(鎽樿 && 鎽樿.琛ヤ竵鏁伴噺 || 0)}`,
        );
      }
      const 鐩爣浣撳姏鑷冲皯鍑忓皯 = Number(棰勬湡.鐩爣浣撳姏鑷冲皯鍑忓皯);
      if (Number.isFinite(鐩爣浣撳姏鑷冲皯鍑忓皯)) {
        const 瀹為檯鍑忓皯鍊?= Math.max(
          0,
          -Number(鎽樿 && 鎽樿.鐩爣璧勬簮鍙樺寲 && 鎽樿.鐩爣璧勬簮鍙樺寲.浣撳姏 || 0),
          -Number(鎽樿 && 鎽樿.鐩爣璧勬簮鍙樺寲 && 鎽樿.鐩爣璧勬簮鍙樺寲.鐢熷懡 || 0),
        );
        杩藉姞妫€鏌?
          '鐩爣浣撳姏鑷冲皯鍑忓皯',
          瀹為檯鍑忓皯鍊?>= 鐩爣浣撳姏鑷冲皯鍑忓皯,
          `鏈熸湜>=${鐩爣浣撳姏鑷冲皯鍑忓皯} / 瀹為檯=${瀹為檯鍑忓皯鍊紏`,
        );
      }
      return {
        閫氳繃: 妫€鏌ラ」.every(item => item.閫氳繃),
        妫€鏌ラ」,
      };
    }

    function createSkillFixtureLibrary() {
      return Object.freeze({
        鍚炲櫖: Object.freeze({
          skillName: '澶瑰叿_鍚炲櫖',
          targetName: '浜戝啣',
          battle: Object.freeze({
            杩涜涓? true,
            鎴樻枟绫诲瀷: '鎿傚彴鍒囩',
            鎴樻枟鎰忓浘: '鐐瑰埌涓烘',
            鍏堟敾: '鏃?,
            鍏佽鎾ょ: true,
            鍥炲悎: 0,
            鐜: '鍥哄畾澶瑰叿鍦?,
            瑁佹柇缁撴灉: '',
            鍙傛垬鑰? Object.freeze({
              player: Object.freeze({ name: '鏇瑰痉鏅? }),
              enemy: Object.freeze({ name: '浜戝啣' }),
              team_player: Object.freeze([]),
              team_enemy: Object.freeze([]),
            }),
          }),
          patchesBefore: Object.freeze([
            { op: 'replace', path: '/char/鏇瑰痉鏅?灞炴€?榄傚姏涓婇檺', value: 100 },
            { op: 'replace', path: '/char/鏇瑰痉鏅?灞炴€?榄傚姏', value: 0 },
            { op: 'replace', path: '/char/鏇瑰痉鏅?灞炴€?绮剧鍔涗笂闄?, value: 100 },
            { op: 'replace', path: '/char/鏇瑰痉鏅?灞炴€?绮剧鍔?, value: 0 },
            { op: 'replace', path: '/char/浜戝啣/灞炴€?榄傚姏涓婇檺', value: 100 },
            { op: 'replace', path: '/char/浜戝啣/灞炴€?榄傚姏', value: 50 },
            { op: 'replace', path: '/char/浜戝啣/灞炴€?绮剧鍔涗笂闄?, value: 100 },
            { op: 'replace', path: '/char/浜戝啣/灞炴€?绮剧鍔?, value: 50 },
          ]),
          skillData: Object.freeze({
            name: '澶瑰叿_鍚炲櫖',
            榄傛妧鍚? '澶瑰叿_鍚炲櫖',
            鎶€鑳芥潵婧? '鑷垱榄傛妧',
            鎶€鑳界被鍨? '鑷垱榄傛妧',
            瀵硅薄: '鏁屾柟鍗曚綋',
            娑堣€? '鏃?,
            _鏁堟灉鏁扮粍: Object.freeze([
              createSkillFixtureSystemBase('鏁屾柟/鍗曚綋'),
              Object.freeze({ 鏈哄埗: '鍚炲櫖', 鐩爣: '鏁屾柟鍗曚綋', 璧勬簮绫诲瀷: '鍙岃祫婧?, 澶哄彇姣斾緥: 0.18, 杞寲姣斾緥: 1 }),
            ]),
          }),
          棰勬湡: Object.freeze({
            鐜╁榄傚姏鑷冲皯鍙樺寲: 1,
            鐜╁绮剧鍔涜嚦灏戝彉鍖? 1,
            瑕佹眰琛ヤ竵杈撳嚭: true,
          }),
        }),
        鑳藉姏鍏变韩: Object.freeze({
          skillName: '澶瑰叿_鑳藉姏鍏变韩',
          targetName: '鏇瑰痉鏅?,
          battle: Object.freeze({
            杩涜涓? true,
            鎴樻枟绫诲瀷: '鎿傚彴鍒囩',
            鎴樻枟鎰忓浘: '鐐瑰埌涓烘',
            鍏堟敾: '鏃?,
            鍏佽鎾ょ: true,
            鍥炲悎: 0,
            鐜: '鍥哄畾澶瑰叿鍦?,
            瑁佹柇缁撴灉: '',
            鍙傛垬鑰? Object.freeze({
              player: Object.freeze({ name: '鏇瑰痉鏅? }),
              enemy: Object.freeze({ name: '浜戝啣' }),
              team_player: Object.freeze([]),
              team_enemy: Object.freeze([]),
            }),
          }),
          patchesBefore: Object.freeze([
            { op: 'replace', path: '/char/鏇瑰痉鏅?灞炴€?榄傚姏涓婇檺', value: 100 },
            { op: 'replace', path: '/char/鏇瑰痉鏅?灞炴€?榄傚姏', value: 0 },
            { op: 'replace', path: '/char/鏇瑰痉鏅?灞炴€?绮剧鍔涗笂闄?, value: 100 },
            { op: 'replace', path: '/char/鏇瑰痉鏅?灞炴€?绮剧鍔?, value: 0 },
          ]),
          skillData: Object.freeze({
            name: '澶瑰叿_鑳藉姏鍏变韩',
            榄傛妧鍚? '澶瑰叿_鑳藉姏鍏变韩',
            鎶€鑳芥潵婧? '鑷垱榄傛妧',
            鎶€鑳界被鍨? '鑷垱榄傛妧',
            瀵硅薄: '鍙嬫柟鍗曚綋',
            娑堣€? '鏃?,
            _鏁堟灉鏁扮粍: Object.freeze([
              createSkillFixtureSystemBase('宸辨柟/鍗曚綋'),
              Object.freeze({ 鏈哄埗: '鑳藉姏鍏变韩', 鐩爣: '鍙嬫柟鍗曚綋', 璧勬簮绫诲瀷: '鍙岃祫婧?, 鍙嶇亴姣斾緥: 0.2 }),
            ]),
          }),
          棰勬湡: Object.freeze({
            鐜╁榄傚姏鑷冲皯鍙樺寲: 1,
            鐜╁绮剧鍔涜嚦灏戝彉鍖? 1,
            瑕佹眰琛ヤ竵杈撳嚭: true,
          }),
        }),
        瀵硅薄宸紓_椋熺墿鑷湇绂佺敤: Object.freeze({
          skillName: '澶瑰叿_瀵硅薄宸紓_椋熺墿鑷湇绂佺敤',
          targetName: '鏇瑰痉鏅?,
          battle: Object.freeze({
            杩涜涓? true,
            鎴樻枟绫诲瀷: '鎿傚彴鍒囩',
            鎴樻枟鎰忓浘: '鐐瑰埌涓烘',
            鍏堟敾: '鏃?,
            鍏佽鎾ょ: true,
            鍥炲悎: 0,
            鐜: '鍥哄畾澶瑰叿鍦?,
            瑁佹柇缁撴灉: '',
            鍙傛垬鑰? Object.freeze({
              player: Object.freeze({ name: '鏇瑰痉鏅? }),
              enemy: Object.freeze({ name: '浜戝啣' }),
              team_player: Object.freeze([]),
              team_enemy: Object.freeze([]),
            }),
          }),
          patchesBefore: Object.freeze([
            { op: 'replace', path: '/char/鏇瑰痉鏅?灞炴€?榄傚姏涓婇檺', value: 100 },
            { op: 'replace', path: '/char/鏇瑰痉鏅?灞炴€?榄傚姏', value: 0 },
            { op: 'replace', path: '/char/鏇瑰痉鏅?灞炴€?绮剧鍔涗笂闄?, value: 100 },
            { op: 'replace', path: '/char/鏇瑰痉鏅?灞炴€?绮剧鍔?, value: 0 },
          ]),
          skillData: Object.freeze({
            name: '澶瑰叿_瀵硅薄宸紓_椋熺墿鑷湇绂佺敤',
            榄傛妧鍚? '澶瑰叿_瀵硅薄宸紓_椋熺墿鑷湇绂佺敤',
            鎶€鑳芥潵婧? '鑷垱榄傛妧',
            鎶€鑳界被鍨? '鑷垱榄傛妧',
            瀵硅薄: '鍙嬫柟鍗曚綋',
            娑堣€? '鏃?,
            _鏁堟灉鏁扮粍: Object.freeze([
              createSkillFixtureSystemBase('宸辨柟/鍗曚綋'),
              Object.freeze({
                鏈哄埗: '鑳藉姏鍏变韩',
                鐩爣: '鍙嬫柟鍗曚綋',
                璧勬簮绫诲瀷: '鍙岃祫婧?,
                鍙嶇亴姣斾緥: 0.22,
                瀵硅薄宸紓瑙勫垯: Object.freeze([
                  Object.freeze({ 鏉′欢: '鑷韩', 澶勭悊: '绂佺敤' }),
                ]),
              }),
            ]),
          }),
          棰勬湡: Object.freeze({
            鐜╁榄傚姏鑷冲鍙樺寲: 0,
            鐜╁绮剧鍔涜嚦澶氬彉鍖? 0,
            瑕佹眰琛ヤ竵杈撳嚭: true,
          }),
        }),
        瀵硅薄宸紓_绁炲湥閫嗛偑: Object.freeze({
          skillName: '澶瑰叿_瀵硅薄宸紓_绁炲湥閫嗛偑',
          targetName: '浜戝啣',
          battle: Object.freeze({
            杩涜涓? true,
            鎴樻枟绫诲瀷: '鎿傚彴鍒囩',
            鎴樻枟鎰忓浘: '鐐瑰埌涓烘',
            鍏堟敾: '鏃?,
            鍏佽鎾ょ: true,
            鍥炲悎: 0,
            鐜: '鍥哄畾澶瑰叿鍦?,
            瑁佹柇缁撴灉: '',
            鍙傛垬鑰? Object.freeze({
              player: Object.freeze({ name: '鏇瑰痉鏅? }),
              enemy: Object.freeze({ name: '浜戝啣' }),
              team_player: Object.freeze([]),
              team_enemy: Object.freeze([]),
            }),
          }),
          patchesBefore: Object.freeze([
            { op: 'replace', path: '/char/浜戝啣/灞炴€?浣撳姏涓婇檺', value: 220 },
            { op: 'replace', path: '/char/浜戝啣/灞炴€?浣撳姏', value: 220 },
            { op: 'replace', path: '/char/浜戝啣/灞炴€?閭瓊甯?, value: true },
          ]),
          skillData: Object.freeze({
            name: '澶瑰叿_瀵硅薄宸紓_绁炲湥閫嗛偑',
            榄傛妧鍚? '澶瑰叿_瀵硅薄宸紓_绁炲湥閫嗛偑',
            鎶€鑳芥潵婧? '鑷垱榄傛妧',
            鎶€鑳界被鍨? '鑷垱榄傛妧',
            瀵硅薄: '鏁屾柟鍗曚綋',
            娑堣€? '鏃?,
            _鏁堟灉鏁扮粍: Object.freeze([
              createSkillFixtureSystemBase('鏁屾柟/鍗曚綋'),
              Object.freeze({
                鏈哄埗: '灞炴€у彉鍖?,
                鐩爣: '鏁屾柟鍗曚綋',
                灞炴€? 'vit',
                鍔ㄤ綔: '鍔犲€?,
                鏁板€? 0.26,
                鎸佺画: 0,
                瑙﹀彂: '绔嬪嵆鐢熸晥',
                瀵硅薄宸紓瑙勫垯: Object.freeze([
                  Object.freeze({ 鏉′欢: '閭瓊甯?, 澶勭悊: '杞负浼ゅ', 鍙傛暟: Object.freeze({ 浼ゅ绫诲瀷: '绁炲湥', 浼ゅ鍊嶇巼: 1 }) }),
                ]),
              }),
            ]),
          }),
          棰勬湡: Object.freeze({
            鐩爣浣撳姏鑷冲皯鍑忓皯: 1,
            瑕佹眰琛ヤ竵杈撳嚭: true,
          }),
        }),
        鏈哄埗鎶规秷_澶嶈嫃: Object.freeze({
          skillName: '澶瑰叿_鏈哄埗鎶规秷_澶嶈嫃',
          targetName: '浜戝啣',
          battle: Object.freeze({
            杩涜涓? true,
            鎴樻枟绫诲瀷: '鎿傚彴鍒囩',
            鎴樻枟鎰忓浘: '鐐瑰埌涓烘',
            鍏堟敾: '鏃?,
            鍏佽鎾ょ: true,
            鍥炲悎: 0,
            鐜: '鍥哄畾澶瑰叿鍦?,
            瑁佹柇缁撴灉: '',
            鍙傛垬鑰? Object.freeze({
              player: Object.freeze({ name: '鏇瑰痉鏅? }),
              enemy: Object.freeze({ name: '浜戝啣' }),
              team_player: Object.freeze([]),
              team_enemy: Object.freeze([]),
            }),
          }),
          patchesBefore: Object.freeze([
            {
              op: 'replace',
              path: '/char/浜戝啣/灞炴€?鐘舵€佹晥鏋?,
              value: {
                澶嶈嫃鎶ょ: {
                  绫诲瀷: 'buff',
                  灞傛暟: 1,
                  鎻忚堪: '澶瑰叿澶嶈嫃',
                  duration: 3,
                  闈㈡澘淇敼姣斾緥: { str: 1, def: 1, agi: 1, sp_max: 1 },
                  鎴樻枟鏁堟灉: { revive_count: 1, revive_heal_ratio: 0.25 },
                },
              },
            },
          ]),
          skillData: Object.freeze({
            name: '澶瑰叿_鏈哄埗鎶规秷_澶嶈嫃',
            榄傛妧鍚? '澶瑰叿_鏈哄埗鎶规秷_澶嶈嫃',
            鎶€鑳芥潵婧? '鑷垱榄傛妧',
            鎶€鑳界被鍨? '鑷垱榄傛妧',
            瀵硅薄: '鏁屾柟鍗曚綋',
            娑堣€? '鏃?,
            _鏁堟灉鏁扮粍: Object.freeze([
              createSkillFixtureSystemBase('鏁屾柟/鍗曚綋'),
              Object.freeze({ 鏈哄埗: '鏈哄埗鎶规秷', 鐩爣: '鏁屾柟鍗曚綋', 鎶规秷鐩爣: '澶嶈嫃', 鎶规秷鏂瑰紡: '绉婚櫎骞跺皝閿?, 鎸佺画鍥炲悎: 2 }),
            ]),
          }),
          棰勬湡: Object.freeze({
            鐩爣绉婚櫎鐘舵€? Object.freeze(['澶嶈嫃鎶ょ']),
            鐩爣鏂板鐘舵€? Object.freeze(['鏈哄埗鎶规秷']),
            瑕佹眰琛ヤ竵杈撳嚭: true,
          }),
        }),
        鏈哄埗鎶规秷_鍥炲鏈哄埗: Object.freeze({
          skillName: '澶瑰叿_鑳藉姏鍏变韩',
          targetName: '鏇瑰痉鏅?,
          battle: Object.freeze({
            杩涜涓? true,
            鎴樻枟绫诲瀷: '鎿傚彴鍒囩',
            鎴樻枟鎰忓浘: '鐐瑰埌涓烘',
            鍏堟敾: '鏃?,
            鍏佽鎾ょ: true,
            鍥炲悎: 0,
            鐜: '鍥哄畾澶瑰叿鍦?,
            瑁佹柇缁撴灉: '',
            鍙傛垬鑰? Object.freeze({
              player: Object.freeze({ name: '鏇瑰痉鏅? }),
              enemy: Object.freeze({ name: '浜戝啣' }),
              team_player: Object.freeze([]),
              team_enemy: Object.freeze([]),
            }),
          }),
          patchesBefore: Object.freeze([
            { op: 'replace', path: '/char/鏇瑰痉鏅?灞炴€?榄傚姏涓婇檺', value: 100 },
            { op: 'replace', path: '/char/鏇瑰痉鏅?灞炴€?榄傚姏', value: 0 },
            { op: 'replace', path: '/char/鏇瑰痉鏅?灞炴€?绮剧鍔涗笂闄?, value: 100 },
            { op: 'replace', path: '/char/鏇瑰痉鏅?灞炴€?绮剧鍔?, value: 0 },
            {
              op: 'replace',
              path: '/char/鏇瑰痉鏅?灞炴€?鐘舵€佹晥鏋?,
              value: {
                鍥炲灏侀攣: {
                  绫诲瀷: 'debuff',
                  灞傛暟: 1,
                  鎻忚堪: '澶瑰叿鍥炲鎶规秷',
                  duration: 2,
                  鏈哄埗鎶规秷鐩爣: ['鍥炲鏈哄埗'],
                  鏈哄埗鎶规秷鏂瑰紡: '浠呭皝閿佸悗缁?,
                  闈㈡澘淇敼姣斾緥: { str: 1, def: 1, agi: 1, sp_max: 1 },
                  鎴樻枟鏁堟灉: {},
                },
              },
            },
          ]),
          skillData: Object.freeze({
            name: '澶瑰叿_鑳藉姏鍏变韩',
            榄傛妧鍚? '澶瑰叿_鑳藉姏鍏变韩',
            鎶€鑳芥潵婧? '鑷垱榄傛妧',
            鎶€鑳界被鍨? '鑷垱榄傛妧',
            瀵硅薄: '鍙嬫柟鍗曚綋',
            娑堣€? '鏃?,
            _鏁堟灉鏁扮粍: Object.freeze([
              createSkillFixtureSystemBase('宸辨柟/鍗曚綋'),
              Object.freeze({ 鏈哄埗: '鑳藉姏鍏变韩', 鐩爣: '鍙嬫柟鍗曚綋', 璧勬簮绫诲瀷: '鍙岃祫婧?, 鍙嶇亴姣斾緥: 0.2 }),
            ]),
          }),
          棰勬湡: Object.freeze({
            鐜╁榄傚姏鑷冲鍙樺寲: 5,
            鐜╁绮剧鍔涜嚦澶氬彉鍖? 5,
            瑕佹眰琛ヤ竵杈撳嚭: true,
          }),
        }),
        鏈哄埗鎶规秷_鎶ょ浘: Object.freeze({
          skillName: '澶瑰叿_鏈哄埗鎶规秷_鎶ょ浘',
          targetName: '浜戝啣',
          battle: Object.freeze({
            杩涜涓? true,
            鎴樻枟绫诲瀷: '鎿傚彴鍒囩',
            鎴樻枟鎰忓浘: '鐐瑰埌涓烘',
            鍏堟敾: '鏃?,
            鍏佽鎾ょ: true,
            鍥炲悎: 0,
            鐜: '鍥哄畾澶瑰叿鍦?,
            瑁佹柇缁撴灉: '',
            鍙傛垬鑰? Object.freeze({
              player: Object.freeze({ name: '鏇瑰痉鏅? }),
              enemy: Object.freeze({ name: '浜戝啣' }),
              team_player: Object.freeze([]),
              team_enemy: Object.freeze([]),
            }),
          }),
          patchesBefore: Object.freeze([
            {
              op: 'replace',
              path: '/char/浜戝啣/灞炴€?鐘舵€佹晥鏋?,
              value: {
                鍦ｅ厜鎶ょ浘: {
                  绫诲瀷: 'buff',
                  灞傛暟: 1,
                  鎻忚堪: '澶瑰叿鎶ょ浘',
                  duration: 3,
                  shield_value: 180,
                  闈㈡澘淇敼姣斾緥: { str: 1, def: 1, agi: 1, sp_max: 1 },
                  鎴樻枟鏁堟灉: { shield_gain_bonus: 0 },
                },
              },
            },
          ]),
          skillData: Object.freeze({
            name: '澶瑰叿_鏈哄埗鎶规秷_鎶ょ浘',
            榄傛妧鍚? '澶瑰叿_鏈哄埗鎶规秷_鎶ょ浘',
            鎶€鑳芥潵婧? '鑷垱榄傛妧',
            鎶€鑳界被鍨? '鑷垱榄傛妧',
            瀵硅薄: '鏁屾柟鍗曚綋',
            娑堣€? '鏃?,
            _鏁堟灉鏁扮粍: Object.freeze([
              createSkillFixtureSystemBase('鏁屾柟/鍗曚綋'),
              Object.freeze({ 鏈哄埗: '鏈哄埗鎶规秷', 鐩爣: '鏁屾柟鍗曚綋', 鎶规秷鐩爣: '鎶ょ浘', 鎶规秷鏂瑰紡: '绉婚櫎骞跺皝閿?, 鎸佺画鍥炲悎: 2 }),
            ]),
          }),
          棰勬湡: Object.freeze({
            鐩爣绉婚櫎鐘舵€? Object.freeze(['鍦ｅ厜鎶ょ浘']),
            鐩爣鏂板鐘舵€? Object.freeze(['鏈哄埗鎶规秷']),
            瑕佹眰琛ヤ竵杈撳嚭: true,
          }),
        }),
        鏈哄埗鎶规秷_闅愯韩: Object.freeze({
          skillName: '澶瑰叿_鏈哄埗鎶规秷_闅愯韩',
          targetName: '浜戝啣',
          battle: Object.freeze({
            杩涜涓? true,
            鎴樻枟绫诲瀷: '鎿傚彴鍒囩',
            鎴樻枟鎰忓浘: '鐐瑰埌涓烘',
            鍏堟敾: '鏃?,
            鍏佽鎾ょ: true,
            鍥炲悎: 0,
            鐜: '鍥哄畾澶瑰叿鍦?,
            瑁佹柇缁撴灉: '',
            鍙傛垬鑰? Object.freeze({
              player: Object.freeze({ name: '鏇瑰痉鏅? }),
              enemy: Object.freeze({ name: '浜戝啣' }),
              team_player: Object.freeze([]),
              team_enemy: Object.freeze([]),
            }),
          }),
          patchesBefore: Object.freeze([
            {
              op: 'replace',
              path: '/char/浜戝啣/灞炴€?鐘舵€佹晥鏋?,
              value: {
                娼滃奖闅愯韩: {
                  绫诲瀷: 'buff',
                  灞傛暟: 1,
                  鎻忚堪: '澶瑰叿闅愯韩',
                  duration: 3,
                  闈㈡澘淇敼姣斾緥: { str: 1, def: 1, agi: 1, sp_max: 1 },
                  鎴樻枟鏁堟灉: { stealth_level: 80, dodge_bonus: 0.1 },
                },
              },
            },
          ]),
          skillData: Object.freeze({
            name: '澶瑰叿_鏈哄埗鎶规秷_闅愯韩',
            榄傛妧鍚? '澶瑰叿_鏈哄埗鎶规秷_闅愯韩',
            鎶€鑳芥潵婧? '鑷垱榄傛妧',
            鎶€鑳界被鍨? '鑷垱榄傛妧',
            瀵硅薄: '鏁屾柟鍗曚綋',
            娑堣€? '鏃?,
            _鏁堟灉鏁扮粍: Object.freeze([
              createSkillFixtureSystemBase('鏁屾柟/鍗曚綋'),
              Object.freeze({ 鏈哄埗: '鏈哄埗鎶规秷', 鐩爣: '鏁屾柟鍗曚綋', 鎶规秷鐩爣: '闅愯韩', 鎶规秷鏂瑰紡: '绉婚櫎骞跺皝閿?, 鎸佺画鍥炲悎: 2 }),
            ]),
          }),
          棰勬湡: Object.freeze({
            鐩爣绉婚櫎鐘舵€? Object.freeze(['娼滃奖闅愯韩']),
            鐩爣鏂板鐘舵€? Object.freeze(['鏈哄埗鎶规秷']),
            瑕佹眰琛ヤ竵杈撳嚭: true,
          }),
        }),
        鐘舵€佽浆绉? Object.freeze({
          skillName: '澶瑰叿_鐘舵€佽浆绉?,
          targetName: '浜戝啣',
          battle: Object.freeze({
            杩涜涓? true,
            鎴樻枟绫诲瀷: '鎿傚彴鍒囩',
            鎴樻枟鎰忓浘: '鐐瑰埌涓烘',
            鍏堟敾: '鏃?,
            鍏佽鎾ょ: true,
            鍥炲悎: 0,
            鐜: '鍥哄畾澶瑰叿鍦?,
            瑁佹柇缁撴灉: '',
            鍙傛垬鑰? Object.freeze({
              player: Object.freeze({ name: '鏇瑰痉鏅? }),
              enemy: Object.freeze({ name: '浜戝啣' }),
              team_player: Object.freeze([]),
              team_enemy: Object.freeze([]),
            }),
          }),
          patchesBefore: Object.freeze([
            {
              op: 'replace',
              path: '/char/鏇瑰痉鏅?灞炴€?鐘舵€佹晥鏋?,
              value: {
                杩熺紦璇呭拻: {
                  绫诲瀷: 'debuff',
                  灞傛暟: 1,
                  鎻忚堪: '澶瑰叿璐熼潰鐘舵€?,
                  duration: 2,
                  闈㈡澘淇敼姣斾緥: { str: 1, def: 1, agi: 0.85, sp_max: 1 },
                  鎴樻枟鏁堟灉: { dodge_penalty: 0.18, cast_speed_penalty: 0.12 },
                },
              },
            },
          ]),
          skillData: Object.freeze({
            name: '澶瑰叿_鐘舵€佽浆绉?,
            榄傛妧鍚? '澶瑰叿_鐘舵€佽浆绉?,
            鎶€鑳芥潵婧? '鑷垱榄傛妧',
            鎶€鑳界被鍨? '鑷垱榄傛妧',
            瀵硅薄: '鏁屾柟鍗曚綋',
            娑堣€? '鏃?,
            _鏁堟灉鏁扮粍: Object.freeze([
              createSkillFixtureSystemBase('鏁屾柟/鍗曚綋'),
              Object.freeze({ 鏈哄埗: '鐘舵€佽浆绉?, 鐩爣: '鏁屾柟鍗曚綋', 杞Щ绫诲瀷: '鑷韩璐熼潰->鐩爣', 杞Щ鏁伴噺: 1, 瑙﹀彂鏉′欢: '鍛戒腑鍚? }),
            ]),
          }),
          棰勬湡: Object.freeze({
            鐜╁绉婚櫎鐘舵€? Object.freeze(['杩熺紦璇呭拻']),
            鐩爣鏂板鐘舵€? Object.freeze(['杩熺紦璇呭拻']),
            瑕佹眰琛ヤ竵杈撳嚭: true,
          }),
        }),
        寮曠垎鎸佺画浼ゅ: Object.freeze({
          skillName: '澶瑰叿_寮曠垎鎸佺画浼ゅ',
          targetName: '浜戝啣',
          battle: Object.freeze({
            杩涜涓? true,
            鎴樻枟绫诲瀷: '鎿傚彴鍒囩',
            鎴樻枟鎰忓浘: '鐐瑰埌涓烘',
            鍏堟敾: '鏃?,
            鍏佽鎾ょ: true,
            鍥炲悎: 0,
            鐜: '鍥哄畾澶瑰叿鍦?,
            瑁佹柇缁撴灉: '',
            鍙傛垬鑰? Object.freeze({
              player: Object.freeze({ name: '鏇瑰痉鏅? }),
              enemy: Object.freeze({ name: '浜戝啣' }),
              team_player: Object.freeze([]),
              team_enemy: Object.freeze([]),
            }),
          }),
          patchesBefore: Object.freeze([
            { op: 'replace', path: '/char/浜戝啣/灞炴€?浣撳姏涓婇檺', value: 200 },
            { op: 'replace', path: '/char/浜戝啣/灞炴€?浣撳姏', value: 200 },
            {
              op: 'replace',
              path: '/char/浜戝啣/灞炴€?鐘舵€佹晥鏋?,
              value: {
                鐏瘨鐏肩儳: {
                  绫诲瀷: 'debuff',
                  灞傛暟: 1,
                  鎻忚堪: '澶瑰叿鎸佺画浼ゅ',
                  duration: 3,
                  闈㈡澘淇敼姣斾緥: { str: 1, def: 1, agi: 1, sp_max: 1 },
                  鎴樻枟鏁堟灉: { dot_damage: 36 },
                },
              },
            },
          ]),
          skillData: Object.freeze({
            name: '澶瑰叿_寮曠垎鎸佺画浼ゅ',
            榄傛妧鍚? '澶瑰叿_寮曠垎鎸佺画浼ゅ',
            鎶€鑳芥潵婧? '鑷垱榄傛妧',
            鎶€鑳界被鍨? '鑷垱榄傛妧',
            瀵硅薄: '鏁屾柟鍗曚綋',
            娑堣€? '鏃?,
            _鏁堟灉鏁扮粍: Object.freeze([
              createSkillFixtureSystemBase('鏁屾柟/鍗曚綋'),
              Object.freeze({ 鏈哄埗: '寮曠垎鎸佺画浼ゅ', 鐩爣: '鏁屾柟鍗曚綋', 寮曠垎鍊嶇巼: 2, 娑堣€楁寔缁激瀹? true }),
            ]),
          }),
          棰勬湡: Object.freeze({
            鐩爣绉婚櫎鐘舵€? Object.freeze(['鐏瘨鐏肩儳']),
            鐩爣浣撳姏鑷冲皯鍑忓皯: 1,
            瑕佹眰琛ヤ竵杈撳嚭: true,
          }),
        }),
        鏂╃浘: Object.freeze({
          skillName: '澶瑰叿_鏂╃浘',
          targetName: '浜戝啣',
          battle: Object.freeze({
            杩涜涓? true,
            鎴樻枟绫诲瀷: '鎿傚彴鍒囩',
            鎴樻枟鎰忓浘: '鐐瑰埌涓烘',
            鍏堟敾: '鏃?,
            鍏佽鎾ょ: true,
            鍥炲悎: 0,
            鐜: '鍥哄畾澶瑰叿鍦?,
            瑁佹柇缁撴灉: '',
            鍙傛垬鑰? Object.freeze({
              player: Object.freeze({ name: '鏇瑰痉鏅? }),
              enemy: Object.freeze({ name: '浜戝啣' }),
              team_player: Object.freeze([]),
              team_enemy: Object.freeze([]),
            }),
          }),
          patchesBefore: Object.freeze([
            { op: 'replace', path: '/char/浜戝啣/灞炴€?浣撳姏涓婇檺', value: 200 },
            { op: 'replace', path: '/char/浜戝啣/灞炴€?浣撳姏', value: 200 },
            {
              op: 'replace',
              path: '/char/浜戝啣/灞炴€?鐘舵€佹晥鏋?,
              value: {
                鐏靛鎶ょ浘: {
                  绫诲瀷: 'buff',
                  灞傛暟: 1,
                  鎻忚堪: '澶瑰叿鎶ょ浘灞?,
                  duration: 2,
                  shield_value: 160,
                  闈㈡澘淇敼姣斾緥: { str: 1, def: 1, agi: 1, sp_max: 1 },
                  鎴樻枟鏁堟灉: {},
                },
              },
            },
          ]),
          skillData: Object.freeze({
            name: '澶瑰叿_鏂╃浘',
            榄傛妧鍚? '澶瑰叿_鏂╃浘',
            鎶€鑳芥潵婧? '鑷垱榄傛妧',
            鎶€鑳界被鍨? '鑷垱榄傛妧',
            瀵硅薄: '鏁屾柟鍗曚綋',
            娑堣€? '鏃?,
            _鏁堟灉鏁扮粍: Object.freeze([
              createSkillFixtureSystemBase('鏁屾柟/鍗曚綋'),
              Object.freeze({ 鏈哄埗: '鏂╃浘', 鐩爣: '鏁屾柟鍗曚綋', 鏂╃浘鍊嶇巼: 1 }),
            ]),
          }),
          棰勬湡: Object.freeze({
            鐩爣绉婚櫎鐘舵€? Object.freeze(['鐏靛鎶ょ浘']),
            鐩爣浣撳姏鑷冲皯鍑忓皯: 1,
            瑕佹眰琛ヤ竵杈撳嚭: true,
          }),
        }),
        绐冨彇鎶ょ浘: Object.freeze({
          skillName: '澶瑰叿_绐冨彇鎶ょ浘',
          targetName: '浜戝啣',
          battle: Object.freeze({
            杩涜涓? true,
            鎴樻枟绫诲瀷: '鎿傚彴鍒囩',
            鎴樻枟鎰忓浘: '鐐瑰埌涓烘',
            鍏堟敾: '鏃?,
            鍏佽鎾ょ: true,
            鍥炲悎: 0,
            鐜: '鍥哄畾澶瑰叿鍦?,
            瑁佹柇缁撴灉: '',
            鍙傛垬鑰? Object.freeze({
              player: Object.freeze({ name: '鏇瑰痉鏅? }),
              enemy: Object.freeze({ name: '浜戝啣' }),
              team_player: Object.freeze([]),
              team_enemy: Object.freeze([]),
            }),
          }),
          patchesBefore: Object.freeze([
            {
              op: 'replace',
              path: '/char/浜戝啣/灞炴€?鐘舵€佹晥鏋?,
              value: {
                鐜勭浘澹佸瀿: {
                  绫诲瀷: 'buff',
                  灞傛暟: 1,
                  鎻忚堪: '澶瑰叿鍙獌鎶ょ浘',
                  duration: 2,
                  shield_value: 120,
                  闈㈡澘淇敼姣斾緥: { str: 1, def: 1, agi: 1, sp_max: 1 },
                  鎴樻枟鏁堟灉: {},
                },
              },
            },
          ]),
          skillData: Object.freeze({
            name: '澶瑰叿_绐冨彇鎶ょ浘',
            榄傛妧鍚? '澶瑰叿_绐冨彇鎶ょ浘',
            鎶€鑳芥潵婧? '鑷垱榄傛妧',
            鎶€鑳界被鍨? '鑷垱榄傛妧',
            瀵硅薄: '鏁屾柟鍗曚綋',
            娑堣€? '鏃?,
            _鏁堟灉鏁扮粍: Object.freeze([
              createSkillFixtureSystemBase('鏁屾柟/鍗曚綋'),
              Object.freeze({ 鏈哄埗: '绐冨彇鎶ょ浘', 鐩爣: '鏁屾柟鍗曚綋', 绐冪浘姣斾緥: 1, 鎸佺画鍥炲悎: 2 }),
            ]),
          }),
          棰勬湡: Object.freeze({
            鐩爣绉婚櫎鐘舵€? Object.freeze(['鐜勭浘澹佸瀿']),
            鐜╁鏂板鐘舵€? Object.freeze(['澶瑰叿_绐冨彇鎶ょ浘绐冪浘']),
            瑕佹眰琛ヤ竵杈撳嚭: true,
          }),
        }),
        浼ゅ閾? Object.freeze({
          skillName: '澶瑰叿_浼ゅ閾?,
          targetName: '浜戝啣',
          battle: Object.freeze({
            杩涜涓? true,
            鎴樻枟绫诲瀷: '鎿傚彴鍒囩',
            鎴樻枟鎰忓浘: '鐐瑰埌涓烘',
            鍏堟敾: '鏃?,
            鍏佽鎾ょ: true,
            鍥炲悎: 0,
            鐜: '鍥哄畾澶瑰叿鍦?,
            瑁佹柇缁撴灉: '',
            鍙傛垬鑰? Object.freeze({
              player: Object.freeze({ name: '鏇瑰痉鏅? }),
              enemy: Object.freeze({ name: '浜戝啣' }),
              team_player: Object.freeze([]),
              team_enemy: Object.freeze([]),
            }),
          }),
          skillData: Object.freeze({
            name: '澶瑰叿_浼ゅ閾?,
            榄傛妧鍚? '澶瑰叿_浼ゅ閾?,
            鎶€鑳芥潵婧? '鑷垱榄傛妧',
            鎶€鑳界被鍨? '鑷垱榄傛妧',
            瀵硅薄: '鏁屾柟鍗曚綋',
            娑堣€? '鏃?,
            _鏁堟灉鏁扮粍: Object.freeze([
              createSkillFixtureSystemBase('鏁屾柟/鍗曚綋'),
              Object.freeze({ 鏈哄埗: '浼ゅ閾?, 鐩爣: '鏁屾柟鍗曚綋', 閾惧紡姣斾緥: 0.5, 閾惧紡鐩爣鏁? 1, 鎸佺画鍥炲悎: 2 }),
            ]),
          }),
          棰勬湡: Object.freeze({
            鐩爣浣撳姏鑷冲皯鍑忓皯: 1,
            瑕佹眰琛ヤ竵杈撳嚭: true,
          }),
        }),
        鐢熷懡閾炬帴: Object.freeze({
          skillName: '澶瑰叿_鐢熷懡閾炬帴',
          targetName: '浜戝啣',
          battle: Object.freeze({
            杩涜涓? true,
            鎴樻枟绫诲瀷: '鎿傚彴鍒囩',
            鎴樻枟鎰忓浘: '鐐瑰埌涓烘',
            鍏堟敾: '鏃?,
            鍏佽鎾ょ: true,
            鍥炲悎: 0,
            鐜: '鍥哄畾澶瑰叿鍦?,
            瑁佹柇缁撴灉: '',
            鍙傛垬鑰? Object.freeze({
              player: Object.freeze({ name: '鏇瑰痉鏅? }),
              enemy: Object.freeze({ name: '浜戝啣' }),
              team_player: Object.freeze([]),
              team_enemy: Object.freeze([]),
            }),
          }),
          skillData: Object.freeze({
            name: '澶瑰叿_鐢熷懡閾炬帴',
            榄傛妧鍚? '澶瑰叿_鐢熷懡閾炬帴',
            鎶€鑳芥潵婧? '鑷垱榄傛妧',
            鎶€鑳界被鍨? '鑷垱榄傛妧',
            瀵硅薄: '鏁屾柟鍗曚綋',
            娑堣€? '鏃?,
            _鏁堟灉鏁扮粍: Object.freeze([
              createSkillFixtureSystemBase('鏁屾柟/鍗曚綋'),
              Object.freeze({ 鏈哄埗: '鐢熷懡閾炬帴', 鐩爣: '鏁屾柟鍗曚綋', 鍒嗘憡姣斾緥: 0.35, 鎸佺画鍥炲悎: 2 }),
            ]),
          }),
          棰勬湡: Object.freeze({
            鐩爣鏂板鐘舵€? Object.freeze(['鐢熷懡閾炬帴']),
            瑕佹眰琛ヤ竵杈撳嚭: true,
          }),
        }),
        寤堕暱鎸佺画浼ゅ: Object.freeze({
          skillName: '澶瑰叿_寤堕暱鎸佺画浼ゅ',
          targetName: '浜戝啣',
          battle: Object.freeze({
            杩涜涓? true,
            鎴樻枟绫诲瀷: '鎿傚彴鍒囩',
            鎴樻枟鎰忓浘: '鐐瑰埌涓烘',
            鍏堟敾: '鏃?,
            鍏佽鎾ょ: true,
            鍥炲悎: 0,
            鐜: '鍥哄畾澶瑰叿鍦?,
            瑁佹柇缁撴灉: '',
            鍙傛垬鑰? Object.freeze({
              player: Object.freeze({ name: '鏇瑰痉鏅? }),
              enemy: Object.freeze({ name: '浜戝啣' }),
              team_player: Object.freeze([]),
              team_enemy: Object.freeze([]),
            }),
          }),
          patchesBefore: Object.freeze([
            {
              op: 'replace',
              path: '/char/浜戝啣/灞炴€?鐘舵€佹晥鏋?,
              value: {
                鐏瘨鐏肩儳: {
                  绫诲瀷: 'debuff',
                  灞傛暟: 1,
                  鎻忚堪: '澶瑰叿鎸佺画浼ゅ',
                  duration: 2,
                  闈㈡澘淇敼姣斾緥: { str: 1, def: 1, agi: 1, sp_max: 1 },
                  鎴樻枟鏁堟灉: { dot_damage: 22 },
                },
              },
            },
          ]),
          skillData: Object.freeze({
            name: '澶瑰叿_寤堕暱鎸佺画浼ゅ',
            榄傛妧鍚? '澶瑰叿_寤堕暱鎸佺画浼ゅ',
            鎶€鑳芥潵婧? '鑷垱榄傛妧',
            鎶€鑳界被鍨? '鑷垱榄傛妧',
            瀵硅薄: '鏁屾柟鍗曚綋',
            娑堣€? '鏃?,
            _鏁堟灉鏁扮粍: Object.freeze([
              createSkillFixtureSystemBase('鏁屾柟/鍗曚綋'),
              Object.freeze({ 鏈哄埗: '寤堕暱鎸佺画浼ゅ', 鐩爣: '鏁屾柟鍗曚綋', 寤堕暱鍥炲悎: 2, 鎸佺画鍥炲悎: 2 }),
            ]),
          }),
          棰勬湡: Object.freeze({
            鐩爣淇濈暀鐘舵€? Object.freeze(['鐏瘨鐏肩儳']),
            瑕佹眰琛ヤ竵杈撳嚭: true,
          }),
        }),
        鍘嬬缉鎸佺画浼ゅ: Object.freeze({
          skillName: '澶瑰叿_鍘嬬缉鎸佺画浼ゅ',
          targetName: '浜戝啣',
          battle: Object.freeze({
            杩涜涓? true,
            鎴樻枟绫诲瀷: '鎿傚彴鍒囩',
            鎴樻枟鎰忓浘: '鐐瑰埌涓烘',
            鍏堟敾: '鏃?,
            鍏佽鎾ょ: true,
            鍥炲悎: 0,
            鐜: '鍥哄畾澶瑰叿鍦?,
            瑁佹柇缁撴灉: '',
            鍙傛垬鑰? Object.freeze({
              player: Object.freeze({ name: '鏇瑰痉鏅? }),
              enemy: Object.freeze({ name: '浜戝啣' }),
              team_player: Object.freeze([]),
              team_enemy: Object.freeze([]),
            }),
          }),
          patchesBefore: Object.freeze([
            {
              op: 'replace',
              path: '/char/浜戝啣/灞炴€?鐘舵€佹晥鏋?,
              value: {
                鐏瘨鐏肩儳: {
                  绫诲瀷: 'debuff',
                  灞傛暟: 1,
                  鎻忚堪: '澶瑰叿鎸佺画浼ゅ',
                  duration: 3,
                  闈㈡澘淇敼姣斾緥: { str: 1, def: 1, agi: 1, sp_max: 1 },
                  鎴樻枟鏁堟灉: { dot_damage: 24 },
                },
              },
            },
          ]),
          skillData: Object.freeze({
            name: '澶瑰叿_鍘嬬缉鎸佺画浼ゅ',
            榄傛妧鍚? '澶瑰叿_鍘嬬缉鎸佺画浼ゅ',
            鎶€鑳芥潵婧? '鑷垱榄傛妧',
            鎶€鑳界被鍨? '鑷垱榄傛妧',
            瀵硅薄: '鏁屾柟鍗曚綋',
            娑堣€? '鏃?,
            _鏁堟灉鏁扮粍: Object.freeze([
              createSkillFixtureSystemBase('鏁屾柟/鍗曚綋'),
              Object.freeze({ 鏈哄埗: '鍘嬬缉鎸佺画浼ゅ', 鐩爣: '鏁屾柟鍗曚綋', 鍘嬬缉鍊嶇巼: 1.3, 鍘嬬缉鍥炲悎: 1, 鎸佺画鍥炲悎: 1 }),
            ]),
          }),
          棰勬湡: Object.freeze({
            鐩爣浣撳姏鑷冲皯鍑忓皯: 1,
            瑕佹眰琛ヤ竵杈撳嚭: true,
          }),
        }),
        鎷嗗眰杞瓨: Object.freeze({
          skillName: '澶瑰叿_鎷嗗眰杞瓨',
          targetName: '浜戝啣',
          battle: Object.freeze({
            杩涜涓? true,
            鎴樻枟绫诲瀷: '鎿傚彴鍒囩',
            鎴樻枟鎰忓浘: '鐐瑰埌涓烘',
            鍏堟敾: '鏃?,
            鍏佽鎾ょ: true,
            鍥炲悎: 0,
            鐜: '鍥哄畾澶瑰叿鍦?,
            瑁佹柇缁撴灉: '',
            鍙傛垬鑰? Object.freeze({
              player: Object.freeze({ name: '鏇瑰痉鏅? }),
              enemy: Object.freeze({ name: '浜戝啣' }),
              team_player: Object.freeze([]),
              team_enemy: Object.freeze([]),
            }),
          }),
          patchesBefore: Object.freeze([
            {
              op: 'replace',
              path: '/char/浜戝啣/灞炴€?鐘舵€佹晥鏋?,
              value: {
                鐏瘨鐏肩儳: {
                  绫诲瀷: 'debuff',
                  灞傛暟: 2,
                  鎻忚堪: '澶瑰叿鎸佺画浼ゅ',
                  duration: 3,
                  闈㈡澘淇敼姣斾緥: { str: 1, def: 1, agi: 1, sp_max: 1 },
                  鎴樻枟鏁堟灉: { dot_damage: 26 },
                },
              },
            },
          ]),
          skillData: Object.freeze({
            name: '澶瑰叿_鎷嗗眰杞瓨',
            榄傛妧鍚? '澶瑰叿_鎷嗗眰杞瓨',
            鎶€鑳芥潵婧? '鑷垱榄傛妧',
            鎶€鑳界被鍨? '鑷垱榄傛妧',
            瀵硅薄: '鏁屾柟鍗曚綋',
            娑堣€? '鏃?,
            _鏁堟灉鏁扮粍: Object.freeze([
              createSkillFixtureSystemBase('鏁屾柟/鍗曚綋'),
              Object.freeze({ 鏈哄埗: '鎷嗗眰杞瓨', 鐩爣: '鏁屾柟鍗曚綋', 鎷嗗眰鏁伴噺: 1, 澶嶅埗绫诲瀷: 'debuff', 鎸佺画鍥炲悎: 2 }),
            ]),
          }),
          棰勬湡: Object.freeze({
            鐜╁鏂板鐘舵€? Object.freeze(['鐏瘨鐏肩儳路澶嶅埗']),
            瑕佹眰琛ヤ竵杈撳嚭: true,
          }),
        }),
        璧勬簮鐕冪儳: Object.freeze({
          skillName: '澶瑰叿_璧勬簮鐕冪儳',
          targetName: '浜戝啣',
          battle: Object.freeze({
            杩涜涓? true,
            鎴樻枟绫诲瀷: '鎿傚彴鍒囩',
            鎴樻枟鎰忓浘: '鐐瑰埌涓烘',
            鍏堟敾: '鏃?,
            鍏佽鎾ょ: true,
            鍥炲悎: 0,
            鐜: '鍥哄畾澶瑰叿鍦?,
            瑁佹柇缁撴灉: '',
            鍙傛垬鑰? Object.freeze({
              player: Object.freeze({ name: '鏇瑰痉鏅? }),
              enemy: Object.freeze({ name: '浜戝啣' }),
              team_player: Object.freeze([]),
              team_enemy: Object.freeze([]),
            }),
          }),
          patchesBefore: Object.freeze([
            { op: 'replace', path: '/char/浜戝啣/灞炴€?榄傚姏涓婇檺', value: 100 },
            { op: 'replace', path: '/char/浜戝啣/灞炴€?榄傚姏', value: 80 },
            { op: 'replace', path: '/char/浜戝啣/灞炴€?绮剧鍔涗笂闄?, value: 100 },
            { op: 'replace', path: '/char/浜戝啣/灞炴€?绮剧鍔?, value: 80 },
          ]),
          skillData: Object.freeze({
            name: '澶瑰叿_璧勬簮鐕冪儳',
            榄傛妧鍚? '澶瑰叿_璧勬簮鐕冪儳',
            鎶€鑳芥潵婧? '鑷垱榄傛妧',
            鎶€鑳界被鍨? '鑷垱榄傛妧',
            瀵硅薄: '鏁屾柟鍗曚綋',
            娑堣€? '鏃?,
            _鏁堟灉鏁扮粍: Object.freeze([
              createSkillFixtureSystemBase('鏁屾柟/鍗曚綋'),
              Object.freeze({ 鏈哄埗: '璧勬簮鐕冪儳', 鐩爣: '鏁屾柟鍗曚綋', 璧勬簮绫诲瀷: '鍙岃祫婧?, 姣忓洖鍚堢噧鐑? 0.2, 鎸佺画鍥炲悎: 2 }),
            ]),
          }),
          棰勬湡: Object.freeze({
            鐩爣榄傚姏鑷冲鍙樺寲: -1,
            鐩爣绮剧鍔涜嚦澶氬彉鍖? -1,
            瑕佹眰琛ヤ竵杈撳嚭: true,
          }),
        }),
        璧勬簮閿佸畾: Object.freeze({
          skillName: '澶瑰叿_璧勬簮閿佸畾',
          targetName: '浜戝啣',
          battle: Object.freeze({
            杩涜涓? true,
            鎴樻枟绫诲瀷: '鎿傚彴鍒囩',
            鎴樻枟鎰忓浘: '鐐瑰埌涓烘',
            鍏堟敾: '鏃?,
            鍏佽鎾ょ: true,
            鍥炲悎: 0,
            鐜: '鍥哄畾澶瑰叿鍦?,
            瑁佹柇缁撴灉: '',
            鍙傛垬鑰? Object.freeze({
              player: Object.freeze({ name: '鏇瑰痉鏅? }),
              enemy: Object.freeze({ name: '浜戝啣' }),
              team_player: Object.freeze([]),
              team_enemy: Object.freeze([]),
            }),
          }),
          skillData: Object.freeze({
            name: '澶瑰叿_璧勬簮閿佸畾',
            榄傛妧鍚? '澶瑰叿_璧勬簮閿佸畾',
            鎶€鑳芥潵婧? '鑷垱榄傛妧',
            鎶€鑳界被鍨? '鑷垱榄傛妧',
            瀵硅薄: '鏁屾柟鍗曚綋',
            娑堣€? '鏃?,
            _鏁堟灉鏁扮粍: Object.freeze([
              createSkillFixtureSystemBase('鏁屾柟/鍗曚綋'),
              Object.freeze({ 鏈哄埗: '璧勬簮閿佸畾', 鐩爣: '鏁屾柟鍗曚綋', 閿佸畾姣斾緥: 0.45, 鎸佺画鍥炲悎: 2 }),
            ]),
          }),
          棰勬湡: Object.freeze({
            鐩爣鏂板鐘舵€? Object.freeze(['璧勬簮閿佸畾']),
            瑕佹眰琛ヤ竵杈撳嚭: true,
          }),
        }),
        鍙敜涓庡満鍦? Object.freeze({
          skillName: '澶瑰叿_鍙敜涓庡満鍦?,
          targetName: '浜戝啣',
          battle: Object.freeze({
            杩涜涓? true,
            鎴樻枟绫诲瀷: '鎿傚彴鍒囩',
            鎴樻枟鎰忓浘: '鐐瑰埌涓烘',
            鍏堟敾: '鏃?,
            鍏佽鎾ょ: true,
            鍥炲悎: 0,
            鐜: '鍥哄畾澶瑰叿鍦?,
            瑁佹柇缁撴灉: '',
            鍙傛垬鑰? Object.freeze({
              player: Object.freeze({ name: '鏇瑰痉鏅? }),
              enemy: Object.freeze({ name: '浜戝啣' }),
              team_player: Object.freeze([]),
              team_enemy: Object.freeze([]),
            }),
          }),
          skillData: Object.freeze({
            name: '澶瑰叿_鍙敜涓庡満鍦?,
            榄傛妧鍚? '澶瑰叿_鍙敜涓庡満鍦?,
            鎶€鑳芥潵婧? '姝﹂瓊铻嶅悎鎶€',
            鎶€鑳界被鍨? '姝﹂瓊铻嶅悎鎶€',
            瀵硅薄: '鏁屾柟鍗曚綋',
            娑堣€? '鏃?,
            _鏁堟灉鏁扮粍: Object.freeze([
              createSkillFixtureSystemBase('鏁屾柟/鍗曚綋', '姝﹂瓊铻嶅悎鎶€'),
              Object.freeze({ 鏈哄埗: '鍙敜涓庡満鍦?, 鐩爣: '鏁屾柟鍗曚綋', 瀹炰綋鍚嶇О: '鍡滆棰嗗煙', 鎸佺画鍥炲悎: 3, 缁ф壙灞炴€ф瘮渚? 0.35, 鏍稿績鏈哄埗鎻忚堪: '鍘嬪埗鏁屾柟鎭㈠鑺傚' }),
            ]),
          }),
          棰勬湡: Object.freeze({
            鐩爣鏂板鐘舵€? Object.freeze(['鍡滆棰嗗煙']),
            瑕佹眰琛ヤ竵杈撳嚭: true,
          }),
        }),
      });
    }

    function buildSkillCoverageMatrix() {
      return Object.freeze({
        宸查獙璇佸す鍏? Object.freeze([
          { 绫荤洰: '璧勬簮', 鏈哄埗: Object.freeze(['鍚炲櫖', '鑳藉姏鍏变韩']), 鐘舵€? '宸查獙璇? },
          { 绫荤洰: '瀵硅薄宸紓', 鏈哄埗: Object.freeze(['瀵硅薄宸紓_椋熺墿鑷湇绂佺敤', '瀵硅薄宸紓_绁炲湥閫嗛偑']), 鐘舵€? '宸查獙璇? },
          { 绫荤洰: '閫氱敤鍙嶅埗', 鏈哄埗: Object.freeze(['鏈哄埗鎶规秷']), 鐘舵€? '宸查獙璇? },
          { 绫荤洰: '鐘舵€佸崥寮?, 鏈哄埗: Object.freeze(['鐘舵€佽浆绉?]), 鐘舵€? '宸查獙璇? },
          { 绫荤洰: '鎸佺画浼ゅ', 鏈哄埗: Object.freeze(['寮曠垎鎸佺画浼ゅ']), 鐘舵€? '宸查獙璇? },
          { 绫荤洰: '鎶ょ浘鍗氬紙', 鏈哄埗: Object.freeze(['鏂╃浘', '绐冨彇鎶ょ浘']), 鐘舵€? '宸查獙璇? },
          { 绫荤洰: '閾炬帴', 鏈哄埗: Object.freeze(['浼ゅ閾?, '鐢熷懡閾炬帴']), 鐘舵€? '宸查獙璇? },
          { 绫荤洰: '鎸佺画灞傛暟鎿嶆帶', 鏈哄埗: Object.freeze(['寤堕暱鎸佺画浼ゅ', '鍘嬬缉鎸佺画浼ゅ', '鎷嗗眰杞瓨']), 鐘舵€? '宸查獙璇? },
          { 绫荤洰: '璧勬簮瑙勫垯', 鏈哄埗: Object.freeze(['璧勬簮鐕冪儳', '璧勬簮閿佸畾']), 鐘舵€? '宸查獙璇? },
          { 绫荤洰: '鍦哄湴', 鏈哄埗: Object.freeze(['鍙敜涓庡満鍦?]), 鐘舵€? '宸查獙璇? },
        ]),
        褰撳墠缂哄彛: Object.freeze([]),
      });
    }

    window.__LWCS_SKILL_FIXTURE_LIBRARY__ = createSkillFixtureLibrary();
    window.__LWCS_SKILL_COVERAGE_MATRIX__ = buildSkillCoverageMatrix();
    window.__LWCS_GET_SKILL_COVERAGE_MATRIX__ = () => window.__LWCS_SKILL_COVERAGE_MATRIX__;
    window.__LWCS_LIST_SKILL_FIXTURES__ = () => Object.keys(window.__LWCS_SKILL_FIXTURE_LIBRARY__ || {});
    window.__LWCS_RUN_SKILL_FIXTURE__ = async function runSkillFixture(fixtureKeyOrConfig, options = {}) {
      const fixtureLibrary = window.__LWCS_SKILL_FIXTURE_LIBRARY__ || {};
      const fixture =
        typeof fixtureKeyOrConfig === 'string'
          ? fixtureLibrary[fixtureKeyOrConfig]
          : fixtureKeyOrConfig && typeof fixtureKeyOrConfig === 'object'
            ? fixtureKeyOrConfig
            : null;
      if (!fixture || typeof fixture !== 'object') throw new Error('skill_fixture_missing');

      await refreshLiveSnapshot({ force: true });
      const snapshot = liveSnapshot || lastRenderableSnapshot;
      if (!snapshot?.rootData) throw new Error('skill_fixture_snapshot_missing');
      syncMvuEditorStoreFromRoot(snapshot.rootData, { force: true });
      const playerName = toText(options.playerName, toText(snapshot.activeName, '鏇瑰痉鏅?)) || '鏇瑰痉鏅?;
      const fixtureTargetName = toText(fixture.targetName, '').trim();
      const battleData = cloneJsonValue(fixture.battle, {});
      if (battleData?.鍙傛垬鑰?.player) battleData.鍙傛垬鑰?player.name = playerName;

      const patches = [
        { op: 'add', path: `/char/${playerName}/鑷垱榄傛妧`, value: { [fixture.skillName || fixture.skillData?.name || '澶瑰叿鎶€鑳?]: cloneJsonValue(fixture.skillData, {}) } },
        ...(Array.isArray(fixture.patchesBefore) ? fixture.patchesBefore : []),
        { op: 'replace', path: '/world/鎴樻枟', value: battleData },
      ];
      const 鎭㈠鍩虹嚎璺緞琛ヤ竵 = [
        { op: 'replace', path: `/char/${playerName}/灞炴€ },
        { op: 'replace', path: `/char/${playerName}/鍐崇瓥璁板繂` },
        { op: 'replace', path: '/world/鎴樻枟' },
      ];
      if (fixtureTargetName) {
        鎭㈠鍩虹嚎璺緞琛ヤ竵.push({ op: 'replace', path: `/char/${fixtureTargetName}/灞炴€ });
        鎭㈠鍩虹嚎璺緞琛ヤ竵.push({ op: 'replace', path: `/char/${fixtureTargetName}/鍐崇瓥璁板繂` });
      }
      const 鎭㈠鎻忚堪鍒楄〃 = 鏋勫缓鎶€鑳藉す鍏锋仮澶嶆弿杩板垪琛?snapshot.rootData, [...patches, ...鎭㈠鍩虹嚎璺緞琛ヤ竵]);

      const originalSendToAI = window.sendToAI;
      try {
        if (typeof window.__MVU_CLOSE_DETAIL_MODAL__ === 'function') {
          try { window.__MVU_CLOSE_DETAIL_MODAL__(); } catch (error) {}
        }
        await applyJsonPatchOpsByEditor(patches, { force: true });
        await refreshLiveSnapshot({ force: true });
        await window.__MVU_OPEN_BATTLE_UI__();
        await new Promise(resolve => setTimeout(resolve, Number(options.waitMs || 220)));
        const actionName = String(fixture.skillName || fixture.skillData?.name || '').trim();
        const action = (window.BattleUI?.state?.availableActions || []).find(item => String(item?.name || '').trim() === actionName) || null;
        if (!action) throw new Error('skill_fixture_action_missing');
        if (fixtureTargetName) action.target_name = fixtureTargetName;

        window.sendToAI = () => ({ blocked: true, offline: true });
        const beforeSnapshot = cloneJsonValue(window.__MVU_GET_LIVE_SNAPSHOT__?.(), {});
        window.BattleUI.state.selectedAction = action;
        window.BattleUI.state.selectedSkillActions = [action];
        const result = window.BattleUI.submitBattleIntent();
        await new Promise(resolve => setTimeout(resolve, Number(options.settleWaitMs || 320)));
        const afterSnapshot = cloneJsonValue(window.__MVU_GET_LIVE_SNAPSHOT__?.(), {});
        const mvuUpdate = window.BattleUIBridge?.getLastMvuUpdateRequest?.() || null;
        const 鎽樿 = 姹囨€绘妧鑳藉す鍏锋墽琛岀粨鏋?fixture, { beforeSnapshot, afterSnapshot, action, result, mvuUpdate }, playerName);
        const 鏍￠獙 = 鏍￠獙鎶€鑳藉す鍏锋憳瑕?fixture, 鎽樿);
        return {
          key: typeof fixtureKeyOrConfig === 'string' ? fixtureKeyOrConfig : '',
          action: { id: action.id, name: action.name, category: action.category, source: action.source_detail || action.source || '' },
          result,
          beforeSnapshot,
          afterSnapshot,
          mvuUpdate,
          summary: 鎽樿,
          validation: 鏍￠獙,
        };
      } finally {
        window.sendToAI = originalSendToAI;
        await applyJsonPatchOpsByEditor(鏋勫缓鎶€鑳藉す鍏锋仮澶嶈ˉ涓?鎭㈠鎻忚堪鍒楄〃), { force: true });
        await refreshLiveSnapshot({ force: true });
        if (liveSnapshot?.rootData) syncMvuEditorStoreFromRoot(liveSnapshot.rootData, { force: true });
        if (typeof window.__MVU_CLOSE_DETAIL_MODAL__ === 'function') {
          try { window.__MVU_CLOSE_DETAIL_MODAL__(); } catch (error) {}
        }
      }
    };
    window.__LWCS_RUN_SKILL_FIXTURE_BATCH__ = async function runSkillFixtureBatch(澶瑰叿閿垪琛?= [], options = {}) {
      const fixtureLibrary = window.__LWCS_SKILL_FIXTURE_LIBRARY__ || {};
      const 寰呰繍琛岄敭鍒楄〃 = Array.isArray(澶瑰叿閿垪琛? && 澶瑰叿閿垪琛?length ? 澶瑰叿閿垪琛?: Object.keys(fixtureLibrary);
      const 缁撴灉椤瑰垪琛?= [];
      const 鏈€澶ч噸璇曟鏁?= Math.max(0, Number(options?.retryCount ?? 1));
      for (const fixtureKey of 寰呰繍琛岄敭鍒楄〃) {
        let 鏈€浣崇粨鏋?= null;
        let 鏈€浣抽敊璇?= '';
        for (let attemptIndex = 0; attemptIndex <= 鏈€澶ч噸璇曟鏁? attemptIndex += 1) {
          try {
            await refreshLiveSnapshot({ force: true });
            const 鎵ц缁撴灉 = await window.__LWCS_RUN_SKILL_FIXTURE__(fixtureKey, options);
            const 褰撳墠缁撴灉 = {
              key: fixtureKey,
              ok: true,
              validation: 鎵ц缁撴灉.validation,
              summary: 鎵ц缁撴灉.summary,
              attempts: attemptIndex + 1,
            };
            鏈€浣崇粨鏋?= 褰撳墠缁撴灉;
            if (褰撳墠缁撴灉.validation && 褰撳墠缁撴灉.validation.閫氳繃) break;
          } catch (error) {
            鏈€浣抽敊璇?= String(error && error.message || error || 'unknown_fixture_error');
            鏈€浣崇粨鏋?= {
              key: fixtureKey,
              ok: false,
              error: 鏈€浣抽敊璇?
              attempts: attemptIndex + 1,
            };
          }
          await new Promise(resolve => setTimeout(resolve, Math.max(0, Number(options?.retryWaitMs || 160))));
        }
        if (鏈€浣崇粨鏋? 缁撴灉椤瑰垪琛?push(鏈€浣崇粨鏋?;
        else
          缁撴灉椤瑰垪琛?push({
            key: fixtureKey,
            ok: false,
            error: 鏈€浣抽敊璇?|| 'unknown_fixture_error',
            attempts: 鏈€澶ч噸璇曟鏁?+ 1,
          });
        await new Promise(resolve => setTimeout(resolve, Math.max(0, Number(options?.betweenWaitMs || 120))));
      }
      const 閫氳繃鏁伴噺 = 缁撴灉椤瑰垪琛?filter(item => item.ok && item.validation && item.validation.閫氳繃).length;
      return {
        total: 缁撴灉椤瑰垪琛?length,
        passed: 閫氳繃鏁伴噺,
        failed: 缁撴灉椤瑰垪琛?length - 閫氳繃鏁伴噺,
        items: 缁撴灉椤瑰垪琛?
      };
    };
    window.__LWCS_RUN_SKILL_FULL_CHECK__ = async function runSkillFullCheck(options = {}) {
      const 鐢熸垚渚у嚱鏁?=
        typeof window.__LWCS_RUN_GENERATION_RULE_CHECKS__ === 'function'
          ? window.__LWCS_RUN_GENERATION_RULE_CHECKS__
          : typeof __mvuBridgeRoot.__LWCS_RUN_GENERATION_RULE_CHECKS__ === 'function'
            ? __mvuBridgeRoot.__LWCS_RUN_GENERATION_RULE_CHECKS__
            : null;
      const 鐢熸垚渚х粨鏋?= 鐢熸垚渚у嚱鏁?        ? 鐢熸垚渚у嚱鏁?{
            鍝佺骇: String(options?.鍝佺骇 || 'A').trim() || 'A',
            榄傜幆浣? Math.max(1, Number(options?.榄傜幆浣?|| 5)),
          })
        : { 閿欒: 'generation_check_unavailable' };
      const 鎴樻枟澶瑰叿閿垪琛?=
        Array.isArray(options?.鎴樻枟澶瑰叿閿垪琛? && options.鎴樻枟澶瑰叿閿垪琛?length
          ? options.鎴樻枟澶瑰叿閿垪琛?          : ['鍚炲櫖', '鑳藉姏鍏变韩', '瀵硅薄宸紓_椋熺墿鑷湇绂佺敤', '瀵硅薄宸紓_绁炲湥閫嗛偑', '鏈哄埗鎶规秷_澶嶈嫃', '鏈哄埗鎶规秷_鍥炲鏈哄埗', '鏈哄埗鎶规秷_鎶ょ浘', '鏈哄埗鎶规秷_闅愯韩', '鐘舵€佽浆绉?, '寮曠垎鎸佺画浼ゅ', '鏂╃浘', '绐冨彇鎶ょ浘'];
      const 鎴樻枟渚х粨鏋?= await window.__LWCS_RUN_SKILL_FIXTURE_BATCH__(鎴樻枟澶瑰叿閿垪琛? {
        waitMs: Number(options?.waitMs || 260),
        settleWaitMs: Number(options?.settleWaitMs || 420),
      });
      return {
        鐢熸垚渚? 鐢熸垚渚х粨鏋?
        鎴樻枟渚? 鎴樻枟渚х粨鏋?
        瑕嗙洊鐭╅樀: window.__LWCS_SKILL_COVERAGE_MATRIX__ || null,
        姹囨€? {
          鐢熸垚鐙崰閫氳繃鏁? Number(鐢熸垚渚х粨鏋?.姹囨€?.鐙崰閫氳繃鏁?|| 0),
          鐢熸垚鐙崰鎬绘暟: Number(鐢熸垚渚х粨鏋?.姹囨€?.鐙崰鎬绘暟 || 0),
          鐢熸垚鍒嗘。閫氳繃鏁? Number(鐢熸垚渚х粨鏋?.姹囨€?.鍒嗘。閫氳繃鏁?|| 0),
          鐢熸垚鍒嗘。鎬绘暟: Number(鐢熸垚渚х粨鏋?.姹囨€?.鍒嗘。鎬绘暟 || 0),
          鎴樻枟閫氳繃鏁? Number(鎴樻枟渚х粨鏋?.passed || 0),
          鎴樻枟鎬绘暟: Number(鎴樻枟渚х粨鏋?.total || 0),
        },
      };
    };
    window.__LWCS_RUN_SKILL_STABLE_CHECK__ = async function runSkillStableCheck(澶瑰叿閿垪琛?= [], options = {}) {
      const 寰呰繍琛岄敭鍒楄〃 =
        Array.isArray(澶瑰叿閿垪琛? && 澶瑰叿閿垪琛?length
          ? 澶瑰叿閿垪琛?          : ['瀵硅薄宸紓_椋熺墿鑷湇绂佺敤', '瀵硅薄宸紓_绁炲湥閫嗛偑', '鐘舵€佽浆绉?, '寮曠垎鎸佺画浼ゅ', '鏂╃浘', '绐冨彇鎶ょ浘', '鏈哄埗鎶规秷_澶嶈嫃', '鏈哄埗鎶规秷_鎶ょ浘', '鏈哄埗鎶规秷_闅愯韩'];
      const 缁撴灉椤瑰垪琛?= [];
      for (const fixtureKey of 寰呰繍琛岄敭鍒楄〃) {
        await refreshLiveSnapshot({ force: true });
        const 鎵ц缁撴灉 = await window.__LWCS_RUN_SKILL_FIXTURE__(fixtureKey, {
          waitMs: Number(options?.waitMs || 260),
          settleWaitMs: Number(options?.settleWaitMs || 420),
        });
        缁撴灉椤瑰垪琛?push({
          key: fixtureKey,
          ok: true,
          validation: 鎵ц缁撴灉.validation,
          summary: 鎵ц缁撴灉.summary,
          mode: 鎵ц缁撴灉.result?.mode || '',
        });
        await new Promise(resolve => setTimeout(resolve, Math.max(0, Number(options?.betweenWaitMs || 220))));
      }
      const 閫氳繃鏁伴噺 = 缁撴灉椤瑰垪琛?filter(item => item.validation && item.validation.閫氳繃).length;
      return {
        total: 缁撴灉椤瑰垪琛?length,
        passed: 閫氳繃鏁伴噺,
        failed: 缁撴灉椤瑰垪琛?length - 閫氳繃鏁伴噺,
        items: 缁撴灉椤瑰垪琛?
      };
    };
    window.__LWCS_INSPECT_PHASE2_LINKAGE__ = function inspectPhase2Linkage(options = {}) {
      const 蹇収 = window.__MVU_GET_LIVE_SNAPSHOT__?.() || liveSnapshot || lastRenderableSnapshot || null;
      if (!蹇収 || !蹇収.rootData) return { ok: false, reason: 'phase2_linkage_snapshot_missing' };
      const 鏍规暟鎹?= 蹇収.rootData || {};
      const 鐜╁鍚?= toText(deepGet(鏍规暟鎹? 'sys.鐜╁鍚?, 蹇収.activeName), 蹇収.activeName);
      const 鐜╁浠诲姟鏉＄洰 = safeEntries(deepGet(鏍规暟鎹? ['char', 鐜╁鍚? '璁板綍'], {})).filter(([, item]) => item && typeof item === 'object');
      const 浠诲姟鑱氱劍 = 鐜╁浠诲姟鏉＄洰.find(([, item]) => !['宸插畬鎴?, '宸叉斁寮?, '澶辫触', '宸插け璐?].includes(toText(item && item['鐘舵€?], '杩涜涓?))) || 鐜╁浠诲姟鏉＄洰[0] || null;
      const 鎯呮姤鏉＄洰 = safeEntries(deepGet(鏍规暟鎹? 'world.鏈哄瘑鎯呮姤', {})).filter(([, item]) => item && typeof item === 'object');
      const 鍥鹃壌鏉＄洰 = safeEntries(deepGet(鏍规暟鎹? 'world.鍥鹃壌', {})).filter(([, item]) => item && typeof item === 'object');
      const 寰呮牳瀹炴潯鐩?= 鎯呮姤鏉＄洰.filter(([, item]) => toText(item && item['鏍稿疄鐘舵€?], '鍙枒') === '寰呮牳瀹?);
      const 鎯呮姤鐘舵€佺粺璁?= 鎯呮姤鏉＄洰.reduce((妗? [, item]) => {
        const 鐘舵€?= toText(item && item['鏍稿疄鐘舵€?], '鍙枒');
        妗禰鐘舵€乚 = (妗禰鐘舵€乚 || 0) + 1;
        return 妗?
      }, {});
      const 鏈€杩戞儏鎶?= 鎯呮姤鏉＄洰
        .slice()
        .sort((a, b) => toNumber(deepGet(b, '1.鏈€杩戞牳瀹瀟ick', 0), 0) - toNumber(deepGet(a, '1.鏈€杩戞牳瀹瀟ick', 0), 0))[0] || null;
      const 鍥鹃壌鑱氱劍 = 鍥鹃壌鏉＄洰
        .slice()
        .sort((a, b) => toNumber(deepGet(b, '1.鏈€杩戞椿璺僼ick', 0), 0) - toNumber(deepGet(a, '1.鏈€杩戞椿璺僼ick', 0), 0))[0] || null;
      const 蹇収瀛楁缂哄け = ['褰撳墠浠诲姟璺嚎', '褰撳墠浠诲姟椋庨櫓绾у埆', '褰撳墠浠诲姟涓嬩竴鑺傜偣杩涘害', '鎯呮姤寰呮牳瀹炴暟閲?, '鎯呮姤璇佹嵁鍑€鍊?, '鎯呮姤鏈€杩戞牳瀹炵粨鏋?, '鍥鹃壌褰撳墠妗ｄ綅', '鍥鹃壌涓嬩竴妗ｈ繘搴?, '鍥鹃壌鎴愰暱鍊惧悜']
        .filter(key => !Object.prototype.hasOwnProperty.call(蹇収, key));
      return {
        ok: true,
        鐜╁鍚?
        浠诲姟: {
          鎬绘暟: 鐜╁浠诲姟鏉＄洰.length,
          鑱氱劍: 浠诲姟鑱氱劍
            ? {
                鍚嶇О: 浠诲姟鑱氱劍[0],
                鐘舵€? toText(deepGet(浠诲姟鑱氱劍, '1.鐘舵€?, '杩涜涓?), '杩涜涓?),
                鍒嗘敮: toText(deepGet(浠诲姟鑱氱劍, '1.鍒嗘敮', '鏈垽瀹?), '鏈垽瀹?),
                椋庨櫓绾у埆: toText(deepGet(浠诲姟鑱氱劍, '1.椋庨櫓绾у埆', '鏃?), '鏃?),
                鎺ㄨ崘璺嚎: toText(deepGet(浠诲姟鑱氱劍, '1.鎺ㄨ崘璺嚎', '涓荤嚎'), '涓荤嚎'),
                褰撳墠杩涘害: toNumber(deepGet(浠诲姟鑱氱劍, '1.褰撳墠杩涘害', 0), 0),
                鐩爣杩涘害: toNumber(deepGet(浠诲姟鑱氱劍, '1.鐩爣杩涘害', 1), 1),
                鎯呮姤璐＄尞鍊? toNumber(deepGet(浠诲姟鑱氱劍, '1.鎯呮姤璐＄尞鍊?, 0), 0),
                鍥鹃壌璐＄尞鍊? toNumber(deepGet(浠诲姟鑱氱劍, '1.鍥鹃壌璐＄尞鍊?, 0), 0),
              }
            : null,
        },
        鎯呮姤: {
          鎬绘暟: 鎯呮姤鏉＄洰.length,
          寰呮牳瀹炴暟: 寰呮牳瀹炴潯鐩?length,
          鐘舵€佺粺璁? 鎯呮姤鐘舵€佺粺璁?
          鏈€杩? 鏈€杩戞儏鎶?            ? {
                鍚嶇О: 鏈€杩戞儏鎶0],
                鏍囬: toText(deepGet(鏈€杩戞儏鎶? '1.鏍囬', 鏈€杩戞儏鎶0]), 鏈€杩戞儏鎶0]),
                鏈€杩戠粨鏋? toText(deepGet(鏈€杩戞儏鎶? '1.鏈€杩戞牳瀹炵粨鏋?, '鏃?), '鏃?),
                璇佹嵁鏉冮噸: toNumber(deepGet(鏈€杩戞儏鎶? '1.璇佹嵁鏉冮噸', 0), 0),
                鍙嶈瘉鏉冮噸: toNumber(deepGet(鏈€杩戞儏鎶? '1.鍙嶈瘉鏉冮噸', 0), 0),
                璇佹嵁鍑€鍊? Number((
                  toNumber(deepGet(鏈€杩戞儏鎶? '1.璇佹嵁鏉冮噸', 0), 0)
                  - toNumber(deepGet(鏈€杩戞儏鎶? '1.鍙嶈瘉鏉冮噸', 0), 0)
                ).toFixed(2)),
                鏉ユ簮鏁? Array.isArray(deepGet(鏈€杩戞儏鎶? '1.璇佹嵁鏉ユ簮鍒楄〃', [])) ? deepGet(鏈€杩戞儏鎶? '1.璇佹嵁鏉ユ簮鍒楄〃', []).length : 0,
              }
            : null,
        },
        鍥鹃壌: {
          鎬绘暟: 鍥鹃壌鏉＄洰.length,
          鑱氱劍: 鍥鹃壌鑱氱劍
            ? {
                鍚嶇О: 鍥鹃壌鑱氱劍[0],
                妗ｄ綅: toText(deepGet(鍥鹃壌鑱氱劍, '1.鍥鹃壌妗ｄ綅', '鍒濊瘑'), '鍒濊瘑'),
                涓嬩竴妗ｈ繘搴? `${toNumber(deepGet(鍥鹃壌鑱氱劍, '1.褰撳墠妗ｇ粡楠?, 0), 0)}/${toNumber(deepGet(鍥鹃壌鑱氱劍, '1.涓嬫。闇€姹?, 0), 0)}`,
                鎴愰暱鍊惧悜: toText(deepGet(鍥鹃壌鑱氱劍, '1.鎴愰暱鍊惧悜', '鍧囪　'), '鍧囪　'),
                浠诲姟鍗忓悓绯绘暟: toNumber(deepGet(鍥鹃壌鑱氱劍, '1.浠诲姟鍗忓悓绯绘暟', 1), 1),
                鎯呮姤鍗忓悓绯绘暟: toNumber(deepGet(鍥鹃壌鑱氱劍, '1.鎯呮姤鍗忓悓绯绘暟', 1), 1),
                鏈€杩戞垬鏂楁爣绛? toText(deepGet(鍥鹃壌鑱氱劍, '1.鏈€杩戞垬鏂楁爣绛?, '鏈煡'), '鏈煡'),
                鎴樻枟鏍锋湰鏁? toNumber(deepGet(鍥鹃壌鑱氱劍, '1.鎴樻枟鏍锋湰鏁?, 0), 0),
              }
            : null,
        },
        鐪嬫澘蹇収: {
          褰撳墠浠诲姟璺嚎: toText(蹇収.褰撳墠浠诲姟璺嚎, '鏃?),
          褰撳墠浠诲姟椋庨櫓绾у埆: toText(蹇収.褰撳墠浠诲姟椋庨櫓绾у埆, '鏃?),
          褰撳墠浠诲姟涓嬩竴鑺傜偣杩涘害: toText(蹇収.褰撳墠浠诲姟涓嬩竴鑺傜偣杩涘害, '--'),
          鎯呮姤寰呮牳瀹炴暟閲? toNumber(蹇収.鎯呮姤寰呮牳瀹炴暟閲? 0),
          鎯呮姤璇佹嵁鍑€鍊? toNumber(蹇収.鎯呮姤璇佹嵁鍑€鍊? 0),
          鎯呮姤鏈€杩戞牳瀹炵粨鏋? toText(蹇収.鎯呮姤鏈€杩戞牳瀹炵粨鏋? '鏆傛棤'),
          鍥鹃壌褰撳墠妗ｄ綅: toText(蹇収.鍥鹃壌褰撳墠妗ｄ綅, '鏃?),
          鍥鹃壌涓嬩竴妗ｈ繘搴? toText(蹇収.鍥鹃壌涓嬩竴妗ｈ繘搴? '--'),
          鍥鹃壌鎴愰暱鍊惧悜: toText(蹇収.鍥鹃壌鎴愰暱鍊惧悜, '鏃?),
          缂哄け瀛楁: 蹇収瀛楁缂哄け,
        },
      };
    };
    window.__LWCS_RUN_PHASE2_LINKAGE_CHECK__ = function runPhase2LinkageCheck(options = {}) {
      const 蹇収缁撴灉 = window.__LWCS_INSPECT_PHASE2_LINKAGE__(options);
      const 妫€鏌ラ」 = [];
      const 杩藉姞妫€鏌?= (椤圭洰, 閫氳繃, 璇︽儏 = '') => 妫€鏌ラ」.push({ 椤圭洰, 閫氳繃: !!閫氳繃, 璇︽儏: toText(璇︽儏, '') });
      const 鍏佽绌轰换鍔℃牱鏈?= options?.鍏佽绌轰换鍔℃牱鏈?!== false;
      if (!蹇収缁撴灉 || 蹇収缁撴灉.ok !== true) {
        杩藉姞妫€鏌?'鑱斿姩蹇収鍙敤', false, toText(蹇収缁撴灉 && 蹇収缁撴灉.reason, 'unknown'));
        return {
          閫氳繃: false,
          妫€鏌ラ」,
          妫€鏌ユ柇瑷€: 妫€鏌ラ」.map(item => ({ 鍚嶇О: item.椤圭洰, 閫氳繃: item.閫氳繃, 璇︽儏: item.璇︽儏 })),
          蹇収: 蹇収缁撴灉 || null,
        };
      }
      const 鐪嬫澘缂哄け瀛楁 = Array.isArray(deepGet(蹇収缁撴灉, '鐪嬫澘蹇収.缂哄け瀛楁', [])) ? deepGet(蹇収缁撴灉, '鐪嬫澘蹇収.缂哄け瀛楁', []) : [];
      杩藉姞妫€鏌?'鐪嬫澘瀛楁瀹屾暣', !鐪嬫澘缂哄け瀛楁.length, 鐪嬫澘缂哄け瀛楁.length ? `缂哄け:${鐪嬫澘缂哄け瀛楁.join(' / ')}` : '宸茶鐩?);
      const 浠诲姟鑱氱劍 = deepGet(蹇収缁撴灉, '浠诲姟.鑱氱劍', null);
      const 瀛樺湪浠诲姟鏍锋湰 = !!浠诲姟鑱氱劍;
      const 浠诲姟妫€鏌ラ€氳繃 = 瀛樺湪浠诲姟鏍锋湰 || 鍏佽绌轰换鍔℃牱鏈?
      const 浠诲姟妫€鏌ヨ鎯?= 瀛樺湪浠诲姟鏍锋湰 ? '瀛樺湪鑱氱劍浠诲姟' : (鍏佽绌轰换鍔℃牱鏈?? '褰撳墠鏃犱换鍔℃牱鏈?鎸夌┖鏍锋湰閫氳繃)' : '褰撳墠鏃犱换鍔℃牱鏈?);
      杩藉姞妫€鏌?'浠诲姟鑱斿姩瀛楁鍙', 浠诲姟妫€鏌ラ€氳繃, 浠诲姟妫€鏌ヨ鎯?;
      const 鎯呮姤鏈€杩?= deepGet(蹇収缁撴灉, '鎯呮姤.鏈€杩?, null);
      const 鎯呮姤瀛楁瀹屾暣 = !鎯呮姤鏈€杩?|| (
        Object.prototype.hasOwnProperty.call(鎯呮姤鏈€杩? '璇佹嵁鏉冮噸')
        && Object.prototype.hasOwnProperty.call(鎯呮姤鏈€杩? '鍙嶈瘉鏉冮噸')
        && Object.prototype.hasOwnProperty.call(鎯呮姤鏈€杩? '鏉ユ簮鏁?)
      );
      杩藉姞妫€鏌?'鎯呮姤璇佹嵁瀛楁鍙', 鎯呮姤瀛楁瀹屾暣, 鎯呮姤鏈€杩?? `鍑€鍊?${toNumber(鎯呮姤鏈€杩?璇佹嵁鍑€鍊? 0)}` : '褰撳墠鏃犳儏鎶ユ牱鏈?);
      const 鍥鹃壌鏈€杩?= deepGet(蹇収缁撴灉, '鍥鹃壌.鑱氱劍', null);
      const 鍥鹃壌瀛楁瀹屾暣 = !鍥鹃壌鏈€杩?|| (
        Object.prototype.hasOwnProperty.call(鍥鹃壌鏈€杩? '鎴愰暱鍊惧悜')
        && Object.prototype.hasOwnProperty.call(鍥鹃壌鏈€杩? '浠诲姟鍗忓悓绯绘暟')
        && Object.prototype.hasOwnProperty.call(鍥鹃壌鏈€杩? '鎯呮姤鍗忓悓绯绘暟')
        && Object.prototype.hasOwnProperty.call(鍥鹃壌鏈€杩? '鏈€杩戞垬鏂楁爣绛?)
      );
      杩藉姞妫€鏌?'鍥鹃壌鍗忓悓瀛楁鍙', 鍥鹃壌瀛楁瀹屾暣, 鍥鹃壌鏈€杩?? `鍊惧悜=${toText(鍥鹃壌鏈€杩?鎴愰暱鍊惧悜, '鍧囪　')}` : '褰撳墠鏃犲浘閴存牱鏈?);
      const 鍩哄噯濂栧姳甯?= Math.max(1, Math.floor(Number(options?.鍩哄噯濂栧姳甯?|| 100000)));
      const 楂樻敹鐩婃垚鍔熷竵 = Math.floor(鍩哄噯濂栧姳甯?* 1.45);
      const 绋冲Ε鎴愬姛甯?= Math.floor(鍩哄噯濂栧姳甯?* 0.85);
      const 楂樻敹鐩婂け璐ヤ唬浠峰竵 = Math.floor(鍩哄噯濂栧姳甯?* 0.12);
      const 绋冲Ε澶辫触浠ｄ环甯?= Math.floor(鍩哄噯濂栧姳甯?* 0.04);
      杩藉姞妫€鏌?'璺嚎鎴愬姛鏀剁泭鎷夊紑', 楂樻敹鐩婃垚鍔熷竵 > 绋冲Ε鎴愬姛甯? `楂樻敹鐩?${楂樻敹鐩婃垚鍔熷竵} / 绋冲Ε=${绋冲Ε鎴愬姛甯亇`);
      杩藉姞妫€鏌?'璺嚎澶辫触浠ｄ环鎷夊紑', 楂樻敹鐩婂け璐ヤ唬浠峰竵 > 绋冲Ε澶辫触浠ｄ环甯? `楂樻敹鐩?${楂樻敹鐩婂け璐ヤ唬浠峰竵} / 绋冲Ε=${绋冲Ε澶辫触浠ｄ环甯亇`);
      const 妫€鏌ユ柇瑷€ = 妫€鏌ラ」.map(item => ({ 鍚嶇О: item.椤圭洰, 閫氳繃: item.閫氳繃, 璇︽儏: item.璇︽儏 }));
      return {
        閫氳繃: 妫€鏌ラ」.every(item => item.閫氳繃),
        妫€鏌ラ」,
        妫€鏌ユ柇瑷€,
        蹇収: 蹇収缁撴灉,
        绯绘暟鏍锋湰: {
          鍩哄噯濂栧姳甯?
          楂樻敹鐩婃垚鍔熷竵,
          绋冲Ε鎴愬姛甯?
          楂樻敹鐩婂け璐ヤ唬浠峰竵,
          绋冲Ε澶辫触浠ｄ环甯?
        },
      };
    };
    window.__LWCS_INSPECT_PHASE3_LINKAGE__ = function inspectPhase3Linkage(options = {}) {
      const 蹇収 = window.__MVU_GET_LIVE_SNAPSHOT__?.() || liveSnapshot || lastRenderableSnapshot || null;
      if (!蹇収 || !蹇収.rootData) return { ok: false, reason: 'phase3_linkage_snapshot_missing' };
      const 鏍规暟鎹?= 蹇収.rootData || {};
      const 绯荤粺鎾姤 = toText(deepGet(鏍规暟鎹? 'sys.绯荤粺鎾姤', ''), '');
      const 鍦扮偣鏉＄洰 = safeEntries(deepGet(鏍规暟鎹? 'world.鍦扮偣', {})).filter(([, item]) => item && typeof item === 'object');
      const 搴撳瓨鏉＄洰 = [];
      鍦扮偣鏉＄洰.forEach(([鍦扮偣鍚? 鍦扮偣鏁版嵁]) => {
        safeEntries(deepGet(鍦扮偣鏁版嵁, '鍟嗗簵', {})).forEach(([鍟嗗簵鍚? 鍟嗗簵鏁版嵁]) => {
          safeEntries(deepGet(鍟嗗簵鏁版嵁, '搴撳瓨', {})).forEach(([鍟嗗搧鍚? 鍟嗗搧鏁版嵁]) => {
            if (!鍟嗗搧鏁版嵁 || typeof 鍟嗗搧鏁版嵁 !== 'object') return;
            搴撳瓨鏉＄洰.push({
              鍦扮偣: 鍦扮偣鍚?
              鍟嗗簵: 鍟嗗簵鍚?
              鍟嗗搧: 鍟嗗搧鍚?
              搴撳瓨: toNumber(鍟嗗搧鏁版嵁 && 鍟嗗搧鏁版嵁['搴撳瓨'], 0),
              浠锋牸: toNumber(鍟嗗搧鏁版嵁 && 鍟嗗搧鏁版嵁['浠锋牸'], 0),
            });
          });
        });
      });
      const 鎯呮姤鏉＄洰 = safeEntries(deepGet(鏍规暟鎹? 'world.鏈哄瘑鎯呮姤', {})).filter(([, item]) => item && typeof item === 'object');
      const 浜ゆ槗瑙﹀彂鍛戒腑鏁?= 鎯呮姤鏉＄洰.filter(([, item]) => {
        const 瑙﹀彂鏉ユ簮 = toText(item && item['瑙﹀彂鏉ユ簮'], '');
        const 鏉ユ簮鍒楄〃 = Array.isArray(item && item['璇佹嵁鏉ユ簮鍒楄〃']) ? item['璇佹嵁鏉ユ簮鍒楄〃'] : [];
        return 瑙﹀彂鏉ユ簮 === '浜ゆ槗' || 鏉ユ簮鍒楄〃.some(鏉ユ簮 => toText(鏉ユ簮, '') === '浜ゆ槗');
      }).length;
      const 鏈€杩戞垚浜ゆ爣绛?= Array.from(绯荤粺鎾姤.matchAll(/\[(涔板叆鐑瓅鍗栧嚭鐑瓅绔炴媿鐑瓅鍏戞崲鐑?\]/g))
        .map(match => toText(match && match[1], '').trim())
        .filter(Boolean)
        .slice(-1)[0] || '鏃?;
      const 甯傚満娉㈠姩鍛戒腑 = /\[甯傚満娉㈠姩\]/.test(绯荤粺鎾姤);
      return {
        ok: true,
        鐪嬫澘: {
          鏈湴渚涚粰: toText(蹇収 && 蹇収.鏈湴渚涚粰, '鏃犱緵缁?),
          浠锋牸甯? toText(蹇収 && 蹇収.浠锋牸甯? '鏃?),
          鏈€杩戞垚浜ゅ奖鍝? toText(蹇収 && 蹇収.鏈€杩戞垚浜ゅ奖鍝? '骞崇ǔ'),
        },
        甯傚満: {
          搴撳瓨鎬婚」鏁? 搴撳瓨鏉＄洰.length,
          璐熷簱瀛橀」鏁? 搴撳瓨鏉＄洰.filter(椤?=> toNumber(椤?&& 椤筟'搴撳瓨'], 0) < 0).length,
          甯傚満娉㈠姩鍛戒腑,
        },
        浜ゆ槗鎯呮姤鑱斿姩: {
          鏈€杩戞垚浜ゆ爣绛?
          浜ゆ槗瑙﹀彂鍛戒腑鏁?
          鎯呮姤鎬绘暟: 鎯呮姤鏉＄洰.length,
        },
      };
    };
    window.__LWCS_RUN_PHASE3_LINKAGE_CHECK__ = function runPhase3LinkageCheck(options = {}) {
      const 蹇収缁撴灉 = window.__LWCS_INSPECT_PHASE3_LINKAGE__(options);
      const 妫€鏌ラ」 = [];
      const 杩藉姞妫€鏌?= (椤圭洰, 閫氳繃, 璇︽儏 = '') => 妫€鏌ラ」.push({ 椤圭洰, 閫氳繃: !!閫氳繃, 璇︽儏: toText(璇︽儏, '') });
      const 鍏佽绌轰氦鏄撴牱鏈?= options?.鍏佽绌轰氦鏄撴牱鏈?!== false;
      if (!蹇収缁撴灉 || 蹇収缁撴灉.ok !== true) {
        杩藉姞妫€鏌?'涓夋湡蹇収鍙敤', false, toText(蹇収缁撴灉 && 蹇収缁撴灉.reason, 'unknown'));
        return {
          閫氳繃: false,
          妫€鏌ラ」,
          妫€鏌ユ柇瑷€: 妫€鏌ラ」.map(item => ({ 鍚嶇О: item.椤圭洰, 閫氳繃: item.閫氳繃, 璇︽儏: item.璇︽儏 })),
          蹇収: 蹇収缁撴灉 || null,
        };
      }
      const 鐪嬫澘 = 蹇収缁撴灉.鐪嬫澘 || {};
      杩藉姞妫€鏌?'鍦板浘浜ゆ槗鎸囨爣鍙', !!(鐪嬫澘.鏈湴渚涚粰 && 鐪嬫澘.浠锋牸甯?&& 鐪嬫澘.鏈€杩戞垚浜ゅ奖鍝?, `渚涚粰=${鐪嬫澘.鏈湴渚涚粰 || '鏃?} / 浠锋牸=${鐪嬫澘.浠锋牸甯?|| '鏃?} / 鎴愪氦=${鐪嬫澘.鏈€杩戞垚浜ゅ奖鍝?|| '鏃?}`);
      const 璐熷簱瀛橀」鏁?= toNumber(deepGet(蹇収缁撴灉, '甯傚満.璐熷簱瀛橀」鏁?, 0), 0);
      const 甯傚満娉㈠姩鍛戒腑 = deepGet(蹇収缁撴灉, '甯傚満.甯傚満娉㈠姩鍛戒腑', false) === true;
      杩藉姞妫€鏌?'搴撳瓨涓嶄负璐?, 璐熷簱瀛橀」鏁?=== 0, `璐熷簱瀛橀」=${璐熷簱瀛橀」鏁皚`);
      杩藉姞妫€鏌?'甯傚満鑰楁暎鍛戒腑鍙拷韪?, 甯傚満娉㈠姩鍛戒腑 ? true : 鍏佽绌轰氦鏄撴牱鏈? 甯傚満娉㈠姩鍛戒腑 ? '妫€娴嬪埌[甯傚満娉㈠姩]' : (鍏佽绌轰氦鏄撴牱鏈?? '鏈疆闅忔満鏈懡涓?鎸夐€氳繃澶勭悊)' : '鏈疆鏈懡涓?));
      const 鏈€杩戞垚浜ゆ爣绛?= toText(deepGet(蹇収缁撴灉, '浜ゆ槗鎯呮姤鑱斿姩.鏈€杩戞垚浜ゆ爣绛?, '鏃?), '鏃?);
      const 浜ゆ槗瑙﹀彂鍛戒腑鏁?= toNumber(deepGet(蹇収缁撴灉, '浜ゆ槗鎯呮姤鑱斿姩.浜ゆ槗瑙﹀彂鍛戒腑鏁?, 0), 0);
      const 鎴愪氦鏍锋湰瀛樺湪 = 鏈€杩戞垚浜ゆ爣绛?!== '鏃?;
      const 鑱斿姩閫氳繃 = !鎴愪氦鏍锋湰瀛樺湪 || 浜ゆ槗瑙﹀彂鍛戒腑鏁?> 0;
      杩藉姞妫€鏌?'浜ゆ槗瑙﹀彂宸插苟鍏ユ儏鎶ラ摼', 鑱斿姩閫氳繃, 鎴愪氦鏍锋湰瀛樺湪 ? `鏍囩=${鏈€杩戞垚浜ゆ爣绛緘 / 鍛戒腑=${浜ゆ槗瑙﹀彂鍛戒腑鏁皚` : '褰撳墠鏃犱氦鏄撶儹搴︽牱鏈?);
      const 妫€鏌ユ柇瑷€ = 妫€鏌ラ」.map(item => ({ 鍚嶇О: item.椤圭洰, 閫氳繃: item.閫氳繃, 璇︽儏: item.璇︽儏 }));
      return {
        閫氳繃: 妫€鏌ラ」.every(item => item.閫氳繃),
        妫€鏌ラ」,
        妫€鏌ユ柇瑷€,
        蹇収: 蹇収缁撴灉,
      };
    };
    window.__LWCS_INSPECT_SKILL_RUNTIME__ = function inspectSkillRuntime() {
      const 褰撳墠琛?= __mvuBridgeRoot.__LWCS_SKILL_MECHANISM_REGISTRY__;
      let 鐖剁骇瀛樺湪 = false;
      let 椤剁骇瀛樺湪 = false;
      try {
        鐖剁骇瀛樺湪 = !!(window.parent && window.parent !== window && window.parent.__LWCS_SKILL_MECHANISM_REGISTRY__);
      } catch (error) {}
      try {
        椤剁骇瀛樺湪 = !!(window.top && window.top !== window && window.top.__LWCS_SKILL_MECHANISM_REGISTRY__);
      } catch (error) {}
      return {
        bridgeLoaded: __mvuBridgeRoot.__MVU_LOGIC_BRIDGE_LOADED__ === true,
        bridgeRegistrySource: __mvuBridgeRoot.__LWCS_BRIDGE_REGISTRY_SOURCE__ || '',
        bridgeMvuHostSource: __mvuBridgeRoot.__LWCS_BRIDGED_MVU_HOST_SOURCE__ || '',
        registryReady: !!(褰撳墠琛?&& typeof 褰撳墠琛?=== 'object'),
        registryMechanismCount: 褰撳墠琛?.鏈哄埗瀹氫箟 ? Object.keys(褰撳墠琛?鏈哄埗瀹氫箟).length : 0,
        registryCurrent: !!(__mvuBridgeRoot.__LWCS_SKILL_MECHANISM_REGISTRY__),
        registryParent: 鐖剁骇瀛樺湪,
        registryTop: 椤剁骇瀛樺湪,
        fixtureKeys: typeof window.__LWCS_LIST_SKILL_FIXTURES__ === 'function' ? window.__LWCS_LIST_SKILL_FIXTURES__() : [],
        fixtureBatchReady: typeof window.__LWCS_RUN_SKILL_FIXTURE_BATCH__ === 'function',
        battleRegistrySource: __mvuBridgeRoot.__LWCS_BATTLE_RUNTIME_REGISTRY_SOURCE__ || '',
        battleRegistrySize: Number(__mvuBridgeRoot.__LWCS_BATTLE_RUNTIME_REGISTRY_SIZE__ || 0),
      };
    };
    window.__LWCS_BOOTSTRAP_MVU_HOST__ = () => ensureBridgedMvuHost();
    function buildSpiritRingGrid(rings, fallbackClass = 'ring-white') {
      const normalized = [...(rings || [])].filter(ring => ring && !ring.empty && ring.glyph && ring.glyph !== '绌?);
      if (!normalized.length) return '';
      return normalized.slice(0, 10).map(ring => {
        return `<div class="ring ${ring.ringClass || fallbackClass} interactive-ring">${ring.glyph}${buildRingHoverMarkup(ring)}</div>`;
      }).join('');
    }

    function buildRingHoverMarkup(ring) {
      return buildRingHoverCardMarkup(ring);
    }

    function renderArchiveSpiritEntry(config, isPrimary = false) {
      const head = isPrimary
        ? `<div class="dual-side-top"><div class="strip-head"><div class="strip-title cyan">姝﹂瓊妗ｆ</div></div><span class="badge ${config.badgeClass || 'cyan'}">${config.badge}</span></div>`
        : `<div class="dual-side-top dual-side-top-right"><span class="badge ${config.badgeClass || 'gold'}">${config.badge}</span></div>`;

      return `${head}<div class="strip-name">${config.name}</div><div class="rings dual-grid ${isPrimary ? 'primary-rings' : 'secondary-rings'}">${buildSpiritRingGrid(config.榄傜幆, isPrimary ? 'ring-white' : 'ring-gold')}</div>`;
    }

    function renderArchiveBloodlineEntry(config) {
      return `
        <div class="dual-side-top dual-side-top-right">
          <span class="badge ${config.badgeClass || 'gold'}">${config.badge}</span>
        </div>
        <div class="strip-name">${config.name}</div>
        <div class="rings dual-grid secondary-rings">${buildSpiritRingGrid(config.榄傜幆, 'ring-gold')}</div>
      `;
    }

    const pageMetaMap = {
      'page-archive': { title: '妗ｆ / 涓汉涓绘帶', subtitle: '璇︾粏妗ｆ銆佹榄傝建閬撱€佺ぞ浼氭憳瑕佷笌鍌ㄧ墿鍏ュ彛', tag: '妗ｆ鏍稿績' },
      'page-map': { title: '鏄熷浘 / 鑺傜偣瀵艰埅', subtitle: '褰撳墠浣嶇疆銆佷笅閽昏矾寰勩€佹湰鍦拌鏂戒笌鍔ㄦ€佸湴鐐?, tag: '鏄熷浘瀵艰埅' },
      'page-world': { title: '涓栫晫 / 鏃剁┖涓灑', subtitle: '鏃堕棿杞淬€佸亸宸笌鎷嶅崠璀︽姤鐨勬眹鎬婚潰鏉?, tag: '涓栫晫涓灑' },
      'page-org': { title: '鍔垮姏 / 鐭╅樀娌欑洏', subtitle: '闃佃惀褰掑睘銆佸ぇ闄嗘牸灞€涓庢嵁鐐圭綉缁滅殑瑙嗚鍖栧叆鍙?, tag: '鍔垮姏鐭╅樀' },
      'page-terminal': { title: '缁堢 / 绯荤粺鎬荤嚎', subtitle: '绯荤粺鎾姤銆佽姹傛€荤嚎涓庤繎鏈熻闂?, tag: '绯荤粺鎬荤嚎' }
    };


    function setMainTab(targetId) {
      try {
        if (typeof window.__MVU_SET_TAB_STATE__ === 'function') window.__MVU_SET_TAB_STATE__(targetId);
      } catch (err) {}

      closeModal();
    }

    
    const initialMainTab = (() => {
      try {
        const currentTab = window.__MVU_TAB_STATE__ && typeof window.__MVU_TAB_STATE__.current === 'string'
          ? window.__MVU_TAB_STATE__.current
          : '';
        return pageMetaMap[currentTab] ? currentTab : 'page-archive';
      } catch (err) {
        return 'page-archive';
      }
    })();
    setMainTab(initialMainTab);

    function buildDynamicPreview(key) {
      if (key.startsWith('鍦板浘鑺傜偣锛?)) {
        const nodeName = key.replace('鍦板浘鑺傜偣锛?, '');
        return {
          title: `鍦板浘鑺傜偣 / ${nodeName}`,
          summary: '浠庡湴鍥捐妭鐐圭洿鎺ュ睍寮€鍦扮偣璇︽儏锛屾煡鐪嬭繖鐗囧尯鍩熺殑鍔垮姏銆佽ˉ缁欍€佸晢璺笌鍛ㄨ竟鍙幓鏂瑰悜銆?,
          highlights: ['褰撳墠鑺傜偣姒傚喌', '鍛ㄨ竟鍔垮姏涓庡晢璺?, '鏈€杩戝彂鐢熺殑鍦板尯鍔ㄥ悜'],
          duties: ['鏌ョ湅鑺傜偣灞傜骇涓庡湴鐐规弿杩?, '鏌ョ湅鎺屾帶鍔垮姏銆佺粡娴庝笌鍟嗗簵鎯呭喌', '鏌ョ湅杩樿兘缁х画鍓嶅線鐨勫懆杈瑰尯鍩?],
          actions: ['鏌ョ湅鏈湴鎹偣', '鏌ョ湅鍙敤鍟嗚矾', '缁х画涓嬮捇鍛ㄨ竟鍖哄煙']
        };
      }

      return {
        title: key,
        summary: '杩欎釜妯″潡浼氫互缁熶竴鐨勮鎯呴〉缁撴瀯灞曞紑锛屾柟渚跨洿鎺ユ煡鐪嬫牳蹇冧俊鎭€?,
        highlights: ['杩欓噷浼氭樉绀鸿妯″潡鐨勬牳蹇冩鍐?, '閲嶇偣淇℃伅浼氭寜闃呰椤哄簭灞曞紑', '鍐呭浼氫繚鎸佺粺涓€缁撴瀯'],
        duties: ['鏌ョ湅璇ユā鍧楃殑涓昏淇℃伅', '鏌ョ湅浠?1绾ч〉涓嬮捇鍚庣殑閲嶅瀷鍐呭', '缁存寔缁熶竴鐨勮鎯呴槄璇荤粨鏋?],
        actions: ['鏌ョ湅閲嶇偣淇℃伅', '鍏抽棴鍚庤繑鍥炰笂涓€灞?]
      };
    }

    var inventoryHoverPanel = null;
    var activeInventoryHoverCell = null;
    var inventoryHoverFlipY = false;
    var inventoryHoverPinned = false;

    function ensureInventoryHoverPanel() {
      if (inventoryHoverPanel) return inventoryHoverPanel;
      inventoryHoverPanel = document.createElement('div');
      inventoryHoverPanel.className = 'inventory-hover-panel';
      inventoryHoverPanel.addEventListener('mouseenter', () => {
        inventoryHoverPinned = true;
      });
      inventoryHoverPanel.addEventListener('mouseleave', () => {
        inventoryHoverPinned = false;
        hideInventoryHoverPanel();
      });
      document.body.appendChild(inventoryHoverPanel);
      return inventoryHoverPanel;
    }

    function hideInventoryHoverPanel() {
      if (inventoryHoverPinned) return;
      if (!inventoryHoverPanel) return;
      inventoryHoverPanel.classList.remove('show');
      inventoryHoverPanel.classList.remove('flip-y');
      activeInventoryHoverCell = null;
    }

    function placeInventoryHoverPanel(panel, event) {
      const gap = 18;
      const vw = window.innerWidth;
      const vh = window.innerHeight;
      const panelWidth = panel.offsetWidth || 236;
      const panelHeight = panel.offsetHeight || 168;

      let left = event.clientX + gap;
      if (left + panelWidth > vw - 12) {
        left = event.clientX - panelWidth - gap;
      }
      left = Math.max(12, Math.min(left, vw - panelWidth - 12));

      let top = event.clientY + gap;
      inventoryHoverFlipY = false;
      if (top + panelHeight > vh - 12) {
        top = event.clientY - panelHeight - gap;
        inventoryHoverFlipY = true;
      }
      top = Math.max(12, Math.min(top, vh - panelHeight - 12));

      panel.classList.toggle('flip-y', inventoryHoverFlipY);
      panel.style.left = `${left}px`;
      panel.style.top = `${top}px`;
    }

    function showInventoryHoverPanel(cell, event, forceRender = false) {
      const panel = ensureInventoryHoverPanel();
      if (forceRender || activeInventoryHoverCell !== cell) {
        const tags = (cell.dataset.hoverTags || '').split('|').filter(Boolean);
        panel.innerHTML = `
          <div class="inventory-hover-title">${cell.dataset.hoverTitle || ''}</div>
          <div class="inventory-hover-grid">
            <div class="meta-item"><b>绫诲瀷</b><span>${cell.dataset.hoverType || '--'}</span></div>
            <div class="meta-item"><b>绋€鏈夊害</b><span>${cell.dataset.hoverRarity || '--'}</span></div>
            <div class="meta-item"><b>鏁伴噺</b><span>${cell.dataset.hoverQty || '--'}</span></div>
            <div class="meta-item"><b>瑙﹀彂鏂瑰紡</b><span>${cell.dataset.hoverTrigger || '--'}</span></div>
            <div class="meta-item"><b>鏈夋晥鏈熻嚦</b><span>${cell.dataset.hoverExpiry || '--'}</span></div>
            <div class="meta-item"><b>鏉ユ簮</b><span>${cell.dataset.hoverSource || '--'}</span></div>
            <div class="meta-item"><b>璇存槑</b><span>${cell.dataset.hoverUsage || '--'}</span></div>
          </div>
          <div class="inventory-hover-tags">${tags.filter(Boolean).map(tag => `<span class="tag-chip">${tag}</span>`).join('')}</div>
          ${cell.dataset.hoverUsable === 'true' ? `
          <button type="button" class="inventory-hover-action-btn" data-inventory-action="use" data-inventory-char="${escapeHtmlAttr(cell.dataset.hoverChar || '')}" data-inventory-item="${escapeHtmlAttr(cell.dataset.hoverTitle || '')}">
            ${htmlEscape(cell.dataset.hoverActionLabel || '浣跨敤')}
          </button>
          ` : ''}
          ${cell.dataset.hoverEquip === 'true' ? `
          <button type="button" class="inventory-hover-action-btn" data-inventory-action="equip" data-inventory-char="${escapeHtmlAttr(cell.dataset.hoverChar || '')}" data-inventory-item="${escapeHtmlAttr(cell.dataset.hoverTitle || '')}">
            绌挎埓瑁呭 / 瑁呰浇
          </button>
          ` : ''}
          ${cell.dataset.hoverTitle ? `
          <div class="tag-cloud armory-quick-actions" style="margin-top:10px;justify-content:flex-end;">
            <button type="button" class="inventory-hover-action-btn" data-inventory-action="discard" data-inventory-mode="one" data-inventory-char="${escapeHtmlAttr(cell.dataset.hoverChar || '')}" data-inventory-item="${escapeHtmlAttr(cell.dataset.hoverTitle || '')}">涓㈠純1浠?/button>
            ${toNumber(cell.dataset.hoverCount, 1) > 1 ? `<button type="button" class="inventory-hover-action-btn" data-inventory-action="discard" data-inventory-mode="all" data-inventory-char="${escapeHtmlAttr(cell.dataset.hoverChar || '')}" data-inventory-item="${escapeHtmlAttr(cell.dataset.hoverTitle || '')}">鍏ㄩ儴涓㈠純</button>` : ''}
          </div>
          ` : ''}
        `;
        activeInventoryHoverCell = cell;
      }
      panel.classList.add('show');
      placeInventoryHoverPanel(panel, event);
    }

    document.addEventListener('mousemove', (event) => {
      const eventTarget = event.target instanceof Element ? event.target : (event.target && event.target.parentElement ? event.target.parentElement : null);
      if (inventoryHoverPanel && eventTarget && inventoryHoverPanel.contains(eventTarget)) return;
      const cell = eventTarget ? eventTarget.closest('.inventory-cell[data-hover-title]') : null;
      const shellInlineHost = getShellInlineHost();
      const isShellInlineCell = !!(cell && isShellInlinePreviewActive() && shellInlineHost && shellInlineHost.contains(cell));
      if (!cell || (!(detailModal.classList.contains('show') && detailModal.contains(cell)) && !isShellInlineCell)) {
        hideInventoryHoverPanel();
        return;
      }
      inventoryHoverPinned = false;
      showInventoryHoverPanel(cell, event);
    });

    document.addEventListener('mouseleave', () => {
      inventoryHoverPinned = false;
      hideInventoryHoverPanel();
    }, true);
    document.addEventListener('scroll', hideInventoryHoverPanel, true);

    function escapeJsonPointerValue(text) {
      return String(text == null ? '' : text).split('~').join('~0').split('/').join('~1');
    }

    function normalizeMapDispatchServices(detail) {
      return Array.isArray(detail && detail.services)
        ? detail.services.map(item => toText(item, '')).filter(Boolean)
        : [];
    }

    function resolveSnapshotCharKey(snapshot, rawName) {
      const wanted = toText(rawName, '').trim();
      const chars = deepGet(snapshot, 'rootData.char', {});
      if (!wanted || !chars || typeof chars !== 'object') return '';
      if (chars[wanted]) return wanted;
      for (const [charKey, charInfo] of Object.entries(chars)) {
        const displayName = toText(charInfo && (charInfo.name || deepGet(charInfo, 'base.name', '')), charKey);
        if (displayName === wanted) return charKey;
      }
      return '';
    }

    function resolveSnapshotCharacter(snapshot, rawName) {
      const key = resolveSnapshotCharKey(snapshot, rawName);
      const chars = deepGet(snapshot, 'rootData.char', {});
      const charInfo = key && chars && typeof chars === 'object' ? (chars[key] || null) : null;
      const displayName = charInfo
        ? toText(charInfo && (charInfo.name || deepGet(charInfo, 'base.name', '')), key)
        : toText(rawName, '');
      return { key, displayName, char: charInfo };
    }

    function normalizeLocForMatch(location) {
      const raw = toText(location, '').replace(/^鏂楃綏澶ч檰-/, '').replace(/^鏂楃伒澶ч檰-/, '').trim();
      const segments = raw.split('-').filter(Boolean);
      return {
        raw,
        leaf: segments[segments.length - 1] || raw,
        segments
      };
    }

    function isLocationCompatible(currentLoc, targetLoc) {
      const current = normalizeLocForMatch(currentLoc);
      const target = normalizeLocForMatch(targetLoc);
      if (!current.raw || !target.raw) return current.raw === target.raw;
      if (current.raw === target.raw || current.leaf === target.leaf) return true;
      return current.segments.some(seg => target.segments.includes(seg));
    }

    function buildRelationInteractRequest(snapshot, actionType, rawTargetName, options = {}) {
      const currentLoc = toText(snapshot && snapshot.currentLoc, '鏈煡鍦扮偣');
      return buildMapInteractDispatchRequest(snapshot, {
        action: actionType,
        npcTarget: rawTargetName,
        target: currentLoc,
        currentLoc,
        itemUsed: toText(options.itemUsed, '鏃?),
        sourceLabel: '浜虹墿鍏崇郴椤电ぞ浜や簰鍔?,
        requestKind: 'relation_interaction'
      });
    }

    function buildRelationBattleInitRequest(snapshot, rawTargetName) {
      const currentLoc = toText(snapshot && snapshot.currentLoc, '鏈煡鍦扮偣');
      return buildMapBattleInitRequest(snapshot, { npcTarget: rawTargetName, target: currentLoc, currentLoc, requestKind: 'relation_sparring' });
    }

    const 姝﹂瓊鐩稿叧搴﹁姹傞攣 = new Set();

    function 鎻愬彇姝﹂瓊鍒ゅ畾鎽樿鏂囨湰(瑙掕壊鏁版嵁 = {}) {
      const 璇嶆潯 = [];
      Object.entries(瑙掕壊鏁版嵁?.姝﹂瓊 || {}).forEach(([妲戒綅鍚? 姝﹂瓊]) => {
        if (!姝﹂瓊 || typeof 姝﹂瓊 !== 'object') return;
        const 琛ㄨ薄鍚嶇О = toText(姝﹂瓊.琛ㄨ薄鍚嶇О, 妲戒綅鍚?;
        const 绯诲埆 = toText(姝﹂瓊.type, '鏈煡绯?);
        const 灞炴€т綋绯?= toText(姝﹂瓊.灞炴€т綋绯? '鏃?);
        const 宸茶В閿佸睘鎬?= Array.isArray(姝﹂瓊.宸茶В閿佸睘鎬? ? 姝﹂瓊.宸茶В閿佸睘鎬?map(item => toText(item, '')).filter(Boolean) : [];
        const 鎻忚堪 = toText(姝﹂瓊.鎻忚堪, '');
        璇嶆潯.push(`${妲戒綅鍚峿:${琛ㄨ薄鍚嶇О}/${绯诲埆}/${灞炴€т綋绯粆${宸茶В閿佸睘鎬?length ? `/${宸茶В閿佸睘鎬?join('|')}` : ''}${鎻忚堪 ? `/${鎻忚堪}` : ''}`);
      });
      return 璇嶆潯.join('锛?) || '鏃犳榄備俊鎭?;
    }

    function 瑙勫垯浼扮畻姝﹂瓊鐩稿叧搴﹀熀纭€(涓昏鏁版嵁 = {}, 瀵硅薄鏁版嵁 = {}, 鍏崇郴鏁版嵁 = {}) {
      const 涓昏璇?= 鎻愬彇姝﹂瓊鍒ゅ畾鎽樿鏂囨湰(涓昏鏁版嵁);
      const 瀵硅薄璇?= 鎻愬彇姝﹂瓊鍒ゅ畾鎽樿鏂囨湰(瀵硅薄鏁版嵁);
      if (!涓昏璇?|| !瀵硅薄璇?|| 涓昏璇?=== '鏃犳榄備俊鎭? || 瀵硅薄璇?=== '鏃犳榄備俊鎭?) return 0;

      const 涓昏灞炴€ц瘝 = new Set();
      Object.values(涓昏鏁版嵁?.姝﹂瓊 || {}).forEach(姝﹂瓊 => {
        if (!姝﹂瓊 || typeof 姝﹂瓊 !== 'object') return;
        [姝﹂瓊.type, 姝﹂瓊.灞炴€т綋绯? ...(Array.isArray(姝﹂瓊.宸茶В閿佸睘鎬? ? 姝﹂瓊.宸茶В閿佸睘鎬?: [])]
          .map(item => toText(item, ''))
          .filter(Boolean)
          .forEach(item => 涓昏灞炴€ц瘝.add(item));
      });
      const 瀵硅薄灞炴€ц瘝 = new Set();
      Object.values(瀵硅薄鏁版嵁?.姝﹂瓊 || {}).forEach(姝﹂瓊 => {
        if (!姝﹂瓊 || typeof 姝﹂瓊 !== 'object') return;
        [姝﹂瓊.type, 姝﹂瓊.灞炴€т綋绯? ...(Array.isArray(姝﹂瓊.宸茶В閿佸睘鎬? ? 姝﹂瓊.宸茶В閿佸睘鎬?: [])]
          .map(item => toText(item, ''))
          .filter(Boolean)
          .forEach(item => 瀵硅薄灞炴€ц瘝.add(item));
      });

      let 鍒嗘暟 = 0;
      const 閲嶅彔灞炴€ц瘝 = Array.from(涓昏灞炴€ц瘝).filter(item => 瀵硅薄灞炴€ц瘝.has(item));
      if (閲嶅彔灞炴€ц瘝.length) 鍒嗘暟 += Math.min(40, 閲嶅彔灞炴€ц瘝.length * 10);
      if (Array.from(涓昏灞炴€ц瘝).some(item => /榫?.test(item)) && Array.from(瀵硅薄灞炴€ц瘝).some(item => /榫?.test(item))) 鍒嗘暟 += 15;
      if (Array.from(涓昏灞炴€ц瘝).some(item => /鍏冪礌/.test(item)) && Array.from(瀵硅薄灞炴€ц瘝).some(item => /鍏冪礌/.test(item))) 鍒嗘暟 += 8;
      if (String(鍏崇郴鏁版嵁?.鍏崇郴璺嚎 || '').trim() === '鎭嬩汉绾?) 鍒嗘暟 += 5;

      return Math.max(0, Math.min(100, Math.floor(鍒嗘暟)));
    }

    function 璇诲彇姝﹂瓊鐩稿叧搴﹀熀纭€鍊糭妗ユ帴(鍏崇郴鏁版嵁 = {}) {
      const 鍘熷€?= Number(鍏崇郴鏁版嵁 && 鍏崇郴鏁版嵁['姝﹂瓊鐩稿叧搴﹀熀纭€']);
      if (!Number.isFinite(鍘熷€?) return null;
      return Math.max(0, Math.min(100, Math.floor(鍘熷€?));
    }

    function 璁＄畻鍏崇郴鍔犳垚鍊糭妗ユ帴(濂芥劅搴?= 0) {
      const 鏁板€?= Number(濂芥劅搴?|| 0);
      if (!Number.isFinite(鏁板€?) return 0;
      return Math.max(0, Math.min(20, Math.floor(Math.max(0, 鏁板€? / 10)));
    }

    function 璁＄畻姝﹂瓊鐩稿叧搴︽€诲垎_妗ユ帴(鍏崇郴鏁版嵁 = {}) {
      const 鍩虹鍊?= 璇诲彇姝﹂瓊鐩稿叧搴﹀熀纭€鍊糭妗ユ帴(鍏崇郴鏁版嵁);
      const 鍏崇郴鍔犳垚 = 璁＄畻鍏崇郴鍔犳垚鍊糭妗ユ帴(toNumber(鍏崇郴鏁版嵁 && 鍏崇郴鏁版嵁['濂芥劅搴?], 0));
      return Math.max(0, Math.min(100, (鍩虹鍊?== null ? 0 : 鍩虹鍊? + 鍏崇郴鍔犳垚));
    }

    function 鏋勫缓鍏崇郴鐩稿叧搴﹁鍥炬暟鎹甠妗ユ帴(鍏崇郴鏁版嵁 = {}) {
      const 濂芥劅搴?= toNumber(鍏崇郴鏁版嵁 && 鍏崇郴鏁版嵁['濂芥劅搴?], 0);
      const 鍩虹鍊?= 璇诲彇姝﹂瓊鐩稿叧搴﹀熀纭€鍊糭妗ユ帴(鍏崇郴鏁版嵁);
      const 鍩虹鏄剧ず鍊?= 鍩虹鍊?== null
        ? toText(鍏崇郴鏁版嵁 && 鍏崇郴鏁版嵁['姝﹂瓊鐩稿叧搴﹀熀纭€'], '寰呰ˉ鍏?)
        : String(鍩虹鍊?;
      const 鍏崇郴鍔犳垚鍊?= 璁＄畻鍏崇郴鍔犳垚鍊糭妗ユ帴(濂芥劅搴?;
      const 鎬诲垎鍊?= 璁＄畻姝﹂瓊鐩稿叧搴︽€诲垎_妗ユ帴(鍏崇郴鏁版嵁);
      return {
        濂芥劅搴?
        鍩虹鍊?
        鍩虹鏄剧ず鍊?
        鍏崇郴鍔犳垚鍊?
        鎬诲垎鍊?
        铻嶅悎瑙﹀彂鐘舵€? 鎬诲垎鍊?>= 70 ? '鍙Е鍙? : '鏈揪闃堝€?,
        鍚屼慨鏁堢巼鍊嶇巼: Number((1 + 鎬诲垎鍊?* 0.0025).toFixed(3)),
      };
    }

    function 鏋勫缓姝﹂瓊鐩稿叧搴﹀垽瀹氱郴缁熸彁绀?涓昏鍚? 瀵硅薄鍚? 涓昏鎽樿, 瀵硅薄鎽樿) {
      return [
        '浣犳槸姝﹂瓊鐩稿叧搴﹀垽瀹氬櫒锛屽彧杈撳嚭绠€鐭腑鏂囩粨璁恒€?,
        '鍒ゅ畾鍙ｅ緞锛氶€氬父涓?鍒嗭紝浠呭湪姝﹂瓊绯诲埆/灞炴€т綋绯?涓婁笅鏂囧嚭鐜版槑鏄惧崗鍚屾椂鎻愰珮銆?,
        '鍩虹鐩稿叧搴﹁寖鍥?-100锛涘叧绯诲姞鎴愮敱绯荤粺鑷姩璁＄畻锛屼笉鐢变綘杈撳嚭銆?,
        `鍒ゅ畾瀵硅薄锛?{涓昏鍚峿 涓?${瀵硅薄鍚峿銆俙,
        `涓昏姝﹂瓊锛?{涓昏鎽樿}`,
        `瀵硅薄姝﹂瓊锛?{瀵硅薄鎽樿}`,
        '杈撳嚭鏍煎紡锛氫竴鍙ヨ瘽璇存槑鍒ゅ畾渚濇嵁锛屼笉瑕佽緭鍑篔SON锛屼笉瑕佽緭鍑哄娈点€?
      ].join('\n');
    }

    function 鏋勫缓姝﹂瓊鐩稿叧搴﹁ˉ涓佽矾寰勫墠缂€(瑙掕壊閿? 鐩爣鍚? {
      return `/char/${escapeJsonPointerValue(瑙掕壊閿?}/绀句氦/鍏崇郴/${escapeJsonPointerValue(鐩爣鍚?}`;
    }

    async function 灏濊瘯瑙﹀彂姝﹂瓊鐩稿叧搴﹁嚜鍔ㄥ垽瀹?snapshot, 瑙掕壊閿? 鐩爣鍚? {
      const safe瑙掕壊閿?= toText(瑙掕壊閿? '').trim();
      const safe鐩爣鍚?= toText(鐩爣鍚? '').trim();
      if (!snapshot || !safe瑙掕壊閿?|| !safe鐩爣鍚? return;
      const 閿侀敭 = `${safe瑙掕壊閿畗::${safe鐩爣鍚峿`;
      if (姝﹂瓊鐩稿叧搴﹁姹傞攣.has(閿侀敭)) return;

      const 鍏崇郴鏁版嵁 = deepGet(snapshot, ['rootData', 'char', safe瑙掕壊閿? '绀句氦', '鍏崇郴', safe鐩爣鍚峕, null);
      if (!鍏崇郴鏁版嵁 || typeof 鍏崇郴鏁版嵁 !== 'object') return;
      const 鍩虹鍊?= 璇诲彇姝﹂瓊鐩稿叧搴﹀熀纭€鍊糭妗ユ帴(鍏崇郴鏁版嵁);
      const 闇€瑕佸垽瀹?= 鍩虹鍊?== null;
      if (!闇€瑕佸垽瀹? return;

      const 涓昏鏁版嵁 = deepGet(snapshot, ['rootData', 'char', safe瑙掕壊閿甝, {}) || {};
      const 瀵硅薄瑙ｆ瀽 = resolveSnapshotCharacter(snapshot, safe鐩爣鍚?;
      const 瀵硅薄鏁版嵁 = 瀵硅薄瑙ｆ瀽.char || {};
      const 鍩虹鍒?= 瑙勫垯浼扮畻姝﹂瓊鐩稿叧搴﹀熀纭€(涓昏鏁版嵁, 瀵硅薄鏁版嵁, 鍏崇郴鏁版嵁);
      const 璺緞鍓嶇紑 = 鏋勫缓姝﹂瓊鐩稿叧搴﹁ˉ涓佽矾寰勫墠缂€(safe瑙掕壊閿? safe鐩爣鍚?;
      const 棰勫啓琛ヤ竵 = [
        { op: 'replace', path: `${璺緞鍓嶇紑}/姝﹂瓊鐩稿叧搴﹀熀纭€`, value: 鍩虹鍒?},
      ];
      const 涓昏鍚?= toText(deepGet(涓昏鏁版嵁, 'name', safe瑙掕壊閿?, safe瑙掕壊閿?;
      const 瀵硅薄鍚?= 瀵硅薄瑙ｆ瀽.displayName || safe鐩爣鍚?
      const 涓昏鎽樿 = 鎻愬彇姝﹂瓊鍒ゅ畾鎽樿鏂囨湰(涓昏鏁版嵁);
      const 瀵硅薄鎽樿 = 鎻愬彇姝﹂瓊鍒ゅ畾鎽樿鏂囨湰(瀵硅薄鏁版嵁);

      姝﹂瓊鐩稿叧搴﹁姹傞攣.add(閿侀敭);
      try {
        await dispatchUiAiRequest(
          `璇峰垽瀹氥€?{涓昏鍚峿銆戜笌銆?{瀵硅薄鍚峿銆戠殑姝﹂瓊鐩稿叧搴﹀熀纭€鍒嗭紝閫氬父涓?锛屾寜姝﹂瓊绯诲埆/灞炴€?涓婁笅鏂囦慨姝ｃ€俙,
          鏋勫缓姝﹂瓊鐩稿叧搴﹀垽瀹氱郴缁熸彁绀?涓昏鍚? 瀵硅薄鍚? 涓昏鎽樿, 瀵硅薄鎽樿),
          {
            requestKind: 'relation_wuhun_affinity',
            patchOps: 棰勫啓琛ヤ竵,
            skipActionLock: true,
          },
        );
      } catch (error) {
      } finally {
        window.setTimeout(() => 姝﹂瓊鐩稿叧搴﹁姹傞攣.delete(閿侀敭), 2400);
      }
    }

    function listAscensionTicketNames(snapshot) {
      return (snapshot && Array.isArray(snapshot.inventoryEntries) ? snapshot.inventoryEntries : [])
        .filter(([name, item]) => /鍗囩伒鍙?.test(toText(name, '')) && toNumber(item && item['鏁伴噺'], 0) > 0)
        .map(([name]) => toText(name, ''))
        .filter(Boolean);
    }

    function listSoulTowerTicketNames(snapshot) {
      return (snapshot && Array.isArray(snapshot.inventoryEntries) ? snapshot.inventoryEntries : [])
        .filter(([name, item]) => {
          const safeName = toText(name, '');
          const itemType = toText(item && item['绫诲瀷'], '');
          return (
            /榄傜伒濉?.test(safeName) &&
            (/(绁▅鍒竱浠鍑瘉)/.test(safeName) || itemType === '闂ㄧエ') &&
            toNumber(item && item['鏁伴噺'], 0) > 0
          );
        })
        .map(([name]) => toText(name, ''))
        .filter(Boolean);
    }

    function buildInventoryConsumePatches(snapshot, charKey = '', itemName = '', quantity = 1) {
      const safeCharKey = toText(charKey, '').trim();
      const safeItemName = toText(itemName, '').trim();
      const consumeCount = Math.max(1, Math.floor(toNumber(quantity, 1)));
      if (!snapshot || !snapshot.rootData || !safeCharKey || !safeItemName) {
        return { ok: false, reason: 'inventory_consume_context_invalid', patchOps: [] };
      }
      const inventoryItem = deepGet(snapshot, ['rootData', 'char', safeCharKey, '鑳屽寘', safeItemName], null);
      const currentQty = Math.max(0, Math.floor(toNumber(inventoryItem && inventoryItem['鏁伴噺'], 0)));
      if (currentQty < consumeCount) {
        return { ok: false, reason: `缂哄皯銆?{safeItemName}銆戙€俙, patchOps: [] };
      }
      const itemPath = `/char/${escapeJsonPointerValue(safeCharKey)}/鑳屽寘/${escapeJsonPointerValue(safeItemName)}`;
      if (currentQty === consumeCount) {
        return {
          ok: true,
          patchOps: [{ op: 'remove', path: itemPath }],
        };
      }
      return {
        ok: true,
        patchOps: [{ op: 'replace', path: `${itemPath}/鏁伴噺`, value: currentQty - consumeCount }],
      };
    }

    function getDefaultSoulTowerChallengeFloor(snapshot) {
      const activeChar = getActiveSnapshotCharacter(snapshot);
      return Math.min(
        SOUL_TOWER_TOTAL_FLOORS,
        Math.max(1, toNumber(deepGet(activeChar, '榄傜伒濉旇褰?鏈€楂樺眰', 0), 0) + 1),
      );
    }

    function 瑙ｆ瀽鍗囩伒鍙版。浣?闂ㄧエ鍚?= '') {
      const safeName = toText(闂ㄧエ鍚? '');
      if (/楂樼骇/.test(safeName)) return '楂樼骇';
      if (/涓骇/.test(safeName)) return '涓骇';
      return '鍒濈骇';
    }

    function 鏄惁鏆村姩鏈熷崌鐏甸棬绁?闂ㄧエ鍚?= '') {
      return /鏆村姩鏈?.test(toText(闂ㄧエ鍚? ''));
    }

    function 鑾峰彇鍗囩伒鍙板己搴﹁寖鍥?妗ｄ綅 = '鍒濈骇', 鏆村姩鏈?= false) {
      const tier = toText(妗ｄ綅, '鍒濈骇');
      const baseMap = {
        鍒濈骇: { 鏈€灏忓勾闄? 100, 鏈€澶у勾闄? 3000, 鏈€灏忕瓑绾? 20, 鏈€澶х瓑绾? 42 },
        涓骇: { 鏈€灏忓勾闄? 3000, 鏈€澶у勾闄? 20000, 鏈€灏忕瓑绾? 42, 鏈€澶х瓑绾? 75 },
        楂樼骇: { 鏈€灏忓勾闄? 20000, 鏈€澶у勾闄? 100000, 鏈€灏忕瓑绾? 75, 鏈€澶х瓑绾? 95 },
      };
      const base = baseMap[tier] || baseMap.鍒濈骇;
      if (!鏆村姩鏈? return cloneJsonValue(base, base);
      return {
        鏈€灏忓勾闄? Math.max(1, Math.floor(base.鏈€灏忓勾闄?* 0.95)),
        鏈€澶у勾闄? Math.max(1, Math.floor(base.鏈€澶у勾闄?* 1.12)),
        鏈€灏忕瓑绾? Math.max(1, Math.floor(base.鏈€灏忕瓑绾?* 0.98)),
        鏈€澶х瓑绾? Math.max(base.鏈€灏忕瓑绾? Math.min(99, Math.ceil(base.鏈€澶х瓑绾?* 1.05))),
      };
    }

    function 瑙勮寖鍖栬瘯鐐肩姸鎬佷笂涓嬫枃(raw = {}) {
      if (!raw || typeof raw !== 'object' || Array.isArray(raw)) return null;
      const 璇曠偧绫诲瀷 = toText(raw.璇曠偧绫诲瀷, '').trim();
      if (!['鍗囩伒鍙?, '榄傜伒濉?].includes(璇曠偧绫诲瀷)) return null;
      if (璇曠偧绫诲瀷 === '鍗囩伒鍙?) {
        const 妗ｄ綅 = 瑙ｆ瀽鍗囩伒鍙版。浣?raw.妗ｄ綅 || raw.闂ㄧエ鍚?|| '');
        const 鏆村姩鏈?= raw.鏆村姩鏈?=== true || 鏄惁鏆村姩鏈熷崌鐏甸棬绁?raw.闂ㄧエ鍚?|| raw.璇曠偧鍐呭湴鐐?|| '');
        const 闂ㄧエ鍚?= toText(raw.闂ㄧエ鍚? `${鏆村姩鏈?? '鏆村姩鏈? : ''}${妗ｄ綅}鍗囩伒鍙伴棬绁╜).trim();
        const 鍏ュ満鍦扮偣 = toText(raw.鍏ュ満鍦扮偣, 鍗囩伒鍙伴€€鍑哄湴鐐?.trim() || 鍗囩伒鍙伴€€鍑哄湴鐐?
        const 璇曠偧鍐呭湴鐐?= toText(raw.璇曠偧鍐呭湴鐐? 鏋勫缓鍗囩伒鍙拌瘯鐐煎湴鐐?妗ｄ綅, 鏆村姩鏈?).trim() || 鏋勫缓鍗囩伒鍙拌瘯鐐煎湴鐐?妗ｄ綅, 鏆村姩鏈?;
        return {
          璇曠偧绫诲瀷: '鍗囩伒鍙?,
          闂ㄧエ鍚?
          妗ｄ綅,
          鏆村姩鏈?
          鍏ュ満鍦扮偣,
          褰撳墠灞? 0,
          璇曠偧鍐呭湴鐐?
        };
      }
      const 褰撳墠灞?= Math.min(
        SOUL_TOWER_TOTAL_FLOORS,
        Math.max(1, Math.floor(toNumber(raw.褰撳墠灞? 1))),
      );
      const 鍏ュ満鍦扮偣 = toText(raw.鍏ュ満鍦扮偣, 榄傜伒濉旈€€鍑哄湴鐐?.trim() || 榄傜伒濉旈€€鍑哄湴鐐?
      const 闂ㄧエ鍚?= toText(raw.闂ㄧエ鍚? '榄傜伒濉旈棬绁?).trim() || '榄傜伒濉旈棬绁?;
      const 璇曠偧鍐呭湴鐐?= toText(raw.璇曠偧鍐呭湴鐐? 鏋勫缓榄傜伒濉旇瘯鐐煎湴鐐?褰撳墠灞?).trim() || 鏋勫缓榄傜伒濉旇瘯鐐煎湴鐐?褰撳墠灞?;
      return {
        璇曠偧绫诲瀷: '榄傜伒濉?,
        闂ㄧエ鍚?
        妗ｄ綅: '榄傜伒濉?,
        鏆村姩鏈? false,
        鍏ュ満鍦扮偣,
        褰撳墠灞?
        璇曠偧鍐呭湴鐐?
      };
    }

    function 璇诲彇璇曠偧鐘舵€佷笂涓嬫枃(snapshot) {
      return 瑙勮寖鍖栬瘯鐐肩姸鎬佷笂涓嬫枃(瑙ｆ瀽璇曠偧鍦扮偣(璇诲彇璇曠偧鐘舵€佹枃鏈?snapshot)));
    }

    function 鏄惁璇曠偧鐘舵€佸彲缁х画(context = null) {
      return !!瑙勮寖鍖栬瘯鐐肩姸鎬佷笂涓嬫枃(context);
    }

    function 鏋勫缓璇曠偧鐘舵€佷笂涓嬫枃(options = {}) {
      const 璇曠偧绫诲瀷 = toText(options.璇曠偧绫诲瀷, '').trim();
      if (璇曠偧绫诲瀷 === '鍗囩伒鍙?) {
        const 闂ㄧエ鍚?= toText(options.闂ㄧエ鍚? '').trim();
        const 妗ｄ綅 = 瑙ｆ瀽鍗囩伒鍙版。浣?闂ㄧエ鍚?|| options.妗ｄ綅 || '鍒濈骇');
        const 鏆村姩鏈?= 鏄惁鏆村姩鏈熷崌鐏甸棬绁?闂ㄧエ鍚? || options.鏆村姩鏈?=== true;
        return {
          璇曠偧绫诲瀷: '鍗囩伒鍙?,
          闂ㄧエ鍚? 闂ㄧエ鍚?|| `${鏆村姩鏈?? '鏆村姩鏈? : ''}${妗ｄ綅}鍗囩伒鍙伴棬绁╜,
          妗ｄ綅,
          鏆村姩鏈?
          鍏ュ満鍦扮偣: toText(options.鍏ュ満鍦扮偣, 鍗囩伒鍙伴€€鍑哄湴鐐?.trim() || 鍗囩伒鍙伴€€鍑哄湴鐐?
          褰撳墠灞? 0,
          璇曠偧鍐呭湴鐐? 鏋勫缓鍗囩伒鍙拌瘯鐐煎湴鐐?妗ｄ綅, 鏆村姩鏈?,
        };
      }
      const 褰撳墠灞?= Math.min(
        SOUL_TOWER_TOTAL_FLOORS,
        Math.max(1, Math.floor(toNumber(options.褰撳墠灞? 1))),
      );
      return {
        璇曠偧绫诲瀷: '榄傜伒濉?,
        闂ㄧエ鍚? toText(options.闂ㄧエ鍚? '榄傜伒濉旈棬绁?).trim() || '榄傜伒濉旈棬绁?,
        妗ｄ綅: '榄傜伒濉?,
        鏆村姩鏈? false,
        鍏ュ満鍦扮偣: toText(options.鍏ュ満鍦扮偣, 榄傜伒濉旈€€鍑哄湴鐐?.trim() || 榄傜伒濉旈€€鍑哄湴鐐?
        褰撳墠灞?
        璇曠偧鍐呭湴鐐? 鏋勫缓榄傜伒濉旇瘯鐐煎湴鐐?褰撳墠灞?,
      };
    }

    function 鐢熸垚鍗囩伒鍙版垬鏂楀弬鏁?snapshot, context = {}) {
      const safeContext = 瑙勮寖鍖栬瘯鐐肩姸鎬佷笂涓嬫枃(context) || 鏋勫缓璇曠偧鐘舵€佷笂涓嬫枃({ 璇曠偧绫诲瀷: '鍗囩伒鍙? });
      const 寮哄害鑼冨洿 = 鑾峰彇鍗囩伒鍙板己搴﹁寖鍥?safeContext.妗ｄ綅, safeContext.鏆村姩鏈?;
      const seedText = [
        safeContext.闂ㄧエ鍚?
        safeContext.妗ｄ綅,
        safeContext.鏆村姩鏈?? '鏆村姩鏈? : '甯歌',
        safeContext.鍏ュ満鍦扮偣,
        toText(snapshot && snapshot.activeName, '鐜╁'),
      ].join('|');
      const 缁撴灉 = withTemporaryBattleRandom(seedText, rng => {
        const 瀵规墜绫诲瀷 = pickTemporaryBattleItem(
          safeContext.鏆村姩鏈?            ? ['榄傚吔', '榄傚笀', '榄傚吔', '榄傚笀']
            : ['榄傚吔', '榄傚吔', '榄傚笀', '榄傚吔'],
          rng,
        ) || '榄傚吔';
        const 鐧惧垎浣?= Math.max(0, Math.min(1, Number(rng())));
        if (瀵规墜绫诲瀷 === '榄傚笀') {
          const 绛夌骇 = pickTemporaryBattleInt(寮哄害鑼冨洿.鏈€灏忕瓑绾? 寮哄害鑼冨洿.鏈€澶х瓑绾? rng);
          const 瀵瑰簲骞撮檺 = Math.max(
            寮哄害鑼冨洿.鏈€灏忓勾闄?
            Math.min(
              寮哄害鑼冨洿.鏈€澶у勾闄?
              Math.floor(寮哄害鑼冨洿.鏈€灏忓勾闄?+ (寮哄害鑼冨洿.鏈€澶у勾闄?- 寮哄害鑼冨洿.鏈€灏忓勾闄? * 鐧惧垎浣?,
            ),
          );
          return {
            瀵规墜绉嶅瓙: {
              name: `${safeContext.妗ｄ綅}${safeContext.鏆村姩鏈?? '鏆村姩鏈? : ''}璇曠偧榄傚笀`,
              鍗曚綅鎬ц川: '浜虹被',
              韬唤: '榄傚笀',
              绛夌骇,
              鏁伴噺: 1,
            },
            瀵规墜绫诲瀷: '榄傚笀',
            瀵瑰簲骞撮檺,
            鐧惧垎浣?
          };
        }
        const 榄傚吔鐗╃ = pickTemporaryBattleItem(BATTLE_SOUL_BEAST_STANDARD_SPECIES, rng) || '榫欑被';
        const 鍝佽川鏄犲皠 = safeContext.妗ｄ綅 === '楂樼骇'
          ? ['A', 'S']
          : safeContext.妗ｄ綅 === '涓骇'
            ? ['B', 'A']
            : ['C', 'B'];
        const 鍝佽川 = pickTemporaryBattleItem(鍝佽川鏄犲皠, rng) || 鍝佽川鏄犲皠[0] || 'C';
        const 骞撮檺 = pickTemporaryBattleInt(寮哄害鑼冨洿.鏈€灏忓勾闄? 寮哄害鑼冨洿.鏈€澶у勾闄? rng);
        return {
          瀵规墜绉嶅瓙: {
            name: `${榄傚吔鐗╃}鎷熸€侀瓊鍏絗,
            鍗曚綅鎬ц川: '榄傚吔',
            鏍囧噯鐗╃: 榄傚吔鐗╃,
            骞撮檺,
            鍝佽川,
            鏁伴噺: 1,
          },
          瀵规墜绫诲瀷: '榄傚吔',
          瀵瑰簲骞撮檺: 骞撮檺,
          鐧惧垎浣?
        };
      });
      return {
        瀵规墜绉嶅瓙: 缁撴灉.瀵规墜绉嶅瓙,
        瀵规墜绫诲瀷: 缁撴灉.瀵规墜绫诲瀷,
        killedAge: Math.max(1, Math.floor(toNumber(缁撴灉.瀵瑰簲骞撮檺, 寮哄害鑼冨洿.鏈€灏忓勾闄?)),
        鐧惧垎浣? Math.max(0, Math.min(1, toNumber(缁撴灉.鐧惧垎浣? 0))),
        寮哄害鑼冨洿,
      };
    }

    function 鏋勫缓榄傜伒濉斿畧濉旂瀛?context = {}) {
      const safeContext = 瑙勮寖鍖栬瘯鐐肩姸鎬佷笂涓嬫枃(context) || 鏋勫缓璇曠偧鐘舵€佷笂涓嬫枃({ 璇曠偧绫诲瀷: '榄傜伒濉?, 褰撳墠灞? 1 });
      const floor = Math.min(SOUL_TOWER_TOTAL_FLOORS, Math.max(1, Math.floor(toNumber(safeContext.褰撳墠灞? 1))));
      const seedText = `${safeContext.鍏ュ満鍦扮偣}|${safeContext.闂ㄧエ鍚峿|tower|${floor}`;
      return withTemporaryBattleRandom(seedText, rng => buildSoulTowerGuardianSeed(floor, { rng }));
    }

    function 鏋勫缓璇曠偧鍏ュ満绯荤粺鎻愮ず(snapshot, context = {}, activeDisplayName = '') {
      const safeContext = 瑙勮寖鍖栬瘯鐐肩姸鎬佷笂涓嬫枃(context) || {};
      const 瑙掕壊鍚?= toText(activeDisplayName, toText(snapshot && snapshot.activeName, '褰撳墠瑙掕壊'));
      const 鍏ュ満鍦扮偣 = toText(safeContext.鍏ュ満鍦扮偣, toText(snapshot && snapshot.currentLoc, '鏈煡鍦扮偣'));
      if (safeContext.璇曠偧绫诲瀷 === '鍗囩伒鍙?) {
        const 寮哄害鑼冨洿 = 鑾峰彇鍗囩伒鍙板己搴﹁寖鍥?safeContext.妗ｄ綅, safeContext.鏆村姩鏈?;
        return `浠ヤ笅鍐呭灞炰簬鍓嶇浠ｅ彂鐨勫崌鐏靛彴鍏ュ満璇锋眰锛岃鐩存帴鎵挎帴鍓ф儏鎺ㄨ繘锛屼笉瑕佽緭鍑?JSON 鍧楁垨鍙橀噺缁存姢鎸囦护銆?[鍏ュ満淇℃伅]
瑙掕壊锛?{瑙掕壊鍚峿
鍦扮偣锛?{鍏ュ満鍦扮偣}
闂ㄧエ锛?{toText(safeContext.闂ㄧエ鍚? '鍗囩伒鍙伴棬绁?)}
妗ｄ綅锛?{toText(safeContext.妗ｄ綅, '鍒濈骇')}
鏆村姩鏈燂細${safeContext.鏆村姩鏈?? '鏄? : '鍚?}
[寮哄害绾︽潫]
鏈満娼滃湪瀵规墜鍙互鏄瓊鍏芥垨榄傚笀锛屾垬鍔涘繀椤婚檺鍒跺湪璇ユ。浣嶅尯闂达細
- 瀵瑰簲骞撮檺鍖洪棿锛?{formatNumber(寮哄害鑼冨洿.鏈€灏忓勾闄?}~${formatNumber(寮哄害鑼冨洿.鏈€澶у勾闄?}
- 瀵瑰簲榄傚笀绛夌骇鍖洪棿锛?{寮哄害鑼冨洿.鏈€灏忕瓑绾~${寮哄害鑼冨洿.鏈€澶х瓑绾
闈炴毚鍔ㄦ湡榄傚笀鑳滃埄涓嶆彁渚涘崌鐏靛鍔憋紱鏆村姩鏈熼瓊甯堣儨鍒╁彲鎻愪緵闅忔満鍗囩伒濂栧姳銆?[娴佺▼瑕佹眰]
鏈疆鍏堟弿鍐欏叆鍦轰笌璇曠偧姘涘洿鎺ㄨ繘锛屽叆鍦轰笉绛変簬鐩存帴寮€鎴樸€傚彧鏈夊綋鍓ф儏鎺ㄨ繘鍒版槑纭氦閿嬭妭鐐规椂锛屾墠杈撳嚭 battle 妯″潡鎰忓浘瑙﹀彂鎴樻枟銆俙;
      }
      const floor = Math.min(SOUL_TOWER_TOTAL_FLOORS, Math.max(1, Math.floor(toNumber(safeContext.褰撳墠灞? 1))));
      const gateMeta = getSoulTowerGateMeta(floor);
      return `浠ヤ笅鍐呭灞炰簬鍓嶇浠ｅ彂鐨勯瓊鐏靛鍏ュ満璇锋眰锛岃鐩存帴鎵挎帴鍓ф儏鎺ㄨ繘锛屼笉瑕佽緭鍑?JSON 鍧楁垨鍙橀噺缁存姢鎸囦护銆?[鍏ュ満淇℃伅]
瑙掕壊锛?{瑙掕壊鍚峿
鍦扮偣锛?{鍏ュ満鍦扮偣}
闂ㄧエ锛?{toText(safeContext.闂ㄧエ鍚? '榄傜伒濉旈棬绁?)}
褰撳墠灞傦細绗?{floor}灞傦紙${gateMeta.gateLabel}锛?{gateMeta.gateRangeLabel}锛?[寮哄害绾︽潫]
瀹堝瀵硅薄寮哄害蹇呴』鍖归厤绗?{floor}灞傝鍒欙紝涓嶅緱瓒婄骇婕傜Щ銆?[娴佺▼瑕佹眰]
鏈疆鍏堝啓鍏ュ鍚庣殑鍓ф儏涓庡宄欐帹杩涳紝鍏ュ涓嶇瓑浜庣珛鍗冲紑鎴樸€傚彧鏈夊湪鍓ф儏鏄庣‘杩涘叆鎴樻枟鑺傜偣鏃讹紝鎵嶈緭鍑?battle 妯″潡鎰忓浘銆俙;
    }

    async function 鐧昏璇曠偧鍏ュ満骞舵帹杩涘墽鎯?snapshot, options = {}) {
      const sourceSnapshot = snapshot && snapshot.rootData ? snapshot : (liveSnapshot || lastRenderableSnapshot);
      if (!sourceSnapshot || !sourceSnapshot.rootData) throw new Error('褰撳墠蹇収鏈氨缁紝鏃犳硶鐧昏璇曠偧鍏ュ満銆?);
      if (!isSnapshotPlayerControlled(sourceSnapshot)) throw new Error('鏃佽瑙嗚涓嶅彲鎿嶄綔銆?);
      if (deepGet(sourceSnapshot, 'rootData.world.鎴樻枟.杩涜涓?, false)) {
        throw new Error('褰撳墠浠嶅湪鎴樻枟涓紝鏃犳硶鐧昏鏂扮殑璇曠偧鍏ュ満銆?);
      }
      const 褰撳墠璇曠偧 = 璇诲彇璇曠偧鐘舵€佷笂涓嬫枃(sourceSnapshot);
      if (褰撳墠璇曠偧) {
        throw new Error(`褰撳墠浠嶅湪${褰撳墠璇曠偧.璇曠偧绫诲瀷}娴佺▼涓紝璇峰厛缁撴潫鍚庡啀杩涘叆鏂拌瘯鐐笺€俙);
      }
      const 璇曠偧绫诲瀷 = toText(options.璇曠偧绫诲瀷, '').trim();
      if (!['鍗囩伒鍙?, '榄傜伒濉?].includes(璇曠偧绫诲瀷)) throw new Error('璇曠偧绫诲瀷鏃犳晥銆?);
      const activeCharKey = resolveSnapshotCharKey(sourceSnapshot, toText(sourceSnapshot.activeName, '')) || toText(sourceSnapshot.activeName, '');
      if (!activeCharKey) throw new Error('缂哄皯瑙掕壊淇℃伅銆?);
      const entryLocation = toText(options.entryLocation, 璇诲彇褰撳墠瑙掕壊浣嶇疆(sourceSnapshot)).trim();
      if (鏄惁璇曠偧鍐呭湴鐐?entryLocation)) throw new Error('褰撳墠浠嶅湪璇曠偧鍖哄煙锛屾棤娉曢噸澶嶅叆鍦恒€?);
      let selectedTicket = '';
      let trialContext = null;
      if (璇曠偧绫诲瀷 === '鍗囩伒鍙?) {
        if (!isTransmissionTowerEntryLocation(entryLocation)) throw new Error('鍗囩伒鍙板彧鑳戒粠浼犵伒濉斿叆鍙ｈ繘鍏ャ€?);
        const ascensionTickets = listAscensionTicketNames(sourceSnapshot);
        selectedTicket = toText(options.ticketName, ascensionTickets[0] || '').trim();
        if (!selectedTicket) throw new Error('缂哄皯鍗囩伒鍙伴棬绁ㄣ€?);
        trialContext = 鏋勫缓璇曠偧鐘舵€佷笂涓嬫枃({
          璇曠偧绫诲瀷,
          闂ㄧエ鍚? selectedTicket,
          鍏ュ満鍦扮偣: entryLocation,
        });
      } else {
        if (!isSoulTowerHeadquartersLocation(entryLocation)) throw new Error('榄傜伒濉斿彧鑳戒粠鍙茶幈鍏嬪煄浼犵伒濉旀€婚儴杩涘叆銆?);
        const activeChar = getActiveSnapshotCharacter(sourceSnapshot);
        if (!isSoulTowerEligibleCharacterData(activeChar)) throw new Error('褰撳墠瑙掕壊涓嶆弧瓒抽瓊鐏靛鍏ュ鏉′欢銆?);
        const soulTowerTickets = listSoulTowerTicketNames(sourceSnapshot);
        selectedTicket = toText(options.ticketName, soulTowerTickets[0] || '').trim();
        if (!selectedTicket) throw new Error('缂哄皯榄傜伒濉旈棬绁ㄣ€?);
        trialContext = 鏋勫缓璇曠偧鐘舵€佷笂涓嬫枃({
          璇曠偧绫诲瀷,
          闂ㄧエ鍚? selectedTicket,
          鍏ュ満鍦扮偣: entryLocation,
          褰撳墠灞? Math.min(
            SOUL_TOWER_TOTAL_FLOORS,
            Math.max(1, Math.floor(toNumber(options.floor, getDefaultSoulTowerChallengeFloor(sourceSnapshot)))),
          ),
        });
      }
      const ticketConsumeResult = buildInventoryConsumePatches(sourceSnapshot, activeCharKey, selectedTicket, 1);
      if (!ticketConsumeResult.ok) throw new Error(ticketConsumeResult.reason || `缂哄皯銆?{selectedTicket}銆戦棬绁ㄣ€俙);
      const 璇曠偧鍐呭湴鐐?= trialContext.璇曠偧绫诲瀷 === '榄傜伒濉?
        ? 鏋勫缓榄傜伒濉旇瘯鐐煎湴鐐?trialContext.褰撳墠灞?
        : 鏋勫缓鍗囩伒鍙拌瘯鐐煎湴鐐?trialContext.妗ｄ綅, trialContext.鏆村姩鏈?;
      const 娓呯悊鍚庢垬鏂楁暟鎹?= (() => {
        const rawBattle = cloneJsonValue(deepGet(sourceSnapshot, 'rootData.world.鎴樻枟', {}), {});
        const nextBattle = rawBattle && typeof rawBattle === 'object' && !Array.isArray(rawBattle) ? rawBattle : {};
        nextBattle.璇曠偧鐘舵€?= trialContext.璇曠偧绫诲瀷 === '榄傜伒濉?
          ? 鏋勫缓榄傜伒濉旇瘯鐐煎湴鐐?trialContext.褰撳墠灞?
          : 鏋勫缓鍗囩伒鍙拌瘯鐐煎湴鐐?trialContext.妗ｄ綅, trialContext.鏆村姩鏈?;
        if (trialContext.璇曠偧绫诲瀷 === '榄傜伒濉?) {
          nextBattle.floor = trialContext.褰撳墠灞?
        }
        return nextBattle;
      })();
      const 鎾姤鍓嶇紑 = trialContext.璇曠偧绫诲瀷 === '鍗囩伒鍙? ? '鍗囩伒鍙? : '榄傜伒濉?;
      const 鎾姤姝ｆ枃 = trialContext.璇曠偧绫诲瀷 === '鍗囩伒鍙?
        ? `${toText(sourceSnapshot.activeName, activeCharKey)} 宸茶繘鍏?{trialContext.妗ｄ綅}${trialContext.鏆村姩鏈?? '鏆村姩鏈? : ''}鍗囩伒鍙帮紝绛夊緟鍓ф儏鎺ㄨ繘瑙﹀彂鎴樻枟銆俙
        : `${toText(sourceSnapshot.activeName, activeCharKey)} 宸茶繘鍏ラ瓊鐏靛绗?{trialContext.褰撳墠灞倉灞傦紝绛夊緟鍓ф儏鎺ㄨ繘瑙﹀彂鎴樻枟銆俙;
      await applyJsonPatchOpsByEditor([
        ...ticketConsumeResult.patchOps,
        { op: 'replace', path: '/world/鎴樻枟', value: 娓呯悊鍚庢垬鏂楁暟鎹?},
        { op: 'add', path: `/char/${escapeJsonPointerValue(activeCharKey)}/鐘舵€?浣嶇疆`, value: 璇曠偧鍐呭湴鐐?},
        { op: 'replace', path: '/sys/绯荤粺鎾姤', value: `[${鎾姤鍓嶇紑}] ${鎾姤姝ｆ枃}` },
      ], { force: true });
      await refreshLiveSnapshot({ force: true });
      const latestSnapshot = liveSnapshot || sourceSnapshot;
      const activeDisplayName = toText(deepGet(latestSnapshot, 'activeChar.name', latestSnapshot.activeName), latestSnapshot.activeName);
      const dispatchRequest = {
        playerInput: trialContext.璇曠偧绫诲瀷 === '鍗囩伒鍙?
          ? `鎴戣繘鍏ヤ簡${trialContext.妗ｄ綅}${trialContext.鏆村姩鏈?? '鏆村姩鏈? : ''}鍗囩伒鍙帮紝鍑嗗寮€濮嬫湰娆¤瘯鐐笺€俙
          : `鎴戣繘鍏ラ瓊鐏靛绗?{trialContext.褰撳墠灞倉灞傦紝鍑嗗寮€濮嬫湰灞傛寫鎴樸€俙,
        systemPrompt: 鏋勫缓璇曠偧鍏ュ満绯荤粺鎻愮ず(latestSnapshot, trialContext, activeDisplayName),
        requestKind: trialContext.璇曠偧绫诲瀷 === '鍗囩伒鍙? ? 'trial_entry_ascension' : 'trial_entry_tower',
      };
      const dispatchResult = await dispatchUiAiRequest(
        dispatchRequest.playerInput,
        dispatchRequest.systemPrompt,
        { requestKind: dispatchRequest.requestKind },
      );
      if (!dispatchResult?.ok) {
        throw new Error('鍏ュ満鐧昏鎴愬姛锛屼絾鍓ф儏鎺ㄨ繘瑙﹀彂澶辫触锛岃閲嶈瘯鎺ㄨ繘銆?);
      }
      return {
        ok: true,
        context: trialContext,
        requestKind: dispatchRequest.requestKind,
        dispatchResult,
      };
    }

    function splitSoulSpiritTargetValue(value) {
      const raw = toText(value, '').trim();
      if (!raw || !raw.includes('::')) return { spiritKey: '', soulSpiritKey: '' };
      const [spiritKey, soulSpiritKey] = raw.split('::');
      return {
        spiritKey: toText(spiritKey, '').trim(),
        soulSpiritKey: toText(soulSpiritKey, '').trim(),
      };
    }

    function getTrialRingColorByAge(age = 0) {
      const value = Math.max(0, Math.floor(Number(age) || 0));
      if (value >= 100000) return '绾?;
      if (value >= 10000) return '榛?;
      if (value >= 1000) return '绱?;
      if (value >= 100) return '榛?;
      return '鐧?;
    }

    async ) {
      const safeCharKey = toText(charKey, '').trim();
      const ticketName = toText(payload.ticketName, '').trim();
      const targetValue = toText(payload.targetValue, '').trim();
      const gainAge = Math.max(0, Math.floor(toNumber(payload.gainAge, 0)));
      if (!safeCharKey) throw new Error('缂哄皯瑙掕壊淇℃伅銆?);
      if (!ticketName) throw new Error('璇烽€夋嫨鍗囩伒鍙伴棬绁ㄣ€?);
      if (!targetValue) throw new Error('璇烽€夋嫨鐩爣榄傜伒銆?);
      if (gainAge <= 0) throw new Error('璇疯緭鍏ユ湁鏁堢殑鎻愬崌骞撮檺銆?);
      const { spiritKey, soulSpiritKey } = splitSoulSpiritTargetValue(targetValue);
      if (!spiritKey || !soulSpiritKey) throw new Error('鐩爣榄傜伒鏍煎紡鏃犳晥銆?);

      await mutateStatDataByEditor(statData => {
        const charData = deepGet(statData, ['char', safeCharKey], null);
        if (!charData || typeof charData !== 'object') throw new Error(`鎵句笉鍒拌鑹层€?{safeCharKey}銆戙€俙);
        const inventory = charData.鑳屽寘 && typeof charData.鑳屽寘 === 'object' ? charData.鑳屽寘 : null;
        if (!inventory || !inventory[ticketName] || toNumber(inventory[ticketName]['鏁伴噺'], 0) < 1) {
          throw new Error(`缂哄皯銆?{ticketName}銆戦棬绁ㄣ€俙);
        }
        const spiritData = deepGet(charData, ['姝﹂瓊', spiritKey], null);
        const soulSpirit = deepGet(charData, ['姝﹂瓊', spiritKey, '榄傜伒', soulSpiritKey], null);
        if (!spiritData || !soulSpirit || typeof soulSpirit !== 'object') {
          throw new Error('鏈壘鍒板彲鍗囩伒鐨勭洰鏍囬瓊鐏点€?);
        }

        inventory[ticketName]['鏁伴噺'] = Math.max(0, toNumber(inventory[ticketName]['鏁伴噺'], 0) - 1);
        if (inventory[ticketName]['鏁伴噺'] <= 0) delete inventory[ticketName];

        const oldAge = Math.max(0, Math.floor(toNumber(soulSpirit['骞撮檺'], 0)));
        const newAge = oldAge + gainAge;
        soulSpirit['骞撮檺'] = newAge;
        if (soulSpirit.榄傜幆 && typeof soulSpirit.榄傜幆 === 'object') {
          Object.values(soulSpirit.榄傜幆).forEach(ringData => {
            if (!ringData || typeof ringData !== 'object') return;
            ringData['骞撮檺'] = newAge;
            if (!String(ringData['棰滆壊'] || '').trim()) ringData['棰滆壊'] = getTrialRingColorByAge(newAge);
          });
        }

        if (!statData.sys || typeof statData.sys !== 'object') statData.sys = {};
        statData.sys.绯荤粺鎾姤 = `[鍗囩伒鍙癩 ${safeCharKey} 浣跨敤銆?{ticketName}銆戜负銆?{spiritKey}/${soulSpiritKey}銆戝畬鎴愬崌鐏碉紝骞撮檺鐢?${oldAge} 鎻愬崌鑷?${newAge}锛?${gainAge}锛夈€俙;
      });
    }

    async ) {
      const safeCharKey = toText(charKey, '').trim();
      const clearedFloor = Math.min(SOUL_TOWER_TOTAL_FLOORS, Math.max(0, Math.floor(toNumber(payload.floor, 0))));
      const actionText = toText(payload.action, '鍐插').trim() || '鍐插';
      if (!safeCharKey) throw new Error('缂哄皯瑙掕壊淇℃伅銆?);
      if (clearedFloor <= 0) throw new Error('璇疯緭鍏ユ湁鏁堢殑閫氬叧灞傛暟銆?);
      let blockedMessage = '';
      const gateMeta = getSoulTowerGateMeta(clearedFloor);

      await mutateStatDataByEditor(statData => {
        const charData = deepGet(statData, ['char', safeCharKey], null);
        if (!charData || typeof charData !== 'object') throw new Error(`鎵句笉鍒拌鑹层€?{safeCharKey}銆戙€俙);
        if (!isSoulTowerEligibleCharacterData(charData)) {
          delete charData['榄傜伒濉旇褰?];
          const ageValue = getSoulTowerAgeValueFromCharacter(charData);
          blockedMessage = Number.isFinite(ageValue)
            ? `${safeCharKey} 宸茶秴杩?${SOUL_TOWER_MAX_AGE} 宀侊紝涓嶈兘鐧昏榄傜伒濉斻€俙
            : `${safeCharKey} 骞撮緞鏈褰曪紝涓嶈兘鐧昏榄傜伒濉斻€俙;
          if (!statData.sys || typeof statData.sys !== 'object') statData.sys = {};
          statData.sys.绯荤粺鎾姤 = `[榄傜伒濉擼 ${blockedMessage.replace('涓嶈兘鐧昏榄傜伒濉斻€?, '榄傜伒濉旇褰曞凡绉婚櫎銆?)}`;
          return;
        }
        const activeBattleTeam = buildSoulTowerTeamMembersFromStatBattle(statData, safeCharKey);
        if (Array.isArray(activeBattleTeam) && activeBattleTeam.length) {
          const teamCheck = buildSoulTowerTeamValidationResult(activeBattleTeam);
          if (!teamCheck.ok) {
            blockedMessage = teamCheck.message;
            if (!statData.sys || typeof statData.sys !== 'object') statData.sys = {};
            statData.sys.绯荤粺鎾姤 = `[榄傜伒濉擼 ${blockedMessage}`;
            return;
          }
        }
        if (!charData['榄傜伒濉旇褰?] || typeof charData['榄傜伒濉旇褰?] !== 'object') {
          deepSetMutable(statData, ['char', safeCharKey, '榄傜伒濉旇褰?], {
            鏈€楂樺眰: 0,
            褰撳墠浜旀姌榄傜伒: createEmptySoulTowerDiscountSpiritRecord(),
          });
        }
        const towerRecord = deepGet(statData, ['char', safeCharKey, '榄傜伒濉旇褰?], {});
        towerRecord['褰撳墠浜旀姌榄傜伒'] = normalizeSoulTowerDiscountSpiritRecord(towerRecord['褰撳墠浜旀姌榄傜伒']);

        const oldMaxFloor = Math.max(0, Math.floor(toNumber(towerRecord['鏈€楂樺眰'], 0)));
        const newMaxFloor = Math.max(oldMaxFloor, clearedFloor);
        towerRecord['鏈€楂樺眰'] = newMaxFloor;
        towerRecord['褰撳墠浜旀姌榄傜伒'] = buildSoulTowerDiscountSpiritRecordFromSeed(
          clearedFloor,
          buildSoulTowerGuardianSeed(clearedFloor),
        );

        if (!statData.sys || typeof statData.sys !== 'object') statData.sys = {};
        statData.sys.绯荤粺鎾姤 = clearedFloor > oldMaxFloor
          ? `[榄傜伒濉擼 ${safeCharKey} 鏈${actionText}杈惧埌${gateMeta.gateLabel}绗?${clearedFloor} 灞傦紝鍒锋柊鍘嗗彶鏈€楂樺眰鏁帮紙${oldMaxFloor} -> ${newMaxFloor}锛夛紝骞舵洿鏂板綋鍓嶄簲鎶樼洰鏍囦负 ${buildSoulTowerDiscountSpiritDisplay(towerRecord['褰撳墠浜旀姌榄傜伒'])}銆俙
          : `[榄傜伒濉擼 ${safeCharKey} 鏈${actionText}杈惧埌${gateMeta.gateLabel}绗?${clearedFloor} 灞傦紝鏈埛鏂板巻鍙叉渶楂樺眰鏁帮紙褰撳墠鏈€楂?${oldMaxFloor} 灞傦級锛屽綋鍓嶄簲鎶樼洰鏍囨洿鏂颁负 ${buildSoulTowerDiscountSpiritDisplay(towerRecord['褰撳墠浜旀姌榄傜伒'])}銆俙;
      });
      if (blockedMessage) throw new Error(blockedMessage);
    }

    function getDonateBasePrice(itemName) {
      const safeName = toText(itemName, '');
      if (/澶╅敾|鍥涘瓧|绾㈢骇|鍗佷竾骞?.test(safeName)) return 100000000;
      if (/榄傞敾|涓夊瓧|榛戠骇|涓囧勾/.test(safeName)) return 10000000;
      if (/鐏甸敾|浜屽瓧|绱骇|鍗冨勾/.test(safeName)) return 1000000;
      if (/鍗冮敾|涓€瀛梶榛勭骇|鐧惧勾/.test(safeName)) return 100000;
      return 10000;
    }

    function getDonateTalentMultiplier(talentTier) {
      return {
        '缁濅笘濡栧': 2.0,
        '椤剁骇澶╂墠': 1.8,
        '澶╂墠': 1.5,
        '浼樼': 1.2,
        '姝ｅ父': 1.0,
        '鍔ｇ瓑': 0.5,
      }[toText(talentTier, '')] || 1.0;
    }

    function getDonateRewardLabel(targetFaction) {
      if (targetFaction === '鍙茶幈鍏嬪闄?) return '瀛﹂櫌绉垎';
      if (targetFaction === '鎴樼娈? || targetFaction === '琛€绁炲啗鍥?) return '鎴樺姛';
      return '鍞愰棬绉垎';
    }

    function buildDonateDispatchRequest(snapshot, options = {}) {
      if (!snapshot || !snapshot.rootData) return null;
      if (!isSnapshotPlayerControlled(snapshot)) return null;
      const activeKey = resolveSnapshotCharKey(snapshot, toText(snapshot.activeName, ''));
      if (!activeKey) return null;
      const itemName = toText(options.itemName, '').trim();
      const targetFaction = toText(options.targetFaction, '').trim();
      const quantity = Math.max(1, Math.floor(toNumber(options.quantity, 1)));
      if (!itemName || !targetFaction || itemName === '鏃? || targetFaction === '鏃?) return null;
      const ownedCount = toNumber(deepGet(snapshot, ['activeChar', '鑳屽寘', itemName, '鏁伴噺'], 0), 0);
      if (ownedCount < quantity) return null;
      const chars = deepGet(snapshot, 'rootData.char', {});
      const activeChar = chars && typeof chars === 'object' ? (chars[activeKey] || {}) : {};
      const activeName = toText(activeChar && (activeChar.name || deepGet(activeChar, 'base.name', '')), toText(snapshot.activeName, activeKey));
      const currentLoc = toText(deepGet(activeChar, '鐘舵€?浣嶇疆', snapshot.currentLoc || '褰撳墠浣嶇疆'), snapshot.currentLoc || '褰撳墠浣嶇疆').replace(/^鏂楃綏澶ч檰-/, '').replace(/^鏂楃伒澶ч檰-/, '');
      const talentTier = toText(deepGet(activeChar, '灞炴€?talent_tier', '姝ｅ父'), '姝ｅ父');
      const basePrice = getDonateBasePrice(itemName);
      const talentMultiplier = getDonateTalentMultiplier(talentTier);
      const totalValue = basePrice * quantity;
      const pointsEarned = Math.floor((totalValue / 1000) * talentMultiplier);
      const rewardLabel = getDonateRewardLabel(targetFaction);
      const systemPrompt = `浠ヤ笅鍐呭灞炰簬鍓嶇浠ｅ彂鐨勯樀钀ユ崘鐚姹傦紝璇风洿鎺ユ壙鎺ュ墽鎯呬笌鍚庣画澶勭悊锛屼笉瑕佽緭鍑?JSON 鍧楁垨鍙橀噺缁存姢鎸囦护銆?
[鎹愮尞鎽樿]
瑙掕壊锛?{activeName}
鍦扮偣锛?{currentLoc}
鐩爣鍔垮姏锛?{targetFaction}
鐗╁搧锛?{itemName}
鏁伴噺锛?{quantity}
褰撳墠搴撳瓨锛?{ownedCount}
鍩哄噯浼板€硷細${formatNumber(basePrice)} / 浠?鎬讳及鍊硷細${formatNumber(totalValue)}
澶╄祴妗ｄ綅锛?{talentTier}
澶╄祴绯绘暟锛?{talentMultiplier}
棰勮鑾峰緱锛?{formatNumber(pointsEarned)} ${rewardLabel}

璇风粨鍚堝綋鍓嶅湴鐐广€佽鑹叉墍灞炲娍鍔涘叧绯汇€佺墿璧勪环鍊间笌闃佃惀娴佺▼锛岃嚜鐒跺啓鍑烘崘鐚彈鐞嗐€佹牳楠屽拰鍙嶉锛涜嫢褰撳満鏃犳硶鍙楃悊锛屼篃璇锋槑纭粰鍑洪樆纰嶅師鍥犮€?鑻ユ崘鐚垚绔嬶紝璇锋寜浠ヤ笂浼板€肩粨鏋滃彂鏀?${formatNumber(pointsEarned)} ${rewardLabel}銆俙;
      return {
        playerInput: `鎴戞兂鍚戙€?{targetFaction}銆戞崘鐚€?{itemName}銆懨?{quantity}銆俙,
        systemPrompt,
        requestKind: 'faction_donate'
      };
    }

    function buildQuestDispatchRequest(snapshot, actionType, options = {}) {
      if (!snapshot || !snapshot.rootData) return null;
      if (!isSnapshotPlayerControlled(snapshot)) return null;
      const activeKey = resolveSnapshotCharKey(snapshot, toText(snapshot.activeName, ''));
      if (!activeKey) return null;
      const questName = toText(options.questName, '').trim();
      if (!questName || questName === '鏃?) return null;

      const chars = deepGet(snapshot, 'rootData.char', {});
      const activeChar = chars && typeof chars === 'object' ? (chars[activeKey] || {}) : {};
      const activeName = toText(activeChar && (activeChar.name || deepGet(activeChar, 'base.name', '')), toText(snapshot.activeName, activeKey));
      const currentLoc = toText(deepGet(activeChar, '鐘舵€?浣嶇疆', snapshot.currentLoc || '褰撳墠浣嶇疆'), snapshot.currentLoc || '褰撳墠浣嶇疆').replace(/^鏂楃綏澶ч檰-/, '').replace(/^鏂楃伒澶ч檰-/, '');
      const boardEntry = deepGet(snapshot, ['rootData', 'world', '濮旀墭鏉?, questName], null);
      const recordEntry = deepGet(snapshot, ['activeChar', '璁板綍', questName], null);
      const boardTitle = toText(boardEntry && boardEntry['鏍囬'], questName);
      const boardFrameDesc = toText(boardEntry && boardEntry['妗嗘灦鎻忚堪'], '鏃?);
      const boardHiddenDesc = toText(boardEntry && boardEntry['鎻忚堪'], boardFrameDesc);
      const boardStatus = toText(boardEntry && boardEntry['鐘舵€?], '寰呮帴鍙?);
      const recordStatus = toText(recordEntry && recordEntry['鐘舵€?], '杩涜涓?);

      let questAction = '';
      let questDesc = '';
      let requiredCount = 1;
      let rewardCoin = 0;
      let rewardRep = 0;
      let progressAdd = Math.max(1, Math.floor(toNumber(options.progressAdd, 1)));
      let statusAction = '浠诲姟澶勭悊涓?;
      let requestLabel = '';
      let analysisLabel = '';
      let playerInput = '';
      let extraRequirement = '';

      if (actionType === 'accept') {
        if (!boardEntry || boardStatus !== '寰呮帴鍙?) return null;
        questAction = '鎺ュ彇';
        questDesc = boardHiddenDesc !== '鏃? ? boardHiddenDesc : boardFrameDesc;
        requiredCount = Math.max(1, Math.floor(toNumber(boardEntry && boardEntry['鐩爣杩涘害'], 1)));
        rewardCoin = Math.max(0, Math.floor(toNumber(boardEntry && boardEntry['濂栧姳甯?], 0)));
        rewardRep = Math.max(0, Math.floor(toNumber(boardEntry && boardEntry['濂栧姳澹版湜'], 0)));
        statusAction = '鎺ュ彇濮旀墭';
        requestLabel = `[浠诲姟鎺ュ彇鐢宠] ${activeName} 鍦ㄣ€?{currentLoc}銆戝噯澶囨帴鍙栧鎵樸€?{boardTitle}銆戙€俙;
        analysisLabel = 'Quest accept request initialized from task panel.';
        playerInput = `鎴戞兂鎺ュ彇濮旀墭鏉夸笂鐨勩€?{boardTitle}銆戙€俙;
        extraRequirement = `杩欐槸涓€涓帴鍙栧墠鍙叕寮€妗嗘灦鐨勫鎵樸€備綘鐜板湪鍙互鍦ㄥ墽鎯呴噷姝ｅ父鎶湶璇︾粏鐩爣锛屼絾蹇呴』淇濈暀 浠诲姟璇锋眰.浠诲姟鎻忚堪 = ${questDesc}銆佺洰鏍囪繘搴?= ${requiredCount}銆佸鍔卞竵 = ${rewardCoin}銆佸鍔卞０鏈?= ${rewardRep}銆俙;
      } else if (actionType === 'submit') {
        if (!recordEntry || ['宸插畬鎴?, '宸叉斁寮?, '澶辫触', '宸插け璐?].includes(recordStatus)) return null;
        questAction = '鎻愪氦';
        questDesc = toText(recordEntry && recordEntry['鎻忚堪'], '鏃?);
        requiredCount = Math.max(1, Math.floor(toNumber(recordEntry && recordEntry['鐩爣杩涘害'], 1)));
        rewardCoin = Math.max(0, Math.floor(toNumber(recordEntry && recordEntry['濂栧姳甯?], 0)));
        rewardRep = Math.max(0, Math.floor(toNumber(recordEntry && recordEntry['濂栧姳澹版湜'], 0)));
        progressAdd = 0;
        statusAction = '鎻愪氦浠诲姟';
        requestLabel = `[浠诲姟鎻愪氦鐢宠] ${activeName} 鍦ㄣ€?{currentLoc}銆戝噯澶囨彁浜や换鍔°€?{questName}銆戙€俙;
        analysisLabel = 'Quest submit request initialized from task panel.';
        playerInput = `鎴戞兂鎻愪氦褰撳墠浠诲姟銆?{questName}銆戙€俙;
        extraRequirement = '浣犲繀椤讳繚鐣?浠诲姟璇锋眰.鍔ㄤ綔 = 鎻愪氦锛屽苟淇濈暀 浠诲姟鍚嶇О銆佷换鍔℃弿杩般€佺洰鏍囪繘搴︺€佸鍔卞竵銆佸鍔卞０鏈?杩欎簺瀛楁锛屼娇 MVU 鍚庣画鍙互鎸夎繘搴﹁繘琛岀粨绠椼€?;
      } else if (actionType === 'abandon') {
        if (!recordEntry || ['宸插畬鎴?, '宸叉斁寮?, '澶辫触', '宸插け璐?].includes(recordStatus)) return null;
        questAction = '鏀惧純';
        questDesc = toText(recordEntry && recordEntry['鎻忚堪'], '鏃?);
        requiredCount = Math.max(1, Math.floor(toNumber(recordEntry && recordEntry['鐩爣杩涘害'], 1)));
        rewardCoin = Math.max(0, Math.floor(toNumber(recordEntry && recordEntry['濂栧姳甯?], 0)));
        rewardRep = Math.max(0, Math.floor(toNumber(recordEntry && recordEntry['濂栧姳澹版湜'], 0)));
        progressAdd = 0;
        statusAction = '鏀惧純浠诲姟';
        requestLabel = `[浠诲姟鏀惧純鐢宠] ${activeName} 鍦ㄣ€?{currentLoc}銆戝噯澶囨斁寮冧换鍔°€?{questName}銆戙€俙;
        analysisLabel = 'Quest abandon request initialized from task panel.';
        playerInput = `鎴戞兂鏀惧純褰撳墠浠诲姟銆?{questName}銆戙€俙;
        extraRequirement = '浣犲繀椤讳繚鐣?浠诲姟璇锋眰.鍔ㄤ綔 = 鏀惧純锛屽苟淇濈暀 浠诲姟鍚嶇О銆佷换鍔℃弿杩般€佺洰鏍囪繘搴︺€佸鍔卞竵銆佸鍔卞０鏈?杩欎簺瀛楁锛屼娇 MVU 鍚庣画鍙互姝ｇ‘鍥炴粴濮旀墭鏉跨姸鎬併€?;
      } else if (actionType === 'progress') {
        if (!recordEntry || ['宸插畬鎴?, '宸叉斁寮?, '澶辫触', '宸插け璐?].includes(recordStatus)) return null;
        questAction = '鏇存柊杩涘害';
        questDesc = toText(recordEntry && recordEntry['鎻忚堪'], '鏃?);
        requiredCount = Math.max(1, Math.floor(toNumber(recordEntry && recordEntry['鐩爣杩涘害'], 1)));
        rewardCoin = Math.max(0, Math.floor(toNumber(recordEntry && recordEntry['濂栧姳甯?], 0)));
        rewardRep = Math.max(0, Math.floor(toNumber(recordEntry && recordEntry['濂栧姳澹版湜'], 0)));
        statusAction = '浠诲姟鎺ㄨ繘';
        requestLabel = `[浠诲姟杩涘害鐢宠] ${activeName} 鍦ㄣ€?{currentLoc}銆戞帹杩涗换鍔°€?{questName}銆戯紝璁″垝澧炲姞 ${progressAdd} 鐐硅繘搴︺€俙;
        analysisLabel = 'Quest progress request initialized from task panel.';
        playerInput = `鎴戞兂鎺ㄨ繘浠诲姟銆?{questName}銆戠殑杩涘害銆俙;
        extraRequirement = `浣犲繀椤讳繚鐣?浠诲姟璇锋眰.鍔ㄤ綔 = 鏇存柊杩涘害 涓?杩涘害澧為噺 = ${progressAdd}锛屽苟淇濈暀 浠诲姟鍚嶇О銆佷换鍔℃弿杩般€佺洰鏍囪繘搴︺€佸鍔卞竵銆佸鍔卞０鏈?杩欎簺瀛楁銆俙;
      } else {
        return null;
      }

      const patchOps = [
        { op: 'replace', path: `/char/${escapeJsonPointerValue(activeKey)}/浠诲姟璇锋眰`, value: { 鍔ㄤ綔: questAction, 浠诲姟鍚嶇О: questName, 浠诲姟鎻忚堪: questDesc, 杩涘害澧為噺: progressAdd, 鐩爣杩涘害: requiredCount, 濂栧姳甯? rewardCoin, 濂栧姳澹版湜: rewardRep } },
        { op: 'replace', path: `/char/${escapeJsonPointerValue(activeKey)}/鐘舵€?琛屽姩`, value: statusAction },
        { op: 'replace', path: '/sys/绯荤粺鎾姤', value: requestLabel }
      ];
      const systemPrompt = `浠ヤ笅鍐呭灞炰簬鍓嶇宸茬粡瀹屾垚鐨勪换鍔¤姹傚垵濮嬪寲锛屼笉瑕佸湪姝ｆ枃鐩存帴澶嶈堪鈥淛SONPatch / 绯荤粺鍒嗘瀽 / 浠茶鏃ュ織鈥濈瓑鏈銆?
${requestLabel}

[浠诲姟瑕佹眰]
${extraRequirement}

浠诲姟璇锋眰涓庡綋鍓嶈鍔ㄥ凡缁忕敱鑴氭湰鍐欏叆 MVU锛屾鏂囧彧鎵挎帴鍓ф儏琛ㄧ幇锛屼笉瑕侀噸澶嶈緭鍑鸿繖鎵瑰彉閲忔洿鏂般€俙;

      return {
        playerInput,
        systemPrompt,
        requestKind: '浠诲姟璇锋眰',
        patchOps
      };
    }


    function buildFactionAffairConsoleHtml(snapshot, preferredFactionName = '') {
      const pendingDonateItem = toText(deepGet(snapshot, 'activeChar.鎹愮尞璇锋眰.鐗╁搧鍚嶇О', '鏃?), '鏃?);
      const pendingDonateFaction = toText(deepGet(snapshot, 'activeChar.鎹愮尞璇锋眰.鐩爣鍔垮姏', '鏃?), '鏃?);
      const pendingDonateQty = Math.max(1, toNumber(deepGet(snapshot, 'activeChar.鎹愮尞璇锋眰.鏁伴噺', 1), 1));
      const factionNames = [];
      [preferredFactionName, pendingDonateFaction, ...(snapshot.鍔垮姏 || []).map(([name]) => name), '鍞愰棬', '鍙茶幈鍏嬪闄?, '鎴樼娈?, '浼犵伒濉?, '鑱旈偊鍐涙柟', '琛€绁炲啗鍥?, '鍦ｇ伒鏁?].forEach(name => {
        const text = toText(name, '').trim();
        if (text && text !== '鏃? && !factionNames.includes(text)) factionNames.push(text);
      });
      const donationEntries = (snapshot.inventoryEntries || []).filter(([, item]) => toNumber(item && item['鏁伴噺'], 0) > 0).slice(0, 50);
      const donationOptionsHtml = donationEntries.length
        ? donationEntries.map(([name, item]) => `<option value="${escapeHtmlAttr(name)}" ${name === pendingDonateItem ? 'selected' : ''}>${htmlEscape(`${name} 脳${toNumber(item && item['鏁伴噺'], 0)}`)}</option>`).join('')
        : '<option value="">鏆傛棤鍙崘鐚墿鍝?/option>';
      const defaultDonateFaction = pendingDonateFaction !== '鏃? ? pendingDonateFaction : (preferredFactionName || factionNames[0] || '');
      const pendingSummary = pendingDonateItem !== '鏃? ? `${pendingDonateItem} 脳 ${pendingDonateQty} / ${pendingDonateFaction}` : '鏃?;
      const isPlayerControlled = isSnapshotPlayerControlled(snapshot);
      if (!isPlayerControlled) {
        return `
          <div class="archive-card dossier-card dossier-console-card request-console-card">
            <div class="archive-card-head"><div class="archive-card-title">闃佃惀浜嬪姟鍙?/div><span class="dossier-pill warn">鏃佽</span></div>
            <section class="dossier-section">
              <div class="dossier-section-title">褰撳墠鐘舵€?/div>
              ${makeDossierRows([
                { label: '鎸傝捣浜嬪姟', value: htmlEscape(pendingSummary) },
                { label: '褰撳墠瑙嗚', value: '鏃佽' },
              ], 'dossier-row-grid--single')}
            </section>
            <section class="dossier-section">
              <div class="dossier-section-title">涓昏璐＄尞姹?/div>
              <div class="dossier-contrib-grid">
                ${makeDossierMeter('鍞愰棬', htmlEscape(formatNumber(deepGet(snapshot, 'activeChar.璐㈠瘜.鍞愰棬绉垎', 0))), ratioPercent(deepGet(snapshot, 'activeChar.璐㈠瘜.鍞愰棬绉垎', 0), 1000), 'dossier-meter--cyan')}
                ${makeDossierMeter('鍙茶幈鍏嬪闄?, htmlEscape(formatNumber(deepGet(snapshot, 'activeChar.璐㈠瘜.瀛﹂櫌绉垎', 0))), ratioPercent(deepGet(snapshot, 'activeChar.璐㈠瘜.瀛﹂櫌绉垎', 0), 1000), 'dossier-meter--green')}
                ${makeDossierMeter('鎴樼娈?鍐涙柟', htmlEscape(formatNumber(deepGet(snapshot, 'activeChar.璐㈠瘜.鎴樺姛', 0))), ratioPercent(deepGet(snapshot, 'activeChar.璐㈠瘜.鎴樺姛', 0), 1000), 'dossier-meter--red')}
              </div>
            </section>
          </div>
        `;
      }
      return `
        <div class="archive-card dossier-card dossier-console-card request-console-card">
          <div class="archive-card-head"><div class="archive-card-title">闃佃惀浜嬪姟鍙?/div><span class="dossier-pill ${preferredFactionName ? 'live' : 'warn'}">${htmlEscape(preferredFactionName || '鏈姞鍏?)}</span></div>
          <section class="dossier-section">
            <div class="dossier-section-title">鎸傝捣浜嬪姟</div>
            ${makeDossierRows([{ label: '褰撳墠鎺掗槦', value: htmlEscape(pendingSummary) }], 'dossier-row-grid--single')}
          </section>
          <section class="dossier-section">
            <div class="dossier-section-title">鐗╄祫鎹愮尞</div>
            <div class="dossier-form-grid">
              <div class="dossier-form-row request-console-row request-console-row--donate dossier-form-row--donate">
                <select class="request-console-input" data-faction-input="donateFaction">${factionNames.map(name => `<option value="${escapeHtmlAttr(name)}" ${name === defaultDonateFaction ? 'selected' : ''}>${htmlEscape(name)}</option>`).join('')}</select>
                <select class="request-console-input" data-faction-input="donateItem" ${donationEntries.length ? '' : 'disabled'}>${donationOptionsHtml}</select>
                <input type="number" min="1" class="request-console-input" data-faction-input="donateQty" value="${escapeHtmlAttr(String(pendingDonateQty))}" placeholder="鏁伴噺" />
                <button type="button" class="relation-action-btn faction-action-btn" data-faction-action="donate" ${donationEntries.length ? '' : 'disabled'}>鎻愪氦鎹愮尞</button>
              </div>
            </div>
          </section>
          <section class="dossier-section">
            <div class="dossier-section-title">涓昏璐＄尞姹?/div>
            <div class="dossier-contrib-grid">
              ${makeDossierMeter('鍞愰棬', htmlEscape(formatNumber(deepGet(snapshot, 'activeChar.璐㈠瘜.鍞愰棬绉垎', 0))), ratioPercent(deepGet(snapshot, 'activeChar.璐㈠瘜.鍞愰棬绉垎', 0), 1000), 'dossier-meter--cyan')}
              ${makeDossierMeter('鍙茶幈鍏嬪闄?, htmlEscape(formatNumber(deepGet(snapshot, 'activeChar.璐㈠瘜.瀛﹂櫌绉垎', 0))), ratioPercent(deepGet(snapshot, 'activeChar.璐㈠瘜.瀛﹂櫌绉垎', 0), 1000), 'dossier-meter--green')}
              ${makeDossierMeter('鎴樼娈?鍐涙柟', htmlEscape(formatNumber(deepGet(snapshot, 'activeChar.璐㈠瘜.鎴樺姛', 0))), ratioPercent(deepGet(snapshot, 'activeChar.璐㈠瘜.鎴樺姛', 0), 1000), 'dossier-meter--red')}
            </div>
          </section>
        </div>
      `;
    }

    function buildFactionRelationDossierList(orgName, orgData, options = {}) {
      const { max = 6, emptyTitle = '鏆傛棤闃佃惀鍏崇郴', emptyDesc = '褰撳墠鏈褰曞澶栧叧绯汇€? } = options || {};
      const safeOrgName = toText(orgName, '').trim();
      const relationEntries = safeEntries(deepGet(orgData, '鍏崇郴', {})).slice(0, Math.max(1, max));
      if (!relationEntries.length) {
        return `<div class="dossier-empty-note">${htmlEscape(emptyTitle)}锛?{htmlEscape(emptyDesc)}</div>`;
      }
      return makeDossierList(relationEntries.map(([relationName, relationData]) => {
        const safeRelData = relationData && typeof relationData === 'object' ? relationData : {};
        const relationMeta = buildFactionRelationMeta(relationName, safeRelData);
        const relationField = Object.prototype.hasOwnProperty.call(safeRelData, '鎬佸害')
          ? '鎬佸害'
          : (Object.prototype.hasOwnProperty.call(safeRelData, '鍏崇郴') ? '鍏崇郴' : '鎬佸害');
        const relationValue = toText(deepGet(safeRelData, relationField, relationMeta.attitude), relationMeta.attitude);
        const detailText = safeEntries(safeRelData)
          .filter(([key]) => !['鎬佸害', '鍏崇郴'].includes(toText(key, '')))
          .map(([key, value]) => {
            if (value && typeof value === 'object') return `${toText(key, '鍏崇郴椤?)} ${safeEntries(value).length}椤筦;
            const textValue = toText(value, '');
            return textValue ? `${toText(key, '鍏崇郴椤?)} ${textValue}` : '';
          })
          .filter(Boolean)
          .join(' / ');
        const editableValue = safeOrgName
          ? makeInlineEditableValue(relationValue, {
              path: ['org', safeOrgName, '鍏崇郴', relationName, relationField],
              kind: 'string',
              rawValue: relationValue,
            })
          : htmlEscape(relationValue);
        return {
          title: relationMeta.name,
          desc: `<strong>${editableValue}</strong>${detailText ? `<small>${htmlEscape(detailText)}</small>` : ''}`,
          className: relationMeta.className || '',
        };
      }), 'dossier-list--compact dossier-list--relations');
    }

    function buildFactionDossierModal(snapshot, previewKey = '鎵€灞炲娍鍔涜缁嗛〉') {
      const currentFactionName = snapshot.鍔垮姏[0] ? snapshot.鍔垮姏[0][0] : '';
      const currentFactionRole = snapshot.鍔垮姏[0] ? toText(deepGet(snapshot.鍔垮姏[0][1], '韬唤', '鏃?), '鏃?) : '鏈姞鍏?;
      const currentFactionPower = snapshot.鍔垮姏[0] ? toText(deepGet(snapshot.鍔垮姏[0][1], '鏉冮檺绾?, 0), '0') : '0';
      const activeCharKey = resolveSnapshotCharKey(snapshot, toText(snapshot.activeName, '')) || toText(snapshot.activeName, '');
      const currentFactionPath = currentFactionName && activeCharKey ? ['char', activeCharKey, '绀句氦', '鍔垮姏', currentFactionName] : [];
      const currentFactionEntry = currentFactionName
        ? (snapshot.orgEntries.find(([name]) => name === currentFactionName) || [currentFactionName, {}])
        : ['', {}];
      const factionRows = snapshot.鍔垮姏.length ? snapshot.鍔垮姏 : [['鏈姞鍏ュ娍鍔?, { 韬唤: '鏃?, 鏉冮檺绾? 0 }]];
      const title = previewKey === '鎴戠殑闃佃惀璇︽儏' ? '鎴戠殑闃佃惀寮圭獥' : '闃佃惀韬唤寮圭獥';

      return {
        title,
        summary: '褰撳墠瑙掕壊鍦ㄥ悇鍔垮姏涓殑韬唤銆佹潈闄愪笌闃佃惀浜嬪姟銆?,
        body: `
          <div class="archive-modal-grid dossier-shell dossier-shell--faction">
            <div class="archive-card dossier-card dossier-card--faction-list">
              <div class="archive-card-head">
                <div class="archive-card-title">鎵€灞炲娍鍔涘垪琛?/div>
                <span class="dossier-pill ${snapshot.鍔垮姏.length ? 'live' : 'warn'}">${snapshot.鍔垮姏.length ? `宸插姞鍏?${snapshot.鍔垮姏.length}` : '鏈姞鍏?}</span>
              </div>
              <div class="dossier-list dossier-list--factions">
                ${factionRows.map(([name, info], idx) => {
                  const factionPath = activeCharKey && name !== '鏈姞鍏ュ娍鍔? ? ['char', activeCharKey, '绀句氦', '鍔垮姏', name] : [];
                  const identityValue = toText(info && info['韬唤'], '鏃?);
                  const powerValue = toNumber(info && info['鏉冮檺绾?], 0);
                  return `
                    <div class="dossier-list-row faction-row ${idx === 0 ? 'highlight' : ''}">
                      <b>${htmlEscape(name)}</b>
                      <span>
                        韬唤锛?{factionPath.length
                          ? makeInlineEditableValue(identityValue, {
                              path: [...factionPath, '韬唤'],
                              kind: 'string',
                              rawValue: identityValue,
                            })
                          : htmlEscape(identityValue)}
                        / 鏉冮檺绾э細${factionPath.length
                          ? makeInlineEditableValue(String(powerValue), {
                              path: [...factionPath, '鏉冮檺绾?],
                              kind: 'number',
                              rawValue: powerValue,
                              editorMeta: { min: 0, integer: true, hint: '鏈€灏?0 路 鏁存暟' },
                            })
                          : htmlEscape(String(powerValue))}
                      </span>
                    </div>
                  `;
                }).join('')}
              </div>
            </div>
            <div class="archive-card dossier-card dossier-card--faction-profile">
              <div class="archive-card-head">
                <div>
                  <div class="archive-card-title">褰撳墠闃佃惀妗ｆ</div>
                  <div class="dossier-head-name">${htmlEscape(currentFactionName || '鏈姞鍏ュ娍鍔?)}</div>
                </div>
                <span class="dossier-pill ${currentFactionName ? 'live' : 'warn'}">${htmlEscape(currentFactionRole)}</span>
              </div>
              <section class="dossier-section">
                <div class="dossier-section-title">韬唤瀹氫綅</div>
                ${makeDossierRows([
                  { label: '褰撳墠鎵€灞?, value: htmlEscape(currentFactionName || '鏃?) },
                  { label: '韬唤', value: currentFactionPath.length
                    ? makeInlineEditableValue(currentFactionRole, {
                        path: [...currentFactionPath, '韬唤'],
                        kind: 'string',
                        rawValue: currentFactionRole,
                      })
                    : htmlEscape(currentFactionRole) },
                  { label: '鏉冮檺绾?, value: currentFactionPath.length
                    ? makeInlineEditableValue(currentFactionPower, {
                        path: [...currentFactionPath, '鏉冮檺绾?],
                        kind: 'number',
                        rawValue: toNumber(currentFactionPower, 0),
                        editorMeta: { min: 0, integer: true, hint: '鏈€灏?0 路 鏁存暟' },
                      })
                    : htmlEscape(currentFactionPower) },
                  { label: '涓诲叕寮€韬唤', value: activeCharKey
                    ? makeInlineEditableValue(toText(deepGet(snapshot, 'activeChar.绀句氦.涓昏韩浠?, '鏃?), '鏃?), {
                        path: ['char', activeCharKey, '绀句氦', '涓昏韩浠?],
                        kind: 'string',
                        rawValue: toText(deepGet(snapshot, 'activeChar.绀句氦.涓昏韩浠?, '鏃?), '鏃?),
                      })
                    : htmlEscape(toText(deepGet(snapshot, 'activeChar.绀句氦.涓昏韩浠?, '鏃?), '鏃?)) },
                  { label: '褰撳墠绉板彿', value: htmlEscape(snapshot.recentTitles[0] || '鏆傛棤') },
                  { label: '褰撳墠浣嶇疆', value: htmlEscape(snapshot.currentLoc || '鏈煡') },
                ], 'dossier-row-grid--two')}
              </section>
              <section class="dossier-section">
                <div class="dossier-section-title">闃佃惀鍏崇郴</div>
                ${buildFactionRelationDossierList(currentFactionName, currentFactionEntry[1] || {}, {
                  max: 6,
                  emptyTitle: '鏆傛棤闃佃惀鍏崇郴',
                  emptyDesc: currentFactionName ? '褰撳墠鎵€灞炲娍鍔涙湭璁板綍瀵瑰鍏崇郴銆? : '褰撳墠瑙掕壊灏氭湭鍔犲叆鍙睍绀哄叧绯荤殑鍔垮姏銆?
                })}
              </section>
            </div>
            ${buildFactionAffairConsoleHtml(snapshot, currentFactionName)}
          </div>
        `
      };
    }

    function buildMapTradeModalOptions(snapshot, dispatchDetail) {
      const detail = dispatchDetail || {};
      const services = normalizeMapDispatchServices(detail);
      const action = toText(detail.action, '');
      const tradeRequest = detail.tradeRequest && typeof detail.tradeRequest === 'object' ? detail.tradeRequest : {};
      const requestAction = toText(tradeRequest.鍔ㄤ綔, '');
      const npcTarget = toText(detail.npcTarget, '');
      const currentLoc = toText(snapshot && snapshot.currentLoc, '');
      const normalizedLoc = toText(snapshot && snapshot.normalizedLoc, currentLoc);
      const 鍦扮偣鏄犲皠 = deepGet(snapshot, 'rootData.world.鍦扮偣', {});
      const currentLocation = snapshot && snapshot.locationData && typeof snapshot.locationData === 'object'
        ? snapshot.locationData
        : (鍦扮偣鏄犲皠 && typeof 鍦扮偣鏄犲皠 === 'object'
          ? (鍦扮偣鏄犲皠[normalizedLoc] || 鍦扮偣鏄犲皠[currentLoc] || {})
          : {});
      const storeMap = currentLocation && typeof currentLocation === 'object' ? (currentLocation.鍟嗗簵 || {}) : {};

      let initialTab = 'tab-shop';
      if (/绔炴媿|鎷嶅崠|auction|bid/i.test(requestAction) || action === 'bid' || services.includes('auction')) {
        initialTab = 'tab-auction';
      } else if ((/鍑哄敭|鍗栧嚭|sell/i.test(requestAction) && !toText(tradeRequest.瀵硅薄, npcTarget)) || action === 'sell') {
        initialTab = 'tab-sell';
      } else if ((requestAction && toText(tradeRequest.瀵硅薄, npcTarget)) || (action === 'trade' && npcTarget)) {
        initialTab = 'tab-private';
      }

      const target = toText(detail.target, '');
      let preferredStore = '';
      if (target && storeMap && typeof storeMap === 'object' && storeMap[target]) {
        preferredStore = target;
      } else if (initialTab === 'tab-shop') {
        const storeNames = Object.keys(storeMap || {});
        if (storeNames.length === 1) preferredStore = storeNames[0];
      }
      const currentDiscountSpirit = normalizeSoulTowerDiscountSpiritRecord(
        deepGet(snapshot, 'activeChar.榄傜伒濉旇褰?褰撳墠浜旀姌榄傜伒', {}),
      );
      if (
        initialTab === 'tab-shop' &&
        !preferredStore &&
        hasActiveSoulTowerDiscountSpirit(currentDiscountSpirit)
      ) {
        preferredStore = SOUL_TOWER_DISCOUNT_STORE_NAME;
      }

      return {
        initialTab,
        prefillNpc: initialTab === 'tab-private' ? toText(tradeRequest.瀵硅薄, npcTarget) : '',
        lockNpc: initialTab === 'tab-private' && !!toText(tradeRequest.瀵硅薄, npcTarget),
        preferredStore: toText(tradeRequest.鐩爣, preferredStore) || preferredStore,
        prefillAction: requestAction,
        prefillItem: toText(tradeRequest.鐗╁搧, ''),
        prefillQty: Math.max(1, toNumber(tradeRequest.鏁伴噺, 1)),
        prefillPrice: Math.max(0, toNumber(tradeRequest.浠锋牸, 0)),
        autoExecute: tradeRequest.鑷姩鎵ц === true || /auto|ready|鎵ц|鎴愪氦|纭/.test(toText(tradeRequest.鐘舵€? ''))
      };
    }

    function buildBattleInlineHostMarkup() {
      return `
        <div id="mvu-battle-inline-host" class="mvu-battle-inline-host">
          <div class="battle-module-scope">
            <section class="battle-shell" aria-label="鎴樻枟">
              <div class="battle-header">
                <div class="combatant-card player" id="ui-player-panel">
                  <div class="name-row">
                    <div class="name-block">
                      <span class="lv-badge" id="ui-player-lv">Lv.0</span>
                      <span class="combatant-name" id="ui-player-name">宸辨柟</span>
                    </div>
                  </div>
                  <div class="bar-stack">
                    <div class="resource-bar" title="鐢熷懡"><span class="resource-label">鐢熷懡</span><div class="resource-fill hp" id="ui-player-hp-bar"></div><div class="resource-text" id="ui-player-hp-text">0 / 0</div></div>
                    <div class="resource-bar" title="浣撳姏"><span class="resource-label">浣撳姏</span><div class="resource-fill vit" id="ui-player-sta-bar"></div><div class="resource-text" id="ui-player-sta-text">0 / 0</div></div>
                    <div class="resource-bar" title="榄傚姏"><span class="resource-label">榄傚姏</span><div class="resource-fill sp" id="ui-player-sp-bar"></div><div class="resource-text" id="ui-player-sp-text">0 / 0</div></div>
                    <div class="resource-bar" title="绮剧"><span class="resource-label">绮剧</span><div class="resource-fill men" id="ui-player-men-bar"></div><div class="resource-text" id="ui-player-men-text">0 / 0</div></div>
                  </div>
                  <div class="stats-grid" id="ui-player-stats"></div>
                  <div class="buff-row" id="ui-player-buffs"></div>
                </div>
                <div class="combatant-card enemy" id="ui-enemy-panel">
                  <div class="name-row">
                    <div class="name-block">
                      <span class="lv-badge" id="ui-enemy-lv">Lv.0</span>
                      <span class="combatant-name" id="ui-enemy-name">瀵规墜</span>
                    </div>
                  </div>
                  <div class="bar-stack">
                    <div class="resource-bar" title="鐢熷懡"><span class="resource-label">鐢熷懡</span><div class="resource-fill hp" id="ui-enemy-hp-bar"></div><div class="resource-text" id="ui-enemy-hp-text">0 / 0</div></div>
                    <div class="resource-bar" title="浣撳姏"><span class="resource-label">浣撳姏</span><div class="resource-fill vit" id="ui-enemy-sta-bar"></div><div class="resource-text" id="ui-enemy-sta-text">0 / 0</div></div>
                    <div class="resource-bar" title="榄傚姏"><span class="resource-label">榄傚姏</span><div class="resource-fill sp" id="ui-enemy-sp-bar"></div><div class="resource-text" id="ui-enemy-sp-text">0 / 0</div></div>
                    <div class="resource-bar" title="绮剧"><span class="resource-label">绮剧</span><div class="resource-fill men" id="ui-enemy-men-bar"></div><div class="resource-text" id="ui-enemy-men-text">0 / 0</div></div>
                  </div>
                  <div class="stats-grid" id="ui-enemy-stats"></div>
                  <div class="buff-row" id="ui-enemy-buffs"></div>
                </div>
              </div>
              <div class="battle-main">
                <div class="side-rail"><div class="side-panel" id="ui-team-player"></div></div>
                <div class="center-column">
                  <div class="intent-bar">
                    <div class="intent-inner">
                      <div class="intent-chip-row" id="ui-combat-chips"></div>
                    </div>
                  </div>
                  <div class="action-wrap">
                    <div class="battle-toolbar">
                      <div class="mode-group" id="ui-mode-group">
                        <button class="mode-btn active" type="button" data-mode="single_round">鍗曞洖鍚?/button>
                        <button class="mode-btn" type="button" data-mode="multi_round">杩炵画鎺ㄦ紨</button>
                      </div>
                      <select class="request-console-input" id="ui-intent-mode" style="flex:0 0 124px;">
                        <option value="鐐瑰埌涓烘" selected>鐐瑰埌涓烘</option>
                        <option value="灏介噺鐢熸搾">灏介噺鐢熸搾</option>
                        <option value="閲嶄激鍘嬪埗">閲嶄激鍘嬪埗</option>
                        <option value="蹇呮潃">蹇呮潃</option>
                      </select>
                      <div class="battle-toolbar-actions">
                        <button class="ghost-btn" id="ui-arbitrate" type="button">缁撶畻</button>
                        <button class="ghost-btn battle-close-btn" id="ui-battle-close" type="button" aria-label="閫€鍑烘垬鏂?>閫€鍑?/button>
                      </div>
                    </div>
                    <div class="action-filters" id="ui-action-filters"></div>
                    <div class="action-grid" id="ui-action-grid"></div>
                    <div class="tower-settlement-panel" id="ui-tower-settlement" hidden></div>
                    <textarea id="ui-intent-output" hidden></textarea>
                  </div>
                </div>
                <div class="side-rail"><div class="side-panel" id="ui-team-enemy"></div></div>
              </div>
              <div class="skill-tooltip" id="ui-skill-tooltip"></div>
            </section>
          </div>
        </div>`;
    }

    function getBattleInlineHost() {
      const host = document.getElementById('mvu-battle-inline-host');
      return host instanceof Element && host.isConnected ? host : null;
    }

    function waitForBattleInlineHost(attempts = 12) {
      return new Promise(resolve => {
        const check = remaining => {
          const host = getBattleInlineHost();
          if (host || remaining <= 0) {
            resolve(host || null);
            return;
          }
          if (typeof window.requestAnimationFrame === 'function') {
            window.requestAnimationFrame(() => check(remaining - 1));
          } else {
            window.setTimeout(() => check(remaining - 1), 16);
          }
        };
        check(attempts);
      });
    }

    function isCombatActiveForBattleInline(snapshot) {
      const combatData = deepGet(snapshot, 'rootData.world.鎴樻枟', {});
      return !!(combatData && typeof combatData === 'object' && combatData.杩涜涓?&& isSnapshotPlayerControlled(snapshot));
    }

    function getBattleInlineSummary(snapshot) {
      const combatData = deepGet(snapshot, 'rootData.world.鎴樻枟', {});
      const player = toText(combatData && combatData.鍙傛垬鑰?&& combatData.鍙傛垬鑰?player && combatData.鍙傛垬鑰?player.name, '宸辨柟');
      const enemy = toText(combatData && combatData.鍙傛垬鑰?&& combatData.鍙傛垬鑰?enemy && combatData.鍙傛垬鑰?enemy.name, '瀵规墜');
      const round = toNumber(combatData && combatData.鍥炲悎, 0);
      return {
        title: '鎴樻枟',
        meta: `${player} / ${enemy}`,
        roundText: `鍥炲悎 ${round}`
      };
    }

    function openBattleInlineFromReturnEntry() {
      battleInlineDismissed = false;
      removeBattleReturnEntries();
      openBattleInlineSurface().then(() => refreshLiveSnapshot({ force: true, reopenBattle: true }));
    }

    function removeBattleReturnEntries() {
      document.querySelectorAll('[data-mvu-battle-return="1"]').forEach(node => node.remove());
    }

    function ensureBattleReturnEntry(container, className, html) {
      if (!(container instanceof Element)) return;
      let entry = container.querySelector('[data-mvu-battle-return="1"]');
      if (!entry) {
        entry = document.createElement('button');
        entry.type = 'button';
        entry.setAttribute('data-mvu-battle-return', '1');
        container.appendChild(entry);
      }
      entry.className = className;
      entry.innerHTML = html;
      entry.onclick = event => {
        event.preventDefault();
        event.stopPropagation();
        openBattleInlineFromReturnEntry();
      };
    }

    function syncBattleReturnEntries(snapshot, isActive) {
      if (!isActive) {
        removeBattleReturnEntries();
        return;
      }
      const summary = getBattleInlineSummary(snapshot);
      const unifiedActionGrid = document.querySelector('#mvu-unified-mount .mvu-unified-action-grid');
      ensureBattleReturnEntry(
        unifiedActionGrid,
        'mvu-unified-action-btn mvu-unified-grid-btn mvu-battle-return-entry',
        `<span>${htmlEscape(summary.title)}</span><small>${htmlEscape(summary.meta)}</small>`
      );
      const unifiedTabRow = document.querySelector('#mvu-unified-mount .mvu-unified-tab-row');
      ensureBattleReturnEntry(
        unifiedTabRow,
        'mvu-tab-btn mvu-battle-return-tab',
        htmlEscape(summary.title)
      );
    }

    function scheduleBattleReturnEntrySync(snapshot, isActive) {
      const token = ++battleReturnEntrySyncToken;
      const run = () => {
        if (token !== battleReturnEntrySyncToken) return;
        const latestSnapshot = liveSnapshot || lastRenderableSnapshot || snapshot;
        syncBattleReturnEntries(latestSnapshot, isActive && isCombatActiveForBattleInline(latestSnapshot));
      };
      if (typeof window.requestAnimationFrame === 'function') {
        window.requestAnimationFrame(run);
      }
      [80, 240, 520].forEach(delay => window.setTimeout(run, delay));
    }

    function openBattleInlineSurface() {
      const layoutState = window.__MVU_LAYOUT_STATE__ || {};
      const useShellSurface = !!layoutState.isMobileViewport || layoutState.surfaceMode === 'shell';
      if (useShellSurface) {
        const bridge = getMobileShellBridge();
        if (bridge && typeof bridge.open === 'function') bridge.open();
        if (bridge && typeof bridge.openPreview === 'function') {
          bridge.openPreview(BATTLE_INLINE_PREVIEW_KEY);
        } else if (typeof window.__MVU_OPEN_SHELL_PREVIEW__ === 'function') {
          window.__MVU_OPEN_SHELL_PREVIEW__(BATTLE_INLINE_PREVIEW_KEY, { preserveMapDispatchContext: true });
        }
        return waitForBattleInlineHost();
      }

      if (typeof window.mvuSetDesktopMode === 'function') {
        window.mvuSetDesktopMode('unified');
      }
      if (typeof window.__MVU_OPEN_UNIFIED_PREVIEW__ !== 'function') {
        return Promise.resolve(null);
      }
      window.__MVU_OPEN_UNIFIED_PREVIEW__(BATTLE_INLINE_PREVIEW_KEY, {
        preserveMapDispatchContext: true,
        replace: true
      });
      return waitForBattleInlineHost();
    }

    const COMBAT_PARTICIPANT_STAT_KEYS = ['骞撮緞', '绛夌骇', '绯诲埆', '澶╄祴姊槦', '閭瓊甯?, 'HP', 'HP涓婇檺', '榄傚姏', '榄傚姏涓婇檺', '绮剧鍔?, '绮剧鍔涗笂闄?, '鍔涢噺', '闃插尽', '鏁忔嵎', '浣撳姏', '浣撳姏涓婇檺'];

    function buildCombatParticipantFromSnapshotChar(snapshot, charKey, faction = '鏁屽') {
      const chars = deepGet(snapshot, 'rootData.char', {});
      const sourceChar = chars && typeof chars === 'object' ? chars[charKey] : null;
      if (!sourceChar || typeof sourceChar !== 'object') return null;
      const participant = cloneJsonValue(sourceChar, {});
      const stat = participant.灞炴€?&& typeof participant.灞炴€?=== 'object' ? participant.灞炴€?: {};
      const status = participant.鐘舵€?&& typeof participant.鐘舵€?=== 'object' ? participant.鐘舵€?: {};
      participant.name = toText(participant.name || deepGet(participant, 'base.name', ''), charKey);
      participant.鍔垮姏 = faction;
      participant.鐘舵€?= status;
      COMBAT_PARTICIPANT_STAT_KEYS.forEach(key => {
        if (stat[key] !== undefined) participant[key] = stat[key];
      });
      participant.绛夌骇 = toNumber(participant.绛夌骇, toNumber(stat.绛夌骇, 1));
      participant.绯诲埆 = toText(participant.绯诲埆, toText(stat.绯诲埆, '鏈煡绯?));
      participant.HP涓婇檺 = Math.max(1, toNumber(participant.HP涓婇檺, toNumber(stat.HP涓婇檺, toNumber(stat.浣撳姏涓婇檺, 1))));
      participant.HP = Math.max(0, toNumber(participant.HP, toNumber(stat.HP, toNumber(stat.浣撳姏, participant.HP涓婇檺))));
      participant.浣撳姏涓婇檺 = Math.max(1, toNumber(participant.浣撳姏涓婇檺, toNumber(stat.浣撳姏涓婇檺, 1)));
      participant.浣撳姏 = Math.max(0, toNumber(participant.浣撳姏, toNumber(stat.浣撳姏, participant.浣撳姏涓婇檺)));
      participant.榄傚姏涓婇檺 = Math.max(1, toNumber(participant.榄傚姏涓婇檺, toNumber(stat.榄傚姏涓婇檺, 1)));
      participant.榄傚姏 = Math.max(0, toNumber(participant.榄傚姏, toNumber(stat.榄傚姏, participant.榄傚姏涓婇檺)));
      participant.绮剧鍔涗笂闄?= Math.max(1, toNumber(participant.绮剧鍔涗笂闄? toNumber(stat.绮剧鍔涗笂闄? 1)));
      participant.绮剧鍔?= Math.max(0, toNumber(participant.绮剧鍔? toNumber(stat.绮剧鍔? participant.绮剧鍔涗笂闄?));
      participant.鍔涢噺 = Math.max(0, toNumber(participant.鍔涢噺, toNumber(stat.鍔涢噺, 1)));
      participant.闃插尽 = Math.max(0, toNumber(participant.闃插尽, toNumber(stat.闃插尽, 1)));
      participant.鏁忔嵎 = Math.max(0, toNumber(participant.鏁忔嵎, toNumber(stat.鏁忔嵎, 1)));
      participant.鐘舵€佹晥鏋?= cloneJsonValue(participant.鐘舵€佹晥鏋?|| stat.鐘舵€佹晥鏋?|| {}, {});
      participant.鎸佺画鏁堟灉 = cloneJsonValue(participant.鎸佺画鏁堟灉 || {}, {});
      participant.钃勫姏鎶€鑳?= participant.钃勫姏鎶€鑳?|| null;
      participant.瀛樻椿 = status.瀛樻椿 !== false;
      return participant;
    }

    function normalizeBattleSeedParticipant(seed, fallbackName = '') {
      if (Array.isArray(seed)) {
        const firstValid = seed.find(item => item !== undefined && item !== null && item !== '');
        return firstValid === undefined ? null : normalizeBattleSeedParticipant(firstValid, fallbackName);
      }
      if (typeof seed === 'string' || typeof seed === 'number') {
        const normalizedName = toText(seed, fallbackName);
        return normalizedName ? { name: normalizedName } : null;
      }
      if (!seed || typeof seed !== 'object' || Array.isArray(seed)) return null;
      const normalized = cloneJsonValue(seed, {});
      const normalizedName = toText(normalized.name, fallbackName);
      if (!normalizedName) return null;
      normalized.name = normalizedName;
      return normalized;
    }

    const BATTLE_SOUL_BEAST_STANDARD_SPECIES = Object.freeze([
      '榫欑被',
      '铔涚被',
      '鐔婄被',
      '妞嶇墿绯?,
      '娴烽瓊鍏?,
      '楦熺被',
      '鐚',
      '铔囩被',
    ]);

    const BATTLE_ABYSS_STANDARD_TIERS = Object.freeze([
      '浣庨樁鐢熺墿',
      '涓樁鐢熺墿',
      '楂橀樁鐢熺墿',
      '娣辨笂鐜嬭€?,
      '娣辨笂甯濆悰',
    ]);

    const BATTLE_ABYSS_STANDARD_RACES = Object.freeze([
      '鍥涚埅铦欒潬',
      '鍏埅铦欒潬',
      '娣辨笂鐐稿脊铚?,
      '鍣湧',
      '鍏埅榄?,
      '宸村畨',
      '瀹堟姢澶╃墰',
      '娣辨笂鐚涚姼',
      '娣辨笂榄斿個',
      '榛戠殗鏃?,
      '榄旈瓍鏃?,
      '娣辨笂鎭堕暟',
      '闄勪綋榄?,
    ]);

    const BATTLE_TEMPORARY_TYPE_MULTIPLIERS = Object.freeze({
      寮烘敾绯? { sp_max: 1.0, men_max: 1.0, str: 1.0, def: 1.0, agi: 1.0, vit_max: 1.0 },
      闃插尽绯? { sp_max: 1.0, men_max: 1.0, str: 0.9, def: 1.5, agi: 0.7, vit_max: 1.0 },
      鏁忔敾绯? { sp_max: 1.0, men_max: 1.0, str: 0.8, def: 0.7, agi: 1.6, vit_max: 0.8 },
      鎺у埗绯? { sp_max: 1.0, men_max: 1.2, str: 0.9, def: 0.8, agi: 1.1, vit_max: 0.9 },
      杈呭姪绯? { sp_max: 1.2, men_max: 1.2, str: 0.7, def: 0.6, agi: 0.8, vit_max: 0.7 },
      椋熺墿绯? { sp_max: 1.2, men_max: 1.2, str: 0.7, def: 0.6, agi: 0.8, vit_max: 0.7 },
      娌荤枟绯? { sp_max: 1.2, men_max: 1.2, str: 0.7, def: 0.6, agi: 0.8, vit_max: 0.7 },
    });

    function getTemporaryBattleBaseStats(level = 1) {
      const lv = Math.max(1, Number(level) || 1);
      let spBase = 100;
      if (lv <= 10) spBase = 100 + (lv - 1) * 73.66;
      else if (lv <= 30) spBase = 763 + (lv - 10) * 161.85;
      else if (lv <= 50) spBase = 4000 + Math.pow(lv - 30, 2) * 27.5;
      else if (lv <= 70) spBase = 15000 + (lv - 50) * 1000;
      else if (lv <= 90) spBase = 35000 + (lv - 70) * 1250;
      else if (lv <= 95) spBase = 60000 + (lv - 90) * 6818;
      else if (lv < 98) spBase = 94090 + (lv - 95) * 15303.3;
      else if (lv === 98) spBase = 140000;
      else if (lv === 99) spBase = 200000;
      else if (lv === 99.5) spBase = 400000;
      else if (lv >= 100) spBase = 700000;

      let strBase = 10;
      if (lv <= 10) strBase = 10 + (lv - 1) * 4.66;
      else if (lv <= 30) strBase = 52 + (lv - 10) * 22.4;
      else if (lv <= 50) strBase = 500 + Math.pow(lv - 30, 2) * 3.75;
      else if (lv <= 70) strBase = 2000 + Math.pow(lv - 50, 2) * 12.5;
      else if (lv <= 90) strBase = 7000 + (lv - 70) * 400;
      else if (lv <= 95) strBase = 15000 + (lv - 90) * 2157;
      else if (lv < 98) strBase = 25785 + (lv - 95) * 3071.6;
      else if (lv === 98) strBase = 35000;
      else if (lv === 99) strBase = 43000;
      else if (lv === 99.5) strBase = 60000;
      else if (lv >= 100) strBase = 80000;

      let menBase = lv;
      if (lv <= 20) menBase = lv;
      else if (lv <= 30) menBase = 20 + (lv - 20) * 3;
      else if (lv <= 50) menBase = 50 + Math.pow(lv - 30, 2) * 1.125;
      else if (lv <= 70) menBase = 500 + Math.pow(lv - 50, 2) * 6.25;
      else if (lv <= 90) menBase = 3000 + (lv - 70) * 100;
      else if (lv <= 95) menBase = 5000 + (lv - 90) * 600;
      else if (lv < 98) menBase = 8000 + (lv - 95) * 2000;
      else if (lv === 98) menBase = 15000;
      else if (lv === 99) menBase = 19000;
      else if (lv === 99.5) menBase = 23000;
      else if (lv >= 100) menBase = 50000;

      return {
        sp_max: Math.floor(spBase),
        men_max: Math.floor(menBase),
        str: Math.floor(strBase),
        def: Math.floor(strBase),
        agi: Math.floor(strBase / 2),
        vit_max: Math.floor(strBase),
      };
    }

    function hashTemporaryBattleSeed(seedText = '') {
      const text = String(seedText || '');
      let hash = 2166136261;
      for (let index = 0; index < text.length; index += 1) {
        hash ^= text.charCodeAt(index);
        hash = Math.imul(hash, 16777619);
      }
      return hash >>> 0;
    }

    function createTemporaryBattleRng(seedText = '') {
      let state = hashTemporaryBattleSeed(seedText) || 1;
      return () => {
        state = (Math.imul(state, 1664525) + 1013904223) >>> 0;
        return state / 4294967296;
      };
    }

    function withTemporaryBattleRandom(seedText, runner) {
      const rng = createTemporaryBattleRng(seedText);
      return runner(rng);
    }

    function pickTemporaryBattleItem(items = [], rng = Math.random) {
      if (!Array.isArray(items) || !items.length) return '';
      return items[Math.max(0, Math.min(items.length - 1, Math.floor(rng() * items.length)))] || items[0];
    }

    function pickTemporaryBattleInt(min = 0, max = min, rng = Math.random) {
      const low = Math.floor(Math.min(min, max));
      const high = Math.floor(Math.max(min, max));
      if (high <= low) return low;
      return low + Math.floor(rng() * (high - low + 1));
    }

    function createTemporaryBattleSkillShell(skillName = '榄傛妧1') {
      return {
        榄傛妧鍚? skillName,
        鐢婚潰鎻忚堪: '鏈煡',
        鏁堟灉鎻忚堪: '鏈煡',
        _鏁堟灉鏁扮粍: [],
      };
    }

    function buildTemporaryBattleSkillMap(skillCount = 0) {
      const total = Math.max(0, Math.min(4, Math.floor(Number(skillCount || 0))));
      const skillMap = {};
      for (let index = 1; index <= total; index += 1) {
        skillMap[`榄傛妧${index}`] = createTemporaryBattleSkillShell(`榄傛妧${index}`);
      }
      return skillMap;
    }

    function estimateTemporaryHumanSkillCount(level = 1) {
      const safeLevel = Math.max(1, Math.floor(Number(level || 1)));
      if (safeLevel >= 80) return 4;
      if (safeLevel >= 50) return 3;
      if (safeLevel >= 30) return 2;
      return 1;
    }

    function estimateTemporaryMonsterSkillCount(unitNature = '榄傚吔', seed = {}) {
      if (unitNature === '榄傚吔') {
        const age = Math.max(0, Math.floor(Number(seed?.骞撮檺 || 0)));
        if (age >= 100000) return 4;
        if (age >= 10000) return 3;
        if (age >= 1000) return 2;
        return 1;
      }
      const tier = String(seed?.绾у埆 || '').trim();
      if (tier === '娣辨笂甯濆悰' || tier === '娣辨笂鐜嬭€?) return 4;
      if (tier === '楂橀樁鐢熺墿') return 3;
      if (tier === '涓樁鐢熺墿') return 2;
      return 1;
    }

    function buildTemporaryBattleEquipmentShell() {
      return {
        姝﹀櫒: { 鍚嶇О: '鏃?, 鍝侀樁: '鏃?, 灞炴€у姞鎴? { 榄傚姏涓婇檺: 0, 绮剧鍔涗笂闄? 0, 鍔涢噺: 0, 闃插尽: 0, 鏁忔嵎: 0, 浣撳姏涓婇檺: 0 } },
        鏂楅摖: { 绛夌骇: 0, 鍚嶇О: '鏃?, 棰嗗煙: '鏃?, 鏉愯川: '鏃?, 瑁呭鐘舵€? '鏈澶?, 閮ㄤ欢: {} },
        鏈虹敳: { 绛夌骇: '鏃?, 鍨嬪彿: '鏃?, 鏉愯川: '鏃?, 鐘舵€? '瀹屽ソ', 瑁呭鐘舵€? '鏈澶?, 姝﹁: '鏃?, 鍝佽川绯绘暟: 1.0 },
      };
    }

    function buildTemporaryBattleSkeleton(name = '鏈煡鍗曚綅', unitNature = '浜虹被') {
      return {
        name,
        鏉ユ簮: '涓存椂鍗曚綅',
        鍗曚綅鎬ц川: unitNature,
        灞炴€? {
          绛夌骇: 0,
          绯诲埆: '鏈煡绯?,
          HP: 1,
          HP涓婇檺: 1,
          浣撳姏: 1,
          浣撳姏涓婇檺: 1,
          榄傚姏: 0,
          榄傚姏涓婇檺: 0,
          绮剧鍔? 1,
          绮剧鍔涗笂闄? 1,
          鍔涢噺: 1,
          闃插尽: 1,
          鏁忔嵎: 1,
          鐘舵€佹晥鏋? {},
        },
        鐘舵€? {
          瀛樻椿: true,
          褰撳墠棰嗗煙: '鏃?,
        },
        瑁呭: buildTemporaryBattleEquipmentShell(),
        鎸佺画鏁堟灉: {},
        钃勫姏鎶€鑳? null,
        鑷垱榄傛妧: {},
        绀句氦: { 鍔垮姏: {} },
      };
    }

    function resolveTemporaryHumanCombatType(identity = '榄傚笀', rng = Math.random) {
      if (identity === '鍐涗汉') {
        return pickTemporaryBattleItem(['寮烘敾绯?, '寮烘敾绯?, '闃插尽绯?, '鏁忔敾绯?, '鎺у埗绯?], rng) || '寮烘敾绯?;
      }
      return pickTemporaryBattleItem(['寮烘敾绯?, '鏁忔敾绯?, '闃插尽绯?, '鎺у埗绯?, '杈呭姪绯?, '娌荤枟绯?, '椋熺墿绯?], rng) || '寮烘敾绯?;
    }

    function resolveTemporaryHumanMechGrade(level = 1) {
      if (level >= 90) return '绾㈢骇';
      if (level >= 70) return '榛戠骇';
      if (level >= 50) return '绱骇';
      return '榛勭骇';
    }

    function resolveTemporaryHumanArmorLevel(level = 1) {
      if (level >= 90) return 4;
      if (level >= 80) return 3;
      if (level >= 70) return 2;
      return 1;
    }

    function applyTemporaryHumanEquipment(stats, equipment, identity = '榄傚笀', level = 1, rng = Math.random) {
      if (identity === '鏅€氫汉') return stats;
      const safeStats = stats;
      const allowMech =
        identity === '鍐涗汉'
          ? level >= 20 && rng() < (level >= 70 ? 0.85 : level >= 50 ? 0.75 : 0.65)
          : level >= 30 && rng() < (level >= 70 ? 0.65 : level >= 50 ? 0.45 : 0.25);
      if (allowMech) {
        const mechGrade = resolveTemporaryHumanMechGrade(level);
        equipment.鏈虹敳.绛夌骇 = mechGrade;
        equipment.鏈虹敳.鍨嬪彿 = `${mechGrade}鍒跺紡鏈虹敳`;
        equipment.鏈虹敳.瑁呭鐘舵€?= '宸茶澶?;
        const mechMult = mechGrade === '绾㈢骇' ? 1.35 : mechGrade === '榛戠骇' ? 1.22 : mechGrade === '绱骇' ? 1.14 : 1.08;
        safeStats.sp_max = Math.floor(safeStats.sp_max * mechMult);
        safeStats.men_max = Math.floor(safeStats.men_max * Math.max(1.02, mechMult - 0.03));
        safeStats.def = Math.floor(safeStats.def * (mechMult + 0.08));
        safeStats.agi = Math.floor(safeStats.agi * Math.max(1.02, mechMult - 0.01));
        safeStats.vit_max = Math.floor(safeStats.vit_max * (mechMult + 0.05));
      }

      const allowArmor =
        identity === '鍐涗汉'
          ? level >= 60 && rng() < (level >= 90 ? 0.4 : level >= 80 ? 0.28 : 0.15)
          : level >= 50 && rng() < (level >= 80 ? 0.5 : level >= 70 ? 0.35 : 0.18);
      if (allowArmor) {
        const armorLevel = resolveTemporaryHumanArmorLevel(level);
        equipment.鏂楅摖.绛夌骇 = armorLevel;
        equipment.鏂楅摖.鍚嶇О = `${armorLevel}瀛楁枟閾燻;
        equipment.鏂楅摖.瑁呭鐘舵€?= '宸茶澶?;
        const armorMult = armorLevel === 4 ? 1.42 : armorLevel === 3 ? 1.28 : armorLevel === 2 ? 1.18 : 1.1;
        safeStats.str = Math.floor(safeStats.str * armorMult);
        safeStats.def = Math.floor(safeStats.def * (armorMult + 0.1));
        safeStats.vit_max = Math.floor(safeStats.vit_max * (armorMult + 0.05));
        safeStats.sp_max = Math.floor(safeStats.sp_max * Math.max(1.03, armorMult - 0.08));
      }
      return safeStats;
    }

    function getTemporarySoulBeastStats(age = 1, species = '', quality = '') {
      let level = 1;
      if (age >= 100000) level = 90 + Math.floor((age - 100000) / 100000);
      else if (age >= 10000) level = 50 + Math.floor((age - 10000) / 2250);
      else if (age >= 1000) level = 30 + Math.floor((age - 1000) / 450);
      else if (age >= 100) level = 10 + Math.floor((age - 100) / 45);
      else level = Math.max(1, Math.floor(age / 10));
      const qualityKey = normalizeSoulSpiritQuality(quality);
      level = Math.max(1, level + Number(SOUL_SPIRIT_QUALITY_LEVEL_OFFSET_MAP[qualityKey] || 0));
      const base = getTemporaryBattleBaseStats(level);
      const mult = {
        榫欑被: { str: 1.5, vit_max: 1.5, def: 1.3, agi: 0.9 },
        铔涚被: { agi: 1.6, str: 0.8, def: 0.8 },
        鐔婄被: { str: 1.8, def: 1.5, agi: 0.6 },
        妞嶇墿绯? { vit_max: 2.0, str: 0.7, def: 0.8 },
        娴烽瓊鍏? { sp_max: 1.3, men_max: 1.2, agi: 1.1 },
        楦熺被: { agi: 1.8, str: 0.7, vit_max: 0.8 },
        鐚: { agi: 1.5, str: 1.2, def: 0.9 },
        铔囩被: { agi: 1.4, str: 1.0, def: 0.9, men_max: 1.1 },
      }[species] || { str: 1.0, def: 1.0, agi: 1.0, vit_max: 1.0, men_max: 1.0, sp_max: 1.0 };
      const qualityMult = Number(SOUL_SPIRIT_QUALITY_MULTIPLIER_MAP[qualityKey] || 1);
      return {
        瀵规爣绛夌骇: level,
        str: Math.floor(base.str * (mult.str || 1) * qualityMult),
        def: Math.floor(base.def * (mult.def || 1) * qualityMult),
        agi: Math.floor(base.agi * (mult.agi || 1) * qualityMult),
        vit_max: Math.floor(base.vit_max * (mult.vit_max || 1) * qualityMult),
        men_max: Math.floor(base.men_max * (mult.men_max || 1) * qualityMult),
        sp_max: Math.floor(base.sp_max * (mult.sp_max || 1) * qualityMult),
      };
    }

    function getTemporaryAbyssStats(tier = '', species = '') {
      return withTemporaryBattleRandom(`${tier}|${species}`, rng => {
        let level = 10;
        let mult = { str: 1.0, def: 1.0, agi: 1.0, vit_max: 1.0, men_max: 1.0, sp_max: 1.0 };
        if (tier === '浣庨樁鐢熺墿') {
          level = 20 + Math.floor(rng() * 20);
          mult = { str: 0.8, def: 0.8, agi: 1.2, vit_max: 0.8, men_max: 0.5, sp_max: 1.0 };
        } else if (tier === '涓樁鐢熺墿') {
          level = 40 + Math.floor(rng() * 30);
          mult = { str: 1.2, def: 1.2, agi: 1.0, vit_max: 1.2, men_max: 0.8, sp_max: 1.2 };
        } else if (tier === '楂橀樁鐢熺墿') {
          level = 70 + Math.floor(rng() * 20);
          mult = { str: 1.5, def: 1.5, agi: 1.5, vit_max: 1.5, men_max: 1.2, sp_max: 1.5 };
        } else if (tier === '娣辨笂鐜嬭€? || tier === '娣辨笂甯濆悰') {
          level = 99;
          mult = { str: 2.0, def: 2.0, agi: 2.0, vit_max: 2.0, men_max: 2.0, sp_max: 2.0 };
        }

        if (species.includes('铦欒潬') || species.includes('榄旈瓍') || species.includes('鎭堕暟')) {
          mult.agi *= 1.5;
          mult.def *= 0.7;
        } else if (species.includes('宸村畨') || species.includes('澶╃墰') || species.includes('鐚涚姼')) {
          mult.def *= 1.8;
          mult.vit_max *= 1.8;
          mult.agi *= 0.6;
        } else if (species.includes('榛戠殗')) {
          mult.sp_max *= 1.5;
          mult.men_max *= 1.5;
        }

        const base = getTemporaryBattleBaseStats(level);
        return {
          瀵规爣绛夌骇: level,
          str: Math.floor(base.str * mult.str),
          def: Math.floor(base.def * mult.def),
          agi: Math.floor(base.agi * mult.agi),
          vit_max: Math.floor(base.vit_max * mult.vit_max),
          men_max: Math.floor(base.men_max * mult.men_max),
          sp_max: Math.floor(base.sp_max * mult.sp_max),
        };
      });
    }

    function inferTemporarySoulBeastCombatType(species = '', stats = {}) {
      const text = String(species || '').trim();
      if (/妞嶇墿/.test(text)) return '鎺у埗绯?;
      if (/娴?.test(text)) return '鎺у埗绯?;
      if (/铔泑楦焲鐚珅铔?.test(text)) return '鏁忔敾绯?;
      if (/鐔?.test(text)) return '闃插尽绯?;
      if (Number(stats?.men_max || 0) > Number(stats?.str || 0) * 1.3) return '鎺у埗绯?;
      if (Number(stats?.agi || 0) > Number(stats?.def || 0) * 1.2) return '鏁忔敾绯?;
      if (Number(stats?.def || 0) > Number(stats?.agi || 0) * 1.2) return '闃插尽绯?;
      return '寮烘敾绯?;
    }

    function inferTemporaryAbyssCombatType(species = '', stats = {}) {
      const text = String(species || '').trim();
      if (/铦欒潬|榄旈瓍|鎭堕暟/.test(text)) return '鏁忔敾绯?;
      if (/闄勪綋榄攟榛戠殗/.test(text)) return '鎺у埗绯?;
      if (/宸村畨|澶╃墰|鐚涚姼|榄斿個/.test(text)) return '闃插尽绯?;
      if (Number(stats?.men_max || 0) > Number(stats?.str || 0) * 1.3) return '鎺у埗绯?;
      if (Number(stats?.agi || 0) > Number(stats?.def || 0) * 1.2) return '鏁忔敾绯?;
      if (Number(stats?.def || 0) > Number(stats?.agi || 0) * 1.2) return '闃插尽绯?;
      return '寮烘敾绯?;
    }

    function buildTemporaryHumanParticipant(seed = {}, slotName = 'enemy') {
      const identity = String(seed?.韬唤 || '鏅€氫汉').trim();
      const quantity = Math.max(1, Math.floor(Number(seed?.鏁伴噺 || 1)));
      const level = identity === '鏅€氫汉' ? 0 : Math.max(1, Math.floor(Number(seed?.绛夌骇 || 1)));
      const unitName = String(seed?.鍚嶇О || seed?.name || slotName || '涓存椂鍗曚綅').trim() || '涓存椂鍗曚綅';
      return withTemporaryBattleRandom(`${unitName}|${identity}|${quantity}|${level}|${slotName}`, rng => {
        const next = buildTemporaryBattleSkeleton(unitName, '浜虹被');
        next.韬唤 = identity;
        next.鏁伴噺 = quantity;
        if (identity === '鏅€氫汉') {
          const hpMax = pickTemporaryBattleInt(48, 82, rng);
          const menMax = pickTemporaryBattleInt(8, 16, rng);
          next.灞炴€?绛夌骇 = 0;
          next.灞炴€?绯诲埆 = '鏅€氫汉';
          next.灞炴€?HP涓婇檺 = hpMax;
          next.灞炴€?HP = hpMax;
          next.灞炴€?浣撳姏涓婇檺 = hpMax;
          next.灞炴€?浣撳姏 = hpMax;
          next.灞炴€?榄傚姏涓婇檺 = 0;
          next.灞炴€?榄傚姏 = 0;
          next.灞炴€?绮剧鍔涗笂闄?= menMax;
          next.灞炴€?绮剧鍔?= menMax;
          next.灞炴€?鍔涢噺 = pickTemporaryBattleInt(6, 10, rng);
          next.灞炴€?闃插尽 = pickTemporaryBattleInt(5, 9, rng);
          next.灞炴€?鏁忔嵎 = pickTemporaryBattleInt(6, 10, rng);
          return next;
        }

        const combatType = resolveTemporaryHumanCombatType(identity, rng);
        const base = getTemporaryBattleBaseStats(level);
        const typeMult = BATTLE_TEMPORARY_TYPE_MULTIPLIERS[combatType] || BATTLE_TEMPORARY_TYPE_MULTIPLIERS['寮烘敾绯?];
        const variance = 0.92 + rng() * 0.16;
        const derived = {
          str: Math.floor(base.str * typeMult.str * variance),
          def: Math.floor(base.def * typeMult.def * variance),
          agi: Math.floor(base.agi * typeMult.agi * variance),
          vit_max: Math.floor(base.vit_max * typeMult.vit_max * variance),
          men_max: Math.floor(base.men_max * typeMult.men_max * variance),
          sp_max: Math.floor(base.sp_max * typeMult.sp_max * variance),
        };
        applyTemporaryHumanEquipment(derived, next.瑁呭, identity, level, rng);
        next.灞炴€?绛夌骇 = level;
        next.灞炴€?绯诲埆 = combatType;
        next.灞炴€?HP涓婇檺 = Math.max(1, derived.vit_max);
        next.灞炴€?HP = next.灞炴€?HP涓婇檺;
        next.灞炴€?浣撳姏涓婇檺 = next.灞炴€?HP涓婇檺;
        next.灞炴€?浣撳姏 = next.灞炴€?HP涓婇檺;
        next.灞炴€?榄傚姏涓婇檺 = Math.max(1, derived.sp_max);
        next.灞炴€?榄傚姏 = next.灞炴€?榄傚姏涓婇檺;
        next.灞炴€?绮剧鍔涗笂闄?= Math.max(1, derived.men_max);
        next.灞炴€?绮剧鍔?= next.灞炴€?绮剧鍔涗笂闄?
        next.灞炴€?鍔涢噺 = Math.max(1, derived.str);
        next.灞炴€?闃插尽 = Math.max(1, derived.def);
        next.灞炴€?鏁忔嵎 = Math.max(1, derived.agi);
        next.鑷垱榄傛妧 = buildTemporaryBattleSkillMap(estimateTemporaryHumanSkillCount(level));
        return next;
      });
    }

    function buildTemporarySoulBeastParticipant(seed = {}, slotName = 'enemy') {
      const unitName = String(seed?.鍚嶇О || seed?.name || slotName || '榄傚吔').trim() || '榄傚吔';
      const species = String(seed?.鏍囧噯鐗╃ || '').trim();
      const age = Math.max(1, Math.floor(Number(seed?.骞撮檺 || 1)));
      const quantity = Math.max(1, Math.floor(Number(seed?.鏁伴噺 || 1)));
      const quality = normalizeSoulSpiritQuality(seed?.鍝佽川 || '');
      const stats = getTemporarySoulBeastStats(age, species, quality);
      const combatType = inferTemporarySoulBeastCombatType(species, stats);
      const next = buildTemporaryBattleSkeleton(unitName, '榄傚吔');
      next.鏁伴噺 = quantity;
      next.骞撮檺 = age;
      next.鏍囧噯鐗╃ = species;
      if (quality) next.鍝佽川 = quality;
      next.灞炴€?骞撮緞 = age;
      next.灞炴€?绛夌骇 = Number(stats.瀵规爣绛夌骇 || 1);
      next.灞炴€?绯诲埆 = combatType;
      next.灞炴€?HP涓婇檺 = Math.max(1, Number(stats.vit_max || 1));
      next.灞炴€?HP = next.灞炴€?HP涓婇檺;
      next.灞炴€?浣撳姏涓婇檺 = next.灞炴€?HP涓婇檺;
      next.灞炴€?浣撳姏 = next.灞炴€?浣撳姏涓婇檺;
      next.灞炴€?榄傚姏涓婇檺 = Math.max(1, Number(stats.sp_max || 1));
      next.灞炴€?榄傚姏 = next.灞炴€?榄傚姏涓婇檺;
      next.灞炴€?绮剧鍔涗笂闄?= Math.max(1, Number(stats.men_max || 1));
      next.灞炴€?绮剧鍔?= next.灞炴€?绮剧鍔涗笂闄?
      next.灞炴€?鍔涢噺 = Math.max(1, Number(stats.str || 1));
      next.灞炴€?闃插尽 = Math.max(1, Number(stats.def || 1));
      next.灞炴€?鏁忔嵎 = Math.max(1, Number(stats.agi || 1));
      next.绀句氦.鍔垮姏['榄傚吔涓€鏃?] = { 韬唤: '鏁屽', 鏉冮檺绾? 1 };
      next.鑷垱榄傛妧 = buildTemporaryBattleSkillMap(estimateTemporaryMonsterSkillCount('榄傚吔', seed));
      return next;
    }

    function buildTemporaryAbyssParticipant(seed = {}, slotName = 'enemy') {
      const unitName = String(seed?.鍚嶇О || seed?.name || slotName || '娣辨笂鐢熺墿').trim() || '娣辨笂鐢熺墿';
      const race = String(seed?.鏍囧噯绉嶆棌 || '').trim();
      const tier = String(seed?.绾у埆 || '').trim();
      const quantity = Math.max(1, Math.floor(Number(seed?.鏁伴噺 || 1)));
      const stats = getTemporaryAbyssStats(tier, race);
      const combatType = inferTemporaryAbyssCombatType(race, stats);
      const next = buildTemporaryBattleSkeleton(unitName, '娣辨笂');
      next.鏁伴噺 = quantity;
      next.绾у埆 = tier;
      next.鏍囧噯绉嶆棌 = race;
      next.灞炴€?绛夌骇 = Number(stats.瀵规爣绛夌骇 || 1);
      next.灞炴€?绯诲埆 = combatType;
      next.灞炴€?HP涓婇檺 = Math.max(1, Number(stats.vit_max || 1));
      next.灞炴€?HP = next.灞炴€?HP涓婇檺;
      next.灞炴€?浣撳姏涓婇檺 = next.灞炴€?HP涓婇檺;
      next.灞炴€?浣撳姏 = next.灞炴€?浣撳姏涓婇檺;
      next.灞炴€?榄傚姏涓婇檺 = Math.max(1, Number(stats.sp_max || 1));
      next.灞炴€?榄傚姏 = next.灞炴€?榄傚姏涓婇檺;
      next.灞炴€?绮剧鍔涗笂闄?= Math.max(1, Number(stats.men_max || 1));
      next.灞炴€?绮剧鍔?= next.灞炴€?绮剧鍔涗笂闄?
      next.灞炴€?鍔涢噺 = Math.max(1, Number(stats.str || 1));
      next.灞炴€?闃插尽 = Math.max(1, Number(stats.def || 1));
      next.灞炴€?鏁忔嵎 = Math.max(1, Number(stats.agi || 1));
      next.绀句氦.鍔垮姏['娣辨笂鐢熺墿'] = { 韬唤: '鏁屽', 鏉冮檺绾? 1 };
      next.鑷垱榄傛妧 = buildTemporaryBattleSkillMap(estimateTemporaryMonsterSkillCount('娣辨笂', seed));
      return next;
    }

    function buildExpandedBattleSeedParticipant(snapshot, participant = null, slotKey = 'enemy') {
      if (!participant || typeof participant !== 'object' || Array.isArray(participant)) return null;
      if (isExpandedBattleParticipant(participant)) return cloneJsonValue(participant, {});
      const participantName = String(participant.name || '').trim();
      const charKey = resolveSnapshotCharKey(snapshot, participantName);
      if (charKey) return buildCombatParticipantFromSnapshotChar(snapshot, charKey, slotKey === 'player' ? '宸辨柟' : '鏁屽');
      const unitNature = String(participant.鍗曚綅鎬ц川 || '').trim();
      if (unitNature === '浜虹被') return buildTemporaryHumanParticipant(participant, slotKey);
      if (unitNature === '榄傚吔') return buildTemporarySoulBeastParticipant(participant, slotKey);
      if (unitNature === '娣辨笂') return buildTemporaryAbyssParticipant(participant, slotKey);
      return cloneJsonValue(participant, {});
    }

    function explodeBattleSeedParticipant(snapshot, participant = null, slotKey = 'enemy') {
      const expanded = buildExpandedBattleSeedParticipant(snapshot, participant, slotKey);
      if (!expanded) return [];
      if (isExpandedBattleParticipant(expanded) && String(expanded.鏉ユ簮 || '').trim() !== '涓存椂鍗曚綅') return [expanded];
      const quantity = Math.max(1, Math.floor(Number(participant?.鏁伴噺 || expanded?.鏁伴噺 || 1)));
      if (quantity <= 1) return [expanded];
      const results = [];
      const baseName = String(participant?.name || expanded?.name || slotKey).trim() || slotKey;
      for (let index = 1; index <= quantity; index += 1) {
        const cloneSeed = cloneJsonValue(participant, {});
        const displayName = index === 1 ? baseName : `${baseName}路${index}`;
        cloneSeed.name = displayName;
        cloneSeed.鏁伴噺 = 1;
        const nextExpanded = buildExpandedBattleSeedParticipant(snapshot, cloneSeed, `${slotKey}_${index}`);
        if (nextExpanded) results.push(nextExpanded);
      }
      return results;
    }

    function expandBattleRosterForSnapshot(snapshot, combatData = {}) {
      const participants = combatData?.鍙傛垬鑰?&& typeof combatData.鍙傛垬鑰?=== 'object' ? combatData.鍙傛垬鑰?: null;
      if (!participants) return null;
      const nextParticipants = {
        player: null,
        enemy: null,
        team_player: [],
        team_enemy: [],
      };
      const playerUnits = explodeBattleSeedParticipant(snapshot, participants.player, 'player');
      nextParticipants.player = playerUnits[0] || null;
      if (playerUnits.length > 1) nextParticipants.team_player.push(...playerUnits.slice(1));

      const enemyUnits = explodeBattleSeedParticipant(snapshot, participants.enemy, 'enemy');
      nextParticipants.enemy = enemyUnits[0] || null;
      if (enemyUnits.length > 1) nextParticipants.team_enemy.push(...enemyUnits.slice(1));

      const teamPlayerSeeds = Array.isArray(participants.team_player) ? participants.team_player : [];
      teamPlayerSeeds.forEach((participant, index) => {
        nextParticipants.team_player.push(...explodeBattleSeedParticipant(snapshot, participant, `team_player_${index + 1}`));
      });
      const teamEnemySeeds = Array.isArray(participants.team_enemy) ? participants.team_enemy : [];
      teamEnemySeeds.forEach((participant, index) => {
        nextParticipants.team_enemy.push(...explodeBattleSeedParticipant(snapshot, participant, `team_enemy_${index + 1}`));
      });

      if (nextParticipants.player) nextParticipants.player.鍔垮姏 = '宸辨柟';
      if (nextParticipants.enemy) nextParticipants.enemy.鍔垮姏 = '鏁屽';
      nextParticipants.team_player.forEach(unit => {
        if (unit && typeof unit === 'object') unit.鍔垮姏 = '宸辨柟';
      });
      nextParticipants.team_enemy.forEach(unit => {
        if (unit && typeof unit === 'object') unit.鍔垮姏 = '鏁屽';
      });
      return nextParticipants;
    }

    function validateBattleSeedParticipant(snapshot, seed, role = 'enemy') {
      const normalized = normalizeBattleSeedParticipant(seed);
      if (!normalized) return { ok: false, reason: `${role}_seed_missing_name` };
      const charKey = resolveSnapshotCharKey(snapshot, normalized.name);
      if (charKey) return { ok: true, normalized, charKey, kind: 'char' };

      const unitNature = toText(normalized.鍗曚綅鎬ц川, '');
      if (!unitNature) return { ok: false, reason: `${role}_seed_missing_unit_nature` };
      if (!['浜虹被', '榄傚吔', '娣辨笂'].includes(unitNature)) return { ok: false, reason: `${role}_seed_invalid_unit_nature` };

      if (toNumber(normalized.鏁伴噺, 0) <= 0) return { ok: false, reason: `${role}_seed_missing_quantity` };
      const quantity = Math.max(1, Math.floor(toNumber(normalized.鏁伴噺, 1)));
      normalized.鏁伴噺 = quantity;

      if (unitNature === '浜虹被') {
        const identity = toText(normalized.韬唤, '');
        if (!['鏅€氫汉', '榄傚笀', '鍐涗汉'].includes(identity)) return { ok: false, reason: `${role}_seed_invalid_identity` };
        normalized.韬唤 = identity;
        if (identity !== '鏅€氫汉' && toNumber(normalized.绛夌骇, 0) <= 0) {
          return { ok: false, reason: `${role}_seed_missing_level` };
        }
        if (identity !== '鏅€氫汉') normalized.绛夌骇 = Math.max(1, Math.floor(toNumber(normalized.绛夌骇, 1)));
      } else if (unitNature === '榄傚吔') {
        if (toNumber(normalized.骞撮檺, 0) <= 0) return { ok: false, reason: `${role}_seed_missing_age` };
        const species = toText(normalized.鏍囧噯鐗╃, '');
        if (!species) return { ok: false, reason: `${role}_seed_missing_species` };
        if (!BATTLE_SOUL_BEAST_STANDARD_SPECIES.includes(species)) {
          return { ok: false, reason: `${role}_seed_invalid_species` };
        }
        normalized.骞撮檺 = Math.max(1, Math.floor(toNumber(normalized.骞撮檺, 1)));
        normalized.鏍囧噯鐗╃ = species;
        const quality = normalizeSoulSpiritQuality(normalized.鍝佽川 || '');
        if (normalized.鍝佽川 !== undefined && !quality) return { ok: false, reason: `${role}_seed_invalid_quality` };
        if (quality) normalized.鍝佽川 = quality;
      } else if (unitNature === '娣辨笂') {
        const tier = toText(normalized.绾у埆, '');
        const race = toText(normalized.鏍囧噯绉嶆棌, '');
        if (!tier) return { ok: false, reason: `${role}_seed_missing_tier` };
        if (!race) return { ok: false, reason: `${role}_seed_missing_race` };
        if (!BATTLE_ABYSS_STANDARD_TIERS.includes(tier)) return { ok: false, reason: `${role}_seed_invalid_tier` };
        if (!BATTLE_ABYSS_STANDARD_RACES.includes(race)) return { ok: false, reason: `${role}_seed_invalid_race` };
        normalized.绾у埆 = tier;
        normalized.鏍囧噯绉嶆棌 = race;
      }

      return { ok: true, normalized, charKey: '', kind: 'seed' };
    }

    function buildSoulTowerTeamMemberEntry(snapshot, charKey = '', fallbackName = '') {
      const safeKey = toText(charKey, '').trim();
      const charData = safeKey ? deepGet(snapshot, ['rootData', 'char', safeKey], null) : null;
      let ageValue = getSoulTowerAgeValueFromCharacter(charData || {});
      if (!(Number.isFinite(ageValue) && ageValue > 0)) {
        const activeName = toText(snapshot && snapshot.activeName, '').trim();
        if (safeKey && activeName && safeKey === activeName) {
          ageValue = getSoulTowerAgeValueFromCharacter(deepGet(snapshot, 'activeChar', {}));
        }
      }
      return {
        key: safeKey,
        name: toText(deepGet(charData, 'name', ''), fallbackName || safeKey || '闃熷憳'),
        age: ageValue,
      };
    }

    function validateSoulTowerRosterFromBattleSeeds(snapshot, activeKey, teamPlayerChecks = []) {
      const memberEntries = [buildSoulTowerTeamMemberEntry(snapshot, activeKey, toText(snapshot && snapshot.activeName, '鐜╁'))];
      if (!memberEntries[0].key) return { ok: false, message: '褰撳墠瑙掕壊鏈粦瀹氭湁鏁堟。妗堬紝鏃犳硶鍙備笌榄傜伒濉斻€? };
      for (const itemCheck of Array.isArray(teamPlayerChecks) ? teamPlayerChecks : []) {
        if (!itemCheck || itemCheck.kind !== 'char' || !toText(itemCheck.charKey, '').trim()) {
          return { ok: false, message: '榄傜伒濉旈槦鍙嬪繀椤绘槸宸插缓妗ｈ鑹层€? };
        }
        memberEntries.push(buildSoulTowerTeamMemberEntry(snapshot, itemCheck.charKey, toText(itemCheck.normalized && itemCheck.normalized.name, '闃熷弸')));
      }
      const unresolvedMember = memberEntries.find(member => !Number.isFinite(member.age) || !(member.age > 0));
      if (unresolvedMember) {
        return { ok: false, message: `${toText(unresolvedMember.name, '闃熷憳')} 骞撮緞鏈褰曪紝鏃犳硶鏍￠獙榄傜伒濉旇祫鏍笺€俙 };
      }
      return buildSoulTowerTeamValidationResult(memberEntries);
    }

    function buildBattleSeedRoster(snapshot, detail = {}) {
      const detailParticipants = detail.鍙傛垬鑰?&& typeof detail.鍙傛垬鑰?=== 'object' ? detail.鍙傛垬鑰?: null;
      const activeKey = resolveSnapshotCharKey(snapshot, toText(snapshot && snapshot.activeName, '') || deepGet(snapshot, 'rootData.sys.鐜╁鍚?, ''));
      if (!snapshot || !snapshot.rootData || !activeKey) return { ok: false, reason: 'player_context_unresolved' };
      const playerChar = buildCombatParticipantFromSnapshotChar(snapshot, activeKey, '宸辨柟');
      if (!playerChar) return { ok: false, reason: 'player_context_unresolved' };
      const soulTowerCombat = isSoulTowerCombatType(detail);
      const activeChar = getActiveSnapshotCharacter(snapshot);
      const defaultSoulTowerFloor = Math.min(
        SOUL_TOWER_TOTAL_FLOORS,
        Math.max(1, toNumber(deepGet(activeChar, '榄傜伒濉旇褰?鏈€楂樺眰', 0), 0) + 1),
      );
      const soulTowerFloor = soulTowerCombat ? Math.min(SOUL_TOWER_TOTAL_FLOORS, Math.max(1, Math.floor(toNumber(detail.floor, defaultSoulTowerFloor)))) : 0;

      if (!detailParticipants) {
        if (soulTowerCombat) {
          const soloCheck = validateSoulTowerRosterFromBattleSeeds(snapshot, activeKey, []);
          if (!soloCheck.ok) return { ok: false, reason: soloCheck.message };
        }
        const npcTarget = toText(detail.npcTarget, '');
        const targetKey = resolveSnapshotCharKey(snapshot, npcTarget);
        if (targetKey) {
          const enemyChar = buildCombatParticipantFromSnapshotChar(snapshot, targetKey, '鏁屽');
          if (!enemyChar) return { ok: false, reason: 'combat_context_unresolved' };
          return {
            ok: true,
            participants: {
              player: playerChar,
              enemy: enemyChar,
              team_player: [],
              team_enemy: []
            }
          };
        }
        if (!soulTowerCombat) return { ok: false, reason: 'combat_context_unresolved' };
        const guardianSeed = buildSoulTowerGuardianSeed(soulTowerFloor);
        return {
          ok: true,
          participants: {
            player: playerChar,
            enemy: guardianSeed,
            team_player: [],
            team_enemy: []
          }
        };
      }

      const normalizedRoster = {
        player: { name: playerChar.name },
        enemy: null,
        team_player: [],
        team_enemy: []
      };

      const enemyCheck = validateBattleSeedParticipant(snapshot, detailParticipants.enemy, 'enemy');
      if (!enemyCheck.ok) return { ok: false, reason: enemyCheck.reason };
      normalizedRoster.enemy = enemyCheck.kind === 'char'
        ? { name: enemyCheck.normalized.name }
        : enemyCheck.normalized;

      const teamPlayerSeeds = Array.isArray(detailParticipants.team_player) ? detailParticipants.team_player : [];
      for (let index = 0; index < teamPlayerSeeds.length; index += 1) {
        const itemCheck = validateBattleSeedParticipant(snapshot, teamPlayerSeeds[index], `team_player_${index}`);
        if (!itemCheck.ok) return { ok: false, reason: itemCheck.reason };
        normalizedRoster.team_player.push(itemCheck.kind === 'char' ? { name: itemCheck.normalized.name } : itemCheck.normalized);
      }
      if (soulTowerCombat) {
        const towerRosterCheck = validateSoulTowerRosterFromBattleSeeds(snapshot, activeKey, teamPlayerSeeds.map((seed, index) => {
          const itemCheck = validateBattleSeedParticipant(snapshot, seed, `team_player_${index}`);
          return itemCheck.ok ? itemCheck : null;
        }).filter(Boolean));
        if (!towerRosterCheck.ok) return { ok: false, reason: towerRosterCheck.message };
      }

      const teamEnemySeeds = Array.isArray(detailParticipants.team_enemy) ? detailParticipants.team_enemy : [];
      for (let index = 0; index < teamEnemySeeds.length; index += 1) {
        const itemCheck = validateBattleSeedParticipant(snapshot, teamEnemySeeds[index], `team_enemy_${index}`);
        if (!itemCheck.ok) return { ok: false, reason: itemCheck.reason };
        normalizedRoster.team_enemy.push(itemCheck.kind === 'char' ? { name: itemCheck.normalized.name } : itemCheck.normalized);
      }

      return { ok: true, participants: normalizedRoster };
    }

    function isExpandedBattleParticipant(participant) {
      return !!(
        participant &&
        typeof participant === 'object' &&
        !Array.isArray(participant) &&
        String(participant.name || '').trim() &&
        participant.灞炴€?&&
        typeof participant.灞炴€?=== 'object' &&
        participant.鐘舵€?&&
        typeof participant.鐘舵€?=== 'object'
      );
    }

    function compactBattleParticipantForStorage(snapshot, participant) {
      if (participant === null || participant === undefined) return undefined;
      if (typeof participant === 'string' || typeof participant === 'number') {
        return { name: String(participant) };
      }
      if (typeof participant !== 'object' || Array.isArray(participant)) return undefined;
      const source = cloneJsonValue(participant, {});
      const participantName = toText(source.name, '');
      const resolvedKey = participantName ? resolveSnapshotCharKey(snapshot, participantName) : '';
      if (resolvedKey) {
        const compact = { name: participantName || resolvedKey };
        if (source.鍔垮姏 !== undefined) compact.鍔垮姏 = toText(source.鍔垮姏, '');
        const aliveCandidate =
          source.瀛樻椿 !== undefined
            ? source.瀛樻椿
            : source.鐘舵€?&& typeof source.鐘舵€?=== 'object'
              ? source.鐘舵€?瀛樻椿
              : undefined;
        if (aliveCandidate !== undefined) compact.瀛樻椿 = aliveCandidate !== false;
        return compact;
      }

      const compact = {};
      if (participantName) {
        compact.name = participantName;
      }
      [
        '鏉ユ簮',
        '鍗曚綅鎬ц川',
        '韬唤',
        '鏁伴噺',
        '绛夌骇',
        '骞撮檺',
        '鍝佽川',
        '鏍囧噯鐗╃',
        '绾у埆',
        '鏍囧噯绉嶆棌',
        '鍔垮姏'
      ].forEach(key => {
        if (source[key] !== undefined) compact[key] = cloneJsonValue(source[key], source[key]);
      });
      const aliveCandidate =
        source.瀛樻椿 !== undefined
          ? source.瀛樻椿
          : source.鐘舵€?&& typeof source.鐘舵€?=== 'object'
            ? source.鐘舵€?瀛樻椿
            : undefined;
      if (aliveCandidate !== undefined) compact.瀛樻椿 = aliveCandidate !== false;
      return Object.keys(compact).length ? compact : undefined;
    }

    function compactBattleRosterForStorage(snapshot, participants) {
      if (!participants || typeof participants !== 'object') return null;
      const compact = {
        player: compactBattleParticipantForStorage(snapshot, participants.player),
        enemy: compactBattleParticipantForStorage(snapshot, participants.enemy),
        team_player: Array.isArray(participants.team_player)
          ? participants.team_player.map(item => compactBattleParticipantForStorage(snapshot, item)).filter(Boolean)
          : [],
        team_enemy: Array.isArray(participants.team_enemy)
          ? participants.team_enemy.map(item => compactBattleParticipantForStorage(snapshot, item)).filter(Boolean)
          : []
      };
      return compact;
    }

    function compactCombatDataForWorldStorage(snapshot, combatData) {
      const source = cloneJsonValue(combatData, {});
      if (!source || typeof source !== 'object') return combatData;
      if (source.鍙傛垬鑰?&& typeof source.鍙傛垬鑰?=== 'object') {
        source.鍙傛垬鑰?= compactBattleRosterForStorage(snapshot, source.鍙傛垬鑰?;
      }
      return source;
    }

    function normalizeCombatForBattleUI(snapshot) {
      const combatData = deepGet(snapshot, 'rootData.world.鎴樻枟', {});
      if (!combatData || typeof combatData !== 'object' || !combatData.杩涜涓? return combatData;
      const participants = combatData.鍙傛垬鑰?&& typeof combatData.鍙傛垬鑰?=== 'object' ? combatData.鍙傛垬鑰?: null;
      if (!participants) return null;
      let nextCombat = combatData;
      if (!isExpandedBattleParticipant(participants.player) || !isExpandedBattleParticipant(participants.enemy)) {
        const expandedParticipants = expandBattleRosterForSnapshot(snapshot, combatData);
        if (!expandedParticipants) return null;
        nextCombat = cloneJsonValue(combatData, {});
        nextCombat.鍙傛垬鑰?= expandedParticipants;
      }
      if (!Array.isArray(nextCombat.鍙傛垬鑰?.team_player) || !Array.isArray(nextCombat.鍙傛垬鑰?.team_enemy)) {
        nextCombat = nextCombat === combatData ? cloneJsonValue(combatData, {}) : nextCombat;
        if (!nextCombat.鍙傛垬鑰?|| typeof nextCombat.鍙傛垬鑰?!== 'object') nextCombat.鍙傛垬鑰?= {};
        if (!Array.isArray(nextCombat.鍙傛垬鑰?team_player)) nextCombat.鍙傛垬鑰?team_player = [];
        if (!Array.isArray(nextCombat.鍙傛垬鑰?team_enemy)) nextCombat.鍙傛垬鑰?team_enemy = [];
      }
      const teamPlayer = nextCombat.鍙傛垬鑰?team_player;
      const teamEnemy = nextCombat.鍙傛垬鑰?team_enemy;
      if (!isExpandedBattleParticipant(nextCombat.鍙傛垬鑰?.player) || !isExpandedBattleParticipant(nextCombat.鍙傛垬鑰?.enemy)) return null;
      if (!teamPlayer.every(isExpandedBattleParticipant) || !teamEnemy.every(isExpandedBattleParticipant)) return null;
      return nextCombat;
    }

    function buildBattleUiSnapshot(snapshot, combatData) {
      if (!snapshot || typeof snapshot !== 'object' || !snapshot.rootData || !combatData || typeof combatData !== 'object') {
        return snapshot;
      }
      if (deepGet(snapshot, 'rootData.world.鎴樻枟', null) === combatData) return snapshot;
      const nextSnapshot = cloneJsonValue(snapshot, {});
      if (!nextSnapshot.rootData || typeof nextSnapshot.rootData !== 'object') nextSnapshot.rootData = {};
      if (!nextSnapshot.rootData.world || typeof nextSnapshot.rootData.world !== 'object') nextSnapshot.rootData.world = {};
      nextSnapshot.rootData.world.鎴樻枟 = combatData;
      return nextSnapshot;
    }

    function syncBattleUiForSnapshot(snapshot, options = {}) {
      const liveCombatData = normalizeCombatForBattleUI(snapshot);
      const isCombatActive = !!(liveCombatData && liveCombatData.杩涜涓?;
      const canUseBattle = isCombatActive && isSnapshotPlayerControlled(snapshot);
      const battleUiSnapshot = canUseBattle ? buildBattleUiSnapshot(snapshot, liveCombatData) : snapshot;
      syncBattleReturnEntries(snapshot, canUseBattle && battleInlineDismissed);
      if (canUseBattle && battleInlineDismissed && !options.reopenBattle) {
        if (activeBattleUI && typeof activeBattleUI.destroy === 'function') activeBattleUI.destroy();
        activeBattleUI = null;
        scheduleBattleReturnEntrySync(snapshot, true);
        return;
      }
      if (canUseBattle) {
        const currentHost = getBattleInlineHost();
        if (activeBattleUI && currentHost && activeBattleUI.container === currentHost && typeof activeBattleUI.updateData === 'function') {
          activeBattleUI.updateData(battleUiSnapshot);
          return;
        }
        if (activeBattleUI && typeof activeBattleUI.destroy === 'function') activeBattleUI.destroy();
        activeBattleUI = null;
        if (pendingBattleInlineMount) return;
        const mountToken = ++battleInlineMountToken;
        pendingBattleInlineMount = openBattleInlineSurface()
          .then(async host => {
            if (mountToken !== battleInlineMountToken) return;
            const latestSnapshot = liveSnapshot || lastRenderableSnapshot || snapshot;
            const latestCombatData = normalizeCombatForBattleUI(latestSnapshot);
            if (!latestCombatData || !latestCombatData.杩涜涓?|| !isSnapshotPlayerControlled(latestSnapshot)) return;
            const latestBattleUiSnapshot = buildBattleUiSnapshot(latestSnapshot, latestCombatData);
            const 妯″潡鍔犺浇缁撴灉 = await 纭繚妯″潡渚濊禆宸插姞杞?'鎴樻枟妯″潡', 'battle_sync_mount');
            if (!妯″潡鍔犺浇缁撴灉 || 妯″潡鍔犺浇缁撴灉.ok === false) {
              showUiToast('鎴樻枟妯″潡灏氭湭灏辩华锛屾殏鏃舵棤娉曟寕杞芥垬鏂楃粓绔€?, 'error', 4200);
              return;
            }
            if (!host || typeof window.mountBattleUI !== 'function') {
              showUiToast('鎴樻枟缁堢宓屽叆瀹夸富鏈氨缁紝鎴樻枟妯″潡鏈墦寮€銆?, 'error', 4200);
              return;
            }
            activeBattleUI = window.mountBattleUI(host, latestBattleUiSnapshot, {
              onAction: (actionData) => {
                dispatchUiAiRequest(actionData.playerInput, actionData.systemPrompt, {
                  requestKind: actionData.requestKind,
                  patchOps: actionData.patchOps
                });
              }
            });
          })
          .catch(error => {
            console.error('[DragonUI] Battle inline mount failed', error);
            showUiToast('鎴樻枟缁堢鎵撳紑澶辫触銆?, 'error', 4200);
          })
          .finally(() => {
            if (mountToken === battleInlineMountToken) pendingBattleInlineMount = null;
          });
      } else {
        battleInlineMountToken++;
        pendingBattleInlineMount = null;
        battleInlineDismissed = false;
        removeBattleReturnEntries();
        if (activeBattleUI && typeof activeBattleUI.destroy === 'function') activeBattleUI.destroy();
        activeBattleUI = null;
        const effectiveUnifiedPreviewKey = getEffectiveUnifiedPreviewKey();
        const shellHost = getShellInlineHost();
        const effectiveShellPreviewKey = shellHost
          ? toText(shellHost.dataset.shellPreview, '').trim()
          : toText(lastRenderedShellPreviewKey, '').trim();
        if (effectiveUnifiedPreviewKey === BATTLE_INLINE_PREVIEW_KEY) {
          clearUnifiedInlinePreview();
        }
        if (effectiveShellPreviewKey === BATTLE_INLINE_PREVIEW_KEY) {
          clearShellInlinePreview();
          notifyShellPreviewClosed();
        }
        if (currentModalPreviewKey === BATTLE_INLINE_PREVIEW_KEY && typeof closeModal === 'function') {
          closeModal();
        }
      }
    }

    function buildMapBattleCombatData(snapshot, dispatchDetail) {
      const detail = dispatchDetail || {};
      const rosterResult = buildBattleSeedRoster(snapshot, detail);
      if (!rosterResult.ok) return { ok: false, reason: rosterResult.reason };
      const arenaName = toText(detail.currentLoc, toText(detail.target, toText(snapshot.currentLoc, '鏈煡鍦扮偣')));
      const soulTowerCombat = isSoulTowerCombatType(detail);
      const activeChar = getActiveSnapshotCharacter(snapshot);
      const defaultSoulTowerFloor = Math.min(
        SOUL_TOWER_TOTAL_FLOORS,
        Math.max(1, toNumber(deepGet(activeChar, '榄傜伒濉旇褰?鏈€楂樺眰', 0), 0) + 1),
      );
      const soulTowerFloor = soulTowerCombat ? Math.min(SOUL_TOWER_TOTAL_FLOORS, Math.max(1, Math.floor(toNumber(detail.floor, defaultSoulTowerFloor)))) : 0;
      const soulTowerMeta = soulTowerCombat ? getSoulTowerGateMeta(soulTowerFloor) : null;
      return {
        杩涜涓? true,
        鎴樻枟绫诲瀷: toText(detail.鎴樻枟绫诲瀷, '鎿傚彴鍒囩'),
        鎴樻枟鎰忓浘: toText(detail.鎴樻枟鎰忓浘, '鐐瑰埌涓烘'),
        鍏堟敾: '鏃?,
        鍏佽鎾ょ: true,
        鍥炲悎: 0,
        鐜: arenaName,
        瑁佹柇缁撴灉: '',
        鍙傛垬鑰? rosterResult.participants,
        source: toText(detail.source, 'map_action'),
        ...(soulTowerCombat
          ? {
              floor: soulTowerFloor,
              澶у叧鍗? soulTowerMeta.gateIndex,
              澶у叧鏍囩: soulTowerMeta.gateLabel,
              鍏冲崱鑼冨洿: soulTowerMeta.gateRangeLabel,
              鍏冲簳鎴? soulTowerMeta.isGateBoss,
            }
          : {}),
        ...(detail.ascension_ticket !== undefined ? { ascension_ticket: toText(detail.ascension_ticket, '') } : {}),
        ...(detail.killed_age !== undefined ? { killed_age: Math.max(1, Math.floor(toNumber(detail.killed_age, 1))) } : {}),
        ...(detail.party_size !== undefined ? { party_size: Math.max(1, Math.floor(toNumber(detail.party_size, 1))) } : {}),
        ...(detail.璇曠偧鐘舵€?!== undefined ? { 璇曠偧鐘舵€? toText(detail.璇曠偧鐘舵€? '') } : {}),
        ...(detail.鍗囩伒瀵规墜绫诲瀷 !== undefined ? { 鍗囩伒瀵规墜绫诲瀷: toText(detail.鍗囩伒瀵规墜绫诲瀷, '') } : {}),
        ...(detail.鍗囩伒妗ｄ綅鐧惧垎浣?!== undefined ? { 鍗囩伒妗ｄ綅鐧惧垎浣? Math.max(0, Math.min(1, toNumber(detail.鍗囩伒妗ｄ綅鐧惧垎浣? 0))) } : {}),
        request: {
          action: '鎴樻枟',
          target: arenaName,
          npcTarget: toText(detail.npcTarget, '')
        }
      };
    }

    async function openMapBattleModule(snapshot, dispatchDetail) {
      const trialContext = 璇诲彇璇曠偧鐘舵€佷笂涓嬫枃(snapshot);
      const trialBattleDetail = trialContext
        ? buildTrialBattleDispatchDetail(snapshot, dispatchDetail, trialContext)
        : null;
      const combatData = buildMapBattleCombatData(snapshot, trialBattleDetail || dispatchDetail);
      if (!combatData || combatData.ok === false) return { ok: false, reason: combatData?.reason || 'combat_context_unresolved' };
      const 妯″潡鍔犺浇缁撴灉 = await 纭繚妯″潡渚濊禆宸插姞杞?'鎴樻枟妯″潡', 'open_map_battle_module');
      if (!妯″潡鍔犺浇缁撴灉 || 妯″潡鍔犺浇缁撴灉.ok === false) {
        return { ok: false, reason: '鎴樻枟妯″潡鍔犺浇澶辫触銆? };
      }
      const playerName = toText(combatData.鍙傛垬鑰?player.name, '鐜╁');
      const enemyName = toText(combatData.鍙傛垬鑰?enemy.name, '瀵规墜');
      const compactCombatData = compactCombatDataForWorldStorage(snapshot, combatData);
      const activeCharKey = resolveSnapshotCharKey(snapshot, toText(snapshot.activeName, '')) || toText(snapshot.activeName, '');
      const towerResetPatches = [];
      const towerEntryTicketPatches = [];
      if (combatData.鎴樻枟绫诲瀷 === '榄傜伒濉斿啿濉?) {
        const currentDiscount = normalizeSoulTowerDiscountSpiritRecord(deepGet(snapshot, 'activeChar.榄傜伒濉旇褰?褰撳墠浜旀姌榄傜伒', {}));
        if (activeCharKey && currentDiscount.灞傛暟 > 0) {
          towerResetPatches.push({
            op: 'replace',
            path: `/char/${escapeJsonPointerValue(activeCharKey)}/榄傜伒濉旇褰?褰撳墠浜旀姌榄傜伒`,
            value: createEmptySoulTowerDiscountSpiritRecord(),
          });
        }
        const alreadyConsumedTicket = (trialBattleDetail || dispatchDetail) && (
          (trialBattleDetail || dispatchDetail).闂ㄧエ宸叉墸闄?=== true
        );
        if (!alreadyConsumedTicket) {
          const towerTicketName = toText((trialBattleDetail || dispatchDetail) && ((trialBattleDetail || dispatchDetail).soulTowerTicket || (trialBattleDetail || dispatchDetail).ticketName), '').trim();
          if (!towerTicketName) return { ok: false, reason: '缂哄皯榄傜伒濉旈棬绁ㄣ€? };
          const ticketConsumeResult = buildInventoryConsumePatches(snapshot, activeCharKey, towerTicketName, 1);
          if (!ticketConsumeResult.ok) return { ok: false, reason: ticketConsumeResult.reason || '榄傜伒濉旈棬绁ㄤ笉瓒炽€? };
          towerEntryTicketPatches.push(...ticketConsumeResult.patchOps);
        }
      }
      const soulTowerText = combatData.鎴樻枟绫诲瀷 === '榄傜伒濉斿啿濉?
        ? ` ${toText(combatData.澶у叧鏍囩, '榄傜伒濉?)} 绗?{toNumber(combatData.floor, 1)}灞俙
        : '';
      battleInlineDismissed = false;
      await applyJsonPatchOpsByEditor([
        ...towerEntryTicketPatches,
        ...towerResetPatches,
        { op: 'replace', path: '/world/鎴樻枟', value: compactCombatData },
        { op: 'replace', path: '/sys/绯荤粺鎾姤', value: `[鎴樻枟妯″潡] ${playerName} 鍚?${enemyName} 鍙戣捣${combatData.鎴樻枟绫诲瀷 === '榄傜伒濉斿啿濉? ? `榄傜伒濉旀寫鎴?{soulTowerText}` : '鍒囩'}锛屾垬鏂楁ā鍧楀凡鎺ョ銆俙 }
      ]);
      await refreshLiveSnapshot({ force: true, reopenBattle: true });
      try {
        if (typeof window.__MVU_OPEN_BATTLE_UI__ === 'function') {
          await window.__MVU_OPEN_BATTLE_UI__();
        }
      } catch (error) {
        console.warn('[DragonUI] 鑷姩鎵撳紑鎴樻枟缁堢澶辫触銆?, error);
      }
      showUiToast('鎴樻枟妯″潡宸插紑鍚€?, 'info', 2400);
      return { ok: true, combatData };
    }

    async ) {
      const sourceSnapshot = snapshot && snapshot.rootData ? snapshot : (liveSnapshot || lastRenderableSnapshot);
      if (!sourceSnapshot || !sourceSnapshot.rootData) return { ok: false, reason: '褰撳墠蹇収鏈氨缁紝鏃犳硶寮€鍚瓊鐏靛銆? };
      const entryLocation = toText(
        options.entryLocation,
        toText(deepGet(sourceSnapshot, 'activeChar.鐘舵€?浣嶇疆', sourceSnapshot.currentLoc), sourceSnapshot.currentLoc),
      );
      if (!isSoulTowerHeadquartersLocation(entryLocation)) {
        return { ok: false, reason: '榄傜伒濉斿彧鑳戒粠鍙茶幈鍏嬪煄浼犵伒濉旀€婚儴杩涘叆銆? };
      }
      const activeChar = getActiveSnapshotCharacter(sourceSnapshot);
      if (!isSoulTowerEligibleCharacterData(activeChar)) {
        return { ok: false, reason: '褰撳墠瑙掕壊涓嶆弧瓒抽瓊鐏靛鍏ュ鏉′欢銆? };
      }
      const soulTowerTickets = listSoulTowerTicketNames(sourceSnapshot);
      const selectedTicket = toText(options.ticketName, soulTowerTickets[0] || '').trim();
      if (!selectedTicket) {
        return { ok: false, reason: '缂哄皯榄傜伒濉旈棬绁ㄣ€? };
      }
      const requestedFloor = Math.floor(toNumber(options.floor, getDefaultSoulTowerChallengeFloor(sourceSnapshot)));
      const floor = Math.min(SOUL_TOWER_TOTAL_FLOORS, Math.max(1, requestedFloor));
      try {
        const result = await 鐧昏璇曠偧鍏ュ満骞舵帹杩涘墽鎯?sourceSnapshot, {
          璇曠偧绫诲瀷: '榄傜伒濉?,
          ticketName: selectedTicket,
          floor,
          entryLocation,
        });
        return { ok: !!(result && result.ok), context: result && result.context ? result.context : null, dispatchResult: result && result.dispatchResult ? result.dispatchResult : null };
      } catch (error) {
        return { ok: false, reason: error && error.message ? error.message : '榄傜伒濉斿叆鍦哄け璐ャ€? };
      }
    }

    function buildTradeDispatchFromRequest(snapshot, tradeRequest) {
      const req = tradeRequest && typeof tradeRequest === 'object' ? tradeRequest : {};
      const actionText = toText(req.鍔ㄤ綔, '');
      let action = 'trade';
      const services = [];
      if (/绔炴媿|鎷嶅崠|auction|bid/i.test(actionText)) {
        action = 'bid';
        services.push('auction');
      } else if (/搴楅摵|閲囪喘|璐拱|shop/i.test(actionText) && !toText(req.瀵硅薄, '')) {
        action = 'shop';
        services.push('shop');
      } else if (/鍑哄敭|鍗栧嚭|sell/i.test(actionText) && !toText(req.瀵硅薄, '')) {
        action = 'sell';
      }
      return {
        action,
        target: toText(req.鐩爣, toText(snapshot && snapshot.currentLoc, '')),
        currentLoc: toText(snapshot && snapshot.currentLoc, ''),
        npcTarget: toText(req.瀵硅薄, ''),
        services,
        tradeRequest: cloneJsonValue(req, {})
      };
    }

    function maybeOpenTradeRequestFromAI(snapshot) {
      const tradeRequest = deepGet(snapshot, 'rootData.world.浜ゆ槗璇锋眰', {});
      const action = toText(tradeRequest && tradeRequest.鍔ㄤ綔, '');
      const status = toText(tradeRequest && tradeRequest.鐘舵€? 'pending');
      if (!tradeRequest || typeof tradeRequest !== 'object' || !action || action === '鏃? || /handled|done|瀹屾垚|宸插鐞唡鍙栨秷|cancel/i.test(status)) {
        lastAutoTradeRequestSignature = '';
        hasHydratedAutoTradeRequestSignature = true;
        return;
      }
      if (!isSnapshotPlayerControlled(snapshot)) return;
      const signature = (() => {
        try { return JSON.stringify(tradeRequest); } catch (error) { return `${action}:${Date.now()}`; }
      })();
      if (!hasHydratedAutoTradeRequestSignature) {
        lastAutoTradeRequestSignature = signature;
        hasHydratedAutoTradeRequestSignature = true;
        return;
      }
      if (signature === lastAutoTradeRequestSignature) return;
      lastAutoTradeRequestSignature = signature;
      mapDispatchContext = buildTradeDispatchFromRequest(snapshot, tradeRequest);
      openModal('浜ゆ槗缃戠粶', { preserveMapDispatchContext: true });
    }

    function buildMapBattleInitRequest(snapshot, dispatchDetail) {
      const detail = dispatchDetail || {};
      let npcTarget = toText(detail.npcTarget, '');
      const npcTargets = Array.isArray(detail.npcTargets) ? detail.npcTargets.map(item => toText(item, '')).filter(Boolean) : [];
      if (!npcTarget && npcTargets.length) npcTarget = npcTargets[0];
      if (!snapshot || !snapshot.rootData || !npcTarget) return null;

      const activeKey = resolveSnapshotCharKey(snapshot, toText(snapshot.activeName, ''));
      const targetKey = resolveSnapshotCharKey(snapshot, npcTarget);
      if (!activeKey || !targetKey) return null;

      const chars = deepGet(snapshot, 'rootData.char', {});
      const activeChar = chars && typeof chars === 'object' ? (chars[activeKey] || {}) : {};
      const targetChar = chars && typeof chars === 'object' ? (chars[targetKey] || {}) : {};
      const activeName = toText(activeChar && (activeChar.name || deepGet(activeChar, 'base.name', '')), activeKey);
      const targetName = toText(targetChar && (targetChar.name || deepGet(targetChar, 'base.name', '')), npcTarget || targetKey);
      const arenaName = toText(detail.currentLoc, toText(detail.target, toText(snapshot.currentLoc, '鏈煡鍦扮偣')));
      const relationMap = deepGet(activeChar, '绀句氦.鍏崇郴', {});
      const relationData = relationMap && typeof relationMap === 'object'
        ? (relationMap[targetName] || relationMap[targetKey] || {})
        : {};
      const relationSummary = relationData && typeof relationData === 'object'
        ? `${toText(relationData['鍏崇郴'], '闄岀敓')} / ${toText(relationData['鍏崇郴璺嚎'], '鏈嬪弸绾?)} / 濂芥劅 ${toNumber(relationData['濂芥劅搴?], 0)}`
        : '鏆傛棤鏄庣‘鍏崇郴璁板綍';
      const activeLoc = toText(deepGet(activeChar, '鐘舵€?浣嶇疆', arenaName), arenaName || '鏈煡鍦扮偣');
      const targetLoc = toText(deepGet(targetChar, '鐘舵€?浣嶇疆', arenaName), arenaName || '鏈煡鍦扮偣');
      const systemPrompt = `浠ヤ笅鍐呭灞炰簬鍓嶇浠ｅ彂鐨勫湴鍥惧垏纾嬭姹傦紝璇风洿鎺ユ壙鎺ュ墽鎯呮帹杩涳紝涓嶈杈撳嚭 JSON 鍧楁垨鍙橀噺缁存姢鎸囦护銆?[鍒囩璇锋眰] ${activeName} 鍦ㄣ€?{arenaName}銆戝悜銆?{targetName}銆戝彂璧峰垏纾嬨€?[鍒囩鍘熷垯]
杩欐槸涓€鍦洪粯璁や互鍒囩銆佽瘯鎺€佽緝閲忎负鐩殑鐨勪氦鎵嬶紝涓嶄互鍑绘潃涓虹洰鏍囷紱璇疯嚜鐒舵壙鎺ヤ负鐐瑰埌涓烘銆佸彲琚洿瑙傛垨琚埗姝㈢殑鎴樻枟鍦烘櫙锛岃€屼笉鏄棤绔崌绾ф垚姝绘垬銆?[瑙掕壊鍙傝€僝
鍙戣捣鑰咃細${activeName} / 褰撳墠鍦扮偣 ${activeLoc}
鐩爣锛?{targetName} / 褰撳墠鍦扮偣 ${targetLoc}
鍙屾柟鍏崇郴锛?{relationSummary}
[瑁佸畾瑕佹眰]
浣犻渶瑕佹壙鎺ヨ繖娆″垏纾嬭姹傦紝鑷劧鍐欏嚭鍙屾柟鏄惁搴旀垬銆佸紑鍦哄Э鎬佷笌鍚庣画鎴樻枟鎺ㄨ繘锛涜嫢鍓ф儏涓婁笉閫傚悎绔嬪埢寮€鎵擄紝涔熻浠モ€滃垏纾嬭姹傚凡缁忔彁鍑衡€濅负鍓嶆彁缁欏嚭鑷劧鍥炲簲锛岃€屼笉鏄拷鐣ヨ繖娆¤姹傘€俙;

      return {
        playerInput: `鎴戞兂鍦ㄣ€?{arenaName}銆戜笌銆?{targetName}銆戝垏纾嬨€俙,
        systemPrompt,
        requestKind: toText(detail.requestKind, 'map_sparring')
      };
    }

    function buildMapInteractDispatchRequest(snapshot, dispatchDetail) {
      const detail = dispatchDetail || {};
      const action = toText(detail.action, '');
      let npcTarget = toText(detail.npcTarget, '');
      const npcTargets = Array.isArray(detail.npcTargets) ? detail.npcTargets.map(item => toText(item, '')).filter(Boolean) : [];
      if (!npcTarget && npcTargets.length) npcTarget = npcTargets[0];

      const interactActionMap = {
        talk: '闂茶亰',
        brief: '璇锋暀',
        intel: '璇锋暀',
        ask: '璇锋暀',
        gift: '閫佺ぜ',
        confess: '琛ㄧ櫧',
        dual: '鍙屼慨'
      };
      const interactAction = toText(interactActionMap[action], '');
      if (!snapshot || !snapshot.rootData || !npcTarget || !interactAction) return null;

      const activeKey = resolveSnapshotCharKey(snapshot, toText(snapshot.activeName, ''));
      if (!activeKey) return null;

      const chars = deepGet(snapshot, 'rootData.char', {});
      const activeChar = chars && typeof chars === 'object' ? (chars[activeKey] || {}) : {};
      const activeName = toText(activeChar && (activeChar.name || deepGet(activeChar, 'base.name', '')), toText(snapshot.activeName, activeKey));
      const resolvedTarget = resolveSnapshotCharacter(snapshot, npcTarget);
      const targetName = toText(resolvedTarget.displayName, npcTarget);
      const relationMap = deepGet(snapshot, 'activeChar.绀句氦.鍏崇郴', {});
      const relationData = relationMap && typeof relationMap === 'object'
        ? (relationMap[targetName] || relationMap[resolvedTarget.key] || {})
        : {};
      const arenaName = toText(detail.currentLoc, toText(detail.target, toText(snapshot.currentLoc, '鏈煡鍦扮偣')));
      const currentTick = toNumber(deepGet(snapshot, 'rootData.world.鏃堕棿.tick', 0), 0);
      const itemUsed = interactAction === '閫佺ぜ'
        ? (toText(detail.itemUsed, toText(detail.item_used, '鏃?)) || '鏃?)
        : '鏃?;
      const targetChar = resolvedTarget && resolvedTarget.char && typeof resolvedTarget.char === 'object' ? resolvedTarget.char : {};
      const activeIdentity = toText(deepGet(activeChar, '绀句氦.涓昏韩浠?, '鏃?), '鏃?);
      const targetIdentity = toText(deepGet(targetChar, '绀句氦.涓昏韩浠?, '鏃?), '鏃?);
      const activePersonality = toText(deepGet(activeChar, '鎬ф牸', '鏈瀹?), '鏈瀹?);
      const targetPersonality = toText(deepGet(targetChar, '鎬ф牸', '鏈瀹?), '鏈瀹?);
      const activeLoc = toText(deepGet(activeChar, '鐘舵€?浣嶇疆', arenaName), arenaName || '鏈煡鍦扮偣');
      const targetLoc = toText(deepGet(targetChar, '鐘舵€?浣嶇疆', '鏈煡鍦扮偣'), '鏈煡鍦扮偣');
      const activeFactions = Object.keys(deepGet(activeChar, '绀句氦.鍔垮姏', {})).filter(Boolean);
      const targetFactions = Object.keys(deepGet(targetChar, '绀句氦.鍔垮姏', {})).filter(Boolean);
      const recentInteractText = relationData && typeof relationData === 'object'
        ? `${toText(relationData['涓婃浜掑姩鍔ㄤ綔'], '鏃?)} / ${toNumber(relationData['鏈€杩戝ソ鎰熷彉鍖?], 0)}`
        : '鏃?;
      const progressNote = relationData && typeof relationData === 'object' ? toText(deepGet(relationData, '_鎺ㄨ繘鎻愮ず', '鏆傛棤'),'鏆傛棤') : '鏆傛棤';
      const routeSwitchable = !!deepGet(relationData, '_鍙垏绾?, false);
      const sourceLabel = toText(detail.sourceLabel, '鍦板浘 NPC 浜掑姩');
      const actionPromptMap = {
        talk: `鎴戞兂鍦ㄣ€?{arenaName}銆戝拰銆?{targetName}銆戦棽鑱娿€俙,
        brief: `鎴戞兂鍦ㄣ€?{arenaName}銆戝悜銆?{targetName}銆戞眹鎶ユ儏鍐靛苟璇风ず瀹夋帓銆俙,
        intel: `鎴戞兂鍦ㄣ€?{arenaName}銆戝悜銆?{targetName}銆戣鏁欐儏鎶ャ€俙,
        ask: `鎴戞兂鍦ㄣ€?{arenaName}銆戝悜銆?{targetName}銆戣鏁欎慨鐐煎績寰椼€俙,
        gift: itemUsed !== '鏃?
          ? `鎴戞兂鍦ㄣ€?{arenaName}銆戝悜銆?{targetName}銆戣禒閫併€?{itemUsed}銆戙€俙
          : `鎴戞兂鍦ㄣ€?{arenaName}銆戝悜銆?{targetName}銆戦€佺ぜ銆俙,
        confess: `鎴戞兂鍦ㄣ€?{arenaName}銆戝悜銆?{targetName}銆戣〃鐧斤紝鏄庣‘褰兼蹇冩剰銆俙,
        dual: `鎴戞兂鍦ㄣ€?{arenaName}銆戜笌銆?{targetName}銆戣繘琛屽弻淇€俙
      };

      const relationSummary = relationData && typeof relationData === 'object'
        ? `${toText(relationData['鍏崇郴'], '闄岀敓')} / 璺嚎 ${toText(relationData['鍏崇郴璺嚎'], '鏈嬪弸绾?)} / 濂芥劅 ${toNumber(relationData['濂芥劅搴?], 0)}`
        : '鏆傛棤宸茬煡鍏崇郴璁板綍';

      const systemPrompt = `浠ヤ笅鍐呭灞炰簬鍓嶇浠ｅ彂鐨?{sourceLabel}璇锋眰锛岃鐩存帴鎵挎帴鍓ф儏鎺ㄨ繘锛屼笉瑕佽緭鍑?JSON 鍧楁垨鍙橀噺缁存姢鎸囦护銆?
[绀句氦浜掑姩] ${activeName} 鍦ㄣ€?{arenaName}銆戝銆?{targetName}銆戝彂璧枫€?{interactAction}銆戙€?
[鍏崇郴鍙傝€僝
褰撳墠鍏崇郴锛?{relationSummary}

[瑙掕壊琛ュ厖]
鍙戣捣鑰咃細${activeName} / 韬唤 ${activeIdentity} / 鎬ф牸 ${activePersonality} / 鎵€灞炲娍鍔?${activeFactions.join(' / ') || '鏃?} / 鎵€鍦ㄥ湴 ${activeLoc}
鐩爣锛?{targetName} / 韬唤 ${targetIdentity} / 鎬ф牸 ${targetPersonality} / 鎵€灞炲娍鍔?${targetFactions.join(' / ') || '鏃?} / 鎵€鍦ㄥ湴 ${targetLoc}
鏈€杩戜簰鍔細${recentInteractText}
鎺ㄨ繘鎻愮ず锛?{progressNote}
褰撳墠鏄惁鍙垏鎭嬩汉绾匡細${routeSwitchable ? '鏄? : '鍚?}
棰勮鑰楁椂锛?{currentTick >= 0 ? '绾?2 tick' : '鐭椂浜掑姩'}

[浜掑姩瑁佸畾鍘熷垯]
璇风患鍚堣€冭檻鍩虹濂芥劅銆佸綋鍓嶅叧绯婚樁娈典笌璺嚎銆佸弻鏂规€ф牸銆佽韩浠戒笌鐜板疄鍘嬪姏銆佹墍灞炲娍鍔涘甫鏉ョ殑绔嬪満褰卞搷銆佸綋鍓嶅湴鐐规槸鍚﹂€傚悎灞曞紑璇ヤ簰鍔紝浠ュ強鏈€杩戜簰鍔ㄧ殑寤剁画鎬с€傝嫢浜掑姩鏃犳硶鎴愮珛锛岃鍦ㄥ墽鎯呴噷鑷劧缁欏嚭鍘熷洜銆?{interactAction === '閫佺ぜ' ? ` 閫佺ぜ鐗╁搧涓恒€?{itemUsed}銆戯紱璇峰垽鏂繖浠界ぜ鐗╂槸鍚﹀鐩爣鎬ф牸銆佽韩浠姐€佸澧冧笌褰撲笅姘旀皼瀵瑰彛銆俙 : ''}${interactAction === '琛ㄧ櫧' ? ' 琛ㄧ櫧鏃惰鍖哄垎鈥滆瘯鐫€浜ゅ線/杩涘叆鏆ф槯鎺ㄨ繘鈥濅笌鈥滄寮忕‘璁ゅ叧绯烩€濅袱绉嶇粨鏋溿€? : ''}${interactAction === '鍙屼慨' ? ' 鍙屼慨灞炰簬楂樹翰瀵嗗害浜掑姩锛岃嫢浜虹墿鐘舵€併€佹€ф牸銆佸満鏅垨鍏崇郴闃舵涓嶅尮閰嶏紝搴旀槑纭啓鍑烘棤娉曟垚绔嬬殑鐞嗙敱銆? : ''}${interactAction === '璇锋暀' ? ' 璇锋暀涓嶄粎鐪嬪ソ鎰燂紝涔熻鐪嬬洰鏍囨槸鍚︽効鎰忎紶鎺堛€佸弻鏂瑰綋鍓嶅叧绯绘槸鍚﹂€傚悎锛屼互鍙婂綋涓嬪満鏅槸鍚︿究浜庤鐪熶氦娴併€? : ''}`;

      return {
        playerInput: actionPromptMap[action] || `鎴戞兂鍦ㄣ€?{arenaName}銆戜笌銆?{targetName}銆戜簰鍔ㄣ€俙,
        systemPrompt,
        requestKind: toText(detail.requestKind, 'map_interaction')
      };
    }

    function getChatSendTextarea() {
      return document.getElementById('send_textarea') || document.querySelector('#send_form textarea');
    }

    function 鍐欏叆閫夐」鍒拌緭鍏ユ(閫夐」鏂囨湰 = '') {
      const 杈撳叆妗?= getChatSendTextarea();
      if (!杈撳叆妗?|| !('value' in 杈撳叆妗?) return false;
      const 瑙勮寖鏂囨湰 = toText(閫夐」鏂囨湰, '').trim();
      if (!瑙勮寖鏂囨湰) return false;
      const 鍐欏叆鏂囨湰 = `鎴戦€夋嫨锛?{瑙勮寖鏂囨湰}`;
      杈撳叆妗?value = 鍐欏叆鏂囨湰;
      杈撳叆妗?dispatchEvent(new Event('input', { bubbles: true }));
      if (typeof 杈撳叆妗?focus === 'function') 杈撳叆妗?focus();
      if (typeof 杈撳叆妗?setSelectionRange === 'function') {
        杈撳叆妗?setSelectionRange(鍐欏叆鏂囨湰.length, 鍐欏叆鏂囨湰.length);
      }
      return true;
    }

    function extractBracketTokens(text) {
      const tokens = [];
      String(text || '').replace(/銆?[^銆慮{1,80})銆?g, (_, token) => {
        const value = toText(token, '').trim();
        if (value) tokens.push(value);
        return _;
      });
      return tokens;
    }

    function getSnapshotActiveCharName(snapshot) {
      return resolveEffectivePlayerName(
        deepGet(snapshot, 'rootData', {}),
        toText(snapshot && snapshot.activeName, '')
      ).trim();
    }

    function schedulePlayerNameRepair(snapshot, forcedPlayerName = '') {
      const rootData = deepGet(snapshot, 'rootData', {});
      if (!rootData || typeof rootData !== 'object') return;
      const currentPlayerName = toText(deepGet(rootData, 'sys.鐜╁鍚?, ''), '').trim();
      const inferredPlayerName = toText(forcedPlayerName, '').trim()
        || resolveEffectivePlayerName(rootData, toText(snapshot && snapshot.activeName, ''));
      if (!isInvalidRuntimePlayerName(currentPlayerName) && !forcedPlayerName) return;
      if (!inferredPlayerName || isInvalidRuntimePlayerName(inferredPlayerName)) return;
      const inferredChar = deepGet(rootData, ['char', inferredPlayerName], null);
      if (!inferredChar || typeof inferredChar !== 'object') return;
      const repairKey = `${toText(snapshot && snapshot.activeName, '')}::${inferredPlayerName}`;
      if (playerNameRepairInFlight === repairKey) return;
      playerNameRepairInFlight = repairKey;
      Promise.resolve()
        .then(() => replaceStatDataByEditor([{ path: ['sys', '鐜╁鍚?], value: inferredPlayerName }]))
        .catch(error => {
          console.warn('[DragonUI] 鑷姩淇鐜╁鍚嶅け璐ャ€?, error);
        })
        .finally(() => {
          if (playerNameRepairInFlight === repairKey) playerNameRepairInFlight = '';
        });
    }

    function resolveDirectIntentCharName(snapshot, rawName) {
      const key = resolveSnapshotCharKey(snapshot, rawName);
      if (!key) return '';
      const charData = deepGet(snapshot, `rootData.char.${key}`, {});
      return toText(charData?.name || deepGet(charData, 'base.name', '') || key, key);
    }

    function findMentionedCharacterName(snapshot, text, options = {}) {
      const activeName = toText(options.activeName, getSnapshotActiveCharName(snapshot));
      const excludedNames = new Set([activeName, ...(options.exclude || [])].map(item => toText(item, '').trim()).filter(Boolean));
      const tokens = extractBracketTokens(text);
      for (let index = tokens.length - 1; index >= 0; index -= 1) {
        const resolvedName = resolveDirectIntentCharName(snapshot, tokens[index]);
        if (resolvedName && !excludedNames.has(resolvedName)) return resolvedName;
      }

      const chars = deepGet(snapshot, 'rootData.char', {});
      const candidates = Object.entries(chars && typeof chars === 'object' ? chars : {})
        .flatMap(([key, charData]) => [
          toText(charData?.name, ''),
          toText(deepGet(charData, 'base.name', ''), ''),
          toText(key, '')
        ].filter(Boolean))
        .filter((name, index, list) => list.indexOf(name) === index && !excludedNames.has(name))
        .sort((a, b) => b.length - a.length);
      return candidates.find(name => name && String(text || '').includes(name)) || '';
    }

    function findBracketLocationToken(snapshot, text) {
      const tokens = extractBracketTokens(text);
      const currentLoc = toText(snapshot && snapshot.currentLoc, '');
      const normalizedLoc = toText(snapshot && snapshot.normalizedLoc, '');
      for (const token of tokens) {
        if (resolveDirectIntentCharName(snapshot, token)) continue;
        if (token.includes('-') || token === currentLoc || token === normalizedLoc) return token;
      }
      return currentLoc || normalizedLoc || '鏈煡鍦扮偣';
    }

    function parseDirectIntentCount(text, fallback = 1) {
      const digitMatch = String(text || '').match(/(?:鏁伴噺|涔皘璐拱|涔板叆|閲囪喘|鍗東鍑哄敭|鍗栧嚭|绔炴媿|鍑轰环)?\s*(\d{1,4})\s*(?:浠絴涓獆浠秥鏋殀鐡秥鏍獆鍧梶鍖厊寮爘濂??/);
      if (digitMatch) return Math.max(1, toNumber(digitMatch[1], fallback));
      const map = { 涓€: 1, 浜? 2, 涓? 2, 涓? 3, 鍥? 4, 浜? 5, 鍏? 6, 涓? 7, 鍏? 8, 涔? 9, 鍗? 10 };
      const chineseMatch = String(text || '').match(/([涓€浜屼袱涓夊洓浜斿叚涓冨叓涔濆崄]{1,3})\s*(?:浠絴涓獆浠秥鏋殀鐡秥鏍獆鍧梶鍖厊寮爘濂?/);
      if (!chineseMatch) return fallback;
      const raw = chineseMatch[1];
      if (raw === '鍗?) return 10;
      if (raw.includes('鍗?)) {
        const [tensRaw, onesRaw] = raw.split('鍗?);
        return Math.max(1, (map[tensRaw] || 1) * 10 + (map[onesRaw] || 0));
      }
      return Math.max(1, map[raw] || fallback);
    }

    function normalizeProfessionMode(rawMode) {
      const value = toText(rawMode, '').trim().toLowerCase();
      if (/manufacture|鍒堕€爘缁勮|鎬昏|灏佽/.test(value)) return 'manufacture';
      if (/design|璁捐|鍥剧焊|钃濆浘/.test(value)) return 'design';
      if (/repair|淇悊|缁翠慨|缁存姢|淇|鏁村/.test(value)) return 'repair';
      if (/forge|閿婚€爘閿绘墦|铻嶉敾|鐧鹃敾|鍗冮敾|鐏甸敾|榄傞敾|澶╅敾/.test(value)) return 'forge';
      return '';
    }

    function normalizeProfessionActionLabel(mode) {
      return ({ forge: '閿婚€?, manufacture: '鍒堕€?, design: '璁捐', repair: '淇悊' })[mode] || '鍓亴涓氭搷浣?;
    }

    function parseDirectProfessionTier(text) {
      const raw = toText(text, '');
      if (/澶╅敾|鍥涘瓧|绾㈢骇/.test(raw)) return 5;
      if (/榄傞敾|涓夊瓧|榛戠骇/.test(raw)) return 4;
      if (/鐏甸敾|浜屽瓧/.test(raw)) return 3;
      if (/鍗冮敾|涓€瀛梶绱骇/.test(raw)) return 2;
      const match = raw.match(/([1-5涓€浜屼袱涓夊洓浜擼)\s*(?:闃秥绾?/);
      const map = { 涓€: 1, 浜? 2, 涓? 2, 涓? 3, 鍥? 4, 浜? 5 };
      return match ? Math.max(1, Math.min(5, Number(match[1]) || map[match[1]] || 1)) : 1;
    }

    function parseDirectProfessionSubtype(text) {
      const raw = toText(text, '');
      if (/鏂楅摖|涓€瀛梶浜屽瓧|涓夊瓧|鍥涘瓧/.test(raw)) return 'armor';
      if (/鏈虹敳|榛勭骇|绱骇|榛戠骇|绾㈢骇/.test(raw)) return 'mech';
      return '';
    }

    function findDirectProfessionTarget(snapshot, text, npcName = '') {
      const currentLoc = toText(snapshot && snapshot.currentLoc, '');
      const normalizedLoc = toText(snapshot && snapshot.normalizedLoc, '');
      const tokens = extractBracketTokens(text);
      for (let index = tokens.length - 1; index >= 0; index -= 1) {
        const token = tokens[index];
        if (!token || token === npcName || token === currentLoc || token === normalizedLoc || token.includes('-')) continue;
        if (resolveDirectIntentCharName(snapshot, token)) continue;
        return token;
      }
      const match = String(text || '').match(/(?:閿婚€爘閿绘墦|鍒堕€爘缁勮|璁捐|缁樺埗|淇悊|缁翠慨|缁存姢|淇|鏁村)\s*([^锛屻€傦紒锛焅s]{2,32})/);
      return toText(match && match[1], '').trim();
    }

    function findDirectProfessionMaterials(snapshot, text, targetName = '', npcName = '') {
      const currentLoc = toText(snapshot && snapshot.currentLoc, '');
      const normalizedLoc = toText(snapshot && snapshot.normalizedLoc, '');
      return extractBracketTokens(text).filter(token => {
        if (!token || token === targetName || token === npcName || token === currentLoc || token === normalizedLoc || token.includes('-')) return false;
        return !resolveDirectIntentCharName(snapshot, token);
      });
    }

    function buildProfessionRequestFromObject(snapshot, source) {
      const req = source && typeof source === 'object' ? source : {};
      const mode = normalizeProfessionMode(req.妯″紡 || req.鍔ㄤ綔 || req.鍓亴涓?|| req.鑱屼笟 || req.绫诲瀷);
      if (!mode) return null;
      const npc = toText(req.瀵硅薄 || req.鎵ц鑰?|| req.鎵ц鑰呭悕绉? '');
      return {
        妯″紡: mode,
        鍔ㄤ綔: normalizeProfessionActionLabel(mode),
        鐩爣: toText(req.鐩爣 || req.浜х墿 || req.鐗╁搧, ''),
        鏉愭枡: Array.isArray(req.鏉愭枡)
          ? req.鏉愭枡.map(item => {
            if (item && typeof item === 'object') {
              return toText(item.鍚嶇О || item.name || item.鏉愭枡 || item.鐗╁搧, '');
            }
            return toText(item, '');
          }).filter(Boolean)
          : (req.鏉愭枡 && typeof req.鏉愭枡 === 'object'
            ? Object.keys(req.鏉愭枡).filter(Boolean)
            : toText(req.鏉愭枡, '')
              .split(/[銆?锛寍/]+/)
              .map(item => item.trim())
              .filter(Boolean)),
        鏁伴噺: Math.max(1, toNumber(req.鏁伴噺 || req.鑰楁潗鏁伴噺, 1)),
        闃剁骇: Math.max(1, Math.min(5, toNumber(req.闃剁骇 || req.绛夌骇, 1))),
        瀛愮被鍨? toText(req.瀛愮被鍨?|| req.鐩爣绫诲瀷, ''),
        瀵硅薄: npc,
        鎵ц鑰呯被鍨? toText(req.鎵ц鑰呯被鍨?|| (npc ? 'private' : ''), ''),
        鐩爣鍦扮偣: toText(req.鐩爣鍦扮偣 || req.鍦扮偣, toText(snapshot && snapshot.currentLoc, '')),
        鐘舵€? toText(req.鐘舵€? req.鑷姩鎵ц ? 'ready' : 'pending'),
        鑷姩鎵ц: req.鑷姩鎵ц === true,
        鏉ユ簮: toText(req.鏉ユ簮, 'module_intent_router')
      };
    }

    function buildDirectProfessionRequest(snapshot, textOrPlan) {
      if (!snapshot || !snapshot.rootData) return null;
      if (textOrPlan && typeof textOrPlan === 'object') return buildProfessionRequestFromObject(snapshot, textOrPlan);
      const raw = toText(textOrPlan, '').trim();
      if (!raw || raw.startsWith('/') || /<UpdateVariable|<JSONPatch/i.test(raw)) return null;
      const mode = normalizeProfessionMode(raw);
      if (!mode) return null;
      if (/(鏄粈涔坾涓轰粈涔坾鎬庝箞|濡備綍|浼氫笉浼殀鑳戒笉鑳絴濡傛灉|鍋囧|瑙勫垯|鏈哄埗|璇存槑|瑙ｉ噴|璁ㄨ|鍚|鍥炲繂)/.test(raw)
        && !/(鎴戞兂|鎴戣|鎴戝噯澶噟鎴戞墦绠梶鐜板湪|鐩存帴|寮€濮媩鍙戣捣|濮旀墭|鍔炵悊|鐢▅鎶妡缁檤甯畖鑷姩)/.test(raw)) {
        return null;
      }
      const npc = findMentionedCharacterName(snapshot, raw, { activeName: getSnapshotActiveCharName(snapshot) });
      const target = findDirectProfessionTarget(snapshot, raw, npc);
      if (!target && !/鎵撳紑|杩涘叆|鍔炵悊|宸ュ潑|鍗忎細|濮旀墭/.test(raw)) return null;
      const materials = findDirectProfessionMaterials(snapshot, raw, target, npc);
      return {
        妯″紡: mode,
        鍔ㄤ綔: normalizeProfessionActionLabel(mode),
        鐩爣: target,
        鏉愭枡: materials,
        鏁伴噺: parseDirectIntentCount(raw, 1),
        闃剁骇: parseDirectProfessionTier(raw),
        瀛愮被鍨? parseDirectProfessionSubtype(raw),
        瀵硅薄: npc,
        鎵ц鑰呯被鍨? /鍗忎細|瀹樻柟/.test(raw) ? 'official' : (npc || /濮旀墭|浠ｅ伐|甯?.test(raw) ? 'private' : 'self'),
        鐩爣鍦扮偣: findBracketLocationToken(snapshot, raw),
        鐘舵€? /鐩存帴|绔嬪嵆|纭|寮€濮媩鑷姩|鎵ц/.test(raw) ? 'ready' : 'pending',
        鑷姩鎵ц: /鐩存帴|绔嬪嵆|纭|寮€濮媩鑷姩|鎵ц/.test(raw),
        鏉ユ簮: 'direct_input_guard'
      };
    }

    function buildCraftDispatchFromRequest(snapshot, professionRequest) {
      const req = professionRequest && typeof professionRequest === 'object' ? professionRequest : {};
      return {
        action: 'craft',
        target: toText(req.鐩爣鍦扮偣 || req.鐩爣, toText(snapshot && snapshot.currentLoc, '')),
        currentLoc: toText(snapshot && snapshot.currentLoc, ''),
        npcTarget: toText(req.瀵硅薄, ''),
        executorType: toText(req.鎵ц鑰呯被鍨? toText(req.瀵硅薄, '') ? 'private' : 'self'),
        services: ['craft'],
        professionRequest: cloneJsonValue(req, {})
      };
    }

    function normalizeInlineModuleAction(actionData, moduleKind, request) {
      const data = actionData && typeof actionData === 'object' ? actionData : {};
      const playerInput = toText(data.playerInput, '').trim();
      const systemPrompt = toText(data.systemPrompt, '').trim();
      const requestKind = toText(data.requestKind, moduleKind ? `module_${moduleKind}` : 'module_inline');
      const patchOps = Array.isArray(data.patchOps) ? cloneJsonValue(data.patchOps, []) : [];
      if (!playerInput || !systemPrompt) return null;
      return {
        playerInput,
        systemPrompt,
        requestKind,
        patchOps,
        moduleKind: toText(moduleKind, ''),
        request: cloneJsonValue(request, {})
      };
    }

    async function captureInlineModuleAction(mountRunner, options = {}) {
      const timeoutMs = Math.max(200, Math.min(3000, toNumber(options.timeoutMs, 1200)));
      const container = document.createElement('div');
      container.className = 'mvu-inline-module-capture';
      container.style.cssText = 'position:fixed;left:-10000px;top:-10000px;width:1px;height:1px;overflow:hidden;opacity:0;pointer-events:none;';
      let component = null;
      let alertMessage = '';
      const originalAlert = window.alert;

      try {
        document.body.appendChild(container);
        window.alert = (message) => {
          alertMessage = toText(message, '妯″潡鏍￠獙鏈€氳繃銆?);
        };
        const result = await new Promise(resolve => {
          let settled = false;
          const finish = value => {
            if (settled) return;
            settled = true;
            resolve(value);
          };
          const timer = window.setTimeout(() => {
            finish({ ok: false, reason: alertMessage || 'inline_action_timeout' });
          }, timeoutMs);
          try {
            component = mountRunner(container, actionData => {
              window.clearTimeout(timer);
              finish({ ok: true, actionData });
            });
          } catch (error) {
            window.clearTimeout(timer);
            finish({ ok: false, reason: error && error.message ? error.message : 'inline_mount_failed', error });
          }
        });
        if (result && result.ok && result.actionData) return result;
        return { ok: false, reason: alertMessage || (result && result.reason) || 'inline_action_unavailable', error: result && result.error };
      } finally {
        window.alert = originalAlert;
        try {
          if (component && typeof component.destroy === 'function') component.destroy();
        } catch (error) {}
        try {
          if (container.parentNode) container.parentNode.removeChild(container);
        } catch (error) {}
      }
    }

    async function buildInlineTradeAction(snapshot, tradeRequest) {
      const 妯″潡鍔犺浇缁撴灉 = await 纭繚妯″潡渚濊禆宸插姞杞?'浜ゆ槗妯″潡', 'inline_trade_action');
      if (!妯″潡鍔犺浇缁撴灉 || 妯″潡鍔犺浇缁撴灉.ok === false) {
        return { ok: false, reason: 'trade_module_load_failed', detail: 妯″潡鍔犺浇缁撴灉 };
      }
      if (typeof window.mountTradeUI !== 'function') {
        return { ok: false, reason: 'trade_ui_unavailable' };
      }
      const dispatchDetail = buildTradeDispatchFromRequest(snapshot, tradeRequest);
      const launchOptions = buildMapTradeModalOptions(snapshot, dispatchDetail);
      const capture = await captureInlineModuleAction((container, done) => window.mountTradeUI(container, snapshot, {
        initialTab: launchOptions.initialTab,
        prefillNpc: launchOptions.prefillNpc,
        lockNpc: launchOptions.lockNpc,
        preferredStore: launchOptions.preferredStore,
        prefillAction: launchOptions.prefillAction,
        prefillItem: launchOptions.prefillItem,
        prefillQty: launchOptions.prefillQty,
        prefillPrice: launchOptions.prefillPrice,
        autoExecute: true,
        onTradeAction: done
      }), { timeoutMs: 1400 });
      if (!capture.ok) return capture;
      const inlineAction = normalizeInlineModuleAction(capture.actionData, 'trade', tradeRequest);
      return inlineAction ? { ok: true, inlineAction } : { ok: false, reason: 'trade_action_invalid' };
    }

    async function buildInlineProfessionAction(snapshot, professionRequest) {
      const 妯″潡鍔犺浇缁撴灉 = await 纭繚妯″潡渚濊禆宸插姞杞?'鑱屼笟妯″潡', 'inline_profession_action');
      if (!妯″潡鍔犺浇缁撴灉 || 妯″潡鍔犺浇缁撴灉.ok === false) {
        return { ok: false, reason: 'profession_module_load_failed', detail: 妯″潡鍔犺浇缁撴灉 };
      }
      if (typeof window.mountProfessionUI !== 'function') {
        return { ok: false, reason: 'profession_ui_unavailable' };
      }
      const dispatchContext = buildCraftDispatchFromRequest(snapshot, professionRequest);
      const capture = await captureInlineModuleAction((container, done) => window.mountProfessionUI(container, snapshot, {
        dispatchContext,
        professionRequest: cloneJsonValue(professionRequest, {}),
        autoExecute: true,
        onAction: done
      }), { timeoutMs: 1600 });
      if (!capture.ok) return capture;
      const inlineAction = normalizeInlineModuleAction(capture.actionData, 'profession', professionRequest);
      return inlineAction ? { ok: true, inlineAction } : { ok: false, reason: 'profession_action_invalid' };
    }

    function buildTradeRequestFromObject(snapshot, source) {
      const req = source && typeof source === 'object' ? source : {};
      const action = toText(req.鍔ㄤ綔 || req.妯″紡 || req.绫诲瀷, '浜ゆ槗');
      const npc = toText(req.瀵硅薄 || req.鐩爣瀵硅薄, '');
      const rawItem = req.鐗╁搧 || req.鐩爣鐗╁搧 || req.鐩爣;
      const item = rawItem && typeof rawItem === 'object'
        ? toText(rawItem.鍚嶇О || rawItem.name || rawItem.鐗╁搧, '')
        : toText(rawItem, '');
      if (!item) return null;
      return {
        鍔ㄤ綔: action,
        鐩爣: toText(req.鐩爣鍦扮偣 || req.鍦扮偣 || req.鐩爣, toText(snapshot && snapshot.currentLoc, '')),
        瀵硅薄: npc,
        鐗╁搧: item,
        鏁伴噺: Math.max(1, toNumber(req.鏁伴噺, 1)),
        浠锋牸: Math.max(0, toNumber(req.浠锋牸 || 0, 0)),
        璐у竵: toText(req.璐у竵, /閲戦瓊甯亅閲戝竵/.test(toText(req.璐у竵鏂囨湰 || req.text, '')) ? 'gold_coin' : 'fed_coin'),
        鐘舵€? toText(req.鐘舵€? req.鑷姩鎵ц ? 'ready' : 'pending'),
        鑷姩鎵ц: req.鑷姩鎵ц === true,
        鏉ユ簮: toText(req.鏉ユ簮, 'module_intent_router')
      };
    }

    function resolveModuleIntentPayload(input) {
      const outerPayload = input && typeof input === 'object' ? input : {};
      const nestedRequest = outerPayload.request && typeof outerPayload.request === 'object' ? outerPayload.request : null;
      const payload = nestedRequest
        ? {
          ...nestedRequest,
          module: outerPayload.module || nestedRequest.module,
          kind: outerPayload.kind || outerPayload.module || nestedRequest.kind || nestedRequest.module,
          type: outerPayload.type || nestedRequest.type,
          action_type: outerPayload.action_type || nestedRequest.action_type,
          鑷姩鎵ц: outerPayload.鑷姩鎵ц === true || nestedRequest.鑷姩鎵ц === true,
          autoExecute: outerPayload.autoExecute === true || nestedRequest.autoExecute === true,
          source: outerPayload.source || nestedRequest.source
        }
        : outerPayload;
      const text = typeof input === 'string'
        ? input
        : toText(outerPayload.text || outerPayload.plan || outerPayload.message || outerPayload.narrative || outerPayload.intent || outerPayload.raw
          || payload.text || payload.plan || payload.message || payload.narrative || payload.intent || payload.raw, '');
      const kind = toText(outerPayload.kind || outerPayload.type || outerPayload.module || outerPayload.action_type
        || payload.kind || payload.type || payload.module || payload.action_type || payload.鍔ㄤ綔, '').toLowerCase();
      return { payload, text, kind };
    }

    function 鏋勫缓妯″潡璺敱澶辫触缁撴灉(moduleKind = '', request = null, reason = 'module_route_failed', extra = {}) {
      return {
        handled: false,
        kind: moduleKind || '',
        request: request || null,
        reason: toText(reason, 'module_route_failed'),
        ...extra,
      };
    }

    function 鏋勫缓妯″潡璺敱鎴愬姛缁撴灉(moduleKind = '', request = null, extra = {}) {
      return {
        handled: true,
        kind: moduleKind || '',
        request: request || null,
        ...extra,
      };
    }

    function 璇诲彇蹇収褰撳墠浣嶇疆(snapshot) {
      return toText(
        deepGet(snapshot, 'activeChar.鐘舵€?浣嶇疆', snapshot && snapshot.currentLoc),
        snapshot && snapshot.currentLoc,
      );
    }

    function 褰掍竴鍖栨ā鍧楁剰鍥剧被鍨?kind = '') {
      const 鏂囨湰 = toText(kind, '').toLowerCase();
      if (/battle|combat|鎴樻枟|鍒囩|鍗曟寫/.test(鏂囨湰)) return 'battle';
      if (/trade|浜ゆ槗|璐拱|鍑哄敭|绔炴媿|鎷嶅崠/.test(鏂囨湰)) return 'trade';
      if (/craft|profession|job|鍓亴涓殀宸ュ潑|閿婚€爘鍒堕€爘璁捐|淇悊|缁翠慨/.test(鏂囨湰)) return 'profession';
      return '';
    }

    function 瑙ｆ瀽璇曠偧鍏ュ満鎰忓浘璇锋眰(snapshot, payload = {}, kind = '', narrativeText = '') {
      const 鍔ㄤ綔鏂囨湰 = toText(
        payload.action
          || payload.鍔ㄤ綔
          || payload.action_type
          || payload.module
          || payload.type
          || payload.kind
          || payload.requestKind
          || payload.request_kind
          || payload.璇曠偧绫诲瀷
          || payload.trialType
          || payload.trial,
        '',
      ).toLowerCase();
      const 璇嗗埆鏂囨湰 = `${toText(kind, '').toLowerCase()}|${鍔ㄤ綔鏂囨湰}|${toText(narrativeText, '').toLowerCase()}`.trim();
      let 璇曠偧绫诲瀷 = '';
      if (/tower|榄傜伒濉?.test(璇嗗埆鏂囨湰)) 璇曠偧绫诲瀷 = '榄傜伒濉?;
      else if (/ascension|鍗囩伒鍙?.test(璇嗗埆鏂囨湰)) 璇曠偧绫诲瀷 = '鍗囩伒鍙?;
      if (!璇曠偧绫诲瀷) return null;

      const 褰撳墠鍦扮偣 = toText(
        payload.currentLoc || payload.current_loc || payload.location || payload.entryLocation || payload.entry_location || payload.targetLocation,
        toText(deepGet(snapshot, 'activeChar.鐘舵€?浣嶇疆', snapshot.currentLoc), snapshot.currentLoc),
      );
      const 璇诲彇闂ㄧエ鍚嶇О = 榛樿闂ㄧエ => toText(
        payload.ticketName || payload.ticket_name || payload.ascensionTicket || payload.ascension_ticket
          || payload.soulTowerTicket || payload.soul_tower_ticket || payload.ticket || payload.闂ㄧエ鍚?
        榛樿闂ㄧエ || '',
      ).trim();
      if (璇曠偧绫诲瀷 === '鍗囩伒鍙?) {
        const 鍙敤闂ㄧエ = listAscensionTicketNames(snapshot);
        const ticketName = 璇诲彇闂ㄧエ鍚嶇О(鍙敤闂ㄧエ[0] || '');
        return {
          璇曠偧绫诲瀷,
          ticketName,
          entryLocation: 褰撳墠鍦扮偣,
        };
      }

      const 鍙敤闂ㄧエ = listSoulTowerTicketNames(snapshot);
      const ticketName = 璇诲彇闂ㄧエ鍚嶇О(鍙敤闂ㄧエ[0] || '');
      const 鏂囨湰灞傛暟鍖归厤 = toText(narrativeText, '').match(/绗琝s*(\d+)\s*灞?);
      const 鏂囨湰灞傛暟 = 鏂囨湰灞傛暟鍖归厤 ? toNumber(鏂囨湰灞傛暟鍖归厤[1], 0) : 0;
      return {
        璇曠偧绫诲瀷,
        ticketName,
        floor: Math.max(
          1,
          Math.floor(
            toNumber(
              payload.floor || payload.灞傛暟 || payload.layer || 鏂囨湰灞傛暟,
              getDefaultSoulTowerChallengeFloor(snapshot),
            ),
          ),
        ),
        entryLocation: 褰撳墠鍦扮偣,
      };
    }

    async function 鎵ц鍐呰仈妯″潡鎰忓浘璺敱(snapshot, moduleKind, request, inlineBuilder, inlineUnavailableReason, dispatchFailReason) {
      const inlineResult = await inlineBuilder(snapshot, request);
      if (!inlineResult?.ok || !inlineResult.inlineAction) {
        return 鏋勫缓妯″潡璺敱澶辫触缁撴灉(moduleKind, request, inlineResult?.reason || inlineUnavailableReason, {
          dispatchMode: 'inline',
          result: inlineResult || null,
        });
      }
      const dispatchResult = await dispatchUiAiRequest(
        inlineResult.inlineAction.playerInput,
        inlineResult.inlineAction.systemPrompt,
        {
          requestKind: inlineResult.inlineAction.requestKind,
          patchOps: inlineResult.inlineAction.patchOps
        }
      );
      if (!dispatchResult?.ok) {
        return 鏋勫缓妯″潡璺敱澶辫触缁撴灉(moduleKind, request, dispatchResult?.reason || dispatchFailReason, {
          dispatchMode: 'inline',
          inlineAction: inlineResult.inlineAction,
          result: inlineResult,
        });
      }
      return 鏋勫缓妯″潡璺敱鎴愬姛缁撴灉(moduleKind, request, {
        dispatchMode: 'inline',
        inlineAction: inlineResult.inlineAction,
        dispatchResult,
        result: inlineResult,
      });
    }

    async function routeModuleIntentPayload(input, options = {}) {
      const snapshot = liveSnapshot || lastRenderableSnapshot;
      const { payload, text, kind } = resolveModuleIntentPayload(input);
      if (!snapshot || !snapshot.rootData) return { handled: false, reason: 'snapshot_unavailable' };

      const dryRun = options.dryRun === true || payload.dryRun === true;
      let moduleKind = '';
      let request = null;

      const 璇曠偧璇锋眰 = 瑙ｆ瀽璇曠偧鍏ュ満鎰忓浘璇锋眰(snapshot, payload, kind, text);
      if (璇曠偧璇锋眰) {
        request = 璇曠偧璇锋眰;
        moduleKind = 'trial_entry';
      } else {
        moduleKind = 褰掍竴鍖栨ā鍧楁剰鍥剧被鍨?kind);
      }

      if (moduleKind === 'battle') {
        const trialContext = 璇诲彇璇曠偧鐘舵€佷笂涓嬫枃(snapshot);
        if (trialContext) {
          const 鎸囧畾灞傛暟 = Math.max(0, Math.floor(toNumber(payload.floor, 0)));
          request = buildTrialBattleDispatchDetail(snapshot, {
            source: toText(payload.source, 'module_intent_router_trial'),
            ...(鎸囧畾灞傛暟 > 0 ? { floor: 鎸囧畾灞傛暟 } : {}),
          }, trialContext);
        }
        if (!request) {
          const explicitParticipants = payload.鍙傛垬鑰?&& typeof payload.鍙傛垬鑰?=== 'object'
            ? cloneJsonValue(payload.鍙傛垬鑰? {})
            : null;
          if (explicitParticipants) {
            request = {
              action: 'battle',
              target: toText(payload.location || payload.targetLocation || payload.arena || payload.target, findBracketLocationToken(snapshot, text)),
              currentLoc: toText(payload.currentLoc || payload.location, toText(snapshot && snapshot.currentLoc, '')),
              鎴樻枟绫诲瀷: toText(payload.鎴樻枟绫诲瀷 || payload.combatType, /姝绘垬|鐢熸|鍑绘潃|琚嚮|浼忓嚮/.test(text) ? '绐佸彂閬亣' : '鎿傚彴鍒囩'),
              source: toText(payload.source, 'module_intent_router'),
              鍙傛垬鑰? explicitParticipants
            };
          } else {
            const npcTarget = toText(payload.npcTarget || payload.enemy || payload.targetNpc || payload.npc, '');
            request = {
              action: 'battle',
              target: toText(payload.location || payload.targetLocation || payload.arena || payload.target, findBracketLocationToken(snapshot, text)),
              currentLoc: toText(payload.currentLoc || payload.location, toText(snapshot && snapshot.currentLoc, '')),
              npcTarget,
              鎴樻枟绫诲瀷: toText(payload.鎴樻枟绫诲瀷 || payload.combatType, /姝绘垬|鐢熸|鍑绘潃|琚嚮|浼忓嚮/.test(text) ? '绐佸彂閬亣' : '鎿傚彴鍒囩'),
              source: toText(payload.source, 'module_intent_router')
            };
            if (!npcTarget) request = null;
          }
        }
      } else if (moduleKind === 'trade') {
        request = buildTradeRequestFromObject(snapshot, payload);
      } else if (moduleKind === 'profession') {
        request = buildDirectProfessionRequest(snapshot, payload);
      } else {
        request = null;
        moduleKind = '';
      }

      if (!request || !moduleKind) return { handled: false, reason: 'no_module_intent' };
      if (moduleKind === 'battle' && deepGet(snapshot, 'rootData.world.鎴樻枟.杩涜涓?, false)) {
        return 鏋勫缓妯″潡璺敱澶辫触缁撴灉(moduleKind, request, 'battle_already_active');
      }
      if (dryRun) return { handled: true, dryRun: true, kind: moduleKind, request };

      if (moduleKind === 'trial_entry') {
        let result = null;
        try {
          result = await 鐧昏璇曠偧鍏ュ満骞舵帹杩涘墽鎯?snapshot, request || {});
        } catch (error) {
          return 鏋勫缓妯″潡璺敱澶辫触缁撴灉(
            moduleKind,
            request,
            error && error.message ? error.message : 'trial_entry_failed',
          );
        }
        await refreshLiveSnapshot({ force: true });
        return 鏋勫缓妯″潡璺敱鎴愬姛缁撴灉(moduleKind, request, { result });
      }

      if (moduleKind === 'battle') {
        let result = await openMapBattleModule(snapshot, request);
        if (!result?.ok && result?.reason === 'combat_context_unresolved') {
          await refreshLiveSnapshot({ force: true });
          result = await openMapBattleModule(liveSnapshot || lastRenderableSnapshot || snapshot, request);
        }
        if (!result?.ok) {
          return 鏋勫缓妯″潡璺敱澶辫触缁撴灉(moduleKind, request, result?.reason || 'battle_open_failed', { result });
        }
        await refreshLiveSnapshot({ force: true });
        return 鏋勫缓妯″潡璺敱鎴愬姛缁撴灉(moduleKind, request, { result });
      }

      if (moduleKind === 'trade') {
        const privateTradeNpc = toText(request && request.瀵硅薄, '');
        if (privateTradeNpc && !resolveSnapshotCharKey(snapshot, privateTradeNpc)) {
          return 鏋勫缓妯″潡璺敱澶辫触缁撴灉(moduleKind, request, 'trade_target_unresolved');
        }
        return await 鎵ц鍐呰仈妯″潡鎰忓浘璺敱(
          snapshot,
          moduleKind,
          request,
          buildInlineTradeAction,
          'trade_inline_action_unavailable',
          'trade_continuation_dispatch_failed',
        );
      }

      return await 鎵ц鍐呰仈妯″潡鎰忓浘璺敱(
        snapshot,
        moduleKind,
        request,
        buildInlineProfessionAction,
        'profession_inline_action_unavailable',
        'profession_continuation_dispatch_failed',
      );
    }

    function installDirectModuleIntentGuard() {
      window.__MVU_ROUTE_MODULE_INTENT__ = (input, options = {}) => routeModuleIntentPayload(input, options);
      if (!window.__MVU_MODULE_INTENT_ROUTER_EVENT_BOUND__) {
        window.addEventListener('mvu-module-intent', event => {
          const detail = event && event.detail && typeof event.detail === 'object' ? event.detail : {};
          detail.result = routeModuleIntentPayload(detail.payload || detail.text || detail.plan || detail, detail.options || {});
        });
        window.addEventListener('mvu-module-plan-ready', event => {
          const detail = event && event.detail && typeof event.detail === 'object' ? event.detail : {};
          detail.result = routeModuleIntentPayload(detail.payload || detail.text || detail.plan || detail, detail.options || {});
        });
        window.__MVU_MODULE_INTENT_ROUTER_EVENT_BOUND__ = true;
      }
    }

    const 鍦板浘宸ュ潑濮旀墭鑱屼笟鍒楄〃 = Object.freeze(['閿婚€犲笀', '鍒堕€犲笀', '璁捐甯?, '淇悊甯?]);

    function 鑾峰彇鍓亴涓氱瓑绾?鑱屼笟鏁版嵁) {
      if (!鑱屼笟鏁版嵁 || typeof 鑱屼笟鏁版嵁 !== 'object') return 0;
      return Math.max(0, Math.floor(toNumber(鑱屼笟鏁版嵁.绛夌骇 ?? 鑱屼笟鏁版嵁.lv ?? 鑱屼笟鏁版嵁.level, 0)));
    }

    function 鎻忚堪鍓亴涓氬彲鎵挎帴鑼冨洿(鑱屼笟鍚? 绛夌骇, 鑱屼笟鏁版嵁 = {}) {
      const 绛夌骇鏁板€?= Math.max(0, Math.floor(toNumber(绛夌骇, 0)));
      const 闄愬埗 = 鑱屼笟鏁版嵁 && typeof 鑱屼笟鏁版嵁 === 'object' && 鑱屼笟鏁版嵁.闄愬埗 && typeof 鑱屼笟鏁版嵁.闄愬埗 === 'object'
        ? 鑱屼笟鏁版嵁.闄愬埗
        : {};
      const 棰濆 = [
        toNumber(闄愬埗.鏈€澶ц瀺鍚堟暟, 0) > 0 ? `鏈€澶?{toNumber(闄愬埗.鏈€澶ц瀺鍚堟暟, 0)}椤瑰鍚坄 : '',
        toNumber(闄愬埗.鎴愬姛鐜? 0) > 0 ? `鍩哄噯${toNumber(闄愬埗.鎴愬姛鐜? 0)}%` : '',
      ].filter(Boolean).join('锛?);
      const 鑼冨洿琛?= {
        閿婚€犲笀: ['涓嶅彲鎵挎帴', '鐧鹃敾/鍩虹閲戝睘', '鍗冮敾/涓€瀛楀墠缃?, '鐏甸敾/浜屽瓧鍓嶇疆', '榄傞敾/楂橀樁閲戝睘', '澶╅敾/椤剁骇閲戝睘'],
        鍒堕€犲笀: ['涓嶅彲鎵挎帴', '榛勭骇鏈虹敳/鍩虹閮ㄤ欢', '绱骇鏈虹敳/涓€瀛楁枟閾犻儴浠?, '浜屽瓧鏂楅摖/涓樁妯″潡', '涓夊瓧鏂楅摖/榛戠骇鏈虹敳閮ㄤ欢', '鍥涘瓧鏂楅摖/绾㈢骇鏈虹敳閮ㄤ欢'],
        璁捐甯? ['涓嶅彲鎵挎帴', '鍩虹鍥剧焊', '涓€瀛楁枟閾?绱骇鏈虹敳鍥剧焊', '浜屽瓧鏂楅摖/涓樁妯″潡鍥剧焊', '涓夊瓧鏂楅摖/榛戠骇鏂规', '鍥涘瓧鏂楅摖/绾㈢骇鏂规'],
        淇悊甯? ['涓嶅彲鎵挎帴', '鍩虹缁存姢', '涓€瀛楁枟閾?绱骇鏈虹敳缁存姢', '浜屽瓧鏂楅摖/涓樁淇', '涓夊瓧鏂楅摖/榛戠骇淇', '鍥涘瓧鏂楅摖/绾㈢骇淇'],
      };
      const 妗?= Math.max(0, Math.min(5, 绛夌骇鏁板€?);
      return `${鑱屼笟鍚峿 Lv.${绛夌骇鏁板€?|| 0}锛?{(鑼冨洿琛╗鑱屼笟鍚峕 || 鑼冨洿琛?鍒堕€犲笀)[妗}${棰濆 ? `锛?{棰濆}锛塦 : ''}`;
    }

    function 鑾峰彇瑙掕壊宸ュ潑濮旀墭鑳藉姏鎽樿(瑙掕壊鏁版嵁) {
      const 鑱屼笟闆嗗悎 = 瑙掕壊鏁版嵁 && typeof 瑙掕壊鏁版嵁 === 'object' && 瑙掕壊鏁版嵁.鑱屼笟 && typeof 瑙掕壊鏁版嵁.鑱屼笟 === 'object'
        ? 瑙掕壊鏁版嵁.鑱屼笟
        : {};
      return 鍦板浘宸ュ潑濮旀墭鑱屼笟鍒楄〃
        .map(鑱屼笟鍚?=> {
          const 鑱屼笟鏁版嵁 = 鑱屼笟闆嗗悎[鑱屼笟鍚峕;
          const 绛夌骇 = 鑾峰彇鍓亴涓氱瓑绾?鑱屼笟鏁版嵁);
          return 绛夌骇 > 0 ? 鎻忚堪鍓亴涓氬彲鎵挎帴鑼冨洿(鑱屼笟鍚? 绛夌骇, 鑱屼笟鏁版嵁) : '';
        })
        .filter(Boolean);
    }

    function 瑙ｆ瀽瀹樻柟宸ュ潑濮旀墭鑱屼笟(鍦扮偣鍚?= '') {
      const 鏂囨湰 = toText(鍦扮偣鍚? '');
      if (/閿婚€?.test(鏂囨湰)) return '閿婚€犲笀';
      if (/鍒堕€?.test(鏂囨湰)) return '鍒堕€犲笀';
      if (/璁捐/.test(鏂囨湰)) return '璁捐甯?;
      if (/淇悊|缁翠慨/.test(鏂囨湰)) return '淇悊甯?;
      return '';
    }

    function 鑾峰彇瀹樻柟宸ュ潑濮旀墭鑳藉姏鎽樿(鍦扮偣鍚?= '') {
      const 鑱屼笟鍚?= 瑙ｆ瀽瀹樻柟宸ュ潑濮旀墭鑱屼笟(鍦扮偣鍚?;
      if (!鑱屼笟鍚? return [];
      return [
        `${鑱屼笟鍚峿鍗忎細锛氬彲鍙楃悊1-3绾у父瑙勫鎵榒,
        '楂橀樁鍗曢渶鍓ф儏瀹℃壒/澶у笀妗ｆ湡',
        '鍏堣浠蜂笌纭鏉愭枡锛屼笉鐩存帴缁撶畻浜х墿',
      ];
    }

    function 鏋勫缓鍦板浘宸ュ潑濮旀墭璇锋眰(snapshot, detail = {}) {
      const executorType = toText(detail.executorType, '').trim();
      const currentLoc = toText(detail.currentLoc, 璇诲彇蹇収褰撳墠浣嶇疆(snapshot || {}));
      const targetLoc = toText(detail.target, currentLoc);
      const isOfficial = executorType === 'official';
      const npcTarget = toText(detail.npcTarget, '');
      let 鎵ц鑰?= '';
      let 鑳藉姏鎽樿 = [];
      let 绾︽潫鎽樿 = '';

      if (isOfficial) {
        鎵ц鑰?= toText(targetLoc, currentLoc);
        鑳藉姏鎽樿 = 鑾峰彇瀹樻柟宸ュ潑濮旀墭鑳藉姏鎽樿(鎵ц鑰?;
        if (!鑳藉姏鎽樿.length) {
          showUiToast('杩欓噷涓嶆槸鍙瘑鍒殑瀹樻柟宸ュ潑濮旀墭鐐广€?, 'error', 3200);
          return null;
        }
      } else {
        if (!npcTarget) {
          showUiToast('璇烽€夋嫨鍏峰鍓亴涓氱殑濮旀墭瀵硅薄銆?, 'error', 3200);
          return null;
        }
        const resolved = resolveSnapshotCharacter(snapshot, npcTarget);
        const targetChar = resolved.char;
        if (!targetChar) {
          showUiToast(`鎵句笉鍒板鎵樺璞★細${npcTarget}`, 'error', 3200);
          return null;
        }
        const targetCharLoc = toText(deepGet(targetChar, '鐘舵€?浣嶇疆', ''), '');
        if (targetCharLoc && currentLoc && !isLocationCompatible(currentLoc, targetCharLoc)) {
          showUiToast(`銆?{resolved.displayName}銆戜笉鍦ㄥ綋鍓嶅湴鐐癸紝鏃犳硶褰撻潰濮旀墭銆俙, 'error', 3600);
          return null;
        }
        鎵ц鑰?= resolved.displayName || npcTarget;
        鑳藉姏鎽樿 = 鑾峰彇瑙掕壊宸ュ潑濮旀墭鑳藉姏鎽樿(targetChar);
        if (!鑳藉姏鎽樿.length) {
          showUiToast(`銆?{鎵ц鑰厎銆戞病鏈夊彲璇嗗埆鐨勫壇鑱屼笟锛屾棤娉曞彂璧峰伐鍧婂鎵樸€俙, 'error', 3600);
          return null;
        }
        const relation = toNumber(
          deepGet(snapshot, ['activeChar', '鍏崇郴', resolved.key, '濂芥劅搴?], deepGet(snapshot, ['activeChar', '绀句氦', '鍏崇郴', resolved.key, '濂芥劅搴?], 0)),
          0,
        );
        绾︽潫鎽樿 = `绉佷汉濮旀墭闇€瀵规柟鎰挎剰鎺ュ崟锛涘ソ鎰熷害鍙傝€冿細${relation}銆俙;
      }

      const abilityText = 鑳藉姏鎽樿.join('锛?);
      const playerInput = isOfficial
        ? `鎴戞兂鍦ㄣ€?{鎵ц鑰厎銆戝姙鐞嗗伐鍧婂鎵橈紝鍏堣浠峰苟纭鑳藉仛浠€涔堛€俙
        : `鎴戞兂濮旀墭銆?{鎵ц鑰厎銆戝鐞嗗伐鍧婃椿锛屽厛闂竻妤氬鏂硅兘鎵挎帴浠€涔堛€俙;
      const systemPrompt = [
        '浠ヤ笅鍐呭灞炰簬鍦板浘椤靛彂璧风殑宸ュ潑濮旀墭璇锋眰銆?,
        '宸ュ潑濮旀墭涓嶆槸鐜╁鑷鎵撻€狅紝涔熶笉鏄珛鍗虫墦寮€鐢熶骇缁撶畻锛涜繖鏄竴娆′笘鐣屽唴璋堝崟/璇环/鎺ュ崟纭銆?,
        '璇锋牴鎹墽琛岃€呯殑鍓亴涓氱瓑绾т笌鍙壙鎺ヨ寖鍥存帹杩涘墽鎯咃紝涓嶈鐩存帴鐢熸垚浜х墿锛屼笉瑕侀粯璁ゆ墸鏉愭枡鎴栨墸閽便€?,
        '鑻ョ帺瀹舵病鏈夌粰鍑哄叿浣撲骇鐗┿€佹潗鏂欍€侀樁绾э紝璇疯瀵规柟鎸夎兘鍔涜寖鍥寸粰鍑哄彲鎺ヤ笟鍔°€佹姤浠峰€惧悜鍜屼笅涓€姝ラ渶瑕佺帺瀹剁‘璁ょ殑淇℃伅銆?,
        `褰撳墠鍦扮偣锛?{currentLoc || '鏈煡鍦扮偣'}`,
        `濮旀墭瀵硅薄锛?{鎵ц鑰厎`,
        `濮旀墭绫诲瀷锛?{isOfficial ? '瀹樻柟宸ュ潑濮旀墭' : '瑙掕壊宸ュ潑濮旀墭'}`,
        `鑳藉姏妫€娴嬶細${abilityText}`,
        绾︽潫鎽樿 ? `濮旀墭绾︽潫锛?{绾︽潫鎽樿}` : '',
      ].filter(Boolean).join('\n');
      return { playerInput, systemPrompt, requestKind: 'map_profession_commission' };
    }

    async function handleMapActionDispatch(event) {
      const detail = event && event.detail ? event.detail : {};
      const action = toText(detail.action, '');
      const services = normalizeMapDispatchServices(detail);
      if (!action && !services.length) return;
      if (!isSnapshotPlayerControlled(liveSnapshot)) {
        console.warn('[DragonUI] 鏃佽瑙嗚涓嶅彲鎿嶄綔銆?, detail);
        return;
      }

      const 褰掍竴鍖栧姩浣?= (() => {
        if (action === 'commission' || action === 'craft') return 'commission';
        if (action === 'ascension' || action === 'tower') return action;
        if (['talk', 'brief', 'intel'].includes(action)) return action;
        if (action === 'battle' || services.includes('battle')) return 'battle';
        if (
          ['trade', 'bid', 'shop', 'auction', 'black_market'].includes(action)
          || services.some(service => ['shop', 'auction', 'black_market'].includes(service))
        ) return 'trade';
        return '';
      })();
      if (!褰掍竴鍖栧姩浣? return;
      mapDispatchContext = { ...detail, action: 褰掍竴鍖栧姩浣? services };

      if (褰掍竴鍖栧姩浣?=== 'commission') {
        const commissionInit = 鏋勫缓鍦板浘宸ュ潑濮旀墭璇锋眰(liveSnapshot || lastRenderableSnapshot || {}, detail);
        if (commissionInit) {
          dispatchUiAiRequest(commissionInit.playerInput, commissionInit.systemPrompt, { requestKind: commissionInit.requestKind });
        }
        return;
      }

      if (褰掍竴鍖栧姩浣?=== 'ascension' || 褰掍竴鍖栧姩浣?=== 'tower') {
        const 鏄瓊鐏靛鍏ュ満 = 褰掍竴鍖栧姩浣?=== 'tower';
        const 鍏ュ満璇锋眰 = {
          璇曠偧绫诲瀷: 鏄瓊鐏靛鍏ュ満 ? '榄傜伒濉? : '鍗囩伒鍙?,
          ticketName: toText(
            detail.ticketName || (鏄瓊鐏靛鍏ュ満 ? detail.soulTowerTicket : detail.ascensionTicket),
            '',
          ),
          entryLocation: toText(detail.currentLoc, 璇诲彇蹇収褰撳墠浣嶇疆(liveSnapshot || lastRenderableSnapshot || {})),
        };
        if (鏄瓊鐏靛鍏ュ満) {
          鍏ュ満璇锋眰.floor = Math.max(
            1,
            Math.floor(
              toNumber(
                detail.floor,
                getDefaultSoulTowerChallengeFloor(liveSnapshot || lastRenderableSnapshot || {}),
              ),
            ),
          );
        }
        try {
          await 鐧昏璇曠偧鍏ュ満骞舵帹杩涘墽鎯?liveSnapshot, 鍏ュ満璇锋眰);
          showUiToast(鏄瓊鐏靛鍏ュ満 ? '榄傜伒濉斿叆鍦哄凡鐧昏銆? : '鍗囩伒鍙板叆鍦哄凡鐧昏銆?, 'info', 2400);
        } catch (error) {
          showUiToast(error && error.message ? error.message : (鏄瓊鐏靛鍏ュ満 ? '榄傜伒濉斿叆鍦哄け璐ャ€? : '鍗囩伒鍙板叆鍦哄け璐ャ€?), 'error', 4200);
        }
        return;
      }

      if (['talk', 'brief', 'intel'].includes(褰掍竴鍖栧姩浣?) {
        const interactInit = buildMapInteractDispatchRequest(liveSnapshot, detail);
        if (interactInit) {
          dispatchUiAiRequest(interactInit.playerInput, interactInit.systemPrompt, { requestKind: interactInit.requestKind });
          return;
        }
        const arenaName = toText(detail.currentLoc, toText(detail.target, toText(liveSnapshot && liveSnapshot.currentLoc, '鏈煡鍦扮偣')));
        const npcTargets = Array.isArray(detail.npcTargets) ? detail.npcTargets.map(item => toText(item, '')).filter(Boolean) : [];
        const targetLabel = npcTargets.length ? `鍦ㄥ満浜虹墿锛?{npcTargets.join('銆?)}锛塦 : '鍦ㄥ満浜哄憳';
        const playerInputMap = {
          talk: `鎴戞兂鍦ㄣ€?{arenaName}銆戜笌${targetLabel}浜よ皥銆俙,
          brief: `鎴戞兂鍦ㄣ€?{arenaName}銆戝悜${targetLabel}姹囨姤鎯呭喌骞惰绀哄畨鎺掋€俙,
          intel: `鎴戞兂鍦ㄣ€?{arenaName}銆戝悜${targetLabel}璇锋暀鎯呮姤銆俙
        };
        const actionLabel = 褰掍竴鍖栧姩浣?=== 'brief' ? '姹囨姤' : (褰掍竴鍖栧姩浣?=== 'intel' ? '璇锋暀' : '瀵硅瘽');
        const systemPrompt = `浠ヤ笅鍐呭灞炰簬鍓嶇宸茬粡鍙戣捣鐨勫湴鍥?{actionLabel}璇锋眰銆傚綋鍓嶆病鏈夐攣瀹氬敮涓€ NPC 鐩爣锛屼笉瑕佹姤閿欙紝涔熶笉瑕佽姹傜帺瀹堕噸鏂扮偣鍑伙紱璇风粨鍚堛€?{arenaName}銆戠殑鍦烘櫙鍔熻兘涓庡湪鍦轰汉鐗╋紝鑷劧閫夋嫨鏈€鍚堥€傜殑鍥炲簲瀵硅薄鎵挎帴鏈浜掑姩銆?{npcTargets.length ? ` 褰撳墠鍙€欓€変汉鐗╋細${npcTargets.join('銆?)}銆俙 : ' 褰撳墠鏈瘑鍒埌鏄庣‘浜虹墿鍚嶅崟锛岃鎸夊湴鐐逛笌浜嬩欢鍔熻兘鑷劧鎵挎帴銆?}`;
        dispatchUiAiRequest(playerInputMap[褰掍竴鍖栧姩浣淽 || `鎴戞兂鍦ㄣ€?{arenaName}銆戜笌鍦ㄥ満浜虹墿浜掑姩銆俙, systemPrompt, { requestKind: 'map_interaction' });
        return;
      }

      if (褰掍竴鍖栧姩浣?=== 'battle') {
        let battleOpenResult = null;
        try {
          battleOpenResult = await openMapBattleModule(liveSnapshot, detail);
        } catch (error) {
          console.warn('[DragonUI] 鎴樻枟妯″潡寮€鍚け璐ャ€?, error);
        }
        if (battleOpenResult && battleOpenResult.ok) return;
        showUiToast(`鎴樻枟妯″潡寮€鍚け璐ワ細${battleOpenResult && battleOpenResult.reason ? battleOpenResult.reason : 'combat_context_unresolved'}`, 'error', 4200);
        return;
      }

      if (褰掍竴鍖栧姩浣?=== 'trade') {
        鎵撳紑鍦板浘浜ゆ槗闈㈡澘();
      }
    }

    window.addEventListener('map-action-dispatch', handleMapActionDispatch);

    function 鎵撳紑鍦板浘浜ゆ槗闈㈡澘() {
      if (document.body && document.body.classList.contains('mvu-layout-unified') && typeof window.__MVU_OPEN_UNIFIED_PREVIEW__ === 'function') {
        window.__MVU_OPEN_UNIFIED_PREVIEW__('浜ゆ槗缃戠粶', { preserveMapDispatchContext: true, replace: true });
        return;
      }
      openModal('浜ゆ槗缃戠粶', { preserveMapDispatchContext: true });
    }

    async function handleMapAiRequest(event) {
      const detail = event && event.detail && typeof event.detail === 'object' ? event.detail : {};
      const playerInput = toText(detail.playerInput || detail.input || '', '').trim();
      const systemPrompt = toText(detail.systemPrompt || detail.prompt || '', '').trim();
      const meta = detail.meta && typeof detail.meta === 'object' ? detail.meta : {};
      if (!playerInput) return;
      detail.handled = true;
      detail.result = await dispatchUiAiRequest(playerInput, systemPrompt, {
        requestKind: toText(meta.requestKind, 'map_ai_request')
      });
    }

    window.addEventListener('map-ai-request', handleMapAiRequest);

    async function handleBattleMvuUpdateRequest(event) {
      const detail = event && event.detail && typeof event.detail === 'object' ? event.detail : {};
      const patchOps = Array.isArray(detail.patchOps) ? detail.patchOps : [];
      if (!patchOps.length) return;
      try {
        await applyJsonPatchOpsByEditor(patchOps);
        detail.delivery = { ok: true, channel: 'mvu_editor_patch', patchCount: patchOps.length };
        await refreshLiveSnapshot({ force: true });
      } catch (error) {
        detail.delivery = { ok: false, channel: 'mvu_editor_patch', error: String(error && error.message || error) };
        console.warn('[DragonUI] 鎴樻枟妯″潡鍙橀噺鍐欏叆澶辫触', error);
      }
    }

    function handleBattleUiCloseRequest(event) {
      const detail = event && event.detail && typeof event.detail === 'object' ? event.detail : {};
      detail.handled = true;
      if (isCombatActiveForBattleInline(liveSnapshot || lastRenderableSnapshot)) {
        battleInlineDismissed = true;
        scheduleBattleReturnEntrySync(liveSnapshot || lastRenderableSnapshot, true);
      }
      if (activeBattleUI && typeof activeBattleUI.destroy === 'function') activeBattleUI.destroy();
      activeBattleUI = null;
      battleInlineMountToken++;
      pendingBattleInlineMount = null;
      const shellBridge = getMobileShellBridge();
      if (shellBridge && typeof shellBridge.onPreviewClosed === 'function') {
        shellBridge.onPreviewClosed();
      } else if (currentUnifiedPreviewKey === BATTLE_INLINE_PREVIEW_KEY && typeof window.__MVU_CLOSE_UNIFIED_PREVIEW__ === 'function') {
        window.__MVU_CLOSE_UNIFIED_PREVIEW__({ force: true });
      }
    }

    window.addEventListener('battle-ui-mvu-update-request', handleBattleMvuUpdateRequest);
    window.addEventListener('battle-ui-close-request', handleBattleUiCloseRequest);

    var currentModalDisplayMode = 'auto';
    var lastRenderedModalPreviewKey = '';
    function computeUnifiedFloatBottomReserve() {
      const viewportHeight = Math.max(0, Number(window.innerHeight) || 0);
      const viewportWidth = Math.max(0, Number(window.innerWidth) || 0);
      if (!viewportHeight || !viewportWidth) return 128;
      let reserve = 128;
      const seen = new Set();
      const minGenericWidth = Math.min(Math.max(140, viewportWidth * 0.18), Math.max(160, viewportWidth - 32));

      const measureCandidate = (el, options = {}) => {
        if (!(el instanceof Element)) return;
        if (seen.has(el)) return;
        seen.add(el);
        if (!el.isConnected) return;
        if (el.id === 'detailModal' || el.closest('#detailModal')) return;
        const style = window.getComputedStyle(el);
        if (!style || style.display === 'none' || style.visibility === 'hidden' || style.opacity === '0') return;
        if (style.pointerEvents === 'none') return;
        const rect = el.getBoundingClientRect();
        if (!rect || rect.height <= 0 || rect.width <= 0) return;
        const visible = Math.max(0, Math.min(viewportHeight, rect.bottom) - Math.max(0, rect.top));
        if (visible <= 0) return;
        if (rect.bottom < viewportHeight - 260) return;
        if ((options.minHeight || 0) > 0 && rect.height < options.minHeight) return;
        if (!options.allowAnyWidth && rect.width < minGenericWidth) return;
        const nextReserve = Math.max(0, viewportHeight - Math.max(0, rect.top)) + 16;
        reserve = Math.max(reserve, nextReserve);
      };

      const primaryCandidates = [
        document.getElementById('mvu-unified-mount'),
        document.getElementById('acu-nav-bar'),
        document.querySelector('.acu-wrapper')
      ];
      for (const el of primaryCandidates) {
        measureCandidate(el, { allowAnyWidth: true, minHeight: 20 });
      }

      const scanRoot = document.body || document.documentElement;
      if (scanRoot) {
        const nodes = scanRoot.querySelectorAll('*');
        for (const el of nodes) {
          const style = window.getComputedStyle(el);
          if (!style) continue;
          const position = style.position;
          if (position !== 'fixed' && position !== 'sticky') continue;
          measureCandidate(el, { allowAnyWidth: false, minHeight: 24 });
        }
      }

      return Math.max(96, Math.min(reserve, Math.max(220, viewportHeight - 84)));
    }
    function applyUnifiedFloatMetrics(detailModalEl, unifiedMode) {
      if (!detailModalEl) return;
      if (!unifiedMode) {
        detailModalEl.style.removeProperty('--mvu-float-bottom-reserve');
        detailModalEl.style.removeProperty('--mvu-float-max-height');
        detailModalEl.style.removeProperty('--mvu-float-min-height');
        return;
      }
      const viewportHeight = Math.max(0, Number(window.innerHeight) || 0);
      const reserve = computeUnifiedFloatBottomReserve();
      const maxHeight = Math.max(260, viewportHeight - reserve - 54);
      const minHeight = Math.max(220, Math.min(360, viewportHeight - reserve - 122));
      detailModalEl.style.setProperty('--mvu-float-bottom-reserve', `${reserve}px`);
      detailModalEl.style.setProperty('--mvu-float-max-height', `${maxHeight}px`);
      detailModalEl.style.setProperty('--mvu-float-min-height', `${minHeight}px`);
    }

    function isUnifiedLayoutActive() {
      return true;
    }

    function shouldUseUnifiedFloatMode(options = {}) {
      const explicitMode = String(options.displayMode || '').trim();
      if (explicitMode === 'floating' || explicitMode === 'unified') return true;
      return isUnifiedLayoutActive();
    }

    function applyModalDisplayMode(refs = getModalRefs(), options = {}) {
      const currentDetailModal = refs && refs.detailModal ? refs.detailModal : null;
      const currentModalPanel = refs && refs.modalPanel ? refs.modalPanel : null;
      if (!currentDetailModal || !currentModalPanel) return;
      const unifiedMode = shouldUseUnifiedFloatMode(options);
      const shellMode = syncDetailModalHost(refs, { ...options, unifiedMode });
      currentDetailModal.classList.remove('drawer-left');
      currentModalPanel.classList.remove('drawer-left');
      currentDetailModal.classList.toggle('mvu-modal-display-unified', unifiedMode && !shellMode);
      currentDetailModal.classList.toggle('mvu-modal-display-split', false);
      currentModalPanel.classList.toggle('mvu-modal-display-unified', unifiedMode && !shellMode);
      currentModalPanel.classList.toggle('mvu-modal-display-split', false);
      currentDetailModal.classList.toggle('mvu-modal-display-shell', shellMode);
      currentModalPanel.classList.toggle('mvu-modal-display-shell', shellMode);
      currentDetailModal.dataset.modalDisplayMode = shellMode ? 'shell' : 'unified';
      currentModalPanel.dataset.modalDisplayMode = shellMode ? 'shell' : 'unified';
      applyUnifiedFloatMetrics(currentDetailModal, unifiedMode && !shellMode);
    }

    function openModal(previewKey, options = {}) {
      if (shouldBlockInlineEditRerender(options)) {
        pendingLiveRefresh = true;
        return;
      }
      const refs = getModalRefs();
      if (!options.preserveMapDispatchContext) {
        mapDispatchContext = null;
      }

      const targetKey = previewKey || '';
      if (targetKey === PRIVATE_ARCHIVE_PREVIEW_KEY && !canOpenPrivateArchive(liveSnapshot)) return;
      if (targetKey) void 璇锋眰棰勭儹棰勮渚濊禆(targetKey, 'modal_open');
      if (targetKey) {
        if (!modalStack.length || modalStack[modalStack.length - 1] !== targetKey) {
          modalStack.push(targetKey);
        }
      }
      currentModalPreviewKey = modalStack[modalStack.length - 1] || '';
      currentModalDisplayMode = 'floating';
      const shellMode = currentModalPreviewKey && isMobileShellModalActive({ ...options, unifiedMode: true });
      if (shellMode) {
        notifyShellPreviewChange(currentModalPreviewKey);
      }
      
      if (currentModalPreviewKey) {
        renderModalContent(currentModalPreviewKey, refs, { ...options, displayMode: currentModalDisplayMode });
      }

      if (shellMode) {
        hideDetailModalElement(refs);
        return;
      }
      if (!refs.detailModal) return;
      applyModalDisplayMode(refs, options);
      refs.detailModal.classList.add('show');
      refs.detailModal.setAttribute('aria-hidden', 'false');
    }
    window.__MVU_OPEN_SHELL_PREVIEW__ = (previewKey, options = {}) => {
      openModal(previewKey, { ...options, displayMode: 'floating' });
    };

    function toggleModal(previewKey, options = {}) {
      const targetKey = previewKey || '';
      if (!targetKey) return;
      const refs = getModalRefs();
      if (((refs.detailModal && refs.detailModal.classList.contains('show')) || isShellInlinePreviewActive()) && currentModalPreviewKey === targetKey) {
        closeModal();
        return;
      }
      openModal(targetKey, options);
    }

    function setArchiveRedesignState(refs = getModalRefs(), enabled = false) {
      const currentDetailModal = refs && refs.detailModal ? refs.detailModal : null;
      const currentModalPanel = refs && refs.modalPanel ? refs.modalPanel : null;
      const currentModalBody = refs && refs.modalBody ? refs.modalBody : null;
      if (currentDetailModal) currentDetailModal.classList.toggle('archive-redesign-mode', !!enabled);
      if (currentModalPanel) currentModalPanel.classList.toggle('archive-redesign-panel', !!enabled);
      if (currentModalBody) currentModalBody.classList.toggle('archive-redesign-body', !!enabled);
      syncArchiveRedesignPanelDecor(refs, enabled);
    }

    function syncArchiveRedesignPanelDecor(refs = getModalRefs(), enabled = false) {
      const currentModalPanel = refs && refs.modalPanel ? refs.modalPanel : null;
      if (!currentModalPanel) return;
      const existingDecor = currentModalPanel.querySelector('.archive-redesign-decor');
      if (!enabled) {
        if (existingDecor) existingDecor.remove();
        return;
      }
      if (existingDecor) return;
      currentModalPanel.insertAdjacentHTML('afterbegin', `
        <div class="archive-redesign-decor" aria-hidden="true">
          <div class="ring-group top-left-rings">
            <div class="astro-circle ring-cyan-1"></div>
            <div class="astro-circle ring-cyan-2"></div>
            <div class="astro-circle ring-cyan-3"></div>
          </div>
          <div class="ring-group bottom-right-rings">
            <div class="astro-circle ring-gold-1"></div>
            <div class="astro-circle ring-gold-2"></div>
            <div class="astro-circle ring-gold-3"></div>
          </div>
        </div>
      `);
    }

    function isUnifiedModalDisplayRequested() {
      return true;
    }

    function syncModalTitleLongPress(previewKey, unifiedMode) {
      if (!modalTitle) return;
      modalTitle.classList.remove('nsfw-trigger-title');
      modalTitle.removeAttribute('data-longpress');
      modalTitle.removeAttribute('data-longpress-delay');
      if (unifiedMode && previewKey === '鐢熷懡鍥捐氨璇︾粏椤? && canOpenPrivateArchive(liveSnapshot)) {
        modalTitle.classList.add('nsfw-trigger-title');
        modalTitle.setAttribute('data-longpress', PRIVATE_ARCHIVE_PREVIEW_KEY);
        modalTitle.setAttribute('data-longpress-delay', '600');
      }
    }

    function wrapArchiveRedesignBody(html, options = {}) {
      const previewKey = toText(options.previewKey, '');
      if (options.shellMode) {
        return `<div class="mvu-shell-detail-root mvu-unified-modal-root" data-ai-maintenance-root="1" data-unified-preview="${escapeHtmlAttr(previewKey)}">
          <div class="mvu-shell-detail-scroll">
            ${html || ''}
          </div>
        </div>`;
      }
      if (options.unifiedMode) {
        return `<div class="archive-redesign-root mvu-unified-modal-root" data-ai-maintenance-root="1" data-unified-preview="${escapeHtmlAttr(previewKey)}">
          <div class="mvu-unified-sheet">
            <div class="mvu-unified-sheet-scroll">
              ${html || ''}
            </div>
          </div>
        </div>`;
      }
      return `<div class="archive-redesign-root" data-ai-maintenance-root="1" data-unified-preview="${escapeHtmlAttr(previewKey)}">
        ${html || ''}
      </div>`;
    }

    function getShellInlineHost() {
      const bridge = getMobileShellBridge();
      if (!bridge || typeof bridge.getModalHost !== 'function') return null;
      const host = bridge.getModalHost();
      return host && host.isConnected ? host : null;
    }

    function isShellInlinePreviewActive() {
      const bridge = getMobileShellBridge();
      if (!bridge || typeof bridge.isOpen !== 'function' || !bridge.isOpen()) return false;
      if (typeof bridge.isDetailActive === 'function' && !bridge.isDetailActive()) return false;
      return !!getShellInlineHost();
    }

    function clearShellInlinePreview() {
      const host = getShellInlineHost();
      lastRenderedShellPreviewKey = '';
      if (!host) return;
      host.innerHTML = '';
      delete host.dataset.shellPreview;
    }

    function buildShellPrivateArchiveView(snapshot) {
      const nsfw = getActiveNsfwData(snapshot);
      const activeName = toText(snapshot && snapshot.activeName, '褰撳墠瑙掕壊');
      const bodyPartEntries = safeEntries(deepGet(nsfw, '韬綋閮ㄤ綅', {}));
      const measurements = deepGet(nsfw, '韬潗鏁版嵁', {});
      const intimateWear = deepGet(nsfw, '璐磋韩琛ｇ墿', {});
      const experienceEntries = safeEntries(deepGet(nsfw, '缁忓巻娆℃暟', {}))
        .filter(([, value]) => toNumber(value, 0) > 0)
        .slice(0, 8);
      const fetishes = Array.isArray(nsfw.鐧栧ソ) ? nsfw.鐧栧ソ.map(item => toText(item, '')).filter(Boolean).slice(0, 8) : [];
      const fantasies = Array.isArray(nsfw.骞绘兂) ? nsfw.骞绘兂.map(item => toText(item, '')).filter(Boolean).slice(0, 8) : [];
      const clampMeter = value => Math.max(0, Math.min(100, toNumber(value, 0)));
      const renderMeter = (label, value, tone = '') => {
        const percent = clampMeter(value);
        return `
          <div class="mvu-shell-private-meter${tone ? ` is-${htmlEscape(tone)}` : ''}">
            <div class="mvu-shell-private-meter-top">
              <span>${htmlEscape(label)}</span>
              <b>${htmlEscape(String(percent))}</b>
            </div>
            <div class="mvu-shell-private-track"><i style="width:${percent}%;"></i></div>
          </div>
        `;
      };
      const renderRow = (label, value) => `
        <div class="mvu-shell-private-row">
          <span>${htmlEscape(label)}</span>
          <b>${htmlEscape(toText(value, '--'))}</b>
        </div>
      `;
      const renderTags = items => items.length
        ? items.map(item => `<span class="mvu-shell-private-tag">${htmlEscape(shortenText(item, 14))}</span>`).join('')
        : '<span class="mvu-shell-private-empty">鏆傛棤璁板綍</span>';
      const bodyPartCards = bodyPartEntries.length
        ? bodyPartEntries.slice(0, 6).map(([partName, partData]) => `
          <div class="mvu-shell-private-part">
            <div class="mvu-shell-private-part-head">
              <b>${htmlEscape(partName)}</b>
              <span>寮€鍙?${htmlEscape(String(toNumber(deepGet(partData, '寮€鍙戝害', 0), 0)))}</span>
            </div>
            <div class="mvu-shell-private-part-text">${htmlEscape(shortenText(toText(deepGet(partData, '鐘舵€佹弿杩?, deepGet(partData, '澶栬鐗瑰緛', '姝ｅ父')), '姝ｅ父'), 34))}</div>
          </div>
        `).join('')
        : '<div class="mvu-shell-private-empty">鏆傛棤璁板綍</div>';
      const experienceTags = experienceEntries.length
        ? experienceEntries.map(([label, value]) => `<span class="mvu-shell-private-tag">${htmlEscape(label)} ${htmlEscape(String(toNumber(value, 0)))}</span>`).join('')
        : '<span class="mvu-shell-private-empty">鏆傛棤璁板綍</span>';

      return {
        title: '绉佸瘑妗ｆ',
        body: `
          <div class="mvu-shell-private-root" data-private-archive-light="1">
            <section class="mvu-shell-private-card mvu-shell-private-card--hero">
              <div class="mvu-shell-private-hero">
                <span>${htmlEscape(activeName)}</span>
                <strong>${htmlEscape(toText(deepGet(measurements, '韬潗鎻忚堪', ''), toText(deepGet(measurements, '缃╂澂', '绉佸瘑姒傝'), '绉佸瘑姒傝')))}</strong>
              </div>
              <div class="mvu-shell-private-meters">
                ${renderMeter('鍙戞儏', nsfw.鍙戞儏搴? 'live')}
                ${renderMeter('寮€鍙?, nsfw.寮€鍙戝害, 'warm')}
                ${renderMeter('鏁忔劅', nsfw.鏁忔劅搴? '')}
              </div>
            </section>

            <section class="mvu-shell-private-card">
              <div class="mvu-shell-private-section-title">韬綋</div>
              <div class="mvu-shell-private-row-grid">
                ${renderRow('缃╂澂', deepGet(measurements, '缃╂澂', '鏈煡'))}
                ${renderRow('涓夊洿', `${toText(deepGet(measurements, '鑳稿洿'), '0')} / ${toText(deepGet(measurements, '鑵板洿'), '0')} / ${toText(deepGet(measurements, '鑷€鍥?), '0')}`)}
                ${renderRow('鍐呰。', deepGet(intimateWear, '鍐呰。', '鏈煡'))}
                ${renderRow('鍐呰￥', deepGet(intimateWear, '鍐呰￥', '鏈煡'))}
              </div>
            </section>

            <section class="mvu-shell-private-card">
              <div class="mvu-shell-private-section-title">鑺傚緥</div>
              <div class="mvu-shell-private-row-grid">
                ${renderRow('闃舵', nsfw._鐢熺悊闃舵 || '瀹夊叏鏈?)}
                ${renderRow('鍒濇疆', nsfw._宸叉潵鍒濇疆 ? '宸插垵娼? : '鏈垵娼?)}
                ${renderRow('鍙楀瓡鑺傜偣', String(toNumber(nsfw.鍙楀瓡tick, -1)))}
                ${renderRow('鎬€瀛曞ぉ鏁?, String(toNumber(nsfw._鎬€瀛曞ぉ鏁? 0)))}
              </div>
            </section>

            <section class="mvu-shell-private-card">
              <div class="mvu-shell-private-section-title">韬綋閮ㄤ綅</div>
              <div class="mvu-shell-private-part-grid">
                ${bodyPartCards}
              </div>
            </section>

            <section class="mvu-shell-private-card">
              <div class="mvu-shell-private-section-title">鍋忓ソ</div>
              <div class="mvu-shell-private-tag-row">${renderTags(fetishes)}</div>
              <div class="mvu-shell-private-tag-row">${renderTags(fantasies)}</div>
            </section>

            <section class="mvu-shell-private-card">
              <div class="mvu-shell-private-section-title">灞ュ巻</div>
              <div class="mvu-shell-private-tag-row">${experienceTags}</div>
            </section>
          </div>
        `
      };
    }

    function stripShellHeavyEntryNodes(host) {
      if (!host || typeof host.querySelectorAll !== 'function') return;
      host.querySelectorAll('[data-preview]').forEach(node => {
        const previewKey = toText(node.getAttribute('data-preview'), '');
        if (!previewKey.startsWith(SKILL_DESIGNER_PREVIEW_PREFIX)) return;
        const removable = node.closest('button, a, .role-switch-tile, .archive-tile, .dossier-list-row, .tag-chip, .relation-action-btn');
        if (!removable) return;
        if (removable.querySelector && removable.querySelector('[data-skill-designer-form], .skill-designer-layout')) return;
        removable.remove();
      });
    }

    function hideDetailModalElement(refs = getModalRefs()) {
      const currentDetailModal = refs && refs.detailModal ? refs.detailModal : null;
      const currentModalPanel = refs && refs.modalPanel ? refs.modalPanel : null;
      const currentModalBody = refs && refs.modalBody ? refs.modalBody : null;
      if (currentDetailModal) {
        currentDetailModal.classList.remove(
          'show',
          'drawer-left',
          'relation-preview-mode',
          'archive-redesign-mode',
          'mvu-modal-display-unified',
          'mvu-modal-display-split',
          'mvu-modal-display-shell'
        );
        currentDetailModal.setAttribute('aria-hidden', 'true');
        delete currentDetailModal.dataset.modalDisplayMode;
      }
      if (currentModalPanel) {
        currentModalPanel.classList.remove(
          'drawer-left',
          'vault-mode',
          'relation-preview-mode',
          'archive-redesign-panel',
          'mvu-modal-display-unified',
          'mvu-modal-display-split',
          'mvu-modal-display-shell'
        );
        delete currentModalPanel.dataset.modalDisplayMode;
      }
      if (currentModalBody) {
        currentModalBody.classList.remove('vault-body', 'archive-redesign-body', 'mvu-unified-sheet-body');
      }
      if (currentDetailModal) {
        syncDetailModalHost(refs, { unifiedMode: false });
      }
    }

    function getUnifiedInlineHost(options = {}) {
      const explicitHost = options && options.host instanceof Element ? options.host : null;
      if (explicitHost && explicitHost.isConnected) return explicitHost;
      try {
        if (typeof window.__MVU_GET_UNIFIED_DETAIL_HOST__ === 'function') {
          const bridgeHost = window.__MVU_GET_UNIFIED_DETAIL_HOST__();
          if (bridgeHost instanceof Element && bridgeHost.isConnected) return bridgeHost;
        }
      } catch (err) {}
      const host = document.querySelector('#mvu-unified-mount [data-unified-detail-host]');
      return host instanceof Element && host.isConnected ? host : null;
    }

    function getDomUnifiedPreviewKey() {
      const host = getUnifiedInlineHost();
      return host ? toText(host.dataset.unifiedPreview, '').trim() : '';
    }

    function getEffectiveUnifiedPreviewKey() {
      const domPreviewKey = getDomUnifiedPreviewKey();
      if (domPreviewKey && domPreviewKey !== currentUnifiedPreviewKey) {
        currentUnifiedPreviewKey = domPreviewKey;
      } else if (!domPreviewKey && currentUnifiedPreviewKey) {
        currentUnifiedPreviewKey = '';
      }
      return currentUnifiedPreviewKey;
    }

    function isUnifiedInlinePreviewActive() {
      const host = getUnifiedInlineHost();
      return !!(host && toText(host.dataset.unifiedPreview, '') && document.body && document.body.classList.contains('mvu-layout-unified'));
    }

    function syncUnifiedTitleLongPress(previewKey) {
      const title = document.querySelector('#mvu-unified-mount .mvu-unified-detail-title');
      if (!title) return;
      title.classList.remove('nsfw-trigger-title');
      title.removeAttribute('data-longpress');
      title.removeAttribute('data-longpress-delay');
      if (String(previewKey || '').includes('鐢熷懡鍥捐氨') && canOpenPrivateArchive(liveSnapshot)) {
        title.classList.add('nsfw-trigger-title');
        title.setAttribute('data-longpress', PRIVATE_ARCHIVE_PREVIEW_KEY);
        title.setAttribute('data-longpress-delay', '600');
      }
    }

    function clearUnifiedInlinePreview() {
      const host = getUnifiedInlineHost();
      const effectivePreviewKey = getEffectiveUnifiedPreviewKey();
      if (effectivePreviewKey === BATTLE_INLINE_PREVIEW_KEY && activeBattleUI && typeof activeBattleUI.destroy === 'function') {
        if (isCombatActiveForBattleInline(liveSnapshot || lastRenderableSnapshot)) {
          battleInlineDismissed = true;
          scheduleBattleReturnEntrySync(liveSnapshot || lastRenderableSnapshot, true);
        }
        activeBattleUI.destroy();
        activeBattleUI = null;
      }
      if (effectivePreviewKey && activeSubUI && typeof activeSubUI.destroy === 'function') {
        activeSubUI.destroy();
        activeSubUI = null;
      }
      currentUnifiedPreviewKey = '';
      lastRenderedUnifiedPreviewKey = '';
      syncUnifiedTitleLongPress('');
      鍚屾AI缁存姢鏍囬鍏ュ彛('');
      if (!host) return;
      host.innerHTML = '';
      delete host.dataset.unifiedPreview;
    }

    function wrapUnifiedInlineBody(html, options = {}) {
      const previewKey = toText(options.previewKey, '');
      return `<div class="mvu-unified-detail-root archive-redesign-root" data-ai-maintenance-root="1" data-unified-preview="${escapeHtmlAttr(previewKey)}">
        <div class="mvu-unified-detail-scroll">
          ${html || ''}
        </div>
      </div>`;
    }

    function isLiveRequiredPreviewKey(previewKey) {
      const key = toText(previewKey, '');
      const liveRequiredKeys = new Set([
        '鐢熷懡鍥捐氨璇︾粏椤?,
        '绉佸瘑妗ｆ璇︾粏椤?,
        '绀句細妗ｆ璇︾粏椤?,
        '鎵€灞炲娍鍔涜缁嗛〉',
        '浜虹墿鍏崇郴璇︾粏椤?,
        '鎯呮姤搴撹缁嗛〉',
        '姝﹁宸ュ潑璇︾粏椤?,
        '姝﹂瓊铻嶅悎鎶€璇︾粏椤?,
        '鍌ㄧ墿浠撳簱璇︾粏椤?,
        '绗竴姝﹂瓊璇︾粏椤?,
        '绗簩姝﹂瓊璇︾粏椤?,
        '琛€鑴夊皝鍗拌缁嗛〉',
        '缂栧勾鍙叉。妗?,
        '鏈湴鎹偣璇︽儏',
        '褰撳墠鑺傜偣璇︽儏',
        '鎿嶄綔鎬荤嚎',
        '璇曠偧涓庢儏鎶?,
        '杩戞湡瑙侀椈',
        '涓栫晫鐘舵€佹€昏',
        '鍔垮姏鐭╅樀鎬昏',
        '鎴戠殑闃佃惀璇︽儏',
        '绯荤粺鎾姤涓庢棩蹇?,
        '鎬墿鍥鹃壌',
        '妫灄浠囨仺鍊?
      ]);
      return liveRequiredKeys.has(key) || String(key || '').startsWith('鍦板浘鑺傜偣锛?);
    }

    function bindUnifiedDetailDelegation(host) {
      if (!host || host.__mvuUnifiedDetailDelegationBound) return;
      host.addEventListener('click', event => {
        handleDetailSurfaceClick(event, host, { surface: 'unified' });
      });
      host.__mvuUnifiedDetailDelegationBound = true;
    }

    function renderUnifiedInlinePreview(previewKey, options = {}) {
      const host = getUnifiedInlineHost(options);
      const targetKey = toText(previewKey, '').trim();
      if (!host || !targetKey) return false;
      if (targetKey === PRIVATE_ARCHIVE_PREVIEW_KEY && !canOpenPrivateArchive(liveSnapshot)) return false;
      void 璇锋眰棰勭儹棰勮渚濊禆(targetKey, 'unified_preview');
      if (shouldBlockInlineEditRerender(options)) {
        pendingLiveRefresh = true;
        return true;
      }
      if (activeSubUI && typeof activeSubUI.destroy === 'function') {
        activeSubUI.destroy();
        activeSubUI = null;
      }

      clearShellInlinePreview();
      hideDetailModalElement(getModalRefs());
      syncModalTitleLongPress('', false);

      const shouldResetScroll = lastRenderedUnifiedPreviewKey !== targetKey || toText(host.dataset.unifiedPreview, '') !== targetKey;
      currentUnifiedPreviewKey = targetKey;
      lastRenderedUnifiedPreviewKey = targetKey;
      host.dataset.unifiedPreview = targetKey;
      bindUnifiedDetailDelegation(host);
      璁剧疆缁熶竴瑙掕壊鍒囨崲鍏ュ彛(liveSnapshot || lastRenderableSnapshot || null);

      const setHostMarkup = (html, onMount = null) => {
        host.innerHTML = wrapUnifiedInlineBody(html, { previewKey: targetKey });
        if (shouldResetScroll) host.scrollTop = 0;
        syncUnifiedTitleLongPress(targetKey);
        鍚屾AI缁存姢鏍囬鍏ュ彛(targetKey);
        if (typeof onMount === 'function') {
          activeSubUI = onMount(host);
        }
        scheduleUnifiedMapCanvasClamp(host);
      };

      if (targetKey === BATTLE_INLINE_PREVIEW_KEY) {
        setHostMarkup(buildBattleInlineHostMarkup());
        return true;
      }

      const liveArchive = buildLiveArchiveModal(targetKey);
      const skeletonArchive = !liveArchive && !liveSnapshot ? buildArchiveSkeletonModal(targetKey) : null;
      if (liveArchive) {
        setHostMarkup(liveArchive.body, liveArchive.onMount);
        return true;
      }
      if (!liveSnapshot && isLiveRequiredPreviewKey(targetKey)) {
        setHostMarkup(skeletonArchive ? skeletonArchive.body : '');
        return true;
      }

      const archiveBuilder = archiveModalBuilders[targetKey];
      if (archiveBuilder) {
        const view = archiveBuilder();
        setHostMarkup(view.body);
        return true;
      }

      const config = previewMap[targetKey] || buildDynamicPreview(targetKey || '璇︾粏淇℃伅');
      setHostMarkup(renderGenericModalBody(config));
      return true;
    }
    window.__MVU_RENDER_UNIFIED_PREVIEW__ = renderUnifiedInlinePreview;
    window.__MVU_CLEAR_UNIFIED_PREVIEW__ = clearUnifiedInlinePreview;

    function renderShellInlinePreview(previewKey, options = {}) {
      const host = getShellInlineHost();
      if (!host) return false;
      const scrollTarget = host.closest('.mvu-mobile-shell-scroll') || host;
      const shouldResetScroll = lastRenderedShellPreviewKey !== previewKey || toText(host.dataset.shellPreview, '') !== toText(previewKey, '');
      lastRenderedShellPreviewKey = previewKey || '';
      host.dataset.shellPreview = previewKey || '';
      if (shouldBlockInlineEditRerender(options)) {
        pendingLiveRefresh = true;
        return true;
      }
      if (activeSubUI && typeof activeSubUI.destroy === 'function') {
        activeSubUI.destroy();
        activeSubUI = null;
      }

      const setHostMarkup = html => {
        host.innerHTML = wrapArchiveRedesignBody(html, { shellMode: true, previewKey });
        stripShellHeavyEntryNodes(host);
        if (shouldResetScroll) scrollTarget.scrollTop = 0;
        鍚屾AI缁存姢鏍囬鍏ュ彛(previewKey);
      };

      if (previewKey === BATTLE_INLINE_PREVIEW_KEY) {
        setHostMarkup(buildBattleInlineHostMarkup());
        return true;
      }

      if (previewKey === PRIVATE_ARCHIVE_PREVIEW_KEY) {
        const snapshot = liveSnapshot || lastRenderableSnapshot;
        if (!canOpenPrivateArchive(snapshot)) return false;
        const shellPrivateArchive = buildShellPrivateArchiveView(snapshot);
        setHostMarkup(shellPrivateArchive.body);
        return true;
      }

      if (previewKey === '鐢熷懡鍥捐氨璇︾粏椤? || previewKey === '鐢熷懡鍥捐氨璇︽儏椤?) {
        const snapshot = liveSnapshot || lastRenderableSnapshot;
        const shellLifeView = buildShellLifeView(snapshot || {});
        setHostMarkup(shellLifeView.body);
        return true;
      }

      if (
        previewKey === '姝﹁宸ュ潑璇︾粏椤?
        || previewKey === '姝﹁璇︽儏锛氭枟閾?
        || previewKey === '姝﹁璇︽儏锛氭満鐢?
        || previewKey === '姝﹁璇︽儏锛氫富姝﹀櫒'
        || String(previewKey || '').startsWith('鏂楅摖閮ㄤ欢锛?)
      ) {
        const snapshot = liveSnapshot || lastRenderableSnapshot;
        const shellArmoryView = buildShellArmoryView(snapshot || {});
        setHostMarkup(shellArmoryView.body);
        return true;
      }

      if (previewKey === '绗竴姝﹂瓊璇︾粏椤? || previewKey === '绗簩姝﹂瓊璇︾粏椤?) {
        const snapshot = liveSnapshot || lastRenderableSnapshot;
        const shellSpiritView = buildShellSpiritView(snapshot || {}, previewKey);
        setHostMarkup(shellSpiritView.body);
        return true;
      }

      if (previewKey === '姝﹂瓊铻嶅悎鎶€璇︾粏椤?) {
        const snapshot = liveSnapshot || lastRenderableSnapshot;
        const shellFusionView = buildShellFusionView(snapshot || {});
        setHostMarkup(shellFusionView.body);
        return true;
      }

      if (previewKey === '琛€鑴夊皝鍗拌缁嗛〉') {
        const snapshot = liveSnapshot || lastRenderableSnapshot;
        const shellBloodlineView = buildShellBloodlineView(snapshot || {});
        setHostMarkup(shellBloodlineView.body);
        return true;
      }

      if (previewKey === '鍌ㄧ墿浠撳簱璇︾粏椤?) {
        const snapshot = liveSnapshot || lastRenderableSnapshot;
        const shellVaultView = buildShellVaultView(snapshot || {});
        setHostMarkup(shellVaultView.body);
        return true;
      }

      if (previewKey === '绀句細妗ｆ璇︾粏椤?) {
        const snapshot = liveSnapshot || lastRenderableSnapshot;
        const shellSocialView = buildShellSocialView(snapshot || {});
        setHostMarkup(shellSocialView.body);
        return true;
      }

      if (previewKey === '褰撳墠鑺傜偣璇︽儏') {
        const snapshot = liveSnapshot || lastRenderableSnapshot;
        const shellAreaView = buildShellCurrentAreaView(snapshot || {});
        setHostMarkup(shellAreaView.body);
        return true;
      }

      if (previewKey === '缂栧勾鍙叉。妗?) {
        const snapshot = liveSnapshot || lastRenderableSnapshot;
        const shellTimelineView = buildShellTimelineView(snapshot || {});
        setHostMarkup(shellTimelineView.body);
        return true;
      }

      if (previewKey === '鍔垮姏鐭╅樀鎬昏') {
        const snapshot = liveSnapshot || lastRenderableSnapshot;
        const shellOrgView = buildShellOrgMatrixView(snapshot || {});
        setHostMarkup(shellOrgView.body);
        return true;
      }

      if (previewKey === '涓栫晫鐘舵€佹€昏') {
        const snapshot = liveSnapshot || lastRenderableSnapshot;
        const shellWorldView = buildShellWorldStateView(snapshot || {});
        setHostMarkup(shellWorldView.body);
        return true;
      }

      if (previewKey === '鎷嶅崠涓庤鎶?) {
        const snapshot = liveSnapshot || lastRenderableSnapshot;
        const shellAlertView = buildShellAlertView(snapshot || {});
        setHostMarkup(shellAlertView.body);
        return true;
      }

      if (previewKey === '鎴戠殑闃佃惀璇︽儏') {
        const snapshot = liveSnapshot || lastRenderableSnapshot;
        const shellFactionView = buildShellFactionView(snapshot || {});
        setHostMarkup(shellFactionView.body);
        return true;
      }

      if (previewKey === '鏈湴鎹偣璇︽儏') {
        const snapshot = liveSnapshot || lastRenderableSnapshot;
        const shellNodeView = buildShellNodeView(snapshot || {});
        setHostMarkup(shellNodeView.body);
        return true;
      }

      if (previewKey === '绯荤粺鎾姤涓庢棩蹇?) {
        const snapshot = liveSnapshot || lastRenderableSnapshot;
        const shellTerminalView = buildShellTerminalView(snapshot || {});
        setHostMarkup(shellTerminalView.body);
        return true;
      }

      if (previewKey === '璇曠偧涓庢儏鎶?) {
        const snapshot = liveSnapshot || lastRenderableSnapshot;
        const shellIntelView = buildShellIntelView(snapshot || {});
        setHostMarkup(shellIntelView.body);
        return true;
      }

      if (previewKey === '杩戞湡瑙侀椈') {
        const snapshot = liveSnapshot || lastRenderableSnapshot;
        const shellNewsView = buildShellNewsView(snapshot || {});
        setHostMarkup(shellNewsView.body);
        return true;
      }

      if (previewKey === '鎬墿鍥鹃壌') {
        const snapshot = liveSnapshot || lastRenderableSnapshot;
        const shellBestiaryView = buildShellBestiaryView(snapshot || {});
        setHostMarkup(shellBestiaryView.body);
        return true;
      }

      if (previewKey === '浠诲姟鐣岄潰') {
        const snapshot = liveSnapshot || lastRenderableSnapshot;
        const shellQuestView = buildShellQuestOverviewView(snapshot || {});
        setHostMarkup(shellQuestView.body);
        return true;
      }

      const liveArchive = buildLiveArchiveModal(previewKey);
      const skeletonArchive = !liveArchive && !liveSnapshot ? buildArchiveSkeletonModal(previewKey) : null;
      if (liveArchive) {
        setHostMarkup(liveArchive.body);
        if (typeof liveArchive.onMount === 'function') {
          activeSubUI = liveArchive.onMount(host);
        }
        return true;
      }
      if (skeletonArchive) {
        setHostMarkup(skeletonArchive.body);
        return true;
      }

      const archiveBuilder = archiveModalBuilders[previewKey];
      if (archiveBuilder) {
        const view = archiveBuilder();
        setHostMarkup(view.body);
        return true;
      }

      const config = previewMap[previewKey] || buildDynamicPreview(previewKey || '璇︾粏寮圭獥');
      setHostMarkup(renderGenericModalBody(config));
      return true;
    }

    function renderModalContent(previewKey, refs = getModalRefs(), options = {}) {
      const {
        detailModal,
        modalPanel,
        modalTitle,
        modalSubtitle,
        modalLevel,
        modalPath,
        modalSummary,
        modalBody
      } = refs;
      if (!detailModal || !modalPanel || !modalTitle || !modalBody) {
        return;
      }
      const shouldResetModalScroll = lastRenderedModalPreviewKey !== previewKey || !detailModal.classList.contains('show');
      const isRelationPreview = previewKey === '浜虹墿鍏崇郴璇︾粏椤?;
      const unifiedMode = isUnifiedModalDisplayRequested(options);
      const shellMode = isMobileShellModalActive({ ...options, unifiedMode });
      if (shellMode) {
        hideDetailModalElement(refs);
        lastRenderedModalPreviewKey = '';
        renderShellInlinePreview(previewKey, options);
        return;
      }
      clearShellInlinePreview();
      lastRenderedModalPreviewKey = previewKey || '';
      detailModal.classList.toggle('relation-preview-mode', isRelationPreview);
      modalPanel.classList.toggle('relation-preview-mode', isRelationPreview);
      applyModalDisplayMode(refs, { ...options, displayMode: unifiedMode ? 'floating' : 'split' });
      syncModalTitleLongPress(previewKey, unifiedMode);
      if (shouldBlockInlineEditRerender(options)) {
        pendingLiveRefresh = true;
        return;
      }
      if (activeSubUI && typeof activeSubUI.destroy === 'function') {
        activeSubUI.destroy();
        activeSubUI = null;
      }
      const liveArchive = buildLiveArchiveModal(previewKey);
      const skeletonArchive = !liveArchive && !liveSnapshot ? buildArchiveSkeletonModal(previewKey) : null;
      if (liveArchive) {
        modalPanel.classList.add('archive-mode');
        modalPanel.classList.toggle('vault-mode', previewKey === '鍌ㄧ墿浠撳簱璇︾粏椤?);
        modalBody.className = previewKey === '鍌ㄧ墿浠撳簱璇︾粏椤? ? 'modal-body archive-body vault-body' : 'modal-body archive-body';
        if (unifiedMode) modalBody.classList.add('mvu-unified-sheet-body');
        if (modalLevel) modalLevel.textContent = '';
        if (modalPath) modalPath.textContent = '';
        modalTitle.textContent = liveArchive.title;
        if (unifiedMode && typeof previewKey === 'string' && previewKey.includes('鐢熷懡鍥捐氨') && canOpenPrivateArchive(liveSnapshot)) {
          modalTitle.classList.add('nsfw-trigger-title');
          modalTitle.setAttribute('data-longpress', PRIVATE_ARCHIVE_PREVIEW_KEY);
          modalTitle.setAttribute('data-longpress-delay', '600');
        }
        if (modalSubtitle) modalSubtitle.textContent = '';
        if (modalSummary) modalSummary.textContent = '';
        setArchiveRedesignState(refs, !shellMode);
        modalBody.innerHTML = wrapArchiveRedesignBody(liveArchive.body, { unifiedMode, previewKey, shellMode });
        if (shouldResetModalScroll) modalBody.scrollTop = 0;
        鍚屾AI缁存姢鏍囬鍏ュ彛(previewKey);
        if (typeof liveArchive.onMount === 'function') {
          activeSubUI = liveArchive.onMount(modalBody);
        }
        return;
      }
      const liveRequiredKeys = new Set([
        '鐢熷懡鍥捐氨璇︾粏椤?,
		'绉佸瘑妗ｆ璇︾粏椤?,
        '绀句細妗ｆ璇︾粏椤?,
        '鎵€灞炲娍鍔涜缁嗛〉',
        '浜虹墿鍏崇郴璇︾粏椤?,
        '鎯呮姤搴撹缁嗛〉',
        '姝﹁宸ュ潑璇︾粏椤?,
        '姝﹂瓊铻嶅悎鎶€璇︾粏椤?,
        '鍌ㄧ墿浠撳簱璇︾粏椤?,
        '绗竴姝﹂瓊璇︾粏椤?,
        '绗簩姝﹂瓊璇︾粏椤?,
        '琛€鑴夊皝鍗拌缁嗛〉',
        '缂栧勾鍙叉。妗?,
        '鏈湴鎹偣璇︽儏',
        '褰撳墠鑺傜偣璇︽儏',
        '鎿嶄綔鎬荤嚎',
        '璇曠偧涓庢儏鎶?,
        '杩戞湡瑙侀椈',
        '涓栫晫鐘舵€佹€昏',
        '鍔垮姏鐭╅樀鎬昏',
        '鎴戠殑闃佃惀璇︽儏',
        '绯荤粺鎾姤涓庢棩蹇?,
        '鎬墿鍥鹃壌',
        '妫灄浠囨仺鍊?
      ]);
      if (!liveSnapshot && (liveRequiredKeys.has(previewKey) || String(previewKey || '').startsWith('鍦板浘鑺傜偣锛?))) {
        if (skeletonArchive) {
          const isVaultSkeleton = previewKey === '鍌ㄧ墿浠撳簱璇︾粏椤?;
          modalPanel.classList.add('archive-mode');
          modalPanel.classList.toggle('vault-mode', isVaultSkeleton);
          modalBody.className = isVaultSkeleton ? 'modal-body archive-body vault-body' : 'modal-body archive-body';
          if (unifiedMode) modalBody.classList.add('mvu-unified-sheet-body');
          if (modalLevel) modalLevel.textContent = '';
          if (modalPath) modalPath.textContent = '';
          modalTitle.textContent = skeletonArchive.title;
          if (modalSubtitle) modalSubtitle.textContent = '';
          if (modalSummary) modalSummary.textContent = '';
          setArchiveRedesignState(refs, !shellMode);
          modalBody.innerHTML = wrapArchiveRedesignBody(skeletonArchive.body, { unifiedMode, previewKey, shellMode });
          if (shouldResetModalScroll) modalBody.scrollTop = 0;
          鍚屾AI缁存姢鏍囬鍏ュ彛(previewKey);
          return;
        }
        modalPanel.classList.add('archive-mode');
        modalPanel.classList.remove('vault-mode');
        modalBody.className = 'modal-body archive-body';
        if (unifiedMode) modalBody.classList.add('mvu-unified-sheet-body');
        if (modalLevel) modalLevel.textContent = '';
        if (modalPath) modalPath.textContent = '';
        modalTitle.textContent = previewKey || '璇︾粏淇℃伅';
        if (modalSubtitle) modalSubtitle.textContent = '';
        if (modalSummary) modalSummary.textContent = '';
        setArchiveRedesignState(refs, !shellMode);
        modalBody.innerHTML = wrapArchiveRedesignBody('', { unifiedMode, previewKey, shellMode });
        if (shouldResetModalScroll) modalBody.scrollTop = 0;
        鍚屾AI缁存姢鏍囬鍏ュ彛(previewKey);
        return;
      }
      const archiveBuilder = archiveModalBuilders[previewKey];
      const isVaultModal = previewKey === '鍌ㄧ墿浠撳簱璇︾粏椤?;
      if (archiveBuilder) {
        const view = archiveBuilder();
        modalPanel.classList.add('archive-mode');
        modalPanel.classList.toggle('vault-mode', isVaultModal);
        modalBody.className = isVaultModal ? 'modal-body archive-body vault-body' : 'modal-body archive-body';
        if (unifiedMode) modalBody.classList.add('mvu-unified-sheet-body');
        if (modalLevel) modalLevel.textContent = '';
        if (modalPath) modalPath.textContent = '';
        modalTitle.textContent = view.title;
        if (modalSubtitle) modalSubtitle.textContent = '';
        if (modalSummary) modalSummary.textContent = '';
        setArchiveRedesignState(refs, !shellMode);
        modalBody.innerHTML = wrapArchiveRedesignBody(view.body, { unifiedMode, previewKey, shellMode });
        if (shouldResetModalScroll) modalBody.scrollTop = 0;
        鍚屾AI缁存姢鏍囬鍏ュ彛(previewKey);
        return;
      }
      const config = previewMap[previewKey] || buildDynamicPreview(previewKey || '璇︾粏寮圭獥');
      modalPanel.classList.remove('archive-mode', 'vault-mode');
      modalBody.className = 'modal-body';
      if (unifiedMode) modalBody.classList.add('mvu-unified-sheet-body');
      setArchiveRedesignState(refs, false);
      if (modalLevel) modalLevel.textContent = '';
      if (modalPath) modalPath.textContent = '';
      modalTitle.textContent = config.title;
      if (modalSubtitle) modalSubtitle.textContent = '';
      if (modalSummary) modalSummary.textContent = '';
      modalBody.innerHTML = renderGenericModalBody(config);
      if (shouldResetModalScroll) modalBody.scrollTop = 0;
      鍚屾AI缁存姢鏍囬鍏ュ彛(previewKey);
    }

    function popModalOrClose() {
      if (modalStack.length > 1) {
        const poppedPreviewKey = modalStack.pop();
        if (isSkillDesignerPreviewKey(poppedPreviewKey)) clearCachedSkillDesignerDraft(poppedPreviewKey);
        currentModalPreviewKey = modalStack[modalStack.length - 1] || '';
        if (currentModalPreviewKey) {
          renderModalContent(currentModalPreviewKey, getModalRefs());
          return;
        }
      }
      closeModal();
    }

    function closeModal() {
      if (activeSubUI && typeof activeSubUI.destroy === 'function') {
        activeSubUI.destroy();
        activeSubUI = null;
      }
      clearCachedSkillDesignerDrafts(modalStack);
      mapDispatchContext = null;

      hideInventoryHoverPanel();
      currentModalPreviewKey = '';
      lastRenderedModalPreviewKey = '';
      lastRenderedShellPreviewKey = '';
      modalStack = [];
      currentModalDisplayMode = 'auto';
      clearShellInlinePreview();
      hideDetailModalElement(getModalRefs());
      syncModalTitleLongPress('', false);
      鍚屾AI缁存姢鏍囬鍏ュ彛('');
      notifyShellPreviewClosed();
      flushPendingLiveRefresh();
    }
    window.__MVU_CLOSE_DETAIL_MODAL__ = closeModal;
    window.__MVU_SYNC_DETAIL_MODAL_HOST__ = () => {
      const refs = getModalRefs();
      applyModalDisplayMode(refs, { displayMode: currentModalDisplayMode });
    };

    const 缁堢鏍囩棰勮鏄犲皠 = Object.freeze({
      鐘舵€? '涓栫晫鐘舵€佹€昏',
      鎾姤: '绯荤粺鎾姤涓庢棩蹇?,
      瀹夋帓: '鎿嶄綔鎬荤嚎',
    });

    function 搴旂敤缁堢鏍囩鍒囨崲(缁堢鏍囩鎸夐挳) {
      if (!缁堢鏍囩鎸夐挳 || !(缁堢鏍囩鎸夐挳 instanceof Element)) return false;
      const 鏍囩瀹夸富 = 缁堢鏍囩鎸夐挳.closest('[data-terminal-tab-host]');
      if (!鏍囩瀹夸富) return false;
      const 鐩爣鏍囩 = toText(缁堢鏍囩鎸夐挳.getAttribute('data-terminal-tab'), '鐘舵€?).trim() || '鐘舵€?;
      鏍囩瀹夸富.querySelectorAll('[data-terminal-tab]').forEach(btn => {
        const isActive = btn === 缁堢鏍囩鎸夐挳;
        btn.classList.toggle('active', isActive);
        if (isActive) btn.setAttribute('aria-pressed', 'true');
        else btn.removeAttribute('aria-pressed');
      });
      鏍囩瀹夸富.querySelectorAll('[data-terminal-tab-panel]').forEach(panel => {
        panel.classList.toggle('active', toText(panel.getAttribute('data-terminal-tab-panel'), '') === 鐩爣鏍囩);
      });
      const 鐩爣棰勮 = toText(缁堢鏍囩鎸夐挳.getAttribute('data-terminal-preview'), '').trim()
        || toText(缁堢鏍囩棰勮鏄犲皠[鐩爣鏍囩], '').trim();
      if (鐩爣棰勮) {
        const 鍙偣鍑诲崱鐗?= 鏍囩瀹夸富.closest('.clickable[data-preview]');
        if (鍙偣鍑诲崱鐗? 鍙偣鍑诲崱鐗?setAttribute('data-preview', 鐩爣棰勮);
      }
      return true;
    }

    function 鏄惁淇濈暀鍗＄墖鍐呴儴鎺т欢鐐瑰嚮(eventTarget, mountEl) {
      if (!eventTarget || typeof eventTarget.closest !== 'function') return false;
      const 鎺т欢鑺傜偣 = eventTarget.closest('button, a, input, select, textarea, label, summary, [role="button"], [role="menuitem"], [contenteditable="true"]');
      if (!鎺т欢鑺傜偣 || (mountEl && !mountEl.contains(鎺т欢鑺傜偣))) return false;
      const 鑷甫棰勮鍏ュ彛 = 鎺т欢鑺傜偣.closest('.clickable[data-preview]');
      if (鑷甫棰勮鍏ュ彛 === 鎺т欢鑺傜偣) return false;
      const 澶栧眰鐘舵€佸崱 = 鎺т欢鑺傜偣.closest('.mvu-unified-card.clickable[data-preview], .mvu-mobile-card.clickable[data-preview]');
      return !!(澶栧眰鐘舵€佸崱 && 澶栧眰鐘舵€佸崱 !== 鎺т欢鑺傜偣);
    }

    function bindVueModalDelegation(mountEl) {
      if (!mountEl || mountEl.__mvuModalDelegationBound) return;
      mountEl.addEventListener('click', (event) => {
        const eventTarget = event.target instanceof Element ? event.target : (event.target && event.target.parentElement ? event.target.parentElement : null);
        if (isInlineEditInteractionTarget(eventTarget)) return;
        const isUnifiedMount = mountEl.id === 'mvu-unified-mount';
        const mapFocusBtn = eventTarget ? eventTarget.closest('[data-map-focus-action]') : null;
        if (mapFocusBtn && mountEl.contains(mapFocusBtn)) {
          const action = mapFocusBtn.getAttribute('data-map-focus-action') || '';
          if (action === 'current') {
            event.preventDefault();
            event.stopPropagation();
            if (typeof window.__sheepMapResync === 'function') {
              try { window.__sheepMapResync({ center: true, syncVisual: true }); } catch (err) {}
              scheduleUnifiedMapCanvasClamp();
            }
          }
          return;
        }
        const 缁堢鏍囩鎸夐挳 = eventTarget ? eventTarget.closest('[data-terminal-tab]') : null;
        if (缁堢鏍囩鎸夐挳 && mountEl.contains(缁堢鏍囩鎸夐挳)) {
          event.preventDefault();
          event.stopPropagation();
          搴旂敤缁堢鏍囩鍒囨崲(缁堢鏍囩鎸夐挳);
          return;
        }
        if (鏄惁淇濈暀鍗＄墖鍐呴儴鎺т欢鐐瑰嚮(eventTarget, mountEl)) {
          return;
        }
        const clickable = eventTarget ? eventTarget.closest('.clickable') : null;
        if (!clickable || !mountEl.contains(clickable)) return;

        const previewKey = clickable.dataset.preview;
        if (!previewKey) return;
        event.preventDefault();
        event.stopPropagation();
        if (isUnifiedMount && !clickable.closest('.mvu-mobile-shell') && typeof window.__MVU_OPEN_UNIFIED_PREVIEW__ === 'function') {
          window.__MVU_OPEN_UNIFIED_PREVIEW__(previewKey, { triggerEl: clickable, preserveMapDispatchContext: true });
          return;
        }
        const displayMode = mountEl.id === 'mvu-unified-mount' ? 'floating' : 'auto';
        toggleModal(previewKey, { triggerEl: clickable, displayMode });
      });
      mountEl.__mvuModalDelegationBound = true;
    }

    function bindAllVueModalDelegations() {
      bindVueModalDelegation(document.getElementById('mvu-unified-mount'));
    }

    function installVueModalDelegationObserver() {
      if (window.__MVU_MODAL_DELEGATION_OBSERVER__ && typeof window.__MVU_MODAL_DELEGATION_OBSERVER__.disconnect === 'function') {
        try { window.__MVU_MODAL_DELEGATION_OBSERVER__.disconnect(); } catch (err) {}
      }
      const root = document.body || document.documentElement;
      if (!root) return;
      const observer = new MutationObserver(() => {
        bindAllVueModalDelegations();
      });
      observer.observe(root, { childList: true, subtree: true });
      window.__MVU_MODAL_DELEGATION_OBSERVER__ = observer;
    }

    document.addEventListener('click', (event) => {
      const eventTarget = event.target instanceof Element ? event.target : (event.target && event.target.parentElement ? event.target.parentElement : null);
      if (isInlineEditInteractionTarget(eventTarget)) return;
      const 缁堢鏍囩鎸夐挳 = eventTarget ? eventTarget.closest('[data-terminal-tab]') : null;
      if (缁堢鏍囩鎸夐挳 && 搴旂敤缁堢鏍囩鍒囨崲(缁堢鏍囩鎸夐挳)) {
        event.preventDefault();
        event.stopPropagation();
        return;
      }
      const AI缁存姢鏍囬鎸夐挳 = eventTarget ? eventTarget.closest('[data-ai-maintenance-toggle]') : null;
      if (AI缁存姢鏍囬鎸夐挳 && document.contains(AI缁存姢鏍囬鎸夐挳)) {
        event.preventDefault();
        event.stopPropagation();
        const previewKey = toText(AI缁存姢鏍囬鎸夐挳.getAttribute('data-ai-maintenance-preview'), '').trim()
          || currentUnifiedPreviewKey
          || currentModalPreviewKey
          || getDomUnifiedPreviewKey();
        const root = 鏌ユ壘AI缁存姢鏍?previewKey);
        if (!root) {
          showUiToast('褰撳墠椤佃繕娌℃湁鍙淮鎶ゅ唴瀹广€?, 'info', 1800);
          return;
        }
        鍚姩AI缁存姢妯″紡(root, previewKey, { surface: currentUnifiedPreviewKey ? 'unified' : 'modal' });
        return;
      }
      const clickable = eventTarget ? eventTarget.closest('.clickable') : null;
      const unifiedMount = document.getElementById('mvu-unified-mount');
      const inUnifiedSurface = (unifiedMount && unifiedMount.contains(clickable))
        || !!(clickable && clickable.closest('.mvu-mobile-shell'));

      if (!clickable || !inUnifiedSurface) return;
      if (鏄惁淇濈暀鍗＄墖鍐呴儴鎺т欢鐐瑰嚮(eventTarget, unifiedMount || document.body)) return;
      const previewKey = clickable.dataset.preview;
      if (!previewKey) return;
      if (unifiedMount && unifiedMount.contains(clickable) && !clickable.closest('.mvu-mobile-shell') && typeof window.__MVU_OPEN_UNIFIED_PREVIEW__ === 'function') {
        event.preventDefault();
        event.stopPropagation();
        window.__MVU_OPEN_UNIFIED_PREVIEW__(previewKey, { triggerEl: clickable, preserveMapDispatchContext: true });
        return;
      }
      const displayMode = 'floating';
      toggleModal(previewKey, { triggerEl: clickable, displayMode });
    });

    function getDetailSurfacePreviewKey(options = {}) {
      const explicitKey = toText(options.previewKey, '').trim();
      if (explicitKey) return explicitKey;
      return options.surface === 'unified'
        ? (currentUnifiedPreviewKey || currentModalPreviewKey || '')
        : (currentModalPreviewKey || currentUnifiedPreviewKey || '');
    }

    function rerenderDetailSurface(previewKey = '', options = {}) {
      const targetKey = toText(previewKey, '').trim();
      if (!targetKey) return;
      if (options.surface === 'unified' || (currentUnifiedPreviewKey === targetKey && isUnifiedInlinePreviewActive())) {
        renderUnifiedInlinePreview(targetKey, { ...options, force: !!options.force });
        return;
      }
      renderModalContent(targetKey, getModalRefs(), { force: !!options.force });
    }

    function openNestedDetailPreview(previewKey, options = {}) {
      const targetKey = toText(previewKey, '').trim();
      if (!targetKey) return;
      if (options.surface === 'unified' && typeof window.__MVU_OPEN_UNIFIED_PREVIEW__ === 'function') {
        window.__MVU_OPEN_UNIFIED_PREVIEW__(targetKey, { preserveMapDispatchContext: true });
        return;
      }
      openModal(targetKey, { preserveMapDispatchContext: true });
    }

    function closeDetailSurface(options = {}) {
      if (options.surface === 'unified' && typeof window.__MVU_CLOSE_UNIFIED_PREVIEW__ === 'function') {
        window.__MVU_CLOSE_UNIFIED_PREVIEW__({ force: true });
        return;
      }
      closeModal();
    }

    if (modalClose) modalClose.addEventListener('click', popModalOrClose);
    async function handleDetailSurfaceClick(event, surfaceHost = modalBody, options = {}) {
      const detailSurfaceHost = surfaceHost instanceof Element ? surfaceHost : modalBody;
      const detailSurfacePanel = options.panel instanceof Element ? options.panel : detailSurfaceHost;
      const detailPreviewKey = getDetailSurfacePreviewKey(options);
      if (!detailSurfaceHost) return;
      const eventTarget = event.target instanceof Element ? event.target : (event.target && event.target.parentElement ? event.target.parentElement : null);
      if (isInlineEditInteractionTarget(eventTarget)) {
        event.stopPropagation();
        return;
      }
      const AI缁存姢鏍?= eventTarget ? eventTarget.closest('[data-ai-maintenance-root]') : null;
      const AI缁存姢鎸夐挳 = eventTarget ? eventTarget.closest('[data-ai-maintenance-toggle]') : null;
      if (AI缁存姢鎸夐挳 && detailSurfaceHost.contains(AI缁存姢鎸夐挳)) {
        event.preventDefault();
        event.stopPropagation();
        鍚姩AI缁存姢妯″紡(AI缁存姢鏍?|| detailSurfaceHost, detailPreviewKey, options);
        return;
      }
      const AI缁存姢鍔ㄤ綔鎸夐挳 = eventTarget ? eventTarget.closest('[data-ai-maintenance-action]') : null;
      if (AI缁存姢_鐘舵€?active && AI缁存姢鍔ㄤ綔鎸夐挳 && AI缁存姢鏍?&& AI缁存姢_鐘舵€?root === AI缁存姢鏍? {
        event.preventDefault();
        event.stopPropagation();
        const action = AI缁存姢鍔ㄤ綔鎸夐挳.getAttribute('data-ai-maintenance-action') || '';
        if (action === 'submit') await 鎻愪氦AI缁存姢璇锋眰(AI缁存姢鏍?;
        else if (action === 'clear') 娓呯┖AI缁存姢閫夋嫨();
        else if (action === 'cancel') 缁撴潫AI缁存姢妯″紡();
        return;
      }
      if (AI缁存姢_鐘舵€?active && AI缁存姢鏍?&& AI缁存姢_鐘舵€?root === AI缁存姢鏍? {
        const AI缁存姢瀛楁 = eventTarget ? eventTarget.closest('[data-ai-maintenance-field]') : null;
        const AI缁存姢鍙橀噺缁?= eventTarget ? eventTarget.closest('[data-ai-maintenance-group]') : null;
        if (AI缁存姢瀛楁 && AI缁存姢鏍?contains(AI缁存姢瀛楁)) {
          event.preventDefault();
          event.stopPropagation();
          鍒囨崲AI缁存姢瀛楁閫夋嫨(AI缁存姢瀛楁);
          return;
        }
        if (AI缁存姢鍙橀噺缁?&& AI缁存姢鏍?contains(AI缁存姢鍙橀噺缁?) {
          event.preventDefault();
          event.stopPropagation();
          鍒囨崲AI缁存姢鍙橀噺缁勯€夋嫨(AI缁存姢鍙橀噺缁?;
          return;
        }
        if (!eventTarget.closest('[data-ai-maintenance-toolbar], [data-ai-maintenance-bar]')) {
          event.preventDefault();
          event.stopPropagation();
          return;
        }
      }
      const 鍙帺閫夐」鎸夐挳 = eventTarget ? eventTarget.closest('[data-lwcs-option-fill]') : null;
      if (鍙帺閫夐」鎸夐挳 && detailSurfaceHost.contains(鍙帺閫夐」鎸夐挳)) {
        event.preventDefault();
        event.stopPropagation();
        const 閫夐」鏂囨湰 = toText(鍙帺閫夐」鎸夐挳.getAttribute('data-lwcs-option-fill'), '').trim();
        const 鐭攣閿?= toText(鍙帺閫夐」鎸夐挳.getAttribute('data-lwcs-option-key'), '').trim() || 閫夐」鏂囨湰;
        const 褰撳墠姣 = Date.now();
        if (褰撳墠姣 < 鍙帺閫夐」鐭攣鎴鏃堕棿 && 鐭攣閿?=== 鍙帺閫夐」鐭攣閿? {
          showUiToast('鎿嶄綔杩囧揩锛岃绋嶅€欏啀鐐广€?, 'info', 1600);
          return;
        }
        if (!閫夐」鏂囨湰) {
          showUiToast('褰撳墠閫夐」涓虹┖锛屾棤娉曞啓鍏ヨ緭鍏ユ銆?, 'error', 2400);
          return;
        }
        const 鍐欏叆鎴愬姛 = 鍐欏叆閫夐」鍒拌緭鍏ユ(閫夐」鏂囨湰);
        if (!鍐欏叆鎴愬姛) {
          showUiToast('鏈壘鍒拌緭鍏ユ锛屾棤娉曞啓鍏ラ€夐」銆?, 'error', 2800);
          return;
        }
        鍙帺閫夐」鐭攣閿?= 鐭攣閿?
        鍙帺閫夐」鐭攣鎴鏃堕棿 = 褰撳墠姣 + 420;
        鍙帺閫夐」鎸夐挳.classList.add('is-short-lock');
        window.setTimeout(() => {
          if (鍙帺閫夐」鎸夐挳 && typeof 鍙帺閫夐」鎸夐挳.classList?.remove === 'function') {
            鍙帺閫夐」鎸夐挳.classList.remove('is-short-lock');
          }
        }, 420);
        showUiToast('宸插啓鍏ヨ緭鍏ユ銆?, 'info', 1800);
        return;
      }
      const actionBtn = eventTarget ? eventTarget.closest('.armory-action-btn') : null;
      if (actionBtn && detailSurfaceHost.contains(actionBtn)) {
        event.preventDefault();
        event.stopPropagation();
        if (!isSnapshotPlayerControlled(liveSnapshot)) {
          if (typeof window.alert === 'function') window.alert('鏃佽瑙嗚涓嶅彲鎿嶄綔銆?);
          return;
        }
        const actionData = buildArmoryActionRequest(liveSnapshot, actionBtn.dataset.armoryAction || '');
        if (actionData) dispatchUiAiRequest(actionData.playerInput, actionData.systemPrompt, {
          requestKind: actionData.requestKind,
          patchOps: actionData.patchOps
        });
        return;
      }

      const switchCharBtn = eventTarget ? eventTarget.closest('[data-mvu-switch-char]') : null;
      if (switchCharBtn && detailSurfaceHost.contains(switchCharBtn)) {
        event.preventDefault();
        event.stopPropagation();
        const targetName = switchCharBtn.getAttribute('data-mvu-switch-char') || '';
        const switched = applyActiveCharacterSelection(targetName, { closeModal: false });
        if (!switched) {
          if (window.MVU_Toast && typeof window.MVU_Toast.show === 'function') window.MVU_Toast.show(`鍒囨崲瑙掕壊澶辫触锛氭壘涓嶅埌銆?{targetName}銆戙€俙, 'error');
          else if (typeof window.alert === 'function') window.alert(`鍒囨崲瑙掕壊澶辫触锛氭壘涓嶅埌銆?{targetName}銆戙€俙);
        }
        return;
      }

      const mapTravelBtn = eventTarget ? eventTarget.closest('[data-map-travel-node]') : null;
      if (mapTravelBtn && detailSurfaceHost.contains(mapTravelBtn)) {
        event.preventDefault();
        event.stopPropagation();
        const nodeName = mapTravelBtn.getAttribute('data-map-travel-node') || '';
        let sheepMapBridge = window.__sheepMapBridge;
        if (!sheepMapBridge || typeof sheepMapBridge.travelToNode !== 'function') {
          await 纭繚妯″潡渚濊禆宸插姞杞?'鍦板浘妯″潡', 'map_travel_action');
          sheepMapBridge = window.__sheepMapBridge;
        }
        if (!sheepMapBridge || typeof sheepMapBridge.travelToNode !== 'function') {
          showUiToast('鍦板浘绉诲姩妗ユ湭灏辩华锛屾殏鏃舵棤娉曠洿鎺ュ墠寰€璇ヨ妭鐐广€?, 'error', 4200);
          return;
        }
        const travelResult = sheepMapBridge.travelToNode(nodeName);
        if (!travelResult || travelResult.ok === false) {
          showUiToast(toText(travelResult && travelResult.reason, `褰撳墠鏃犳硶鍓嶅線銆?{nodeName}銆戙€俙), 'error', 4200);
          return;
        }
        showUiToast(travelResult.committed ? `宸叉彁浜ゅ墠寰€銆?{nodeName}銆戠殑绉诲姩銆俙 : `宸茶鍒掑墠寰€銆?{nodeName}銆戙€俙, 'info', 3200);
        closeDetailSurface(options);
        return;
      }

      const relationFocusBtn = eventTarget ? eventTarget.closest('[data-relation-focus]') : null;
      if (relationFocusBtn && detailSurfaceHost.contains(relationFocusBtn)) {
        event.preventDefault();
        event.stopPropagation();
        const targetName = relationFocusBtn.getAttribute('data-relation-focus') || '';
        if (targetName && detailPreviewKey) {
          modalFocusState[`${detailPreviewKey}::relation-focus`] = targetName;
          rerenderDetailSurface(detailPreviewKey, options);
        }
        return;
      }

      const inventoryActionBtn = eventTarget ? eventTarget.closest('.inventory-hover-action-btn[data-inventory-action]') : null;
      if (inventoryActionBtn) {
        event.preventDefault();
        event.stopPropagation();
        const action = inventoryActionBtn.getAttribute('data-inventory-action') || '';
        const charKey = inventoryActionBtn.getAttribute('data-inventory-char') || '';
        const itemName = inventoryActionBtn.getAttribute('data-inventory-item') || '';
        const mode = inventoryActionBtn.getAttribute('data-inventory-mode') || 'one';
        if (!window.EquipmentManager || !charKey || !itemName) {
          if (window.MVU_Toast && typeof window.MVU_Toast.show === 'function') window.MVU_Toast.show('褰撳墠鐗╁搧鍔ㄤ綔缂哄皯蹇呰淇℃伅锛屾棤娉曟墽琛屻€?, 'error');
          else if (typeof window.alert === 'function') window.alert('褰撳墠鐗╁搧鍔ㄤ綔缂哄皯蹇呰淇℃伅锛屾棤娉曟墽琛屻€?);
          return;
        }
        hideInventoryHoverPanel();
        if (action === 'use') {
          window.EquipmentManager.performUse(charKey, itemName);
          return;
        }
        if (action === 'equip') {
          window.EquipmentManager.performEquip(charKey, itemName);
          return;
        }
        if (action === 'discard') {
          window.EquipmentManager.performDiscard(charKey, itemName, mode);
          return;
        }
      }

      const skillActionBtn = eventTarget ? eventTarget.closest('.skill-inline-action-btn[data-skill-action]') : null;
      if (skillActionBtn && detailSurfaceHost.contains(skillActionBtn)) {
        event.preventDefault();
        event.stopPropagation();
        const action = skillActionBtn.getAttribute('data-skill-action') || '';
        const skillPathRaw = skillActionBtn.getAttribute('data-skill-path') || '[]';
        let skillPath = [];
        try {
          const parsed = JSON.parse(skillPathRaw);
          if (Array.isArray(parsed)) skillPath = parsed;
        } catch (error) {}
        if (!skillPath.length) {
          showUiToast('褰撳墠鎶€鑳界己灏戝畾浣嶄俊鎭紝鏃犳硶鏂藉睍銆?, 'error');
          return;
        }
        if (action === 'cast-construct') {
          await window.EquipmentManager.performConstructSkillUse(skillPath);
          return;
        }
      }

      const equipmentActionBtn = eventTarget ? eventTarget.closest('.equipment-action-btn[data-equipment-action]') : null;
      if (equipmentActionBtn && detailSurfaceHost.contains(equipmentActionBtn)) {
        event.preventDefault();
        event.stopPropagation();
        const action = equipmentActionBtn.getAttribute('data-equipment-action') || '';
        const charKey = equipmentActionBtn.getAttribute('data-equipment-char') || '';
        const kind = equipmentActionBtn.getAttribute('data-equipment-kind') || '';
        const targetName = equipmentActionBtn.getAttribute('data-equipment-name') || '';
        if (!window.EquipmentManager || !charKey || !kind) {
          if (window.MVU_Toast && typeof window.MVU_Toast.show === 'function') window.MVU_Toast.show('褰撳墠瑁呭鍔ㄤ綔缂哄皯蹇呰淇℃伅锛屾棤娉曟墽琛屻€?, 'error');
          else if (typeof window.alert === 'function') window.alert('褰撳墠瑁呭鍔ㄤ綔缂哄皯蹇呰淇℃伅锛屾棤娉曟墽琛屻€?);
          return;
        }
        if (action === 'unequip') {
          window.EquipmentManager.performUnequip(charKey, kind, targetName);
          return;
        }
      }

      const pendingSoulRingActionBtn = eventTarget ? eventTarget.closest('[data-soul-ring-action]') : null;
      if (pendingSoulRingActionBtn && detailSurfaceHost.contains(pendingSoulRingActionBtn)) {
        event.preventDefault();
        event.stopPropagation();
        const actionType = pendingSoulRingActionBtn.getAttribute('data-soul-ring-action') || '';
        const candidateSoulName = pendingSoulRingActionBtn.getAttribute('data-soul-ring-candidate') || '';
        try {
          if (actionType === 'generate') {
            await handlePendingSoulRingGenerate(liveSnapshot || lastRenderableSnapshot, candidateSoulName);
            return;
          }
          if (actionType === 'defer') {
            await handlePendingSoulRingDefer(liveSnapshot || lastRenderableSnapshot);
            return;
          }
        } catch (error) {
          showUiToast(error && error.message ? error.message : '榄傜幆鐢熸垚澶勭悊澶辫触銆?, 'error');
          return;
        }
      }

      const injuryActionBtn = eventTarget ? eventTarget.closest('[data-injury-action]') : null;
      if (injuryActionBtn && detailSurfaceHost.contains(injuryActionBtn)) {
        event.preventDefault();
        event.stopPropagation();
        const actionType = injuryActionBtn.getAttribute('data-injury-action') || '';
        const charKey = injuryActionBtn.getAttribute('data-injury-char') || '';
        const panel = injuryActionBtn.closest('[data-injury-panel]') || detailSurfaceHost;
        const readInjuryInput = key => {
          const input = panel ? panel.querySelector(`[data-injury-input="${key}"]`) : null;
          return toText(input && 'value' in input ? input.value : '', '').trim();
        };
        try {
          if (actionType === 'add') {
            const injuryName = readInjuryInput('name');
            const injuryDegree = normalizeInjurySeverity(readInjuryInput('degree') || '杞?);
            const injuryDesc = readInjuryInput('desc');
            if (!charKey) throw new Error('缂哄皯瑙掕壊淇℃伅銆?);
            if (!injuryName) throw new Error('璇疯緭鍏ュ彈浼ら儴浣嶅悕绉般€?);
            await mutateStatDataByEditor(statData => {
              const injuryPath = ['char', charKey, '鐘舵€?, '鍙椾激閮ㄤ綅'];
              let injuryMap = deepGet(statData, injuryPath, null);
              if (!injuryMap || typeof injuryMap !== 'object' || Array.isArray(injuryMap)) {
                deepSetMutable(statData, injuryPath, {});
                injuryMap = deepGet(statData, injuryPath, null);
              }
              if (injuryMap[injuryName] !== undefined) throw new Error('鍚屽悕鍙椾激閮ㄤ綅宸插瓨鍦ㄣ€?);
              injuryMap[injuryName] = { 绋嬪害: injuryDegree, 鎻忚堪: injuryDesc };
            });
            const resetNameInput = panel ? panel.querySelector('[data-injury-input="name"]') : null;
            const resetDegreeInput = panel ? panel.querySelector('[data-injury-input="degree"]') : null;
            const resetDescInput = panel ? panel.querySelector('[data-injury-input="desc"]') : null;
            if (resetNameInput && 'value' in resetNameInput) resetNameInput.value = '';
            if (resetDegreeInput && 'value' in resetDegreeInput) resetDegreeInput.value = '杞?;
            if (resetDescInput && 'value' in resetDescInput) resetDescInput.value = '';
            showUiToast('宸叉柊澧炲彈浼ら儴浣嶃€?);
            if (detailPreviewKey) rerenderDetailSurface(detailPreviewKey, options);
            return;
          }
          if (actionType === 'delete') {
            const injuryKey = injuryActionBtn.getAttribute('data-injury-key') || '';
            if (!charKey || !injuryKey) throw new Error('褰撳墠浼ゅ缂哄皯瀹氫綅淇℃伅銆?);
            await deleteStatDataPathByEditor(['char', charKey, '鐘舵€?, '鍙椾激閮ㄤ綅', injuryKey]);
            showUiToast('宸插垹闄ゅ彈浼ら儴浣嶃€?);
            if (detailPreviewKey) rerenderDetailSurface(detailPreviewKey, options);
            return;
          }
        } catch (error) {
          showUiToast(error && error.message ? error.message : '浼ゅ鎿嶄綔澶辫触銆?, 'error');
          return;
        }
      }

      const collectionActionBtn = eventTarget ? eventTarget.closest('[data-collection-action]') : null;
      if (collectionActionBtn && detailSurfaceHost.contains(collectionActionBtn)) {
        event.preventDefault();
        event.stopPropagation();
        const actionType = collectionActionBtn.getAttribute('data-collection-action') || '';
        const panel = collectionActionBtn.closest('[data-collection-panel]') || detailSurfaceHost;
        const readCollectionInput = (key) => {
          const input = panel ? panel.querySelector(`[data-collection-input="${key}"]`) : null;
          return toText(input && 'value' in input ? input.value : '', '').trim();
        };
        const readCollectionNumber = (key, fallback = 0) => {
          const raw = readCollectionInput(key);
          return raw === '' ? fallback : toNumber(raw, fallback);
        };
        const 鎸夐挳鍘熸枃 = toText(collectionActionBtn.textContent, '鎵ц').trim() || '鎵ц';
        collectionActionBtn.disabled = true;
        collectionActionBtn.classList.add('is-busy');
        collectionActionBtn.textContent = '鍔犺浇涓?..';
        try {
          if (actionType === 'add-intel') {
            const charKey = collectionActionBtn.getAttribute('data-collection-char') || '';
            const intelText = readCollectionInput('intel-text');
            if (!charKey) throw new Error('缂哄皯瑙掕壊淇℃伅銆?);
            if (!intelText) throw new Error('璇疯緭鍏ユ儏鎶ュ唴瀹广€?);
            await mutateStatDataByEditor(statData => {
              const intelPath = ['char', charKey, '宸叉帉鎻℃儏鎶?];
              let knowledges = deepGet(statData, intelPath, null);
              if (!Array.isArray(knowledges)) {
                deepSetMutable(statData, intelPath, []);
                knowledges = deepGet(statData, intelPath, null);
              }
              if (knowledges.some(item => toText(item, '').trim() === intelText)) {
                throw new Error('杩欐潯鎯呮姤宸茬粡瀛樺湪銆?);
              }
              knowledges.push(intelText);
            });
            showUiToast('宸叉柊澧炴儏鎶ャ€?);
            if (detailPreviewKey) rerenderDetailSurface(detailPreviewKey, options);
            return;
          }
          if (actionType === 'delete-intel') {
            const charKey = collectionActionBtn.getAttribute('data-collection-char') || '';
            const index = toNumber(collectionActionBtn.getAttribute('data-collection-index'), -1);
            if (!charKey || index < 0) throw new Error('褰撳墠鎯呮姤缂哄皯瀹氫綅淇℃伅銆?);
            await deleteStatDataPathByEditor(['char', charKey, '宸叉帉鎻℃儏鎶?, index]);
            showUiToast('宸插垹闄ゆ儏鎶ャ€?);
            if (detailPreviewKey) rerenderDetailSurface(detailPreviewKey, options);
            return;
          }
          if (actionType === 'add-item') {
            const charKey = collectionActionBtn.getAttribute('data-collection-char') || '';
            const itemName = readCollectionInput('item-name');
            const quantity = Math.max(1, readCollectionNumber('item-qty', 1));
            const itemType = readCollectionInput('item-type') || '鐗╁搧';
            const itemDesc = readCollectionInput('item-desc') || '鏈煡';
            if (!charKey) throw new Error('缂哄皯瑙掕壊淇℃伅銆?);
            if (!itemName) throw new Error('璇疯緭鍏ョ墿鍝佸悕銆?);
            await mutateStatDataByEditor(statData => {
              const inventoryPath = ['char', charKey, '鑳屽寘'];
              let inventory = deepGet(statData, inventoryPath, null);
              if (!inventory || typeof inventory !== 'object' || Array.isArray(inventory)) {
                deepSetMutable(statData, inventoryPath, {});
                inventory = deepGet(statData, inventoryPath, null);
              }
              const currentItem = inventory[itemName] && typeof inventory[itemName] === 'object'
                ? cloneJsonValue(inventory[itemName], {})
                : {};
              inventory[itemName] = {
                ...currentItem,
                鏁伴噺: Math.max(1, toNumber(currentItem['鏁伴噺'], 0) + quantity),
                绫诲瀷: itemType || toText(currentItem['绫诲瀷'], '鐗╁搧'),
                鎻忚堪: itemDesc || toText(currentItem['鎻忚堪'], '鏈煡'),
                鍝佽川: toText(currentItem['鍝佽川'], '鏅€?) || '鏅€?,
                鍝侀樁: toText(currentItem['鍝侀樁'], '鏃?) || '鏃?,
                瑙﹀彂鏂瑰紡: toText(currentItem['瑙﹀彂鏂瑰紡'], /椋熺墿/.test(itemType) ? '椋熺敤' : '浣跨敤') || (/椋熺墿/.test(itemType) ? '椋熺敤' : '浣跨敤')
              };
            });
            showUiToast('宸叉柊澧炵墿鍝併€?);
            if (detailPreviewKey) rerenderDetailSurface(detailPreviewKey, options);
            return;
          }
          if (actionType === 'run-ascension') {
            const charKey = collectionActionBtn.getAttribute('data-collection-char') || '';
            const ticketName = readCollectionInput('asc-ticket');
            if (!charKey) throw new Error('缂哄皯瑙掕壊淇℃伅銆?);
            const currentSnapshot = liveSnapshot || lastRenderableSnapshot;
            if (!currentSnapshot) throw new Error('褰撳墠蹇収鏈氨缁紝鏃犳硶杩涘叆鍗囩伒鍙般€?);
            const currentLoc = toText(
              deepGet(currentSnapshot, 'activeChar.鐘舵€?浣嶇疆', currentSnapshot.currentLoc),
              currentSnapshot.currentLoc,
            );
            window.dispatchEvent(new CustomEvent('map-action-dispatch', {
              detail: {
                action: 'ascension',
                ticketName,
                currentLoc,
                target: currentLoc,
                services: [],
                source: 'trial_console',
              },
            }));
            if (detailPreviewKey) rerenderDetailSurface(detailPreviewKey, options);
            return;
          }
          if (actionType === 'run-soul-tower') {
            const charKey = collectionActionBtn.getAttribute('data-collection-char') || '';
            const ticketName = readCollectionInput('tower-ticket');
            if (!charKey) throw new Error('缂哄皯瑙掕壊淇℃伅銆?);
            const currentSnapshot = liveSnapshot || lastRenderableSnapshot;
            if (!currentSnapshot) throw new Error('褰撳墠蹇収鏈氨缁紝鏃犳硶寮€鍚瓊鐏靛銆?);
            const currentLoc = toText(
              deepGet(currentSnapshot, 'activeChar.鐘舵€?浣嶇疆', currentSnapshot.currentLoc),
              currentSnapshot.currentLoc,
            );
            window.dispatchEvent(new CustomEvent('map-action-dispatch', {
              detail: {
                action: 'tower',
                ticketName,
                floor: Math.max(1, getDefaultSoulTowerChallengeFloor(currentSnapshot)),
                currentLoc,
                target: currentLoc,
                services: [],
                source: 'trial_console',
              },
            }));
            if (detailPreviewKey) rerenderDetailSurface(detailPreviewKey, options);
            return;
          }
          if (actionType === 'add-record') {
            const charKey = collectionActionBtn.getAttribute('data-collection-char') || '';
            const recordName = readCollectionInput('record-name');
            const recordType = readCollectionInput('record-type') || '浠诲姟';
            const targetProgress = Math.max(1, readCollectionNumber('record-target', 1));
            const rewardCoin = Math.max(0, Math.floor(readCollectionNumber('record-rewardCoin', 0)));
            const rewardRep = Math.max(0, Math.floor(readCollectionNumber('record-rewardRep', 0)));
            const recordDesc = readCollectionInput('record-desc') || '鏈煡';
            if (!charKey) throw new Error('缂哄皯瑙掕壊淇℃伅銆?);
            if (!recordName) throw new Error('璇疯緭鍏ヤ换鍔″悕銆?);
            if (!recordDesc || recordDesc === '鏈煡') throw new Error('璇峰～鍐欎换鍔＄洰鏍囥€?);
            await mutateStatDataByEditor(statData => {
              const recordPath = ['char', charKey, '璁板綍'];
              let records = deepGet(statData, recordPath, null);
              if (!records || typeof records !== 'object' || Array.isArray(records)) {
                deepSetMutable(statData, recordPath, {});
                records = deepGet(statData, recordPath, null);
              }
              if (records[recordName] !== undefined) throw new Error('鍚屽悕浠诲姟宸插瓨鍦ㄣ€?);
              records[recordName] = {
                鐘舵€? '杩涜涓?,
                绫诲瀷: recordType,
                褰撳墠杩涘害: 0,
                鐩爣杩涘害: targetProgress,
                濂栧姳甯? rewardCoin,
                濂栧姳澹版湜: rewardRep,
                鎻忚堪: recordDesc
              };
            });
            modalFocusState['浠诲姟鐣岄潰::quest-focus'] = recordName;
            showUiToast('宸茶褰曚换鍔°€?);
            if (detailPreviewKey) rerenderDetailSurface(detailPreviewKey, options);
            return;
          }
          if (actionType === 'delete-record') {
            const charKey = collectionActionBtn.getAttribute('data-collection-char') || '';
            const recordName = collectionActionBtn.getAttribute('data-collection-key') || '';
            if (!charKey || !recordName) throw new Error('褰撳墠浠诲姟缂哄皯瀹氫綅淇℃伅銆?);
            await deleteStatDataPathByEditor(['char', charKey, '璁板綍', recordName]);
            if (modalFocusState['浠诲姟鐣岄潰::quest-focus'] === recordName) delete modalFocusState['浠诲姟鐣岄潰::quest-focus'];
            showUiToast('宸插垹闄や换鍔°€?);
            if (detailPreviewKey) rerenderDetailSurface(detailPreviewKey, options);
            return;
          }
          if (actionType === 'add-board') {
            const boardName = readCollectionInput('board-name');
            const boardType = readCollectionInput('board-type') || '濮旀墭';
            const boardPublisher = readCollectionInput('board-publisher') || '绯荤粺';
            const boardDesc = readCollectionInput('board-desc') || '鏈煡';
            const boardTarget = Math.max(1, Math.floor(readCollectionNumber('board-target', 1)));
            const boardDifficulty = readCollectionInput('board-difficulty') || '涓?;
            const boardResource = readCollectionInput('board-resource') || '鏃?;
            const boardRewardCoin = Math.max(0, Math.floor(readCollectionNumber('board-rewardCoin', 0)));
            const boardRewardRep = Math.max(0, Math.floor(readCollectionNumber('board-rewardRep', 0)));
            if (!boardName) throw new Error('璇疯緭鍏ュ鎵樺悕銆?);
            if (!boardDesc || boardDesc === '鏈煡') throw new Error('璇峰～鍐欏鎵樼洰鏍囥€?);
            const 褰撳墠蹇収 = liveSnapshot || lastRenderableSnapshot || {};
            const 濮旀墭鏉?= deepGet(褰撳墠蹇収, 'rootData.world.濮旀墭鏉?, {});
            if (濮旀墭鏉?&& typeof 濮旀墭鏉?=== 'object' && !Array.isArray(濮旀墭鏉? && 濮旀墭鏉縖boardName] !== undefined) {
              throw new Error('鍚屽悕濮旀墭宸插瓨鍦ㄣ€?);
            }
            const activeName = toText(褰撳墠蹇収.activeName, '褰撳墠瑙掕壊');
            const currentLoc = toText(
              deepGet(褰撳墠蹇収, 'activeChar.鐘舵€?浣嶇疆', 褰撳墠蹇収.currentLoc),
              褰撳墠蹇収.currentLoc || '褰撳墠浣嶇疆',
            );
            const 濮旀墭鏁版嵁 = {
              鏍囬: boardName,
              鐘舵€? '寰呮帴鍙?,
              绫诲瀷: boardType,
              鍙戝竷鑰? boardPublisher,
              闈㈠悜: '鍏紑',
              鎵挎帴鑰? '鏃?,
              鐩爣杩涘害: boardTarget,
              闅惧害: boardDifficulty,
              璧勬簮绾у埆: boardResource,
              濂栧姳甯? boardRewardCoin,
              濂栧姳澹版湜: boardRewardRep,
              妗嗘灦鎻忚堪: boardDesc,
              鎻忚堪: boardDesc
            };
            const 濮旀墭琛ヤ竵 = [
              { op: 'replace', path: `/world/濮旀墭鏉?${escapeJsonPointerValue(boardName)}`, value: 濮旀墭鏁版嵁 },
              { op: 'replace', path: '/sys/绯荤粺鎾姤', value: `[濮旀墭鍙戝竷] ${activeName} 鍦ㄣ€?{currentLoc}銆戝彂甯冨鎵樸€?{boardName}銆戙€俙 }
            ];
            const 琛屽姩璺緞瑙掕壊 = resolveSnapshotCharKey(褰撳墠蹇収, activeName);
            if (琛屽姩璺緞瑙掕壊) {
              濮旀墭琛ヤ竵.push({ op: 'replace', path: `/char/${escapeJsonPointerValue(琛屽姩璺緞瑙掕壊)}/鐘舵€?琛屽姩`, value: '鍙戝竷濮旀墭' });
            }
            modalFocusState['浠诲姟鐣岄潰::quest-board-focus'] = boardName;
            await dispatchUiAiRequest(
              `鎴戞兂鍦ㄣ€?{currentLoc}銆戝彂甯冨鎵樸€?{boardName}銆戙€俙,
              `浠ヤ笅鍐呭灞炰簬鍓嶇宸茬粡瀹屾垚鐨勫鎵樺彂甯冪櫥璁帮紝涓嶈鍦ㄦ鏂囩洿鎺ュ杩扳€淛SONPatch / 绯荤粺鍒嗘瀽 / 浠茶鏃ュ織鈥濈瓑鏈銆?
[濮旀墭鍙戝竷]
鍙戝竷鑰咃細${boardPublisher}
濮旀墭鍚嶏細${boardName}
绫诲瀷锛?{boardType}
鐩爣锛?{boardDesc}
棰勮闃舵锛?{boardTarget}
闅惧害锛?{boardDifficulty}
璧勬簮绾у埆锛?{boardResource}
濂栧姳锛?{boardRewardCoin} 鑱旈偊甯?/ ${boardRewardRep} 澹版湜

濮旀墭宸茬粡鍐欏叆濮旀墭鏉裤€傛鏂囬渶瑕佽嚜鐒跺弽棣堝彂甯冪粨鏋溿€佸懆鍥村弽搴斾笌鍚庣画鍙帴鍙栫┖闂达紝涓嶈閲嶅杈撳嚭杩欐壒鍙橀噺鏇存柊銆俙,
              { requestKind: '濮旀墭鍙戝竷', patchOps: 濮旀墭琛ヤ竵 }
            );
            if (detailPreviewKey) rerenderDetailSurface(detailPreviewKey, options);
            return;
          }
          if (actionType === 'delete-board') {
            const boardName = collectionActionBtn.getAttribute('data-collection-key') || '';
            if (!boardName) throw new Error('褰撳墠濮旀墭缂哄皯瀹氫綅淇℃伅銆?);
            await deleteStatDataPathByEditor(['world', '濮旀墭鏉?, boardName]);
            if (modalFocusState['浠诲姟鐣岄潰::quest-board-focus'] === boardName) delete modalFocusState['浠诲姟鐣岄潰::quest-board-focus'];
            showUiToast('宸插垹闄ゅ鎵樸€?);
            if (detailPreviewKey) rerenderDetailSurface(detailPreviewKey, options);
            return;
          }
        } catch (error) {
          showUiToast(error && error.message ? error.message : '鎿嶄綔澶辫触銆?, 'error');
          if (actionType === 'run-ascension' || actionType === 'run-soul-tower') {
            showUiToast('鍙鏌ラ棬绁ㄤ笌鍦扮偣鍚庨噸璇曘€?, 'info', 2600);
          }
          return;
        } finally {
          if (collectionActionBtn && collectionActionBtn.isConnected) {
            collectionActionBtn.disabled = false;
            collectionActionBtn.classList.remove('is-busy');
            collectionActionBtn.textContent = 鎸夐挳鍘熸枃;
          }
        }
      }

      const questActionBtn = eventTarget ? eventTarget.closest('.quest-action-btn[data-quest-action]') : null;
      if (questActionBtn && detailSurfaceHost.contains(questActionBtn)) {
        event.preventDefault();
        event.stopPropagation();
        if (!isSnapshotPlayerControlled(liveSnapshot)) {
          if (window.MVU_Toast && typeof window.MVU_Toast.show === 'function') window.MVU_Toast.show('鏃佽瑙嗚涓嶅彲鎿嶄綔銆?, 'error');
          else if (typeof window.alert === 'function') window.alert('鏃佽瑙嗚涓嶅彲鎿嶄綔銆?);
          return;
        }
        const actionType = questActionBtn.getAttribute('data-quest-action') || '';
        const requestPanel = questActionBtn.closest('.request-console-card') || detailSurfaceHost;
        const nameInput = requestPanel ? requestPanel.querySelector('[data-quest-input="name"]') : null;
        const descInput = requestPanel ? requestPanel.querySelector('[data-quest-input="desc"]') : null;
        const requiredInput = requestPanel ? requestPanel.querySelector('[data-quest-input="required"]') : null;
        const rewardCoinInput = requestPanel ? requestPanel.querySelector('[data-quest-input="rewardCoin"]') : null;
        const rewardRepInput = requestPanel ? requestPanel.querySelector('[data-quest-input="rewardRep"]') : null;
        const progressInput = requestPanel ? requestPanel.querySelector('[data-quest-input="progress"]') : null;
        const targetQuestName = toText(questActionBtn.getAttribute('data-quest-target'), '') || toText(nameInput && nameInput.value, '').trim();
        const actionData = buildQuestDispatchRequest(liveSnapshot, actionType, {
          questName: targetQuestName,
          questDesc: toText(descInput && descInput.value, '').trim(),
          requiredCount: toNumber(requiredInput && requiredInput.value, 1),
          rewardCoin: toNumber(rewardCoinInput && rewardCoinInput.value, 0),
          rewardRep: toNumber(rewardRepInput && rewardRepInput.value, 0),
          progressAdd: toNumber(progressInput && progressInput.value, 1)
        });
        if (!actionData) {
          if (window.MVU_Toast && typeof window.MVU_Toast.show === 'function') window.MVU_Toast.show('浠诲姟璇锋眰鍙傛暟涓嶅畬鏁达紝璇锋鏌ヤ换鍔″悕绉颁笌璇存槑銆?, 'error');
          else if (typeof window.alert === 'function') window.alert('浠诲姟璇锋眰鍙傛暟涓嶅畬鏁达紝璇锋鏌ヤ换鍔″悕绉颁笌璇存槑銆?);
          return;
        }
        dispatchUiAiRequest(actionData.playerInput, actionData.systemPrompt, {
          requestKind: actionData.requestKind,
          patchOps: actionData.patchOps
        });
        return;
      }

      const relationActionBtn = eventTarget ? eventTarget.closest('.relation-action-btn[data-relation-action][data-relation-target]') : null;
      if (relationActionBtn && detailSurfaceHost.contains(relationActionBtn)) {
        event.preventDefault();
        event.stopPropagation();
        if (!isSnapshotPlayerControlled(liveSnapshot)) {
          if (window.MVU_Toast && typeof window.MVU_Toast.show === 'function') window.MVU_Toast.show('鏃佽瑙嗚涓嶅彲鎿嶄綔銆?, 'error');
          else if (typeof window.alert === 'function') window.alert('鏃佽瑙嗚涓嶅彲鎿嶄綔銆?);
          return;
        }

        const actionType = relationActionBtn.getAttribute('data-relation-action') || '';
        const targetName = relationActionBtn.getAttribute('data-relation-target') || '';
        const resolvedTarget = resolveSnapshotCharacter(liveSnapshot, targetName);
        if (!resolvedTarget.char) {
          if (window.MVU_Toast && typeof window.MVU_Toast.show === 'function') window.MVU_Toast.show(`鎵句笉鍒板叧绯诲璞°€?{targetName}銆戙€俙, 'error');
          else if (typeof window.alert === 'function') window.alert(`鎵句笉鍒板叧绯诲璞°€?{targetName}銆戙€俙);
          return;
        }

        const currentLocFull = toText(deepGet(liveSnapshot, 'activeChar.鐘舵€?浣嶇疆', liveSnapshot.currentLoc || ''), liveSnapshot.currentLoc || '');
        const targetLoc = toText(deepGet(resolvedTarget.char, '鐘舵€?浣嶇疆', ''), '');
        const isSameLocation = isLocationCompatible(currentLocFull, targetLoc);
        const isContactable = deepGet(resolvedTarget.char, '鐘舵€?瀛樻椿', true) !== false;
        const relationMap = deepGet(liveSnapshot, 'activeChar.绀句氦.鍏崇郴', {});
        const relationData = relationMap && typeof relationMap === 'object' ? (relationMap[targetName] || relationMap[resolvedTarget.key] || {}) : {};
        const favor = toNumber(relationData && relationData['濂芥劅搴?], 0);
        const route = toText(relationData && relationData['鍏崇郴璺嚎'], '鏈嬪弸绾?);
        const routeSwitchable = !!deepGet(relationData, '_鍙垏绾?, false);

        if (!isContactable || !isSameLocation) {
          if (window.MVU_Toast && typeof window.MVU_Toast.show === 'function') window.MVU_Toast.show(`銆?{targetName}銆戝綋鍓嶄笉鍦ㄤ綘韬竟锛屾棤娉曡繘琛屽綋闈㈠叧绯讳簰鍔ㄣ€俙, 'error');
          else if (typeof window.alert === 'function') window.alert(`銆?{targetName}銆戝綋鍓嶄笉鍦ㄤ綘韬竟锛屾棤娉曡繘琛屽綋闈㈠叧绯讳簰鍔ㄣ€俙);
          return;
        }
        if (actionType === 'ask' && favor < 30) {
          if (window.MVU_Toast && typeof window.MVU_Toast.show === 'function') window.MVU_Toast.show(`鍚戙€?{targetName}銆戣鏁欓渶瑕佸ソ鎰熷害杈惧埌 30銆俙, 'error');
          else if (typeof window.alert === 'function') window.alert(`鍚戙€?{targetName}銆戣鏁欓渶瑕佸ソ鎰熷害杈惧埌 30銆俙);
          return;
        }
        if (actionType === 'confess' && !(route === '鎭嬩汉绾? || routeSwitchable || favor >= 80)) {
          if (window.MVU_Toast && typeof window.MVU_Toast.show === 'function') window.MVU_Toast.show(`銆?{targetName}銆戝綋鍓嶅皻鏈揪鍒伴€傚悎琛ㄧ櫧鐨勫叧绯婚樁娈点€俙, 'error');
          else if (typeof window.alert === 'function') window.alert(`銆?{targetName}銆戝綋鍓嶅皻鏈揪鍒伴€傚悎琛ㄧ櫧鐨勫叧绯婚樁娈点€俙);
          return;
        }
        if (actionType === 'dual' && !(route === '鎭嬩汉绾? && favor >= 80)) {
          if (window.MVU_Toast && typeof window.MVU_Toast.show === 'function') window.MVU_Toast.show('鍙屼慨浠呭湪楂樺ソ鎰熸亱浜虹嚎鍏崇郴涓嬪紑鏀俱€?, 'error');
          else if (typeof window.alert === 'function') window.alert('鍙屼慨浠呭湪楂樺ソ鎰熸亱浜虹嚎鍏崇郴涓嬪紑鏀俱€?);
          return;
        }

        let actionData = null;
        if (actionType === 'battle') {
          actionData = buildRelationBattleInitRequest(liveSnapshot, targetName);
        } else {
          let itemUsed = '鏃?;
          if (actionType === 'gift') {
            const actionPanel = relationActionBtn.closest('.relation-action-panel') || detailSurfaceHost;
            const giftSelect = actionPanel ? actionPanel.querySelector('.relation-gift-select') : null;
            itemUsed = toText(giftSelect && giftSelect.value, '');
            if (!itemUsed) {
              if (window.MVU_Toast && typeof window.MVU_Toast.show === 'function') window.MVU_Toast.show('璇峰厛閫夋嫨瑕佽禒閫佺殑鐗╁搧銆?, 'error');
              else if (typeof window.alert === 'function') window.alert('璇峰厛閫夋嫨瑕佽禒閫佺殑鐗╁搧銆?);
              return;
            }
          }
          actionData = buildRelationInteractRequest(liveSnapshot, actionType, targetName, { itemUsed });
        }

        if (actionData) dispatchUiAiRequest(actionData.playerInput, actionData.systemPrompt, {
          requestKind: actionData.requestKind,
          patchOps: actionData.patchOps
        });
        return;
      }

      const questBoardFocusBtn = eventTarget ? eventTarget.closest('[data-quest-board-focus]') : null;
      if (questBoardFocusBtn && detailSurfaceHost.contains(questBoardFocusBtn)) {
        event.preventDefault();
        event.stopPropagation();
        const boardId = questBoardFocusBtn.getAttribute('data-quest-board-focus') || '';
        if (boardId && detailPreviewKey) {
          modalFocusState[`${detailPreviewKey}::quest-board-focus`] = boardId;
          rerenderDetailSurface(detailPreviewKey, options);
        }
        return;
      }

      const questFocusBtn = eventTarget ? eventTarget.closest('[data-quest-focus]') : null;
      if (questFocusBtn && detailSurfaceHost.contains(questFocusBtn)) {
        event.preventDefault();
        event.stopPropagation();
        const questName = questFocusBtn.getAttribute('data-quest-focus') || '';
        if (questName && detailPreviewKey) {
          modalFocusState[`${detailPreviewKey}::quest-focus`] = questName;
          rerenderDetailSurface(detailPreviewKey, options);
        }
        return;
      }

      const factionActionBtn = eventTarget ? eventTarget.closest('.faction-action-btn[data-faction-action]') : null;
      if (factionActionBtn && detailSurfaceHost.contains(factionActionBtn)) {
        event.preventDefault();
        event.stopPropagation();
        if (!isSnapshotPlayerControlled(liveSnapshot)) {
          if (window.MVU_Toast && typeof window.MVU_Toast.show === 'function') window.MVU_Toast.show('鏃佽瑙嗚涓嶅彲鎿嶄綔銆?, 'error');
          else if (typeof window.alert === 'function') window.alert('鏃佽瑙嗚涓嶅彲鎿嶄綔銆?);
          return;
        }
        const factionAction = factionActionBtn.getAttribute('data-faction-action') || '';
        const requestPanel = factionActionBtn.closest('.request-console-card') || detailSurfaceHost;
        let actionData = null;
        if (factionAction === 'donate') {
          const factionInput = requestPanel ? requestPanel.querySelector('[data-faction-input="donateFaction"]') : null;
          const itemInput = requestPanel ? requestPanel.querySelector('[data-faction-input="donateItem"]') : null;
          const qtyInput = requestPanel ? requestPanel.querySelector('[data-faction-input="donateQty"]') : null;
          actionData = buildDonateDispatchRequest(liveSnapshot, {
            targetFaction: toText(factionInput && factionInput.value, ''),
            itemName: toText(itemInput && itemInput.value, ''),
            quantity: toNumber(qtyInput && qtyInput.value, 1)
          });
          if (!actionData) {
            if (window.MVU_Toast && typeof window.MVU_Toast.show === 'function') window.MVU_Toast.show('璇风‘璁ゆ崘鐚墿鍝併€佺洰鏍囧娍鍔涗笌鏁伴噺锛屽苟纭繚鑳屽寘搴撳瓨鍏呰冻銆?, 'error');
            else if (typeof window.alert === 'function') window.alert('璇风‘璁ゆ崘鐚墿鍝併€佺洰鏍囧娍鍔涗笌鏁伴噺锛屽苟纭繚鑳屽寘搴撳瓨鍏呰冻銆?);
            return;
          }
        }
        if (actionData) dispatchUiAiRequest(actionData.playerInput, actionData.systemPrompt, {
          requestKind: actionData.requestKind,
          patchOps: actionData.patchOps
        });
        return;
      }

      const previewClickable = eventTarget ? eventTarget.closest('.clickable[data-preview]') : null;
      if (previewClickable && detailSurfaceHost.contains(previewClickable)) {
        event.preventDefault();
        event.stopPropagation();
        const previewKey = previewClickable.dataset.preview;
        if (previewKey) openNestedDetailPreview(previewKey, options);
        return;
      }

      const clickedInsideDetailSurface = !!(eventTarget && detailSurfacePanel && detailSurfacePanel.contains(eventTarget));
      if (!clickedInsideDetailSurface && options.surface !== 'unified') popModalOrClose();
    }
    if (detailModal) detailModal.addEventListener('click', event => {
      handleDetailSurfaceClick(event, modalBody, { surface: 'modal', previewKey: currentModalPreviewKey, panel: modalPanel });
    });
    document.addEventListener('change', event => {
      const eventTarget = event.target instanceof Element ? event.target : null;
      const apiSelect = eventTarget ? eventTarget.closest('[data-ai-maintenance-api-select]') : null;
      if (!apiSelect) return;
      const presetName = 淇濆瓨AI缁存姢宸查€堿PI棰勮(apiSelect.value || '');
      showUiToast(presetName ? `AI缁存姢API锛?{presetName}` : 'AI缁存姢API锛氶粯璁PI', 'info', 1800);
    });

    function getFloatingHoverTrigger(eventTarget) {
      if (!eventTarget || typeof eventTarget.closest !== 'function') return null;
      const trigger = eventTarget.closest('.interactive-ring');
      if (!trigger) return null;
      if (!trigger.querySelector('.ring-hover-card, .relation-hover-card')) return null;
      return trigger;
    }

    let activeFloatingHoverTrigger = null;
    let 椤跺眰娴獥鍗＄墖 = null;
    let 娴獥绂诲尯澶嶆牳璁℃椂鍣?= 0;
    let 鏈€杩戞寚閽堝潗鏍?= null;

    function 绉婚櫎椤跺眰娴獥鍗＄墖() {
      if (椤跺眰娴獥鍗＄墖 && 椤跺眰娴獥鍗＄墖.parentNode) {
        椤跺眰娴獥鍗＄墖.parentNode.removeChild(椤跺眰娴獥鍗＄墖);
      }
      椤跺眰娴獥鍗＄墖 = null;
    }

    function 閲嶇疆鍘熸诞绐楀崱鐗?card) {
      if (!(card instanceof HTMLElement)) return;
      card.classList.remove('mvu-hover-card-floating');
      card.removeAttribute('aria-hidden');
      [
        'position',
        'left',
        'right',
        'top',
        'bottom',
        'width',
        'maxWidth',
        'maxHeight',
        'opacity',
        'visibility',
        'pointerEvents',
        'transform',
        'zIndex'
      ].forEach(prop => { card.style[prop] = ''; });
    }

    function clearFloatingHoverCard(trigger) {
      if (!trigger || !trigger.classList) return;
      trigger.classList.remove('mvu-hover-floating-active');
      const card = trigger.querySelector('.ring-hover-card, .relation-hover-card');
      if (card) 閲嶇疆鍘熸诞绐楀崱鐗?card);
      绉婚櫎椤跺眰娴獥鍗＄墖();
      if (activeFloatingHoverTrigger === trigger) activeFloatingHoverTrigger = null;
    }

    function clearAllFloatingHoverCards() {
      cancelFloatingHoverClearTimer();
      cancelFloatingHoverAutoHideTimer();
      鍙栨秷娴獥绂诲尯澶嶆牳璁℃椂鍣?);
      document.querySelectorAll('.interactive-ring.mvu-hover-floating-active').forEach(clearFloatingHoverCard);
      绉婚櫎椤跺眰娴獥鍗＄墖();
      activeFloatingHoverTrigger = null;
    }

    let floatingHoverClearTimer = 0;
    let floatingHoverAutoHideTimer = 0;
    const 娴獥鑷姩闅愯棌姣 = 0;
    const 椤跺眰娴獥灞傜骇 = '320';

    function cancelFloatingHoverClearTimer() {
      if (!floatingHoverClearTimer) return;
      window.clearTimeout(floatingHoverClearTimer);
      floatingHoverClearTimer = 0;
    }

    function cancelFloatingHoverAutoHideTimer() {
      if (!floatingHoverAutoHideTimer) return;
      window.clearTimeout(floatingHoverAutoHideTimer);
      floatingHoverAutoHideTimer = 0;
    }

    function 鍙栨秷娴獥绂诲尯澶嶆牳璁℃椂鍣?) {
      if (!娴獥绂诲尯澶嶆牳璁℃椂鍣? return;
      window.clearTimeout(娴獥绂诲尯澶嶆牳璁℃椂鍣?;
      娴獥绂诲尯澶嶆牳璁℃椂鍣?= 0;
    }

    function scheduleClearFloatingHoverCard(trigger) {
      cancelFloatingHoverClearTimer();
      floatingHoverClearTimer = window.setTimeout(() => {
        floatingHoverClearTimer = 0;
        clearFloatingHoverCard(trigger);
      }, 80);
    }

    function scheduleFloatingHoverAutoHide(trigger) {
      if (!娴獥鑷姩闅愯棌姣) return;
      cancelFloatingHoverAutoHideTimer();
      floatingHoverAutoHideTimer = window.setTimeout(() => {
        floatingHoverAutoHideTimer = 0;
        clearFloatingHoverCard(trigger);
      }, 娴獥鑷姩闅愯棌姣);
    }

    function isPointerStillWithinFloatingHover(trigger, event) {
      if (!(trigger instanceof Element) || !(event instanceof PointerEvent)) return false;
      const pointTarget = document.elementFromPoint(event.clientX, event.clientY);
      if (!(pointTarget instanceof Element)) return false;
      if (椤跺眰娴獥鍗＄墖 && 椤跺眰娴獥鍗＄墖.contains(pointTarget)) return true;
      return trigger.contains(pointTarget);
    }

    function 鍧愭爣鍦ㄧ煩褰㈠唴(x, y, rect, 浣欓噺 = 0) {
      if (!rect) return false;
      return x >= rect.left - 浣欓噺
        && x <= rect.right + 浣欓噺
        && y >= rect.top - 浣欓噺
        && y <= rect.bottom + 浣欓噺;
    }

    function 鎸囬拡浠嶅湪娴獥鍖哄煙(event) {
      if (!(event instanceof PointerEvent)) return false;
      const 瑙﹀彂鑺傜偣 = activeFloatingHoverTrigger;
      if (!(瑙﹀彂鑺傜偣 instanceof Element)) return false;
      const 瑙﹀彂鐭╁舰 = 瑙﹀彂鑺傜偣.getBoundingClientRect();
      const 娴獥鐭╁舰 = 椤跺眰娴獥鍗＄墖 instanceof HTMLElement ? 椤跺眰娴獥鍗＄墖.getBoundingClientRect() : null;
      return 鍧愭爣鍦ㄧ煩褰㈠唴(event.clientX, event.clientY, 瑙﹀彂鐭╁舰, 2)
        || 鍧愭爣鍦ㄧ煩褰㈠唴(event.clientX, event.clientY, 娴獥鐭╁舰, 2);
    }

    function 鎸囬拡鍛戒腑娴獥閾捐矾() {
      if (!鏈€杩戞寚閽堝潗鏍?|| !activeFloatingHoverTrigger) return false;
      const 鍛戒腑鑺傜偣 = document.elementFromPoint(鏈€杩戞寚閽堝潗鏍?x, 鏈€杩戞寚閽堝潗鏍?y);
      if (!(鍛戒腑鑺傜偣 instanceof Element)) return false;
      if (activeFloatingHoverTrigger.contains(鍛戒腑鑺傜偣)) return true;
      return !!(椤跺眰娴獥鍗＄墖 && 椤跺眰娴獥鍗＄墖.contains(鍛戒腑鑺傜偣));
    }

    function 瀹夋帓娴獥绂诲尯澶嶆牳(trigger) {
      鍙栨秷娴獥绂诲尯澶嶆牳璁℃椂鍣?);
      娴獥绂诲尯澶嶆牳璁℃椂鍣?= window.setTimeout(() => {
        娴獥绂诲尯澶嶆牳璁℃椂鍣?= 0;
        if (!trigger || activeFloatingHoverTrigger !== trigger) return;
        if (!鎸囬拡鍛戒腑娴獥閾捐矾()) clearFloatingHoverCard(trigger);
      }, 120);
    }

    function clampHoverValue(value, min, max) {
      if (!Number.isFinite(value)) return min;
      if (!Number.isFinite(max) || max < min) return min;
      return Math.min(Math.max(value, min), max);
    }

    function positionFloatingHoverCard(trigger) {
      if (!(trigger instanceof Element)) return;
      if (activeFloatingHoverTrigger && activeFloatingHoverTrigger !== trigger) {
        clearFloatingHoverCard(activeFloatingHoverTrigger);
      }
      const 鍘熷崱鐗?= trigger.querySelector('.ring-hover-card, .relation-hover-card');
      if (!(鍘熷崱鐗?instanceof HTMLElement)) return;
      const preferSidePlacement = 鍘熷崱鐗?classList.contains('ring-hover-card');
      const triggerRect = trigger.getBoundingClientRect();
      const viewportWidth = Math.max(document.documentElement.clientWidth || 0, window.innerWidth || 0);
      const viewportHeight = Math.max(document.documentElement.clientHeight || 0, window.innerHeight || 0);
      if (!viewportWidth || !viewportHeight) return;

      const margin = 12;
      const gap = 8;
      const maxWidth = Math.max(300, Math.min(preferSidePlacement ? 460 : 430, viewportWidth - margin * 2));
      const maxHeight = Math.max(260, Math.min(Math.floor(viewportHeight * 0.78), viewportHeight - margin * 2));

      trigger.classList.add('mvu-hover-floating-active');
      activeFloatingHoverTrigger = trigger;
      鍘熷崱鐗?setAttribute('aria-hidden', 'true');
      鍘熷崱鐗?style.opacity = '0';
      鍘熷崱鐗?style.visibility = 'hidden';
      鍘熷崱鐗?style.pointerEvents = 'none';

      绉婚櫎椤跺眰娴獥鍗＄墖();
      const 椤跺眰鍗＄墖 = 鍘熷崱鐗?cloneNode(true);
      if (!(椤跺眰鍗＄墖 instanceof HTMLElement)) return;
      椤跺眰鍗＄墖.removeAttribute('id');
      椤跺眰鍗＄墖.removeAttribute('aria-hidden');
      椤跺眰鍗＄墖.classList.add('mvu-hover-card-floating', 'mvu-hover-card-floating--portal');
      椤跺眰鍗＄墖.dataset.mvuHoverPortal = '1';
      椤跺眰鍗＄墖.style.position = 'fixed';
      椤跺眰鍗＄墖.style.right = 'auto';
      椤跺眰鍗＄墖.style.bottom = 'auto';
      椤跺眰鍗＄墖.style.transform = 'none';
      椤跺眰鍗＄墖.style.zIndex = 椤跺眰娴獥灞傜骇;
      椤跺眰鍗＄墖.style.width = `${maxWidth}px`;
      椤跺眰鍗＄墖.style.maxWidth = `${maxWidth}px`;
      椤跺眰鍗＄墖.style.maxHeight = `${maxHeight}px`;
      椤跺眰鍗＄墖.style.left = '0px';
      椤跺眰鍗＄墖.style.top = '0px';
      椤跺眰鍗＄墖.addEventListener('pointerenter', cancelFloatingHoverClearTimer);
      椤跺眰鍗＄墖.addEventListener('pointerleave', () => scheduleClearFloatingHoverCard(trigger));
      椤跺眰鍗＄墖.addEventListener('click', event => {
        const eventTarget = event.target instanceof Element ? event.target : null;
        const 棰勮鍏ュ彛 = eventTarget ? eventTarget.closest('.clickable[data-preview]') : null;
        if (!棰勮鍏ュ彛 || !椤跺眰鍗＄墖.contains(棰勮鍏ュ彛)) return;
        const 棰勮閿?= toText(棰勮鍏ュ彛.getAttribute('data-preview'), '').trim();
        if (!棰勮閿? return;
        event.preventDefault();
        event.stopPropagation();
        clearFloatingHoverCard(trigger);
        if (typeof window.__MVU_OPEN_UNIFIED_PREVIEW__ === 'function') {
          window.__MVU_OPEN_UNIFIED_PREVIEW__(棰勮閿? { preserveMapDispatchContext: true });
        } else {
          openModal(棰勮閿?;
        }
      });
      document.body.appendChild(椤跺眰鍗＄墖);
      椤跺眰娴獥鍗＄墖 = 椤跺眰鍗＄墖;

      const cardRect = 椤跺眰鍗＄墖.getBoundingClientRect();
      let width = Math.min(Math.ceil(cardRect.width || maxWidth), maxWidth);
      const height = Math.min(Math.ceil(cardRect.height || 260), maxHeight);
      let left = 0;
      let top = 0;
      const centeredTop = clampHoverValue(
        triggerRect.top + triggerRect.height / 2 - height / 2,
        margin,
        viewportHeight - height - margin
      );

      if (preferSidePlacement) {
        const minSideWidth = Math.min(340, maxWidth);
        const rightSpace = Math.max(0, viewportWidth - triggerRect.right - margin - gap);
        const leftSpace = Math.max(0, triggerRect.left - margin - gap);
        const canUseRight = rightSpace >= minSideWidth;
        const canUseLeft = leftSpace >= minSideWidth;

        if (canUseRight || canUseLeft) {
          const useRight = canUseRight && (!canUseLeft || rightSpace >= leftSpace);
          const sideSpace = useRight ? rightSpace : leftSpace;
          width = Math.min(width, sideSpace);
          left = useRight ? triggerRect.right + gap : triggerRect.left - width - gap;
          top = centeredTop;
        }
      }

      if (!(left || top)) {
        left = clampHoverValue(
          triggerRect.left + triggerRect.width / 2 - width / 2,
          margin,
          viewportWidth - width - margin
        );
        const belowTop = triggerRect.bottom + gap;
        const aboveTop = triggerRect.top - height - gap;
        top = belowTop + height <= viewportHeight - margin
          ? belowTop
          : (aboveTop >= margin ? aboveTop : clampHoverValue(belowTop, margin, viewportHeight - height - margin));
      }

      椤跺眰鍗＄墖.style.width = `${width}px`;
      椤跺眰鍗＄墖.style.left = `${Math.round(left)}px`;
      椤跺眰鍗＄墖.style.top = `${Math.round(top)}px`;
    }

    window.__MVU_CLEAR_FLOATING_HOVER__ = clearAllFloatingHoverCards;

    document.addEventListener('pointerover', event => {
      鏈€杩戞寚閽堝潗鏍?= { x: event.clientX, y: event.clientY };
      const eventTarget = event.target instanceof Element ? event.target : null;
      const trigger = getFloatingHoverTrigger(eventTarget);
      if (!trigger) {
        if (eventTarget && 椤跺眰娴獥鍗＄墖 && 椤跺眰娴獥鍗＄墖.contains(eventTarget)) {
          cancelFloatingHoverClearTimer();
          鍙栨秷娴獥绂诲尯澶嶆牳璁℃椂鍣?);
          return;
        }
        if (activeFloatingHoverTrigger) scheduleClearFloatingHoverCard(activeFloatingHoverTrigger);
        return;
      }
      cancelFloatingHoverClearTimer();
      鍙栨秷娴獥绂诲尯澶嶆牳璁℃椂鍣?);
      positionFloatingHoverCard(trigger);
      scheduleFloatingHoverAutoHide(trigger);
    }, true);

    document.addEventListener('pointerout', event => {
      鏈€杩戞寚閽堝潗鏍?= { x: event.clientX, y: event.clientY };
      const trigger = getFloatingHoverTrigger(event.target);
      if (!trigger) return;
      if (isPointerStillWithinFloatingHover(trigger, event)) return;
      scheduleClearFloatingHoverCard(trigger);
      瀹夋帓娴獥绂诲尯澶嶆牳(trigger);
    }, true);

    document.addEventListener('pointermove', event => {
      鏈€杩戞寚閽堝潗鏍?= { x: event.clientX, y: event.clientY };
      if (!activeFloatingHoverTrigger || !椤跺眰娴獥鍗＄墖) return;
      if (鎸囬拡浠嶅湪娴獥鍖哄煙(event)) {
        cancelFloatingHoverClearTimer();
        鍙栨秷娴獥绂诲尯澶嶆牳璁℃椂鍣?);
        return;
      }
      scheduleClearFloatingHoverCard(activeFloatingHoverTrigger);
      瀹夋帓娴獥绂诲尯澶嶆牳(activeFloatingHoverTrigger);
    }, true);

    document.addEventListener('focusin', event => {
      cancelFloatingHoverClearTimer();
      const trigger = getFloatingHoverTrigger(event.target);
      if (trigger) {
        positionFloatingHoverCard(trigger);
        scheduleFloatingHoverAutoHide(trigger);
      }
    }, true);

    document.addEventListener('focusout', event => {
      const trigger = getFloatingHoverTrigger(event.target);
      if (!trigger) return;
      const related = event.relatedTarget instanceof Node ? event.relatedTarget : null;
      if (related && trigger.contains(related)) return;
      if (related instanceof Element && related.closest('.ring-hover-card.mvu-hover-card-floating, .relation-hover-card.mvu-hover-card-floating')) return;
      scheduleClearFloatingHoverCard(trigger);
    }, true);

    window.addEventListener('resize', clearAllFloatingHoverCards);
    window.addEventListener('blur', clearAllFloatingHoverCards);
    window.addEventListener('pagehide', clearAllFloatingHoverCards);
    document.addEventListener('visibilitychange', () => {
      if (document.hidden) clearAllFloatingHoverCards();
    });
    document.documentElement.addEventListener('mouseleave', clearAllFloatingHoverCards, true);
    document.addEventListener('scroll', event => {
      const target = event.target instanceof Element ? event.target : null;
      if (target && target.closest('.ring-hover-card.mvu-hover-card-floating, .relation-hover-card.mvu-hover-card-floating')) return;
      clearAllFloatingHoverCards();
    }, true);

    document.addEventListener('pointerdown', event => {
      const target = event.target instanceof Element ? event.target : null;
      if (target && target.closest('.ring-hover-card.mvu-hover-card-floating, .relation-hover-card.mvu-hover-card-floating, .interactive-ring')) return;
      clearAllFloatingHoverCards();
    }, true);

let activeLongPressState = null;
    let suppressedLongPressClickState = null;

    function clearActiveLongPressState() {
      if (!activeLongPressState) return;
      if (activeLongPressState.timer) clearTimeout(activeLongPressState.timer);
      if (activeLongPressState.target && activeLongPressState.target.classList) {
        activeLongPressState.target.classList.remove('active-press');
      }
      activeLongPressState = null;
    }

    function getLongPressTarget(eventTarget) {
      return eventTarget && eventTarget.closest ? eventTarget.closest('[data-longpress]') : null;
    }

    function getLongPressDelay(target) {
      const rawDelay = target ? Number(target.getAttribute('data-longpress-delay')) : NaN;
      return Number.isFinite(rawDelay) && rawDelay >= 250 ? rawDelay : 800;
    }

    function openLongPressPreview(previewKey, target) {
      const shellBridge = getMobileShellBridge();
      const targetInShell = !!(target && typeof target.closest === 'function' && target.closest('.mvu-mobile-shell'));
      if (targetInShell && shellBridge && typeof shellBridge.openPreview === 'function') {
        shellBridge.openPreview(previewKey);
        return;
      }
      const targetInUnified = !!(target && typeof target.closest === 'function' && target.closest('#mvu-unified-mount'));
      if (targetInUnified && typeof window.__MVU_OPEN_UNIFIED_PREVIEW__ === 'function') {
        window.__MVU_OPEN_UNIFIED_PREVIEW__(previewKey, { preserveMapDispatchContext: true });
        return;
      }
      openModal(previewKey);
    }

    function canTriggerLongPressPreview(previewKey) {
      if (!previewKey) return false;
      return previewKey === PRIVATE_ARCHIVE_PREVIEW_KEY && canOpenPrivateArchive(liveSnapshot);
    }

    function markSuppressedLongPressClick(target) {
      suppressedLongPressClickState = {
        target,
        until: Date.now() + 450
      };
    }

    function shouldSuppressLongPressClick(eventTarget) {
      if (!suppressedLongPressClickState) return false;
      if (Date.now() > suppressedLongPressClickState.until) {
        suppressedLongPressClickState = null;
        return false;
      }
      return !!(eventTarget && suppressedLongPressClickState.target && (
        eventTarget === suppressedLongPressClickState.target
        || suppressedLongPressClickState.target.contains(eventTarget)
        || eventTarget.contains(suppressedLongPressClickState.target)
      ));
    }

    window.__MVU_CONSUME_LONG_PRESS_CLICK__ = eventTarget => {
      if (!shouldSuppressLongPressClick(eventTarget)) return false;
      suppressedLongPressClickState = null;
      return true;
    };

    function getLongPressScrollContainer(target) {
      let current = target instanceof Element ? target : null;
      while (current && current !== document.body) {
        try {
          const style = window.getComputedStyle(current);
          const overflowY = style ? style.overflowY : '';
          if ((overflowY === 'auto' || overflowY === 'scroll') && current.scrollHeight > current.clientHeight + 2) {
            return current;
          }
        } catch (err) {}
        current = current.parentElement;
      }
      return document.scrollingElement || document.documentElement || document.body;
    }

    const handleLongPressStart = (event) => {
      const target = getLongPressTarget(event.target);
      if (!target) return;
      if (typeof event.button === 'number' && event.button !== 0) return;
      const previewKey = target.getAttribute('data-longpress');
      if (!canTriggerLongPressPreview(previewKey)) return;
      clearActiveLongPressState();
      target.classList.add('active-press');
      const delay = getLongPressDelay(target);
      const scrollContainer = getLongPressScrollContainer(target);
      activeLongPressState = {
        pointerId: event.pointerId,
        target,
        startX: Number.isFinite(event.clientX) ? event.clientX : 0,
        startY: Number.isFinite(event.clientY) ? event.clientY : 0,
        scrollContainer,
        scrollTop: scrollContainer && typeof scrollContainer.scrollTop === 'number' ? scrollContainer.scrollTop : 0,
        scrollLeft: scrollContainer && typeof scrollContainer.scrollLeft === 'number' ? scrollContainer.scrollLeft : 0,
        timer: setTimeout(() => {
          target.classList.remove('active-press');
          activeLongPressState = null;
          if (!previewKey) return;
          if (!canTriggerLongPressPreview(previewKey)) return;
          markSuppressedLongPressClick(target);
          if (navigator.vibrate) navigator.vibrate(50);
          openLongPressPreview(previewKey, target);
        }, delay)
      };
    };

    const handleLongPressEnd = (event) => {
      if (!activeLongPressState) return;
      if (typeof event.pointerId === 'number' && typeof activeLongPressState.pointerId === 'number' && event.pointerId !== activeLongPressState.pointerId) {
        return;
      }
      clearActiveLongPressState();
    };

    const handleLongPressMove = (event) => {
      if (!activeLongPressState) return;
      if (typeof event.pointerId === 'number' && typeof activeLongPressState.pointerId === 'number' && event.pointerId !== activeLongPressState.pointerId) {
        return;
      }
      const moveX = Math.abs((Number.isFinite(event.clientX) ? event.clientX : 0) - activeLongPressState.startX);
      const moveY = Math.abs((Number.isFinite(event.clientY) ? event.clientY : 0) - activeLongPressState.startY);
      if (moveX > 10 || moveY > 10) {
        clearActiveLongPressState();
        return;
      }
      if (activeLongPressState.scrollContainer) {
        const currentTop = typeof activeLongPressState.scrollContainer.scrollTop === 'number' ? activeLongPressState.scrollContainer.scrollTop : 0;
        const currentLeft = typeof activeLongPressState.scrollContainer.scrollLeft === 'number' ? activeLongPressState.scrollContainer.scrollLeft : 0;
        if (Math.abs(currentTop - activeLongPressState.scrollTop) > 2 || Math.abs(currentLeft - activeLongPressState.scrollLeft) > 2) {
          clearActiveLongPressState();
          return;
        }
      }
      const hoverEl = document.elementFromPoint(event.clientX, event.clientY);
      if (!hoverEl || !activeLongPressState.target.contains(hoverEl)) {
        clearActiveLongPressState();
      }
    };

    document.addEventListener('pointerdown', handleLongPressStart);
    document.addEventListener('pointerup', handleLongPressEnd);
    document.addEventListener('pointercancel', handleLongPressEnd);
    document.addEventListener('pointermove', handleLongPressMove, { passive: true });
    document.addEventListener('scroll', clearActiveLongPressState, true);
    document.addEventListener('mouseleave', clearActiveLongPressState);
    document.addEventListener('contextmenu', (event) => {
      const target = getLongPressTarget(event.target);
      if (target) event.preventDefault();
    });
    document.addEventListener('click', (event) => {
      const eventTarget = event.target instanceof Element ? event.target : (event.target && event.target.parentElement ? event.target.parentElement : null);
      if (!window.__MVU_CONSUME_LONG_PRESS_CLICK__(eventTarget)) return;
      event.preventDefault();
      event.stopPropagation();
      event.stopImmediatePropagation();
    }, true);

// =========================================================================
// 鍏ㄥ眬閫氱煡/Toast (鍙栦唬娴忚鍣ㄥ師鐢熺殑 alert)
// =========================================================================
window.MVU_Toast = {
  show(msg, type = 'info', duration = 3500) {
    let container = document.getElementById('mvu-toast-container');
    if (!container) {
      container = document.createElement('div');
      container.id = 'mvu-toast-container';
      container.className = 'mvu-toast-container';
      document.body.appendChild(container);
    }
    
    const toast = document.createElement('div');
    toast.className = `mvu-toast ${type === 'error' ? 'error' : ''}`;
    toast.innerHTML = htmlEscape(msg).replace(/\n/g, '<br/>');

    const closeBtn = document.createElement('div');
    closeBtn.className = 'mvu-toast-close';
    closeBtn.innerHTML = '&times;';
    closeBtn.onclick = () => {
      closeToast();
    };
    toast.appendChild(closeBtn);
    
    container.appendChild(toast);

    let hideTimeout;
    const closeToast = () => {
      if (hideTimeout) clearTimeout(hideTimeout);
      toast.style.transition = 'opacity 0.3s ease, transform 0.3s ease';
      toast.style.opacity = '0';
      toast.style.transform = 'translateY(-10px) scale(0.95)';
      setTimeout(() => {
        if (toast.parentNode) toast.parentNode.removeChild(toast);
      }, 300);
    };

    if (duration > 0) hideTimeout = setTimeout(closeToast, duration);
  }
};

// =========================================================================
// 瑁呭涓庢崲瑁呯鐞嗗櫒 (Equipment Manager)
// 璐熻矗鍦ㄥ墠绔洿鎺ュ鐞嗙墿鍝佹爮涓庤澶囨爮鐨勫璋冦€佹贩绌跨害鏉熼獙璇侊紝浠ュ強鐢熸垚 JSON Patch
// =========================================================================
window.EquipmentManager = {
  async resolveCharContext(charRef) {
    const vars = cloneJsonValue(await ensureMvuEditorStoreReady(), {});
    if (!vars || !vars.char || typeof vars.char !== 'object') {
      throw new Error('鑾峰彇瑙掕壊鏁版嵁澶辫触銆?);
    }
    const charEntries = Object.entries(vars.char);
    let charKey = '';
    if (typeof charRef === 'string' && vars.char[charRef]) {
      charKey = charRef;
    } else if (typeof charRef === 'number' && charEntries[charRef]) {
      charKey = charEntries[charRef][0];
    } else if (typeof charRef === 'string') {
      const matched = charEntries.find(([key, value]) => {
        const displayName = toText(value && (value.name || deepGet(value, 'base.name', '')), key);
        return displayName === charRef;
      });
      charKey = matched ? matched[0] : '';
    }
    if (!charKey) {
      charKey = charEntries[0] ? charEntries[0][0] : '';
    }
    if (!charKey || !vars.char[charKey]) {
      throw new Error('鏈壘鍒扮洰鏍囪鑹蹭俊鎭€?);
    }
    return { vars, charKey, activeChar: vars.char[charKey] || {} };
  },

  buildInventoryItemValue(itemName, itemData = {}, options = {}) {
    const safeItem = cloneJsonValue(itemData, {});
    delete safeItem.瑁呭鐘舵€?
    if (safeItem.name === itemName) delete safeItem.name;
    if (safeItem['鍚嶇О'] === itemName) delete safeItem['鍚嶇О'];
    safeItem.鏁伴噺 = Math.max(1, toNumber(options.quantity, 1));
    if (!safeItem['绫诲瀷'] && options.type) safeItem['绫诲瀷'] = options.type;
    if (!safeItem['鍝佽川'] && options.quality) safeItem['鍝佽川'] = options.quality;
    return safeItem;
  },

  queueInventoryAdd(patches, charPath, inventoryBuffer, itemName, itemValue) {
    if (!itemName) return;
    const nextValue = this.buildInventoryItemValue(itemName, itemValue, { quantity: 1 });
    const existing = inventoryBuffer[itemName];
    if (existing) {
      const nextQty = Math.max(1, toNumber(existing.鏁伴噺, 1) + toNumber(nextValue.鏁伴噺, 1));
      inventoryBuffer[itemName] = { ...existing, ...nextValue, 鏁伴噺: nextQty };
      patches.push({ op: 'replace', path: `${charPath}/鑳屽寘/${this.escapePtr(itemName)}`, value: cloneJsonValue(inventoryBuffer[itemName], {}) });
      return;
    }
    inventoryBuffer[itemName] = cloneJsonValue(nextValue, {});
    patches.push({ op: 'add', path: `${charPath}/鑳屽寘/${this.escapePtr(itemName)}`, value: cloneJsonValue(inventoryBuffer[itemName], {}) });
  },

  queueInventoryConsume(patches, charPath, inventoryBuffer, itemName, quantity = 1) {
    const existing = inventoryBuffer[itemName];
    if (!existing) return false;
    const nextQty = Math.max(0, toNumber(existing.鏁伴噺, 1) - Math.max(1, toNumber(quantity, 1)));
    if (nextQty <= 0) {
      delete inventoryBuffer[itemName];
      patches.push({ op: 'remove', path: `${charPath}/鑳屽寘/${this.escapePtr(itemName)}` });
      return true;
    }
    inventoryBuffer[itemName] = { ...existing, 鏁伴噺: nextQty };
    patches.push({ op: 'replace', path: `${charPath}/鑳屽寘/${this.escapePtr(itemName)}`, value: cloneJsonValue(inventoryBuffer[itemName], {}) });
    return true;
  },

  async performEquip(charRef, itemName) {
    try {
      const { charKey, activeChar } = await this.resolveCharContext(charRef);
      const inventory = activeChar.鑳屽寘 || {};
      const 瑁呭鏁版嵁 = activeChar.瑁呭 || {};
      const soulBone = activeChar.榄傞 || {};
      const itemData = inventory[itemName];
      if (!itemData || (itemData.鏁伴噺 || 0) <= 0) {
        throw new Error(`鑳屽寘涓湭鎵惧埌鐗╁搧: ${itemName}`);
      }

      const slotInfo = this.parseEquipSlot(itemName, itemData);
      if (!slotInfo) {
        throw new Error(`銆?{itemName}銆戜笉鍙澶囷紒`);
      }

      if (slotInfo.mainSlot === 'armor') {
        const newTier = this.getArmorTier(itemName);
        if (newTier) {
          const armorParts = (瑁呭鏁版嵁.鏂楅摖 && 瑁呭鏁版嵁.鏂楅摖.閮ㄤ欢) ? 瑁呭鏁版嵁.鏂楅摖.閮ㄤ欢 : {};
          for (const [key, piece] of Object.entries(armorParts)) {
            if (piece && typeof piece === 'object' && piece['鍚嶇О'] && piece['鍚嶇О'] !== '鏃? && key !== slotInfo.subSlot) {
              const pieceTier = this.getArmorTier(piece['鍚嶇О']);
              if (pieceTier && pieceTier !== newTier) {
                throw new Error(`銆愭崲瑁呭け璐ャ€慭n鑳屽寘涓殑銆?{newTier}銆戜笉鍙笌韬笂鐨勩€?{pieceTier}銆戞贩绌匡紒\n蹇呴』鎴愬鏇存崲鐩稿悓绾у埆鐨勬枟閾犮€俙);
              }
            }
          }
        }
      }

      if (slotInfo.mainSlot === 'soul_bone') {
        const existingBone = soulBone[slotInfo.subSlot];
        if (existingBone && typeof existingBone === 'object' && existingBone['鍚嶇О'] && !existingBone['鍚嶇О'].includes('鏈壌瀹氫箣')) {
          throw new Error(`銆愯杞藉け璐ャ€慭n${slotInfo.subSlot} 宸茶瀺鍚堜簡銆?{existingBone['鍚嶇О']}銆戯紒\n榄傞涓€缁忚瀺鍚堟棤娉曠洿鎺ユ浛鎹紝闄ら潪寮鸿鍓ョ锛乣);
        }
      }

      const patches = [];
      const charPath = `/char/${this.escapePtr(charKey)}`;
      const inventoryBuffer = cloneJsonValue(inventory, {});
      this.queueInventoryConsume(patches, charPath, inventoryBuffer, itemName, 1);

      if (slotInfo.mainSlot === 'soul_bone') {
        const newBoneData = {
          鍚嶇О: itemName,
          骞撮檺: itemData['骞撮檺'] || (itemData['鍝佽川'] && itemData['鍝佽川'].includes('涓?) ? 10000 : 1000),
          鍝佽川: itemData['鍝佽川'] || '甯歌',
          鐘舵€? '宸茶杞?
        };
        patches.push({ op: 'add', path: `${charPath}/榄傞/${slotInfo.subSlot}`, value: newBoneData });
      } else {
        let oldItem = null;
        const equipKeyMap = { armor: '鏂楅摖', mech: '鏈虹敳', wpn: '姝﹀櫒' };
        const equipKey = equipKeyMap[slotInfo.mainSlot] || slotInfo.mainSlot;
        let equipPath = `${charPath}/瑁呭/${equipKey}`;
        if (slotInfo.subSlot) {
          equipPath += `/閮ㄤ欢/${slotInfo.subSlot}`;
          oldItem = equip[equipKey]?.閮ㄤ欢?.[slotInfo.subSlot];
        } else {
          oldItem = equip[equipKey];
        }

        if (oldItem && typeof oldItem === 'object' && oldItem['鍚嶇О'] && toText(oldItem['鍚嶇О'], '鏃?) !== '鏃?) {
          const oldName = toText(oldItem['鍚嶇О'], '');
          this.queueInventoryAdd(patches, charPath, inventoryBuffer, oldName, oldItem);
        }

        const newEquipData = Object.assign({}, itemData, { 鍚嶇О: itemName });
        delete newEquipData.name;
        delete newEquipData.鏁伴噺;
        if (slotInfo.subSlot && (!equip[equipKey] || !equip[equipKey].閮ㄤ欢)) {
          patches.push({ op: 'add', path: `${charPath}/瑁呭/${equipKey}/閮ㄤ欢`, value: {} });
        }
        patches.push({ op: oldItem !== undefined ? 'replace' : 'add', path: equipPath, value: newEquipData });
      }

      await this.submitPatch(patches, `宸茬┛鎴?${itemName}`);
    } catch (error) {
      window.MVU_Toast.show(error && error.message ? error.message : '鎹㈣澶辫触銆?, 'error');
    }
  },

  async performUnequip(charRef, targetType, targetName = '') {
    try {
      const { charKey, activeChar } = await this.resolveCharContext(charRef);
      const 瑁呭鏁版嵁 = activeChar.瑁呭 || {};
      const charPath = `/char/${this.escapePtr(charKey)}`;
      const inventoryBuffer = cloneJsonValue(activeChar.鑳屽寘 || {}, {});
      const patches = [];

      if (targetType === 'armor') {
        const armor = 瑁呭鏁版嵁.鏂楅摖 || {};
        const armorParts = armor.閮ㄤ欢 && typeof armor.閮ㄤ欢 === 'object' ? armor.閮ㄤ欢 : {};
        const returnParts = Object.values(armorParts).filter(part => part && typeof part === 'object' && toText(part['鍚嶇О'], '') && toText(part['鍚嶇О'], '鏃?) !== '鏃?);
        if (!returnParts.length && toText(armor.瑁呭鐘舵€? '鏈澶?) === '鏈澶?) {
          throw new Error('褰撳墠娌℃湁鍙嵏涓嬬殑鏂楅摖閮ㄤ欢銆?);
        }
        returnParts.forEach(part => {
          const itemName = toText(part['鍚嶇О'], '');
          this.queueInventoryAdd(patches, charPath, inventoryBuffer, itemName, part);
        });
        patches.push({ op: 'replace', path: `${charPath}/瑁呭/鏂楅摖/閮ㄤ欢`, value: {} });
        patches.push({ op: 'replace', path: `${charPath}/瑁呭/鏂楅摖/瑁呭鐘舵€乣, value: '鏈澶? });
        patches.push({ op: 'replace', path: `${charPath}/瑁呭/鏂楅摖/_灞炴€у姞鎴恅, value: {} });
        await this.submitPatch(patches, '宸插嵏涓嬫枟閾?);
        return;
      }

      if (targetType === 'mech') {
        const mech = 瑁呭鏁版嵁.鏈虹敳 || {};
        const mechName = toText(mech.鍨嬪彿, '');
        if (!mechName) throw new Error('褰撳墠娌℃湁宸茶杞界殑鏈虹敳銆?);
        this.queueInventoryAdd(patches, charPath, inventoryBuffer, mechName, mech);
        patches.push({ op: 'replace', path: `${charPath}/瑁呭/鏈虹敳`, value: {} });
        await this.submitPatch(patches, `宸插嵏涓嬫満鐢?${mechName}`);
        return;
      }

      if (targetType === 'wpn') {
        const weapon = 瑁呭鏁版嵁.姝﹀櫒 || {};
        const weaponName = toText(weapon.鍚嶇О || weapon['鍚嶇О'], '');
        if (!weaponName) throw new Error('褰撳墠娌℃湁宸茶澶囩殑涓绘鍣ㄣ€?);
        this.queueInventoryAdd(patches, charPath, inventoryBuffer, weaponName, weapon);
        patches.push({ op: 'replace', path: `${charPath}/瑁呭/姝﹀櫒`, value: {} });
        await this.submitPatch(patches, `宸插嵏涓嬩富姝﹀櫒 ${weaponName}`);
        return;
      }

      throw new Error('鏆備笉鏀寔璇ヨ澶囩被鍨嬬殑鎷嗗嵏銆?);
    } catch (error) {
      window.MVU_Toast.show(error && error.message ? error.message : '鎷嗗嵏澶辫触銆?, 'error');
    }
  },

  async performDiscard(charRef, itemName, mode = 'one') {
    try {
      const { charKey, activeChar } = await this.resolveCharContext(charRef);
      const inventory = activeChar.鑳屽寘 || {};
      const itemData = inventory[itemName];
      if (!itemData) throw new Error(`鑳屽寘涓湭鎵惧埌鐗╁搧: ${itemName}`);
      const currentQty = Math.max(1, toNumber(itemData.鏁伴噺, 1));
      const discardQty = mode === 'all' ? currentQty : 1;
      const charPath = `/char/${this.escapePtr(charKey)}`;
      const inventoryBuffer = cloneJsonValue(inventory, {});
      const patches = [];
      this.queueInventoryConsume(patches, charPath, inventoryBuffer, itemName, discardQty);
      await this.submitPatch(patches, mode === 'all' ? `宸蹭涪寮?${itemName} 鍏ㄩ儴搴撳瓨` : `宸蹭涪寮?${itemName} 脳1`);
    } catch (error) {
      window.MVU_Toast.show(error && error.message ? error.message : '涓㈠純澶辫触銆?, 'error');
    }
  },

  isUsableInventoryItem(itemData = {}) {
    return Array.isArray(itemData?.浣跨敤鏁堟灉) && itemData.浣跨敤鏁堟灉.length > 0;
  },

  getInventoryUseActionLabel(itemData = {}) {
    return /椋熺墿/.test(String(itemData?.绫诲瀷 || '')) || /椋熺敤/.test(String(itemData?.瑙﹀彂鏂瑰紡 || '')) ? '椋熺敤' : '浣跨敤';
  },

  isSelfResolvableUsageTarget(target = '') {
    const text = String(target || '').trim();
    if (!text) return true;
    return /鑷韩|椋熺敤鑰厊浣跨敤鑰厊鍙嬫柟鍗曚綋|鍙嬫柟缇や綋/.test(text);
  },

  mapUsagePropertyKey(property = '') {
    const key = String(property || '').trim();
    const map = {
      vit: { current: '浣撳姏', max: '浣撳姏涓婇檺', statMod: 'vit_max', label: '浣撳姏' },
      sp: { current: '榄傚姏', max: '榄傚姏涓婇檺', statMod: 'sp_max', label: '榄傚姏' },
      men: { current: '绮剧鍔?, max: '绮剧鍔涗笂闄?, statMod: 'men_max', label: '绮剧鍔? },
      str: { current: '鍔涢噺', max: '', statMod: 'str', label: '鍔涢噺' },
      def: { current: '闃插尽', max: '', statMod: 'def', label: '闃插尽' },
      agi: { current: '鏁忔嵎', max: '', statMod: 'agi', label: '鏁忔嵎' },
      '浣撳姏': { current: '浣撳姏', max: '浣撳姏涓婇檺', statMod: 'vit_max', label: '浣撳姏' },
      '榄傚姏': { current: '榄傚姏', max: '榄傚姏涓婇檺', statMod: 'sp_max', label: '榄傚姏' },
      '绮剧鍔?: { current: '绮剧鍔?, max: '绮剧鍔涗笂闄?, statMod: 'men_max', label: '绮剧鍔? },
      '鍔涢噺': { current: '鍔涢噺', max: '', statMod: 'str', label: '鍔涢噺' },
      '闃插尽': { current: '闃插尽', max: '', statMod: 'def', label: '闃插尽' },
      '鏁忔嵎': { current: '鏁忔嵎', max: '', statMod: 'agi', label: '鏁忔嵎' },
      '浣撳姏涓婇檺': { current: '浣撳姏', max: '浣撳姏涓婇檺', statMod: 'vit_max', label: '浣撳姏涓婇檺' },
      '榄傚姏涓婇檺': { current: '榄傚姏', max: '榄傚姏涓婇檺', statMod: 'sp_max', label: '榄傚姏涓婇檺' },
      '绮剧鍔涗笂闄?: { current: '绮剧鍔?, max: '绮剧鍔涗笂闄?, statMod: 'men_max', label: '绮剧鍔涗笂闄? },
      '閫熷害': { current: '鏁忔嵎', max: '', statMod: 'agi', label: '鏁忔嵎' },
    };
    return map[key] || null;
  },

    createInventoryStatusRecord(itemName, effect = {}, statusName = '', 褰撳墠tick = 0) {
      const value = effect && typeof effect === 'object' ? effect.value || {} : {};
      const 淇偧鍊嶇巼 = Number(value.淇偧閫熷害鍊嶇巼 || 0);
      if (Number.isFinite(淇偧鍊嶇巼) && 淇偧鍊嶇巼 > 1) {
        const 鐘舵€佽褰?= {
          淇偧閫熷害鍊嶇巼: Number(淇偧鍊嶇巼.toFixed(4)),
        };
        const 鏄惧紡缁撴潫tick = Math.max(0, toNumber(value.缁撴潫tick, 0));
        if (鏄惧紡缁撴潫tick > 0) {
          鐘舵€佽褰?缁撴潫tick = 鏄惧紡缁撴潫tick;
          return 鐘舵€佽褰?
        }
        const 鎸佺画tick = Math.max(0, toNumber(value.鎸佺画tick, 0));
        const 鎸佺画鍥炲悎 = Math.max(0, toNumber(value.鎸佺画, toNumber(value.durationRounds, 0)));
        const 鎺ㄥ鎸佺画tick = 鎸佺画tick > 0 ? 鎸佺画tick : Math.max(0, Math.round(鎸佺画鍥炲悎 * 6));
        if (鎺ㄥ鎸佺画tick > 0 && 褰撳墠tick >= 0) 鐘舵€佽褰?缁撴潫tick = 褰撳墠tick + 鎺ㄥ鎸佺画tick;
        return 鐘舵€佽褰?
      }
      const duration = Math.max(1, toNumber(value.鎸佺画, toNumber(value.durationRounds, 1)));
      const 鐘舵€佽褰?= {
        绫诲瀷: effect?.type === 'debuff' ? 'debuff' : 'buff',
        灞傛暟: 1,
        鎻忚堪: toText(effect?.description, `${itemName}瑙﹀彂鏁堟灉`),
        鎸佺画鍥炲悎: duration,
      };
      const 鎸佺画tick = Math.max(0, toNumber(value.鎸佺画tick, 0));
      if (鎸佺画tick > 0) 鐘舵€佽褰?鎸佺画tick = 鎸佺画tick;
      const 缁撴潫tick = Math.max(0, toNumber(value.缁撴潫tick, 0));
      if (缁撴潫tick > 0) 鐘舵€佽褰?缁撴潫tick = 缁撴潫tick;
      else if (鎸佺画tick > 0 && 褰撳墠tick >= 0) 鐘舵€佽褰?缁撴潫tick = 褰撳墠tick + 鎸佺画tick;
      return 鐘舵€佽褰?
  },

  applyInventoryHealEffect(stat = {}, effect = {}, logs = []) {
    const value = effect && typeof effect === 'object' ? effect.value || {} : {};
    const propertyMeta = this.mapUsagePropertyKey(value.灞炴€?;
    if (!propertyMeta || !propertyMeta.current) return false;
    const currentKey = propertyMeta.current;
    const maxKey = propertyMeta.max || currentKey;
    const maxValue = Math.max(1, toNumber(stat[maxKey], toNumber(stat[currentKey], 1)));
    const rawValue = Number(value.鏁板€?|| 0);
    if (!(rawValue > 0)) return false;
    const delta = rawValue <= 1 ? Math.max(1, Math.round(maxValue * rawValue)) : Math.round(rawValue);
    const currentValue = Math.max(0, toNumber(stat[currentKey], 0));
    stat[currentKey] = Math.min(maxValue, currentValue + delta);
    logs.push(`${propertyMeta.label}+${formatNumber(delta)}`);
    return true;
  },

  appendInventoryStatusEffect(statusMap = {}, itemName, effect = {}, index = 0, logs = [], 褰撳墠tick = 0) {
    const key = `${itemName}#${index + 1}`;
    const value = effect && typeof effect === 'object' ? effect.value || {} : {};
    const record = this.createInventoryStatusRecord(itemName, effect, key, 褰撳墠tick);
    if (value && value.statMods && typeof value.statMods === 'object') {
      record.stat_mods = cloneJsonValue(value.statMods, {});
    } else {
      const propertyMeta = this.mapUsagePropertyKey(value.灞炴€?;
      const numericValue = Number(value.鏁板€?|| 0);
      const action = String(value.鍔ㄤ綔 || '').trim();
      if (propertyMeta && propertyMeta.statMod && Number.isFinite(numericValue) && numericValue > 0) {
        if (/鍊嶇巼鍘嬪埗|娑堣€楁彁楂榺鍓嶆憞鎷夐暱/.test(action)) {
          record.stat_mods = { [propertyMeta.statMod]: Math.max(0.1, Number((1 - numericValue).toFixed(4))) };
        } else if (/鍊嶇巼鎻愬崌|娑堣€楅檷浣巪鍓嶆憞缂╃煭|鍔犻€焲鍔犲€?.test(action)) {
          record.stat_mods = { [propertyMeta.statMod]: Number((1 + numericValue).toFixed(4)) };
        }
      }
    }
    if (value && value.combatEffects && typeof value.combatEffects === 'object') {
      record.鎴樻枟鏁堟灉 = cloneJsonValue(value.combatEffects, {});
    }
    if (effect?.type === 'shield') {
      record.鎶ょ浘鍊?= Math.max(0, toNumber(value, 0));
    }
    const cleanseLevel = Math.max(0, toNumber(value && value.cleanseLevel, 0));
    if (cleanseLevel > 0) {
      const debuffKeys = Object.keys(statusMap).filter(name => String(statusMap[name]?.绫诲瀷 || '') === 'debuff').slice(0, cleanseLevel);
      debuffKeys.forEach(name => delete statusMap[name]);
      if (debuffKeys.length) logs.push(`娓呴櫎${debuffKeys.length}椤瑰噺鐩奰);
    }
    statusMap[key] = record;
    logs.push(toText(effect?.description, key));
    return true;
  },

  getSkillSystemBase(skill = {}) {
    const effectArray = Array.isArray(skill?._鏁堟灉鏁扮粍) ? skill._鏁堟灉鏁扮粍 : [];
    return effectArray.find(effect => toText(effect && effect['鏈哄埗'], '').trim() === '绯荤粺鍩虹') || {};
  },

  getConstructCreateEffects(skill = {}) {
    return (Array.isArray(skill?._鏁堟灉鏁扮粍) ? skill._鏁堟灉鏁扮粍 : []).filter(effect =>
      ['鐢熸垚閫犵墿', '閫犵墿鐢熸垚'].includes(toText(effect && effect['鏈哄埗'], '').trim())
    );
  },

  splitSkillCostModes(costText = '鏃?) {
    const raw = String(costText || '鏃?).trim();
    if (!raw || raw === '鏃?) return { upfront: '鏃?, sustain: '', hasSustain: false };
    const parts = raw.split(/缁存寔[:锛歖?/);
    return {
      upfront: (parts[0] || '鏃?).trim() || '鏃?,
      sustain: (parts[1] || '').trim(),
      hasSustain: parts.length > 1,
    };
  },

  parseDailyResourceCostUnit(rawValue, currentValue, maxValue) {
    const text = String(rawValue || '').trim();
    if (!text || text === '鏃?) return 0;
    const currentPercentMatch = text.match(/褰撳墠\s*(\d+(?:\.\d+)?)%/);
    if (currentPercentMatch) {
      return Math.max(1, Math.floor(Math.max(0, Number(currentValue || 0)) * Number(currentPercentMatch[1]) / 100));
    }
    const percentMatch = text.match(/(\d+(?:\.\d+)?)%/);
    if (percentMatch) {
      return Math.max(1, Math.floor(Math.max(0, Number(maxValue || 0)) * Number(percentMatch[1]) / 100));
    }
    const numericMatch = text.match(/-?\d+(?:\.\d+)?/);
    const numeric = Number(numericMatch ? numericMatch[0] : text);
    return Number.isFinite(numeric) ? Math.max(0, Math.round(numeric)) : 0;
  },

  parseDailySkillCost(costText = '鏃?, stat = {}) {
    const mode = this.splitSkillCostModes(costText);
    const result = { reqSp: 0, reqVit: 0, reqMen: 0, canCast: true, text: mode.upfront || '鏃? };
    if (!mode.upfront || mode.upfront === '鏃?) return result;
    const matches = String(mode.upfront).matchAll(/(榄傚姏|浣撳姏|绮剧鍔?\s*[:锛歖\s*([^|+锛媇+?)(?=\s*(?:榄傚姏|浣撳姏|绮剧鍔?\s*[:锛歖|[|+锛媇|$)/g);
    Array.from(matches).forEach(match => {
      const label = match[1];
      const valueText = String(match[2] || '').trim();
      if (label === '榄傚姏') result.reqSp += this.parseDailyResourceCostUnit(valueText, stat.榄傚姏, stat.榄傚姏涓婇檺);
      if (label === '浣撳姏') result.reqVit += this.parseDailyResourceCostUnit(valueText, stat.浣撳姏, stat.浣撳姏涓婇檺);
      if (label === '绮剧鍔?) result.reqMen += this.parseDailyResourceCostUnit(valueText, stat.绮剧鍔? stat.绮剧鍔涗笂闄?;
    });
    result.canCast =
      toNumber(stat.榄傚姏, 0) >= result.reqSp &&
      toNumber(stat.浣撳姏, 0) >= result.reqVit &&
      toNumber(stat.绮剧鍔? 0) >= result.reqMen;
    return result;
  },

  buildConstructInventoryValue(skill = {}, effect = {}, currentTick = 0) {
    const template = cloneJsonValue(effect?.鑳屽寘妯℃澘 || {}, {});
    const itemName = toText(skill?.榄傛妧鍚?|| effect?.榄傛妧鍚?|| skill?.name, '涓存椂閫犵墿');
    const itemType = toText(effect?.浜х墿绫诲瀷, toText(template?.绫诲瀷, '榄傛妧閫犵墿'));
    const triggerMode = toText(effect?.瑙﹀彂鏂瑰紡, itemType === '椋熺墿' ? '椋熺敤' : '浣跨敤');
    const relativeExpiryTick = Math.max(0, toNumber(effect?.鏈夋晥鏈焧ick, 0));
    const nextItem = {
      ...template,
      鏁伴噺: Math.max(1, toNumber(effect?.鏁伴噺, 1)),
      绫诲瀷: itemType,
      瑙﹀彂鏂瑰紡: triggerMode,
      浣跨敤鏁堟灉: cloneJsonValue(Array.isArray(effect?.浣跨敤鏁堟灉) ? effect.浣跨敤鏁堟灉 : [], []),
      鏉ユ簮鎶€鑳? toText(template?.鏉ユ簮鎶€鑳? itemName) || itemName,
    };
    if (relativeExpiryTick > 0) {
      nextItem.鏈夋晥鏈熻嚦tick = currentTick + relativeExpiryTick;
      nextItem.鏈夋晥鏈熻嚦 = formatTickToCalendarDateText(nextItem.鏈夋晥鏈熻嚦tick);
    } else {
      nextItem.鏈夋晥鏈熻嚦tick = 0;
      nextItem.鏈夋晥鏈熻嚦 = '鏃?;
    }
    if (!toText(nextItem.鎻忚堪, '').trim()) nextItem.鎻忚堪 = toText(skill?.鏁堟灉鎻忚堪, '浣跨敤鍚庤Е鍙戝搴旈瓊鎶€鏁堟灉');
    return { itemName, itemValue: nextItem };
  },

  async performUse(charRef, itemName) {
    try {
      const { charKey } = await this.resolveCharContext(charRef);
      let actionLabel = '浣跨敤';
      let summaryText = '';
      await mutateStatDataByEditor(statData => {
        const charData = deepGet(statData, ['char', charKey], null);
        if (!charData || typeof charData !== 'object') throw new Error('鏈壘鍒扮洰鏍囪鑹蹭俊鎭€?);
        const inventory = charData.鑳屽寘 && typeof charData.鑳屽寘 === 'object' ? charData.鑳屽寘 : null;
        if (!inventory) throw new Error('褰撳墠瑙掕壊娌℃湁鑳屽寘鏁版嵁銆?);
        const itemData = inventory[itemName];
        if (!itemData || typeof itemData !== 'object') throw new Error(`鑳屽寘涓湭鎵惧埌鐗╁搧: ${itemName}`);
        if (!this.isUsableInventoryItem(itemData)) throw new Error('褰撳墠鐗╁搧娌℃湁鍙墽琛岀殑浣跨敤鏁堟灉銆?);
        actionLabel = this.getInventoryUseActionLabel(itemData);
        if (!charData.灞炴€?|| typeof charData.灞炴€?!== 'object') charData.灞炴€?= {};
        if (!charData.灞炴€?鐘舵€佹晥鏋?|| typeof charData.灞炴€?鐘舵€佹晥鏋?!== 'object' || Array.isArray(charData.灞炴€?鐘舵€佹晥鏋?) {
          charData.灞炴€?鐘舵€佹晥鏋?= {};
        }

        const logs = [];
        let appliedCount = 0;
        const 褰撳墠tick = Math.max(0, toNumber(deepGet(statData, 'world.鏃堕棿.tick', 0), 0));
        (Array.isArray(itemData.浣跨敤鏁堟灉) ? itemData.浣跨敤鏁堟灉 : []).forEach((effect, index) => {
          if (!effect || typeof effect !== 'object') return;
          if (!this.isSelfResolvableUsageTarget(effect.target)) return;
          if (effect.type === 'heal') {
            if (this.applyInventoryHealEffect(charData.灞炴€? effect, logs)) appliedCount += 1;
            return;
          }
          if (['buff', 'debuff', 'shield', 'custom'].includes(String(effect.type || ''))) {
            if (this.appendInventoryStatusEffect(charData.灞炴€?鐘舵€佹晥鏋? itemName, effect, index, logs, 褰撳墠tick)) appliedCount += 1;
            return;
          }
        });

        if (appliedCount <= 0) {
          throw new Error('褰撳墠鐗╁搧鐨勪娇鐢ㄦ晥鏋滄棤娉曞湪鑳屽寘鍦烘櫙涓粨绠椼€?);
        }

        const currentQty = Math.max(1, toNumber(itemData.鏁伴噺, 1));
        if (currentQty <= 1) delete inventory[itemName];
        else itemData.鏁伴噺 = currentQty - 1;

        if (!statData.sys || typeof statData.sys !== 'object') statData.sys = {};
        summaryText = logs.join('锛?) || '宸茶Е鍙戠墿鍝佹晥鏋?;
        statData.sys.绯荤粺鎾姤 = `[鐗╁搧${actionLabel}] ${charKey}${actionLabel}浜嗐€?{itemName}銆戙€?{summaryText}`;
      }, { force: true });
      window.MVU_Toast.show(`宸?{actionLabel}銆?{itemName}銆慲, 'info');
      await refreshLiveSnapshot({ force: true });
      const request = buildInventoryUseRequest(liveSnapshot, { charKey, itemName, actionLabel, summaryText });
      if (request) {
        await dispatchUiAiRequest(request.playerInput, request.systemPrompt, { requestKind: request.requestKind });
      }
    } catch (error) {
      window.MVU_Toast.show(error && error.message ? error.message : '浣跨敤鐗╁搧澶辫触銆?, 'error');
    }
  },

  async performConstructSkillUse(skillPath = []) {
    try {
      const safePath = Array.isArray(skillPath) ? skillPath : [];
      if (safePath.length < 2 || safePath[0] !== 'char') throw new Error('褰撳墠鎶€鑳界己灏戣鑹插綊灞炰俊鎭€?);
      const charKey = toText(safePath[1], '').trim();
      if (!charKey) throw new Error('褰撳墠鎶€鑳界己灏戣鑹插綊灞炰俊鎭€?);
      let skillName = '閫犵墿鎶€鑳?;
      let producedItems = [];
      let costText = '鏃?;
      await mutateStatDataByEditor(statData => {
        if (deepGet(statData, 'world.鎴樻枟.杩涜涓?, false)) throw new Error('鎴樻枟杩涜涓紝璇峰湪鎴樻枟妯″潡鍐呮柦灞曡鎶€鑳姐€?);
        const charData = deepGet(statData, ['char', charKey], null);
        if (!charData || typeof charData !== 'object') throw new Error('鏈壘鍒版柦娉曡鑹层€?);
        const skill = deepGet(statData, safePath, null);
        if (!skill || typeof skill !== 'object') throw new Error('鏈壘鍒扮洰鏍囨妧鑳姐€?);
        skillName = toText(skill['榄傛妧鍚?] || skill.name || safePath[safePath.length - 1], '閫犵墿鎶€鑳?);
        const createEffects = this.getConstructCreateEffects(skill);
        if (!createEffects.length) throw new Error('褰撳墠鎶€鑳戒笉鏄彲鏃ュ父鏂藉睍鐨勯€犵墿绫绘妧鑳姐€?);
        if (!charData.灞炴€?|| typeof charData.灞炴€?!== 'object') charData.灞炴€?= {};
        if (!charData.鑳屽寘 || typeof charData.鑳屽寘 !== 'object' || Array.isArray(charData.鑳屽寘)) charData.鑳屽寘 = {};
        const systemBase = this.getSkillSystemBase(skill);
        costText = toText(systemBase['娑堣€?], '鏃?);
        const parsedCost = this.parseDailySkillCost(costText, charData.灞炴€?;
        if (!parsedCost.canCast) {
          throw new Error(`璧勬簮涓嶈冻锛屾柦灞曘€?{skillName}銆戦渶瑕?${parsedCost.reqSp ? `榄傚姏:${parsedCost.reqSp} ` : ''}${parsedCost.reqVit ? `浣撳姏:${parsedCost.reqVit} ` : ''}${parsedCost.reqMen ? `绮剧鍔?${parsedCost.reqMen}` : ''}`.trim());
        }
        charData.灞炴€?榄傚姏 = Math.max(0, toNumber(charData.灞炴€?榄傚姏, 0) - parsedCost.reqSp);
        charData.灞炴€?浣撳姏 = Math.max(0, toNumber(charData.灞炴€?浣撳姏, 0) - parsedCost.reqVit);
        charData.灞炴€?绮剧鍔?= Math.max(0, toNumber(charData.灞炴€?绮剧鍔? 0) - parsedCost.reqMen);

        const currentTick = Math.max(0, toNumber(deepGet(statData, 'world.鏃堕棿.tick', 0), 0));
        producedItems = [];
        createEffects.forEach(effect => {
          const { itemName, itemValue } = this.buildConstructInventoryValue(skill, effect, currentTick);
          const 鏄惁椋熺墿閫犵墿 =
            /椋熺墿/.test(toText(itemValue?.绫诲瀷, '')) ||
            /椋熺敤/.test(toText(itemValue?.瑙﹀彂鏂瑰紡, '')) ||
            /椋熺墿绯?.test(toText(deepGet(skill, '鎶€鑳藉畾浣?绫诲瀷', ''), ''));
          const 浣跨敤鏁堟灉鍒楄〃 = Array.isArray(itemValue?.浣跨敤鏁堟灉) ? itemValue.浣跨敤鏁堟灉 : [];
          const 鍛戒腑淇偧澧炵泭鏁堟灉 = 浣跨敤鏁堟灉鍒楄〃.some(浣跨敤鏁堟灉椤?=> {
            if (!浣跨敤鏁堟灉椤?|| typeof 浣跨敤鏁堟灉椤?!== 'object') return false;
            const 鏁板€间綋 = 浣跨敤鏁堟灉椤?.value && typeof 浣跨敤鏁堟灉椤?value === 'object' ? 浣跨敤鏁堟灉椤?value : {};
            const 鍊嶇巼鍊?= Number(鏁板€间綋?.淇偧閫熷害鍊嶇巼 || 0);
            return Number.isFinite(鍊嶇巼鍊? && 鍊嶇巼鍊?> 1;
          });
          if (鏄惁椋熺墿閫犵墿 && 鍛戒腑淇偧澧炵泭鏁堟灉) {
            itemValue.鍝佺骇 = toText(itemValue.鍝佺骇, 'S');
            itemValue.鍝佽川 = toText(itemValue.鍝佽川, 'S');
          }
          const existing = charData.鑳屽寘[itemName] && typeof charData.鑳屽寘[itemName] === 'object'
            ? charData.鑳屽寘[itemName]
            : null;
          if (existing) {
            existing.鏁伴噺 = Math.max(1, toNumber(existing.鏁伴噺, 1) + toNumber(itemValue.鏁伴噺, 1));
            Object.assign(existing, { ...itemValue, 鏁伴噺: existing.鏁伴噺 });
          } else {
            charData.鑳屽寘[itemName] = cloneJsonValue(itemValue, {});
          }
          producedItems.push(`${itemName}脳${toNumber(itemValue.鏁伴噺, 1)}`);
        });

        if (!statData.sys || typeof statData.sys !== 'object') statData.sys = {};
        const costParts = [
          parsedCost.reqSp ? `榄傚姏:${parsedCost.reqSp}` : '',
          parsedCost.reqVit ? `浣撳姏:${parsedCost.reqVit}` : '',
          parsedCost.reqMen ? `绮剧鍔?${parsedCost.reqMen}` : '',
        ].filter(Boolean).join(' ');
        statData.sys.绯荤粺鎾姤 = `[鏃ュ父鏂藉睍] ${charKey}鏂藉睍銆?{skillName}銆戝苟鐢熸垚 ${producedItems.join('锛?)}銆?{costParts ? ` 鎵ｉ櫎 ${costParts}銆俙 : ''}`;
      }, { force: true });
      window.MVU_Toast.show(`宸叉柦灞曘€?{skillName}銆慲, 'info');
      await refreshLiveSnapshot({ force: true });
      const request = buildConstructSkillUseRequest(liveSnapshot, { charKey, skillName, producedItems, costText });
      if (request) {
        await dispatchUiAiRequest(request.playerInput, request.systemPrompt, { requestKind: request.requestKind });
      }
    } catch (error) {
      window.MVU_Toast.show(error && error.message ? error.message : '鏂藉睍閫犵墿鎶€鑳藉け璐ャ€?, 'error');
    }
  },

  parseEquipSlot(name, data) {
    const tName = name || '';
    // 鍖归厤鏂楅摖閮ㄤ欢
    if (/澶寸洈|澶撮/.test(tName)) return { mainSlot: 'armor', subSlot: '澶寸洈' };
    if (/鑳搁摖|韬共楠?.test(tName)) return { mainSlot: 'armor', subSlot: '鑳搁摖' };
    if (/宸﹁偐/.test(tName)) return { mainSlot: 'armor', subSlot: '宸﹁偐' };
    if (/鍙宠偐/.test(tName)) return { mainSlot: 'armor', subSlot: '鍙宠偐' };
    if (/宸﹁噦/.test(tName)) return { mainSlot: 'armor', subSlot: '宸﹁噦' };
    if (/鍙宠噦/.test(tName)) return { mainSlot: 'armor', subSlot: '鍙宠噦' };
    if (/宸︽墜|鎵嬮/.test(tName)) return { mainSlot: 'armor', subSlot: '宸﹁噦' }; // 瀹归敊鏄犲皠
    if (/鍙虫墜/.test(tName)) return { mainSlot: 'armor', subSlot: '鍙宠噦' }; // 瀹归敊鏄犲皠
    if (/宸﹁吙|宸﹁吙楠?.test(tName)) return { mainSlot: 'armor', subSlot: '宸﹁吙' };
    if (/鍙宠吙|鍙宠吙楠?.test(tName)) return { mainSlot: 'armor', subSlot: '鍙宠吙' };
    if (/鎴樿/.test(tName)) return { mainSlot: 'armor', subSlot: '鎴樿' };
    if (/鎴橀澊/.test(tName)) return { mainSlot: 'armor', subSlot: '鎴橀澊' };
    if (/鎴掓寚/.test(tName)) return { mainSlot: 'armor', subSlot: '鎴掓寚' };
    
    // 鍖归厤榄傞
    if (tName.includes('澶撮儴榄傞') || /澶撮/.test(tName)) return { mainSlot: 'soul_bone', subSlot: '澶撮儴榄傞' };
    if (tName.includes('韬共榄傞') || /韬共楠?.test(tName)) return { mainSlot: 'soul_bone', subSlot: '韬共榄傞' };
    if (tName.includes('宸﹁噦榄傞') || /宸﹁噦楠?.test(tName)) return { mainSlot: 'soul_bone', subSlot: '宸﹁噦榄傞' };
    if (tName.includes('鍙宠噦榄傞') || /鍙宠噦楠?.test(tName)) return { mainSlot: 'soul_bone', subSlot: '鍙宠噦榄傞' };
    if (tName.includes('宸﹁吙榄傞') || /宸﹁吙楠?.test(tName)) return { mainSlot: 'soul_bone', subSlot: '宸﹁吙榄傞' };
    if (tName.includes('鍙宠吙榄傞') || /鍙宠吙楠?.test(tName)) return { mainSlot: 'soul_bone', subSlot: '鍙宠吙榄傞' };
    
    // 鍖归厤鍏朵粬鍝佺被
    if (/鏈虹敳/.test(tName)) return { mainSlot: 'mech', subSlot: null };
    if (/(鍓憒鍒€|鏋獆鐐畖寮搢鐩緗閿鏆楀櫒|鍖曢)$/.test(tName)) return { mainSlot: 'wpn', subSlot: null };
    
    return null;
  },

  getArmorTier(name) {
    if (name.includes('涓€瀛?)) return '涓€瀛楁枟閾?;
    if (name.includes('浜屽瓧')) return '浜屽瓧鏂楅摖';
    if (name.includes('涓夊瓧')) return '涓夊瓧鏂楅摖';
    if (name.includes('鍥涘瓧')) return '鍥涘瓧鏂楅摖';
    return null;
  },

  escapePtr(str) {
    return String(str).replace(/~/g, '~0').replace(/\//g, '~1');
  },

  async submitPatch(patches, successMessage = '瑁呭鎿嶄綔宸插畬鎴?) {
    await applyJsonPatchOpsByEditor(patches);
    window.MVU_Toast.show(successMessage, 'info');
  }
};

    document.addEventListener('click', (event) => {
      const eventTarget = event.target instanceof Element ? event.target : (event.target && event.target.parentElement ? event.target.parentElement : null);
      const actionBtn = eventTarget ? eventTarget.closest('.map-dispatch-action-btn') : null;
      if (!actionBtn) return;

      event.preventDefault();
      event.stopPropagation();

      const detail = {
        action: actionBtn.dataset.action || '',
        target: actionBtn.dataset.target || '',
        currentLoc: actionBtn.dataset.currentLoc || '',
        npcTarget: actionBtn.dataset.npcTarget || '',
        executorType: actionBtn.dataset.executorType || '',
        services: (actionBtn.dataset.services || '').split('|').filter(Boolean)
      };

      window.dispatchEvent(new CustomEvent('map-action-dispatch', { detail }));
    }, true);

    document.addEventListener('keydown', (event) => {
      if (event.key !== 'Escape') return;
      if (isUnifiedInlinePreviewActive() && typeof window.__MVU_CLOSE_UNIFIED_PREVIEW__ === 'function') {
        window.__MVU_CLOSE_UNIFIED_PREVIEW__();
        return;
      }
      closeModal();
    });

    window.addEventListener('resize', () => {
      const refs = getModalRefs();
      if (!refs.detailModal || !refs.detailModal.classList.contains('show')) return;
      applyModalDisplayMode(refs, { displayMode: currentModalDisplayMode });
    });

    if (document.body) {
      const layoutClassObserver = new MutationObserver(() => {
        const refs = getModalRefs();
        if (!refs.detailModal || !refs.detailModal.classList.contains('show')) return;
        applyModalDisplayMode(refs);
      });
      layoutClassObserver.observe(document.body, { attributes: true, attributeFilter: ['class'] });
    }

    const initialModalRefs = getModalRefs();
    if (initialModalRefs.detailModal && initialModalRefs.detailModal.classList.contains('show')) {
      applyModalDisplayMode(initialModalRefs);
    }

    bindAllVueModalDelegations();
    installVueModalDelegationObserver();

    initLiveBindings();
  
window.mvuSetMainTabExternal = setMainTab;
window.mvuSetMainTab = setMainTab;


})();





